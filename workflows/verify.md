# pensmith verify

> Verify citations + claims in one section (DOI, fuzzy-match, quote-check, plagiarism, GPTZero transparency).

<capability_check>
required:
  - Task
  - MCP library.read

degrade_if_missing:
  - if no Task: run sequentially (slower)
  - if no MCP library: direct read of .paper/library.json
</capability_check>

## Overview

(Phase 2 stub — Phase 3+ fills this in.)

## Steps

1. (stub)

## Outputs

- (stub)
