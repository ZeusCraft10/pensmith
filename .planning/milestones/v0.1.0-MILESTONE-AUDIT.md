---
milestone: v0.1.0
audited: 2026-06-22
status: tech_debt
scores:
  requirements: 122/122 satisfied (all traceability rows Complete after audit reconciliation)
  phases: 11/11 verified (Phase 0 = human_needed for external CI confirmation only)
  integration: 9/10 seams WIRED (1 MEDIUM wiring island)
  flows: core pipeline intake→research→outline→[plan→write→verify]→compile→done WIRED end-to-end
gaps:
  integration:
    - id: "CITE-02 / CITE-03 / CITE-04 (export application)"
      severity: "MEDIUM (non-blocking; gate green; .bib/.ris sidecars ship)"
      seam: "citations.ts renderStyle/resolveStyleName → compile.ts → exporter.ts (Pandoc argv)"
      phase: "10 (engine) / 6 (export consumer)"
      evidence: "renderStyle/resolveStyleName have ZERO production callers (only citations.ts + tests). exporter.ts buildPandocArgs passes no --citeproc/--csl/--bibliography. Exported .docx/.pdf carry literal [@key] tokens + no rendered bibliography; the user's chosen citation style is never applied to the deliverable. Deferred in 10-01, planned for 10-04, never built. Confirmed independently by the Phase-10 verifier (M6) AND the milestone integration checker."
      fix: "At export, read the discipline/intake style via resolveStyleName, then add `--citeproc --bibliography <CITATIONS.bib> --csl templates/citation-styles/<style>.csl` to buildPandocArgs (docx/pdf), OR render renderStyle(parseBib(bib), style) and append the reference list before export. Add an exporter test asserting a formatted reference (not just a copied sidecar) appears in the output. One bounded closure phase."
tech_debt:
  - phase: "all phases"
    items:
      - "Security review DEFERRED: workflow.security_enforcement=true but NO per-phase SECURITY.md exists (secure-phase was not part of the requested plan→converge→execute→verify pipeline). Each PLAN.md carries a <threat_model> block, but no gsd:secure-phase audit ran. Run /gsd:secure-phase per phase before public release, OR accept + track."
      - "Nyquist sign-off flag: VALIDATION.md frontmatter nyquist_compliant is false on phases 1,2,4-10 (the per-task verification maps were filled and the tests run GREEN — 856/856 — but the manual sign-off flag was never flipped). Bookkeeping, not a coverage gap."
  - phase: "00-repo-skeleton"
    items:
      - "human_needed: push repo to GitHub + confirm the CI matrix is green on linux-x64/macos-arm64/windows-x64. External manual step — cannot be confirmed locally. CI workflow is configured (.github/workflows/ci.yml, 3-OS matrix)."
  - phase: "01-foundation-nfrs"
    items:
      - "SC-5 PARTIAL (documented override): Retry-After / X-Rate-Limit honored via full-jitter retry rather than the per-source TokenBucket; politeness floor still enforced. Accepted at Phase-1 verification."
  - phase: "08-style-match"
    items:
      - "intake bootstrap boundary: intake.ts is still a Tier-2 placeholder that produces no paper-level STATE.json/paperId, so global-library registration + the style-match producer WARN-skip in the real flow (tests pass via tmpdir pre-seeding). The intake→STATE.json LLM bootstrap is acknowledged earlier-phase/Phase-4+ scope; the Phase-8 features degrade gracefully and ship today. Wire the real intake bootstrap to light up library/style in the live flow."
  - phase: "10-discipline-citation"
    items:
      - "Same as the integration MEDIUM above (renderStyle has no export consumer)."
  - phase: "manual-only (by design)"
    items:
      - "Live Pandoc-produced export + zero-trace on real Pandoc output (Pandoc + PDF engine absent on build machine; in-process scrub is CI-gated against committed fixtures)."
      - "Live pymupdf shellout on a fitz-enabled machine (absent-fallback is CI-gated)."
      - "Live Zotero MCP transport against a real server + ZOTERO_API_KEY (absence-non-breaking + injected-client used-as-source are CI-gated)."
      - "Live GPTZero / DuckDuckGo network behavior (cassette-backed offline in CI)."
      - "Live in-chat natural-language trigger routing (skill-description registry is CI-asserted; live model routing is a model-behavior check)."
      - "Live goal=learning end-to-end tutorial render in a real LLM session."
---

# Milestone v0.1.0 — Audit Report

## Verdict: TECH_DEBT — all 11 phases delivered + verified, full gate GREEN; one MEDIUM export-wiring island + documented deferrals to review before public release.

Pensmith v0.1.0 (Claude Code plugin Tier 1 + portable Node CLI Tier 2 for academic paper writing) is functionally complete across all 11 phases. The full quality gate is GREEN at HEAD: `npm run check` exits 0 — eslint, tsc --noEmit, build, tier-contract (46 cases), **856 tests / 0 fail / 0 skip**, plugin/marketplace/mcp manifests valid. Exactly 16 verbs; 16 workflow bodies bijective with verbs.

No CRITICAL blockers. One MEDIUM cross-phase wiring gap (citation-style/inline-cite application at export) and a set of documented deferrals (security review, manual-only live integrations, the intake bootstrap boundary) need review before a public release, but none gate the v0.1.0 functional milestone.

## Phase Verification Summary

| Phase | Status | Evidence |
|-------|--------|----------|
| 0. Repo skeleton & plugin manifest | human_needed | All 6 local checks pass; sole open item = push-to-GitHub 3-OS CI confirmation (external) |
| 1. Foundation NFRs | passed | 5/5 must-haves (1 PARTIAL override: Retry-After via jitter) |
| 2. Tier shells + doctor + tier-contract gate | pass | tier-contract gate live + green |
| 3. Vertical slice (one section) | PASS-WITH-CAVEATS | 12/12 goal-backward checks (APA-only, Pass 1+3) |
| 4. Breadth — N sections + compile + wave scheduling | passed | 5/5 SCs (this session) |
| 5. Verifier completeness (Pass 2 + Pass 4) | passed | 3/3 SCs (this session) |
| 6. Done / export pipeline + zero-trace gate | passed | 5/5 SCs (this session) |
| 7. Single-command UX + hooks + flags | passed | 5/5 SCs (this session; 7-cycle convergence) |
| 8. Style match + sketch + add + library + BYO PDF | passed | 4/4 SCs (this session) |
| 9. Educator/tutorial mode + PII polish | passed | 3/3 SCs (this session) |
| 10. Discipline + citation-style breadth + Zotero MCP | passed | 3/3 SCs (this session) |

Phases 4–10 were planned → cross-AI-converged (codex+claude+opencode quorum; gemini persistently unavailable via IneligibleTierError) to ZERO HIGH concerns, executed, and goal-backward verified in this autonomous run. Phases 0–3 were completed in prior sessions (their VERIFICATION.md files confirm passed/pass/PASS-WITH-CAVEATS).

## Requirements Coverage (3-source cross-reference)

- **Total v1 requirements:** 122 traceability rows — **all Complete** after this audit.
- **Reconciled during audit:** 30 stale "Pending" rows flipped to Complete — Phase 1 (18: ARCH-05..17, TEST-05/06/07/08/11), Phase 2 (9: ARCH-01/03/18, TIER-01/02/03/06/07, DOCT-06), Phase 3 (2: DOCT-05, RSCH-05a), Phase 8 (1: RSCH-05b). Each is matrix-authorized: its phase's VERIFICATION.md is passed AND the code is green at HEAD; the central traceability table simply lagged. (Phase 1–3 executors/verifiers never synced the table; RSCH-05b was delivered + verified in Phase-8 SC4 but the checkbox wasn't flipped.) No requirement was flipped without an existing passing VERIFICATION.md.
- **Note on CITE-02/03/04:** marked Complete at the library/engine + compile/verify level (renderStyle works + is tested; Phase-3 APA render shipped). The EXPORT-time application of the chosen style + inline-cite resolution is the single integration MEDIUM below — the requirements are satisfied at their phase scope but the end-to-end export consumer is unbuilt.

## Cross-Phase Integration (independent integration-checker audit)

**Verdict: PASS with one confirmed MEDIUM wiring island.** Seams verified WIRED:
- Verifier → compile refuse-gate (Pass 1+3 FABRICATED/MIS-CITED/NOT_FOUND → hard refuse before DRAFT.md; staleness re-runs Pass 1+3 only). The load-bearing verifier-blocks-compile contract HOLDS.
- Verifier → export gate (DONE-09: Pass 2 UNSUPPORTED + Pass 4 orphans + plagiarism → runDoneGate; advisory-only; --yolo sole skip; export never runs on an uncompiled draft).
- Bare /pensmith router (total/never-throws) → dispatchVerb (forwards global flags incl. yolo) → real verb loaders.
- Zero-trace export (mandatory docx/pdf scrub last-step; md/tex trace-free; consumes compiled DRAFT.md + copies CITATIONS.bib + .ris).
- RIS sidecar (research-time CITATIONS.ris → exporter risCopied) — end-to-end.
- Source providers (7 adapters + zotero-mcp normalized + tri-state doctor probe, absence-non-breaking).
- Observers (style-match STYLE.json → write voiceHint; tutorial goal.ts → TUTORIAL.md) hook seams without Foundation pollution.
- Two-tier parity (16-verb single source; tier-contract enforced).

### MEDIUM-1 — Chosen citation style + inline `[@key]` cites are never applied to the exported document
See the `gaps.integration` block in the frontmatter for full evidence + fix. In short: `renderStyle`/`resolveStyleName` have no production consumer and `exporter.ts buildPandocArgs` enables no citeproc, so exported `.docx/.pdf` show literal `[@key]` tokens and no rendered bibliography. Non-blocking (the gate is green and `.bib/.ris` sidecars ship for reference managers; the **core value still holds — every citekey is a REAL, verifier-confirmed source**, it's just not Pandoc-formatted into the deliverable). One bounded closure phase wires citeproc into the exporter.

## Nyquist Coverage

| Status | Phases |
|--------|--------|
| COMPLIANT (flag true) | 0, 3 |
| PARTIAL (VALIDATION.md exists, nyquist_compliant:false — per-task maps filled, tests GREEN, sign-off flag not flipped) | 1, 2, 4, 5, 6, 7, 8, 9, 10 |
| MISSING | none |

Discovery only. The PARTIAL phases have green test coverage (856/856); the `nyquist_compliant: true` sign-off flag was simply never set. Run `/gsd:validate-phase <N>` if a formal Nyquist sign-off is wanted per phase.

## Recommended Closure (before public release; none gate the functional milestone)

1. **Wire citeproc into the exporter** (MEDIUM-1) — one closure phase; makes exported docs carry formatted citations + bibliography in the user's chosen style. Highest user-facing value.
2. **Security review** — run `/gsd:secure-phase` per phase (or accept + track) since security_enforcement=true but no SECURITY.md exists.
3. **Phase 0 CI confirmation** — push to GitHub, confirm the 3-OS matrix is green.
4. **Flip Nyquist sign-off flags** (or run `/gsd:validate-phase`) for the 9 PARTIAL phases.
5. **Intake bootstrap** — wire the real intake→STATE.json/paperId path so library/style light up in the live flow (currently graceful-WARN-skip).
