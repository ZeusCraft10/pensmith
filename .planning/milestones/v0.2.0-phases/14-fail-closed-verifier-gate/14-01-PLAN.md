---
phase: 14-fail-closed-verifier-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/verdict-rows.test.ts
  - tests/compile-refuse.test.ts
  - tests/gate-retraction.test.ts
  - tests/done-recheck.test.ts
  - tests/fixtures/cassettes/retraction-watch/gate03-blocking-doi.json
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03, GATE-04]
must_haves:
  truths:
    - "All four gate test files exist and run as RED-by-skip (skip, not fail) until their target modules/exports ship"
    - "The full suite stays GREEN (0 failures) after Wave 0 lands"
    - "The GATE-03 blocking cassette exists with a distinct fixture DOI"
  artifacts:
    - path: "tests/verdict-rows.test.ts"
      provides: "GATE-02 round-trip + mutation + freshness-row scaffold"
      contains: "renderPass1VerdictRow"
    - path: "tests/compile-refuse.test.ts"
      provides: "GATE-01 absent/empty/no-Status refuse cases (extended)"
      contains: "no verifiable VERIFICATION.md"
    - path: "tests/gate-retraction.test.ts"
      provides: "GATE-03 live-retraction-blocks + transport-skip scaffold"
      contains: "MIS-CITED"
    - path: "tests/done-recheck.test.ts"
      provides: "GATE-04 citekey-diff + Pass-3 re-check scaffold"
      contains: "reCheckFinalMd"
    - path: "tests/fixtures/cassettes/retraction-watch/gate03-blocking-doi.json"
      provides: "Offline cassette for the GATE-03 live-retracted DOI"
      contains: "10.0000/gate03-retracted"
  key_links:
    - from: "tests/*.test.ts"
      to: "the not-yet-shipped exports"
      via: "RED-by-skip guard (existsSync / typeof feature-detect)"
      pattern: "skip|existsSync|typeof"
---

<objective>
Wave-0 RED-by-skip scaffolds for all four Phase-14 gates. Create the four gate test files (one new GATE-02 round-trip test, one new GATE-03 cassette test, one new GATE-04 re-check test, and an extension to the existing GATE-01 compile-refuse test) plus the GATE-03 blocking cassette — every behavioral assertion is guarded so it SKIPS (never fails) until Wave-1 ships the target modules/exports.

Purpose: lock the four gate contracts as executable tests BEFORE the implementations exist, so Wave-1 implementations turn skips into passes with no test re-authoring. This follows the locked project convention (05-01 / 06-01 / 08-00 / 10-00): Wave-0 gates are RED-by-skip so the full suite stays GREEN with zero failures throughout.
Output: 4 test files + 1 cassette fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-fail-closed-verifier-gate/14-RESEARCH.md
@.planning/phases/14-fail-closed-verifier-gate/14-PATTERNS.md
@.planning/phases/14-fail-closed-verifier-gate/14-VALIDATION.md

<read_first>
- tests/compile-refuse.test.ts — existing GATE-01 baseline (COMP-01); extend it, do not rewrite. Header comment + node:test/node:assert imports + tmpdir paper-seed pattern (lines ~23-109).
- tests/freshness-probe.test.ts — the definitive analog for the GATE-03 cassette-backed test (DOI constants at top, one test per behavior, no tmpdir, cassette owns the network layer; transport-error-silent at lines ~60-67).
- tests/known-bad-citations.test.ts — runPass1Unit call shape (lines ~58-76) for the GATE-03 verdict assertion; also the regression that the stored claimed.retracted fast-path still blocks.
- tests/fixtures/cassettes/retraction-watch/fetchById-fake.json — exact cassette shape to copy for gate03-blocking-doi.json (scope/method/path/status/response.items[].doi+retractedDate+reason).
</read_first>

<interfaces>
<!-- Contracts the Wave-1 plans will implement. Tests reference these names; until they exist the assertions skip. -->

GATE-02 — bin/lib/verify/verdict-rows.ts (Plan 02 creates):
  renderPass1VerdictRow(citekey, verdict, titleJW, authorJW, reason): string
  renderPass3VerdictRow(citekey, quoteSnippet, verdict, levRatio, reason): string
  parseVerdictRows(verificationMd): string[]   // failing citekeys only (FABRICATED/MIS-CITED/NOT_FOUND)

GATE-01 — bin/lib/compile.ts runCompile (Plan 02 modifies):
  refuse reason on absent/empty/no-Status VERIFICATION.md = "section N (slug): no verifiable VERIFICATION.md ..."

GATE-03 — bin/lib/verify/pass1.ts (Plan 03 modifies verdictForCitekey, called via runPass1Unit):
  a live-retracted resolved DOI → verdict 'MIS-CITED'; fetchById null (transport error / no hit) → NOT a false MIS-CITED

GATE-04 — bin/cli/done.ts (Plan 04 adds + exports):
  reCheckFinalMd(finalMd, draftMd, bibPath): Promise<{ passed: boolean; reason: string }>
  citekey-set mismatch OR Pass-3 NOT_FOUND → { passed: false }; equal sets + clean quotes → { passed: true }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GATE-02 verdict-rows round-trip scaffold + GATE-04 done-recheck scaffold</name>
  <files>tests/verdict-rows.test.ts, tests/done-recheck.test.ts</files>
  <read_first>tests/compile-refuse.test.ts (imports + RED-by-skip precedent), 14-RESEARCH.md §GATE-02 round-trip shape (lines 340-356) and §GATE-04 helper (lines 706-740), 14-PATTERNS.md verdict-rows + done.ts analog sections.</read_first>
  <action>
    Create tests/verdict-rows.test.ts as a pure string-in/out test (no tmpdir, no fs). RED-by-skip: dynamically import bin/lib/verify/verdict-rows.js via a runtime URL .href specifier and feature-detect typeof renderPass1VerdictRow/renderPass3VerdictRow/parseVerdictRows === 'function' (the 08-00 / 10-00 RED-by-skip pattern); if absent, call test's skip — do NOT assertion-fail. Cover three behaviors: (1) round-trip — render a mixed set [FABRICATED smith2020, MIS-CITED jones2019, OK vaswani2017 via renderPass1VerdictRow + a NOT_FOUND smith2020 via renderPass3VerdictRow], join with '\n', parse via parseVerdictRows, assert deepEqual(new Set(failing), new Set(['smith2020','jones2019'])) — Set comparison NOT array deepEqual (Pitfall 5); (2) format-drift mutation — replace '**FABRICATED**' with '**FABRICATD**' in a rendered row and assert that row yields NO citekey from parseVerdictRows (drift detected); (3) freshness-table immunity (Pitfall 2) — feed parseVerdictRows a markdown body containing a '| smith2020 | retraction-watch | WARN | ... |' table row and assert it returns [] (the '^\s*-\s*' list anchor excludes table rows).

    Create tests/done-recheck.test.ts for the GATE-04 reCheckFinalMd contract. RED-by-skip: feature-detect the reCheckFinalMd export from bin/cli/done.js; skip if absent. Use the compile-refuse tmpdir paper-seed pattern (mkdtempSync 'pensmith-gate04-' + mkdirSync .paper) for tests needing a real CITATIONS.bib path. Cover: (1) citekey-set match + no quotes → { passed: true }; (2) added citekey in finalMd not in draftMd → { passed: false } with reason naming the added key; (3) dropped citekey → { passed: false }; (4) swapped citekey → { passed: false }; (5) absent bib file (no CITATIONS.bib written) → { passed: true } (skip-clean, no quotes to check). Use PENSMITH_NO_LLM-safe offline construction; quote NOT_FOUND coverage is exercised via the integration path in Plan 04 — here assert the citekey-diff branch deterministically with inline strings.
  </action>
  <verify>
    <automated>node --import tsx --test tests/verdict-rows.test.ts tests/done-recheck.test.ts</automated>
  </verify>
  <done>Both files run; all behavioral assertions SKIP (target exports absent); zero failures. No tsc error (dynamic .href import keeps noEmit clean).</done>
</task>

<task type="auto">
  <name>Task 2: GATE-01 compile-refuse extension + GATE-03 retraction scaffold + cassette</name>
  <files>tests/compile-refuse.test.ts, tests/gate-retraction.test.ts, tests/fixtures/cassettes/retraction-watch/gate03-blocking-doi.json</files>
  <read_first>tests/compile-refuse.test.ts (existing COMP-01 cases + tmpdir seed), tests/freshness-probe.test.ts (cassette test structure + transport-silent test lines ~60-67), tests/known-bad-citations.test.ts (runPass1Unit call shape lines ~58-76), tests/fixtures/cassettes/retraction-watch/fetchById-fake.json (cassette template), 14-RESEARCH.md §GATE-03 (lines 360-458) and §GATE-01 (lines 188-255), 14-PATTERNS.md cassette section (lines 348-376).</read_first>
  <action>
    Extend tests/compile-refuse.test.ts (do NOT rewrite — append GATE-01 cases) covering: (1) section with NO VERIFICATION.md → runCompile refuses, refuseReasons matches /no verifiable VERIFICATION.md/ naming the section; (2) empty/whitespace-only VERIFICATION.md → same refuse; (3) VERIFICATION.md with body but no parseable 'Status:' line → same refuse; (4) valid VERIFICATION.md with 'Status: verified' and an OK verdict row → compiles (no GATE-01 refuse reason); (5) regression — 'Status: unverifiable' section (the bib-missing legitimate path, Pitfall 3) must NOT be refused by GATE-01. These run against the real runCompile — they are NOT RED-by-skip; instead, make the new assertions tolerant: guard the GATE-01-specific assertions behind a check that the refuse message contains 'no verifiable' (skip those specific assertions if the running compile.ts has not yet shipped GATE-01) so the suite stays green pre-implementation while becoming load-bearing post-implementation. Keep the existing COMP-01 cases unchanged and green throughout.

    Create tests/gate-retraction.test.ts modeled on freshness-probe.test.ts: header comment naming GATE-03; DOI constants at top (RETRACTED_DOI = '10.0000/gate03-retracted', the new cassette DOI; NO_CASSETTE_DOI = a DOI with no cassette entry). RED-by-skip via a source-grep predicate (read bin/lib/verify/pass1.ts source and skip until it imports retraction-watch fetchById — existsSync alone cannot detect the not-yet-wired behavior; this mirrors the 07-01 source-grep skip predicate). Cover: (1) a bib entry whose resolved DOI is live-retracted (cassette hit) → runPass1Unit yields verdict 'MIS-CITED' with a reason naming the live re-query; (2) NO_CASSETTE_DOI (fetchById returns null = transport/no-hit) → verdict is NOT 'MIS-CITED' from the retraction path (it follows the normal JW outcome); reference known-bad-citations.test.ts for the runPass1Unit + bib fixture call shape.

    Create tests/fixtures/cassettes/retraction-watch/gate03-blocking-doi.json by copying fetchById-fake.json verbatim and changing the DOI to 10.0000/gate03-retracted in BOTH the path (.../data/retractions?filter=record:10.0000/gate03-retracted) and response.items[0].doi, and updating title + reason to distinguish it from the existing cassette.
  </action>
  <verify>
    <automated>node --import tsx --test tests/compile-refuse.test.ts tests/gate-retraction.test.ts</automated>
  </verify>
  <done>compile-refuse existing COMP-01 cases pass; new GATE-01 assertions skip-or-pass (skip pre-implementation); gate-retraction behavioral assertions SKIP (pass1 not yet wired); cassette parses as valid JSON; zero failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → not-yet-shipped module | Wave-0 tests reference exports that do not exist yet; a hard import would crash the run |
| cassette JSON → test runner | a malformed cassette would break offline dispatch for the whole retraction-watch suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-SCAFFOLD | Tampering | Wave-0 test that hard-fails before impl | mitigate | RED-by-skip (typeof feature-detect / source-grep predicate / .href dynamic import) so the suite stays GREEN; matches locked 05-01/08-00/10-00 convention |
| T-14-01-CASSETTE | Tampering | gate03-blocking-doi.json shape drift | mitigate | copy fetchById-fake.json verbatim; change only DOI/title/reason; assert it parses as JSON in the gate-retraction test setup |
| T-14-01-FALSEGREEN | Repudiation | a scaffold that silently passes vacuously | accept | mutation + freshness-row immunity cases in verdict-rows make the GATE-02 scaffold non-vacuous; Wave-1 turns skips into real passes (audited at plan close) |
</threat_model>

<verification>
- `node --import tsx --test tests/verdict-rows.test.ts tests/done-recheck.test.ts tests/compile-refuse.test.ts tests/gate-retraction.test.ts` → zero failures, behavioral assertions skip.
- `npm test` → full suite GREEN (0 failures); skip count increases by the new behavioral cases.
- Wave-0 path resolution uses fileURLToPath / .href specifiers (spaced-path safe — OneDrive dev folder, Phase-11 lesson).
</verification>

<success_criteria>
- All four gate test files exist (one extended, three new) + the GATE-03 cassette.
- Every behavioral assertion is RED-by-skip (skip, not fail) until Wave-1 ships its target.
- Full suite GREEN with zero failures.
- Set-comparison (not array deepEqual) used in the round-trip; freshness-row immunity + mutation cases present (GATE-02 non-vacuous).
</success_criteria>

<output>
Create `.planning/phases/14-fail-closed-verifier-gate/14-01-SUMMARY.md` when done
</output>
