---
phase: 16-ci-dx-parity-docs-packaging
plan: 03
type: execute
wave: 3
depends_on: [16-02]
files_modified:
  - README.md
  - bin/cli/intake.ts
  - workflows/new.md
  - tests/repo-files.test.ts
autonomous: true
requirements: [DOCS-01]
must_haves:
  truths:
    - "README ships real install (both tiers) + the /pensmith single-command quickstart + the 16-verb power-user reference"
    - "README contains the PRD §18 GSD credit and the PRD §3 dual-use disclaimer verbatim"
    - "A CLI-only user who never reads the README sees the §3 disclaimer printed at intake"
    - "The stale `v0.1.0 in development` / `Phase 6` README assertions are removed in the same commit as the rewrite"
  artifacts:
    - path: "README.md"
      provides: "real user-facing README with §3 disclaimer + §18 credit + /pensmith quickstart + Style Match section"
      contains: "Get Shit Done"
    - path: "bin/cli/intake.ts"
      provides: "PRD §3 disclaimer printed at intake start before any prompt"
      contains: "not a guarantee against AI detectors"
    - path: "workflows/new.md"
      provides: "documents the §3 disclaimer print as Body step 1"
    - path: "tests/repo-files.test.ts"
      provides: "real-README assertions; stale stub assertions removed"
  key_links:
    - from: "README.md disclaimer"
      to: "PRD §3"
      via: "verbatim copy (the ONLY disclosure mechanism)"
      pattern: "not a guarantee against AI detectors"
    - from: "bin/cli/intake.ts run()"
      to: "process.stdout"
      via: "disclaimer print before first ask()"
      pattern: "process\\.stdout\\.write"
---

<objective>
Ship the real user-facing README (DOCS-01) and surface the PRD §3 dual-use
disclaimer at intake so a CLI-only user always sees it. Replace the 19-line stub
README with the non-negotiable structure: what-it-is, two-tier install,
`/pensmith`-only quickstart, 16-verb power-user reference, PRD §18 GSD credit
(verbatim), the preserved `## Style Match` section, and the PRD §3 disclaimer
(verbatim). Remove the stale `v0.1.0 in development` / `Phase 6` assertions in
the SAME commit. This plan owns README.md, intake.ts, new.md, and (in this wave)
repo-files.test.ts.

Purpose: The README disclaimer is the ONLY disclosure mechanism (CLAUDE.md
non-negotiable); single-command UX means `/pensmith` is the only command in the
quickstart. The intake print ensures the disclosure is unmissable.
Output: rewritten README.md, edited intake.ts + new.md, updated repo-files.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/16-ci-dx-parity-docs-packaging/16-RESEARCH.md
@.planning/phases/16-ci-dx-parity-docs-packaging/16-PATTERNS.md

@README.md
@workflows/new.md
@tests/repo-files.test.ts

<interfaces>
<!-- The two verbatim copy blocks (sourced from PRD §3 and §18 — reproduced in 16-RESEARCH.md and 16-PATTERNS.md). -->
<!-- DOCS-01 requires these be byte-faithful to the PRD; do not paraphrase. -->

PRD §3 disclaimer (verbatim — see 16-RESEARCH.md "DOCS-01" / 16-PATTERNS.md "README.md"):
  Two paragraphs. Opens "pensmith is a structured research-and-drafting assistant for academic writing."
  Closes "... You are responsible for the work you submit."
  MUST contain the honest-framing sentence "It is not a guarantee against AI detectors ...".

PRD §18 GSD credit (verbatim):
  Opens "pensmith is heavily inspired by [Get Shit Done](https://github.com/gsd-build/get-shit-done) ..."

<!-- README §Style Match (lines 9-19 of current README) MUST be preserved verbatim — STYL-04 test (repo-files.test.ts:112) enforces present + honest framing. -->

<!-- intake.ts run() insertion point: immediately after `const cwd = process.cwd();` (line 326), -->
<!-- BEFORE persistProjectConfig / the PII block / any ask(). Static copy, no await, no try/catch. -->
<!-- Use process.stdout.write (never console.log) — repo convention. -->

<!-- repo-files.test.ts: the README test is `'README, PRIVACY, README-DEV, CONTRIBUTING stubs are correct'` (line 90). -->
<!-- Lines 91-92 are the stale asserts to DELETE: /v0\.1\.0 in development/ and /Phase 6/. -->
<!-- The PRIVACY (93-94), README-DEV (95-96), CONTRIBUTING (97-99) asserts in that test STAY. -->
<!-- STYL-04 test (112-130) is SEPARATE and already enforces ## Style Match — do not duplicate it. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md (DOCS-01) + remove stale repo-files assertions atomically</name>
  <files>README.md, tests/repo-files.test.ts</files>
  <read_first>
    - README.md (current 19-line stub — preserve the `## Style Match` section lines 9-19 byte-for-byte).
    - 16-RESEARCH.md "DOCS-01: Real README Structure" (the PRD §3 + §18 verbatim blocks + the non-negotiable section order) and Pitfall 1 (delete lines 91-92).
    - 16-PATTERNS.md "README.md — DOCS-01" (structure + verbatim copy) and "tests/repo-files.test.ts — DOCS-01 assertions" (what to remove + add).
    - CLAUDE.md non-negotiables (single-command UX: /pensmith only in quickstart; §3 disclaimer is the ONLY disclosure; honest framing — never "undetectable"; GSD credit per §18).
    - tests/repo-files.test.ts lines 90-100 + 112-130.
  </read_first>
  <action>
Replace README.md with the real README, in this non-negotiable section order
(per RESEARCH + CLAUDE.md):
  1. Title + one-paragraph what-it-is.
  2. Install — TWO tiers: Claude Code plugin (the `/pensmith` slash command) and
     portable CLI (`npm install -g pensmith` / `npx pensmith`).
  3. Quick start — `/pensmith` and ONLY `/pensmith` (single-command UX is a
     non-negotiable; do NOT list other verbs in the quickstart).
  4. 16-verb power-user reference — a brief table of the locked 16 verbs (source
     the names from bin/lib/verbs.ts UX02_VERBS; this is a REFERENCE, separate
     from the quickstart, so it does not violate the single-command rule).
  5. GSD credit — PRD §18 VERBATIM (the block in RESEARCH/PATTERNS).
  6. `## Style Match` — PRESERVE the existing section (lines 9-19) byte-for-byte
     (STYL-04 test enforces it present + honest framing).
  7. Disclaimer — PRD §3 VERBATIM (both paragraphs incl. the
     "not a guarantee against AI detectors" sentence). This is the ONLY
     disclosure mechanism — ship it exactly.
Keep honest framing throughout: never "undetectable" / "evade detection" /
"impersonate" (STYL-04 negative asserts). README.md is NOT hash-pinned (substring
asserts only) so no SHA pin is needed.

In the SAME commit, edit tests/repo-files.test.ts:
  - DELETE the two stale assertions at lines 91-92 (`/v0\.1\.0 in development/`
    and `/Phase 6/`).
  - ADD real-content asserts to the same test: README matches
    `/pensmith is a structured research/i` (§3 opener), matches
    `/not a guarantee against AI detectors/i` (§3 honest framing), and matches
    `/Get Shit Done/` (§18 credit). Optionally rename the test to drop "stubs"
    per PATTERNS. Keep the PRIVACY / README-DEV / CONTRIBUTING asserts unchanged.
  - Do NOT touch the STYL-04 test (it already enforces `## Style Match`).
  - Do NOT touch the doctor-output.md hash-pin (Plan 04 owns it).
  </action>
  <verify>
    <automated>node --import tsx --test tests/repo-files.test.ts</automated>
  </verify>
  <acceptance_criteria>README contains the §3 opener, the §3 honest-framing sentence, the §18 GSD credit, the preserved `## Style Match` section, and a `/pensmith` quickstart; repo-files.test.ts no longer asserts the stale stub strings and now asserts the real content; STYL-04 negative asserts (no undetectable/evade/impersonate) pass.</acceptance_criteria>
  <done>README rewritten in the locked order; stale asserts removed + real asserts added in the same change; `node --import tsx --test tests/repo-files.test.ts` GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Print PRD §3 disclaimer at intake (DOCS-01) + document in workflows/new.md</name>
  <files>bin/cli/intake.ts, workflows/new.md</files>
  <read_first>
    - bin/cli/intake.ts run() (lines 325-380) — the `const cwd = process.cwd();` anchor (326) and the existing `process.stdout.write` calls.
    - 16-RESEARCH.md "Intake disclaimer placement" + Code Examples "Where to print the §3 disclaimer in intake.ts".
    - 16-PATTERNS.md "bin/cli/intake.ts — DOCS-01 intake disclaimer" (the array-join print pattern + placement constraint) and "workflows/new.md — DOCS-01 Body update".
    - workflows/new.md `## Body` (lines 42-69) — insert the disclaimer as Body step 1.
  </read_first>
  <action>
In bin/cli/intake.ts run(), immediately after `const cwd = process.cwd();`
(line 326) and BEFORE persistProjectConfig / the PII block / any ask(), print
the PRD §3 disclaimer verbatim to stdout. Build a static `DISCLAIMER` constant
(array of lines joined with `\n`, the two §3 paragraphs verbatim incl. the
"not a guarantee against AI detectors" sentence) and write it via
`process.stdout.write(DISCLAIMER + '\n\n')`. Static copy — no await, no try/catch,
no model call. The text must be byte-faithful to PRD §3 (it is the ONLY disclosure
mechanism for CLI-only users). Use process.stdout.write (never console.log) per
repo convention.

Then in workflows/new.md `## Body`, insert a new step 1 documenting the print:
"Print §3 disclaimer (DOCS-01): at the top of the run, before any prompts or
model calls, print the PRD §3 dual-use disclaimer verbatim to stdout so CLI-only
users who never read the README still see the disclosure. Static copy — no user
input needed." Renumber the subsequent Body steps. Do NOT change the
`<capability_check>` block (ARCH-03 / W4 bijection must stay intact — new.md still
has `required: AskUserQuestion` + degrade list).
  </action>
  <verify>
    <automated>node -e "const s=require('node:fs').readFileSync('bin/cli/intake.ts','utf8'); if(!/not a guarantee against AI detectors/.test(s)) throw new Error('intake §3 missing'); const w=require('node:fs').readFileSync('workflows/new.md','utf8'); if(!/disclaimer/i.test(w)) throw new Error('new.md disclaimer step missing'); if(!/<capability_check>/.test(w)) throw new Error('new.md capability_check regressed')"</automated>
    <automated>npx tsc --noEmit && node --import tsx --test tests/workflows-keyequal.test.ts</automated>
  </verify>
  <acceptance_criteria>intake.ts prints the §3 disclaimer (incl. the honest-framing sentence) before any prompt; new.md documents it as a Body step and keeps its capability_check; tsc clean; workflows-keyequal (ARCH-01/ARCH-03 bijection) still GREEN.</acceptance_criteria>
  <done>Disclaimer printed at intake start; new.md Body documents it; capability_check preserved; tsc + workflows-keyequal pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pensmith → user (disclosure) | the PRD §3 disclaimer is the ONLY disclosure mechanism; it must actually ship in README AND surface at intake, byte-faithful and honest |
| README copy → STYL-04 honest-framing gate | the README must never claim undetectability/evasion/impersonation (even in negation outside the locked sentence) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-DISC | Repudiation / disclosure-integrity | the §3 disclaimer as the sole disclosure | mitigate | Verbatim §3 in README (repo-files.test.ts asserts the opener + honest-framing sentence) AND printed at intake (intake.ts assert greps "not a guarantee against AI detectors"). The disclosure cannot silently drop or be paraphrased away — both surfaces are CI-asserted |
| T-16-HONEST | Spoofing / dishonest framing | README marketing copy | mitigate | STYL-04 negative assertions (no `undetectable` / `evade detection` / `impersonate`) run against the new README; the `## Style Match` honest section is preserved byte-for-byte |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (docs + one stdout print). No `[ASSUMED]`/`[SUS]` packages; no install checkpoint required |
</threat_model>

<verification>
- README contains: §3 opener, §3 honest-framing sentence, §18 GSD credit, the preserved `## Style Match` section, a `/pensmith`-only quickstart, and the 16-verb reference table.
- `node --import tsx --test tests/repo-files.test.ts` GREEN (stale asserts removed; real asserts added; STYL-04 passes).
- intake.ts prints the §3 disclaimer before any prompt; `npx tsc --noEmit` clean.
- `node --import tsx --test tests/workflows-keyequal.test.ts` GREEN (new.md capability_check + bijection intact).
- No edits to package.json, ci.yml, http-mock.ts, or the doctor-output.md pin (owned by Plans 02/04).
</verification>

<success_criteria>
- README rewritten in the locked order with both verbatim PRD blocks + preserved Style Match.
- Stale stub assertions removed and real-content assertions added in the same change.
- §3 disclaimer printed at intake; documented in new.md Body; capability_check preserved.
- repo-files + workflows-keyequal green; tsc clean.
</success_criteria>

<output>
Create `.planning/phases/16-ci-dx-parity-docs-packaging/16-03-SUMMARY.md` when done.
</output>
