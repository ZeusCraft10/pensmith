---
phase: 16-ci-dx-parity-docs-packaging
plan: 04
type: execute
wave: 4
depends_on: [16-01, 16-03]
files_modified:
  - workflows/doctor.md
  - workflows/status.md
  - workflows/next.md
  - workflows/resume.md
  - bin/lib/doctor/probes/http-crossref-ping.ts
  - references/doctor-output.md
  - PRIVACY.md
  - tests/repo-files.test.ts
autonomous: true
requirements: [DOCS-02]
must_haves:
  truths:
    - "The four stub workflow bodies (doctor/status/next/resume) are filled with real ## Body content + accurate capability_check"
    - "Stale `Phase 3+ / deferred to Phase 3` copy in the http-crossref-ping probe + doctor-output.md is refreshed to shipped reality"
    - "Stale `ships with v0.1.0` copy in PRIVACY.md is replaced with the real shipped privacy content"
    - "The doctor-output.md SHA-256 pin is recomputed in the same commit as the probe-copy refresh"
    - "The 16-verb/16-body bijection + tier-contract parity are not regressed"
  artifacts:
    - path: "workflows/doctor.md"
      provides: "filled body: 11 probes, renderTty/renderJson, exit-1-on-FAIL"
      min_lines: 30
    - path: "workflows/status.md"
      provides: "filled body: loadState + per-section readSectionState + resolveNextAction"
      min_lines: 30
    - path: "workflows/next.md"
      provides: "filled body: resolveNextAction state machine + dispatchVerb (bare /pensmith)"
      min_lines: 30
    - path: "workflows/resume.md"
      provides: "filled body: HANDOFF summary read → resolveNextAction → dispatchVerb → clear HANDOFF (H4)"
      min_lines: 30
    - path: "references/doctor-output.md"
      provides: "refreshed http-crossref-ping copy (shipped reality)"
    - path: "tests/repo-files.test.ts"
      provides: "recomputed doctor-output.md SHA-256 pin"
      contains: "PINNED"
  key_links:
    - from: "bin/lib/doctor/probes/http-crossref-ping.ts summary"
      to: "references/doctor-output.md http-crossref-ping section"
      via: "doctor render reads the locked copy"
      pattern: "http-crossref-ping"
    - from: "references/doctor-output.md bytes"
      to: "tests/repo-files.test.ts PINNED constant"
      via: "SHA-256 hash-pin (WN-3)"
      pattern: "PINNED"
---

<objective>
Fill the four stub workflow bodies (doctor/status/next/resume) with real
`## Body` content matching compile.md's shape, refresh the stale "Phase 3
deferred" copy in the http-crossref-ping probe + doctor-output.md (re-pinning
the SHA-256 in the SAME commit per WN-3), and replace the stale "ships with
v0.1.0" sentence in PRIVACY.md with real shipped privacy content. This plan owns
the four workflow bodies, the http-crossref-ping probe, doctor-output.md,
PRIVACY.md, and (in this wave) the doctor-output.md hash-pin in
repo-files.test.ts.

Purpose: DOCS-02 fills bodies (adds NO verb — the 16-verb bijection is unchanged)
and refreshes copy that has drifted from shipped reality. The doctor-output.md
re-pin is WN-3-mandatory: any edit to the locked file must update its pin in the
same commit.
Output: four filled workflow bodies, refreshed probe + doctor-output.md +
PRIVACY.md, recomputed pin in repo-files.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/16-ci-dx-parity-docs-packaging/16-RESEARCH.md
@.planning/phases/16-ci-dx-parity-docs-packaging/16-PATTERNS.md

# Shape model for all four bodies
@workflows/compile.md
@workflows/doctor.md
@workflows/status.md
@workflows/next.md
@workflows/resume.md

# Probe + locked-copy + privacy targets
@bin/lib/doctor/probes/http-crossref-ping.ts
@references/doctor-output.md
@PRIVACY.md
@tests/repo-files.test.ts

# The non-stub guard authored in Plan 01 — this plan must turn it GREEN
@tests/workflow-bodies.test.ts

<interfaces>
<!-- Real impls the bodies must describe (from RESEARCH/PATTERNS, VERIFIED file reads): -->
  doctor → runDoctor() (bin/lib/doctor/probes.ts) → renderTty()/renderJson(); exit 1 on any FAIL. 11 probes.
  status → loadState() (bin/lib/state.ts) → walk sections via readSectionState() (bin/lib/router.ts, C6-HIGH guarded) → resolveNextAction(); stdout-only.
  next  → resolveNextAction() (bin/lib/router.ts; never throws; HANDOFF-blind) → dispatchVerb(); bare /pensmith flow; state machine new→research→outline→(plan→write→verify)*→compile→done.
  resume → safeReadHandoff() SUMMARY only (H4: never routes from HANDOFF) → resolveNextAction() → dispatchVerb() → clear HANDOFF.json (best-effort rmSync).

<!-- Workflow body shape (compile.md): title + `> one-liner`; <capability_check> (required: / degrade_if_missing:); -->
<!-- ## Overview; ## Outputs; ## Body (numbered, ends with "Shell fallback (TIER-06): pensmith <verb> ..."). -->
<!-- ARCH-03/W4 closed required-token vocabulary: {Task, AskUserQuestion, MCP <name>, Pandoc, Zotero MCP, humanizer skill, (none required)} -->
<!--   doctor → required: (none required). status/next/resume → required: MCP state.read (already in the stubs). -->

<!-- WN-3 re-pin: references/doctor-output.md is byte-pinned at tests/repo-files.test.ts:179 -->
<!--   PINNED = '509f90add8664e559a3ab817684381777e1b624b63ebe0dfc77054267997eec0' (current). -->
<!--   After editing doctor-output.md, recompute and replace PINNED in the SAME commit. -->
<!-- PRIVACY.md: NOT pinned (substring asserts /local-only/ + /No telemetry/ at repo-files.test.ts:93-94 must remain). -->
<!-- http-crossref-ping.ts stale strings at lines 31-33; doctor-output.md matching section at lines 57-59. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill the four workflow bodies (doctor/status/next/resume) (DOCS-02)</name>
  <files>workflows/doctor.md, workflows/status.md, workflows/next.md, workflows/resume.md</files>
  <read_first>
    - workflows/compile.md (the canonical filled-body shape: capability_check → Overview → Outputs → Body → "Shell fallback (TIER-06)").
    - workflows/{doctor,status,next,resume}.md (the current 23-line stubs — note doctor uses `## Steps` and "(Phase 2 stub — Phase 3+ fills this in.)").
    - 16-PATTERNS.md sections "workflows/doctor.md", "workflows/status.md", "workflows/next.md", "workflows/resume.md" (each has the exact filled body to produce, sourced from the real impls).
    - 16-RESEARCH.md "DOCS-02: Workflow Body Shape + Content" + Pitfall 5 (preserve <capability_check>; validate-plugin-manifest checks every .md has it).
    - tests/workflow-bodies.test.ts (Plan 01) + tests/workflows-keyequal.test.ts (ARCH-03 closed vocabulary) — the contracts to satisfy.
  </read_first>
  <action>
Replace each of the four stub bodies with a real body matching compile.md's
shape (per the 16-PATTERNS.md per-file content). For each: keep/restore a
`<capability_check>` block (required: + degrade_if_missing:), then `## Overview`,
`## Outputs`, `## Body` (numbered steps ending with "Shell fallback (TIER-06):
pensmith <verb> ..."). Replace the stub's `## Steps` heading with `## Body`.
Remove the stub sentinels ("Phase 2 stub", "Phase 3+").
  - doctor.md: required `(none required)`; Body describes runDoctor() running the
    11 probes (grouped by DOCT category per PATTERNS), renderTty vs renderJson
    (--json), exit 1 on any FAIL. Note the doctor render reads
    references/doctor-output.md (locked) and the probe is READ-ONLY.
  - status.md: required `MCP state.read` + degrade to direct STATE.json read;
    Body describes loadState() (StateNotFoundError → "no active paper"),
    per-section walk via readSectionState() (C6-HIGH guarded, never raw
    parseFrontmatter), resolveNextAction() "next:" line; stdout-only, exit 0.
  - next.md: required `MCP state.read` + degrade; Body describes
    resolveNextAction() (never throws; HANDOFF-blind), the verb→action mapping,
    dispatchVerb() forwarding global flags; the bare /pensmith flow + the state
    machine.
  - resume.md: required `MCP state.read` + degrade; Body describes safeReadHandoff()
    SUMMARY-only (H4 — never routes from HANDOFF), resolveNextAction(),
    dispatchVerb(), then clear HANDOFF.json (best-effort rmSync in finally). State
    the H4 invariant (no resume→resume loop).
Keep all required: tokens inside the ARCH-03/W4 closed vocabulary. Do NOT add or
rename any verb — the 16-body bijection and tier-contract parity must stay intact.
  </action>
  <verify>
    <automated>node --import tsx --test tests/workflow-bodies.test.ts tests/workflows-keyequal.test.ts && node scripts/validate-plugin-manifest.cjs</automated>
  </verify>
  <acceptance_criteria>All four bodies have Overview/Outputs/Body + "Shell fallback (TIER-06)" + a capability_check, with no stub sentinels; workflow-bodies.test.ts GREEN (guard opened); workflows-keyequal (bijection + W4 vocabulary) GREEN; validate-plugin-manifest GREEN.</acceptance_criteria>
  <done>Four stubs replaced with real bodies; non-stub guard + bijection + manifest validator all pass; no 17th verb introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh stale copy — http-crossref-ping probe + doctor-output.md + WN-3 re-pin (DOCS-02)</name>
  <files>bin/lib/doctor/probes/http-crossref-ping.ts, references/doctor-output.md, tests/repo-files.test.ts</files>
  <read_first>
    - bin/lib/doctor/probes/http-crossref-ping.ts (full — stale strings at lines 31-33: "deferred to Phase 3 ... not yet shipped" / "No action required in Phase 2. Phase 3 will land bin/lib/http-mock.ts").
    - references/doctor-output.md http-crossref-ping section (lines 57-59) — the doctor render reads this; its summary must match the probe's new summary verbatim.
    - 16-PATTERNS.md "bin/lib/doctor/probes/http-crossref-ping.ts" (replacement copy) + "references/doctor-output.md — DOCS-02 WN-3 re-pin" (the re-pin procedure).
    - 16-RESEARCH.md Pitfall 2 (re-pin command) + "Stale copy in doctor probes".
    - tests/repo-files.test.ts:174-180 (the doctor-output.md hash-pin test; PINNED at line 179).
  </read_first>
  <action>
Refresh the stale "Phase 3 deferred / not yet shipped" copy in
http-crossref-ping.ts to shipped reality (http-mock.ts landed in Phase 3): the
probe summary/fix should describe the active Crossref cassette-wiring probe (PASS
in CI, SKIP outside the repo where cassettes aren't shipped; FAIL hint points at
tests/fixtures/cassettes/crossref/), per the PATTERNS replacement copy. Do NOT
change the probe's stable id or its position in the probe list (02-07 tier-fact
extraction relies on presence). If the probe's runtime severity/logic is
SKIP-only today, keep the behavior conservative — prefer a copy-only refresh that
matches what the probe actually returns, so doctor-output.md stays a faithful
mirror (do not invent a PASS/FAIL the probe does not produce).

Update references/doctor-output.md's http-crossref-ping section (lines 57-59) so
its summary text matches the probe's new summary VERBATIM (the doctor render
reads this file). Then WN-3 re-pin IN THE SAME COMMIT:
  - Recompute the SHA-256:
    `node -e "console.log(require('node:crypto').createHash('sha256').update(require('node:fs').readFileSync('references/doctor-output.md')).digest('hex'))"`
  - Replace the PINNED constant at tests/repo-files.test.ts:179 with the new hash.
Do NOT touch any other pinned file (honesty-framing.md, http-warnings.md, prompt
templates) or the README/PRIVACY asserts (README owned by Plan 03; PRIVACY in
Task 3 of this plan).
  </action>
  <verify>
    <automated>node --import tsx --test tests/repo-files.test.ts && npx tsc --noEmit && node --import tsx --test tests/doctor.test.ts</automated>
  </verify>
  <acceptance_criteria>http-crossref-ping probe + doctor-output.md no longer say "deferred to Phase 3 / not yet shipped"; doctor-output.md section summary matches the probe summary verbatim; the doctor-output.md hash-pin test passes with the recomputed PINNED; tsc + doctor tests GREEN.</acceptance_criteria>
  <done>Stale probe + locked-copy refreshed; SHA-256 re-pinned in the same change; doctor render parity preserved; hash-pin test + doctor tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh stale PRIVACY.md copy (DOCS-02)</name>
  <files>PRIVACY.md</files>
  <read_first>
    - PRIVACY.md (full — 5 lines; preserve "Pensmith is local-only. No telemetry, no cloud state, no remote logging." which feeds the substring asserts; the stale sentence is the final "ships with v0.1.0" line).
    - 16-PATTERNS.md "PRIVACY.md — DOCS-02 stale sentence replace" + 16-RESEARCH.md "Stale copy in PRIVACY.md".
    - tests/repo-files.test.ts:93-94 — the /local-only/ + /No telemetry/ substring asserts that MUST keep matching.
  </read_first>
  <action>
Replace the stale final sentence in PRIVACY.md ("The full privacy document ...
ships with v0.1.0.") with the REAL shipped privacy content covering all external
data flows that actually ship: external API calls (OpenAlex, Crossref, arXiv,
PubMed, Unpaywall, GPTZero, DuckDuckGo), the PENSMITH_CONTACT_EMAIL polite-pool
requirement, PII redaction at intake, the GPTZero consent gate (HARD-05, shipped
Phase 15), and humanizer/honesty-score data flows. Write it as shipped reality
(no "ships in Phase X" placeholders). PRESERVE the first two lines verbatim
("Pensmith is local-only. No telemetry, no cloud state, no remote logging.") so
the /local-only/ and /No telemetry/ substring asserts keep matching. PRIVACY.md
is NOT hash-pinned — no SHA pin needed.
  </action>
  <verify>
    <automated>node -e "const p=require('node:fs').readFileSync('PRIVACY.md','utf8'); if(!/local-only/i.test(p)) throw new Error('local-only lost'); if(!/No telemetry/i.test(p)) throw new Error('No telemetry lost'); if(/ships with v0\.1\.0/.test(p)) throw new Error('stale ships-with-v0.1.0 sentence still present')"</automated>
    <automated>node --import tsx --test tests/repo-files.test.ts</automated>
  </verify>
  <acceptance_criteria>PRIVACY.md keeps "local-only" + "No telemetry"; the stale "ships with v0.1.0" sentence is gone; replaced with real shipped data-flow content; repo-files PRIVACY asserts pass.</acceptance_criteria>
  <done>Stale PRIVACY sentence replaced with shipped reality; substring asserts intact; repo-files.test.ts GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| locked-copy doc → SHA-256 pin | doctor-output.md is byte-pinned; an edit without re-pin silently breaks the integrity gate or blocks CI — WN-3 requires the pin update in the same commit |
| workflow body fill → 16-verb bijection | filling bodies must add NO verb; the bijection + tier-contract parity are load-bearing invariants |
| doc copy → shipped reality | stale "Phase 3 / v0.1.0" copy misrepresents the shipped product to users/auditors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-PIN | Tampering / integrity | references/doctor-output.md hash-pin | mitigate | The edit + the recomputed SHA-256 land in the same commit (WN-3); repo-files.test.ts:179 fails if the bytes drift from PINNED — the integrity gate stays meaningful (the doctor render reads the same file) |
| T-16-BIJ | Tampering | 16-verb/16-body bijection | mitigate | workflow-bodies.test.ts + workflows-keyequal.test.ts (ARCH-01/03 + W4 vocabulary) + validate-plugin-manifest.cjs all run in Task 1 — no verb can be added/renamed without a red test |
| T-16-DOC | Information disclosure | PRIVACY.md accuracy | mitigate | The refreshed PRIVACY.md documents the real external data flows incl. the GPTZero consent gate; substring asserts keep the local-only/no-telemetry guarantees verifiable |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (docs + one probe-copy edit). No `[ASSUMED]`/`[SUS]` packages; no install checkpoint required |
</threat_model>

<verification>
- Four bodies filled (Overview/Outputs/Body + Shell fallback + capability_check); no stub sentinels.
- `node --import tsx --test tests/workflow-bodies.test.ts tests/workflows-keyequal.test.ts` GREEN; `node scripts/validate-plugin-manifest.cjs` GREEN.
- http-crossref-ping probe + doctor-output.md refreshed; doctor-output.md SHA-256 re-pinned; `node --import tsx --test tests/repo-files.test.ts` GREEN; `node --import tsx --test tests/doctor.test.ts` GREEN.
- PRIVACY.md keeps local-only + No telemetry; "ships with v0.1.0" gone.
- `npx tsc --noEmit` clean.
- No edits to package.json, ci.yml, http-mock.ts, README.md, intake.ts, or new.md (owned by Plans 02/03).
</verification>

<success_criteria>
- doctor/status/next/resume bodies filled, bijection + tier-contract + manifest validator intact.
- http-crossref-ping + doctor-output.md refreshed with WN-3 re-pin in the same commit.
- PRIVACY.md refreshed to shipped reality with substring asserts preserved.
- Full suite green after this plan.
</success_criteria>

<output>
Create `.planning/phases/16-ci-dx-parity-docs-packaging/16-04-SUMMARY.md` when done.
</output>
