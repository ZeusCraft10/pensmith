---
phase: 12-live-research-intake-bootstrap-humanizer-task
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - bin/lib/intake-parse.ts
  - bin/lib/research-orchestrator.ts
  - bin/cli/research.ts
autonomous: true
requirements: [GEN-03]

must_haves:
  truths:
    - "pensmith research queries the searchable adapters and returns >=1 real deduplicated candidate (offline cassettes), then runs the retraction cross-check"
    - "The zero-candidate placeholder library is gone: a genuinely-empty discovery writes a real empty LIBRARY.json (entries:[]), never a tier2-placeholder marker"
    - "topic/discipline/assignment are wired from the real INTAKE.md (not the Phase-11 placeholder strings)"
    - "The locked chokepoint order survives: crossCheckRetractions BEFORE writeBibtex BEFORE writeRis BEFORE LIBRARY.json write (D-15/D-19/D-20)"
    - "The research approval gate is default-ON (scope select + candidate prune); --yolo skips; non-TTY raises ApprovalUnavailableError / exit-3"
  artifacts:
    - path: "bin/lib/intake-parse.ts"
      provides: "parseIntakeMd(text) → { topic, discipline, assignment } with heuristic fallback (full INTAKE.md as assignment, discipline default 'other')"
      exports: ["parseIntakeMd"]
    - path: "bin/lib/research-orchestrator.ts"
      provides: "runResearchDiscovery() — adapter fan-out, DOI+title dedup, source-evaluator filtering; injectable adapter seam"
      exports: ["runResearchDiscovery"]
      min_lines: 60
    - path: "bin/cli/research.ts"
      provides: "swap-seam (lines 139-197) replaced with live discovery wiring; INTAKE.md vars; approval gates; preserved locked ordering"
      contains: "runResearchDiscovery"
  key_links:
    - from: "bin/cli/research.ts"
      to: "bin/lib/research-orchestrator.ts"
      via: "runResearchDiscovery import + call"
      pattern: "runResearchDiscovery"
    - from: "bin/lib/research-orchestrator.ts"
      to: "bin/lib/sources/index.ts"
      via: "Object.entries(sources).filter('search' in adapter)"
      pattern: "'search' in"
    - from: "bin/cli/research.ts"
      to: "crossCheckRetractions → writeBibtex"
      via: "preserved LOCKED ordering (D-15)"
      pattern: "crossCheckRetractions"
---

<objective>
Replace the Phase-11 swap-seam in research.ts (lines 139-197) with real live-adapter discovery (GEN-03). Extract a `bin/lib/research-orchestrator.ts` helper that fans out the searchable adapters, aggregates and dedups candidates, and tiers them via the source-evaluator LLM step; wire real INTAKE.md vars into the topic-disambiguator call; add the default-ON approval gates; and preserve the locked crossCheckRetractions→writeBibtex→writeRis→LIBRARY.json chokepoint sequence untouched.

Purpose: Turn `pensmith research` from a placeholder into a real source-discovery pipeline — the single largest unlock in the phase. The infrastructure (7 adapters, dedup primitives, transport, retraction cross-check, writers) already exists; this plan connects them with real data flow.
Output: bin/lib/intake-parse.ts (new), bin/lib/research-orchestrator.ts (new), bin/cli/research.ts (swap-seam replaced).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-live-research-intake-bootstrap-humanizer-task/12-RESEARCH.md
@.planning/phases/12-live-research-intake-bootstrap-humanizer-task/12-PATTERNS.md

<read_first>
- bin/cli/research.ts — the FULL current file; the swap-seam block is lines 139-197; the GEN-06 probe (95-115), the topic-disambiguator complete() call (122-137), and the locked ordering (199-215) all stay. The topic/discipline/assignment vars are placeholders at lines 124-128 — replace with INTAKE.md content.
- bin/lib/sources/index.ts — the `sources` registry + AdapterName; the documented `'search' in adapter` guard that excludes retraction-watch and includes zotero-mcp (degrades to []).
- bin/lib/sources/retraction-cross-check.ts — crossCheckRetractions() signature + the injectable RetractionLookup seam shape to mirror for the orchestrator's adapter seam.
- bin/lib/sources/zotero-mcp.ts — the module-level null-seam + graceful-degradation pattern (setZoteroClientForTest) to mirror for __setAdapterRegistryForTest.
- bin/lib/doi.ts — normalizeDoi() for DOI dedup keys.
- bin/lib/fuzzy.ts — jaroWinkler() + TITLE_JW_THRESHOLD for no-DOI title dedup.
- bin/lib/schemas/source-candidate.ts — SourceCandidateSchema for per-element safeParse (T-11-10 trust boundary).
- bin/lib/prompts.ts + bin/lib/prompts/schema.ts — ask() supports kind:'select' and kind:'multiselect' (confirmed) with non-TTY numbered fallback; use ask() for both gates.
- bin/cli/outline.ts — the approval-gate precedent: --yolo skip + non-TTY ApprovalUnavailableError/exit-3 (copy the gate shape).
- templates/prompts/intake-clarifier.md — INTAKE.md is the clarifier output (numbered clarifying questions, NOT structured ## Topic/## Discipline headings) → parseIntakeMd must use the heuristic fallback path.
- templates/prompts/topic-disambiguator.md + templates/prompts/source-evaluator.md — input vars + output shapes ({scopes:[{label,queries[]}]} and [{citekey,keep,reason}]).
</read_first>

<interfaces>
<!-- Contracts the executor builds against. Extracted from the codebase + RESEARCH. -->

Adapter search signature (crossref/openalex/arxiv/pubmed/semanticscholar/zotero-mcp):
- search(query: string, opts?: { limit?: number }): Promise<SourceCandidate[]>
- Excluded from fan-out: retraction-watch (no search export; D-15), unpaywall (search() returns [] by design; use fetchById() post-discovery only).

Locked prompt slugs (already hash-pinned in EXPECTED_PROMPT_HASHES — DO NOT edit the prompt files):
- topic-disambiguator: vars {{topic}},{{discipline}},{{assignment}} → { scopes: [{ label, queries[] }] }
- source-evaluator: vars {{candidateSources}},{{topic}},{{scope}},{{discipline}} → [{ citekey, keep, reason }]

Transport:
- complete({ system, messages:[{role:'user',content}], scope:'task', scopeId:'research' }): Promise<CompleteResult{ text }>
- PENSMITH_NO_LLM=1 → deterministic mock text (NOT valid JSON) → defensive parse MUST fall back.

Dedup:
- normalizeDoi(doi: string): string (canonical key)
- jaroWinkler(a,b): number; TITLE_JW_THRESHOLD = 0.92

Approval gate (ask):
- PromptQuestion kind:'select' (scope), kind:'multiselect' (candidate prune); non-TTY → numbered/ApprovalUnavailableError → exit-3 (outline precedent).

Locked ordering (research.ts owns it — orchestrator returns candidates BEFORE this):
- crossCheckRetractions(candidates) → writeBibtex(candidates,bibPath) → writeRis(candidates,risPath) → atomicWriteFile(libraryPath, JSON{$schemaVersion:1,entries:candidates})

INTAKE.md location: paperDir()/INTAKE.md (read with readFileSync; if absent, WARN + treat as empty assignment).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parseIntakeMd + research-orchestrator (fan-out, dedup, evaluate)</name>
  <read_first>
    - templates/prompts/intake-clarifier.md (INTAKE.md is free-form clarifier output, not structured headings)
    - bin/lib/sources/index.ts (the 'search' in adapter guard)
    - bin/lib/sources/retraction-cross-check.ts + bin/lib/sources/zotero-mcp.ts (injectable seam shapes)
    - bin/lib/doi.ts + bin/lib/fuzzy.ts (dedup primitives)
    - bin/lib/schemas/source-candidate.ts (SourceCandidateSchema)
    - tests/research-discovery.test.ts (the Wave-0 contract this satisfies)
  </read_first>
  <files>bin/lib/intake-parse.ts, bin/lib/research-orchestrator.ts</files>
  <behavior>
    - parseIntakeMd: structured INTAKE.md (if a ## Topic / ## Discipline ever appears) → extract those; otherwise heuristic — topic = first non-empty line/heading, discipline = 'other', assignment = full INTAKE.md text. Never throws.
    - runResearchDiscovery(scope queries) under PENSMITH_NO_LLM + offline cassettes → returns >=1 SourceCandidate from the searchable adapters.
    - Two candidates sharing a normalized DOI → one survivor (first-wins, prefer the record with an abstract).
    - No-DOI candidates with jaroWinkler(title) >= TITLE_JW_THRESHOLD → deduped.
    - source-evaluator parse failure (PENSMITH_NO_LLM mock is not JSON) → keep ALL candidates with a WARN (defensive fallback, T-11-10).
    - Per-adapter search() failure is swallowed with a WARN (non-fatal); total empty across all adapters → returns [] (research.ts writes the real empty LIBRARY.json).
    - retraction-watch is NEVER in the search fan-out ('search' in adapter guard); unpaywall.search() ([]) is excluded by name (fetchById enrichment is optional/out-of-scope here).
  </behavior>
  <action>Create bin/lib/intake-parse.ts exporting parseIntakeMd(text: string): { topic: string; discipline: string; assignment: string } with the heuristic fallback (INTAKE.md is clarifier output per intake-clarifier.md, so the structured-heading branch will rarely fire — implement it defensively but default to full-text-as-assignment, discipline 'other'). Create bin/lib/research-orchestrator.ts exporting runResearchDiscovery: it builds the searchable adapter set via Object.entries(sources).filter(([name,a]) => 'search' in a) and EXPLICITLY excludes unpaywall by name (its search returns [] by design) while keeping zotero-mcp (degrades to []); for each query in the chosen scope it calls adapter.search(query, { limit: 10 }) (cap at 10 not 20 — Pitfall 6 budget), swallows per-adapter errors with a stderr WARN, aggregates into SourceCandidate[], runs the DOI+title dedup (normalizeDoi map first-wins, then jaroWinkler>=TITLE_JW_THRESHOLD for no-DOI), then calls complete() with the source-evaluator prompt (interpolate {{candidateSources}} as JSON, {{topic}},{{scope}},{{discipline}}), defensively JSON.parses the response into a Set of keep:true citekeys, filters deduped by that set, and on any source-evaluator parse failure keeps ALL deduped candidates with a WARN. Validate each candidate through SourceCandidateSchema.safeParse before returning (T-11-10). Provide an injectable adapter-registry seam mirroring zotero-mcp.ts (a module-level let + __setAdapterRegistryForTest, OR accept the registry as an optional function parameter — prefer the function-parameter DI for cleaner offline tests). Route ALL adapter network through the existing adapter modules (which already use http.ts cassettes); add NO new fetch surface. Do NOT call crossCheckRetractions or writeBibtex here — research.ts owns the locked chokepoint sequence. Wire the Wave-0 tests/research-discovery.test.ts skip guard to GREEN by making discoverySeamWired() true.</action>
  <verify>
    <automated>node --import tsx --test tests/research-discovery.test.ts</automated>
  </verify>
  <done>parseIntakeMd + runResearchDiscovery exist; tests/research-discovery.test.ts un-skips and passes the fan-out, DOI-dedup, and source-evaluator-fallback cases offline; retraction-watch/unpaywall are excluded from the fan-out; no new fetch surface added.</done>
</task>

<task type="auto">
  <name>Task 2: wire research.ts swap-seam to live discovery + INTAKE.md vars + approval gates</name>
  <read_first>
    - bin/cli/research.ts (the swap-seam lines 139-197 to replace; locked ordering 199-215 to preserve; probe + topic-disambiguator call to keep)
    - bin/lib/intake-parse.ts (from Task 1)
    - bin/lib/research-orchestrator.ts (from Task 1)
    - bin/cli/outline.ts (approval-gate precedent: --yolo skip + non-TTY ApprovalUnavailableError/exit-3)
    - bin/lib/prompts.ts (ask() select/multiselect + non-TTY behavior)
  </read_first>
  <files>bin/cli/research.ts</files>
  <action>Replace the ENTIRE swap-seam block (lines 139-197, between the topic-disambiguator complete() call and the crossCheckRetractions call) with live discovery wiring. Steps: (1) read paperDir()/INTAKE.md via readFileSync (WARN + empty string if absent) and call parseIntakeMd to get {topic,discipline,assignment}; feed these into the EXISTING topic-disambiguator interpolate call at lines 124-128 (replace the placeholder strings). (2) Defensively parse the topic-disambiguator response into { scopes:[{label,queries[]}] } with a Zod safeParse; on parse failure, WARN to stderr and fall back to a single scope { label:'auto', queries:[topic] }. (3) Scope approval gate (default-ON): when scopes.length>1 and not --yolo, ask() a kind:'select' over scope labels; --yolo auto-selects scopes[0]; non-TTY follows outline's ApprovalUnavailableError → exit-3 pattern. (4) Call runResearchDiscovery with the chosen scope's queries → deduped+evaluated candidates. (5) Candidate approval gate (default-ON): when not --yolo and candidates.length>0, ask() a kind:'multiselect' to prune; --yolo keeps all; non-TTY exit-3. (6) Pass the final candidates into the UNCHANGED locked chokepoint sequence (crossCheckRetractions BEFORE writeBibtex BEFORE writeRis BEFORE LIBRARY.json). Zero-candidate path: skip the candidate gate, WARN, and let the existing writers emit a real empty LIBRARY.json (entries:[]) — assert NO placeholder marker. Remove the obsolete `void loadPrompt` / scope-fence comments that reference Phase-11; keep the two loadPrompt() hash-pin calls at run() start. Keep complete()'s offline seam intact (do not add PENSMITH_NO_LLM checks in research.ts — complete() owns it).</action>
  <verify>
    <automated>node --import tsx --test tests/research-discovery.test.ts && PENSMITH_NO_LLM=1 node --import tsx bin/pensmith.ts research --yolo</automated>
  </verify>
  <done>research.ts swap-seam replaced; topic-disambiguator vars come from INTAKE.md; both approval gates present (default-ON, --yolo skip, non-TTY exit-3); locked D-15/D-19/D-20 ordering preserved verbatim; tests/research-discovery.test.ts fully GREEN; --yolo offline run writes a real LIBRARY.json with no placeholder marker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → orchestrator | topic-disambiguator + source-evaluator responses are UNTRUSTED (may carry prompt-injection via source titles/abstracts); cross into Zod parsing |
| adapter network → orchestrator | source abstracts/titles fetched from external APIs flow into the source-evaluator transport call |
| user TTY → research | scope/candidate selections cross the approval gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | hostile LLM JSON (topic-disambiguator/source-evaluator) | mitigate | Defensive JSON.parse → Zod safeParse; fallback single-scope / keep-all; SourceCandidateSchema.safeParse per element (T-11-10) |
| T-12-02 | Tampering | prompt-injection via source abstract/title into source-evaluator transport | mitigate | candidateSources serialized as JSON (structured), validated per element; HARD-04 fenced-delimiter wrapping is Phase-15 scope — flag, do not implement here |
| T-12-03 | Denial of Service | candidate-count budget blow-up at source-evaluator | mitigate | per-query limit cap 10 (not 20); dedup BEFORE source-evaluator; assertBudget fires inside complete() |
| T-12-04 | Denial of Service | retraction-watch.search() TypeError crashing fan-out | mitigate | 'search' in adapter guard excludes retraction-watch (D-15); ESLint chokepoint backstops |
| T-12-05 | Information Disclosure | new fetch surface bypassing http.ts | mitigate | NO new fetch added — all adapter network stays in existing adapter modules routed through http.ts; SSRF (HARD-02) is Phase-15 but no new surface introduced here |
| T-12-06 | Information Disclosure | API key / PENSMITH_S2_API_KEY leak via transport call sites | mitigate | complete() owns the no-leak header path (T-11-12); orchestrator never reads/logs key values |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `node --import tsx --test tests/research-discovery.test.ts` — all GEN-03 cases GREEN.
- `PENSMITH_NO_LLM=1 node --import tsx bin/pensmith.ts research --yolo` (in a tmp paper dir) — writes a real LIBRARY.json; grep the output for absence of `tier2-placeholder`/`PLACEHOLDER_LIBRARY`/`_note`.
- `npm run test:tier-contract` — research tier parity stays GREEN.
- Grep research.ts confirming crossCheckRetractions appears BEFORE writeBibtex (D-15 ordering intact).
- `npm test` — full suite GREEN.
</verification>

<success_criteria>
- pensmith research returns >=1 real deduped candidate under offline cassettes + PENSMITH_NO_LLM; zero-candidate placeholder library is gone.
- topic/discipline/assignment wired from real INTAKE.md.
- Approval gates default-ON; --yolo skips; non-TTY exit-3.
- Locked crossCheckRetractions→writeBibtex→writeRis→LIBRARY.json ordering preserved.
- No new fetch surface; all adapter network via http.ts.
</success_criteria>

<output>
Create `.planning/phases/12-live-research-intake-bootstrap-humanizer-task/12-02-SUMMARY.md` when done.
</output>
