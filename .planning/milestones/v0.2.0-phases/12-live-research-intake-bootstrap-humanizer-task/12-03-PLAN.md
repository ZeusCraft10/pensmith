---
phase: 12-live-research-intake-bootstrap-humanizer-task
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - bin/cli/intake.ts
autonomous: true
requirements: [GEN-04]

must_haves:
  truths:
    - "pensmith intake writes .paper/STATE.json with a non-null paperId conforming to the v2 StateSchema"
    - "Re-running intake on an existing paper is idempotent: the paperId is NOT regenerated (StateAlreadyExistsError is caught and skipped)"
    - "With STATE.json present, global-library registration and the style-match producer proceed instead of WARN-skipping"
  artifacts:
    - path: "bin/cli/intake.ts"
      provides: "initState(paperDir(cwd)) call before the INTAKE.md write + runSideEffects, with idempotent StateAlreadyExistsError catch"
      contains: "initState"
  key_links:
    - from: "bin/cli/intake.ts initState()"
      to: "bin/lib/state.ts"
      via: "initState import + call before runSideEffects"
      pattern: "initState\\("
    - from: "bin/cli/intake.ts resolvePaperId()"
      to: "the freshly written STATE.json"
      via: "loadState finds the file → paperId non-null → registerPaperNonFatal proceeds"
      pattern: "resolvePaperId"
---

<objective>
Bootstrap the paper-level STATE.json at intake (GEN-04). Add an `initState(paperDir(cwd))` call to `bin/cli/intake.ts` before the INTAKE.md write and `runSideEffects`, with an idempotent catch of `StateAlreadyExistsError`, so `resolvePaperId()` returns a non-null paperId and the two WARN-skip guards (global-library registration + style-match producer) flip from skipping to active in the real flow.

Purpose: This is the stateful foundation the rest of the paper-level flow depends on. Today intake never writes STATE.json, so resolvePaperId returns null and registration/style-match silently WARN-skip. One correctly-placed call fixes the whole downstream chain.
Output: bin/cli/intake.ts (initState wiring).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-live-research-intake-bootstrap-humanizer-task/12-RESEARCH.md
@.planning/phases/12-live-research-intake-bootstrap-humanizer-task/12-PATTERNS.md

<read_first>
- bin/cli/intake.ts — the FULL file. Key sites: loadState already imported (line 48); resolvePaperId (167-174); registerPaperNonFatal WARN-skip guard (195-200); runStyleProducerNonFatal (231-270); the run() body where complete() returns result.text and atomicWriteFile(targetPath, result.text) + runSideEffects() are called (after line 272).
- bin/lib/state.ts — initState(paperRoot, seed?) (line 146): throws StateAlreadyExistsError (code 'STATE_ALREADY_EXISTS') if STATE.json exists; uses withLock atomically; validates StateSchema.parse before write; paperId = randomUUID() when seed omitted.
- bin/lib/schemas/state.ts — the v2 StateSchema: { $schemaVersion: 2, paperId: string(min 1), createdAt: ISO-8601, sections?: [{n,slug}] }.
- tests/intake-bootstrap.test.ts — the Wave-0 contract this plan satisfies (STATE.json write, idempotency, WARN-skip flip).
- tests/state.test.ts — initState idempotency / StateAlreadyExistsError precedent (do not duplicate; reuse semantics).
</read_first>

<interfaces>
<!-- Contracts the executor wires against. Extracted from the codebase + RESEARCH. -->

initState(paperRoot: string, seed?: { paperId?: string }): Promise<State>
  - paperRoot = paperDir(cwd)  (the .paper directory)
  - throws StateAlreadyExistsError { code: 'STATE_ALREADY_EXISTS' } if STATE.json present
  - seed omitted → paperId = randomUUID() (node:crypto, already imported in state.ts) — DO NOT use a content-hash (breaks idempotency: re-running intake on the same text must not re-register a new id)

StateAlreadyExistsError — importable from bin/lib/state.ts alongside initState.

Placement (RESEARCH §"Where to call initState() in intake.ts"):
  AFTER complete() returns result.text, BEFORE atomicWriteFile(targetPath, result.text) and runSideEffects():
    try { await initState(paperDir(cwd)); }
    catch (e) { if ((e as {code?:string}).code !== 'STATE_ALREADY_EXISTS') throw e; /* else idempotent skip */ }

Downstream effect (no code change needed in these — they already guard on paperId):
  resolvePaperId(cwd) → loadState(cwd).paperId → now non-null
  registerPaperNonFatal: the `if (!paperId) { WARN; return; }` guard no longer fires → registration proceeds
  runStyleProducerNonFatal: paperId non-null → uses real id (not the unregistered: synthetic)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: add idempotent initState() bootstrap to intake.ts</name>
  <read_first>
    - bin/cli/intake.ts (the run() body: complete() → atomicWriteFile(targetPath,...) → runSideEffects(); the import block; resolvePaperId/registerPaperNonFatal guards)
    - bin/lib/state.ts (initState + StateAlreadyExistsError signatures)
    - tests/intake-bootstrap.test.ts (the Wave-0 contract)
  </read_first>
  <files>bin/cli/intake.ts</files>
  <behavior>
    - First intake run on a fresh paper: writes .paper/STATE.json with $schemaVersion 2 + a randomUUID paperId + ISO-8601 createdAt; resolvePaperId then returns that id.
    - Second intake run (STATE.json already present): initState throws StateAlreadyExistsError; the catch swallows it (code === 'STATE_ALREADY_EXISTS'); paperId is unchanged (not regenerated); run() does not crash.
    - Any other initState error (not STATE_ALREADY_EXISTS) re-throws (fail-loud — bootstrap failure is real).
    - After the bootstrap, registerPaperNonFatal proceeds (no 'skipping global-library registration' WARN); runStyleProducerNonFatal (if --style-samples) uses the real paperId.
  </behavior>
  <action>Add `import { initState, StateAlreadyExistsError } from '../lib/state.js';` to intake.ts (loadState is already imported at line 48 — extend that import or add a sibling). In run(), after complete() returns result.text and BEFORE `await atomicWriteFile(targetPath, result.text)` and `await runSideEffects()`, insert the idempotent bootstrap: `try { await initState(paperDir(cwd)); } catch (e) { if ((e as { code?: string }).code !== 'STATE_ALREADY_EXISTS') throw e; }` — on STATE_ALREADY_EXISTS, skip silently (idempotent; the existing paperId is correct). Use seed-less initState so paperId = randomUUID() (do NOT pass a content-hash). Do NOT add new fields to STATE.json (no topic/discipline — STATE.json stays {$schemaVersion,paperId,createdAt,sections?}; the assignment context lives only in INTAKE.md). Do NOT modify resolvePaperId / registerPaperNonFatal / runStyleProducerNonFatal — they already guard on paperId and flip to active once STATE.json exists. Wire the Wave-0 tests/intake-bootstrap.test.ts skip guard to GREEN by making intakeBootstrapWired() (source-grep for `initState(`) true.</action>
  <verify>
    <automated>node --import tsx --test tests/intake-bootstrap.test.ts</automated>
  </verify>
  <done>intake.ts calls initState(paperDir(cwd)) before the INTAKE.md write + runSideEffects, with an idempotent StateAlreadyExistsError catch; tests/intake-bootstrap.test.ts un-skips and passes (write, idempotency, WARN-skip flip); STATE.json carries no new fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| intake → filesystem | STATE.json write (via initState → withLock + atomicWriteFile chokepoints) |
| repeated intake runs → STATE.json | the never-clobber idempotency contract crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Tampering | paperId regeneration across runs (registry collision / staleness) | mitigate | StateAlreadyExistsError caught + skipped (idempotent); seed-less initState uses randomUUID (non-guessable, V6 crypto-appropriate for an identifier) |
| T-12-08 | Tampering | bypassing the STATE.json schema/lock by writing it directly | mitigate | initState() is the SOLE writer (enforces StateSchema.parse + withLock atomically); intake never calls atomicWriteFile for STATE.json directly |
| T-12-09 | Denial of Service | unhandled initState error crashing intake | mitigate | only STATE_ALREADY_EXISTS is swallowed; other errors re-throw (fail-loud, never silent) |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `node --import tsx --test tests/intake-bootstrap.test.ts` — STATE.json write, idempotency, WARN-skip flip all GREEN.
- `node --import tsx --test tests/state.test.ts` — initState idempotency precedent still GREEN.
- `npm run test:tier-contract` — intake tier parity stays GREEN.
- `npm test` — full suite GREEN.
</verification>

<success_criteria>
- pensmith intake writes .paper/STATE.json with a non-null paperId (v2 schema).
- Re-running intake is idempotent (paperId unchanged; StateAlreadyExistsError caught).
- Global-library registration + style-match producer proceed (no WARN-skip) once STATE.json exists.
- STATE.json carries no new fields.
</success_criteria>

<output>
Create `.planning/phases/12-live-research-intake-bootstrap-humanizer-task/12-03-SUMMARY.md` when done.
</output>
