---
phase: 12-live-research-intake-bootstrap-humanizer-task
plan: 04
type: execute
wave: 3
depends_on: [12-01]
files_modified:
  - bin/lib/exporter.ts
autonomous: true
requirements: [GEN-05]

must_haves:
  truths:
    - "In Tier 1, runHumanizer invokes the humanizer skill via an injectable Task seam, writes .paper/FINAL.md, and returns its path"
    - "When the skill is absent OR no Task transport is available (null runner / Tier 2), runHumanizer prints a clear skip banner and returns null — export never fails on humanizer absence"
    - "The before/after honesty score and locked honest-framing copy stay owned by done.ts; runHumanizer does not call scoreHonesty and never weakens the framing to 'undetectable'"
    - "FINAL.md is left ready for GATE-04 (Phase 14 re-verify) but re-verification is NOT implemented here"
  artifacts:
    - path: "bin/lib/exporter.ts"
      provides: "TaskRunner type + __setTaskRunnerForTest seam + filled runHumanizer body (Task call → FINAL.md write); null-runner clean skip"
      contains: "__setTaskRunnerForTest"
  key_links:
    - from: "bin/lib/exporter.ts runHumanizer"
      to: "injectable TaskRunner seam"
      via: "_taskRunner invocation (live Task API in Tier 1, null in Tier 2)"
      pattern: "_taskRunner"
    - from: "bin/lib/exporter.ts runHumanizer"
      to: ".paper/FINAL.md"
      via: "atomicWriteFile(join(paperDir(paperRoot),'FINAL.md'), output)"
      pattern: "FINAL\\.md"
---

<objective>
Fill the `runHumanizer` body in `bin/lib/exporter.ts` (GEN-05). Add an injectable `TaskRunner` type + `__setTaskRunnerForTest` seam (mirroring intake.ts `__setInterpolateForTest` and zotero-mcp.ts `setZoteroClientForTest`), invoke the user's humanizer skill via the Claude Code Task transport in Tier 1, write the humanized output to `.paper/FINAL.md`, and return its path — while a null runner (Tier 2 / no transport) cleanly skips with a banner and returns null so export always proceeds.

Purpose: This is the last unlock — it makes a real before/after honesty score possible in Tier 1. The honesty bookkeeping and locked framing already live in done.ts; this plan only wires the Task invocation behind a testable seam. The FINAL.md output is left in the shape Phase 14 (GATE-04) will re-verify.
Output: bin/lib/exporter.ts (TaskRunner seam + filled runHumanizer).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-live-research-intake-bootstrap-humanizer-task/12-RESEARCH.md
@.planning/phases/12-live-research-intake-bootstrap-humanizer-task/12-PATTERNS.md

<read_first>
- bin/lib/exporter.ts — the FULL runHumanizer stub (lines 72-100): the present-but-no-transport banner, the absent-skill banner, the never-throws catch, and the `void draftMd`/`void paperRoot` stubs to replace. Imports already present: atomicWriteFile, isHumanizerSkillPresent, paperDir, join (node:path).
- bin/cli/done.ts — the runHumanizer call site (line 412) + the before/after scoreHonesty + renderHonestyReport flow (406-427). done.ts OWNS honesty; runHumanizer must NOT call scoreHonesty.
- bin/cli/intake.ts lines 35-46 — the __setInterpolateForTest seam (canonical injectable-seam precedent: double-underscore prefix, module-level let, returns a restore function).
- bin/lib/sources/zotero-mcp.ts lines 70-80 — the simpler module-level null-seam (setZoteroClientForTest).
- bin/lib/honesty.ts — renderHonestyReport + the LOCKED honest-framing copy ('improves prose', NEVER 'undetectable'); do NOT modify this file.
- bin/lib/ecosystem-presence.ts — isHumanizerSkillPresent() (the skill-present gate).
- tests/humanizer-task.test.ts — the Wave-0 contract this plan satisfies.
- tests/humanizer-wrap.test.ts — the existing absent-skill base test that must STAY GREEN.
</read_first>

<interfaces>
<!-- Contracts the executor builds against. Extracted from the codebase + RESEARCH. -->

New seam (add above runHumanizer):
- export type TaskRunner = (skill: string, input: Record<string, string>) => Promise<{ output: string }>;
- let _taskRunner: TaskRunner | null = null;
- export function __setTaskRunnerForTest(fn: TaskRunner | null): void;  // test-only

runHumanizer(draftMd: string, paperRoot?: string): Promise<string | null>
  - !isHumanizerSkillPresent() → stdout 'humanizer skill not found at ~/.claude/skills/humanizer/ — skipping humanize step.' + return null
  - skill present, _taskRunner === null (Tier 2 / no transport) → stdout 'humanizer skill present but no Task transport in this tier — skipping humanize step (export proceeds on DRAFT.md).' + return null
  - skill present, _taskRunner !== null (Tier 1) → const { output } = await _taskRunner('humanizer', { draft: draftMd }); write join(paperDir(paperRoot),'FINAL.md') via atomicWriteFile; return that path
  - NEVER throws — any unexpected error → absent-skill banner + return null (advisory)

done.ts ownership (do NOT change in this plan):
  before = scoreHonesty(draftMd); finalPath = runHumanizer(...); after = scoreHonesty(read(finalPath)); renderHonestyReport(before,after,backend)

FINAL.md path: ALWAYS join(paperDir(paperRoot),'FINAL.md') — never cwd+'/FINAL.md' (Pitfall 8; Phase 14 GATE-04 expects it in .paper/).

Live Task invocation: when _taskRunner is null in Tier 1 production, the real Claude Code Task API is the runner. The injectable seam is what tests use; the production Tier-1 wiring substitutes the real Task call where _taskRunner would be. Per RESEARCH A2 the Task API accepts (skill, input dict) → { output }. Keep the seam interface the single point of change if the real shape differs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TaskRunner seam + filled runHumanizer body</name>
  <read_first>
    - bin/lib/exporter.ts (runHumanizer stub 72-100; existing imports)
    - bin/cli/intake.ts lines 35-46 (__setInterpolateForTest precedent)
    - bin/lib/sources/zotero-mcp.ts lines 70-80 (null-seam precedent)
    - bin/lib/honesty.ts (locked framing — read, do not modify)
    - tests/humanizer-task.test.ts + tests/humanizer-wrap.test.ts (the contracts)
  </read_first>
  <files>bin/lib/exporter.ts</files>
  <behavior>
    - Tier-1 path (injected TaskRunner returns {output}): runHumanizer writes .paper/FINAL.md under paperDir(paperRoot), returns the path, and FINAL.md content equals the runner output.
    - Tier-2 path (_taskRunner null, skill present): returns null, prints the 'no Task transport' banner, never throws.
    - Absent-skill path (isHumanizerSkillPresent false): returns null, prints the 'humanizer skill not found' banner (the existing tests/humanizer-wrap.test.ts stays GREEN).
    - Unexpected error inside the Task call: degrades to the absent-skill banner + null (never throws — advisory).
    - runHumanizer does NOT call scoreHonesty (done.ts owns before/after); the framing copy is never weakened to 'undetectable'.
  </behavior>
  <action>In bin/lib/exporter.ts, above runHumanizer, add the TaskRunner type, the module-level `let _taskRunner: TaskRunner | null = null`, and the test-only `__setTaskRunnerForTest(fn)` seam (mirror intake.ts __setInterpolateForTest / zotero-mcp.ts setZoteroClientForTest — double-underscore prefix marks it test-only). Replace the `void draftMd` (line 76) and `void paperRoot` (line 88) stubs with the real body: keep the isHumanizerSkillPresent() === false → 'humanizer skill not found' banner + return null path exactly as is (tests/humanizer-wrap.test.ts pins it); then, when the skill IS present, branch on _taskRunner — if null, print the existing 'humanizer skill present but no Task transport' banner and return null (Tier-2); if non-null, `const { output } = await _taskRunner('humanizer', { draft: draftMd });` then `await atomicWriteFile(join(paperDir(paperRoot), 'FINAL.md'), output)` and return that FINAL.md path. Keep the outer try/catch that degrades any unexpected error to the absent-skill banner + null (never throws). Do NOT call scoreHonesty here (done.ts owns it). Do NOT modify honesty.ts or its locked framing copy. Do NOT implement Phase-14 GATE-04 re-verification — only leave FINAL.md in .paper/ ready for it. Wire the Wave-0 tests/humanizer-task.test.ts skip guard to GREEN by making taskSeamWired() (source-grep for `__setTaskRunnerForTest`) true.</action>
  <verify>
    <automated>node --import tsx --test tests/humanizer-task.test.ts tests/humanizer-wrap.test.ts</automated>
  </verify>
  <done>exporter.ts exports TaskRunner + __setTaskRunnerForTest; runHumanizer writes .paper/FINAL.md via the injected runner and returns its path; null-runner + absent-skill paths both return null with their banners and never throw; tests/humanizer-task.test.ts un-skips and passes; tests/humanizer-wrap.test.ts stays GREEN; honesty.ts framing untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Task runner → exporter | the humanizer skill's output is written to .paper/FINAL.md (inside the trusted paper boundary) |
| FINAL.md → export / Phase-14 | FINAL.md crosses into the export path; GATE-04 re-verification (Phase 14) re-fetches its citations |
| honesty framing copy | the user-facing before/after copy must stay the locked 'improves prose' wording |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 | Tampering | Task runner output not sanitized / could alter citations | accept | FINAL.md is written inside the trusted .paper/ boundary; re-verification of altered/introduced citations is GATE-04/Phase-14 (explicitly out of scope here; FINAL.md left ready for it) |
| T-12-11 | Repudiation/Integrity | honesty framing weakened to 'undetectable' | mitigate | runHumanizer prints only the locked banners; honesty.ts (renderHonestyReport) is NOT modified; Wave-0 framing-integrity test asserts no 'undetectable' token |
| T-12-12 | Denial of Service | humanizer absence/Task failure crashing export | mitigate | runHumanizer never throws — every path (absent skill, null runner, unexpected error) returns null with a banner; export proceeds on DRAFT.md |
| T-12-13 | Information Disclosure | FINAL.md written outside the paper sandbox | mitigate | always join(paperDir(paperRoot),'FINAL.md') via atomicWriteFile (D-07); never cwd-relative (Pitfall 8) |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `node --import tsx --test tests/humanizer-task.test.ts` — call-through (FINAL.md), null-runner skip, framing-integrity all GREEN.
- `node --import tsx --test tests/humanizer-wrap.test.ts` — existing absent-skill base STAYS GREEN.
- `node --import tsx --test tests/honesty.test.ts` — honesty flow unchanged, GREEN.
- `npm run test:tier-contract` — done/export tier parity stays GREEN.
- Grep exporter.ts + honesty.ts output for absence of the token `undetectable`.
- `npm test` — full suite GREEN.
</verification>

<success_criteria>
- Tier-1 runHumanizer invokes the skill via the injectable seam, writes .paper/FINAL.md, returns the path.
- Absent skill / null runner → clean skip banner + null; export never fails.
- Honesty before/after + locked framing stay owned by done.ts; no 'undetectable' anywhere.
- FINAL.md left ready for GATE-04; re-verification NOT implemented here.
</success_criteria>

<output>
Create `.planning/phases/12-live-research-intake-bootstrap-humanizer-task/12-04-SUMMARY.md` when done.
</output>
