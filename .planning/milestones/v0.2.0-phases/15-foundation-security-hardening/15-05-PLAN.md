---
phase: 15-foundation-security-hardening
plan: 05
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - bin/lib/pdf-text.ts
autonomous: true
requirements: [HARD-04]

must_haves:
  truths:
    - "A PDF over the byte cap is rejected with a clear error before parsing (no OOM)"
    - "A parse exceeding the wall-clock timeout is rejected with a clear error (no hang)"
    - "The byte cap and timeout values are named exports so they are testable"
    - "Normal in-bounds PDFs still parse unchanged"
  artifacts:
    - path: "bin/lib/pdf-text.ts"
      provides: "byte cap + Promise.race wall-clock timeout around the pdf-parse call"
      contains: "MAX_PDF_BYTES"
  key_links:
    - from: "bin/lib/pdf-text.ts extractPdfText"
      to: "parseWithRetry"
      via: "input.length > MAX_PDF_BYTES guard + Promise.race([parseWithRetry, timeoutPromise])"
      pattern: "Promise\\.race"
---

<objective>
Close HARD-04b: bound pdf-parse with a max-input-bytes cap and a wall-clock timeout so an attacker-controlled (or just huge) PDF fed via `add <pdf>` / BYO PDF cannot hang or OOM the process. pdf-parse@1.1.1 is already pinned; bounds are the fix (replacement deferred unless bounds prove insufficient).

Purpose: pdf-text.ts:93 calls pdfParse with no byte cap and no timeout on attacker-controlled bytes. A byte-cap check before parse + a Promise.race timeout around it turns a crash/hang into a clear rejection.
Output: pdf-text.ts gains MAX_PDF_BYTES + PDF_TIMEOUT_MS named exports and enforces both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-foundation-security-hardening/15-RESEARCH.md
@.planning/phases/15-foundation-security-hardening/15-PATTERNS.md

<read_first>
- bin/lib/pdf-text.ts (extractPdfText entry 126-134; parseWithRetry 87-105 — pdfParse call at line 93; the existing setImmediate inter-attempt delay at line 100 — the Promise idiom to mirror)
- bin/lib/http.ts (lines 530-536 — serverRetryDelay setTimeout Promise pattern to mirror for the timeout promise)
- tests/pdf-text-bounds.test.ts (Wave-0 scaffold — the acceptance target; expects MAX_PDF_BYTES + a timeout constant as named exports)
</read_first>

<interfaces>
Byte cap (RESEARCH lines 411-421): `export const MAX_PDF_BYTES = 50 * 1024 * 1024;` (50 MB — configurable constant). In extractPdfText BEFORE parseWithRetry(input): if input.length > MAX_PDF_BYTES, throw an Error whose message states the cap and the actual size (clear error, not crash). Per RESEARCH A2, Promise.race + setTimeout is sufficient (pdf-parse is async event-loop-scheduled, not a synchronous CPU lock) — no Worker thread.

Wall-clock timeout (RESEARCH lines 423-435): `export const PDF_TIMEOUT_MS = 30_000;` Wrap parseWithRetry: `const timeoutPromise = new Promise<never>((_, rej) => setTimeout(() => rej(new Error('extractPdfText: parse timed out after '+PDF_TIMEOUT_MS+'ms')), PDF_TIMEOUT_MS)); const result = await Promise.race([parseWithRetry(input), timeoutPromise]);` Clear the timer in a finally so the timeout promise does not keep the event loop alive after success (use a captured timer handle + clearTimeout). Both constants exported so the scaffold can construct an over-cap buffer and (with a tiny injected timeout) exercise the timeout path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Byte cap + wall-clock timeout in extractPdfText (HARD-04b)</name>
  <files>bin/lib/pdf-text.ts</files>
  <read_first>bin/lib/pdf-text.ts, bin/lib/http.ts, tests/pdf-text-bounds.test.ts</read_first>
  <behavior>
    - A Buffer of MAX_PDF_BYTES + 1 → extractPdfText rejects with a cap error message (no OOM)
    - A parse that exceeds PDF_TIMEOUT_MS → rejects with a timeout error (no hang)
    - An in-bounds valid PDF still returns its text unchanged
    - The timeout timer is cleared on success (no dangling timer keeping the loop alive)
  </behavior>
  <action>
Add `export const MAX_PDF_BYTES = 50 * 1024 * 1024;` and `export const PDF_TIMEOUT_MS = 30_000;` to bin/lib/pdf-text.ts. In extractPdfText (126-134), BEFORE the `await parseWithRetry(input)` call: add `if (input.length > MAX_PDF_BYTES) throw new Error(...)` with a message naming the cap and the actual byte count (RESEARCH lines 416-420). Then replace `const result = await parseWithRetry(input);` with a Promise.race against a timeout promise per the interface, capturing the setTimeout handle and clearing it in a finally (or after the race settles) so a successful parse leaves no dangling timer. Keep parseWithRetry's internal retry loop and its setImmediate inter-attempt delay unchanged. The cap + timeout are the ONLY new behavior; the happy path stays identical.

If the Wave-0 scaffold injects a tiny timeout for the timeout-path assertion, ensure PDF_TIMEOUT_MS is consumed in a way the test can override (e.g. read the exported const at call time, or accept an internal opts override the scaffold uses) — match whatever seam tests/pdf-text-bounds.test.ts probes; do not invent a second seam.
  </action>
  <verify>
    <automated>node --import tsx --test tests/pdf-text-bounds.test.ts 2>&1 | grep -v '^#' | grep -qE '(^|[^0-9])fail 0' && echo OK || echo FAIL</automated>
  </verify>
  <acceptance_criteria>tests/pdf-text-bounds.test.ts goes from skip to PASS (cap + timeout asserted); existing pdf-text consumers (add-source.test.ts, known-bad-quotes.test.ts) still pass; npx tsc --noEmit clean.</acceptance_criteria>
  <done>Over-cap PDF rejected cleanly; timeout fires on slow parse; in-bounds PDFs unchanged; no dangling timer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| attacker-controlled PDF bytes (add <pdf> / BYO) → pdf-parse | Untrusted binary input crosses into the parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04b | Denial of Service | pdf-text.ts pdfParse on unbounded input | mitigate | byte cap (MAX_PDF_BYTES) rejects oversize before parse (OOM defense) + Promise.race wall-clock timeout (PDF_TIMEOUT_MS) rejects hangs (ASVS V5 input-size validation, V7 explicit error) |
| T-15-04b-sc | Tampering (supply chain) | pdf-parse@1.1.1 | accept | pin already enforced (08-03 dual-surface pin guard); bounds make a malicious-input exploit non-fatal; outright replacement deferred per CONTEXT unless bounds prove insufficient |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | no package installs — node:timers Promise.race built-in; RESEARCH audit confirms zero new deps |
</threat_model>

<verification>
`node --import tsx --test tests/pdf-text-bounds.test.ts` — cap + timeout green. `npm test` — full suite GREEN (no regression in add-source / known-bad-quotes pdf paths). `npx tsc --noEmit` clean. Timeout tested deterministically via the injected tiny-timeout seam — no real 30s wait in CI.
</verification>

<success_criteria>
- MAX_PDF_BYTES + PDF_TIMEOUT_MS exported and enforced
- Over-cap PDF rejected with a clear error before parse; timeout fires on slow parse; timer cleared on success
- In-bounds PDFs parse unchanged; bounds scaffold green
</success_criteria>

<output>
Create `.planning/phases/15-foundation-security-hardening/15-05-SUMMARY.md` when done
</output>
