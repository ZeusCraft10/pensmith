---
phase: 08-style-match-sketch-add-library-byo-pdf-polish
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/global-library.test.ts
  - tests/style-match.test.ts
  - tests/intake-style-producer.test.ts
  - tests/sketch.test.ts
  - tests/add-source.test.ts
  - tests/pymupdf-shellout.test.ts
  - tests/write-style-integration.test.ts
  - tests/fixtures/style-samples/paperA/sample-1.md
  - tests/fixtures/style-samples/paperA/sample-2.txt
  - tests/fixtures/style-samples/paperB/sample-1.md
  - tests/fixtures/pdf/byo-text.pdf
  - tests/fixtures/cassettes/crossref/add-doi.json
autonomous: true
requirements: [LIB-01, LIB-02, LIB-03, LIB-05, ERGO-05, ERGO-06, RSCH-05, STYL-01, STYL-02, STYL-03]
must_haves:
  truths:
    - "Full suite stays GREEN: every new RED test is RED-by-skip (skip-guarded on existsSync of the unbuilt module), never a hard failure"
    - "A deterministic .md/.txt sample-set fixture exists for pure-stats style profiling, plus a cross-paper-reuse fixture pair (paperA reused, paperB distinct)"
    - "A committed text-bearing PDF fixture exists for pdf-parse ingestion in add-source"
    - "An offline Crossref cassette exists for `add <doi>` hydration"
    - "A RED-by-skip producer test asserts the intake style-match opt-in builds .paper/STYLE.json AND surfaces the cross-paper-reuse notice unconditionally when a prior paper shares the fingerprint"
    - "A RED-by-skip status-cycle test asserts deriveLibraryStatus (the DERIVE-AT-DISPLAY resolver) returns the live lifecycle value per paper from its STATE.json + section PLAN.md frontmatter — an outline-stage paper shows 'outline', a mid-sectioning paper shows 'sectioning X/Y' with real numbers, a done paper shows 'done', AND a paper with a missing/corrupt STATE.json renders 'intake'/'unknown' WITHOUT crashing (never-throw)"
  artifacts:
    - path: "tests/global-library.test.ts"
      provides: "RED-by-skip suite for LIB-01/02/03/05, incl. the DERIVE-AT-DISPLAY status-cycle + never-throw cases"
      contains: "existsSync"
    - path: "tests/style-match.test.ts"
      provides: "RED-by-skip suite for STYL-01/02"
      contains: "existsSync"
    - path: "tests/intake-style-producer.test.ts"
      provides: "RED-by-skip suite for the intake style-match opt-in PRODUCER (STYL-01/02 wiring): builds STYLE.json + unconditional reuse notice"
      contains: "existsSync"
    - path: "tests/sketch.test.ts"
      provides: "RED-by-skip suite for ERGO-05 no-advance invariant"
      contains: "existsSync"
    - path: "tests/add-source.test.ts"
      provides: "RED-by-skip cassette-backed suite for ERGO-06 + RSCH-05"
      contains: "existsSync"
    - path: "tests/pymupdf-shellout.test.ts"
      provides: "RED-by-skip suite for RSCH-05 absent-graceful fallback"
      contains: "existsSync"
    - path: "tests/write-style-integration.test.ts"
      provides: "RED-by-skip suite for STYL-03 voice_hint override"
      contains: "existsSync"
    - path: "tests/fixtures/pdf/byo-text.pdf"
      provides: "text-bearing PDF for add-source ingestion"
  key_links:
    - from: "tests/*.test.ts (Phase 8 RED)"
      to: "bin/lib/global-library.ts, style-match.ts, pymupdf-shellout.ts, bin/cli/{sketch,add,intake}.ts"
      via: "existsSync skip-guard"
      pattern: "existsSync\\("
---

<objective>
Land the Wave-0 RED scaffold for Phase 8: failing-by-skip test suites plus every non-code fixture (style sample sets, a cross-paper-reuse pair, a text-bearing PDF, a Crossref `add` cassette) that Waves 1-3 implement against. This is the Nyquist prerequisite — no Phase 8 implementation task may run without its test already on disk. Includes the producer-path RED test for the intake style-match opt-in (the wiring the cross-AI review found missing) AND the status-cycle RED test for the DERIVE-AT-DISPLAY library-status resolver (the wiring cycle-2 review found missing — status stuck at intake).

Purpose: Establish the deterministic offline test contract BEFORE any implementation, so each later task flips a known RED-by-skip test to GREEN. Mirrors the Phase 5/6/7 Wave-0 pattern (RED-by-skip via existsSync so the full suite stays GREEN with zero failures, only skips).
Output: 7 new test files + 5 fixture artifacts; full suite remains GREEN (skips only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-RESEARCH.md
@.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-PATTERNS.md
@.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-VALIDATION.md

<interfaces>
<!-- RED tests assert against these contracts. Executor codes the skip-guarded tests to these signatures so Wave 1-3 implementations satisfy them without renegotiation. -->

Global library (bin/lib/global-library.ts — built in 08-01):
  initGlobalLibrary(): Promise<GlobalLibrary>
  loadGlobalLibrary(): Promise<GlobalLibrary>
  registerPaperInGlobalLibrary(entry: GlobalLibraryEntry): Promise<GlobalLibrary>   // UPSERT by id
  deriveLibraryStatus(folderPath: string, storedStatus?: string):                   // DERIVE-AT-DISPLAY (LIB-05) — NEVER throws
    { status: 'intake'|'research'|'outline'|'sectioning'|'compile'|'done'|'archived'|'unknown';
      sectioningProgress?: { done: number; total: number } }
  // deriveLibraryStatus mapping (mirrors router.resolveNextAction's on-disk machine onto LIB-05 vocab):
  //   storedStatus==='archived' → 'archived' (terminal manual flag; no on-disk marker)
  //   STATE.json ABSENT (StateNotFoundError) → 'intake' ; STATE.json corrupt/any-other-throw → 'unknown'
  //   STATE.json present, no .paper/RESEARCH.md → 'intake' ; +RESEARCH.md, no OUTLINE.md → 'research'
  //   +OUTLINE.md, sections absent/empty → 'outline'
  //   sections present, not all 'verified' → 'sectioning' + {done:X, total:Y}
  //     (Y = sections.length ; X = count whose PLAN.md status ∈ {written,verifying,verified,failed,unverifiable})
  //   all sections 'verified', no .paper/DRAFT.md → 'compile' ; +DRAFT.md → 'done'
  // GlobalLibraryEntry: { id(uuid), name, folderPath, class(default 'Unfiled'),
  //   status: 'intake'|'research'|'outline'|'sectioning'|'compile'|'done'|'archived',
  //   sectioningProgress?: {done,total}, createdAt, updatedAt }
  //   NOTE: the PAPER registry entry KEEPS folderPath (open + status derivation need it) — distinct from the
  //   path-free FINGERPRINT registry (08-02). stored entry.status is seeded at intake and used by list ONLY for 'archived'.

Style-match (bin/lib/style-match.ts — built in 08-02):
  buildStyleProfile(samplesDir): Promise<StyleProfile>           // pure-stats, NO LLM
  writeStyleProfile(paperDir, profile): Promise<void>           // writes paperDir/STYLE.json ONLY
  checkAndRegisterFingerprint(fingerprint, paperId, paperName):
    Promise<{ priorPapers: Array<{paperId,paperName,addedAt}> }>   // hashes + paper identity ONLY in registry (no features, no paths)
  styleMatchToVoiceHint(profile): string                        // pure render

Intake style-match opt-in PRODUCER (bin/cli/intake.ts — wired in 08-05):
  `intake --style-samples <dir>` runs buildStyleProfile → checkAndRegisterFingerprint
  (surfaces priorPapers UNCONDITIONALLY) → writeStyleProfile(.paper/STYLE.json)
  // opt-in only (absent flag = no profiling); reuse notice is NOT --yolo-gated; non-fatal on bad samples dir

pymupdf (bin/lib/pymupdf-shellout.ts — built in 08-03):
  pymupdfShellout(buf: Buffer): Promise<string | null>          // null on ANY subprocess failure

Authoritative status sources the deriveLibraryStatus test seeds + asserts against (existing chokepoints):
  state.ts: initState(paperRoot)/initSection(paperRoot,n,slug) — seed STATE.json + sections
  paths.ts: paperDir(root), sectionPlan(n,slug,root) — write per-section PLAN.md frontmatter (status) the resolver reads
  router.ts: readSectionState(planPath) — the never-throw per-section helper deriveLibraryStatus REUSES
  PlanFrontmatterSchema.status enum: planned|writing|written|verifying|verified|failed|unverifiable

Existing chokepoints the tests reuse:
  paths.ts: pensmithDataDir(platform?, env?), paperDir(root?)
  pdf-text.ts: extractPdfText(buf: Buffer|Uint8Array): Promise<string>
  Env-override isolation (tests/library.test.ts pattern):
    process.env.LOCALAPPDATA / XDG_DATA_HOME / HOME = mkdtempSync(...)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Style sample fixtures + cross-paper-reuse pair + text-bearing PDF + Crossref cassette</name>
  <read_first>
    - tests/library.test.ts (env-override tmpdir isolation; cassette/fixture conventions)
    - tests/fixtures/cassettes/crossref/ (existing cassette JSON shape — copy structure exactly)
    - bin/lib/sources/crossref.ts lines 84-130 (search/fetchById cassette loader: loadCassetteFile('crossref', '<name>'))
    - bin/lib/pdf-text.ts lines 55-111 (IMAGE_ONLY_TEXT_THRESHOLD=50; a fixture PDF must yield ≥50 non-whitespace chars to take the pdf-parse-succeeds path)
  </read_first>
  <action>
    Create deterministic offline fixtures. (a) Under tests/fixtures/style-samples/paperA/ write sample-1.md and sample-2.txt with several real prose paragraphs each (varied sentence lengths, some passive-voice constructions like "was analyzed"/"were observed", repeated opening words such as "The"/"This"/"In") so pure-stats extraction in 08-02 produces stable, non-degenerate features. (b) Under tests/fixtures/style-samples/paperB/ write sample-1.md with DISTINCT content so its content-hash fingerprint differs from paperA — this is the cross-paper-reuse negative control. The reuse-positive case is paperA reused for two paper IDs (same files → same fingerprint), exercised in the style-match + producer tests. (c) Create tests/fixtures/pdf/byo-text.pdf as a genuine text-bearing PDF (use a minimal hand-authored PDF with an embedded text stream of at least a full sentence, OR generate via pdf-lib already present in deps — emit raw bytes committed to the repo) such that extractPdfText returns ≥50 non-whitespace chars. (d) Create tests/fixtures/cassettes/crossref/add-doi.json matching the existing crossref cassette JSON shape (array of {request, response} or the loadCassetteFile contract used by fetchById) so `add <doi>` hydration resolves a SourceCandidate offline.
  </action>
  <verify>
    <automated>node --import tsx -e "import('node:fs').then(fs=>{['tests/fixtures/style-samples/paperA/sample-1.md','tests/fixtures/style-samples/paperA/sample-2.txt','tests/fixtures/style-samples/paperB/sample-1.md','tests/fixtures/pdf/byo-text.pdf','tests/fixtures/cassettes/crossref/add-doi.json'].forEach(f=>{if(!fs.existsSync(f))throw new Error('missing '+f)});console.log('OK')})"</automated>
  </verify>
  <done>All 5 fixture paths exist; byo-text.pdf yields ≥50 non-whitespace chars via extractPdfText; paperA and paperB sample sets have distinct content hashes; add-doi.json matches the crossref cassette contract.</done>
</task>

<task type="auto">
  <name>Task 2: RED-by-skip suites — global-library (incl. DERIVE-AT-DISPLAY status-cycle + never-throw), style-match, intake style-producer, pymupdf, write-style</name>
  <read_first>
    - tests/library.test.ts (full file — env-override isolation, addEntry/duplicate assertions to adapt for UPSERT)
    - .planning/STATE.md decisions [05-01]/[06-01]/[07-01] (RED-by-skip via existsSync of the unbuilt module; full suite stays GREEN)
    - 08-PATTERNS.md "Env-override test isolation" + global-library/style-match interface blocks
    - bin/lib/paths.ts lines 82-117 (pensmithDataDir injection points for tmpdir isolation) + sectionPlan/paperDir
    - bin/lib/state.ts (initState/initSection — seed STATE.json + sections for the status-cycle test)
    - bin/lib/router.ts (readSectionState + resolveNextAction's on-disk stage machine — the contract deriveLibraryStatus mirrors)
    - bin/lib/schemas/plan-frontmatter.ts (PLAN.md status enum the status-cycle fixtures write)
    - bin/cli/intake.ts (current Tier-2 stub — the producer skip-guard must detect "opt-in not yet wired")
  </read_first>
  <action>
    Create five test files, each top-guarded so it SKIPS (not fails) until its target module/wiring exists — `const READY = existsSync('bin/lib/<module>.ts'); test('...', { skip: !READY }, ...)` — mirroring the [05-01]/[06-01]/[07-01] RED-by-skip precedent so `npm test` stays GREEN. (a) tests/global-library.test.ts (skip-guard on bin/lib/global-library.ts): init creates index.json under pensmithDataDir()/library/ inside lock; registerPaperInGlobalLibrary UPSERTs by id (insert new, update existing — NOT reject-on-duplicate); loadGlobalLibrary auto-inits on ENOENT; the index lives in pensmithDataDir(), NEVER inside any .paper/; the PAPER registry entry RETAINS folderPath (assert a registered entry round-trips its folderPath — load-bearing for open + status derivation). status lifecycle round-trips intake→research→outline→sectioning(with sectioningProgress)→compile→done→archived in the SCHEMA (LIB-05 representable set). PLUS the DERIVE-AT-DISPLAY status-cycle + never-throw block (the cycle-2 HIGH fix — this is what makes the lifecycle FUNCTIONAL beyond intake): in env-override tmpdirs, build several papers in DIFFERENT on-disk lifecycle states and assert deriveLibraryStatus(folderPath, storedStatus) returns the DERIVED value (NOT the stored entry.status): (1) a paper whose STATE.json exists but has no .paper/RESEARCH.md → 'intake'; (2) +.paper/RESEARCH.md, no OUTLINE.md → 'research'; (3) +.paper/OUTLINE.md with sections seeded via initSection but no section reaching written → 'outline' when sections empty, else 'sectioning' — construct the OUTLINE case with sections.length===0; (4) an OUTLINE-stage paper WITH N sections seeded where M of them have a PLAN.md whose frontmatter status ∈ {written,verifying,verified,failed,unverifiable} and the rest 'planned' → assert status 'sectioning' AND sectioningProgress === {done: M, total: N} (a REAL X/Y, proving the branch is live, not dead); (5) a paper with ALL sections 'verified' and no .paper/DRAFT.md → 'compile'; with .paper/DRAFT.md present → 'done'; (6) deriveLibraryStatus(folderPath, 'archived') → 'archived' regardless of on-disk state (terminal flag honored); (7) NEVER-THROW: a folderPath whose STATE.json is ABSENT → 'intake' (no throw); a folderPath whose STATE.json is PRESENT-but-corrupt (write invalid JSON / a schema-invalid object) → 'unknown' (no throw); a folderPath with a corrupt section PLAN.md does NOT crash the derivation — wrap each deriveLibraryStatus call to assert it RESOLVES rather than rejects. Write the per-section PLAN.md frontmatter directly (parseFrontmatter/atomicWriteFile or a raw `---\nsection: N\nslug: ...\nstatus: written\n---` body at sectionPlan(n,slug,root)) so the resolver's readSectionState reads a real status. (b) tests/style-match.test.ts (skip-guard on bin/lib/style-match.ts): buildStyleProfile(paperA) returns deterministic features (median/p25/p75 sentence length, typeTokenRatio in [0,1], passiveVoiceRate in [0,1]) and a 64-hex fingerprint; writeStyleProfile writes to paperDir/STYLE.json and the fingerprint registry at pensmithDataDir()/style-fingerprints.json contains NO prose features AND NO folderPath/absolute-path key (Pitfall 1 assertion — assert the registry JSON has no "features" key and no "folderPath" key, hashes + paper identity ONLY); checkAndRegisterFingerprint returns priorPapers=[] first call then a non-empty priorPapers when the SAME fingerprint is registered for a second paperId (STYL-02 reuse detection). (c) tests/intake-style-producer.test.ts (skip-guard: source-grep that bin/cli/intake.ts imports buildStyleProfile / references styleSamples — mirroring the [07-01] source-grep skip-predicate so it stays RED-by-skip until 08-05 wires the opt-in): in an env-override tmpdir, run intake with the style-match opt-in (`--style-samples` pointing at tests/fixtures/style-samples/paperA) and assert (1) .paper/STYLE.json is WRITTEN (existsSync true, parses via StyleProfileSchema); (2) when a PRIOR paper already registered the SAME paperA fingerprint under a different paperId, the intake run SURFACES a cross-paper-reuse notice on stdout UNCONDITIONALLY — assert the notice fires even without --yolo and is not suppressible (capture stdout, assert it names/flags the prior paper); (3) WITHOUT the opt-in flag, NO .paper/STYLE.json is produced (opt-in is explicit). Seed the prior-paper fingerprint via checkAndRegisterFingerprint (or a first intake run) before the assertion. (d) tests/pymupdf-shellout.test.ts (skip-guard on bin/lib/pymupdf-shellout.ts): pymupdfShellout returns null (never throws) when the shellout fails — assert by pointing it at the absent-fitz path; a null result is the designed graceful degradation. (e) tests/write-style-integration.test.ts (skip-guard on bin/cli/write.ts containing a styleProfilePath reference): asserts DrafterInput voiceHint resolution priority is PLAN.md voice_hint > style-match render > default (STYL-03 / Pitfall 7); use a stubbed PLAN.md with a non-empty voice_hint and assert it WINS over a present STYLE.json. Use the tests/library.test.ts env-override tmpdir isolation in every file.
  </action>
  <verify>
    <automated>node --import tsx --test tests/global-library.test.ts tests/style-match.test.ts tests/intake-style-producer.test.ts tests/pymupdf-shellout.test.ts tests/write-style-integration.test.ts</automated>
  </verify>
  <done>All five files run with 0 failures (skips only, because target modules/wiring do not yet exist). Each file uses existsSync (or source-grep) skip-guards and tmpdir env-override isolation. Assertions encode the UPSERT, folderPath-retained-in-PAPER-registry, the DERIVE-AT-DISPLAY status-cycle (outline→sectioning X/Y→compile→done from STATE.json, archived from the stored flag) + never-throw on missing/corrupt STATE.json, per-paper-only STYLE.json, no-features-and-no-paths-in-FINGERPRINT-registry, reuse-detection, the PRODUCER path (STYLE.json built + unconditional reuse notice + opt-in gating), null-on-failure, and voice_hint-override contracts.</done>
</task>

<task type="auto">
  <name>Task 3: RED-by-skip suites — sketch no-advance + add-source cassette-backed</name>
  <read_first>
    - bin/cli/status.ts (never-crash thin-orchestrator + stdout-only conventions)
    - bin/cli/sketch.ts (current Phase-2 stub — skip-guard must detect "not yet real")
    - bin/cli/add.ts (current Phase-2 stub)
    - tests/cassette-size.test.ts + bin/lib/sources/crossref.ts (offline cassette loader; PENSMITH offline-mode env)
    - 08-RESEARCH.md Pitfall 6 (sketch must NOT create .paper/ before confirm) + Pattern 6 (add flow + assigned_sources-only remap)
  </read_first>
  <action>
    Create two skip-guarded test files (RED-by-skip; full suite stays GREEN). (a) tests/sketch.test.ts (skip-guard: the sketch verb is still a stub until 08-04 — guard on a source-grep that bin/cli/sketch.ts imports dispatchVerb, mirroring the [07-01] source-grep skip-predicate precedent rather than bare existsSync): run sketch in a tmpdir with a stubbed/declined confirm and assert NO .paper/ directory, NO STATE.json, and NO LIBRARY.json were created (ERGO-05 / Pitfall 6 — the load-bearing no-advance invariant); assert that on confirm it dispatches `new` with a thesis arg (spy on dispatchVerb or assert the downstream effect) and does NOT itself call initState. (b) tests/add-source.test.ts (skip-guard: source-grep that bin/cli/add.ts imports extractPdfText/writeBibtex): in offline cassette mode, `add <doi>` hydrates from tests/fixtures/cassettes/crossref/add-doi.json and appends to .paper/CITATIONS.bib via writeBibtex; `add <pdf>` reads tests/fixtures/pdf/byo-text.pdf, extracts text, and hydrates; the remap gate, when approved, appends the citekey to a section PLAN.md's assigned_sources[] and asserts status + verified_against_draft_hash are UNCHANGED (Pitfall 3 / A6 — section-state-corruption guard). Build the fixture .paper/ with one section PLAN.md INLINE in the test (mkdtemp + write the section PLAN.md via atomicWriteFile) — do NOT rely on an unlisted committed fixture path.
  </action>
  <verify>
    <automated>node --import tsx --test tests/sketch.test.ts tests/add-source.test.ts</automated>
  </verify>
  <done>Both files run with 0 failures (skips only). sketch.test asserts the no-.paper/-before-confirm invariant; add-source.test asserts cassette-backed DOI hydration, PDF ingestion via the existing chokepoint, and the assigned_sources-only remap (status/verified_against_draft_hash untouched), with the section-PLAN.md fixture built inline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → repo | committed PDF + cassette bytes become part of the offline test corpus |
| sample-set fixtures → style-match | fixture content drives deterministic fingerprint + feature assertions |
| seeded STATE.json/PLAN.md → deriveLibraryStatus | the status-cycle test drives the resolver across its full lifecycle + corrupt-input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-00-01 | Tampering | byo-text.pdf fixture | mitigate | Hand-authored / pdf-lib-emitted committed bytes; no network fetch; deterministic ≥50-char text body |
| T-08-00-02 | Information Disclosure | style-fingerprints registry assertion | mitigate | RED test asserts the FINGERPRINT registry JSON contains NO "features" key AND NO "folderPath" key (Pitfall 1 + path-leak negative control baked into Wave 0); the SEPARATE PAPER registry is asserted to RETAIN folderPath |
| T-08-00-03 | Denial of Service | full-suite stability | mitigate | All new tests RED-by-skip (existsSync / source-grep guards) so npm test stays GREEN until Waves 1-3 land modules/wiring |
| T-08-00-04 | Repudiation | producer reuse-notice not asserted | mitigate | tests/intake-style-producer.test.ts asserts the cross-paper-reuse notice fires UNCONDITIONALLY (not --yolo-gated) when a prior paper shares the fingerprint — the surfacing contract is encoded in Wave 0 |
| T-08-00-05 | Denial of Service | status lifecycle stuck at intake / dead sectioning branch | mitigate | tests/global-library.test.ts asserts deriveLibraryStatus returns the DERIVED lifecycle value per paper (real sectioning X/Y, not the stored intake) AND never throws on a missing/corrupt STATE.json — the cycle-2 HIGH fix is encoded as a Wave-0 assertion |
| T-08-00-SC | Tampering | npm/pip/cargo installs | mitigate | NONE — Phase 8 installs no new packages (08-RESEARCH Package Legitimacy Audit: pdf-parse + proper-lockfile already installed, both [OK]) |
</threat_model>

<verification>
- `npm test` stays GREEN (skips only; zero failures) after all three tasks.
- 5 fixture artifacts exist and are committed.
- Each RED suite skip-guards on the unbuilt module/stub/wiring and uses tmpdir env-override isolation.
- The producer RED test (intake-style-producer) asserts STYLE.json build + unconditional reuse notice + opt-in gating.
- The status-cycle RED test (global-library) asserts the DERIVE-AT-DISPLAY resolver across the full lifecycle (outline → sectioning X/Y → compile → done → archived) + never-throw on missing/corrupt STATE.json.
</verification>

<success_criteria>
- 7 new test files exist, all RED-by-skip, full suite GREEN.
- Style sample sets (reuse-positive paperA, reuse-negative paperB), text-bearing PDF, and Crossref `add` cassette committed.
- The Pitfall-1 (no features/paths in FINGERPRINT registry; PAPER registry retains folderPath), Pitfall-3 (assigned_sources-only), Pitfall-6 (no advance before confirm), STYL-02 (unconditional reuse notice via the producer), STYL-03 (voice_hint override), and LIB-05 (DERIVE-AT-DISPLAY status-cycle + never-throw) invariants are encoded as assertions ready to flip GREEN.
</success_criteria>

<output>
Create `.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-00-SUMMARY.md` when done
</output>
