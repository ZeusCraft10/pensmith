---
phase: 08-style-match-sketch-add-library-byo-pdf-polish
plan: 04
type: execute
wave: 2
depends_on: ["08-01", "08-03"]
files_modified:
  - bin/cli/add.ts
  - bin/cli/sketch.ts
  - bin/pensmith.ts
autonomous: true
requirements: [ERGO-05, ERGO-06, RSCH-05]
must_haves:
  truths:
    - "`pensmith add <doi>` hydrates via Crossref through http.ts, appends to .paper/CITATIONS.bib via writeBibtex, and prompts 'remap sections?'"
    - "`pensmith add <pdf>` extracts text via the pdf-text chokepoint (with pymupdf fallback) and hydrates metadata via Crossref"
    - "The remap gate touches ONLY assigned_sources[] in section PLAN.md frontmatter — never status or verified_against_draft_hash"
    - "`pensmith sketch` runs a thinking-partner thesis loop and does NOT create .paper/ / STATE.json / LIBRARY.json until the user confirms"
  artifacts:
    - path: "bin/cli/add.ts"
      provides: "mid-paper DOI/PDF/URL ingestion + remap gate"
      exports: ["addCommand"]
    - path: "bin/cli/sketch.ts"
      provides: "Socratic thesis-discovery, no-advance-until-confirm"
      exports: ["sketchCommand"]
  key_links:
    - from: "bin/cli/add.ts"
      to: "bin/lib/sources/crossref.ts (via http.ts)"
      via: "fetchById / search"
      pattern: "crossref"
    - from: "bin/cli/add.ts remapSections"
      to: "section PLAN.md assigned_sources[]"
      via: "withLock + updateFrontmatter + atomicWriteFile"
      pattern: "assigned_sources"
    - from: "bin/cli/sketch.ts (after confirm)"
      to: "dispatchVerb('new', {args:{thesis}})"
      via: "dispatchVerb"
      pattern: "dispatchVerb\\('new'"
---

<objective>
Promote `add` (ERGO-06 + RSCH-05b) and `sketch` (ERGO-05) from Phase-2 stubs to real thin-orchestrator verbs, then register both in REAL_VERB_LOADERS. `add` ingests a source mid-paper (DOI via Crossref, PDF via the pdf-text chokepoint with the 08-03 pymupdf fallback, URL via http.ts) and offers a remap gate that touches ONLY assigned_sources[]. `sketch` runs a Socratic thesis loop and never advances paper state until the user confirms.

Purpose: SC-3 (sketch no-advance) and SC-4 (add doi|pdf|url + remap + pymupdf) — the two new interactive verbs. Both honor approval-gates-default-on and the section-state-isolation non-negotiable.
Output: 2 promoted CLI verbs + 2 REAL_VERB_LOADERS entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-RESEARCH.md
@.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-PATTERNS.md
@.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-01-SUMMARY.md
@.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-03-SUMMARY.md

<interfaces>
<!-- All chokepoints add/sketch compose. Never bypass: writeBibtex, httpFetch, atomicWriteFile, withLock, dispatchVerb. -->

From bin/lib/sources/crossref.ts:
  search(query, {limit?}): Promise<SourceCandidate[]>
  fetchById(doi): Promise<SourceCandidate | null>      // offline cassette-backed
From bin/lib/pdf-text.ts: extractPdfText(buf): Promise<string>   // pymupdf fallback inside (08-03)
From bin/lib/bibtex-write.ts: writeBibtex(candidates, bibPath): Promise<void>   // citation-js chokepoint
From bin/lib/doi.ts: normalizeDoi(s): string ; verifyDoi(doi): Promise<...>
From bin/lib/http.ts: fetch as httpFetch(url, opts?)   // D-06 chokepoint — NEVER raw fetch
From bin/lib/frontmatter.ts:
  parseFrontmatter(text): { frontmatter, body }
  updateFrontmatter(text, (fm)=>void): string          // round-trip safe
From bin/lib/atomic-write.ts: atomicWriteFile(path, contents)
From bin/lib/lock.ts: withLock(path, fn)
From bin/lib/paths.ts: paperDir(root?), sectionPlan(n, slug, root)
From bin/lib/prompts.ts: ask({id, type:'confirm'|'text', label}): Promise<{value}>
From bin/pensmith.ts (lines 110-145): dispatchVerb(verb, {args?, globalFlags?}): Promise<unknown>
From bin/cli/status.ts: never-crash, stdout-only conventions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: bin/cli/add.ts — DOI/PDF/URL ingestion + assigned_sources-only remap gate</name>
  <read_first>
    - bin/cli/compile.ts (thin-orchestrator template + how it reads the yolo arg to skip its gate)
    - bin/cli/done.ts lines 58-115 (approval-gate via ask(); --yolo skip)
    - bin/lib/frontmatter.ts lines 29-107 (parseFrontmatter / updateFrontmatter round-trip)
    - bin/lib/sources/crossref.ts lines 84-130 (search/fetchById + cassette offline mode)
    - bin/lib/bibtex-write.ts + bin/lib/citekey.ts (writeBibtex regenerates the full file; citekey collision handling)
    - 08-PATTERNS.md sections "bin/cli/add.ts" + "Remap-sections pattern" + Shared "parseFrontmatter + updateFrontmatter"
    - 08-RESEARCH.md Pattern 6 (add flow steps 1-8) + Pitfall 3 (no section-state corruption) + A6
  </read_first>
  <action>
    Create bin/cli/add.ts as a thin orchestrator (compile.ts shape): addCommand with a required positional `source` and a boolean `yolo` (default false). paperRoot = process.cwd(). (1) Detect type + hydrate into a SourceCandidate: if isDoi(source) → crossrefFetchById(normalizeDoi(source)); else if it ends with .pdf or exists on disk → readFile(path.resolve(source)) → extractPdfText(buf) (pymupdf fallback runs inside per 08-03) → extract a title heuristic → crossrefSearch(title,{limit:1})[0]; else treat as URL → httpFetch(url) (D-06 chokepoint, NEVER raw fetch), sniff Content-Type: PDF bytes → PDF path; HTML → scrape a DOI from <meta> then retry as DOI. If no candidate hydrates, print "could not hydrate … NOT added" and return {ok:false}. (2) Run verifyDoi(doi) when a DOI is present — a 404 flags the source as unverified but it is still added with a warning (the Pass-1 verifier still blocks compile on FABRICATED at verify time). (3) Dedup citekey against existing CITATIONS.bib; warn on collision. (4) Write: load existing candidates, append the new one, writeBibtex([...existing, candidate], join(paperDir(paperRoot),'CITATIONS.bib')) — the citation-js chokepoint, never a hand-rolled writer. (5) Append a RESEARCH.md table row (title/authors/year/doi/citekey, status 'unverified-new'). (6) Remap gate: if !args.yolo, ask({id:'add-remap', type:'confirm', label:'Source added. Remap sections to reference it?'}); on yes call remapSections(paperRoot, candidate.citekey). remapSections: for each section, withLock(sectionPlan(n,slug,paperRoot)) → read PLAN.md → updateFrontmatter to append citekey to assigned_sources[] (idempotent — skip if already present) → atomicWriteFile. NEVER touch status or verified_against_draft_hash (Pitfall 3 / A6 — a verified section stays verified; the user runs `plan <N> --revise` to rebuild the claim mapping). Use ask() chokepoint, atomicWriteFile, withLock — no raw fs.writeFile. stdout-only, return {ok, citekey}.
  </action>
  <verify>
    <automated>node --import tsx --test tests/add-source.test.ts</automated>
  </verify>
  <done>tests/add-source.test.ts flips skip→GREEN: DOI hydrates from the cassette and lands in CITATIONS.bib; PDF ingestion routes through extractPdfText; the remap gate appends ONLY to assigned_sources[] and leaves status + verified_against_draft_hash untouched; all I/O via the chokepoints.</done>
</task>

<task type="auto">
  <name>Task 2: bin/cli/sketch.ts — Socratic thesis loop, no-advance-until-confirm + REAL_VERB_LOADERS (add + sketch)</name>
  <read_first>
    - bin/cli/next.ts (dispatchVerb + global-flag forwarding pattern)
    - bin/cli/status.ts (never-crash, stdout-only)
    - bin/lib/prompts.ts lines 74-83 (ask() text + confirm)
    - bin/cli/intake.ts (intakeCommand — confirm it accepts a thesis/prefill arg; if not, add an OPTIONAL `thesis` arg per Open-Q2 — NOT a new verb)
    - bin/pensmith.ts lines 57-80 (REAL_VERB_LOADERS edit point)
    - 08-PATTERNS.md section "bin/cli/sketch.ts" + "REAL_VERB_LOADERS promotion pattern"
    - 08-RESEARCH.md ERGO-05 + Pitfall 6 (sketch must NOT initState / mkdir .paper / initLibrary before confirm)
  </read_first>
  <action>
    Create bin/cli/sketch.ts: sketchCommand with boolean `yolo` + `dry-run` args. Run a Tier-2 Socratic loop via ask() — 4-5 text questions (interests, disagreements with the conventional view, audience, candidate thesis claim). Synthesize a candidate thesis string and print it to stdout. CRITICAL no-advance invariant (Pitfall 6): sketch MUST NOT call initState, MUST NOT mkdir .paper/, MUST NOT call initLibrary anywhere in the Socratic loop. Confirm gate: if !args.yolo, ask({id:'sketch-confirm', type:'confirm', label:'Proceed to intake with this thesis?'}); on decline print "cancelled — re-run to try again" and return {ok:false} WITHOUT creating any state. ONLY after confirm (or --yolo): dispatchVerb('new', {args:{thesis: synthesized}, globalFlags:{yolo:args.yolo===true, dryRun:args['dry-run']===true}}). If intakeCommand does not already accept a thesis arg, add an OPTIONAL `thesis` positional/flag to intakeCommand that pre-fills the intake (do NOT add a new verb — Open-Q2). Then add BOTH verbs to REAL_VERB_LOADERS in bin/pensmith.ts: `add: () => import('./cli/add.js').then(m=>m.addCommand)` and `sketch: () => import('./cli/sketch.js').then(m=>m.sketchCommand)` — the only edit point; buildSubCommands auto-registers. Do NOT touch UX02_VERBS (add/sketch are already members).
  </action>
  <verify>
    <automated>node --import tsx --test tests/sketch.test.ts && node --import tsx -e "import('./bin/pensmith.ts').then(m=>{const v=m.REAL_VERB_LOADERS;['list','open','add','sketch'].forEach(k=>{if(!v[k])throw new Error('missing loader '+k)});console.log('OK')})"</automated>
  </verify>
  <done>tests/sketch.test.ts flips skip→GREEN: the Socratic loop creates NO .paper/ / STATE.json / LIBRARY.json before confirm; on confirm it dispatches `new` with a thesis arg. add + sketch are both in REAL_VERB_LOADERS; UX02_VERBS still length 16.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user `<source>` arg → add | untrusted DOI / local path / URL |
| add URL fetch → network | SSRF surface on `add <url>` |
| add remap → section PLAN.md | a write into already-verified section state |
| sketch loop → paper state | premature state creation risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Tampering | section-state corruption on remap | mitigate | remapSections touches ONLY assigned_sources[] via updateFrontmatter; NEVER status or verified_against_draft_hash (Pitfall 3 / A6); withLock + atomicWriteFile, no raw fs.writeFile |
| T-08-04-02 | Spoofing/SSRF | `add <url>` fetch | mitigate | All network via httpFetch (http.ts D-06 chokepoint — per-source rate limits, no raw fetch); DOI normalized via normalizeDoi before any DOI request |
| T-08-04-03 | Tampering | path traversal via `add <pdf>` | mitigate | fs.readFile(path.resolve(source)); only a Buffer is handed to extractPdfText (bytes-only chokepoint T-3-FS-01) |
| T-08-04-04 | Elevation of Privilege | unverified source bypassing the verifier | mitigate | verifyDoi runs at add-time; a FABRICATED verdict still blocks compile via Pass 1 (verifier-blocks-compile non-negotiable preserved — add cannot smuggle a verified source) |
| T-08-04-05 | Tampering | premature state advance in sketch | mitigate | sketch never calls initState / mkdir .paper / initLibrary before confirm (Pitfall 6); asserted by tests/sketch.test.ts |
| T-08-04-06 | Information Disclosure | Crossref hydration leaking PDF content | mitigate | Only the title is sent to crossrefSearch (08-RESEARCH Security row); fetchById sends only the DOI — no PDF body leaves the box |
| T-08-04-SC | Tampering | npm/pip/cargo installs | accept | No installs — citation-js, @clack/prompts, doi-regex already present (08-RESEARCH audit) |
</threat_model>

<verification>
- `node --import tsx --test tests/add-source.test.ts tests/sketch.test.ts` GREEN.
- REAL_VERB_LOADERS has all four Phase-8 verbs; UX02_VERBS length 16.
- `npm run check` passes — no raw fetch, no raw fs.writeFile in add.ts/sketch.ts.
</verification>

<success_criteria>
- add ingests DOI/PDF/URL via the existing chokepoints, writes CITATIONS.bib via writeBibtex, and the remap gate only edits assigned_sources[].
- sketch never advances paper state before confirm; dispatches `new` with a thesis after confirm.
- No 17th verb (16-bijection intact).
</success_criteria>

<output>
Create `.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-04-SUMMARY.md` when done
</output>
