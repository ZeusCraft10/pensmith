---
phase: 09-educator-tutorial-mode-pii-polish
plan: 03
type: execute
wave: 2
depends_on: ["09-00", "09-01", "09-02"]
files_modified:
  - bin/cli/intake.ts
  - bin/cli/goal.ts
  - bin/lib/router.ts
  - bin/pensmith.ts
  - bin/cli/next.ts
  - bin/cli/resume.ts
  - bin/cli/status.ts
  - bin/cli/write.ts
  - bin/lib/prompt-loader.ts
  - tests/repo-files.test.ts
  - tests/zero-trace-export.test.ts
autonomous: true
requirements: [ERGO-07]
must_haves:
  truths:
    - "intake accepts --goal (draft|learning|both, default draft) and persists it to config.toml"
    - "PII redaction (opt-in) runs and produces a reviewable diff BEFORE loadPrompt('intake-clarifier')"
    - "the model-bound payload (the value interpolated into the prompt) is the REDACTED text — raw answers never cross the LLM boundary"
    - "raw answers go to INTAKE.raw.local (gitignored); redacted answers to INTAKE.md"
    - "bin/lib/router.ts stays GOAL-UNAWARE: resolveNextAction takes a goal-AGNOSTIC stopAfterResearch? behavior param; router.ts contains ZERO goal/learning/educator_mode references"
    - "the goal-aware CLI callers (pensmith/next/resume/status) read goal and pass stopAfterResearch=true when goal=learning, hard-stopping after research per PRD §7.13"
    - "when the learning hard-stop fires, the CLI caller renders per-claim research provenance into TUTORIAL.md from LIBRARY.json + RESEARCH.md (the learning end-state)"
    - "goal=draft and goal=both route through outline unchanged (no regression)"
    - "TUTORIAL.md content never appears in any export format"
    - "tutorial prompt slugs carry real SHA-256 hashes (WN-3 re-pin), no pending bypass needed"
  artifacts:
    - path: "bin/cli/intake.ts"
      provides: "--goal arg + config.toml persistence + PII opt-in diff block before loadPrompt + redacted-into-interpolate egress"
      contains: "diffPii"
    - path: "bin/cli/goal.ts"
      provides: "CLI-tier goal helpers: readGoalFromConfig + stopAfterResearchFor(goal) + renderLearningEndState (research-stage TutorialSubscriber emit+flush)"
      contains: "stopAfterResearch"
    - path: "bin/lib/router.ts"
      provides: "goal-AGNOSTIC stopAfterResearch? param on resolveNextAction; NO goal read; NO config.toml access for goal"
      contains: "stopAfterResearch"
    - path: "tests/zero-trace-export.test.ts"
      provides: "TUTORIAL.md export-exclusion assertion"
  key_links:
    - from: "bin/cli/intake.ts redacted answers"
      to: "interpolate(prompt, { seed: <redacted> })"
      via: "the model-bound payload is the redacted text; egress test asserts no raw PII sentinel in the interpolated payload"
      pattern: "interpolate\\("
    - from: "bin/cli/goal.ts stopAfterResearchFor"
      to: "bin/lib/router.ts resolveNextAction(paperRoot, { stopAfterResearch })"
      via: "goal-agnostic DI: CLI maps goal=learning → stopAfterResearch=true; router never sees goal"
      pattern: "stopAfterResearch"
    - from: "bin/cli/goal.ts renderLearningEndState"
      to: ".paper/TUTORIAL.md"
      via: "build research.done payload from LIBRARY.json + RESEARCH.md, emit into TutorialSubscriber, flush"
      pattern: "research.done"
    - from: "bin/lib/prompt-loader.ts EXPECTED_PROMPT_HASHES"
      to: "tests/repo-files.test.ts PENDING_HASH_PINS"
      via: "WN-3 atomic re-pin — both surfaces carry the same real SHA-256"
      pattern: "tutorial-section-provenance"
---

<objective>
Wire the goal field end-to-end (SC-1 + SC-3) while keeping bin/lib/router.ts GOAL-UNAWARE (H1): add `--goal` to intake + persist to config.toml; insert the PII opt-in redact+diff block BEFORE `loadPrompt('intake-clarifier')` AND flow the REDACTED text into the model-bound `interpolate(...)` payload (H3); add a goal-AGNOSTIC `stopAfterResearch?` param to `resolveNextAction` and have the goal-aware CLI callers map goal=learning → stopAfterResearch=true (H1 DI); when the learning hard-stop fires, render per-claim research provenance into TUTORIAL.md from LIBRARY.json + RESEARCH.md (H2 end-state); assert TUTORIAL.md export-exclusion; and perform the WN-3 atomic re-pin of the two tutorial prompt slugs to real SHA-256.

Purpose: SC-1 (goal=learning end-state WITH per-claim provenance; draft/both unchanged), SC-2 (Foundation router stays goal-unaware — DI not a goal branch), SC-3 (PII opt-in, redacted egress by content, deterministic diff), and the resolved open questions (short enum draft|learning|both; learning hard-stops after research; TUTORIAL.md excluded from exports — DONE-07).
Output: intake.ts (goal + redacted egress), goal.ts (CLI-tier DI + end-state render), router.ts (goal-agnostic param), 4 caller wirings, write.ts import dedup, export-exclusion test, real prompt hashes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-educator-tutorial-mode-pii-polish/09-RESEARCH.md
@.planning/phases/09-educator-tutorial-mode-pii-polish/09-PATTERNS.md

<read_first>
- bin/cli/intake.ts — the goal arg + PII block go here; the `loadPrompt('intake-clarifier')` call (~line 267) and the `interpolate(prompt, { seed })` payload (~line 269) are the pre-LLM boundary AND the model-bound payload; resolvePaperMeta (~line 52) shows the smol-toml config.toml read pattern (use stringify for persistence).
- bin/lib/pii.ts — now exports redactPii + diffPii (from 09-01); intake consumes both.
- bin/lib/router.ts — resolveNextAction (~line 121); the RESEARCH.md check (~line 145) and OUTLINE.md check (~line 146) bracket the stop-point; NEVER-THROW invariant (header) and the outer try/catch backstop already cover the body. router.ts MUST remain goal-unaware — do NOT add readGoalBestEffort or any config.toml goal read here.
- bin/pensmith.ts (resolveNextAction call ~line 307), bin/cli/next.ts (~line 32), bin/cli/resume.ts (~line 58), bin/cli/status.ts (~line 66) — the FOUR goal-aware CLI callers of resolveNextAction; each gets the goal read + stopAfterResearch pass-through.
- bin/cli/write.ts (from 09-02) — already has a local `readGoalFromConfig`; replace it with an import of the shared bin/cli/goal.ts version so write.ts + the 4 callers share ONE helper (resolves L5 duplication). DRAFT.md byte output and the existing subscriber wiring stay unchanged — only the goal-read source changes.
- bin/lib/tutorial.ts (from 09-02) — `TutorialSubscriber` with the research.done per-claim render; goal.ts's renderLearningEndState constructs it and emits research.done.
- bin/lib/schemas/source-candidate.ts — LIBRARY.json is SourceCandidate[]; the research.done payload maps each to { citekey, supportedClaim }.
- bin/lib/prompt-loader.ts (EXPECTED_PROMPT_HASHES ~line 91; interpolate) — the two tutorial sentinels to re-pin; interpolate is the egress payload builder.
- tests/repo-files.test.ts (PENDING_HASH_PINS ~line 305) — the matching pin to re-pin in the SAME commit.
- tests/intake-pii-ordering.test.ts, tests/intake-pii-egress.test.ts, tests/goal-routing.test.ts, tests/tutorial-provenance.test.ts — the RED-by-skip suites this plan makes GREEN.
- tests/zero-trace-export.test.ts — extend with the TUTORIAL.md exclusion test.
- bin/lib/exporter.ts (exportDraft ~line 450) — exporter reads ONLY inputPath; TUTORIAL.md exclusion is structural-by-construction, the test pins it.

<interfaces>
From bin/lib/pii.ts (after 09-01): `redactPii(text): string`, `diffPii(original, redacted): PiiDiff[]` where `PiiDiff = { span:[number,number]; kind; raw; tag }`.
From bin/lib/router.ts (after this plan): `resolveNextAction(paperRoot, opts?: { stopAfterResearch?: boolean }): Promise<RouterDecision>`; opts is GOAL-AGNOSTIC (a plain behavior flag); the `{ verb:'status'; reason:'done' }` terminal already exists.
From bin/lib/tutorial.ts (after 09-02): `new TutorialSubscriber({ tutorialPath, goal }).emit({ kind:'research.done', payload:{ sources:[{citekey, supportedClaim}] } }); await flush();`.
From bin/lib/prompt-loader.ts: `loadPrompt(slug)`, `interpolate(template, vars)`.
From smol-toml: `parse(s)`, `stringify(obj)`.
</interfaces>
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: intake --goal + config.toml persistence + PII opt-in diff before loadPrompt + REDACTED text into the interpolate egress</name>
  <files>bin/cli/intake.ts</files>
  <read_first>bin/cli/intake.ts, bin/lib/pii.ts, tests/intake-pii-ordering.test.ts, tests/intake-pii-egress.test.ts</read_first>
  <action>
Add a `goal` arg to intakeCommand (`type:'string'`, `default:'draft'`, description "Workflow goal: draft (default), learning, or both.") — short enum per resolved open question 2; NOT a 17th verb (the 16-verb bijection is unchanged). Validate to one of `draft|learning|both`, coercing anything else to `draft`.

Add a `piiRedact` opt-in (read from a `--pii-redact` boolean arg defaulting false, OR from config.toml `[project] pii_redaction`; the CLI arg WINS when both are present — document this precedence in a comment, resolves L6). PII is OPT-IN per the non-negotiables.

Insert the PII block STRUCTURALLY BEFORE the `loadPrompt('intake-clarifier')` call (ordering — threat T-09-PII-EGRESS, verified by intake-pii-ordering.test.ts source-grep `diffPiiIdx < loadPromptIdx`). The block: collect the raw answer text (the `--from` seed and/or thesis seed — the user's answers, NEVER the prompt template); if piiRedact is on, compute `const redacted = redactPii(rawAnswers); const diff = diffPii(rawAnswers, redacted);` print the diff for review (one line per PiiDiff: `[kind] "raw" → tag`); write raw answers to `.paper/INTAKE.raw.local` via atomicWriteFile (gitignored), and redacted answers to `.paper/INTAKE.md` via atomicWriteFile. If piiRedact is off, write raw answers to INTAKE.md as today.

CRITICAL (H3 — egress by content, not just ordering): the value handed to the model MUST be the redacted text. In BOTH the noLlm branch and the Tier-1 branch, the variable interpolated into the prompt — `interpolate(prompt, { seed: <X> })` — MUST be `X = redacted` when piiRedact is on (and `rawAnswers` only when piiRedact is off). Bind a single `const egressSeed = piiRedact ? redacted : rawAnswers;` and pass `egressSeed` into `interpolate(...)` (and into any other model/network-bound payload constructed in intake). Do NOT pass the raw `--from` file contents or `rawAnswers` into interpolate when redaction is on — the egress test (intake-pii-egress.test.ts) captures the interpolated payload and FAILS if any raw PII sentinel survives. The redacted text (carrying `[REDACTED:...]` tags) is what crosses the LLM boundary. Only AFTER the PII block + egressSeed binding may `loadPrompt('intake-clarifier')` and `interpolate(...)` run. Apply this in BOTH branches so the structural-ordering grep AND the egress-content assertion hold in the live path.

Persist `goal` (and pii_redaction if set via arg) into config.toml: read existing config.toml with `parseToml` (best-effort, default `{}`), set `project.goal`, re-serialize with `stringify`, and write via atomicWriteFile (D-07 chokepoint — same as all state files). Per RESEARCH assumption A2 + PRD §10, config.toml is the canonical store; do NOT add a STATE.json field. Keep the write best-effort/non-fatal (a malformed config.toml must not break intake — mirror resolvePaperMeta's try/catch). On persist failure, emit a VISIBLE stderr WARN AND keep the selected goal in-memory for THIS session's downstream behavior (M1 robustness — a silent persist failure must not strand the learning goal; the warn makes the degradation observable).
  </action>
  <verify>
    <automated>node --import tsx --test tests/intake-pii-ordering.test.ts tests/intake-pii-egress.test.ts 2>&1 | grep -E "pass|fail" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - source: in bin/cli/intake.ts, `indexOf('diffPii') < indexOf("loadPrompt('intake-clarifier')")` (PII before LLM)
    - source: intake binds `egressSeed = piiRedact ? redacted : rawAnswers` and passes egressSeed (NOT raw `--from`/rawAnswers) into `interpolate(...)` when redaction is on
    - source: intake has a `goal` arg defaulting `'draft'` and writes `project.goal` to config.toml via atomicWriteFile + smol-toml stringify; persist failure WARNs to stderr
    - behavior: with PII opt-in, INTAKE.md is redacted, INTAKE.raw.local holds the raw email, AND the captured interpolate payload contains NO raw PII sentinel (intake-pii-egress.test.ts green); with opt-out, INTAKE.md keeps raw and no INTAKE.raw.local
    - test-command: `node --import tsx --test tests/intake-pii-ordering.test.ts tests/intake-pii-egress.test.ts` green (no longer skipped)
  </acceptance_criteria>
  <done>intake persists goal to config.toml, runs opt-in PII redaction+diff before any LLM call, and hands the REDACTED text (not raw answers) to interpolate — raw → INTAKE.raw.local, redacted → INTAKE.md, redacted → model payload.</done>
</task>

<task type="auto">
  <name>Task 2: Goal-AGNOSTIC stopAfterResearch DI on router (router stays goal-unaware) + CLI goal helper + learning end-state render + 4-caller wiring + write.ts dedup</name>
  <files>bin/lib/router.ts, bin/cli/goal.ts, bin/pensmith.ts, bin/cli/next.ts, bin/cli/resume.ts, bin/cli/status.ts, bin/cli/write.ts</files>
  <read_first>bin/lib/router.ts, bin/cli/goal.ts, bin/pensmith.ts, bin/cli/next.ts, bin/cli/resume.ts, bin/cli/status.ts, bin/cli/write.ts, bin/lib/tutorial.ts, bin/lib/schemas/source-candidate.ts, tests/goal-routing.test.ts, tests/tutorial-provenance.test.ts</read_first>
  <action>
ROUTER (H1 — keep bin/lib/router.ts GOAL-UNAWARE): change `resolveNextAction(paperRoot)` to `resolveNextAction(paperRoot, opts: { stopAfterResearch?: boolean } = {})`. opts is a GOAL-AGNOSTIC behavior flag — it is NOT `goal`, NOT `educator_mode`, NOT `learning`. Insert the stop AFTER the RESEARCH.md existsSync check and BEFORE the OUTLINE.md check: `if (opts.stopAfterResearch && existsSync(join(pDir, 'RESEARCH.md'))) return { verb: 'status', reason: 'done' };` (reuse the existing terminal; do NOT widen RouterDecision). router.ts must contain ZERO `goal`/`learning`/`educator_mode` references and NO config.toml read — the lint-tutorial-no-branch invariant now SCANS router.ts and asserts zero such tokens. REMOVE any prior plan's `readGoalBestEffort`, `goal === 'learning'` branch, `const LEARNING` dodge, and router exclusion — none of those exist anymore. (M8 note: reusing `status/done` carries export-ready messaging; the CLI caller that triggers stopAfterResearch is responsible for printing a learning-appropriate end-state message AFTER rendering TUTORIAL.md — see goal.ts below — so the misleading "ready to export" line is replaced/suppressed in the learning path.)

CLI GOAL HELPER (bin/cli/goal.ts — the goal-aware tier): export (a) `readGoalFromConfig(paperRoot): 'draft'|'learning'|'both'` — best-effort smol-toml parse of config.toml `[project] goal`, default `'draft'` on any error (this becomes the SINGLE shared goal-read helper; replace write.ts's local copy with an import of this one, resolving L5); (b) `stopAfterResearchFor(goal): boolean` — returns `goal === 'learning'` (this is the ONLY place the goal→behavior mapping lives; it is in the CLI tier, NOT Foundation); (c) `async renderLearningEndState(paperRoot): Promise<void>` — build the research.done payload from LIBRARY.json (SourceCandidate[]) + RESEARCH.md (the per-source `supports:` claim lines), construct `new TutorialSubscriber({ tutorialPath: <paperDir>/TUTORIAL.md, goal:'learning' })`, `emit({ kind:'research.done', payload:{ sources } })`, `await flush()`, then print a learning-appropriate stderr/stdout message (e.g. "Learning mode: wrote per-claim source provenance to TUTORIAL.md; stopping after research per your goal=learning."). Wrap in try/catch non-fatal (mirror runStyleProducerNonFatal). This is the H2 end-state: per-claim provenance produced at the hard-stop, from research-stage data, with NO section written.

WRITE.TS DEDUP (L5): replace bin/cli/write.ts's local `readGoalFromConfig` (added in 09-02) with an import of `readGoalFromConfig` from bin/cli/goal.ts. The subscriber construction + emit + flush wiring from 09-02 is unchanged; DRAFT.md byte output stays identical. This is a pure refactor — one helper, two consumers.

4-CALLER WIRING (the goal-aware tier maps goal→stopAfterResearch and renders the end-state): in EACH of bin/pensmith.ts (bare-dispatch ~line 307), bin/cli/next.ts (~line 32), bin/cli/resume.ts (~line 58): read `const goal = readGoalFromConfig(paperRoot); const stop = stopAfterResearchFor(goal);` then call `resolveNextAction(paperRoot, { stopAfterResearch: stop })`. When `stop === true` AND the decision is the learning terminal (status/done reached via the stop), call `await renderLearningEndState(paperRoot)` BEFORE/INSTEAD OF dispatching the status verb's export-ready message (so the user sees the learning end-state, not "ready to export"). For bin/cli/status.ts (~line 66, read-only summary): pass `{ stopAfterResearch: stop }` so the surfaced "next" line reflects the hard-stop, but status.ts does NOT render TUTORIAL.md (it is a read-only status view — rendering belongs to the action-taking callers). Keep all existing NEVER-THROW + flag-forwarding behavior intact. Any caller wanting the old behavior simply omits opts (default `{}` → no stop → identical to today, so draft/both are byte-unchanged: no regression).

These wirings are in bin/cli/* and bin/pensmith.ts (the goal-aware tier) — they read goal and contain the `goal === 'learning'` mapping inside stopAfterResearchFor. The lint invariant does NOT scan bin/cli/* or bin/pensmith.ts (only bin/lib/** + workflows/**), so the goal-aware mapping living in goal.ts is correct-by-architecture, not an exclusion.
  </action>
  <verify>
    <automated>node --import tsx --test tests/goal-routing.test.ts tests/tutorial-provenance.test.ts tests/lint-tutorial-no-branch.test.ts 2>&1 | grep -E "pass|fail" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - source: bin/lib/router.ts has resolveNextAction(paperRoot, { stopAfterResearch? }) and contains ZERO `goal`/`learning`/`educator_mode` tokens and NO config.toml read (lint-tutorial-no-branch.test.ts scans router.ts and passes)
    - source: bin/cli/goal.ts exports readGoalFromConfig, stopAfterResearchFor, renderLearningEndState; the `goal === 'learning'` mapping lives ONLY here; write.ts imports readGoalFromConfig from it (no duplicate)
    - behavior: resolveNextAction(root, { stopAfterResearch:true }) + RESEARCH.md present → status/done; resolveNextAction(root) (default) + RESEARCH.md present → outline (no regression for draft/both)
    - behavior: goal=learning end-to-end → renderLearningEndState writes TUTORIAL.md with ≥1 per-claim provenance line, no section ever written (tutorial-provenance RESEARCH-stage test green)
    - test-command: `npm test` full suite green; lint-tutorial-no-branch passes WITH router.ts scanned (no exclusion)
  </acceptance_criteria>
  <done>router.ts stays goal-unaware (goal-agnostic stopAfterResearch DI); the CLI tier owns goal→stop mapping in goal.ts (one shared helper, write.ts deduped); the learning hard-stop renders per-claim research provenance to TUTORIAL.md; draft/both unchanged; the zero-branch invariant passes scanning ALL of bin/lib/** incl router.ts.</done>
</task>

<task type="auto">
  <name>Task 3: TUTORIAL.md export-exclusion test + WN-3 atomic re-pin of tutorial prompt slugs to real SHA-256</name>
  <files>tests/zero-trace-export.test.ts, bin/lib/prompt-loader.ts, tests/repo-files.test.ts</files>
  <read_first>tests/zero-trace-export.test.ts, bin/lib/exporter.ts, bin/lib/prompt-loader.ts, tests/repo-files.test.ts, templates/prompts/tutorial-section-provenance.md, templates/prompts/tutorial-research-rationale.md</read_first>
  <action>
In tests/zero-trace-export.test.ts add a test: create a `.paper/` with a DRAFT.md (the compiled paper) AND a TUTORIAL.md containing a unique sentinel string (e.g. `TUTORIAL_SENTINEL_PROVENANCE`). Run `exportDraft` with inputPath = DRAFT.md for the `md` format (offline, no Pandoc), then assert the exported output does NOT contain `TUTORIAL_SENTINEL_PROVENANCE` and does NOT contain `pensmith`. (Scope the `pensmith` assertion to the export bytes the exporter PRODUCES, not arbitrary paper content the author wrote — assert on the exporter's own emitted wrapper/metadata region so legitimate paper prose mentioning "pensmith" cannot false-positive, addressing the codex M-note.) This pins the structural guarantee that the exporter reads only inputPath and never concatenates TUTORIAL.md (DONE-07 / resolved open question 3).

WN-3 RE-PIN: compute the real SHA-256 of each finalized tutorial prompt file (the byte content as committed in 09-00, UNCHANGED — add a guard comment that the two files must be byte-identical to their 09-00 form, resolving L3). In a SINGLE atomic edit, replace `'tutorial-section-provenance': '__PENDING_HASH_tutorial-section-provenance__'` and `'tutorial-research-rationale': '__PENDING_HASH_tutorial-research-rationale__'` in bin/lib/prompt-loader.ts EXPECTED_PROMPT_HASHES with the real hashes, AND replace the matching PENDING_HASH_PINS entries in tests/repo-files.test.ts with the SAME hashes (WN-3 lockstep — both surfaces re-pin together so drift is structurally impossible; mirror the smoother/claim-support precedent). After this, `loadPrompt('tutorial-section-provenance')` and `loadPrompt('tutorial-research-rationale')` resolve WITHOUT PENSMITH_ALLOW_PENDING_PROMPT_HASHES — runtime drift detection is restored.
  </action>
  <verify>
    <automated>node --import tsx --test tests/zero-trace-export.test.ts tests/repo-files.test.ts 2>&1 | grep -E "pass|fail" | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - behavior: exported md does not contain the TUTORIAL sentinel; the exporter's emitted wrapper region contains no `pensmith` fingerprint
    - source: neither prompt-loader.ts nor repo-files.test.ts contains `__PENDING_HASH_tutorial` after this task
    - behavior: loadPrompt resolves both tutorial slugs without the pending-hash bypass env var
    - test-command: `npm test` full suite green WITHOUT PENSMITH_ALLOW_PENDING_PROMPT_HASHES set
  </acceptance_criteria>
  <done>TUTORIAL.md is provably export-excluded; both tutorial prompt slugs carry real SHA-256 hashes in lockstep across the loader and repo-files; no pending bypass remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user intake answers → LLM/network | PII must be redacted before crossing into loadPrompt/interpolate/the model; the egress payload (interpolate vars) must be the redacted text |
| config.toml goal → CLI caller → router | goal is read in the goal-aware CLI tier and mapped to a goal-agnostic stopAfterResearch flag; router never sees goal; a corrupt config must not crash routing |
| tutorial prompt files → loader | unpinned prompt must not reach production; re-pin restores drift detection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Information Disclosure | PII egress before redaction | mitigate | redactPii+diffPii block STRUCTURALLY precedes loadPrompt('intake-clarifier') AND the REDACTED text (egressSeed) is the value interpolated into the model payload; intake-pii-ordering asserts ordering, intake-pii-egress asserts no raw sentinel in the captured payload (content) |
| T-09-03-02 | Information Disclosure | raw PII persisted/committed | mitigate | raw → INTAKE.raw.local (gitignored, 09-00); redacted → INTAKE.md; atomicWriteFile chokepoint |
| T-09-03-03 | Tampering | educator-mode branch leakage via router (H1) | mitigate | router takes a goal-AGNOSTIC stopAfterResearch flag (DI) and contains ZERO goal/learning/educator_mode tokens; lint-tutorial-no-branch.test.ts SCANS router.ts (no exclusion) and asserts zero; goal→stop mapping lives only in bin/cli/goal.ts (goal-aware tier, not scanned) |
| T-09-03-04 | Information Disclosure | learning mode produces no per-claim provenance (H2) | mitigate | the CLI caller renders research.done per-claim provenance to TUTORIAL.md at the hard-stop from LIBRARY.json + RESEARCH.md; tutorial-provenance RESEARCH-stage test asserts ≥1 citekey+claim line with no section written |
| T-09-03-05 | Information Disclosure | TUTORIAL.md leaking into exports | mitigate | exporter reads only inputPath (structural); zero-trace test pins a TUTORIAL sentinel absent from exports (DONE-07) |
| T-09-03-06 | Tampering | unpinned tutorial prompt reaching production | mitigate | WN-3 atomic re-pin to real SHA-256 across loader + repo-files in one commit; drift detection restored |
| T-09-03-07 | Denial of Service | corrupt config.toml crashing routing | mitigate | readGoalFromConfig (CLI tier) never-throws (existsSync + try/catch → 'draft'); persist failure WARNs + keeps in-memory goal (M1); router's outer backstop covers it |
</threat_model>

<verification>
- `node --import tsx --test tests/intake-pii-ordering.test.ts tests/intake-pii-egress.test.ts tests/goal-routing.test.ts tests/tutorial-provenance.test.ts tests/zero-trace-export.test.ts tests/lint-tutorial-no-branch.test.ts tests/repo-files.test.ts` — all green.
- `npm test` full suite green WITHOUT PENSMITH_ALLOW_PENDING_PROMPT_HASHES.
- `npm run check` (lint + typecheck + build + tier-contract + tests + manifests) green before /gsd:verify-work.
- grep: `diffPii` precedes `loadPrompt('intake-clarifier')` in intake.ts; `egressSeed` (redacted) is what interpolate receives; bin/lib/router.ts has zero `goal`/`learning`/`educator_mode` tokens.
</verification>

<success_criteria>
goal is persisted + drives the learning hard-stop after research via a goal-AGNOSTIC router param (router stays goal-unaware, invariant scans it clean); the learning hard-stop renders per-claim research provenance to TUTORIAL.md (SC-1 for learning); opt-in PII runs before any LLM call with a deterministic reviewable diff AND the model-bound payload is the redacted text (SC-3 egress by content); draft/both unchanged; TUTORIAL.md is provably export-excluded; tutorial prompts carry real hashes. ERGO-07 fully satisfied.
</success_criteria>

<output>
Create `.planning/phases/09-educator-tutorial-mode-pii-polish/09-03-SUMMARY.md` when done.
</output>
</content>
