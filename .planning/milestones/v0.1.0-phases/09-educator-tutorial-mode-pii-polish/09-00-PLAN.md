---
phase: 09-educator-tutorial-mode-pii-polish
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/pii-polish.test.ts
  - tests/tutorial-observer.test.ts
  - tests/goal-routing.test.ts
  - tests/tutorial-provenance.test.ts
  - tests/intake-pii-ordering.test.ts
  - tests/intake-pii-egress.test.ts
  - tests/lint-tutorial-no-branch.test.ts
  - tests/fixtures/pii-polish-corpus.ts
  - tests/fixtures/tutorial-paper/OUTLINE.md
  - tests/fixtures/tutorial-paper/sections/01-background/PLAN.md
  - tests/fixtures/tutorial-paper/RESEARCH.md
  - tests/fixtures/tutorial-paper/LIBRARY.json
  - bin/lib/tutorial.ts
  - templates/prompts/tutorial-section-provenance.md
  - templates/prompts/tutorial-research-rationale.md
  - bin/lib/prompt-loader.ts
  - tests/repo-files.test.ts
  - .gitignore
autonomous: true
requirements: [ERGO-07]
must_haves:
  truths:
    - "Full suite stays GREEN: every new Phase-9 test is RED-by-skip until its target is wired"
    - "A grep-invariant test exists asserting zero educator-mode branches in ALL of Foundation (bin/lib/**, INCLUDING router.ts) + workflows/**"
    - "A goal-unawareness test exists asserting bin/lib/router.ts contains ZERO `goal`/`learning`/`educator_mode` references AND resolveNextAction takes a goal-AGNOSTIC `stopAfterResearch?` behavior param (DI), not a config goal read"
    - "A source-ordering test exists asserting redactPii/diffPii precede loadPrompt in intake.ts"
    - "An egress test exists asserting the LLM-bound payload (interpolated prompt) carries NO raw PII sentinel — redaction is by-content, not merely by-ordering"
    - "A learning-mode per-claim provenance test exists asserting goal=learning produces ≥1 per-claim source annotation from RESEARCH-stage data WITHOUT any section being written"
    - "TUTORIAL.md and INTAKE.raw.local are gitignored"
  artifacts:
    - path: "tests/pii-polish.test.ts"
      provides: "RED-by-skip corpus + diffPii determinism/idempotence + NAME-suppression suite"
    - path: "tests/tutorial-observer.test.ts"
      provides: "RED-by-skip subscriber activation + zero-branch grep-invariant + router goal-unawareness assertion"
    - path: "tests/goal-routing.test.ts"
      provides: "RED-by-skip goal enum + learning stop-point via stopAfterResearch DI + draft/both no-regression"
    - path: "tests/tutorial-provenance.test.ts"
      provides: "RED-by-skip section-level per-claim provenance + RESEARCH-stage (no-section) learning per-claim provenance + TUTORIAL.md export-exclusion"
    - path: "tests/intake-pii-ordering.test.ts"
      provides: "RED-by-skip source-ordering gate + INTAKE.raw.local write"
    - path: "tests/intake-pii-egress.test.ts"
      provides: "RED-by-skip offline egress test: model-bound payload carries no raw PII sentinel"
    - path: "tests/lint-tutorial-no-branch.test.ts"
      provides: "filesystem-scan zero-branch invariant over ALL bin/lib/** (incl router.ts) + workflows/**, only tutorial.ts excluded"
    - path: "bin/lib/tutorial.ts"
      provides: "TutorialSubscriber stub (types + class shell; body filled in 09-02)"
      contains: "TutorialSubscriber"
    - path: "tests/fixtures/tutorial-paper/RESEARCH.md"
      provides: "RESEARCH-stage fixture: curated source → claim/topic-facet mapping for learning-mode per-claim provenance"
    - path: "tests/fixtures/tutorial-paper/LIBRARY.json"
      provides: "SourceCandidate[] fixture the research.done payload renders per-claim provenance from"
    - path: "templates/prompts/tutorial-section-provenance.md"
      provides: "hash-pinned teaching-wrapper prompt"
    - path: "templates/prompts/tutorial-research-rationale.md"
      provides: "hash-pinned research-rationale prompt"
  key_links:
    - from: "tests/repo-files.test.ts PENDING_HASH_PINS"
      to: "bin/lib/prompt-loader.ts EXPECTED_PROMPT_HASHES"
      via: "WN-3 lockstep sentinel for tutorial prompt slugs"
      pattern: "tutorial-section-provenance"
    - from: "tests/lint-tutorial-no-branch.test.ts"
      to: "bin/lib/**/*.ts (INCLUDING router.ts) + workflows/**/*.md"
      via: "fs.readdirSync recursive grep scan; ONLY tutorial.ts excluded"
      pattern: "educator_mode|goal\\s*===|goal\\s*!==|\\bgoal\\b|learning|TutorialSubscriber"
    - from: "tests/goal-routing.test.ts"
      to: "bin/lib/router.ts resolveNextAction"
      via: "goal-agnostic stopAfterResearch DI param set by the goal-aware CLI caller"
      pattern: "stopAfterResearch"
---

<objective>
Land the Phase-9 RED scaffold: all new test files, fixtures, the TutorialSubscriber stub, the two hash-pinned tutorial prompts (as WN-3 sentinels), and the .gitignore additions — BEFORE any implementation. Every new test is RED-by-skip (guarded on existsSync/source-grep of its not-yet-wired target) so the full suite stays GREEN throughout Waves 1-2.

Purpose: Establish the testable contracts (zero-branch invariant scanning ALL of Foundation incl router.ts, router goal-UNAWARENESS via DI, PII-before-loadPrompt ordering, PII egress-by-content, goal routing via a goal-agnostic behavior flag, RESEARCH-stage per-claim provenance for learning mode, section-level per-claim provenance for both mode, deterministic diff) so Waves 1-2 implement against fixed acceptance criteria. RED-by-skip mirrors the established Phase 5/6/8 Wave-0 pattern.
Output: 7 new test files, 5 fixtures, tutorial.ts stub, 2 prompt files + sentinels, .gitignore entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-educator-tutorial-mode-pii-polish/09-RESEARCH.md
@.planning/phases/09-educator-tutorial-mode-pii-polish/09-PATTERNS.md

<read_first>
- tests/pii.test.ts — EXACT structure to copy for tests/pii-polish.test.ts (corpus loop + idempotence + fast-check property)
- tests/fixtures/pii-corpus.ts — POSITIVES/NEGATIVES/KEY_FIXTURES shape to mirror in pii-polish-corpus.ts
- tests/intake-style-producer.test.ts — source-grep skip-predicate pattern (the `*Wired()` helper + `{ skip: !READY }`)
- tests/write-orchestrator.test.ts — stub-callback sink pattern for tutorial-observer.test.ts
- tests/library.test.ts — mkPaperRoot() tmpdir env-override isolation (LOCALAPPDATA/XDG_DATA_HOME/HOME)
- tests/lint-chokepoint.test.ts — grep-style invariant test pattern for lint-tutorial-no-branch.test.ts
- tests/cassette-no-leak.test.ts — the committed-fixture string-scan pattern to mirror for the PII-egress payload scan (assert a sentinel string is ABSENT from a captured payload)
- tests/repo-files.test.ts (PENDING_HASH_PINS ~line 305) — WN-3 sentinel + lockstep pin pattern for tutorial prompts
- bin/lib/prompt-loader.ts (EXPECTED_PROMPT_HASHES ~line 91) — slug registration; sentinel format `__PENDING_HASH_<slug>__`; `interpolate(prompt, vars)` is the model-bound payload builder used by intake (the egress seam)
- bin/lib/router.ts (resolveNextAction ~line 121; RESEARCH.md check ~line 145; OUTLINE.md check ~line 146) — the resolver MUST stay goal-UNAWARE; the stop-point is driven by a goal-agnostic `stopAfterResearch?` param injected by the CLI caller (see H1 fix in 09-03)
- bin/lib/session-log.ts (enqueue ~line 251, makeLogger ~line 351) — the chain/enqueue + never-throw emit pattern to copy into tutorial.ts stub typing
- bin/lib/schemas/source-candidate.ts — SourceCandidate shape (citekey, title, authors, year) the research.done payload + LIBRARY.json fixture use
- bin/lib/exporter.ts (exportDraft ~line 450) — exporter reads a SINGLE inputPath (DRAFT.md/FINAL.md), never TUTORIAL.md; the export-exclusion guarantee is structural-by-construction (test it, do not add exclusion code)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PII-polish RED suite + corpus fixture + tutorial-prompt sentinels</name>
  <files>tests/pii-polish.test.ts, tests/fixtures/pii-polish-corpus.ts, templates/prompts/tutorial-section-provenance.md, templates/prompts/tutorial-research-rationale.md, bin/lib/prompt-loader.ts, tests/repo-files.test.ts</files>
  <read_first>tests/pii.test.ts, tests/fixtures/pii-corpus.ts, bin/lib/prompt-loader.ts, tests/repo-files.test.ts</read_first>
  <action>
Create tests/fixtures/pii-polish-corpus.ts exporting PII_POLISH_POSITIVES (each `{ input, kind, raw }`) covering: IP (`192.168.1.1` → kind IP), IBAN-like (`GB29NWBK60161331926819` → kind IBAN), and NAME-suppression cases. Include NAME_SUPPRESS_NEGATIVES (must NOT match NAME: `Results Section`, `Methods Discussion`, `January March`) and NAME_TWO_TOKEN_POSITIVES (MUST match NAME: `Jane Smith`, `In Smith` — last token not in the suppression set per RESEARCH Pitfall 6). Export DIFF_CASES for diffPii determinism. ALSO export a single canonical PII egress sentinel set `PII_EGRESS_SENTINELS` (used by tests/intake-pii-egress.test.ts in Task 3): `{ email:'leak.sentinel@example.test', ssn:'123-45-6789', name:'Jane Sentinel' }` — distinctive fake values that MUST be redacted out of any model-bound payload.

Create tests/pii-polish.test.ts mirroring tests/pii.test.ts structure. Guard with a RED-by-skip predicate `piiPolishReady()` that checks bin/lib/pii.ts source contains `diffPii` AND `RE_IP` (or exports `diffPii`); each test runs `{ skip: !READY }`. Tests: (a) classifyPii finds IP/IBAN positives; (b) NAME suppression — suppress-negatives produce NO NAME match, two-token positives DO; (c) diffPii determinism: `assert.deepEqual(diffPii(x,y), diffPii(x,y))`; (d) diffPii idempotence: `diffPii(redactPii(x), redactPii(redactPii(x))).length === 0`; (e) diffPii purity: no Date.now/Math.random in output (assert structural shape `{span,kind,raw,tag}` with tag === `[REDACTED:${kind}]`); (f) fast-check property: redactPii output contains no raw IP from an IP arbitrary.

Create templates/prompts/tutorial-section-provenance.md and templates/prompts/tutorial-research-rationale.md as teaching-wrapper prompt bodies (YAML frontmatter with `slug:` + body prose; the body instructs the model to explain why each assigned source supports a claim, per RESEARCH Pattern 2). These are pensmith-controlled strings — contain NO user PII placeholders.

Register both slugs in bin/lib/prompt-loader.ts EXPECTED_PROMPT_HASHES as WN-3 sentinels: `'tutorial-section-provenance': '__PENDING_HASH_tutorial-section-provenance__'` and `'tutorial-research-rationale': '__PENDING_HASH_tutorial-research-rationale__'`, with a `// Phase 9 D-12` comment. Add matching PENDING_HASH_PINS entries in tests/repo-files.test.ts pointing at the two template paths with `__PENDING_HASH_<slug>__` hashes (lockstep — both surfaces carry the sentinel; the real SHA-256 re-pin happens in 09-03).
  </action>
  <verify>
    <automated>node --import tsx --test tests/pii-polish.test.ts 2>&1 | grep -E "pass|skip" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - source: tests/pii-polish.test.ts uses a source-grep `piiPolishReady()` predicate and `{ skip: !READY }` on every test
    - source: tests/fixtures/pii-polish-corpus.ts exports PII_EGRESS_SENTINELS (email/ssn/name fake sentinels) for the Task-3 egress test
    - behavior: full suite `npm test` reports 0 failures (new tests skip until 09-01 wires diffPii)
    - test-command: `node --import tsx --test tests/pii-polish.test.ts` exits 0 with skips, no failures
    - source: both tutorial prompt slugs appear in EXPECTED_PROMPT_HASHES AND PENDING_HASH_PINS as `__PENDING_HASH_<slug>__`
  </acceptance_criteria>
  <done>pii-polish RED suite + corpus (incl PII_EGRESS_SENTINELS) exist (RED-by-skip); both tutorial prompts exist with WN-3 sentinels in lockstep across prompt-loader.ts and repo-files.test.ts; full suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Tutorial/goal/provenance RED suites + router goal-unawareness invariant + tutorial.ts stub + paper+research fixtures</name>
  <files>tests/tutorial-observer.test.ts, tests/goal-routing.test.ts, tests/tutorial-provenance.test.ts, tests/lint-tutorial-no-branch.test.ts, tests/fixtures/tutorial-paper/OUTLINE.md, tests/fixtures/tutorial-paper/sections/01-background/PLAN.md, tests/fixtures/tutorial-paper/RESEARCH.md, tests/fixtures/tutorial-paper/LIBRARY.json, bin/lib/tutorial.ts</files>
  <read_first>tests/write-orchestrator.test.ts, tests/library.test.ts, tests/lint-chokepoint.test.ts, tests/intake-style-producer.test.ts, bin/lib/session-log.ts, bin/lib/schemas/plan-frontmatter.ts, bin/lib/schemas/source-candidate.ts, bin/lib/router.ts</read_first>
  <action>
Create bin/lib/tutorial.ts as a STUB: export `TutorialEventKind` union (`'research.done' | 'outline.done' | 'section.written' | 'section.verified' | 'compile.done'`), `TutorialEvent` interface (`{ kind; payload }`), and the `TutorialSubscriber` class shell with constructor `{ tutorialPath: string; goal: 'learning' | 'both' }`, an `emit(event): void` method, and a `flush(): Promise<void>` method. Stub bodies may be minimal (e.g. emit enqueues a no-op, flush awaits a resolved chain) — Wave 1 (09-02) fills the real rendering. Import only `node:events`, `./atomic-write.js`, `node:path` (NOT pii.js — subscriber handles post-redaction metadata only). The stub MUST compile under tsc --noEmit.

Create tests/fixtures/tutorial-paper/ : OUTLINE.md (a minimal GFM-table outline with one section `01-background`) and sections/01-background/PLAN.md with frontmatter including `assigned_sources: [smith2021, jones2019]` (valid per PlanFrontmatterSchema) plus a `## Brief` body. This is the SECTION-stage provenance source fixture (for goal=both, post-section).

ALSO create tests/fixtures/tutorial-paper/RESEARCH.md and tests/fixtures/tutorial-paper/LIBRARY.json — the RESEARCH-STAGE provenance fixtures (for goal=learning, BEFORE any section). RESEARCH.md is a curated source list where each source is mapped to the claim/topic-facet it supports (one `### <citekey>` block per source with a "supports:" claim line — e.g. `### smith2021` → "supports the claim that transformer attention scales sub-quadratically"). LIBRARY.json is a SourceCandidate[] (valid per SourceCandidateSchema: citekey, title, authors, year, source, id, last_verified) with at least smith2021 and jones2019. This pins the data the research.done render produces per-claim provenance from.

Create tests/tutorial-observer.test.ts (RED-by-skip on tutorial.ts exporting a functional subscriber): (a) subscriber activation — `new TutorialSubscriber({...})` for goal=learning records section.written emits; (b) goal=draft zero-activation — assert when no subscriber is created TUTORIAL.md is never written; (c) never-throw — a malformed payload to emit() does not throw. Use mkPaperRoot() tmpdir isolation copied from library.test.ts. ALSO include the ROUTER GOAL-UNAWARENESS assertion here (NOT skip-guarded — it must pass NOW and KEEP passing): read bin/lib/router.ts source and assert it contains ZERO matches of `/\bgoal\b|learning|educator_mode/` (router must never read goal); AND assert resolveNextAction's signature accepts a goal-agnostic options surface — grep the source for `stopAfterResearch` will be ABSENT in 09-00 (router unchanged) so phrase this as a FORWARD-LOOKING skip-guarded test `{ skip: !routerHasStopParam() }` where `routerHasStopParam()` greps for `stopAfterResearch` — it goes GREEN in 09-03 when the DI param lands. The goal-unawareness (zero `goal` token) assertion is the NON-SKIP guard and is the load-bearing invariant.

Create tests/lint-tutorial-no-branch.test.ts — the zero-branch invariant. Use fs.readdirSync over bin/lib/ RECURSIVELY (into subdirs like verify/, schemas/, migrations/) EXCLUDING ONLY tutorial.ts, and over workflows/**/*.md RECURSIVELY. Assert each file does NOT match the AUTHORITATIVE pattern `/(educator_mode|TutorialSubscriber|\bgoal\b|\blearning\b|\beducator\b)/` (one canonical pattern — catches `goal === 'x'`, `goal !== 'draft'`, `goal === LEARNING`, `switch(goal)`, `isLearningGoal`, helper-extraction, and the const-hoist dodge, because it matches the bare `goal`/`learning` token, not just the `=== 'learning'` literal). router.ts IS scanned (NO exclusion). This test is NOT skip-guarded — it must PASS now (clean codebase: router.ts currently has zero `goal` tokens) and KEEP passing through Waves 1-2. To avoid comment-only false positives, strip `^\s*//` and `^\s*\*` comment lines AND markdown comment prose before matching (mirror lint-chokepoint's comment-strip), so a `// goal-aware` doc comment cannot self-invalidate the gate; the forbidden tokens must be matched in CODE only.

Create tests/goal-routing.test.ts (RED-by-skip on the DI mechanism landing — grep router.ts for `stopAfterResearch`): (a) goal enum is `draft|learning|both`; (b) DI behavior — `resolveNextAction(paperRoot, { stopAfterResearch: true })` with RESEARCH.md present routes to a terminal (status/done) NOT outline; (c) `resolveNextAction(paperRoot, { stopAfterResearch: false })` (and the no-arg default) with RESEARCH.md present routes to outline (no-regression for draft/both). Seed a tmp paper dir with STATE.json + RESEARCH.md. NOTE: the test exercises router via the GOAL-AGNOSTIC param ONLY — it never asserts router reads config.toml goal (it must not). The goal→stopAfterResearch mapping is the CLI caller's job, covered by 09-03's wiring + the egress/ordering suites; goal-routing.test.ts pins ONLY that the behavior flag steers the resolver.

Create tests/tutorial-provenance.test.ts (RED-by-skip on tutorial.ts rendering): (a) SECTION-stage — after a section.written event with assignedSources, TUTORIAL.md contains a `## Section` provenance header naming the citekeys (goal=both path); (b) RESEARCH-stage per-claim (the H2 contract) — after a `research.done` event whose payload carries the curated sources + per-source claim mapping (from the RESEARCH.md / LIBRARY.json fixture), TUTORIAL.md contains AT LEAST ONE per-claim provenance line that names a citekey AND its supported claim, asserted WITHOUT any section.written event ever being emitted (proves learning mode produces per-claim provenance at/before the hard-stop); (c) TUTORIAL.md content does NOT reference `.paper/sections/` paths; (d) export-exclusion — feed exportDraft an inputPath of a DRAFT.md and assert no TUTORIAL.md content leaks into the export (structural: exporter reads only inputPath). Use the tutorial-paper fixtures.
  </action>
  <verify>
    <automated>node --import tsx --test tests/lint-tutorial-no-branch.test.ts tests/tutorial-observer.test.ts 2>&1 | grep -E "pass|fail" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - source: bin/lib/tutorial.ts exports TutorialSubscriber, TutorialEvent, TutorialEventKind and compiles under `tsc --noEmit`
    - behavior: lint-tutorial-no-branch.test.ts PASSES now (not skipped) and SCANS router.ts (no router exclusion) — codebase is clean
    - behavior: tutorial-observer.test.ts router goal-unawareness assertion PASSES now (router.ts has zero `goal`/`learning`/`educator_mode` tokens)
    - source: tests/tutorial-provenance.test.ts has a RESEARCH-stage per-claim test asserting ≥1 citekey+claim line in TUTORIAL.md from a research.done event WITHOUT any section.written
    - test-command: `node --import tsx --test tests/goal-routing.test.ts tests/tutorial-provenance.test.ts` exits 0 with skips, no failures
    - source: tests/fixtures/tutorial-paper/{RESEARCH.md,LIBRARY.json} exist; PLAN.md has `assigned_sources` valid against PlanFrontmatterSchema; LIBRARY.json valid against SourceCandidateSchema
  </acceptance_criteria>
  <done>tutorial.ts stub compiles; tutorial/goal/provenance RED suites are RED-by-skip (incl the RESEARCH-stage learning per-claim test); the zero-branch invariant test PASSES and scans ALL of bin/lib/** incl router.ts; the router goal-unawareness assertion PASSES; section + research fixtures exist.</done>
</task>

<task type="auto">
  <name>Task 3: Intake PII-ordering RED gate + PII-egress (no-leak payload) RED gate + .gitignore additions</name>
  <files>tests/intake-pii-ordering.test.ts, tests/intake-pii-egress.test.ts, .gitignore</files>
  <read_first>tests/intake-style-producer.test.ts, tests/cassette-no-leak.test.ts, bin/cli/intake.ts, bin/lib/prompt-loader.ts, .gitignore</read_first>
  <action>
Create tests/intake-pii-ordering.test.ts following the intake-style-producer.test.ts source-grep skip-predicate pattern. Define `piiOrderingWired()`: read bin/cli/intake.ts source, compute `diffPiiIdx = src.indexOf('diffPii')` and `loadPromptIdx = src.indexOf("loadPrompt('intake-clarifier')")`, return `diffPiiIdx !== -1 && loadPromptIdx !== -1 && diffPiiIdx < loadPromptIdx`. Tests (each `{ skip: !READY }`): (a) STRUCTURAL ORDERING — assert `diffPiiIdx < loadPromptIdx` (the PII-before-LLM mitigation, threat T-09-PII-EGRESS); (b) INTAKE.raw.local write — seed a tmp paper dir, run intake with `PENSMITH_NO_LLM=1` + PII opt-in (env or arg the 09-03 wiring will read) on raw text containing an email, assert `.paper/INTAKE.md` is redacted AND `.paper/INTAKE.raw.local` contains the raw email; (c) opt-out path — with PII opt-in OFF, INTAKE.md keeps raw answers and no INTAKE.raw.local is written. Use mkPaperRoot() tmpdir isolation. This gate is NECESSARY-BUT-NOT-SUFFICIENT (it only proves source ORDERING); the egress test below proves CONTENT.

Create tests/intake-pii-egress.test.ts — the H3 EGRESS-BY-CONTENT gate (proves the model-bound payload is the REDACTED text, not the raw answers). Guard RED-by-skip on `piiEgressWired()`: grep bin/cli/intake.ts for `redactPii` (the redaction must be wired) AND for `interpolate(` being fed a REDACTED variable (the wiring 09-03 lands). Mechanism (offline, no network — mirror the cassette-no-leak.test.ts committed-string-scan philosophy but against the LIVE interpolated payload): seed a tmp paper dir; write a `--from` seed file whose body contains the PII_EGRESS_SENTINELS (fake email/ssn/name from pii-polish-corpus.ts); CAPTURE the model-bound payload at the egress seam. Capture strategy (pick the one the 09-03 wiring exposes, document in the test): EITHER (i) monkeypatch/spy `interpolate` (imported from bin/lib/prompt-loader.ts) to record every `vars`/return value it is called with during intake — intake's only LLM-bound payload is `interpolate(prompt, { seed })`; OR (ii) if 09-03 routes egress through bin/lib/http.ts, install an offline interceptor (undici MockAgent / http-mock) and capture the request body. Run intake with PII opt-in ON and the API-key path active enough to reach the interpolate/egress call (PENSMITH_NO_LLM may be unset for this test, OR drive the Tier-1 branch directly — whichever 09-03's wiring makes reachable offline). ASSERT: NO captured payload string contains ANY of the PII_EGRESS_SENTINELS raw values (email/ssn/name) — i.e. every sentinel was redacted to its `[REDACTED:KIND]` tag BEFORE crossing the model boundary. ALSO assert the redacted tag (e.g. `[REDACTED:EMAIL]`) IS present in the captured payload (proves it was the redacted text that flowed, not an empty/stripped payload). This test FAILS for a verbatim implementer who passes `rawAnswers`/`seed` (unredacted) to interpolate, even though the ordering grep in (a) above still passes — that is exactly the gap H3 closes.

Add to .gitignore (under a `# Phase 9 tutorial/PII outputs` comment): the raw-intake and tutorial outputs. NOTE: the existing `.paper/` line already gitignores the whole workspace; add explicit `**/TUTORIAL.md` and `**/INTAKE.raw.local` defensively so the guarantee survives if `.paper/` is ever un-ignored, AND so RESEARCH Pitfall 7 (`TUTORIAL.md not gitignored`) is structurally satisfied. Confirm the repo-files / gitignore assertion (if any) still passes.
  </action>
  <verify>
    <automated>node --import tsx --test tests/intake-pii-ordering.test.ts tests/intake-pii-egress.test.ts 2>&1 | grep -E "pass|skip|fail" | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - source: tests/intake-pii-ordering.test.ts asserts `diffPiiIdx < loadPromptIdx` via source-grep (structural, not runtime instrumentation)
    - source: tests/intake-pii-egress.test.ts captures the model-bound interpolate payload (or http request body) and asserts NO PII_EGRESS_SENTINELS raw substring appears, AND the `[REDACTED:...]` tag DOES
    - behavior: both gates are RED-by-skip until 09-03 wires redaction before/into the egress; full suite green
    - source: .gitignore contains `**/TUTORIAL.md` and `**/INTAKE.raw.local`
    - test-command: `npm test` exits 0 (no failures across full suite)
  </acceptance_criteria>
  <done>PII-ordering gate (necessary) AND PII-egress-by-content gate (sufficient) are RED-by-skip; .gitignore defensively ignores TUTORIAL.md + INTAKE.raw.local; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test scaffold → repo | RED-by-skip tests must never go RED on a clean tree; a hard-fail scaffold would block the whole phase |
| prompt sentinels → loader | WN-3 sentinels must keep loadPrompt resolvable (PENSMITH_ALLOW_PENDING_PROMPT_HASHES=1) without leaking an unpinned prompt into production |
| user intake answers → model payload | the egress test pins that the interpolated payload carries no raw PII (content), not merely that redaction precedes loadPrompt (ordering) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-00-01 | Tampering | tutorial prompt slugs | mitigate | WN-3 sentinel pinned in BOTH prompt-loader.ts and repo-files.test.ts; real SHA-256 re-pin deferred to 09-03 single atomic commit |
| T-09-00-02 | DoS | RED-by-skip scaffold | mitigate | every new test guarded with `{ skip: !READY }`; lint-tutorial-no-branch + router goal-unawareness are the only non-skip tests and both pass on the clean tree |
| T-09-00-03 | Tampering | zero-branch invariant bypass | mitigate | ONE authoritative pattern matching the bare `goal`/`learning` token (defeats const-hoist + helper-extraction); scans ALL bin/lib/** incl router.ts; only tutorial.ts excluded; comment lines stripped before matching |
| T-09-00-04 | Information Disclosure | PII egress proven only by ordering | mitigate | tests/intake-pii-egress.test.ts captures the live model-bound payload and asserts sentinel absence (content), closing the ordering-only gap |
| T-09-00-SC | Tampering | npm/pip installs | accept | Phase 9 installs ZERO packages (09-RESEARCH Package Legitimacy Audit: none); no install task in this plan |
</threat_model>

<verification>
- `npm test` is GREEN (RED-by-skip suites skip; lint-tutorial-no-branch + router goal-unawareness pass).
- `tsc --noEmit` passes (tutorial.ts stub compiles).
- Both tutorial prompt slugs resolve under PENSMITH_ALLOW_PENDING_PROMPT_HASHES=1.
- lint-tutorial-no-branch.test.ts scans router.ts and finds zero `goal`/`learning`/`educator_mode` tokens (clean tree).
</verification>

<success_criteria>
7 new test files + 5 fixtures + tutorial.ts stub + 2 prompt files (WN-3 sentinels) + .gitignore additions exist; full suite green; the zero-branch invariant scans ALL of bin/lib/** (incl router.ts) and passes; the router goal-unawareness assertion passes; the RESEARCH-stage learning per-claim provenance test and the PII-egress-by-content test exist as RED-by-skip contracts.
</success_criteria>

<output>
Create `.planning/phases/09-educator-tutorial-mode-pii-polish/09-00-SUMMARY.md` when done.
</output>
</content>
</invoke>
