---
phase: 10-discipline-citation-style-breadth-zotero-mcp
plan: 04
type: execute
wave: 2
depends_on: ["10-01", "10-02", "10-03"]
files_modified:
  - templates/prompts/intake-clarifier.md
  - bin/lib/prompt-loader.ts
  - tests/repo-files.test.ts
  - workflows/research.md
  - tests/tier-contract.test.ts
autonomous: true
requirements: [CITE-02, CITE-03, RSCH-06]
must_haves:
  truths:
    - "The intake-clarifier prompt offers all 8 citation styles (APA, MLA, Chicago NB, Chicago AD, IEEE, AMA, Vancouver, Harvard) and no longer says APA is the only option"
    - "The intake-clarifier hash re-pin is atomic across both surfaces — bin/lib/prompt-loader.ts EXPECTED_PROMPT_HASHES and tests/repo-files.test.ts PENDING_HASH_PINS carry the SAME new SHA-256 in the same commit"
    - "loadPrompt('intake-clarifier') succeeds at runtime against the new bytes (no drift error, no pending bypass)"
    - "workflows/research.md capability_check declares the Zotero MCP fallback: when present AND authenticated the workflow body wires the real MCP-backed client into the zotero-mcp adapter (via setZoteroClientForTest) so its search() contributes normalized SourceCandidates; when absent OR unauthenticated it skips the Zotero pull and research continues on the other 7 adapters"
    - "The locked 16-verb UX-02 set is unchanged — no 17th verb; CITATIONS.ris lands alongside CITATIONS.bib in the research tier-contract path"
  artifacts:
    - path: "templates/prompts/intake-clarifier.md"
      provides: "Q3 listing all 8 styles, deferral caveat removed"
      contains: "Harvard"
    - path: "bin/lib/prompt-loader.ts"
      provides: "re-pinned intake-clarifier SHA-256 in EXPECTED_PROMPT_HASHES"
      contains: "intake-clarifier"
    - path: "tests/repo-files.test.ts"
      provides: "re-pinned intake-clarifier SHA-256 in PENDING_HASH_PINS (lockstep)"
      contains: "intake-clarifier"
    - path: "workflows/research.md"
      provides: "Zotero MCP capability_check absent-fallback wording"
      contains: "Zotero"
  key_links:
    - from: "bin/lib/prompt-loader.ts EXPECTED_PROMPT_HASHES['intake-clarifier']"
      to: "tests/repo-files.test.ts PENDING_HASH_PINS intake-clarifier hash"
      via: "identical SHA-256 re-pinned in the same commit (WN-3 lockstep)"
      pattern: "intake-clarifier"
    - from: "loadPrompt('intake-clarifier')"
      to: "templates/prompts/intake-clarifier.md bytes"
      via: "runtime SHA-256 validation against EXPECTED_PROMPT_HASHES"
      pattern: "loadPrompt"
    - from: "tests/tier-contract.test.ts research case"
      to: ".paper/CITATIONS.ris"
      via: "research verb emits .ris alongside .bib in both tiers"
      pattern: "CITATIONS\\.ris"
---

<objective>
Integration wave: make the new styles user-reachable and the Zotero fallback
declared, then re-assert the invariants. Three coordinated changes:
(1) update the hash-pinned `intake-clarifier.md` prompt to offer all 8 styles
(removing the "APA is the only option" deferral) WITH the WN-3 atomic dual-surface
hash re-pin; (2) update `workflows/research.md` capability_check so Zotero MCP
absence has a declared fallback; (3) extend tier-contract coverage so the research
path's `.ris` emission is exercised and the 16-verb bijection is re-asserted (no
17th verb).

Purpose: This wave runs AFTER Wave 1 because the prompt may only offer styles whose
.csl files render (10-01) and whose disciplines.json defaults resolve (10-03). The
intake-clarifier is a D-12 LOCKED hash-pinned slug — editing it REQUIRES re-pinning
both the runtime map (prompt-loader.ts) and the PR-time map (repo-files.test.ts) in
the SAME commit, or the loader throws a drift error (the WN-3 sentinel-then-real
precedent from Phases 4/5/9).
Output: 8-style intake prompt + declared Zotero fallback + re-asserted invariants.

CONSTRAINT: NO new npm deps. NO 17th verb (Zotero is a source provider + doctor
probe). The 16-workflow bijection stays intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-discipline-citation-style-breadth-zotero-mcp/10-RESEARCH.md
@.planning/phases/10-discipline-citation-style-breadth-zotero-mcp/10-PATTERNS.md

<interfaces>
<!-- The exact hash-pin mechanism + files to edit. Use these directly. -->

The intake-clarifier is hash-pinned in TWO surfaces that MUST agree (WN-3 lockstep):
  bin/lib/prompt-loader.ts (line 97):
    'intake-clarifier': 'bc93c546f5853196379c8958b1d8895b3cc3d0c2aabef94858e48638e181ba94',  // D-12 LOCKED
  tests/repo-files.test.ts PENDING_HASH_PINS (line 308):
    { slug: 'intake-clarifier', path: 'templates/prompts/intake-clarifier.md', decision: 'D-12', hash: 'bc93c546...e181ba94' }
The hash is SHA-256 over the FULL file bytes (frontmatter included). loadPrompt()
re-validates at runtime; repo-files.test.ts asserts it at PR time. BOTH must be
updated to the new hash in the SAME commit (the test error message states this rule).

To compute the new hash after editing the prompt:
  node -e "console.log(require('crypto').createHash('sha256').update(require('fs').readFileSync('templates/prompts/intake-clarifier.md')).digest('hex'))"

templates/prompts/intake-clarifier.md current Q3 (lines 34-36) — the deferral to remove:
  "3. Citation style — propose `APA` as the default (Phase 3 ships only
   `templates/citation-styles/apa.csl`; `chicago-author-date` and `mla` are
   referenced by the preset table but their CSL files are deferred)."
And the example output line 61: "3. Which citation style? (APA is the default in this build)".
PATTERNS.md §intake-clarifier.md shows the exact replacement text for both.

workflows/research.md capability_check (lines 8-17) — current Zotero declaration:
  required: [Task, MCP library.read, Zotero MCP]
  degrade_if_missing:
    - if no Task: run sequentially (slower)
    - if no MCP library / Zotero MCP: read .paper/library.json directly (Phase 3+)

bin/lib/verbs.ts UX02_VERBS — the locked 16 (doctor,new,next,status,research,outline,
plan,write,verify,compile,done,resume,list,open,sketch,add). Do NOT add to this.

tests/tier-contract.test.ts PHASE_3_CASES — the 'research' case (lines 370-376) is
mcpTool:null (CLI-only), expectedArtifact '.paper/LIBRARY.json'. CONTRIBUTING.md D-24:
a workflow body changed in a plan must have its contract test exercised in that plan.

bin/lib/sources/zotero-mcp.ts (from 10-03) — the EXECUTABLE used-as-source seam the
workflow body wires into: setZoteroClientForTest(client) injects a real MCP-backed
ZoteroClient (Tier-1: built on the Claude Code Task tool / MCP transport); after
injection, adapter.search(query) PULLS + NORMALIZES to SourceCandidate[] when
isZoteroAuthenticated() (ZOTERO_API_KEY set). The adapter is NOT a stub — the prose
MUST NOT call it one. Tier 2 (no client wired) and absent/unauth → search() returns [].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update intake-clarifier prompt to 8 styles + atomic WN-3 hash re-pin</name>
  <files>templates/prompts/intake-clarifier.md, bin/lib/prompt-loader.ts, tests/repo-files.test.ts</files>
  <read_first>
    - templates/prompts/intake-clarifier.md (the full prompt — Q3 at lines 32-38 + example at line 61)
    - bin/lib/prompt-loader.ts (EXPECTED_PROMPT_HASHES intake-clarifier line 97 + the WN-3 re-pin doc + loadPrompt drift logic)
    - tests/repo-files.test.ts (PENDING_HASH_PINS intake-clarifier line 308 + the lockstep guard ~lines 345-362 + the byte-pin error-message rule)
    - .planning/phases/10-discipline-citation-style-breadth-zotero-mcp/10-PATTERNS.md (intake-clarifier.md extend block — exact Q3 + example replacement text)
  </read_first>
  <action>
    Edit templates/prompts/intake-clarifier.md per PATTERNS.md:
    - Replace the Q3 deferral block (lines 34-36) with the 8-style version: propose the
      discipline preset default from templates/presets/disciplines.json as the suggested
      value; list all 8 styles available now (APA, MLA, Chicago (Notes-Bibliography),
      Chicago (Author-Date), IEEE, AMA, Vancouver, Harvard); note that an unlisted style is
      recorded verbatim and the workflow falls back to APA at render time (matches
      resolveStyleName's fallback shipped in 10-01).
    - Update the example-output line 61 to the 8-style options list.
    - Keep the frontmatter (promptId, decision, requirements) UNCHANGED — only the Q3 body
      and example change. Do NOT add a new requirement to the frontmatter.

    Then perform the ATOMIC WN-3 re-pin (the load-bearing step — both surfaces in ONE commit,
    or loadPrompt throws a drift error at runtime):
    1. Compute the new SHA-256 of the edited file via the node one-liner in the interfaces block.
    2. Replace the intake-clarifier hash value in bin/lib/prompt-loader.ts EXPECTED_PROMPT_HASHES
       (line 97) with the new hash.
    3. Replace the intake-clarifier hash value in tests/repo-files.test.ts PENDING_HASH_PINS
       (line 308) with the SAME new hash.
    Both files MUST carry the identical new hash. This mirrors the Phase-9 tutorial-prompt
    re-pin precedent (09-03 Task 3) and the Phase-5 claim-support/orphan-label re-pin exactly.
    Do NOT introduce a __PENDING_HASH_ sentinel — the prompt body is byte-stable on this edit,
    so re-pin directly to the real hash (the sentinel-then-real two-step is only needed when a
    file lands before its consumers; here the file exists and is edited in place).
  </action>
  <verify>
    <automated>node --import tsx --test tests/repo-files.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>intake-clarifier.md offers all 8 styles with no deferral text; prompt-loader.ts and repo-files.test.ts carry the identical new SHA-256; repo-files.test.ts intake-clarifier byte-pin test passes; loadPrompt('intake-clarifier') resolves without the pending bypass; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Declare Zotero MCP absent-fallback in workflows/research.md</name>
  <files>workflows/research.md</files>
  <read_first>
    - workflows/research.md (the full file — capability_check lines 8-17 + Step 3 adapter list line 47)
    - .planning/phases/10-discipline-citation-style-breadth-zotero-mcp/10-RESEARCH.md (§Architecture Patterns Pattern 3 — the degrade contract; the capability_check must declare Zotero presence/absence inline fallback)
    - tests/workflows-keyequal.test.ts (the W4 closed-vocabulary validator — confirm any capability_check keys you add stay within the allowed vocabulary)
  </read_first>
  <action>
    Update workflows/research.md so the Zotero MCP absence is an explicitly declared,
    non-breaking fallback (RSCH-06 / ARCH-03):
    - In the capability_check degrade_if_missing block, make the Zotero line concrete: when
      Zotero MCP is absent OR configured-without-ZOTERO_API_KEY, SKIP the Zotero pull step and
      note it in the research log; research continues on the other 7 adapters. Keep the
      capability_check keys within the W4 closed vocabulary (do not invent new top-level keys —
      extend the existing degrade_if_missing list entries).
    - In Step 3 (the adapter list, line 47), note that the registry now includes the
      'zotero-mcp' adapter (8 entries). Its search() returns [] unless Zotero MCP is
      present AND authenticated AND a client is wired. When present + authenticated, the
      Tier-1 workflow body wires the real MCP-backed Zotero client INTO the adapter via
      setZoteroClientForTest(client); the adapter's search() then PULLS from that client and
      NORMALIZES results to SourceCandidate[] (source:'zotero-mcp') — i.e. Zotero is actually
      USED AS A SOURCE, flowing through the same scoring + retraction checks as the other
      adapters, not a documentation-only stub. In Tier 2 (no MCP transport) or when absent /
      unauthenticated, search() returns [] and research proceeds on the other 7 adapters.
    - Add a sentence making explicit that Zotero is a SOURCE PROVIDER, not a verb (no 17th
      verb), consistent with the locked 16-verb set.
    Do NOT change the verb name, the outputs section (.bib/.ris are emitted by bin/cli/research.ts,
    already wired in 10-02), or the D-19/D-20 chokepoint language, and do NOT add a 17th verb.
    This is a capability_check + adapter-list update (declaring the present-path client wiring
    and the absent/no-auth fallback) only — no new verb, no new top-level capability_check key.
  </action>
  <verify>
    <automated>node --import tsx --test tests/workflows-keyequal.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>workflows/research.md capability_check declares BOTH the present+authenticated used-as-source path (Tier-1 wires the MCP-backed client into the zotero-mcp adapter so it contributes normalized SourceCandidates) AND the absent/no-auth fallback (skip Zotero pull, continue on 7 adapters); the adapter-list mentions the 8th adapter + no-17th-verb note; the W4 closed-vocabulary test (workflows-keyequal) still passes; no other workflow body changed.</done>
</task>

<task type="auto">
  <name>Task 3: Exercise research .ris parity + re-assert 16-verb bijection in tier-contract</name>
  <files>tests/tier-contract.test.ts</files>
  <read_first>
    - tests/tier-contract.test.ts (the PHASE_3_CASES research case lines 370-376 + the bijection assertions; search for UX02_VERBS and 'workflows' to find the verb↔workflow bijection check)
    - bin/lib/verbs.ts (UX02_VERBS — the locked 16)
    - .planning/phases/10-discipline-citation-style-breadth-zotero-mcp/10-VALIDATION.md (10-TIER row — tier-contract parity for the new style/RIS/zotero surfaces; 16-verb bijection intact)
  </read_first>
  <action>
    Per CONTRIBUTING.md D-24, the workflows/research.md change in this plan obligates exercising
    its contract test. The 'research' case already exists (CLI-only). Add a focused assertion
    rather than a new verb case (RIS is a library surface reached through research, not a verb):

    1. RIS parity assertion: in the research tier-contract path (or a dedicated bespoke test in
       this file), assert that after running the research verb in a temp .paper/ root,
       BOTH .paper/CITATIONS.bib AND .paper/CITATIONS.ris exist (the Tier-2 placeholder path in
       bin/cli/research.ts now emits both — zero-length when no candidates). Use the existing
       seed/spawn helpers in this file (the same pattern the research case uses). This proves
       the CITE-05 surface lands in the research path on both tiers (the Tier-1 surface is the
       workflow body delegating to the SAME bin/cli/research.ts → writeRis path — a documented
       asymmetry like compile/done, mcpTool:null).
    2. 16-verb bijection re-assertion: add (or confirm) a guard asserting UX02_VERBS.length === 16
       AND that no Phase-10 surface (zotero-mcp adapter key, renderStyle, writeRis) leaked into
       UX02_VERBS — i.e. 'zotero-mcp', 'zotero', 'ris', 'style' are NOT members of UX02_VERBS,
       and 'zotero-mcp' IS a key of the sources registry (a source provider, not a verb). Import
       { sources } from '../bin/lib/sources/index.js' and { UX02_VERBS } from '../bin/lib/verbs.js'.

    Keep all existing PHASE_3_CASES and parity tests unchanged.
  </action>
  <verify>
    <automated>node --import tsx --test tests/tier-contract.test.ts 2>&1 | tail -25</automated>
  </verify>
  <done>tier-contract.test.ts asserts CITATIONS.ris lands alongside CITATIONS.bib in the research path; the 16-verb bijection guard confirms UX02_VERBS.length===16 with no Zotero/RIS/style verb leak and 'zotero-mcp' present only in the sources registry; the full tier-contract suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edited prompt bytes → loadPrompt runtime validation | A hash-pinned prompt edited without re-pinning would hard-fail at runtime |
| workflows/research.md capability_check → research orchestrator | The declared fallback governs whether Zotero absence breaks the pipeline |
| Phase-10 surfaces → UX-02 verb set | New library/source surfaces must not silently become a 17th verb |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Tampering | intake-clarifier hash drift | mitigate | The WN-3 atomic dual-surface re-pin updates EXPECTED_PROMPT_HASHES AND PENDING_HASH_PINS to the identical new SHA-256 in one commit; repo-files.test.ts (PR-time) + loadPrompt (runtime) both re-validate, so a desync is structurally caught. Mirrors the Phase-9 tutorial-prompt re-pin precedent. |
| T-10-04-02 | Denial of Service | Zotero absence breaks research | mitigate | research.md capability_check explicitly declares the absent/no-auth fallback (skip Zotero pull, continue on 7 adapters); backstopped by the adapter returning [] (10-03) — the verifier gate is never conditional on Zotero. |
| T-10-04-03 | Elevation of Privilege | 17th-verb leak | mitigate | tier-contract bijection guard asserts UX02_VERBS.length===16 and that no Zotero/RIS/style token entered the verb set; 'zotero-mcp' is confined to the sources registry. Three independent guards (07-01 nl-triggers, the standing guard, this case) preserve the bijection. |
| T-10-04-04 | Tampering | W4 capability_check vocabulary drift | mitigate | The research.md edit stays within the W4 closed vocabulary; tests/workflows-keyequal.test.ts re-validates the allowed key set. |
| T-10-04-SC | Tampering | npm/pip/cargo installs | mitigate | NO installs this phase. RESEARCH Package Legitimacy Audit [OK]; no install checkpoint required. |
</threat_model>

<verification>
- `node --import tsx --test tests/repo-files.test.ts` — intake-clarifier byte-pin GREEN against the new hash.
- `node --import tsx -e` smoke: loadPrompt('intake-clarifier') returns a body containing 'IEEE' and 'Harvard' and NOT 'are deferred'/'only option'.
- `node --import tsx --test tests/workflows-keyequal.test.ts tests/tier-contract.test.ts tests/nl-triggers.test.ts` — W4 vocabulary + tier parity + 16-verb bijection all GREEN.
- `npm run check` fully green (lint + tsc + build + tier-contract + tests + manifests) — the whole Phase-10 suite is now GREEN.
</verification>

<success_criteria>
- intake-clarifier offers all 8 styles; hash re-pinned atomically across both surfaces; loadPrompt resolves.
- workflows/research.md declares the Zotero absent-fallback.
- 16-verb bijection re-asserted; CITATIONS.ris parity exercised in tier-contract.
- Full `npm run check` green.
</success_criteria>

<output>
Create `.planning/phases/10-discipline-citation-style-breadth-zotero-mcp/10-04-SUMMARY.md` when done.
</output>
