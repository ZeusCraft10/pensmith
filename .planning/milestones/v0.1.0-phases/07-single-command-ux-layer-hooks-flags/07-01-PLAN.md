---
phase: 07-single-command-ux-layer-hooks-flags
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/pensmith-router.test.ts
  - tests/estimator.test.ts
  - tests/flags.test.ts
  - tests/hooks/session-start.test.ts
  - tests/hooks/stop.test.ts
  - tests/hooks/pre-compact.test.ts
  - tests/hooks/post-tool-use.test.ts
  - tests/skill-descriptions.test.ts
  - tests/nl-triggers.test.ts
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05, ERGO-01, ERGO-02, ERGO-03, ERGO-04, HOOK-01, HOOK-02, HOOK-03, HOOK-04]
must_haves:
  truths:
    - "A RED suite exists that fails (or skips on absence) for every Phase 7 behavior before any implementation lands"
    - "The full suite stays GREEN (0 failures) by RED-by-skip on existsSync of the unbuilt module, mirroring the 05-01 / 06-01 precedent"
    - "Every new test names the exact export/identifier it will assert so Waves 1-2 implement against a fixed contract"
    - "H1 gate: a --yolo NON-GATE verb (write/plan) WITHOUT --estimate that exceeds 50% cap exits non-zero (RED until 07-02 runs the cap pre-flight for any --yolo verb); AND --yolo in a paper-less dir does NOT crash (C2-H1 fresh-dir guard)"
    - "H2 gate: an explicit verb runs EXACTLY once (no second router dispatch) and --show-prompts/--dry-run take effect for explicit verbs"
    - "H3 gate: `verify <N> --dry-run` WITH a fake ANTHROPIC_API_KEY present makes ZERO network calls and appends no COSTS.jsonl (the LLM call sites honor PENSMITH_NO_LLM), asserted by driving the verify path that WOULD egress absent the guard — NOT by env state alone, and NOT by driving write/research (vacuous)"
    - "H4 gate: bare /pensmith with a non-done HANDOFF resolves to the next WORK verb (plan/write/verify), NEVER an infinite resume"
    - "C3-HIGH-1 gate: the router RED suite fixtures EVERY SectionStateSchema state (writing/written/verifying/failed/unverifiable) PLUS the mixed [verified,failed,verified]-no-DRAFT case, asserting a valid non-undefined verb for each (failed/unverifiable → verify) — it would FAIL against a non-total resolveNextAction that fell through to undefined"
    - "C3-HIGH-2 gate: a RED case drives bare `pensmith --yolo` and `pensmith resume --yolo` and asserts the manually-dispatched gate verb RECEIVES yolo:true / skips its OWN approval gate — it would FAIL against a plan that dispatches with a bare args object (no flag forwarding)"
    - "C4-HIGH gate: the router RED suite fixtures a PRESENT-but-corrupt STATE.json (invalid JSON) AND a schema-invalid STATE.json (a sections entry missing slug), asserting resolveNextAction returns a valid non-undefined decision (verb is a string; specifically status/attention) and does NOT throw — it would FAIL against the cycle-3 StateNotFoundError-ONLY catch that re-threw every other load error and crashed the bare dispatcher before decision.verb; the estimator RED suite fixtures the same corrupt cases asserting the empty projection + no throw"
    - "C5-HIGH gate: the router RED suite fixtures a per-section PLAN.md that is PRESENT but has UNPARSEABLE YAML frontmatter (and, if practical, an unreadable PLAN.md), asserting resolveNextAction returns a valid non-undefined decision (status/attention with the section) and does NOT throw, AND fixtures a section whose PLAN.md is ABSENT asserting { verb:'plan', n, slug } — it would FAIL against the unguarded `parseFrontmatter(readFileSync(planPath))` section walk that throws before the switch default and crashes bare /pensmith; the estimator is UNAFFECTED (counts sections from STATE.json, never reads PLAN.md) so no estimator case is added"
    - "C6-HIGH gate: flags.test.ts adds an END-TO-END execFileSync bare-`pensmith` no-crash case against a CORRUPT per-section PLAN.md (malformed YAML) — the twin of the existing corrupt-STATE.json end-to-end case — asserting the process does NOT crash with an uncaught exception (graceful exit; shows status/attention). The router-unit corrupt-PLAN.md case (C5) stays; this case additionally pins the DISPATCH leg (`resolveNextAction → status` + the dispatchVerb backstop), which the router-unit test cannot observe. It would FAIL against a status verb that re-walks the corrupt PLAN.md via a raw unguarded parseFrontmatter and a dispatchVerb with no backstop"
  artifacts:
    - path: "tests/pensmith-router.test.ts"
      provides: "RED coverage for resolveNextAction() decision table (UX-01) — TOTAL over SectionStateSchema incl. failed/unverifiable + the mixed [verified,failed,verified] stuck case (C3-HIGH-1), over the STATE.json LOAD-FAILURE surface: invalid-JSON + schema-invalid STATE.json → status/attention, no throw (C4-HIGH), AND over the per-section PLAN.md read: present-but-unparseable PLAN.md → status/attention + absent PLAN.md → plan, no throw (C5-HIGH)"
      contains: "unverifiable"
    - path: "tests/estimator.test.ts"
      provides: "RED coverage for projectEstimate() + 50%-cap predicate + fresh-dir StateNotFoundError guard (ERGO-02, ERGO-03) + the corrupt/schema-invalid STATE.json guard → empty projection, no throw (C4-HIGH)"
      contains: "projectEstimate"
    - path: "tests/flags.test.ts"
      provides: "RED coverage for --dry-run / --show-prompts / --estimate / --yolo wiring (ERGO-01..04) PLUS the H1 (yolo cap refusal for a NON-GATE verb without --estimate + paper-less-dir no-crash, exit!=0), H2 (no double-dispatch + flags apply to explicit verbs), H3 (dry-run gates the LLM call sites — zero network on the verify path WITH a key present), C3-HIGH-2 (--yolo forwarded through the BARE + RESUME manual-dispatch paths → dispatched gate verb skips its own approval gate), C4-HIGH (bare `pensmith --yolo` against a corrupt STATE.json no-crash), and C6-HIGH (END-TO-END bare-`pensmith` no-crash against a CORRUPT per-section PLAN.md — the dispatch-leg twin of the corrupt-STATE.json case, pinning `resolveNextAction → status` + the dispatchVerb backstop) regression gates"
      contains: "dry-run"
    - path: "tests/hooks/session-start.test.ts"
      provides: "RED coverage for SessionStart resume-context emission (HOOK-02)"
      contains: "systemMessage"
    - path: "tests/hooks/stop.test.ts"
      provides: "RED coverage for Stop lock-release + log-flush, incl. flush-survives-release-rejection (HOOK-04 / M1)"
      contains: "release"
    - path: "tests/hooks/pre-compact.test.ts"
      provides: "RED coverage for PreCompact 10s timeout + ≤5KB HANDOFF (HOOK-01)"
      contains: "5120"
    - path: "tests/hooks/post-tool-use.test.ts"
      provides: "HOOK-03 throttle-gate coverage check (no reimplementation)"
      contains: "THROTTLE"
    - path: "tests/skill-descriptions.test.ts"
      provides: "RED coverage that skill files carry the PRD §5.4 NL trigger phrases (UX-03, UX-04)"
      contains: "where am I"
    - path: "tests/nl-triggers.test.ts"
      provides: "RED coverage for inline-correction → existing revise/plan verb mapping (UX-05)"
      contains: "revise"
  key_links:
    - from: "tests/pensmith-router.test.ts"
      to: "bin/lib/router.ts"
      via: "skip-guarded existsSync import"
      pattern: "existsSync.*router"
    - from: "tests/estimator.test.ts"
      to: "bin/lib/estimator.ts"
      via: "skip-guarded existsSync import"
      pattern: "existsSync.*estimator"
---

<objective>
Land the Phase 7 RED test suite BEFORE any implementation, following the RED-by-skip precedent locked in 05-01 and 06-01: each new test skip-guards on `existsSync()` of the unbuilt module so the full suite reports skips with ZERO failures until Waves 1-2 land the real code.

Purpose: Phase 7 is a wiring phase. RED tests fix the exact contracts (export names, return shapes, stdout protocol, env-var behavior) that the router, estimator, flags, hooks, and skills must satisfy — so executors implement against a frozen target with no interpretation.
Output: 9 new test files (one per behavior cluster) plus the `tests/hooks/` subdirectory, all GREEN-by-skip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-single-command-ux-layer-hooks-flags/07-RESEARCH.md
@.planning/phases/07-single-command-ux-layer-hooks-flags/07-PATTERNS.md
@.planning/phases/07-single-command-ux-layer-hooks-flags/07-VALIDATION.md

<interfaces>
<!-- Contracts the RED tests must assert against. Extracted from codebase + RESEARCH.md. -->

From bin/lib/schemas/handoff.ts (the HANDOFF.json shape these tests fixture):
```typescript
export const HANDOFF_MAX_BYTES = 5120;
export type Handoff = {
  schema_version: 1;
  last_updated: string;            // ISO-8601 datetime
  current_section: string | null;
  phase: 'intake'|'research'|'outline'|'plan'|'write'|'verify'|'compile'|'done';
  next_action: string;             // 1..200 chars
  breadcrumbs: Array<{ ts: string; verb: string; section: string | null; ok: boolean }>;  // max 5
  section_pointers: Array<{
    slug: string; plan_path: string; draft_path: string | null;
    verification_path: string | null; state: SectionState;
  }>;
};
```

From bin/lib/verbs.ts (the LOCKED 16 — no 17th):
```typescript
export const UX02_VERBS = ['doctor','new','next','status','research','outline',
  'plan','write','verify','compile','done','resume','list','open','sketch','add'] as const;
```

From bin/lib/state.ts + bin/lib/migrations/loader.ts (the loadState error contract the C4-HIGH router/estimator cases fixture):
```typescript
// loadState (state.ts:209-247) translates ONLY ENOENT → StateNotFoundError (line 234).
// EVERY OTHER load error bubbles UNCHANGED:
//   - invalid JSON   → SyntaxError           (JSON.parse, loader.ts:98)
//   - schema-invalid → SchemaValidationError  (loader.ts:119 — e.g. a sections entry missing slug, non-numeric n, bad $schemaVersion)
//   - disk newer     → ForwardIncompatError   (loader.ts:102)
//   - permission     → ErrnoException EACCES/EPERM
// schema/state.ts:87 — Schema.sections is .optional(), so state.sections may be undefined.
// C4-HIGH: a PRESENT-but-corrupt STATE.json must NOT crash the router/estimator —
// resolveNextAction → { verb:'status', reason:'attention' } (no throw);
// projectEstimate → { rows:[], totalUsd:0, exceedsHalfCap:false } (no throw).
```

From bin/lib/verify/pass2.ts:215 + pass4.ts:392 (the LLM gate --dry-run relies on — the H3 test exercises this path):
```typescript
const noLlm = process.env['PENSMITH_NO_LLM'] === '1' || !process.env['ANTHROPIC_API_KEY'];
// noLlm === true → offline UNCLEAR placeholder, ZERO messages.create(), ZERO appendCost.
```
NOTE (C2-H3): bin/lib/runtime.ts is config/API-key load only — NO model-invocation seam. The dry-run LLM gate is PENSMITH_NO_LLM (set by 07-02 Task 3), honored by the pass2/pass4 call sites above.

Router contract (to be built in 07-02 — assert against this):
```typescript
// bin/lib/router.ts
export type RouterDecision =
  | { verb: 'new' } | { verb: 'research' } | { verb: 'outline' }
  | { verb: 'plan'; n: number; slug: string }
  | { verb: 'write'; n: number; slug: string }
  | { verb: 'verify'; n: number; slug: string }
  | { verb: 'compile' } | { verb: 'done' }
  // C3-HIGH-1: status.reason widened ('done' | 'attention') + optional section so
  // resolveNextAction is TOTAL over SectionStateSchema and never returns undefined.
  // C4-HIGH: 'attention' is ALSO the disposition for a present-but-corrupt STATE.json.
  | { verb: 'status'; reason: 'done' | 'attention'; section?: { n: number; slug: string } }
  | { verb: 'resume'; handoff: Handoff };
export function resolveNextAction(paperRoot: string): Promise<RouterDecision>;
// TOTALITY (C3-HIGH-1): for the first non-'verified' section the resolver returns
// plan/write/verify ('failed'/'unverifiable' route to 'verify', NOT 'continue');
// 'verified' is the only continue case; an unrecognized status → status/attention.
// C4-HIGH: loadState only translates ENOENT → new; ANY OTHER load/parse error
// (invalid JSON, schema-invalid, forward-incompat, permission) → status/attention.
// No input yields undefined and no input throws — the bare dispatcher's decision.verb access can't crash.
```

Estimator contract (to be built in 07-02 — assert against this):
```typescript
// bin/lib/estimator.ts
export interface EstimateRow { step: string; inputTokens: number; outputTokens: number; usd: number; }
export interface EstimateResult { rows: EstimateRow[]; totalUsd: number; exceedsHalfCap: boolean; }
export function projectEstimate(args: { paperRoot: string; sessionCapUsd?: number }): Promise<EstimateResult>;
// C2-H1 / C4-HIGH: in a paper-less dir (no STATE.json) AND against a present-but-corrupt/schema-invalid
// STATE.json, projectEstimate returns { rows:[], totalUsd:0, exceedsHalfCap:false } — never throws.
```
</interfaces>

<!-- Existing test analogs to copy structure from -->
@tests/hooks-noop.test.ts
@tests/repo-files.test.ts
@hooks/post-tool-use.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED suite for router + estimator + flags (UX-01, ERGO-01..04)</name>
  <read_first>
    - tests/hooks-noop.test.ts (node:test + execFileSync structure — REUSE for the child-process dispatch cases in flags.test.ts)
    - tests/repo-files.test.ts (file-existence + content assertion helpers)
    - tests/cost-fixture.test.ts or bin/lib/cost-fixture.ts (how COSTS.jsonl appends are observed — H3 asserts NO append)
    - bin/lib/http-mock.ts isOfflineMode() (env-gate contract for --dry-run; gates SOURCE adapters only — H3's LLM gate is PENSMITH_NO_LLM, separate)
    - bin/lib/verify/pass2.ts:215 + pass4.ts:392 (the noLlm guard the H3 test exercises — `verify --dry-run` rides this path)
    - bin/cli/verify.ts (the verb the H3 test drives under --dry-run — it WOULD call the LLM absent the guard)
    - bin/lib/session-log.ts setMirrorPromptsToStderr (the --show-prompts target; H2 asserts it engages for explicit verbs)
    - bin/lib/budget.ts assertBudget + COSTS.jsonl path (H1 cap math + H3 no-billing assertion)
    - bin/lib/state.ts loadState (state.ts:234 — translates ONLY ENOENT) + bin/lib/migrations/loader.ts (SchemaValidationError / SyntaxError surface) + bin/lib/schemas/state.ts:62-66 (SectionEntrySchema — slug REQUIRED, n must be a number; the source of the schema-invalid corrupt fixture) — the C4-HIGH corrupt-STATE cases fixture these
    - bin/lib/frontmatter.ts parseFrontmatter (calls `yaml.parseDocument(text).toJSON()` — THROWS on malformed YAML / duplicate map keys) + hooks/pre-compact.ts:178-187 (the repo's OWN try/catch around the IDENTICAL `parseFrontmatter(readFileSync(planPath,'utf8'))` call) — the C5-HIGH corrupt-PLAN.md case fixtures unparseable frontmatter that makes this call throw
    - node_modules/citty/dist/index.mjs:209-228 (the run()/setup/subCommands semantics H2 depends on — pre-dispatch via argv pre-parse)
    - The router + estimator <interfaces> contracts above
  </read_first>
  <files>tests/pensmith-router.test.ts, tests/estimator.test.ts, tests/flags.test.ts</files>
  <action>
    Create three RED test files using node:test + node:assert/strict, all RED-by-skip per the 05-01/06-01 precedent: at top of each file compute `const built = existsSync(resolve('bin/lib/<module>.ts'))` and pass `{ skip: !built }` to each test (the skip-guard idiom from 05-01 known-bad-pass2). Use mkdtempSync(join(tmpdir(),'pensmith-...')) fixtures (analog: tests/tier-contract.test.ts freshPaperRoot).

    pensmith-router.test.ts (UX-01) asserts resolveNextAction(paperRoot) for the H4-PINNED decision table per 07-RESEARCH "PINNED ORDERING" (resolveNextAction IGNORES HANDOFF and NEVER returns 'resume'): (a) no STATE.json → { verb:'new' } (catches StateNotFoundError, does NOT throw); (b) STATE.json + no RESEARCH.md → { verb:'research' }; (c) RESEARCH.md present, no OUTLINE.md / zero sections → { verb:'outline' }; (d) sections with a 'planned' PLAN.md status → { verb:'plan', n, slug }; (e) all sections 'verified' + no DRAFT.md → { verb:'compile' }; (f) DRAFT.md present + no FINAL.md → { verb:'done' }.

    C3-HIGH-1 TOTALITY RED CASES (this catches the original "decision table is not total → undefined → crash" failure). The original 07-01 suite fixtured ONLY planned/verified/DRAFT/FINAL, so a reachable stuck state stayed GREEN. Add a case for EVERY SectionStateSchema state plus the mixed-stuck case, each in a fixture that ALSO creates STATE.json + RESEARCH.md + OUTLINE.md so the walk reaches the section branch (per M5):
      - (g) first section status 'writing'      → assert { verb:'write',  n, slug };
      - (h) first section status 'written'      → assert { verb:'verify', n, slug };
      - (i) first section status 'verifying'    → assert { verb:'verify', n, slug };
      - (j) first section status 'failed'       → assert { verb:'verify', n, slug } (NOT 'continue'/compile) — the key totality assertion;
      - (k) first section status 'unverifiable' → assert { verb:'verify', n, slug } (NOT 'continue'/compile);
      - (l) MIXED STUCK CASE: three sections with statuses ['verified','failed','verified'] and NO DRAFT.md → assert the result is a VALID RouterDecision (assert `decision !== undefined` AND `typeof decision.verb === 'string'`) and SPECIFICALLY assert `decision.verb === 'verify'` with the failed section's n/slug (the stuck section is routed somewhere sensible, NOT left to fall through to undefined and NOT prematurely compiled). This is the exact reachable state from the cycle-3 HIGH.
      For EACH of (g)-(l) the assertion message names C3-HIGH-1 and explicitly asserts the returned verb is non-undefined. Use the SectionStateSchema vocabulary for the PLAN.md `status:` frontmatter so the fixtures stay in lock-step with state.ts.

    C4-HIGH CORRUPT-STATE RED CASES (this catches the cycle-4 "loadState throws on a present-but-corrupt STATE.json → router re-throws → bare dispatcher crashes before decision.verb" failure). loadState translates ONLY ENOENT; a present-but-malformed STATE.json makes it throw a SyntaxError/SchemaValidationError that the cycle-3 catch (StateNotFoundError-only) re-threw. Add two cases, each writing a STATE.json that PRESENT but unloadable:
      - (m) INVALID-JSON CASE: write STATE.json with non-JSON content (e.g. the bytes `{ this is not json `) so JSON.parse throws a SyntaxError inside loadState. Call resolveNextAction(paperRoot) and assert: `await assert.doesNotReject(...)` / no throw; `decision !== undefined`; `typeof decision.verb === 'string'`; SPECIFICALLY `decision.verb === 'status'` and `decision.reason === 'attention'` (a present-but-corrupt file is the attention terminus — NOT 'new', which is the ABSENT-file disposition). The assertion message names C4-HIGH and notes a present-but-corrupt file must NOT crash and must NOT be treated as absent.
      - (n) SCHEMA-INVALID CASE: write a STATE.json that is VALID JSON but FAILS StateSchema — the cleanest is a sections entry missing the required slug (e.g. `{ "$schemaVersion":2, "paperId":"p", "createdAt":"<iso>", "sections":[{ "n":1 }] }`) so loadAndMigrate throws SchemaValidationError. Assert the SAME contract as (m): no throw; `decision.verb === 'status'`; `decision.reason === 'attention'`. The message names C4-HIGH and notes a hand-edited / schema-broken STATE.json routes to status/attention without crashing. (A second valid variant: a non-numeric `n` — choose whichever produces a clean SchemaValidationError.)
      For BOTH (m) and (n) the regression-gate property is: against the cycle-3 StateNotFoundError-ONLY catch, loadState's thrown SyntaxError/SchemaValidationError escapes resolveNextAction and the test FAILS on an unexpected throw; the widened catch (StateNotFoundError → new; ELSE → status/attention) makes it PASS.

    C5-HIGH CORRUPT-PLAN.md RED CASES (this catches the cycle-5 "the section walk reads parseFrontmatter(readFileSync(planPath)) with NO try/catch → a present-but-corrupt PLAN.md throws before the switch default → bare /pensmith crashes on decision.verb" failure — the sibling of C4, one file over). The repo's OWN hooks/pre-compact.ts:178-187 wraps this identical call in try/catch, proving the throw is real. Each fixture creates a VALID STATE.json (with at least one section `{n,slug}`) + RESEARCH.md + OUTLINE.md so the walk REACHES the section branch (per M5):
      - (o) CORRUPT-PLAN.md (UNPARSEABLE FRONTMATTER) CASE: write the section's PLAN.md with frontmatter that PARSES-FAILS in yaml.parseDocument().toJSON() — the cleanest is a frontmatter block with a duplicate map key or broken YAML structure inside the `---` fences (e.g. `---\nstatus: planned\n  : : bad\nstatus: written\n---\nbody`) so parseFrontmatter THROWS when the router reads it. Call resolveNextAction(paperRoot) and assert: `await assert.doesNotReject(...)` / no throw; `decision !== undefined`; `typeof decision.verb === 'string'`; SPECIFICALLY `decision.verb === 'status'` and `decision.reason === 'attention'` and `decision.section` names the section's n/slug (a present-but-corrupt PLAN.md is the attention terminus — NOT 'plan', which is the ABSENT-file disposition). The assertion message names C5-HIGH and notes a present-but-corrupt PLAN.md must NOT crash and must NOT be treated as absent. (Optional sub-case if practical: an UNREADABLE PLAN.md — e.g. chmod 000 / a directory at planPath — asserting the same status/attention contract; skip on platforms where the unreadable fixture can't be created.)
      - (p) ABSENT-PLAN.md CASE: write STATE.json with a section `{n,slug}` but DO NOT create that section's PLAN.md on disk (existsSync(planPath)===false). Call resolveNextAction(paperRoot) and assert `decision.verb === 'plan'` with the section's n/slug (the genuinely-absent disposition is 'plan', distinct from the corrupt disposition 'status/attention' in (o)). The assertion message names C5-HIGH and notes absent → plan vs corrupt → status/attention.
      For (o) the regression-gate property is: against the unguarded section-walk read (`parseFrontmatter(readFileSync(planPath))` with no try/catch), the thrown error escapes resolveNextAction and the test FAILS on an unexpected throw; the per-section try/catch guard (present-but-corrupt → status/attention) makes it PASS. (p) pins that the absent-file path stays 'plan' so the guard does not over-broaden. The estimator suite gets NO new case — projectEstimate counts sections from STATE.json and never reads PLAN.md, so it is unaffected by this defect.

    H4 RED CASE (this catches the original resume-circularity failure — bare /pensmith must NOT get stuck on resume): write a VALID non-done HANDOFF.json (phase:'write', schema_version:1, valid section_pointers) INTO a fixture whose STATE.json + RESEARCH.md + OUTLINE.md + a section PLAN.md with status:'planned' all exist. Assert resolveNextAction(paperRoot) returns { verb:'plan', ... } (the actual next WORK verb) and assert `decision.verb !== 'resume'` explicitly. This is the regression gate: the original design returned { verb:'resume' } here, which re-dispatched to itself forever. The test message names H4 / UX-01. Fixtures write STATE.json with the SectionStateSchema-valid `sections:[{n,slug}]` shape and PLAN.md files with `status:` frontmatter; cases (c)-(f) and the H4 case MUST also create RESEARCH.md + OUTLINE.md so the walk reaches the section/compile/done branches (per review M5).

    estimator.test.ts (ERGO-02, ERGO-03) asserts projectEstimate({paperRoot, sessionCapUsd}) returns { rows, totalUsd, exceedsHalfCap }; assert totalUsd === sum of row.usd; assert exceedsHalfCap===true when totalUsd > sessionCapUsd*0.5 and ===false when under; assert NO network/LLM call occurred (set process.env PENSMITH_DRY_RUN='1' and assert COSTS.jsonl was not written during projection). C2-H1 FRESH-DIR CASE: call projectEstimate on a tmpdir with NO STATE.json and assert it RETURNS { rows:[], totalUsd:0, exceedsHalfCap:false } and does NOT throw (StateNotFoundError must be caught). C4-HIGH CORRUPT-STATE CASE: write a PRESENT-but-corrupt STATE.json (invalid JSON, e.g. `{ not json`) AND, as a second sub-case, a schema-invalid STATE.json (a sections entry missing slug) into separate tmpdir fixtures; for EACH call projectEstimate and assert `await assert.doesNotReject(...)` / no throw AND the return is exactly { rows:[], totalUsd:0, exceedsHalfCap:false } (same empty-projection disposition as the fresh-dir case). The message names C4-HIGH and notes a corrupt STATE.json must behave identically to a missing one so `--yolo`/`--estimate` against it does not crash. Against the cycle-3 StateNotFoundError-ONLY guard, the thrown SyntaxError/SchemaValidationError escapes and the test FAILS; the widened catch-all makes it PASS.

    flags.test.ts (ERGO-01/04 + the H1/H2/H3 regression gates) asserts the following. Run the CLI as a child process via execFileSync(process.execPath, ['--import','tsx','bin/pensmith.ts', ...args], {cwd: fixtureRoot, env}) so the assertions exercise the REAL dispatch path (not just module introspection). RED-by-skip: guard the dispatch-driving cases on a `flagsWired` predicate that greps bin/pensmith.ts for a 'dry-run' token (the four flags do not exist yet, so these cases SKIP until 07-02 lands; the file already exists so existsSync alone would not skip — review note on the skip-guard). Keep one un-skipped case asserting isOfflineMode()===true when PENSMITH_NETWORK_TESTS !== '1'.

      - Flag declaration: the four global flag keys dry-run/estimate/yolo/show-prompts are reachable on the explicit-verb surface (assert `pensmith write --dry-run` and `pensmith compile --show-prompts` parse without an "unknown flag" error).

      - H1 RED CASE (yolo cap refusal fires for a NON-GATE verb, WITHOUT --estimate — C2-H1): seed a fixture whose projected cost exceeds 50% of the session cap (large section count, or a low injected cap via env PENSMITH_COST_CAP_USD). Run a NON-GATE verb under --yolo and NO --estimate — primarily `pensmith write --yolo` (and ALSO `pensmith plan --yolo`); assert the process EXITS NON-ZERO (status 1) and stderr names the >50%-cap refusal. This is the C2-H1 regression gate: the revised design had re-scoped the refusal to gate-skipping verbs (compile/done/outline)+bare, so `write --yolo` / `plan --yolo` over-cap were NOT refused. Also keep `pensmith compile --yolo` and bare `pensmith --yolo` over-cap → exit 1 (the original gate-skipping cases). Assert the CONVERSE: a verb under --yolo whose estimate is UNDER 50% cap exits 0 (no false refusal). C2-H1 FRESH-DIR CASE: in a tmpdir with NO STATE.json, run `pensmith --yolo` (and `pensmith write --yolo`) and assert the process does NOT crash with StateNotFoundError (exit 0 — projectEstimate returns the empty projection). C4-HIGH CORRUPT-STATE CASE: in a tmpdir with a PRESENT-but-corrupt STATE.json (invalid JSON), run bare `pensmith --yolo` and assert it does NOT crash (exit 0 — projectEstimate returns the empty projection so the cap pre-flight sees under-cap), the regression gate for the cycle-4 crash. C6-HIGH END-TO-END CORRUPT-PLAN.md CASE (the dispatch-leg twin of the corrupt-STATE.json case — pins the FULL bare invocation, not just the resolver): build a fixture with a VALID STATE.json (at least one section `{n,slug}`) + RESEARCH.md + OUTLINE.md so the walk REACHES the section branch, and write that section's PLAN.md with UNPARSEABLE YAML frontmatter (the same duplicate-key / broken-structure block as router case (o), e.g. `---
status: planned
  : : bad
status: written
---
body`). Run bare `pensmith` (NO verb — drives `resolveNextAction → status` dispatch via the loader table; do NOT pass `--yolo` here so the cost-cap pre-flight does not exit first and mask the dispatch leg) via execFileSync(process.execPath, ['--import','tsx','bin/pensmith.ts'], {cwd: fixtureRoot, env}) and assert the process does NOT crash with an uncaught exception — i.e. it exits cleanly (status code 0 OR a defined non-crash status exit, NOT a Node uncaught-exception stack trace) and stderr/stdout shows the status/attention disposition for the corrupt section. The assertion message names C6-HIGH and notes this drives the END-TO-END bare-dispatch leg (`resolveNextAction → status` + the dispatchVerb backstop), which the router-unit corrupt-PLAN.md case (pensmith-router.test.ts case (o)) cannot observe. REGRESSION-GATE PROPERTY: against the ORIGINAL plan (status.ts re-walks the corrupt PLAN.md via a raw unguarded parseFrontmatter(readFileSync(planPath)) AND dispatchVerb has no backstop try/catch), the throw propagates to the top level and the child process exits with an uncaught-exception crash → this case FAILS; against the fix (status.ts consumes the shared guarded readSectionState helper AND dispatchVerb wraps cmd.run() in an outer try/catch) the process exits gracefully → it PASSES. RED-by-skip: guard on the same `flagsWired` predicate (the status readSectionState consumption + dispatchVerb backstop land in 07-02, so this case SKIPS until then). Keep the router-unit corrupt-PLAN.md case (case (o)) too — the two are complementary (unit asserts the resolver's RETURN; this asserts the dispatch leg does not crash).

      - H2 RED CASE (no double-dispatch + flags take effect for explicit verbs): (a) run an explicit verb whose output is countable (e.g. `pensmith status` or a verb that prints exactly one structured line) and assert the resolved verb runs EXACTLY ONCE — no second router-dispatched verb appears in stdout (the original root run() fall-through ran a SECOND verb). Concretely: assert the explicit verb's signature stdout line appears exactly once AND no router/next diagnostic ('→ <verb>') leaks to stdout. (b) Assert `--show-prompts` takes effect for an EXPLICIT verb (run `pensmith write --show-prompts` and assert setMirrorPromptsToStderr was engaged — e.g. a prompts-mirrored marker reaches stderr, or the mirror flag is observable), proving the flag was applied via the PRE-DISPATCH seam BEFORE the verb ran, not after.

      - C3-HIGH-2 RED CASE (global flags — esp. --yolo — propagate through the BARE and RESUME manual-dispatch paths): drive the CLI via execFileSync so a verb reached via the loader-table dispatch (NOT citty's explicit-verb parse) actually RECEIVES yolo:true. Use a fixture where the resolved bare verb is a GATE-bearing verb (compile or done) whose approval gate is observable — i.e. set the fixture state so resolveNextAction lands on `compile` or `done`, keep the projected cost UNDER the 50% cap (so the cost-cap pre-flight does NOT exit first and mask the test), and arrange the gate so that WITHOUT --yolo it would prompt/block and WITH a forwarded yolo:true it skips. Two sub-cases:
        (a) BARE path: run bare `pensmith --yolo` (no verb) in that fixture and assert the dispatched gate verb SKIPPED its approval gate (e.g. the gate-skip signature on stdout/stderr, or the export/compile proceeded without a confirm prompt) — proving yolo:true was forwarded into the verb's args, not just consumed by the pre-flight.
        (b) RESUME path: with a non-done HANDOFF present, run `pensmith resume --yolo` and assert the work verb it dispatches likewise receives yolo:true / skips its approval gate.
      Against the ORIGINAL non-forwarding plan (manual dispatch calls cmd.run() with a bare args object), the verb's `args.yolo` is undefined → the gate is NOT skipped → BOTH sub-cases FAIL. The assertion message names C3-HIGH-2 / SC3 and explicitly notes it drives the bare + resume manual-dispatch paths (NOT the explicit-verb path, which citty parses directly). RED-by-skip: guard on the same `flagsWired` predicate (the dispatchVerb forwarding lands in 07-02, so this case SKIPS until then).

      - H3 RED CASE (dry-run gates the LLM call sites on a path that WOULD egress — NON-VACUOUS, C2-H3): run `pensmith verify <N> --dry-run` inside a fixture that has a verifiable section (a DRAFT/section with at least one [@citekey] so runPass2/runPass4 would normally call messages.create()), WITH a FAKE `ANTHROPIC_API_KEY` set in the child env (env: {...process.env, ANTHROPIC_API_KEY:'sk-fake-test-key', PENSMITH_NETWORK_TESTS: undefined}). This is the path that WOULD make a live LLM call absent the guard — the previous test drove write/research (Tier-2 placeholders, zero LLM calls in any mode) and so gated NOTHING (vacuous). Assert: (1) ZERO network egress (no outbound attempt — e.g. instrument via PENSMITH_NETWORK_TESTS staying unset → isOfflineMode() true for adapters, AND assert no live provider call by asserting the run completes without a network error AND) (2) NO COSTS.jsonl line is appended (the pass2/pass4 placeholder path skips appendCost under PENSMITH_NO_LLM). The assertion message names ERGO-01 / H3 / C2-H3 and explicitly notes it drives the verify (LLM-calling) path with a key present, NOT write/research. Do NOT assert env state alone.

    Keep the ERGO-03 50%-cap PREDICATE unit assertion (exceedsHalfCap arithmetic) + the fresh-dir guard + the corrupt-STATE guard in estimator.test.ts; flags.test.ts owns the END-TO-END refusal exit-code assertion (H1/C2-H1, incl. the non-gate verb) and the H3 verify-path egress assertion.

    NO fenced code in the final test bodies beyond normal TS. Every assertion message names the requirement ID.
  </action>
  <verify>
    <automated>node --import tsx --test tests/pensmith-router.test.ts tests/estimator.test.ts tests/flags.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: all three files import the named contract symbols (resolveNextAction, projectEstimate) under a skip-guard; flags.test.ts drives the CLI via execFileSync for the H1/H2/H3/C3-HIGH-2 cases — H1 drives a NON-GATE verb (`write --yolo`/`plan --yolo`) over-cap AND a paper-less-dir `--yolo` no-crash case AND a corrupt-STATE `--yolo` no-crash case (C4-HIGH); H3 drives `verify <N> --dry-run` WITH a fake ANTHROPIC_API_KEY (LLM-calling path, non-vacuous); C3-HIGH-2 drives bare `pensmith --yolo` + `pensmith resume --yolo` and asserts the dispatched gate verb skips its approval gate (yolo forwarded); C6-HIGH drives bare `pensmith` (no verb) against a CORRUPT per-section PLAN.md and asserts the END-TO-END invocation does NOT crash with an uncaught exception (the dispatch-leg twin of the corrupt-STATE.json bare-dispatch case); pensmith-router.test.ts includes the H4 non-done-HANDOFF case asserting verb !== 'resume', the C3-HIGH-1 totality cases (every section state incl. failed/unverifiable + the mixed [verified,failed,verified]-no-DRAFT case → valid non-undefined verb), the C4-HIGH corrupt-STATE cases (invalid-JSON + schema-invalid STATE.json → status/attention, no throw), AND the C5-HIGH corrupt-PLAN.md case (present-but-unparseable-frontmatter PLAN.md → status/attention+section, no throw) + the absent-PLAN.md case (→ plan); estimator.test.ts includes the C2-H1 fresh-dir guard case AND the C4-HIGH corrupt-STATE guard case (invalid-JSON + schema-invalid → empty projection, no throw) — estimator gets NO C5-HIGH case (it never reads PLAN.md)
    - behavior: suite runs with 0 failures — the H1/H2/H3/C3-HIGH-2 dispatch cases SKIP (flags/forwarding not wired yet), the H4 + C3-HIGH-1 + C4-HIGH router cases + estimator fresh-dir + corrupt-STATE cases SKIP (modules absent), and would FAIL against the ORIGINAL broken design once un-skipped (regression-gate property): H1 against the gate-only scoping, H3 against the write/research vacuous path, C3-HIGH-1 against a non-total resolveNextAction (undefined on [verified,failed,verified]), C3-HIGH-2 against a manual dispatch that omits yolo forwarding, C4-HIGH against the StateNotFoundError-ONLY catch that re-throws SyntaxError/SchemaValidationError and crashes on a present-but-corrupt STATE.json, C5-HIGH against the unguarded section-walk `parseFrontmatter(readFileSync(planPath))` that throws on a present-but-corrupt PLAN.md and crashes before the switch default, C6-HIGH (the END-TO-END dispatch-leg case in flags.test.ts) against a status verb that re-walks the corrupt PLAN.md via a raw unguarded parseFrontmatter + a dispatchVerb with no backstop, which crashes bare `pensmith` end-to-end (the fix routes status through the shared readSectionState helper AND wraps dispatchVerb's cmd.run() in an outer try/catch backstop)
    - test-command: `node --import tsx --test tests/pensmith-router.test.ts tests/estimator.test.ts tests/flags.test.ts` exits 0 with skips, no failures
  </acceptance_criteria>
  <done>Three RED-by-skip test files exist; targeted run is 0 failures; each asserts the exact export name and return shape from the &lt;interfaces&gt; contracts; H1 (yolo cap exit!=0 for a NON-GATE verb without --estimate + paper-less-dir no-crash + corrupt-STATE no-crash), H2 (single dispatch + flags-for-explicit-verb), H3 (dry-run zero network on the verify LLM-calling path with a key present — non-vacuous), H4 (non-done HANDOFF → next work verb, not resume), C3-HIGH-1 (every section state incl. failed/unverifiable + the mixed [verified,failed,verified]-no-DRAFT case → valid non-undefined verb), C3-HIGH-2 (--yolo forwarded through bare + resume → dispatched gate verb skips its approval gate), and C4-HIGH (present-but-corrupt invalid-JSON + schema-invalid STATE.json → router status/attention + estimator empty projection, neither throws), C5-HIGH (present-but-unparseable-frontmatter PLAN.md → router status/attention+section, no throw; absent PLAN.md → plan), and C6-HIGH (END-TO-END bare-`pensmith` against a corrupt PLAN.md exits cleanly — the dispatch-leg twin of the corrupt-STATE.json bare case, RED-by-skip until 07-02 routes status through readSectionState + adds the dispatchVerb backstop) each have a RED assertion that would catch the original failure.</done>
</task>

<task type="auto">
  <name>Task 2: RED suite for hooks — session-start, stop, pre-compact, post-tool-use coverage (HOOK-01, HOOK-02, HOOK-03, HOOK-04)</name>
  <read_first>
    - tests/hooks-noop.test.ts (execFileSync hook-run + `out===''` stdout protocol gate)
    - hooks/session-start.ts + hooks/stop.ts (current exit-0 stubs)
    - hooks/pre-compact.ts (onPreCompact + writeHandoff call to wrap with a 10s race)
    - hooks/post-tool-use.ts (THROTTLE_MS=60_000, CHECKPOINTS_PATH='.claude/CHECKPOINTS.jsonl' — HOOK-03 ALREADY COMPLETE)
    - bin/lib/lock.ts release(resource) export (Stop calls this — it REJECTS on an unheld lock, so Stop uses Promise.allSettled; M1)
    - bin/lib/session-log.ts closeSessionLog (added in 07-03) + the active-chain flush
    - bin/lib/schemas/handoff.ts HANDOFF_MAX_BYTES=5120
  </read_first>
  <files>tests/hooks/session-start.test.ts, tests/hooks/stop.test.ts, tests/hooks/pre-compact.test.ts, tests/hooks/post-tool-use.test.ts</files>
  <action>
    Create the tests/hooks/ subdirectory and four hook test files (node:test). Hooks are executed via execFileSync(process.execPath, ['--import','tsx', hookPath], {stdio:['ignore','pipe','pipe']}) exactly like hooks-noop.test.ts. Use a mkdtempSync fixture cwd so .paper artifacts are isolated.

    session-start.test.ts (HOOK-02): (a) with NO .paper/HANDOFF.json → stdout MUST be empty and exit 0 (the unchanged noop path); (b) with a valid HANDOFF.json fixture (phase!=='done', schema_version:1, valid section_pointers) → stdout MUST be parseable JSON carrying a `systemMessage` string field that mentions the phase and next_action; (c) with phase==='done' → stdout empty. RED-by-skip: guard the JSON-emission cases on whether session-start.ts still contains only `process.exit(0)` (read the file; if it is the stub, skip the emission assertions). Keep the empty-stdout-no-handoff case un-skipped (the stub already satisfies it).

    stop.test.ts (HOOK-04): assert exit 0 and empty stdout always; assert that after running stop.ts with a pre-acquired lock fixture, the lock is released (lock file absent / isLocked()===false) — RED-by-skip guarded on stop.ts no longer being the bare `process.exit(0)` stub. M1/C2-M2 FLUSH-SURVIVES-REJECTION CASE: run stop.ts in a fixture where NO `.paper` resource lock is held (so release('.paper') will REJECT). Assert the session log was STILL flushed despite the rejected release (e.g. assert the session-log file's pending records were written / the flush sentinel is present) — this is the Promise.allSettled gate; with the old Promise.all the flush would be abandoned. RED-by-skip guarded on stop.ts no longer being the stub.

    pre-compact.test.ts (HOOK-01): invoke onPreCompact({paperDir}) against a fixture; assert .paper/HANDOFF.json is written, parses under HandoffSchema, and Buffer.byteLength <= 5120; assert the call completes well under 10s. Add a timeout-path assertion RED-by-skip guarded on the presence of a `PRECOMPACT_TIMEOUT_MS` token in hooks/pre-compact.ts (the 10s race lands in 07-03): when present, a slow-write injection must reject within ~10s.

    post-tool-use.test.ts (HOOK-03 coverage check — DO NOT reimplement the hook): assert exit 0 + empty stdout; assert the throttle by running onPostToolUse twice in quick succession against a fixture cwd and confirming .claude/CHECKPOINTS.jsonl gains at most ONE line (THROTTLE_MS gate); read hooks/post-tool-use.ts and assert it still contains `THROTTLE_MS` and the proper-lockfile sentinel pattern (regression guard that HOOK-03 stays intact).
  </action>
  <verify>
    <automated>node --import tsx --test tests/hooks/session-start.test.ts tests/hooks/stop.test.ts tests/hooks/pre-compact.test.ts tests/hooks/post-tool-use.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: tests/hooks/ directory exists with the four named files; stop.test.ts includes the flush-survives-release-rejection case (M1/C2-M2)
    - behavior: post-tool-use throttle assertion PASSES now (HOOK-03 already implemented); session-start/stop emission + flush-survives-rejection assertions SKIP (stubs not yet upgraded); pre-compact size/parse assertions PASS (onPreCompact already writes HANDOFF.json)
    - test-command: targeted run exits 0 with the expected mix of pass + skip, zero failures
  </acceptance_criteria>
  <done>Four hook test files exist; HOOK-03 coverage is GREEN; the not-yet-built upgrade behaviors (session-start emission, stop release + flush-survives-rejection) are RED-by-skip; suite has 0 failures.</done>
</task>

<task type="auto">
  <name>Task 3: RED suite for skills, NL triggers, and tier-contract verb parity (UX-02, UX-03, UX-04, UX-05)</name>
  <read_first>
    - tests/repo-files.test.ts (file-existence + assert.match content-assertion idiom)
    - tests/cli-verbs.test.ts (introspects command.subCommands for the locked 16)
    - tests/tier-contract.test.ts (the parity-case structure to extend)
    - bin/lib/verbs.ts UX02_VERBS (the 16-verb bijection — no 17th)
    - 07-RESEARCH "Natural-Language Trigger Routing" + PRD §5.4 trigger-phrase mapping
  </read_first>
  <files>tests/skill-descriptions.test.ts, tests/nl-triggers.test.ts</files>
  <action>
    skill-descriptions.test.ts (UX-03, UX-04): RED-by-skip guarded on existsSync('skills/pensmith.md'). Assert each plumbing skill file (skills/pensmith.md, skills/plan-section.md, skills/write-section.md, skills/verify-section.md) exists and that its `description:` frontmatter contains the EXACT PRD §5.4 NL trigger phrases — at minimum: skills/pensmith.md matches /where am I/i AND /what'?s next/i AND /resume/i; skills/plan-section.md matches /plan section/i AND /redo section/i; skills/verify-section.md matches /verify section/i. Assert .claude-plugin/plugin.json (once it has a skills array) registers exactly the colon-prefix names pensmith / pensmith:plan-section / pensmith:write-section / pensmith:verify-section — RED-by-skip guarded on the plugin.json `skills` key existing.

    nl-triggers.test.ts (UX-05): assert that inline-correction phrasings map to EXISTING verbs and NEVER introduce a 17th verb. Concretely: import UX02_VERBS and assert the set of skill-mapped target verbs is a SUBSET of UX02_VERBS (no new verb names); assert that the revise/swap-source/redo corrections route to the existing `plan` verb path (per 04-04: revise ships via `plan --revise`, not a 17th verb). RED-by-skip guarded on the skills files existing. Also assert that adding the skills/plumbing namespace did not change UX02_VERBS.length (still 16) — this is a guard that survives implementation (no skip).

    Both files use only assert.match / assert.deepEqual; no execFileSync needed.
  </action>
  <verify>
    <automated>node --import tsx --test tests/skill-descriptions.test.ts tests/nl-triggers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: both files import UX02_VERBS from ../bin/lib/verbs.js and assert subset/length invariants
    - behavior: the 16-verb-length invariant PASSES now; skill-file content assertions SKIP (skills not yet written); suite has 0 failures
    - test-command: targeted run exits 0 with pass + skip, zero failures
  </acceptance_criteria>
  <done>Two RED-by-skip files exist; the no-17th-verb / 16-bijection invariant is asserted and GREEN; skill-content + plumbing-namespace assertions are RED-by-skip until 07-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → test process | Test fixtures read/write tmpdir .paper artifacts; no untrusted external input |
| hook subprocess → stdout | Hook test asserts stdout protocol (empty or single JSON frame) — the regression gate for Pitfall 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Hook stdout protocol | mitigate | hooks-noop + new hook tests assert stdout is empty OR exactly one JSON frame; diagnostics-to-stderr is the contract these tests enforce |
| T-07-02 | Tampering | 16-verb bijection | mitigate | nl-triggers.test.ts asserts UX02_VERBS.length===16 and skill targets ⊆ UX02_VERBS — a 17th verb fails CI |
| T-07-03 | Information Disclosure | --estimate must not bill | mitigate | estimator.test.ts asserts COSTS.jsonl is not written during projectEstimate (no real LLM/network) |
| T-07-04 | Denial of Service | HANDOFF bloat | mitigate | pre-compact.test.ts asserts HANDOFF.json <= 5120 bytes (HANDOFF_MAX_BYTES) |
| T-07-14 | Elevation of Privilege | --yolo gate bypass for non-gate verbs (H1/C2-H1) | mitigate | flags.test.ts drives `write --yolo` / `plan --yolo` (non-gate) AND compile/bare WITHOUT --estimate and asserts non-zero exit when over 50% cap; plus a paper-less-dir no-crash case — the gate cannot be skipped on any verb |
| T-07-15 | Tampering | citty root run() double-dispatch (H2) | mitigate | flags.test.ts asserts an explicit verb runs exactly once (no second router dispatch) and that --show-prompts/--dry-run apply to explicit verbs (pre-dispatch seam) |
| T-07-16 | Information Disclosure | --dry-run LLM network leak (H3/C2-H3) | mitigate | flags.test.ts drives `verify <N> --dry-run` WITH a fake key present (the LLM-calling path) and asserts ZERO network egress + no COSTS.jsonl append (PENSMITH_NO_LLM honored by pass2/pass4); the assertion is non-vacuous (not write/research) |
| T-07-17 | Denial of Service | resume/router circularity (H4) | mitigate | pensmith-router.test.ts asserts a non-done HANDOFF resolves to the next WORK verb (verb !== 'resume') so bare /pensmith always advances |
| T-07-22 | Denial of Service | router non-totality → undefined → dispatcher crash (C3-HIGH-1) | mitigate | pensmith-router.test.ts fixtures EVERY SectionStateSchema state + the mixed [verified,failed,verified]-no-DRAFT case and asserts a valid non-undefined verb (failed/unverifiable → verify) so bare /pensmith never crashes on decision.verb |
| T-07-23 | Elevation of Privilege | --yolo not forwarded into manually-dispatched verbs → approval gate not skipped (C3-HIGH-2) | mitigate | flags.test.ts drives bare `pensmith --yolo` + `pensmith resume --yolo` and asserts the dispatched gate verb receives yolo:true / skips its approval gate; the shared dispatchVerb helper forwards parsed global flags |
| T-07-25 | Denial of Service | present-but-corrupt STATE.json crashes router + estimator → bare /pensmith + --yolo crash (C4-HIGH) | mitigate | pensmith-router.test.ts fixtures an invalid-JSON + a schema-invalid STATE.json and asserts resolveNextAction → status/attention WITHOUT throwing; estimator.test.ts fixtures the same and asserts the empty projection without throwing; flags.test.ts asserts bare `pensmith --yolo` against a corrupt STATE.json exits 0 — the StateNotFoundError-only catch that re-threw is the regression these gates catch |
| T-07-26 | Denial of Service | present-but-corrupt per-section PLAN.md crashes router → bare /pensmith crash (C5-HIGH) | mitigate | pensmith-router.test.ts fixtures a PLAN.md with UNPARSEABLE YAML frontmatter (parseFrontmatter throws) asserting resolveNextAction → status/attention+section WITHOUT throwing, plus an ABSENT PLAN.md asserting → plan — the unguarded section-walk `parseFrontmatter(readFileSync(planPath))` is the regression this gate catches; estimator unaffected (never reads PLAN.md) |
| T-07-27 | Denial of Service | corrupt per-section PLAN.md crashes the bare-dispatch LEG (resolveNextAction → status) → bare /pensmith crash END-TO-END (C6-HIGH) | mitigate | flags.test.ts adds an END-TO-END execFileSync bare-`pensmith` no-crash case against a corrupt PLAN.md (the twin of the corrupt-STATE.json bare case) asserting the process exits without an uncaught exception — gating the dispatch leg (status re-walking the file + the dispatchVerb backstop) that the router-unit test cannot observe; would FAIL against a status verb using a raw parseFrontmatter + a dispatchVerb with no backstop |
| T-07-SC | Tampering | npm installs | accept | No new packages this phase (07-RESEARCH Package Legitimacy Audit: none); no install task to gate |
</threat_model>

<verification>
- `node --import tsx --test tests/pensmith-router.test.ts tests/estimator.test.ts tests/flags.test.ts tests/hooks/*.test.ts tests/skill-descriptions.test.ts tests/nl-triggers.test.ts` → 0 failures (pass + skip mix)
- `npm test` → full suite stays GREEN (0 failures) per the RED-by-skip precedent
- HOOK-03 post-tool-use throttle assertion is GREEN (already-implemented coverage, no reimplementation)
</verification>

<success_criteria>
- 9 new test files exist; tests/hooks/ subdirectory created
- Every Phase 7 requirement (UX-01..05, ERGO-01..04, HOOK-01..04) has at least one RED assertion
- Each cross-AI HIGH (H1 yolo-cap-for-any-verb incl. non-gate + paper-less no-crash, H2 no-double-dispatch + flags-for-explicit-verb, H3 dry-run-zero-network on the verify LLM-calling path with a key present, H4 non-done-HANDOFF→next-work-verb, C3-HIGH-1 router totality over every section state incl. the mixed stuck case, C3-HIGH-2 --yolo forwarded through bare + resume manual-dispatch, C4-HIGH present-but-corrupt/schema-invalid STATE.json → router status/attention + estimator empty projection without throwing, C5-HIGH present-but-unparseable-frontmatter per-section PLAN.md → router status/attention+section without throwing while an absent PLAN.md → plan, estimator unaffected, C6-HIGH END-TO-END bare-`pensmith` against a corrupt PLAN.md exits cleanly — the dispatch-leg twin (`resolveNextAction → status` + dispatchVerb backstop) that the router-unit case cannot observe) has a dedicated RED assertion that would catch the original failure
- Full suite has 0 failures (RED-by-skip on unbuilt modules)
- No 17th verb introduced; UX02_VERBS.length===16 invariant asserted
</success_criteria>

<output>
Create `.planning/phases/07-single-command-ux-layer-hooks-flags/07-01-SUMMARY.md` when done
</output>
</output>
</output>
