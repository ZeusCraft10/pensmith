---
phase: 05-verifier-completeness-pass-2-pass-4
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - bin/cli/verify.ts
  - tests/tier-contract.test.ts
autonomous: true
requirements: [VRFY-03, VRFY-06]

must_haves:
  truths:
    - "verify <N> writes a ## Pass-2 and a ## Pass-4 section into sections/<N>/VERIFICATION.md, AFTER the blocking status is frozen"
    - "Pass 2 and Pass 4 calls live BELOW the hasFail/hasUnverifiable/status computation in verify.ts and never feed back into it (VRFY-07)"
    - "tier-contract verify-section asserts ## Pass-2 + ## Pass-4 appear in VERIFICATION.md in both tiers (modulo prose), with an UNCLEAR verdict row in the no-LLM path"
    - "The live D-13 chokepoint holds after wiring: `grep -c \"loadPrompt\" bin/cli/verify.ts == 0` (whole-file literal symbol count, COMMENTS INCLUDED). runPass2/runPass4 imports carry no `loadPrompt` literal; Pass 2/4 load their prompts inside their own modules. No comment in verify.ts introduces the literal symbol `loadPrompt`."
  artifacts:
    - path: "bin/cli/verify.ts"
      provides: "advisory Pass-2 + Pass-4 wiring below the frozen status line + VERIFICATION.md section rendering + widened bibByCitekey carrying title/author/abstract"
      contains: "runPass2"
    - path: "tests/tier-contract.test.ts"
      provides: "extended verify-section case asserting ## Pass-2 + ## Pass-4 + an **UNCLEAR** row; a verify-section-scoped seed step writes a section DRAFT.md carrying a [@citekey] (shared seedPaperFixture left untouched)"
      contains: "## Pass-2"
  key_links:
    - from: "bin/cli/verify.ts (below status freeze, line ~124)"
      to: "bin/lib/verify/pass2.ts runPass2 + bin/lib/verify/pass4.ts runPass4"
      via: "advisory call site mirroring the runFreshnessForDraft position"
      pattern: "runPass2|runPass4"
    - from: "tests/tier-contract.test.ts verify-section case"
      to: "VERIFICATION.md ## Pass-2 / ## Pass-4 sections"
      via: "artifact-content assertion for both tiers (TIER-06/07)"
      pattern: "## Pass-2|## Pass-4|\\*\\*UNCLEAR\\*\\*"
---

<objective>
Wire the two advisory passes into the verify orchestrator and prove tier parity. In `bin/cli/verify.ts`, add `runPass2` + `runPass4` calls AFTER the frozen `hasFail`/`hasUnverifiable`/`status` block (mirroring the existing `runFreshnessForDraft` call site), widen `bibByCitekey` so Pass 2 receives title/author/abstract, render the `## Pass-2` and `## Pass-4` sections into VERIFICATION.md via `renderPass2Section` / `renderPass4Section`, and add `pass2`/`pass4` to the return value for DONE-09 (Phase 6) consumption. Then extend the existing `verify-section` tier-contract case to assert both advisory sections appear (modulo prose) in both tiers, seeding a section DRAFT.md that carries a `[@citekey]` so Pass 2 has something to judge.

Purpose: VRFY-03 + VRFY-06 SC#3 (tier parity). This is the wave that makes the advisory passes observable end-to-end while structurally guaranteeing they never block (calls sit below the frozen status line; the advisory-isolation test from Plan 05-01 stays GREEN). The wiring is purely the `runPass2`/`runPass4` call seam — verify.ts still never loads a prompt itself (Pass 2/4 load `claim-support`/`orphan-label` inside their own modules), so the live D-13 chokepoint (`grep -c "loadPrompt" bin/cli/verify.ts == 0`, comments included) remains intact and is re-asserted by the committed regression test added in Plan 05-01.
Output: orchestrator wiring + VERIFICATION.md sections + extended tier-contract verify-section case. After this plan, `npm test` and `npm run test:tier-contract` are GREEN with Pass 2/4 visible in both tiers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-verifier-completeness-pass-2-pass-4/05-RESEARCH.md
@.planning/phases/05-verifier-completeness-pass-2-pass-4/05-PATTERNS.md
@bin/cli/verify.ts
@bin/lib/verify/pass1.ts

<interfaces>
<!-- Extracted from codebase — executor wires against these directly. -->

LIVE D-13 INVARIANT (confirmed from bin/cli/verify.ts + Plan 03-07 SUMMARY Invariant #3 / auto-fix #6):
`grep -c "loadPrompt" bin/cli/verify.ts` returns 0 — a WHOLE-FILE literal search for the symbol `loadPrompt`, COMMENTS INCLUDED. The verify.ts comment deliberately PARAPHRASES (it says "prompt-loader call" / "prompt-loader symbol"; the hyphenated token `prompt-loader` is NOT gated and legitimately appears in the comment). When wiring this plan: the `runPass2`/`renderPass2Section`/`runPass4`/`renderPass4Section` imports contain NO `loadPrompt` literal, so wiring them keeps the count at 0. Do NOT add any comment, import, or code containing the literal symbol `loadPrompt`. Permission for claim-support/orphan-label was granted in Plan 05-01 via EXPECTED_PROMPT_HASHES registration — NOT via any verify.ts change. The committed regression `tests/verify-advisory-isolation.test.ts` guard (B) asserts `(text.match(/loadPrompt/g) ?? []).length === 0` on the FULL verify.ts source; it must stay GREEN after this wiring.

From bin/cli/verify.ts (current shape — the frozen status block is lines 119-124; the freshness advisory call is line 115; the VERIFICATION.md lines[] array is 126-141; the return is line 144; the D-13 comment is lines 1-13 and is UNCHANGED — Plan 05-01 does NOT touch verify.ts):
```
const pass1 = await runPass1(draftMd, bibPath);
const bibEntries = await parseBibtex(readFileSync(bibPath, 'utf8'));
const bibByCitekey = new Map<string, { DOI?: string }>(   // ← WIDEN this value type
  bibEntries.map((e) => [String((e as { id?: string }).id ?? ''), e as { DOI?: string }]),
);
const pass3 = await runPass3(draftMd, bibByCitekey);
const freshness = await runFreshnessForDraft(draftMd, bibPath);   // advisory, after blocking
const hasFail = pass1.some((r) => r.verdict !== 'OK') || pass3.some((r) => r.verdict === 'NOT_FOUND');
const hasUnverifiable = pass3.some((r) => r.verdict === 'PDF_UNAVAILABLE' || r.verdict === 'TEXT_UNAVAILABLE');
const status: 'verified'|'failed'|'unverifiable' = hasFail ? 'failed' : (hasUnverifiable ? 'unverifiable' : 'verified');
// lines[] VERIFICATION.md array, then atomicWriteFile(verifPath, lines.join('\n'))
return { ok: status !== 'failed', status, path: verifPath, pass1, pass3, freshness };
```
NOTE: runPass3 already consumes bibByCitekey as Map<string, { DOI?: string }>. Widening the value type to add optional title/author/abstract is additive and backward-compatible with runPass3 (it only reads DOI).

From bin/lib/verify/pass2.ts (Plan 05-02):
```
export async function runPass2(draftMd: string, bibByCitekey: Map<string, { DOI?: string; title?: string | string[]; author?: Array<{ family?: string; given?: string }> | string[]; abstract?: string }>, opts: { n: number; scopeCapUsd?: number }): Promise<Pass2Result[]>;
export function renderPass2Section(results: Pass2Result[]): string;   // returns a '## Pass-2 ...' section, **UNCLEAR** rows in the no-LLM path
```

From bin/lib/verify/pass4.ts (Plan 05-03):
```
export async function runPass4(draftMd: string, opts: { n: number; scopeCapUsd?: number }): Promise<Pass4Result[]>;
export function renderPass4Section(results: Pass4Result[]): string;   // returns a '## Pass-4 ...' section
```

From tests/tier-contract.test.ts (the verify-section registry entry + generic case loop + seedPaperFixture — all need surgical extension, NOT a new case per D-24):
```
{ name: 'verify-section', mcpTool: 'pensmith_verify', cliArgs: ['verify', MIDDLE_SECTION, '--yolo'], verbFile: 'bin/cli/verify.ts', expectedArtifact: `.paper/sections/0${MIDDLE_SECTION}-placeholder/VERIFICATION.md` }
function seedPaperFixture(): string { /* creates .paper/, seeds CITATIONS.bib from known-good-fixture; does NOT seed a section DRAFT.md */ }
// generic loop @ line 557: runCliInDir uses env PENSMITH_NO_LLM='1'; reads cliArtifactBytes; runMcpToolInDir uses PENSMITH_NO_LLM='1' + PENSMITH_PAPER_ROOT
```
CRITICAL FINDING: the current verify-section case seeds NO section DRAFT.md, so verify.ts hits the "DRAFT.md missing → unverifiable" short-circuit (line 72-77) and never reaches Pass 2/4. A VERIFY-SECTION-SCOPED seed step (NOT a mutation of the shared seedPaperFixture) MUST write `.paper/sections/0${MIDDLE_SECTION}-placeholder/DRAFT.md` containing at least one `[@citekey]` sentence so verify reaches the advisory passes and the **UNCLEAR** assertion can hold — without perturbing the other PHASE_3_CASES that rely on the clean shared fixture.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire runPass2 + runPass4 into verify.ts below the frozen status line and render the advisory sections</name>
  <read_first>
    - bin/cli/verify.ts (full file — the frozen status block lines 119-124, the freshness call line 115, the lines[] array 126-141, the return line 144, and the D-13 comment lines 1-13 which Plan 05-01 left UNCHANGED)
    - tests/verify-advisory-isolation.test.ts (Plan 05-01 — guard (A) advisory isolation + guard (B) the committed whole-file `loadPrompt`-count==0 regression this wiring must keep GREEN)
    - bin/lib/verify/pass2.ts (runPass2 signature + widened bibByCitekey value shape + renderPass2Section)
    - bin/lib/verify/pass4.ts (runPass4 signature + renderPass4Section)
    - bin/lib/verify/pass1.ts (normalizeBibAuthors + bibTitle — how the bib entry's title/author shapes are read, so the widened map is built consistently)
    - .planning/phases/05-verifier-completeness-pass-2-pass-4/05-PATTERNS.md (section "bin/cli/verify.ts (MODIFIED)" — exact insertion points)
  </read_first>
  <files>bin/cli/verify.ts</files>
  <action>
    Add imports: `import { runPass2, renderPass2Section } from '../lib/verify/pass2.js';` and `import { runPass4, renderPass4Section } from '../lib/verify/pass4.js';`. These import strings contain NO `loadPrompt` literal, so the D-13 whole-file `loadPrompt`-count==0 invariant is preserved. Do NOT import the prompt loader and do NOT write the literal symbol `loadPrompt` ANYWHERE in verify.ts (code OR comments) — Pass 2/4 load their own prompts internally; if you add a call-site comment, paraphrase as "advisory prompt-load path" / "prompt-loader" exactly as the existing D-13 comment does (the hyphenated `prompt-loader` token is allowed; the symbol `loadPrompt` is not). Do NOT edit the existing D-13 comment block (lines 1-13).

    Widen the bibByCitekey Map value type (line 107-109) from `{ DOI?: string }` to `{ DOI?: string; title?: string | string[]; author?: Array<{ family?: string; given?: string }> | string[]; abstract?: string }` and cast each parsed entry to that shape so Pass 2 receives title/author/abstract from the bib metadata. runPass3 still reads only DOI — the widening is additive and must not change the runPass3 call.

    After the freshness call (line 115) AND below the frozen status block (lines 119-124) — i.e. AFTER `const status = ...` — add the two advisory calls, mirroring the freshness call-site comment style:
      const pass2 = await runPass2(draftMd, bibByCitekey, { n });
      const pass4 = await runPass4(draftMd, { n });
    Add a comment at the call site stating these run AFTER hasFail/status is frozen and NEVER feed back into it (VRFY-07) — paraphrasing per the rule above (no literal `loadPrompt`). Place them physically below the status assignment so the advisory-isolation regex guard from Plan 05-01 (no `hasFail =`/`status =` on a line with `runPass2`/`runPass4`) stays GREEN.

    Extend the VERIFICATION.md lines[] array (after the `renderFreshnessTable(freshness)` entry) with: `renderPass2Section(pass2), '', renderPass4Section(pass4), ''`.

    Extend the return value (line 144) to add pass2 and pass4: `return { ok: status !== 'failed', status, path: verifPath, pass1, pass3, freshness, pass2, pass4 };` so Phase 6 DONE-09 can consume the advisory data.

    Do NOT modify the hasFail/hasUnverifiable/status computation in any way. Do NOT make status depend on pass2/pass4.
  </action>
  <verify>
    <automated>PENSMITH_NO_LLM=1 PENSMITH_ALLOW_PENDING_PROMPT_HASHES=1 node --import tsx --test tests/verify-advisory-isolation.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - tests/verify-advisory-isolation.test.ts guard (A) PASSES (runPass2/runPass4 present but never on a `hasFail =`/`status =` line).
    - tests/verify-advisory-isolation.test.ts guard (B) PASSES post-wiring: the WHOLE-FILE literal symbol count of `loadPrompt` in bin/cli/verify.ts is 0 (COMMENTS INCLUDED) — this is the REAL D-13 chokepoint, NOT a `grep -v '^//'` non-comment proxy. Independently confirmable: `grep -c "loadPrompt" bin/cli/verify.ts` returns 0. (The hyphenated `prompt-loader` token may still appear in the existing comment; do NOT gate on it.)
    - Running verify on a seeded fixture writes a VERIFICATION.md containing both '## Pass-2' and '## Pass-4' strings.
    - `npm run lint && npm run typecheck` GREEN.
  </acceptance_criteria>
  <done>verify.ts wires both advisory passes below the frozen status line, renders ## Pass-2 + ## Pass-4 into VERIFICATION.md, widens the bib map, and returns pass2/pass4 — with the real D-13 whole-file `loadPrompt`-count==0 invariant (comments included) and advisory-isolation invariants intact, asserted by the committed regression test.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the verify-section tier-contract case to assert ## Pass-2 + ## Pass-4 parity across both tiers</name>
  <read_first>
    - tests/tier-contract.test.ts (PHASE_3_CASES verify-section entry @ line 415-420; seedPaperFixture @ line 508-525; generic case loop @ line 557-616; runCliInDir @ 457; runMcpToolInDir @ 483)
    - bin/cli/verify.ts (the DRAFT.md-missing short-circuit lines 72-77 — why the fixture MUST seed a section DRAFT.md with a [@citekey])
    - .planning/phases/05-verifier-completeness-pass-2-pass-4/05-PATTERNS.md (section "tests/tier-contract.test.ts (MODIFIED)" — the Pass-2/Pass-4 assertion snippet + verify-section-scoped DRAFT.md seed note)
    - .planning/phases/05-verifier-completeness-pass-2-pass-4/05-RESEARCH.md (§"Tier-Contract Case for Pass 2/4" — no new CASE; extend the existing verify-section case)
  </read_first>
  <files>tests/tier-contract.test.ts</files>
  <action>
    Add a VERIFY-SECTION-SCOPED seed step (do NOT mutate the shared seedPaperFixture — other PHASE_3_CASES rely on its current "no DRAFT.md → unverifiable" clean state). Inside the verify-section path (after seedPaperFixture runs, before runCliInDir/runMcpToolInDir for that case), write a section DRAFT.md at `.paper/sections/0${MIDDLE_SECTION}-placeholder/DRAFT.md` containing at least one claim sentence with a `[@citekey]` token whose key EXISTS in the seeded known-good-fixture CITATIONS.bib (so Pass 1 does not flag it FABRICATED and verify reaches the advisory passes). Keep the draft minimal and deterministic — one short paragraph with one `[@citekey]`. Ensure the citekey matches an entry present in tests/fixtures/known-good-fixture/CITATIONS.bib (read that bib to pick a real citekey). Guard the directory creation with mkdirSync(..., { recursive: true }). Because both the CLI and MCP runs operate on the same seeded paper dir, the scoped seed must run for both tiers of the verify-section case.

    In the generic case loop (after the existing `cliArtifactBytes` length assertion ~line 577), add a `verify-section`-specific block: when tc.name === 'verify-section', assert cliArtifactBytes.includes('## Pass-2') and cliArtifactBytes.includes('## Pass-4') (VRFY-03/VRFY-06), and assert.match(cliArtifactBytes, /\*\*UNCLEAR\*\*/) — the no-LLM placeholder path must emit at least one UNCLEAR verdict row (PENSMITH_NO_LLM='1' is already set by runCliInDir).

    In the MCP branch (after the existing mcpArtifactBytes read ~line 602), add the same `## Pass-2` + `## Pass-4` presence assertions for the verify-section case on mcpArtifactBytes — this is the cross-tier parity check (TIER-06). The existing ±20% length-equivalence assertion already covers TIER-07 prose tolerance; do not weaken it. NOTE (SC3 scope): in the only CI-exercised path both tiers run under PENSMITH_NO_LLM=1, so both deterministically emit all-UNCLEAR Pass-2 placeholders and identical deterministic Pass-4 orphan counts — presence + the all-UNCLEAR row + the ±20% length equivalence pins the scoped (no-LLM) SC3 equivalence; live-path verdict parity is out of CI scope by design. State this no-LLM scoping in a comment so a future reader does not mistake presence-only for full live parity.

    Do NOT add a new PHASE_3_CASES entry and do NOT add a 17th verb — the D-24 obligation is satisfied by extending the existing verify-section case (the verify workflow body changes in Phase 5). Do not touch the write-wave / revise / compile bespoke tests.
  </action>
  <verify>
    <automated>npm run test:tier-contract</automated>
  </verify>
  <acceptance_criteria>
    - tier-contract verify-section case PASSES: VERIFICATION.md from BOTH the CLI and the MCP tool contain '## Pass-2' and '## Pass-4'.
    - The CLI artifact contains an '**UNCLEAR**' verdict row (no-LLM placeholder path).
    - The seeded section DRAFT.md carries a [@citekey] that resolves in the seeded CITATIONS.bib (verify reaches the advisory passes rather than the DRAFT.md-missing short-circuit), and is created by a verify-section-scoped seed — seedPaperFixture is NOT mutated (other PHASE_3_CASES keep their clean state and unchanged expected status).
    - No new PHASE_3_CASES entry was added; locked-16 verb set + workflow bijection invariants preserved (the full `npm test` suite — including any locked-verb-count assertion — stays GREEN).
    - `npm run test:tier-contract` and `npm test` GREEN.
  </acceptance_criteria>
  <done>verify-section tier-contract case proves Pass 2 + Pass 4 parity (modulo prose) across both tiers on the no-LLM fixture path via a verify-section-scoped DRAFT.md seed (shared fixture untouched); D-24 satisfied by extension, locked-16 invariants untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| advisory pass output → blocking verdict | Pass 2/4 results must remain structurally unable to influence hasFail/status |
| bib metadata → Pass 2 prompt vars | title/author/abstract from CITATIONS.bib flow into Pass 2 (handled inside pass2.ts; this plan only widens the map) |
| CLI process vs MCP server process | Both must produce equivalent VERIFICATION.md (modulo prose) under PENSMITH_NO_LLM=1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Elevation of Privilege | advisory Pass 2/4 escalating to a compile/export block | mitigate | runPass2/runPass4 calls are placed BELOW the frozen status block in verify.ts; status never references pass2/pass4; tests/verify-advisory-isolation.test.ts guard (A) enforces no `hasFail =`/`status =` line touches runPass2/runPass4 (VRFY-07) |
| T-05-04-02 | Tampering | a refactor slipping the literal symbol `loadPrompt` into bin/cli/verify.ts (comment OR code) — breaking the live D-13 chokepoint | mitigate | the REAL invariant is `grep -c "loadPrompt" bin/cli/verify.ts == 0` (whole-file, comments included); Task 1 forbids writing the literal `loadPrompt` anywhere in verify.ts and re-asserts it via the committed regression tests/verify-advisory-isolation.test.ts guard (B) — NOT via a non-comment `grep -v '^//'` proxy. Pass 2/4 load their prompts only from their own modules; permission for the new slugs came from EXPECTED_PROMPT_HASHES registration in Plan 05-01, not from any verify.ts edit |
| T-05-04-03 | Tampering | tier divergence — CLI emits advisory sections but MCP does not (or vice versa) | mitigate | tier-contract verify-section asserts ## Pass-2 + ## Pass-4 presence in BOTH cliArtifactBytes and mcpArtifactBytes + retains ±20% length equivalence (TIER-06/07); no-LLM scope of SC3 documented |
| T-05-04-04 | Tampering | LLM rationale/evidence text breaking the VERIFICATION.md Markdown table | mitigate | rendering is delegated to renderPass2Section/renderPass4Section (Plans 02/03) which emit table-cell-safe text only; verify.ts concatenates the pre-rendered sections, adds no raw model text |
| T-05-SC | Tampering | npm installs | accept | No package installs in Phase 5 (05-RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- `PENSMITH_NO_LLM=1 PENSMITH_ALLOW_PENDING_PROMPT_HASHES=1 node --import tsx --test tests/verify-advisory-isolation.test.ts` — GREEN (guard A isolation + guard B whole-file `loadPrompt`-count==0, both preserved post-wiring).
- `npm run test:tier-contract` — verify-section asserts ## Pass-2 + ## Pass-4 in both tiers + an **UNCLEAR** row.
- Real D-13 0-hit check on verify.ts: `grep -c "loadPrompt" bin/cli/verify.ts` returns 0 (whole file, comments included).
- `npm test` and `npm run lint && npm run typecheck` — GREEN.
</verification>

<success_criteria>
- VERIFICATION.md for a verified section carries a ## Pass-2 and a ## Pass-4 advisory section, written after the blocking verdict.
- Pass 2/4 are wired below the frozen status line and cannot influence it (advisory-isolation test guard A GREEN).
- tier-contract verify-section proves Pass 2/4 parity (modulo prose) across both tiers on the no-LLM CI path (SC#3, scoped to no-LLM).
- Live D-13 chokepoint preserved: whole-file `loadPrompt` count in verify.ts is 0 (comments included), pinned by the committed regression test; locked-16 verb set untouched.
</success_criteria>

<output>
Create `.planning/phases/05-verifier-completeness-pass-2-pass-4/05-04-SUMMARY.md` when done.
</output>
