---
phase: 15-foundation-security-hardening
plan: 07
type: execute
wave: 3
depends_on: ["15-01", "15-06"]
files_modified:
  - bin/lib/honesty.ts
  - references/honesty-framing.md
  - tests/repo-files.test.ts
autonomous: true
requirements: [HARD-05]

must_haves:
  truths:
    - "Before the GPTZero POST the user sees a disclosure that their full paper text is sent to an external service (transparency-only, not 'undetectable')"
    - "A consent gate runs before the POST; declining returns null and performs NO POST"
    - "Non-TTY (CI/piped) silently declines (returns null) rather than exit-3, because honesty is advisory"
    - "--yolo skips the consent gate (disclosure still shows)"
    - "Over-cap input is truncated before the POST with a note"
    - "The GPTZERO_API_KEY value is still never logged"
    - "The honesty-framing.md disclosure extends the LOCKED transparency copy; its standalone SHA-256 pin is re-pinned in the same commit"
  artifacts:
    - path: "bin/lib/honesty.ts"
      provides: "disclosure + consent gate + size cap in scoreWithGptzero"
      contains: "ask"
    - path: "references/honesty-framing.md"
      provides: "GPTZero full-text-transmission disclosure line (extends locked copy)"
      contains: "GPTZero"
  key_links:
    - from: "bin/lib/honesty.ts scoreWithGptzero"
      to: "bin/lib/prompts.ts ask()"
      via: "consent gate before httpFetch (skipped on --yolo; non-TTY default-decline)"
      pattern: "ask\\("
    - from: "references/honesty-framing.md (edited)"
      to: "tests/repo-files.test.ts PINNED (line 208)"
      via: "regenerated SHA-256 updated same commit"
      pattern: "honesty-framing"
---

<objective>
Close HARD-05: the GPTZero honesty check currently POSTs the FULL paper body to an external service with no disclosure, no consent, and no size cap. Add a disclosure (always shown), a consent gate before the POST (default-on, --yolo skips, non-TTY silently declines), and a size cap. Keep the API key unlogged. The disclosure copy extends the LOCKED honesty-framing.md and requires a same-commit standalone re-pin.

Purpose: PRD non-negotiable — honest framing on detection; approval gates default-on. The disclosure must say "sends your full text to GPTZero" and stay transparency-only (never "makes output undetectable"). The score is advisory, so non-TTY declines silently instead of exit-3 (deliberate divergence from the standard exit-3 approval gate).
Output: honesty.ts gates + caps + discloses; honesty-framing.md gains the disclosure; repo-files.test.ts PINNED re-pinned.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-foundation-security-hardening/15-RESEARCH.md
@.planning/phases/15-foundation-security-hardening/15-PATTERNS.md

<read_first>
- bin/lib/honesty.ts (scoreWithGptzero 182-234 — key-absence guard, offline branch, live POST; loadFramingNote pattern; the advisory-never-throws catch)
- references/honesty-framing.md (the LOCKED transparency-only copy to extend)
- bin/cli/add.ts (lines 254-266 — the ask() consent-gate pattern to copy: id/kind/label/default, answer.kind==='confirm'?answer.value)
- bin/lib/prompts.ts (ask() — the consent primitive; its non-TTY exit-3 behavior to deliberately bypass here)
- tests/repo-files.test.ts (line 204-209 — the STANDALONE honesty-framing.md hash-pin test; PINNED at line 208)
- tests/honesty.test.ts (Wave-0 disclosure/consent/cap scaffolds + existing key-absence/offline regression tests)
</read_first>

<interfaces>
Three additions to scoreWithGptzero, in order, BEFORE the httpFetch POST (RESEARCH lines 556-597, PATTERNS lines 409-454):
1. Disclosure (always shown, even on opt-out): render the GPTZero full-text-transmission disclosure from references/honesty-framing.md (NOT inlined in code — loadFramingNote / the existing framing-load pattern). Copy must say it sends the full paper text to GPTZero (api.gptzero.me) and is transparency-only, NOT "undetectable".
2. Consent gate: use ask() from bin/lib/prompts.ts exactly like add.ts:254-266 — `{ id:'honesty-gptzero-consent', kind:'confirm', label:'Send paper text to GPTZero for honesty scoring?', default:true }`. Skip the gate when a --yolo/opts flag is set (consistent with PRD approval-gates). On decline → stdout note + return null (NO POST). Per Pitfall 6 / RESEARCH A4 / open-Q2: in non-TTY (check process.stdout.isTTY) and not-yolo, SILENTLY decline (return null) instead of exit-3 — honesty is advisory and must never break automated export.
3. Size cap (RESEARCH lines 587-595, A3): `GPTZERO_MAX_BYTES` (≈50_000 — configurable; value adjustable at review per A3). If Buffer.byteLength(text,'utf8') > cap, truncate to the cap with a stdout note, POST the truncated body.

Key-never-logged invariant: read process.env['GPTZERO_API_KEY'] for presence only; the value reaches only the x-api-key header. Unchanged — do not introduce any log of the value.

WN-3 standalone re-pin (RESEARCH lines 598-609, PATTERNS lines 442-452): honesty-framing.md is pinned by a STANDALONE test (NOT the PENDING_HASH_PINS loop). After editing it, regenerate `node -e "console.log(require('node:crypto').createHash('sha256').update(require('node:fs').readFileSync('references/honesty-framing.md')).digest('hex'))"` and update the PINNED constant at tests/repo-files.test.ts:208 in THIS commit. Review the transparency-only constraint in CONTRIBUTING.md (the test message points to it).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend honesty-framing.md disclosure + standalone re-pin (HARD-05)</name>
  <files>references/honesty-framing.md, tests/repo-files.test.ts</files>
  <read_first>references/honesty-framing.md, tests/repo-files.test.ts</read_first>
  <action>
Add a disclosure line/section to references/honesty-framing.md stating that the honesty check sends the user's FULL paper text to GPTZero (api.gptzero.me) for AI-detection scoring, and that this is transparency-only — it does NOT make output undetectable. Keep the existing locked transparency copy intact; only EXTEND it (the framing file is the single source of truth — never inline this in code). Then regenerate the SHA-256 and update the PINNED constant at tests/repo-files.test.ts:208 in this same commit (standalone pin test, NOT the PENDING_HASH_PINS loop). Verify the new copy still honors the transparency-only constraint in CONTRIBUTING.md.
  </action>
  <verify>
    <automated>node --import tsx --test tests/repo-files.test.ts 2>&1 | grep -v '^#' | grep -qE '(^|[^0-9])fail 0' && echo OK || echo FAIL</automated>
  </verify>
  <acceptance_criteria>honesty-framing.md mentions GPTZero full-text transmission + transparency-only; the standalone hash-pin test PASSES with the new PINNED value; no other repo-files pins disturbed.</acceptance_criteria>
  <done>Disclosure copy extends the locked framing; PINNED re-pinned same commit; CI green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Disclosure + consent gate + size cap in scoreWithGptzero (HARD-05)</name>
  <files>bin/lib/honesty.ts</files>
  <read_first>bin/lib/honesty.ts, bin/cli/add.ts, bin/lib/prompts.ts, tests/honesty.test.ts</read_first>
  <behavior>
    - Disclosure copy is written to stdout when scoring is attempted (even when later declined)
    - Consent declined (ask→false, or non-TTY + not-yolo) → returns null, performs NO POST
    - --yolo (or opts flag) → skips the consent gate, disclosure still shown, POST proceeds
    - Over-cap input (> GPTZERO_MAX_BYTES) → POST body truncated to the cap with a note
    - GPTZERO_API_KEY value never appears in any logged/printed output
    - Existing key-absence skip + offline-cassette behavior unchanged (regression)
  </behavior>
  <action>
In bin/lib/honesty.ts scoreWithGptzero (182-234), after the key-absence guard and the offline branch, and BEFORE the live httpFetch POST, add the three steps per the interface: (1) render+print the disclosure from honesty-framing.md; (2) the ask() consent gate copied from add.ts:254-266, skipped on --yolo/opts, non-TTY-and-not-yolo → return null (silent decline, NOT exit-3 — guard on process.stdout.isTTY); (3) the GPTZERO_MAX_BYTES truncation with a stdout note. Thread the yolo/consent option into scoreWithGptzero's signature (and its caller in done.ts/the honesty entry point) as needed — match whatever seam tests/honesty.test.ts probes (e.g. an options object); do not invent a divergent seam. Keep the advisory-never-throws catch (229-233) and the key-presence-only read intact. Do NOT log the key value anywhere.
  </action>
  <verify>
    <automated>node --import tsx --test tests/honesty.test.ts 2>&1 | grep -v '^#' | grep -qE '(^|[^0-9])fail 0' && echo OK || echo FAIL</automated>
  </verify>
  <acceptance_criteria>tests/honesty.test.ts disclosure/consent/cap scaffolds go from skip to PASS; the existing key-absence + offline-cassette tests still pass; the key value never appears in output; npx tsc --noEmit clean.</acceptance_criteria>
  <done>Disclosure shown, consent gated (yolo-skippable, non-TTY silent-decline), input size-capped; key still unlogged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user paper body → GPTZero (external third party) | Full document text egresses to api.gptzero.me |
| GPTZERO_API_KEY (secret) → http header | Secret must reach only the header, never a log |
| edited framing file → standalone hash-pin gate | Locked-copy edit must re-pin in lockstep |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Information Disclosure (third-party egress without consent) | honesty.ts GPTZero POST of full paper body | mitigate | disclosure (always shown) + ask() consent gate before POST (default-on, --yolo skips, non-TTY silent-decline) + size cap; declining performs NO POST (ASVS V5/V7) |
| T-15-05b | Information Disclosure (secret leak) | GPTZERO_API_KEY | mitigate | key read for presence only; value reaches only the x-api-key header; never logged/printed (invariant preserved) |
| T-15-05c | Tampering (locked-copy drift) | references/honesty-framing.md vs PINNED | mitigate | standalone SHA-256 re-pin at repo-files.test.ts:208 in the same commit; transparency-only constraint re-checked against CONTRIBUTING.md |
| T-15-05d | Misrepresentation (honest framing) | disclosure wording | mitigate | copy says transparency-only, explicitly NOT "undetectable" — extends the LOCKED framing, never weakens it (PRD non-negotiable) |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | no package installs — reuses ask()/httpFetch/loadFramingNote; RESEARCH audit confirms zero new deps |
</threat_model>

<verification>
`node --import tsx --test tests/honesty.test.ts tests/repo-files.test.ts` — disclosure/consent/cap + framing-pin green. `npm test` — full suite GREEN. `npx tsc --noEmit` clean. Live GPTZero egress is MANUAL-only (PENSMITH_NETWORK_TESTS) per VALIDATION; CI exercises consent/cap/disclosure via the offline seam + mocked ask().
</verification>

<success_criteria>
- Disclosure always shown; consent gate before POST (--yolo skips, non-TTY silent-decline); over-cap truncation
- GPTZERO_API_KEY value never logged
- honesty-framing.md extended (transparency-only) + standalone PINNED re-pinned same commit
- honesty + framing-pin scaffolds green; existing regressions green
</success_criteria>

<output>
Create `.planning/phases/15-foundation-security-hardening/15-07-SUMMARY.md` when done
</output>
