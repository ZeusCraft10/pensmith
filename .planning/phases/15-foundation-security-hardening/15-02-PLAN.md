---
phase: 15-foundation-security-hardening
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - bin/lib/http.ts
  - bin/cli/add.ts
autonomous: true
requirements: [HARD-02, HARD-06]

must_haves:
  truths:
    - "Any request through http.ts to a hostname resolving to RFC1918/loopback/link-local/ULA/0.0.0.0 is rejected BEFORE the socket connects"
    - "Non-https/http schemes are rejected by the SSRF guard"
    - "Public https hosts (crossref, openalex, etc.) pass the guard unchanged"
    - "Redirect hops are re-checked through the SSRF guard before re-fetch"
    - "TokenBucket grants tokens in strict FIFO order via a single grant timer; no token-grab race"
    - "The false 'SSRF mitigation' comment in add.ts is replaced with an accurate one"
  artifacts:
    - path: "bin/lib/http.ts"
      provides: "checkSsrf(url, resolveFn?) guard + isPrivateIp classifier + FIFO TokenBucket"
      contains: "checkSsrf"
    - path: "bin/cli/add.ts"
      provides: "accurate SSRF comment at the add <url> call site"
      contains: "checkSsrf"
  key_links:
    - from: "bin/lib/http.ts callOnce"
      to: "checkSsrf"
      via: "pre-request guard call before request(url, reqInit)"
      pattern: "checkSsrf\\("
    - from: "bin/lib/http.ts TokenBucket.acquire"
      to: "waiters FIFO queue"
      via: "waiters.shift() in a single _scheduleGrant timer"
      pattern: "waiters\\.shift\\(\\)"
---

<objective>
Close HARD-02 (real SSRF guard) and HARD-06 (FIFO-fair TokenBucket) — BOTH live in bin/lib/http.ts, so they MUST ship in one plan to avoid parallel same-file edits. Add a DNS-preflight SSRF guard applied to untrusted (source==='generic') outbound requests, and rebuild TokenBucket with an explicit FIFO waiter queue and a single grant timer.

Purpose: http.ts is the sole network chokepoint (D-06). SSRF protection and concurrency fairness both belong here. The current `add <url>` "SSRF mitigation" comment is FALSE — there is no IP inspection today. TokenBucket waiters currently race on wakeup, letting any coroutine bypass longer-waiting ones past --max-parallel.
Output: http.ts gains checkSsrf + isPrivateIp + a FIFO TokenBucket; add.ts gets an accurate comment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-foundation-security-hardening/15-RESEARCH.md
@.planning/phases/15-foundation-security-hardening/15-PATTERNS.md

<read_first>
- bin/lib/http.ts (callOnce ~495-516 injection site; TokenBucket 252-285; refill 262-268; CACHE_HEADER_ALLOWLIST 408-417 allowlist pattern; the redirect/retry path)
- bin/cli/add.ts (lines 200-205 — the false SSRF comment + httpFetch call)
- bin/lib/budget.ts (Semaphore 166-210 — the FIFO model to copy: waiters array + shift())
- tests/ssrf-guard.test.ts (Wave-0 scaffold — the acceptance target, expects checkSsrf(url, resolveFn?))
- tests/token-bucket-fairness.test.ts (Wave-0 scaffold — expects FIFO completion order)
</read_first>

<interfaces>
checkSsrf signature the Wave-0 scaffold expects (RESEARCH lines 278-289):
- `checkSsrf(url: string, resolveFn?: (hostname: string) => Promise<Array<{address: string; family: number}>>): Promise<void>` — default resolveFn is node:dns/promises `lookup(host, {all:true})`. Throws on private IP / bad scheme; resolves on public host. Injectable resolver keeps CI offline.

isPrivateIp ranges to block (RESEARCH lines 220-240): IPv4 10/8, 172.16-31, 192.168/16, 127/8, 169.254/16, 0.x; IPv6 ::1, fe80:: (link-local), fc/fd (ULA). Per RESEARCH A6, also handle IPv4-mapped IPv6 (::ffff:192.168.x.x) by extracting the embedded v4 and re-classifying.

TokenBucket FIFO shape (RESEARCH lines 838-887): private `waiters: Array<() => void>`, private `timerPending = false`; acquire() fast-path only when tokens>=1 AND waiters.length===0; else push resolver + _scheduleGrant() if !timerPending; _scheduleGrant() runs ONE setTimeout that refills, shifts the oldest waiter, decrements a token, calls it, and recurses if more waiters remain.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SSRF guard in http.ts + add.ts comment (HARD-02)</name>
  <files>bin/lib/http.ts, bin/cli/add.ts</files>
  <read_first>bin/lib/http.ts, bin/cli/add.ts, tests/ssrf-guard.test.ts</read_first>
  <behavior>
    - checkSsrf rejects a URL whose hostname resolves to 127.0.0.1 / 10.x / 169.254.x / ::1 / fc00:: (via injected resolver)
    - checkSsrf rejects a non-https/http scheme (e.g. file:)
    - checkSsrf resolves for a public host (93.184.216.34) via injected resolver
    - An IPv4-mapped IPv6 address (::ffff:127.0.0.1) is classified private
    - A request via httpFetch with source:'generic' triggers checkSsrf; trusted sources do not break (offline cassettes unaffected)
  </behavior>
  <action>
Add `import { lookup as dnsLookup } from 'node:dns/promises'` to http.ts. Add a module-level `isPrivateIp(addr: string): boolean` per RESEARCH lines 220-240 covering all listed v4/v6 ranges PLUS the IPv4-mapped-IPv6 extraction (A6). Add `const SSRF_ALLOWED_SCHEMES = new Set(['https:', 'http:'])` mirroring the CACHE_HEADER_ALLOWLIST pattern at http.ts:408. Add `checkSsrf(url, resolveFn = (h) => dnsLookup(h, {all:true}))` per the interface above: parse the URL (throw on invalid), reject on disallowed scheme, resolve ALL addresses via resolveFn, reject if ANY resolved address isPrivateIp. Fail-CLOSED (reject) on resolver error for untrusted URLs.

Wire checkSsrf into callOnce (~495) BEFORE `request(url, reqInit)`, gated so it runs for untrusted requests only — apply when `opts.source === 'generic'` (the value add.ts passes for user URLs) OR when an explicit `opts.untrusted === true` flag is set; trusted hardcoded API hosts (crossref/openalex/arxiv/pubmed/s2/unpaywall/retraction-watch/gptzero/ddg) bypass so offline cassette tests and internal calls are unaffected.

Redirect handling (RESEARCH open-Q1 / A1): first confirm undici request() default redirect behavior in this codebase's retry/redirect path. If undici auto-follows, set maxRedirections:0 and follow manually, calling checkSsrf on each new hop URL before re-fetch; if it does NOT auto-follow (returns 3xx as a normal response), ensure any caller-driven redirect re-fetch routes back through callOnce so checkSsrf re-fires per hop. Document which branch was taken in the SUMMARY.

In bin/cli/add.ts replace the false comment at line 201 ("...NEVER raw fetch (T-08-04-02 SSRF mitigation)") with an accurate one per PATTERNS lines 147-154: note that the URL routes through http.ts checkSsrf which DNS-resolves the host and blocks RFC1918/loopback/link-local. The httpFetch call already passes source:'generic' — confirm that triggers the guard.
  </action>
  <verify>
    <automated>node --import tsx --test tests/ssrf-guard.test.ts 2>&1 | grep -v '^#' | grep -qE '(^|[^0-9])fail 0' && echo OK || echo FAIL</automated>
  </verify>
  <acceptance_criteria>tests/ssrf-guard.test.ts goes from skip to PASS (all assertions green, 0 fail); existing http.test.ts cassette tests still pass (trusted hosts bypass the guard); npx tsc --noEmit clean.</acceptance_criteria>
  <done>checkSsrf + isPrivateIp shipped and wired into callOnce for untrusted requests; redirect hops re-checked; add.ts comment accurate; SSRF scaffold green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FIFO-fair TokenBucket in http.ts (HARD-06)</name>
  <files>bin/lib/http.ts</files>
  <read_first>bin/lib/http.ts, bin/lib/budget.ts, tests/token-bucket-fairness.test.ts</read_first>
  <behavior>
    - With capacity 1, after the first token is taken, 3 concurrent acquires queue and complete in FIFO (acquisition) order
    - Only one grant timer is pending at a time (no per-waiter timer storm — RESEARCH Pitfall 7)
    - refill() math is unchanged; tokens are consumed-not-returned (RESEARCH A5 — no release-on-exception for a rate bucket)
  </behavior>
  <action>
Rebuild TokenBucket.acquire (http.ts:269-284) per the RESEARCH lines 838-887 / PATTERNS lines 517-521 design, copying the Semaphore FIFO model from budget.ts:166-210: add `private waiters: Array<() => void> = []` and `private timerPending = false`. acquire(): call refill(); fast-path `this.tokens -= 1; return` ONLY when `tokens >= 1 && waiters.length === 0`; otherwise return a Promise that pushes its resolver to waiters and calls `_scheduleGrant()` only if `!timerPending`. Add `private _scheduleGrant()`: set timerPending=true, compute waitMs from the deficit (Math.max(1, ...)), single setTimeout that on fire sets timerPending=false, refills, shifts the oldest waiter, decrements one token and invokes it, then recurses (`_scheduleGrant()`) if waiters remain. Leave refill() (262-268) byte-unchanged.

To make Task-1 of plan 15-01's scaffold green, ensure TokenBucket (or a __TokenBucketForTest seam) is reachable by the test exactly as the Wave-0 scaffold probes it — if the scaffold imports a named export, add that named export; do not invent a second seam. Keep the class otherwise private to the module if the scaffold uses a test-only export hook.

Do NOT add a release()/return-token path — RESEARCH A5: rate-bucket tokens are consumed permanently and refill over time; the concern is FIFO fairness only, not leak. Document this semantic in a code comment.
  </action>
  <verify>
    <automated>node --import tsx --test tests/token-bucket-fairness.test.ts 2>&1 | grep -v '^#' | grep -qE '(^|[^0-9])fail 0' && echo OK || echo FAIL</automated>
  </verify>
  <acceptance_criteria>tests/token-bucket-fairness.test.ts goes from skip to PASS with FIFO order asserted; existing http.test.ts rate-limit behavior unchanged; npx tsc --noEmit clean.</acceptance_criteria>
  <done>TokenBucket grants FIFO via a single timer + waiters.shift(); fairness scaffold green; refill unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI arg / remote response → http.ts → socket | User-supplied or redirect-target URLs cross into outbound network |
| concurrent callers → TokenBucket | Multiple coroutines contend for paid-LLM rate slots |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Spoofing + Info Disclosure (SSRF) | http.ts callOnce outbound request | mitigate | checkSsrf DNS-preflight: scheme allowlist (https/http only) + reject any resolved RFC1918/loopback/link-local/ULA/0.0.0.0/IPv4-mapped address; fail-CLOSED on resolver error; re-check on each redirect hop (ASVS V13.3.1, V5, V9.2.1) |
| T-15-02b | Spoofing | redirect target controlled by remote host | mitigate | checkSsrf re-runs per redirect hop before re-fetch (maxRedirections:0 + manual follow, or per-hop re-route through callOnce) |
| T-15-06 | Denial of Service / cost overrun | TokenBucket non-FIFO grant race | mitigate | explicit FIFO waiters queue + single grant timer so concurrent paid LLM calls respect --max-parallel; no waiter starvation |
| T-15-02-FP | Availability (false positive) | guard blocking legit public hosts | accept | guard only inspects resolved IPs against fixed private ranges; trusted hardcoded API hosts resolve public and pass; bypass for non-generic sources keeps offline cassettes working |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | no package installs — uses node:dns/promises built-in; RESEARCH audit confirms zero new deps |
</threat_model>

<verification>
`node --import tsx --test tests/ssrf-guard.test.ts tests/token-bucket-fairness.test.ts` — both green, 0 fail. `npm test` — full suite GREEN (no regression in http.test.ts cassette/rate-limit tests). `npx tsc --noEmit` clean. SSRF tested via injected resolver only — no real DNS in CI.
</verification>

<success_criteria>
- checkSsrf + isPrivateIp shipped in http.ts and wired into callOnce for untrusted requests, re-checking redirect hops
- add.ts false SSRF comment replaced with an accurate one
- TokenBucket grants FIFO via a single grant timer (no per-waiter timer storm)
- Both Wave-0 scaffolds (ssrf-guard, token-bucket-fairness) green; no http.test.ts regression
</success_criteria>

<output>
Create `.planning/phases/15-foundation-security-hardening/15-02-SUMMARY.md` when done
</output>
