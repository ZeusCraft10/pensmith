---
phase: 15-foundation-security-hardening
plan: 04
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - bin/lib/pii.ts
  - bin/lib/session-log.ts
autonomous: true
requirements: [HARD-03]

must_haves:
  truths:
    - "A PII string at any nesting depth is redacted before the SESSION.log line is written"
    - "A sensitive key at any depth is still redacted (redactKeys recursion preserved)"
    - "The oversize-spill payload is built FROM the recursively-redacted record (no raw bypass)"
    - "Redaction stays deterministic and proto-pollution-safe (null-prototype output containers)"
  artifacts:
    - path: "bin/lib/pii.ts"
      provides: "deepRedactPii(node) recursive string-leaf redactor (exported)"
      contains: "deepRedactPii"
    - path: "bin/lib/session-log.ts"
      provides: "buildRecord using deepRedactPii instead of the top-level-only loop"
      contains: "deepRedactPii"
  key_links:
    - from: "bin/lib/session-log.ts buildRecord"
      to: "bin/lib/pii.ts deepRedactPii"
      via: "import { deepRedactPii } + per-key deep walk after redactKeys"
      pattern: "deepRedactPii\\(safe\\["
---

<objective>
Close HARD-03: make PII redaction recurse into nested string leaves before any SESSION.log write. Today buildRecord redacts only TOP-LEVEL string values; a PII string nested under a non-sensitive key (e.g. `{section:{draft:'call John Smith 555-1234'}}`) escapes redactPii and is written to the OneDrive-synced log.

Purpose: redactKeys already recurses for SENSITIVE-key replacement, but it does NOT apply redactPii to non-sensitive nested string leaves. Add a deepRedactPii helper in pii.ts mirroring walkAndRedact's recursion and call it in buildRecord. The spill payload inherits the fix automatically because it is built from the redacted record.
Output: pii.ts exports deepRedactPii; session-log.ts buildRecord uses it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-foundation-security-hardening/15-RESEARCH.md
@.planning/phases/15-foundation-security-hardening/15-PATTERNS.md

<read_first>
- bin/lib/pii.ts (walkAndRedact 336-366 — recursion model to mirror; isPlainObject 308-312; deepClone 318-332; redactPii — reuse directly)
- bin/lib/session-log.ts (buildRecord 160-184 — the gap loop at 167-176 to replace; the redactPii import at line 29; the spill path that consumes the record)
- tests/session-log.test.ts (Wave-0 deep-PII scaffold — the acceptance target)
</read_first>

<interfaces>
deepRedactPii (RESEARCH lines 333-346 / 820-834, PATTERNS lines 213-235):
- `export function deepRedactPii(node: unknown): unknown` — if string → redactPii(node); if Array → node.map(deepRedactPii); if isPlainObject → new `Object.create(null)` container, recurse every key; else return node unchanged (non-string scalars + opaque objects).
- Reuse the existing `isPlainObject` and `redactPii` from pii.ts directly (same file) — do not redefine.
- Output containers use Object.create(null) (proto-pollution defense, matching deepClone at 321-328).
- Pitfall 4: circular plain objects are unlikely in log payloads; isPlainObject rejects class instances/Maps/Sets so the blast radius is bounded — a WeakSet visited-guard is optional (add only if a test surfaces a cycle).

buildRecord swap (PATTERNS lines 201-218): replace the top-level-only loop (session-log.ts 167-176) with `for (const k of Object.keys(safe)) { safe[k] = deepRedactPii(safe[k]); }` AFTER redactKeys. Import deepRedactPii alongside redactPii at line 29. Order: redactKeys first (sensitive keys at any depth), then deepRedactPii (PII in remaining string leaves).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deepRedactPii to pii.ts (HARD-03)</name>
  <files>bin/lib/pii.ts</files>
  <read_first>bin/lib/pii.ts, tests/session-log.test.ts</read_first>
  <behavior>
    - deepRedactPii('call 555-123-4567') redacts the phone
    - deepRedactPii({a:{b:'John Smith'}}) redacts the nested name leaf
    - deepRedactPii([{x:'pii@example.com'}]) redacts inside arrays
    - deepRedactPii(42) / deepRedactPii(null) / deepRedactPii(classInstance) returned unchanged
    - output objects have null prototype (no __proto__ pollution)
  </behavior>
  <action>
Add an exported `deepRedactPii(node: unknown): unknown` to bin/lib/pii.ts, placed near walkAndRedact so it shares isPlainObject and redactPii. Implement per the interface: string→redactPii, array→map, plain-object→recurse into a fresh Object.create(null) container, else passthrough. Mirror walkAndRedact's structure (336-366) and deepClone's null-prototype output (318-332). Do NOT mutate the input (return a new structure, like deepClone). No new imports needed.
  </action>
  <verify>
    <automated>node --import tsx --test tests/pii.test.ts 2>&1 | grep -v '^#' | grep -qE '(^|[^0-9])fail 0' && echo OK || echo FAIL</automated>
  </verify>
  <acceptance_criteria>deepRedactPii exported; existing tests/pii.test.ts still green; npx tsc --noEmit clean.</acceptance_criteria>
  <done>deepRedactPii recurses every string leaf, returns null-prototype containers, leaves non-strings untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: buildRecord uses deepRedactPii (HARD-03 wiring)</name>
  <files>bin/lib/session-log.ts</files>
  <read_first>bin/lib/session-log.ts, tests/session-log.test.ts</read_first>
  <behavior>
    - A nested PII string + a nested secret key are both absent (redacted) from the written SESSION.log line
    - The oversize-spill payload contains no raw nested PII (built from the redacted record)
    - Existing top-level redaction + sensitive-key tests still pass (regression)
  </behavior>
  <action>
Import deepRedactPii from ./pii.js alongside redactPii at session-log.ts:29. Replace the top-level-only loop at 167-176 with: after `const safe = redactKeys(merged)...`, `for (const k of Object.keys(safe)) { safe[k] = deepRedactPii(safe[k]); }`. Update the now-stale comment ("Nested structures are already walked by redactKeys") to explain the two-stage redaction (redactKeys for sensitive keys at depth, then deepRedactPii for PII in remaining string leaves). Confirm the spill path (writeLineOrTruncate) consumes the record returned by buildRecord so the spill inherits the fix (RESEARCH lines 360-366) — no separate spill edit needed; verify by reading, do not duplicate redaction.
  </action>
  <verify>
    <automated>node --import tsx --test tests/session-log.test.ts 2>&1 | grep -v '^#' | grep -qE '(^|[^0-9])fail 0' && echo OK || echo FAIL</automated>
  </verify>
  <acceptance_criteria>The Wave-0 deep-PII scaffold goes from skip to PASS; existing session-log redaction + spill tests still pass; npx tsc --noEmit clean.</acceptance_criteria>
  <done>buildRecord deep-redacts every string leaf before write; spill inherits the fix; no raw-payload bypass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-memory log payload → SESSION.log file (OneDrive-synced) | Nested PII/secrets in payload cross into a synced-to-cloud file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Information Disclosure | session-log.ts buildRecord top-level-only redaction | mitigate | deepRedactPii recurses every string leaf at any depth before write; redactKeys still removes sensitive keys at depth; the log lives in an OneDrive-synced tree so leakage is high-impact (ASVS V5/V7) |
| T-15-03b | Information Disclosure | oversize-spill payload | mitigate | spill is built FROM the already-deep-redacted record (no raw bypass) — invariant T-01-LOG-03 preserved |
| T-15-03c | Tampering (proto-pollution) | deepRedactPii output containers | mitigate | Object.create(null) containers + isPlainObject guard rejects class instances/Maps/Sets (Pitfall 4) |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | no package installs — pure in-module helper; RESEARCH audit confirms zero new deps |
</threat_model>

<verification>
`node --import tsx --test tests/pii.test.ts tests/session-log.test.ts` — deep-PII scaffold green + existing redaction/spill regressions green. `npm test` — full suite GREEN. `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- deepRedactPii exported from pii.ts, recursing every string leaf with null-prototype output
- buildRecord deep-redacts before write; spill inherits the fix
- Nested-PII + nested-secret scaffold green; existing redaction regressions green
</success_criteria>

<output>
Create `.planning/phases/15-foundation-security-hardening/15-04-SUMMARY.md` when done
</output>
