---
phase: 15-foundation-security-hardening
plan: 08
type: execute
wave: 4
depends_on: ["15-02", "15-03", "15-04", "15-05", "15-06", "15-07"]
files_modified:
  - bin/lib/budget.ts
  - .planning/SECURITY.md
autonomous: true
requirements: [HARD-04, HARD-06]

must_haves:
  truths:
    - "Semaphore.acquire/release documents that bare callers (outside withLock) MUST wrap in try/finally to avoid a slot leak on exception"
    - "Semaphore stays FIFO-correct with no behavior change (it was already correct)"
    - ".planning/SECURITY.md enumerates every milestone security threat marked PROVEN (citing its enforcing test) or UNPROVEN (with a follow-up)"
    - "SECURITY.md covers SSRF, PII/key leak, lock races, prompt injection, zero-trace, pdf supply-chain/DoS, concurrency overrun"
  artifacts:
    - path: ".planning/SECURITY.md"
      provides: "milestone security audit with PROVEN/UNPROVEN-per-test table"
      contains: "PROVEN"
    - path: "bin/lib/budget.ts"
      provides: "Semaphore bare-caller try/finally documentation"
      contains: "try/finally"
  key_links:
    - from: ".planning/SECURITY.md"
      to: "the enforcing tests (ssrf-guard, session-log, lock, pass2-injection, repo-files, pdf-text-bounds, honesty, token-bucket-fairness, budget)"
      via: "each threat row cites its test file"
      pattern: "tests/"
---

<objective>
Close HARD-06 (Semaphore half) and HARD-04a. The Semaphore in budget.ts is ALREADY FIFO-correct with try/finally in withLock — the only gap is that BARE acquire()/release() callers can leak a slot on exception, so this needs a documentation guard + a regression assertion (no behavior change). Then author .planning/SECURITY.md LAST: a milestone security audit marking each threat PROVEN (citing the enforcing test that now exists) or UNPROVEN.

Purpose: SECURITY.md satisfies the deferred secure-phase by cross-referencing every chokepoint threat against a real, now-passing test. It must be authored after Plans 02-07 so each PROVEN claim cites a green test. HARD-06's Semaphore concern is fairness+leak; the code is correct, so this is a doc + regression task, not a rewrite.
Output: budget.ts Semaphore doc note; .planning/SECURITY.md audit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-foundation-security-hardening/15-RESEARCH.md
@.planning/phases/15-foundation-security-hardening/15-PATTERNS.md

<read_first>
- bin/lib/budget.ts (Semaphore 166-210 — already FIFO via waiters.shift(); withLock try/finally; the existing class doc comment to extend)
- tests/budget.test.ts (existing Semaphore acquire/release tests — the regression home for the bare-caller try/finally assertion)
- the per-phase <threat_model> blocks in the Phase-15 PLAN.md files (02-07) + RESEARCH §HARD-04a (lines 370-386) — the threat inventory + enforcing-test map for SECURITY.md
- tests/repo-files.test.ts (zero-trace fixtures 212-236; WN-3 pins — the PROVEN citations for zero-trace + supply-chain rows)
</read_first>

<interfaces>
Semaphore documentation (RESEARCH lines 627-647, PATTERNS lines 602-614):
- Semaphore is already FIFO-correct (waiters.shift()) and withLock already try/finally — DO NOT change behavior. Extend the class doc comment to state explicitly: callers using bare acquire()/release() (outside withLock) MUST wrap the held section in try/finally { release() } or an exception leaks a permit; withLock is the recommended seam. Optionally add a regression test in tests/budget.test.ts asserting: (a) FIFO grant order on contention; (b) withLock releases the permit when fn throws (verify a subsequent acquire succeeds).

SECURITY.md audit (RESEARCH lines 370-386, PATTERNS lines 369-386):
- Write .planning/SECURITY.md with a PROVEN/UNPROVEN table. One row per threat, each citing its enforcing test:
  | Threat | Chokepoint | Enforcing Test | Status |
  - SSRF → http.ts checkSsrf → tests/ssrf-guard.test.ts → PROVEN
  - Key/PII leak to SESSION.log → pii.ts deepRedactPii + redactKeys → tests/pii.test.ts + tests/session-log.test.ts → PROVEN
  - Lock races (BLOCKER-01/02) → lock.ts stubFor canonicalization → tests/lock.test.ts → PROVEN
  - Prompt injection (Pass-2/4) → fenced delimiters → tests/pass2-injection.test.ts → PROVEN
  - Zero-trace in export → exporter/pandoc pipeline → tests/repo-files.test.ts zero-trace fixtures → PROVEN
  - pdf-parse OOM/hang → pdf-text.ts byte cap + timeout → tests/pdf-text-bounds.test.ts → PROVEN
  - pdf-parse supply chain → pinned @1.1.1 + dual-surface pin guard → tests/repo-files.test.ts / 08-03 pin → PROVEN
  - Key never logged → honesty.ts presence-only pattern → tests/honesty.test.ts → PROVEN
  - Supply-chain / prompt drift → prompt-loader.ts EXPECTED_PROMPT_HASHES → tests/repo-files.test.ts WN-3 pins → PROVEN
  - Concurrency overrun → TokenBucket FIFO (http.ts) + Semaphore (budget.ts) → tests/token-bucket-fairness.test.ts + tests/budget.test.ts → PROVEN
- Mark any threat WITHOUT a passing enforcing test as UNPROVEN with a concrete follow-up (e.g. live-DNS SSRF and live-GPTZero egress are MANUAL-only per VALIDATION — list them UNPROVEN-in-CI / PROVEN-manual with the PENSMITH_NETWORK_TESTS instruction).
- Cross-reference the per-phase <threat_model> blocks and the prior-milestone chokepoint threats (pii.ts T-01-06/07/08, lock.ts D-26/D-40, http.ts ARCH-12/13/D-06).
- Consider structuring/spawning via the gsd-security-auditor where it fits; at minimum the doc must be thorough and every PROVEN row must cite a test that is actually green at this point in the wave.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Semaphore bare-caller try/finally + regression (HARD-06)</name>
  <files>bin/lib/budget.ts</files>
  <read_first>bin/lib/budget.ts, tests/budget.test.ts</read_first>
  <action>
Extend the Semaphore class doc comment in bin/lib/budget.ts (the block above line 166) to state explicitly that bare acquire()/release() callers MUST wrap the held section in try/finally { release() } to avoid a permit leak on exception, and that withLock is the recommended seam (it already pairs acquire+release in try/finally). Do NOT change Semaphore behavior — it is already FIFO via waiters.shift() and leak-safe under withLock (RESEARCH lines 627-645). Optionally add to tests/budget.test.ts a regression test asserting (a) FIFO grant order under contention and (b) withLock releases the permit when fn throws (a subsequent acquire succeeds). If tests/budget.test.ts is the cleaner home for the assertion and is not owned by any parallel-wave plan (it is not), add it there.
  </action>
  <verify>
    <automated>node --import tsx --test tests/budget.test.ts 2>&1 | grep -v '^#' | grep -qE '(^|[^0-9])fail 0' && echo OK || echo FAIL</automated>
  </verify>
  <acceptance_criteria>Semaphore doc comment documents the bare-caller try/finally requirement; budget.test.ts green (including any added FIFO/leak regression); npx tsc --noEmit clean; no Semaphore behavior change.</acceptance_criteria>
  <done>Bare-caller try/finally documented; FIFO+leak-safety asserted; no behavior change.</done>
</task>

<task type="auto">
  <name>Task 2: Author .planning/SECURITY.md milestone audit (HARD-04a)</name>
  <files>.planning/SECURITY.md</files>
  <read_first>.planning/phases/15-foundation-security-hardening/15-RESEARCH.md, tests/repo-files.test.ts</read_first>
  <action>
Create .planning/SECURITY.md as a milestone security audit per the interface table. One row per threat (SSRF, PII/key leak, lock races, prompt injection, zero-trace, pdf OOM/hang, pdf supply-chain, key-never-logged, prompt-drift supply-chain, concurrency overrun), each citing its enforcing test and marked PROVEN or UNPROVEN. Before marking any row PROVEN, the cited test MUST be green at this wave (Plans 02-07 have landed) — run each cited test to confirm before claiming PROVEN. Mark live-DNS SSRF and live-GPTZero egress as PROVEN-manual / UNPROVEN-in-CI with the PENSMITH_NETWORK_TESTS manual-verification instruction from VALIDATION. Cross-reference the per-phase <threat_model> blocks and prior-milestone chokepoint threats. Keep it a planning doc (NOT the README — README/docs are Phase 16, explicitly out of scope here).
  </action>
  <verify>
    <automated>test -f .planning/SECURITY.md && grep -q PROVEN .planning/SECURITY.md && grep -qE 'tests/ssrf-guard|tests/lock|tests/session-log|tests/pass2-injection|tests/pdf-text-bounds|tests/honesty|tests/token-bucket-fairness' .planning/SECURITY.md && echo OK || echo FAIL</automated>
  </verify>
  <acceptance_criteria>.planning/SECURITY.md exists with a PROVEN/UNPROVEN table; every PROVEN row cites a test that is green; SSRF/PII/lock/injection/zero-trace/pdf/concurrency all covered; no README/docs scope leak.</acceptance_criteria>
  <done>SECURITY.md enumerates each milestone threat PROVEN-against-its-test or UNPROVEN-with-follow-up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concurrent paid-LLM callers → Semaphore | Bare acquire/release outside withLock can leak a permit on exception |
| milestone threat surface → audit record | The audit must not claim PROVEN without a green enforcing test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06b | Denial of Service / cost overrun | Semaphore bare-caller permit leak | mitigate | document try/finally requirement for bare acquire/release; withLock already leak-safe; regression asserts FIFO + release-on-throw (no behavior change — already correct) |
| T-15-04a | Repudiation (unverified security claims) | SECURITY.md | mitigate | every PROVEN row cites a test confirmed green at this wave; unverifiable-in-CI threats marked UNPROVEN/PROVEN-manual with explicit follow-up — no false assurance |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | no package installs — doc + comment only; RESEARCH audit confirms zero new deps |
</threat_model>

<verification>
`node --import tsx --test tests/budget.test.ts` — green (incl. any added regression). `test -f .planning/SECURITY.md` + the grep gate above. Re-run each test cited as PROVEN in SECURITY.md to confirm it is actually green. `npm test` — full suite GREEN. `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- Semaphore bare-caller try/finally documented; FIFO + release-on-throw asserted; no behavior change
- .planning/SECURITY.md covers all milestone threats PROVEN-against-test or UNPROVEN-with-follow-up; every PROVEN row's test is green
- No README/docs scope leak (Phase 16)
</success_criteria>

<output>
Create `.planning/phases/15-foundation-security-hardening/15-08-SUMMARY.md` when done
</output>
