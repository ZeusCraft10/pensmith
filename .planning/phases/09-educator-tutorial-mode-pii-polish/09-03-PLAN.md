---
phase: 09-educator-tutorial-mode-pii-polish
plan: 03
type: execute
wave: 2
depends_on: ["09-00", "09-01", "09-02"]
files_modified:
  - bin/cli/intake.ts
  - bin/lib/router.ts
  - bin/lib/prompt-loader.ts
  - tests/repo-files.test.ts
  - tests/zero-trace-export.test.ts
autonomous: true
requirements: [ERGO-07]
must_haves:
  truths:
    - "intake accepts --goal (draft|learning|both, default draft) and persists it to config.toml"
    - "PII redaction (opt-in) runs and produces a reviewable diff BEFORE loadPrompt('intake-clarifier')"
    - "raw answers go to INTAKE.raw.local (gitignored); redacted answers to INTAKE.md"
    - "router hard-stops goal=learning after research (no outline/sectioning) per PRD §7.13"
    - "goal=draft and goal=both route through outline unchanged (no regression)"
    - "TUTORIAL.md content never appears in any export format"
    - "tutorial prompt slugs carry real SHA-256 hashes (WN-3 re-pin), no pending bypass needed"
  artifacts:
    - path: "bin/cli/intake.ts"
      provides: "--goal arg + config.toml persistence + PII opt-in diff block before loadPrompt"
      contains: "diffPii"
    - path: "bin/lib/router.ts"
      provides: "goal=learning stop-point after research (best-effort config read, never-throw)"
      contains: "goal"
    - path: "bin/lib/prompt-loader.ts"
      provides: "real SHA-256 for tutorial-section-provenance + tutorial-research-rationale"
    - path: "tests/zero-trace-export.test.ts"
      provides: "TUTORIAL.md export-exclusion assertion"
  key_links:
    - from: "bin/cli/intake.ts diffPii/redactPii block"
      to: "loadPrompt('intake-clarifier')"
      via: "structural source ordering: diffPiiIdx < loadPromptIdx"
      pattern: "diffPii[\\s\\S]*loadPrompt\\('intake-clarifier'\\)"
    - from: "bin/lib/router.ts resolveNextAction"
      to: "config.toml [project] goal"
      via: "best-effort readGoalBestEffort between RESEARCH.md and OUTLINE.md checks"
      pattern: "goal"
    - from: "bin/lib/prompt-loader.ts EXPECTED_PROMPT_HASHES"
      to: "tests/repo-files.test.ts PENDING_HASH_PINS"
      via: "WN-3 atomic re-pin — both surfaces carry the same real SHA-256"
      pattern: "tutorial-section-provenance"
---

<objective>
Wire the goal field end-to-end and close the PII pre-LLM ordering (SC-1 + SC-3): add `--goal` to intake + persist to config.toml, insert the PII opt-in redact+diff block BEFORE `loadPrompt('intake-clarifier')`, write INTAKE.raw.local (raw) + INTAKE.md (redacted), add the goal=learning hard-stop-after-research to router.ts (PRD §7.13), assert TUTORIAL.md export-exclusion, and perform the WN-3 atomic re-pin of the two tutorial prompt slugs to real SHA-256.

Purpose: SC-1 (goal=learning end-state; draft/both unchanged), SC-3 (PII opt-in, pre-LLM, deterministic diff), and the resolved open questions (short enum draft|learning|both; learning hard-stops after research; TUTORIAL.md excluded from exports — DONE-07).
Output: intake.ts + router.ts wiring, export-exclusion test, real prompt hashes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-educator-tutorial-mode-pii-polish/09-RESEARCH.md
@.planning/phases/09-educator-tutorial-mode-pii-polish/09-PATTERNS.md

<read_first>
- bin/cli/intake.ts — the goal arg + PII block go here; the `loadPrompt('intake-clarifier')` call (~line 267) is the pre-LLM boundary; resolvePaperMeta (~line 52) shows the smol-toml config.toml read pattern (use stringify for persistence).
- bin/lib/pii.ts — now exports redactPii + diffPii (from 09-01); intake consumes both.
- bin/lib/router.ts — resolveNextAction (~line 121); the RESEARCH.md check (~line 145) and OUTLINE.md check (~line 146) bracket the stop-point insertion; NEVER-THROW invariant (header) and the outer try/catch backstop already cover a best-effort goal read.
- bin/lib/prompt-loader.ts (EXPECTED_PROMPT_HASHES ~line 91) — the two tutorial sentinels to re-pin; the smoother/claim-support re-pin precedent (~line 116/131) is the exact lockstep pattern.
- tests/repo-files.test.ts (PENDING_HASH_PINS ~line 305) — the matching pin to re-pin in the SAME commit.
- tests/intake-pii-ordering.test.ts, tests/goal-routing.test.ts — the RED-by-skip suites this plan makes GREEN.
- tests/zero-trace-export.test.ts — extend with the TUTORIAL.md exclusion test.
- bin/lib/exporter.ts (exportDraft ~line 450) — exporter reads ONLY inputPath; TUTORIAL.md exclusion is structural-by-construction, the test pins it.

<interfaces>
From bin/lib/pii.ts (after 09-01): `redactPii(text): string`, `diffPii(original, redacted): PiiDiff[]` where `PiiDiff = { span:[number,number]; kind; raw; tag }`.
From bin/lib/router.ts: `resolveNextAction(paperRoot): Promise<RouterDecision>`; the `{ verb:'status'; reason:'done' }` terminal already exists for "nothing left to do".
From smol-toml: `parse(s)`, `stringify(obj)`.
</interfaces>
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: intake --goal + config.toml persistence + PII opt-in diff before loadPrompt</name>
  <files>bin/cli/intake.ts</files>
  <read_first>bin/cli/intake.ts, bin/lib/pii.ts, tests/intake-pii-ordering.test.ts</read_first>
  <action>
Add a `goal` arg to intakeCommand (`type:'string'`, `default:'draft'`, description "Workflow goal: draft (default), learning, or both.") — short enum per resolved open question 2; NOT a 17th verb (the 16-verb bijection is unchanged). Validate to one of `draft|learning|both`, coercing anything else to `draft`.

Add a `piiRedact` opt-in (read from a `--pii-redact` boolean arg defaulting false, OR from config.toml `[project] pii_redaction`). PII is OPT-IN per the non-negotiables.

Insert the PII block STRUCTURALLY BEFORE the `loadPrompt('intake-clarifier')` call (the load-bearing ordering — threat T-09-PII-EGRESS, verified by intake-pii-ordering.test.ts source-grep `diffPiiIdx < loadPromptIdx`). The block: collect the raw answer text (the `--from` seed and/or thesis seed — the user's answers, NEVER the prompt template); if piiRedact is on, compute `const redacted = redactPii(rawAnswers); const diff = diffPii(rawAnswers, redacted);` print the diff for review (one line per PiiDiff: `[kind] "raw" → tag`); write raw answers to `.paper/INTAKE.raw.local` via atomicWriteFile (gitignored), and redacted answers to `.paper/INTAKE.md` via atomicWriteFile. If piiRedact is off, write raw answers to INTAKE.md as today. Only AFTER this block may `loadPrompt('intake-clarifier')` run. Apply the same ordering to BOTH the noLlm branch and the Tier-1 branch so the structural-ordering grep holds in the live path.

Persist `goal` (and pii_redaction if set via arg) into config.toml: read existing config.toml with `parseToml` (best-effort, default `{}`), set `project.goal`, re-serialize with `stringify`, and write via atomicWriteFile (D-07 chokepoint — same as all state files). Per RESEARCH assumption A2 + PRD §10, config.toml is the canonical store; do NOT add a STATE.json field. Keep the write best-effort/non-fatal (a malformed config.toml must not break intake — mirror resolvePaperMeta's try/catch).
  </action>
  <verify>
    <automated>node --import tsx --test tests/intake-pii-ordering.test.ts 2>&1 | grep -E "pass|fail" | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - source: in bin/cli/intake.ts, `indexOf('diffPii') < indexOf("loadPrompt('intake-clarifier')")` (PII before LLM)
    - source: intake has a `goal` arg defaulting `'draft'` and writes `project.goal` to config.toml via atomicWriteFile + smol-toml stringify
    - behavior: with PII opt-in, INTAKE.md is redacted and INTAKE.raw.local holds the raw email; with opt-out, INTAKE.md keeps raw and no INTAKE.raw.local
    - test-command: `node --import tsx --test tests/intake-pii-ordering.test.ts` green (no longer skipped)
  </acceptance_criteria>
  <done>intake persists goal to config.toml and runs opt-in PII redaction+diff before any LLM call, writing raw to INTAKE.raw.local and redacted to INTAKE.md.</done>
</task>

<task type="auto">
  <name>Task 2: Router goal=learning hard-stop-after-research + zero-trace TUTORIAL.md exclusion test</name>
  <files>bin/lib/router.ts, tests/zero-trace-export.test.ts</files>
  <read_first>bin/lib/router.ts, tests/goal-routing.test.ts, tests/zero-trace-export.test.ts, bin/lib/exporter.ts</read_first>
  <action>
In bin/lib/router.ts add a `readGoalBestEffort(paperRoot): 'draft'|'learning'|'both'` helper that reads config.toml `[project] goal` with the existing never-throw discipline (`existsSync` probe + try/catch returning `'draft'` on any error — mirror readSectionState's guard style). Import smol-toml `parse`. The helper must NEVER throw (the resolver's NEVER-THROW invariant is load-bearing; the outer try/catch backstop also covers it).

Insert the stop-point in resolveNextAction AFTER the RESEARCH.md existsSync check and BEFORE the OUTLINE.md check: when `readGoalBestEffort(paperRoot) === 'learning'` AND RESEARCH.md exists, return the learning terminal — `{ verb: 'status', reason: 'done' }` (reuse the existing terminal; do NOT widen RouterDecision unless a distinct terminal is required by the test — prefer the simpler route per PATTERNS). This hard-stops learning mode after research (PRD §7.13 — no outline/sectioning). goal=draft and goal=both fall through to the unchanged outline→sections→compile routing (no regression). The branch in router.ts is a ROUTING decision in the goal-aware tier (CLI/router), NOT a Foundation lib — but note: the lint-tutorial-no-branch invariant greps bin/lib/** for `goal===['"]learning`. Router.ts IS in bin/lib/. RESOLUTION: write the comparison so it does NOT match the forbidden pattern `goal\s*===\s*['"]learning` — use a switch on the helper's return or `goal === LEARNING` with a `const LEARNING = 'learning'` module constant, OR explicitly add bin/lib/router.ts to the lint-tutorial-no-branch exclusion list alongside tutorial.ts (router is the documented goal-aware routing seam, not Foundation orchestration). Choose the exclusion-list approach and document why in a router.ts comment: routing IS the goal-aware tier; the FORBIDDEN coupling is educator-mode branches in WRITE/COMPILE/VERIFY Foundation orchestrators, which router is not. Update tests/lint-tutorial-no-branch.test.ts's exclusion set to include router.ts with a justifying comment.

In tests/zero-trace-export.test.ts add a test: create a `.paper/` with a DRAFT.md (the compiled paper) AND a TUTORIAL.md containing a unique sentinel string (e.g. `TUTORIAL_SENTINEL_PROVENANCE`). Run `exportDraft` with inputPath = DRAFT.md for the `md` format (offline, no Pandoc), then assert the exported output does NOT contain `TUTORIAL_SENTINEL_PROVENANCE` and does NOT contain `pensmith`. This pins the structural guarantee that the exporter reads only inputPath and never concatenates TUTORIAL.md (DONE-07 / resolved open question 3).
  </action>
  <verify>
    <automated>node --import tsx --test tests/goal-routing.test.ts tests/zero-trace-export.test.ts tests/lint-tutorial-no-branch.test.ts 2>&1 | grep -E "pass|fail" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - behavior: goal=learning + RESEARCH.md present resolves to a terminal (status/done), NOT { verb:'outline' }
    - behavior: goal=draft and goal=both + RESEARCH.md present resolve to { verb:'outline' } (no regression)
    - source: lint-tutorial-no-branch.test.ts excludes router.ts (documented goal-aware routing seam) and STILL asserts zero educator-mode branches in write/compile/verify Foundation orchestrators
    - behavior: exported md does not contain the TUTORIAL sentinel or 'pensmith'
    - test-command: `npm test` full suite green
  </acceptance_criteria>
  <done>Router hard-stops goal=learning after research; draft/both unchanged; TUTORIAL.md is provably excluded from exports; the zero-branch invariant remains enforced over the true Foundation orchestrators.</done>
</task>

<task type="auto">
  <name>Task 3: WN-3 atomic re-pin of tutorial prompt slugs to real SHA-256</name>
  <files>bin/lib/prompt-loader.ts, tests/repo-files.test.ts</files>
  <read_first>bin/lib/prompt-loader.ts, tests/repo-files.test.ts, templates/prompts/tutorial-section-provenance.md, templates/prompts/tutorial-research-rationale.md</read_first>
  <action>
Compute the real SHA-256 of each finalized tutorial prompt file (the byte content as committed in 09-00, unchanged). In a SINGLE atomic edit, replace `'tutorial-section-provenance': '__PENDING_HASH_tutorial-section-provenance__'` and `'tutorial-research-rationale': '__PENDING_HASH_tutorial-research-rationale__'` in bin/lib/prompt-loader.ts EXPECTED_PROMPT_HASHES with the real hashes, AND replace the matching PENDING_HASH_PINS entries in tests/repo-files.test.ts with the SAME hashes (WN-3 lockstep — both surfaces re-pin together so drift is structurally impossible; mirror the smoother/claim-support precedent). After this, `loadPrompt('tutorial-section-provenance')` and `loadPrompt('tutorial-research-rationale')` resolve WITHOUT PENSMITH_ALLOW_PENDING_PROMPT_HASHES — runtime drift detection is restored for the tutorial prompts.
  </action>
  <verify>
    <automated>node --import tsx --test tests/repo-files.test.ts 2>&1 | grep -E "pass|fail" | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - source: neither prompt-loader.ts nor repo-files.test.ts contains `__PENDING_HASH_tutorial` after this task
    - behavior: loadPrompt resolves both tutorial slugs without the pending-hash bypass env var
    - test-command: `npm test` full suite green WITHOUT PENSMITH_ALLOW_PENDING_PROMPT_HASHES set
  </acceptance_criteria>
  <done>Both tutorial prompt slugs carry real SHA-256 hashes in lockstep across the loader and repo-files; no pending bypass remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user intake answers → LLM/network | PII must be redacted before crossing into loadPrompt/the model |
| config.toml goal → router routing | goal steers whether learning hard-stops; a corrupt config must not crash routing |
| tutorial prompt files → loader | unpinned prompt must not reach production; re-pin restores drift detection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Information Disclosure | PII egress before redaction | mitigate | redactPii+diffPii block STRUCTURALLY precedes loadPrompt('intake-clarifier') in both noLlm and Tier-1 branches; intake-pii-ordering.test.ts asserts diffPiiIdx < loadPromptIdx |
| T-09-03-02 | Information Disclosure | raw PII persisted/committed | mitigate | raw → INTAKE.raw.local (gitignored, 09-00); redacted → INTAKE.md; atomicWriteFile chokepoint |
| T-09-03-03 | Tampering | educator-mode branch leakage via router | mitigate | router is the documented goal-aware ROUTING tier, added to the lint exclusion list with justification; the invariant still asserts zero educator branches in write/compile/verify Foundation orchestrators |
| T-09-03-04 | Information Disclosure | TUTORIAL.md leaking into exports | mitigate | exporter reads only inputPath (structural); zero-trace test pins a TUTORIAL sentinel absent from exports (DONE-07) |
| T-09-03-05 | Tampering | unpinned tutorial prompt reaching production | mitigate | WN-3 atomic re-pin to real SHA-256 across loader + repo-files in one commit; drift detection restored |
| T-09-03-06 | Denial of Service | corrupt config.toml crashing router | mitigate | readGoalBestEffort never-throws (existsSync + try/catch → 'draft'); outer backstop covers it |
</threat_model>

<verification>
- `node --import tsx --test tests/intake-pii-ordering.test.ts tests/goal-routing.test.ts tests/zero-trace-export.test.ts tests/lint-tutorial-no-branch.test.ts tests/repo-files.test.ts` — all green.
- `npm test` full suite green WITHOUT PENSMITH_ALLOW_PENDING_PROMPT_HASHES.
- `npm run check` (lint + typecheck + build + tier-contract + tests + manifests) green before /gsd:verify-work.
- grep: `diffPii` precedes `loadPrompt('intake-clarifier')` in intake.ts.
</verification>

<success_criteria>
goal is persisted + drives the learning hard-stop after research (draft/both unchanged); opt-in PII runs before any LLM call with a deterministic reviewable diff (raw→INTAKE.raw.local, redacted→INTAKE.md); TUTORIAL.md is provably export-excluded; tutorial prompts carry real hashes. ERGO-07 fully satisfied.
</success_criteria>

<output>
Create `.planning/phases/09-educator-tutorial-mode-pii-polish/09-03-SUMMARY.md` when done.
</output>
