---
phase: 09-educator-tutorial-mode-pii-polish
plan: 01
type: execute
wave: 1
depends_on: ["09-00"]
files_modified:
  - bin/lib/pii.ts
  - bin/lib/name-suppression.json
autonomous: true
requirements: [ERGO-07]
must_haves:
  truths:
    - "PII classifier fires on IP and IBAN-like tokens in addition to the Phase-1 classes"
    - "NAME false positives on academic terms (months, section headings) are suppressed without an NLP dependency"
    - "diffPii produces a deterministic, idempotent, purely-positional reviewable diff"
    - "pii.ts remains a pure module: no I/O, no fs, no fetch, no randomness, no Date.now"
  artifacts:
    - path: "bin/lib/pii.ts"
      provides: "extended classifyPii (IP/IBAN), NAME suppression, pure diffPii export"
      contains: "export function diffPii"
    - path: "bin/lib/name-suppression.json"
      provides: "~500-token curated common-capitalized non-name dictionary (deterministic, bundled, not a dep)"
  key_links:
    - from: "bin/lib/pii.ts diffPii"
      to: "bin/lib/pii.ts classifyPii"
      via: "diffPii derives spans from classifyPii(original)"
      pattern: "classifyPii\\(original\\)"
    - from: "bin/lib/pii.ts classifyPii (NAME branch)"
      to: "bin/lib/name-suppression.json"
      via: "Set membership lookup suppresses single-token / first-token-only matches"
      pattern: "NAME_SUPPRESSION"
---

<objective>
Polish bin/lib/pii.ts beyond regex-only (ARCH-17 / ERGO-07 PII requirement) while preserving the pure-module guarantee. Add IP + IBAN-like pattern classes, a curated NAME-suppression dictionary (pure-Node, deterministic — NO NLP/Presidio, deferred to v2 per PII-V2-01), and a pure `diffPii()` producing a deterministic reviewable diff. All Phase-1 exports stay byte-compatible.

Purpose: SC-3 (PII redaction produces a deterministic diff the user can review) and the "beyond regex-only" goal. The diff is what intake.ts (09-03) displays before persisting.
Output: extended pii.ts + bundled name-suppression.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-educator-tutorial-mode-pii-polish/09-RESEARCH.md
@.planning/phases/09-educator-tutorial-mode-pii-polish/09-PATTERNS.md

<read_first>
- bin/lib/pii.ts — extend IN PLACE; never break existing exports (PiiKind, PiiMatch, classifyPii, redactPii, redactKeys). The pure-module header (lines 1-21) is the contract.
- tests/pii-polish.test.ts — the RED suite landed in 09-00; this plan makes it GREEN. Read it for the exact API the tests expect (diffPii signature, IP/IBAN kinds, suppression behavior).
- tests/fixtures/pii-polish-corpus.ts — the corpus this implementation must satisfy.
- tests/pii.test.ts — the EXISTING Phase-1 suite that MUST stay green (no regression on EMAIL/PHONE/SSN/NAME/DATE).

<interfaces>
From bin/lib/pii.ts (current, Phase 1):
```typescript
export type PiiKind = 'EMAIL' | 'PHONE' | 'SSN' | 'NAME' | 'DATE';
export interface PiiMatch { kind: PiiKind; span: [number, number]; raw: string; }
export function classifyPii(text: string): PiiMatch[];   // source-order, no overlaps; longer-wins
export function redactPii(text: string): string;          // splice spans right-to-left
export function redactKeys<T>(obj: T): T;
```
Phase 9 extends PiiKind to add `'IP' | 'IBAN'` and adds `diffPii`.
</interfaces>
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend PiiKind + IP/IBAN patterns + NAME suppression dictionary</name>
  <files>bin/lib/pii.ts, bin/lib/name-suppression.json</files>
  <read_first>bin/lib/pii.ts, tests/pii-polish.test.ts, tests/fixtures/pii-polish-corpus.ts, tests/pii.test.ts</read_first>
  <behavior>
    - classifyPii("192.168.1.1") returns one match kind IP
    - classifyPii("GB29NWBK60161331926819") returns one match kind IBAN
    - classifyPii("Results Section") returns NO NAME match (both tokens in suppression set)
    - classifyPii("January March") returns NO NAME match
    - classifyPii("Jane Smith") returns a NAME match (neither token suppressed)
    - classifyPii("In Smith") returns a NAME match (last token "Smith" not in suppression set per Pitfall 6)
    - existing EMAIL/PHONE/SSN/NAME/DATE behavior unchanged (tests/pii.test.ts stays green)
  </behavior>
  <action>
Extend the `PiiKind` union to `'EMAIL' | 'PHONE' | 'SSN' | 'NAME' | 'DATE' | 'IP' | 'IBAN'`. Add `RE_IP = /\b(?:\d{1,3}\.){3}\d{1,3}\b/g` and `RE_IBAN_LIKE = /\b[A-Z]{2}\d{2}[A-Z0-9]{4,30}\b/g` (the {4,30} length cap preserves the T-01-REDOS-01 bounded-quantifier guarantee). Insert IP and IBAN into the PATTERNS array BEFORE the NAME entry (earlier = higher overlap-resolution priority per the existing sort/overlap pass).

Create bin/lib/name-suppression.json — a JSON array of ~500 curated common capitalized non-name tokens: the 12 months, 7 weekdays, common section/academic headings (Introduction, Methods, Results, Discussion, Conclusion, Abstract, Background, Section, Figure, Table, Appendix, References), common sentence-leading capitalized words (The, In, On, At, This, These, That, We, Our), and additional academic/common tokens. This is a bundled data file, NOT a runtime dependency. Import it in pii.ts and build `const NAME_SUPPRESSION = new Set<string>(...)` at module load (still pure — reading a bundled JSON via static import is not runtime I/O).

In classifyPii, after collecting NAME candidates, apply the suppression rule from RESEARCH Pitfall 6: a NAME match is SUPPRESSED only if (a) the ENTIRE match (all tokens) is in NAME_SUPPRESSION (single- or multi-token all-suppressed case), OR (b) the FIRST token is in NAME_SUPPRESSION AND the remaining token(s) are ALSO each in NAME_SUPPRESSION. A multi-token name whose LAST token is NOT in the set is KEPT (so "In Smith" survives). Implement as a filter on NAME-kind candidates before the overlap-resolution pass; IP/IBAN/other kinds are never suppressed.

Keep pii.ts pure: NO fs.readFile (use a static JSON import), NO Date.now, NO Math.random. Add an inline comment block above RE_IP/RE_IBAN_LIKE documenting the Phase-9 additions (matching the existing per-pattern comment convention so the diff stays reviewable, D-49).
  </action>
  <verify>
    <automated>node --import tsx --test tests/pii.test.ts tests/pii-polish.test.ts 2>&1 | grep -E "pass|fail" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - test-command: `node --import tsx --test tests/pii.test.ts` passes (Phase-1 no-regression)
    - test-command: the IP/IBAN/NAME-suppression cases in tests/pii-polish.test.ts pass (no longer skipped)
    - source: pii.ts contains RE_IP, RE_IBAN_LIKE, and NAME_SUPPRESSION Set built from name-suppression.json
    - behavior: "In Smith" classifies as NAME; "Results Section" does not
  </acceptance_criteria>
  <done>classifyPii fires on IP/IBAN and suppresses academic-term NAME false positives per the two-token rule; Phase-1 suite unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure deterministic diffPii export</name>
  <files>bin/lib/pii.ts</files>
  <read_first>bin/lib/pii.ts, tests/pii-polish.test.ts</read_first>
  <behavior>
    - diffPii(x, redactPii(x)) returns one PiiDiff per classified span: { span:[start,end], kind, raw, tag:`[REDACTED:${kind}]` }
    - diffPii(x, y) === diffPii(x, y) on the same input (deterministic; deepEqual)
    - diffPii(redactPii(x), redactPii(redactPii(x))).length === 0 (idempotence — already-redacted text has no new spans)
    - diffPii output spans are sorted by start position (stable)
    - diffPii is pure: no Date.now, no Math.random, no randomUUID, no I/O
  </behavior>
  <action>
Add `export interface PiiDiff { span: [number, number]; kind: PiiKind; raw: string; tag: string }` and `export function diffPii(original: string, redacted: string): PiiDiff[]`. Derive the diff from `classifyPii(original)` (which is already deterministic and overlap-resolved): map each PiiMatch to a PiiDiff with `tag = `[REDACTED:${m.kind}]``. The `redacted` param is accepted for API symmetry; the implementation derives spans from `original` only (do NOT diff strings character-by-character — that would be non-deterministic under tied spans). Spans inherit classifyPii's start-ascending order. NO Date.now / Math.random / randomUUID anywhere — purely positional math. Update the pure-module header comment to note diffPii is the new pure export.
  </action>
  <verify>
    <automated>node --import tsx --test tests/pii-polish.test.ts 2>&1 | grep -E "pass|fail" | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - source: bin/lib/pii.ts exports `diffPii` and `PiiDiff`
    - behavior: diffPii determinism + idempotence tests in pii-polish.test.ts pass
    - source: no Math.random / Date.now / randomUUID in pii.ts (grep confirms zero)
    - test-command: `node --import tsx --test tests/pii-polish.test.ts` exits 0 with all diffPii tests green
  </acceptance_criteria>
  <done>diffPii is a pure, deterministic, idempotent reviewable-diff export derived from classifyPii; pii-polish suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user intake answers → pii.ts | untrusted free text classified for PII before any persistence or LLM egress |
| name-suppression.json → classifyPii | bundled data influencing NAME match suppression (over-suppression risk) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Information Disclosure | NAME over-suppression | mitigate | two-token rule (suppress only if ALL tokens suppressed); corpus test pins "In Smith" KEPT and "Results Section" suppressed |
| T-09-01-02 | Tampering | non-deterministic diff | mitigate | diffPii is pure positional math derived from classifyPii; no Date.now/Math.random; determinism + idempotence tests are blocking |
| T-09-01-03 | DoS | regex ReDoS on new patterns | mitigate | RE_IP fixed-shape, RE_IBAN_LIKE bounded {4,30}; no nested quantifiers (T-01-REDOS-01 invariant preserved) |
| T-09-01-04 | Information Disclosure | PII slipping past classifier | accept | regex+dictionary recall is bounded; stronger NLP (Presidio) deferred to v2 per PII-V2-01; false-negatives are the documented acceptable failure mode (D-49) |
</threat_model>

<verification>
- `node --import tsx --test tests/pii.test.ts tests/pii-polish.test.ts` — both green.
- `tsc --noEmit` passes; pii.ts imports name-suppression.json statically.
- `npm test` full suite green (no regression).
</verification>

<success_criteria>
pii.ts fires on IP/IBAN, suppresses academic-term NAME false positives via a pure dictionary, and exports a deterministic idempotent diffPii — all while preserving the pure-module guarantee and Phase-1 behavior.
</success_criteria>

<output>
Create `.planning/phases/09-educator-tutorial-mode-pii-polish/09-01-SUMMARY.md` when done.
</output>
