---
phase: 09-educator-tutorial-mode-pii-polish
plan: 02
type: execute
wave: 1
depends_on: ["09-00"]
files_modified:
  - bin/lib/tutorial.ts
  - bin/lib/write-orchestrator.ts
  - bin/cli/write.ts
autonomous: true
requirements: [ERGO-07]
must_haves:
  truths:
    - "goal=learning|both constructs a TutorialSubscriber in the CLI tier; goal=draft constructs none"
    - "TUTORIAL.md receives per-claim annotated provenance rendered from assigned_sources"
    - "Foundation (write-orchestrator.ts) gains exactly ONE callback-invocation guard, never a goal check"
    - "TutorialSubscriber is imported ONLY by bin/cli/*; never by any bin/lib/* except tutorial.ts"
    - "TUTORIAL.md is the ONLY file the subscriber writes — never .paper/sections/ or DRAFT.md"
  artifacts:
    - path: "bin/lib/tutorial.ts"
      provides: "full TutorialSubscriber (enqueue chain, never-throw emit, flush, provenance render)"
      contains: "class TutorialSubscriber"
    - path: "bin/lib/write-orchestrator.ts"
      provides: "additive onSectionWritten? callback in RunAllSectionsOpts, invoked after each fulfilled section"
      contains: "onSectionWritten"
    - path: "bin/cli/write.ts"
      provides: "goal-aware subscriber construction + subscriber?.emit(...) in the writeSection callback"
      contains: "subscriber?.emit"
  key_links:
    - from: "bin/cli/write.ts"
      to: "bin/lib/tutorial.ts TutorialSubscriber"
      via: "CLI-tier construction gated on goal; passes a callback into runAllSections"
      pattern: "new TutorialSubscriber"
    - from: "bin/lib/write-orchestrator.ts"
      to: "opts.onSectionWritten"
      via: "if (opts.onSectionWritten) invoked once after each fulfilled section with assignedSources"
      pattern: "opts.onSectionWritten"
    - from: "bin/lib/tutorial.ts"
      to: ".paper/TUTORIAL.md"
      via: "atomicAppendFile via enqueue chain"
      pattern: "atomicAppendFile"
---

<objective>
Implement the observer/dependency-injection architecture (SC-2): the full `TutorialSubscriber` in bin/lib/tutorial.ts, an ADDITIVE optional `onSectionWritten?` callback seam in write-orchestrator.ts (Foundation gets exactly one callback-invocation guard — NOT a goal check), and the goal-aware `subscriber?.emit(...)` wiring in bin/cli/write.ts. The `?.` optional-chain IS the entire zero-branch mechanism: goal=draft holds `undefined` and the call is a no-op; Foundation never imports tutorial.ts and never learns what `goal` is.

Purpose: SC-1 (per-claim annotated provenance to TUTORIAL.md) + SC-2 (observer architecture, zero educator-mode branches in Foundation). goal=draft/both DRAFT.md pipeline stays byte-unchanged.
Output: tutorial.ts (full), write-orchestrator.ts seam, write.ts wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-educator-tutorial-mode-pii-polish/09-RESEARCH.md
@.planning/phases/09-educator-tutorial-mode-pii-polish/09-PATTERNS.md

<read_first>
- bin/lib/tutorial.ts — the 09-00 stub; this plan fills the real body. Keep imports limited to node:events + ./atomic-write.js + ./paths.js (NEVER ./pii.js — subscriber handles post-redaction metadata only).
- bin/lib/session-log.ts (enqueue ~line 251; makeLogger emit ~line 351; child/close ~line 380) — copy the `chain`/`enqueue` sequential-append pattern and the never-throw try/catch-swallow emit contract and the `close()`/drain pattern (tutorial's `flush()`).
- bin/lib/write-orchestrator.ts (RunAllSectionsOpts ~line 57; the fulfilled branch ~line 163; the plans map ~line 120-125) — the ADDITIVE seam goes in the fulfilled branch; the `plans` map already holds PlanFrontmatter with assigned_sources.
- bin/cli/write.ts (the wave-mode writeSection callback ~line 204-215; writeOneSection ~line 125) — the subscriber construction + emit wiring goes here; mirror the intake.ts runStyleProducerNonFatal try/catch non-fatal guard.
- bin/cli/intake.ts (runStyleProducerNonFatal ~line 144; resolvePaperMeta config.toml read ~line 52) — the non-fatal subscriber-construction guard + the config.toml read pattern for goal.
- tests/tutorial-observer.test.ts, tests/tutorial-provenance.test.ts, tests/lint-tutorial-no-branch.test.ts — the contracts this plan makes GREEN.

<interfaces>
From bin/lib/write-orchestrator.ts:
```typescript
export interface RunAllSectionsOpts {
  maxParallel: number;
  writeSection: (node: SectionNode) => Promise<void>;
  only?: string[];
}
export async function runAllSections(paperRoot: string, opts: RunAllSectionsOpts): Promise<WaveResult[]>;
```
From bin/lib/schemas/plan-frontmatter.ts: PlanFrontmatter has `assigned_sources: string[]`.
From bin/lib/paths.ts: `paperDir(paperRoot)`, `sectionPlan(n, slug, paperRoot)`.
</interfaces>
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Full TutorialSubscriber (enqueue chain, never-throw, flush, provenance render)</name>
  <files>bin/lib/tutorial.ts</files>
  <read_first>bin/lib/tutorial.ts, bin/lib/session-log.ts, tests/tutorial-observer.test.ts, tests/tutorial-provenance.test.ts</read_first>
  <behavior>
    - new TutorialSubscriber({ tutorialPath, goal:'learning' }) then emit({kind:'section.written', payload:{n,slug,assignedSources}}) then await flush() → TUTORIAL.md exists with a "## Section N" provenance header naming the assigned citekeys
    - emit() never throws even on a malformed payload (swallow + continue)
    - TUTORIAL.md content never contains a ".paper/sections/" path string
    - emits append in order (enqueue chain preserves emission sequence)
    - flush() resolves after all queued appends settle
  </behavior>
  <action>
Fill bin/lib/tutorial.ts. Define `TutorialEventKind` (`'research.done' | 'outline.done' | 'section.written' | 'section.verified' | 'compile.done'`) and `TutorialEvent { kind; payload: Record<string, unknown> }` (already stubbed in 09-00 — keep stable). Implement `class TutorialSubscriber`:
- constructor `{ tutorialPath: string; goal: 'learning' | 'both' }` stores both, creates an internal `EventEmitter`, and wires handlers in a private `_wire()`.
- copy the session-log `let chain = Promise.resolve(); enqueue(work)` sequential-append pattern (the `.then(work, work)` two-handler form so one rejection never breaks the chain) for ordered TUTORIAL.md appends.
- `emit(event: TutorialEvent): void` forwards to the EventEmitter; handlers enqueue an `atomicAppendFile(this.tutorialPath, block)` wrapped in try/catch-swallow (NEVER throw — mirror makeLogger's emit contract). Errors in tutorial emission must never surface into Foundation.
- `_emitSectionProvenance(payload)`: render a markdown block titled `## Section ${n} — ${slug}: Source Provenance` that, for each citekey in `assignedSources`, emits a "Why <citekey> was assigned here" teaching line. Render from the payload's assignedSources (citekeys) — do NOT re-read DRAFT.md and NEVER include a `.paper/sections/` path in the output. (The hash-pinned tutorial-section-provenance prompt is the Tier-1 model-driven render; the Tier-2 deterministic render here is a structured fallback that satisfies the provenance contract offline.)
- handlers for the other event kinds (research.done → source-selection rationale; outline.done → structure note; section.verified → verdict walkthrough; compile.done → transition note) may be thinner but must each append a labeled block via the same enqueue path.
- `flush(): Promise<void>` awaits the chain (drain) — the CLI verb awaits it before exiting.
- Imports: `node:events`, `./atomic-write.js` (atomicAppendFile/atomicWriteFile — D-07 chokepoint, no raw fs.writeFile), `node:path`. DO NOT import ./pii.js or anything that would couple Foundation. The subscriber writes ONLY to its `tutorialPath`; never to `.paper/sections/` or DRAFT.md (Pitfall 2).
  </action>
  <verify>
    <automated>node --import tsx --test tests/tutorial-observer.test.ts tests/tutorial-provenance.test.ts 2>&1 | grep -E "pass|fail" | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - source: tutorial.ts uses atomicAppendFile (no raw fs.writeFile — D-07) and imports neither pii.js nor any Foundation orchestrator
    - behavior: emit→flush writes a "## Section" provenance header naming citekeys; emit never throws
    - source: subscriber writes only to this.tutorialPath; grep confirms no `.paper/sections/` write target
    - test-command: `node --import tsx --test tests/tutorial-observer.test.ts tests/tutorial-provenance.test.ts` green
  </acceptance_criteria>
  <done>TutorialSubscriber renders per-claim provenance to TUTORIAL.md via an ordered never-throw enqueue chain, with flush() draining; tutorial tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Additive onSectionWritten? seam in write-orchestrator + goal-aware emit in write.ts</name>
  <files>bin/lib/write-orchestrator.ts, bin/cli/write.ts</files>
  <read_first>bin/lib/write-orchestrator.ts, bin/cli/write.ts, bin/cli/intake.ts, tests/lint-tutorial-no-branch.test.ts, tests/tutorial-observer.test.ts</read_first>
  <action>
In bin/lib/write-orchestrator.ts: add an exported `SectionWrittenCallback` type `(opts: { n: number; slug: string; planPath: string; assignedSources: string[] }) => void` and an OPTIONAL `onSectionWritten?: SectionWrittenCallback` field to `RunAllSectionsOpts`. In the fulfilled branch (where `node.status='done'` and the result is pushed), add the SINGLE permitted Foundation guard: `if (opts.onSectionWritten) { opts.onSectionWritten({ n: node.n, slug: node.slug, planPath: sectionPlan(node.n, node.slug, paperRoot), assignedSources: plans.get(node.slug)?.assigned_sources ?? [] }); }`. This is a callback-invocation guard, NOT a goal check — write-orchestrator.ts must contain ZERO reference to `goal`, `learning`, `educator_mode`, or `TutorialSubscriber` (the lint-tutorial-no-branch test enforces this). Do NOT import tutorial.ts. The `plans` map already holds PlanFrontmatter (line ~124) so assigned_sources is in hand without a re-read.

In bin/cli/write.ts: add a `readGoalFromConfig(cwd)` helper (best-effort: parse config.toml `[project] goal` via smol-toml, default `'draft'` on any error — mirror resolvePaperMeta's try/catch). Before `runAllSections`, construct the subscriber in the CLI tier ONLY: `const goal = readGoalFromConfig(paperRoot); let subscriber: TutorialSubscriber | undefined; try { subscriber = (goal === 'learning' || goal === 'both') ? new TutorialSubscriber({ tutorialPath: path.join(paperDir(paperRoot), 'TUTORIAL.md'), goal }) : undefined; } catch (e) { /* WARN to stderr, subscriber stays undefined — non-fatal, mirror runStyleProducerNonFatal */ }`. Pass `onSectionWritten: subscriber ? (evt) => subscriber.emit({ kind: 'section.written', payload: evt }) : undefined` into the runAllSections opts. The `subscriber?.` / `subscriber ? ... : undefined` is the zero-branch mechanism — goal=draft yields undefined and the Foundation guard is a no-op. After the wave loop completes, `await subscriber?.flush()` before returning so TUTORIAL.md is complete. Single-section `pensmith write <n>` path: also emit + flush if a subscriber exists (so a single-section re-do in learning mode still annotates) — but the byte content of DRAFT.md must remain unchanged for all goals.
  </action>
  <verify>
    <automated>node --import tsx --test tests/lint-tutorial-no-branch.test.ts tests/tutorial-observer.test.ts 2>&1 | grep -E "pass|fail" | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - source: write-orchestrator.ts contains `onSectionWritten` and a single `if (opts.onSectionWritten)` guard, and ZERO occurrences of goal/learning/educator_mode/TutorialSubscriber
    - source: write.ts contains `new TutorialSubscriber` and `subscriber?.emit` / `subscriber ? ... : undefined`
    - behavior: lint-tutorial-no-branch.test.ts still PASSES (Foundation + workflows clean)
    - behavior: goal=draft writes NO TUTORIAL.md (tutorial-observer zero-activation test green); goal=learning|both writes provenance
    - test-command: `tsc --noEmit` passes; `npm test` full suite green
  </acceptance_criteria>
  <done>Foundation has exactly one additive callback guard; the CLI tier owns all goal awareness via subscriber?.emit; zero-branch invariant holds; DRAFT.md byte-unchanged across goals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI tier (goal-aware) → Foundation (goal-unaware) | the onSectionWritten callback crosses this boundary; Foundation must never reach back for goal |
| TutorialSubscriber → .paper filesystem | subscriber writes; must be confined to TUTORIAL.md, never section/DRAFT artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Tampering | educator-mode branch leakage into Foundation | mitigate | write-orchestrator gets ONE callback-invocation guard (not a goal check); lint-tutorial-no-branch.test.ts greps bin/lib/** + workflows/** for educator_mode/goal===learning/TutorialSubscriber → asserts zero |
| T-09-02-02 | Information Disclosure | subscriber writing to section/DRAFT artifacts | mitigate | subscriber writes ONLY to this.tutorialPath; provenance rendered from citekeys/verdicts (no raw user PII); test asserts no `.paper/sections/` reference in TUTORIAL.md |
| T-09-02-03 | Denial of Service | tutorial emit error crashing the write verb | mitigate | emit() try/catch-swallows (never-throw, session-log contract); subscriber construction wrapped non-fatal in write.ts |
| T-09-02-04 | Tampering | goal=draft/both DRAFT.md regression | mitigate | subscriber only appends to TUTORIAL.md; DRAFT.md path untouched; goal=draft yields undefined subscriber (zero-activation test) |
</threat_model>

<verification>
- `node --import tsx --test tests/lint-tutorial-no-branch.test.ts tests/tutorial-observer.test.ts tests/tutorial-provenance.test.ts` — all green.
- `tsc --noEmit` passes.
- `npm test` full suite green (write-orchestrator + write CLI no-regression).
- grep: zero `goal|learning|educator_mode|TutorialSubscriber` in write-orchestrator.ts.
</verification>

<success_criteria>
Observer/DI architecture lands: Foundation gets one additive callback guard, the CLI tier owns goal via subscriber?.emit, TUTORIAL.md carries per-claim provenance, and the zero-branch invariant + draft/both no-regression hold.
</success_criteria>

<output>
Create `.planning/phases/09-educator-tutorial-mode-pii-polish/09-02-SUMMARY.md` when done.
</output>
