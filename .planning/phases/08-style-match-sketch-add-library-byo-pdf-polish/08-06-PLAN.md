---
phase: 08-style-match-sketch-add-library-byo-pdf-polish
plan: 06
type: execute
wave: 4
depends_on: ["08-01", "08-04", "08-05"]
files_modified:
  - workflows/list.md
  - workflows/open.md
  - workflows/sketch.md
  - workflows/add.md
  - tests/tier-contract.test.ts
autonomous: true
requirements: [LIB-02, LIB-03, ERGO-05, ERGO-06]
must_haves:
  truths:
    - "Each of list/open/sketch/add ships a real workflow body with a <capability_check> block (Tier 1 / Tier 2 degradation per ARCH-03)"
    - "tier-contract parity holds for the four promoted verbs across both tiers (modulo prose)"
    - "The 16-verb bijection is intact — no 17th verb, no colon-prefix or -section alias leaked into UX02_VERBS"
  artifacts:
    - path: "workflows/list.md"
      provides: "filled list workflow body with capability_check"
      contains: "capability_check"
    - path: "workflows/open.md"
      provides: "filled open workflow body"
      contains: "capability_check"
    - path: "workflows/sketch.md"
      provides: "filled Socratic sketch workflow body"
      contains: "capability_check"
    - path: "workflows/add.md"
      provides: "filled add workflow body"
      contains: "capability_check"
  key_links:
    - from: "tests/tier-contract.test.ts"
      to: "bin/cli/{list,open,sketch,add}.ts via UX02_VERBS"
      via: "promoted-verb parity cases"
      pattern: "list|open|sketch|add"
---

<objective>
Fill the four Phase-2 workflow stubs (list/open/sketch/add) with real bodies including <capability_check> blocks for Tier 1 / Tier 2 degradation, and extend tier-contract parity to cover the four promoted verbs. Re-assert the 16-verb bijection guards so no 17th verb (and no colon-prefix or -section alias) leaked in. This is the merge-gate closer for Phase 8.

Purpose: Two-tier non-negotiable — every workflow body added must have a tier-contract entry (CONTRIBUTING.md D-24 rule). Closes SC-1 (list/open both tiers) and SC-3/SC-4 (sketch/add both tiers).
Output: 4 filled workflow bodies + tier-contract parity cases + bijection guard re-assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-RESEARCH.md
@.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-PATTERNS.md
@.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-01-SUMMARY.md
@.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-04-SUMMARY.md

<interfaces>
<!-- The tier-contract + workflow conventions. -->

From workflows/*.md (an already-filled body, e.g. workflows/research.md or workflows/compile.md):
  the <capability_check> block format (ARCH-03) — Tier-1 (Task/MCP/AskUserQuestion available) vs Tier-2 (CLI degrade)
  W4 closed vocabulary enforced by tests/workflows-keyequal.test.ts + scripts/validate-plugin-manifest.cjs

From tests/tier-contract.test.ts:
  import { UX02_VERBS } from '../bin/lib/verbs.js'   // line 28
  the existing promoted-verb parity case structure (compile/done/write-section/verify-section)
  the no-17th-verb + no-colon-prefix + no--section-alias assertions (~line 1262)
  ±20% length-equivalence helper for artifact prose (TIER-07)

From bin/lib/verbs.ts: UX02_VERBS is length-16; list/open/sketch/add are ALREADY members (verbs 13-16)
From tests/cli-verbs.test.ts: TIER-04 dispatcher-registers-exactly-16 assertion (the standing bijection guard)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill workflows/list.md, open.md, sketch.md, add.md with capability_check bodies</name>
  <read_first>
    - workflows/research.md + workflows/compile.md (filled bodies — capability_check format, Tier-1/Tier-2 branches, W4 vocabulary)
    - workflows/list.md, open.md, sketch.md, add.md (current stubs to replace)
    - tests/workflows-keyequal.test.ts (W4 closed-vocabulary gate the bodies must satisfy)
    - 08-PATTERNS.md "workflows/*.md" section + No-Analog note on the Socratic sketch body
    - 08-RESEARCH.md Pattern 6 (add flow) + ERGO-05 (sketch state machine)
  </read_first>
  <action>
    Replace the four stub workflow bodies with real content, each containing a <capability_check> block per ARCH-03. workflows/list.md: Tier-1 reads paper://library (or runs the list verb); Tier-2 runs `pensmith list` — group by class, show status. workflows/open.md: Tier-1/Tier-2 switch the active paper by name, write the active pointer. workflows/sketch.md: the Socratic thinking-partner body — 4-5 questions, synthesize thesis, confirm gate, NO state advance until confirm (ERGO-05 / Pitfall 6 stated in the body); Tier-1 uses AskUserQuestion, Tier-2 degrades to @clack/prompts via the CLI. workflows/add.md: detect DOI/PDF/URL, hydrate via Crossref/pdf-text, write CITATIONS.bib, then the remap gate touching only assigned_sources[] (ERGO-06). Keep each body within the W4 closed vocabulary (workflows-keyequal gate). Do NOT add a new workflow file — the 16-workflow bijection must stay intact.
  </action>
  <verify>
    <automated>node --import tsx --test tests/workflows-keyequal.test.ts && node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" --version >/dev/null 2>&1; node scripts/validate-plugin-manifest.cjs</automated>
  </verify>
  <done>All four workflow bodies are filled with <capability_check> blocks; workflows-keyequal (W4) passes; the manifest validator passes (16-workflow bijection intact, no new workflow file).</done>
</task>

<task type="auto">
  <name>Task 2: tier-contract parity for list/open/sketch/add + 16-verb bijection re-assertion</name>
  <read_first>
    - tests/tier-contract.test.ts (FULL structure — existing promoted-verb cases, the ±20% length helper, the no-17th-verb / no-colon-prefix / no--section-alias block ~line 1262)
    - tests/cli-verbs.test.ts (TIER-04 standing 16-verb guard)
    - bin/lib/verbs.ts (UX02_VERBS — list/open/sketch/add already members)
    - 08-RESEARCH.md "Adding a 17th verb" anti-pattern (three independent guards) + STATE.md decision [07-04] (plumbing namespace is a Tier-1 alias onto the locked 16)
  </read_first>
  <action>
    Extend tests/tier-contract.test.ts with parity cases for the four promoted verbs. For the deterministic/offline-checkable surface (list + open are pure-local; sketch + add have interactive/network parts), assert each verb is dispatchable in BOTH tiers and produces equivalent structured output modulo prose — use the existing ±20% length-equivalence helper where an artifact is produced (list output table, open confirmation), and a presence/shape assertion for the interactive verbs (CONTRIBUTING.md D-24 obligation: every workflow body added gets a contract-test entry; document any CLI-only asymmetry the way compile/done did). Re-assert the bijection guard: UX02_VERBS length 16, list/open/sketch/add present, and NO colon-prefix or -section alias leaked into UX02_VERBS (the [07-04] three-guard pattern — keep the plumbing namespace a Tier-1-only alias). Ensure tests/cli-verbs.test.ts TIER-04 still asserts exactly 16 dispatchable verbs.
  </action>
  <verify>
    <automated>npm run test:tier-contract && node --import tsx --test tests/cli-verbs.test.ts</automated>
  </verify>
  <done>tier-contract parity covers list/open/sketch/add (both tiers, modulo prose / ±20% length); UX02_VERBS stays length-16 with no alias leak; cli-verbs TIER-04 asserts exactly 16 verbs. Any CLI-only asymmetry is documented like the compile/done precedent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow bodies → both tiers | the same body must drive Tier-1 plugin and Tier-2 CLI |
| verb registry → UX surface | a leaked alias would break the locked 16-verb contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-01 | Tampering | 17th verb / alias leak | mitigate | UX02_VERBS stays length-16; three independent guards (cli-verbs TIER-04, tier-contract no-alias, validate-plugin-manifest.cjs) re-asserted; plumbing namespace stays a Tier-1-only alias ([07-04]) |
| T-08-06-02 | Tampering | tier drift between plugin + CLI | mitigate | tier-contract parity cases (±20% length / shape) for all four promoted verbs; W4 closed-vocabulary gate on workflow bodies |
| T-08-06-03 | Repudiation | workflow body added without a contract entry | mitigate | CONTRIBUTING.md D-24 rule enforced by adding the four parity cases in this plan; documented CLI-only asymmetry for interactive parts (compile/done precedent) |
| T-08-06-SC | Tampering | npm/pip/cargo installs | accept | No installs (08-RESEARCH audit) |
</threat_model>

<verification>
- `npm run test:tier-contract` GREEN; `node --import tsx --test tests/cli-verbs.test.ts tests/workflows-keyequal.test.ts` GREEN.
- `node scripts/validate-plugin-manifest.cjs` passes.
- Full suite: `npm run check` GREEN (lint + typecheck + build + tier-contract + tests + manifests).
</verification>

<success_criteria>
- Four workflow bodies filled with capability_check (ARCH-03), W4 vocabulary satisfied.
- tier-contract parity for list/open/sketch/add across both tiers (modulo prose).
- 16-verb / 16-workflow bijection intact (three guards green).
</success_criteria>

<output>
Create `.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-06-SUMMARY.md` when done
</output>
</content>
