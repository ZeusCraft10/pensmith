---
phase: 08-style-match-sketch-add-library-byo-pdf-polish
plan: 01
type: execute
wave: 1
depends_on: ["08-00"]
files_modified:
  - bin/lib/schemas/global-library.ts
  - bin/lib/global-library.ts
  - bin/lib/paths.ts
  - bin/cli/list.ts
  - bin/cli/open.ts
  - bin/pensmith.ts
autonomous: true
requirements: [LIB-01, LIB-02, LIB-03, LIB-05]
must_haves:
  truths:
    - "`pensmith list` prints all papers grouped by class, with status (sectioning shows X/Y)"
    - "`pensmith open <name>` writes the active-paper pointer and reports the switch"
    - "The global paper registry lives at pensmithDataDir()/library/index.json — separate from per-paper .paper/LIBRARY.json — and is concurrency-safe (withLock + atomicWriteFile)"
    - "Paper status cycles intake|research|outline|sectioning(X/Y)|compile|done|archived via UPSERT"
  artifacts:
    - path: "bin/lib/schemas/global-library.ts"
      provides: "GlobalLibrary + GlobalLibraryEntry zod schemas (separate from library.ts D-59 schema)"
      contains: "GlobalLibraryEntrySchema"
    - path: "bin/lib/global-library.ts"
      provides: "init/load/register(UPSERT) under the library.ts triple chokepoint"
      exports: ["initGlobalLibrary", "loadGlobalLibrary", "registerPaperInGlobalLibrary"]
    - path: "bin/cli/list.ts"
      provides: "list verb — group by class, stdout-only"
      exports: ["listCommand"]
    - path: "bin/cli/open.ts"
      provides: "open verb — write active pointer, stdout-only"
      exports: ["openCommand"]
  key_links:
    - from: "bin/cli/list.ts"
      to: "bin/lib/global-library.ts"
      via: "loadGlobalLibrary()"
      pattern: "loadGlobalLibrary"
    - from: "bin/cli/open.ts"
      to: "pensmithDataDir()/active.json"
      via: "atomicWriteFile(pensmithActivePointerPath())"
      pattern: "pensmithActivePointerPath"
    - from: "bin/lib/global-library.ts"
      to: "pensmithDataDir()/library/index.json"
      via: "withLock + atomicWriteFile"
      pattern: "pensmithGlobalLibraryIndexPath"
---

<objective>
Ship the global paper registry (LIB-01) and the `list` (LIB-02) + `open` (LIB-03) verbs, with the LIB-05 status lifecycle stored per entry. The registry is a NEW file at pensmithDataDir()/library/index.json with its OWN zod schema — strictly separate from the per-paper .paper/LIBRARY.json (D-59 source/citation store). Promote list/open from Phase-2 stubs via REAL_VERB_LOADERS (no 17th verb).

Purpose: Enable multi-paper management — see every paper grouped by class, switch the active paper without `cd`. This is the SC-1 backbone.
Output: 2 new schema/lib files, 3 new paths.ts exports, 2 promoted CLI verbs, 2 REAL_VERB_LOADERS entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-RESEARCH.md
@.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-PATTERNS.md

<interfaces>
<!-- Templates the executor copies. global-library.ts mirrors library.ts EXACTLY in shape, different scope. -->

From bin/lib/library.ts (the D-59 per-paper store — the structural analog, DO NOT modify it):
  triple chokepoint = withLock(file, fn) + loadAndMigrate({file,schema,schemaName,currentVersion,writeBack}) with the
  fs.access existence-check INSIDE the lock (BLOCKER-01) + atomicWriteFile(file, JSON.stringify(x,null,2)+'\n')
  lazy singleton logger: openSessionLog({scope:'auto'}).child({module:'global-library'})

From bin/lib/schemas/library.ts (schema shape analog):
  export const CURRENT_<X>_VERSION = 1
  export const <X>Schema = z.object({ $schemaVersion: z.literal(CURRENT_<X>_VERSION), entries: z.array(...).default([]) })

From bin/lib/paths.ts (insertion point = after pensmithHttpCacheDir() line 133, before projectRoot() line 140):
  pensmithDataDir(platform=process.platform, env=process.env): string

From bin/cli/status.ts (thin-orchestrator template for list/open — stdout-only, never console.*, return {ok,...}):
  defineCommand({ meta, args, async run({args}) { ... process.stdout.write(...); return {ok:true} } })

From bin/pensmith.ts lines 57-80 (REAL_VERB_LOADERS — the ONLY edit point; buildSubCommands auto-registers):
  list: () => import('./cli/list.js').then((m) => m.listCommand),
  open: () => import('./cli/open.js').then((m) => m.openCommand),
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GlobalLibrary schema + global-library.ts (init/load/register UPSERT) + paths.ts exports</name>
  <read_first>
    - bin/lib/library.ts (FULL — copy the triple-chokepoint shape: init with existence-check inside lock, loadGlobalLibrary auto-init on ENOENT, register inside single lock)
    - bin/lib/schemas/library.ts (schema shape + CURRENT_*_VERSION literal)
    - bin/lib/paths.ts lines 112-140 (pensmithDataDir + helper-function style; insert new exports between line 133 and 140)
    - bin/lib/migrations/loader.ts (loadAndMigrate signature)
    - 08-PATTERNS.md sections "bin/lib/global-library.ts", "bin/lib/schemas/global-library.ts", "bin/lib/paths.ts (modify)"
  </read_first>
  <behavior>
    - initGlobalLibrary creates pensmithDataDir()/library/index.json with {$schemaVersion:1, entries:[]} only if absent (existence-check INSIDE the lock); returns the seeded library
    - loadGlobalLibrary returns the parsed library; on ENOENT (or cause.code ENOENT) auto-inits rather than throwing
    - registerPaperInGlobalLibrary UPSERTS by entry.id: insert if new, merge+bump updatedAt if existing (NOT reject-on-duplicate, unlike library.ts addEntry)
    - status lifecycle round-trips intake→research→outline→sectioning(+sectioningProgress {done,total})→compile→done→archived
    - the registry is written via withLock + atomicWriteFile; the file is NEVER inside any .paper/
  </behavior>
  <action>
    Create bin/lib/schemas/global-library.ts with CURRENT_GLOBAL_LIBRARY_VERSION=1, GlobalLibraryEntrySchema (id: z.string().uuid(); name: min(1); folderPath: min(1); class: z.string().default('Unfiled'); status: z.enum of the 7 LIB-05 states; sectioningProgress: {done:int≥0, total:int≥1}.optional(); createdAt/updatedAt: z.string().datetime()), and GlobalLibrarySchema ({$schemaVersion: z.literal(CURRENT_GLOBAL_LIBRARY_VERSION), entries: array(entry).default([])}), exporting both types. Add three exports to bin/lib/paths.ts (between pensmithHttpCacheDir and projectRoot, with the platform/env injection-point signature): pensmithGlobalLibraryIndexPath → join(pensmithDataDir(...), 'library', 'index.json'); pensmithActivePointerPath → join(pensmithDataDir(...), 'active.json'); pensmithStyleFingerprintsPath → join(pensmithDataDir(...), 'style-fingerprints.json'). Create bin/lib/global-library.ts cloning the library.ts triple chokepoint: initGlobalLibrary (mkdir dirname recursive, withLock, fs.access existence-check INSIDE the lock, atomicWriteFile the seeded library); loadGlobalLibrary (withLock + loadAndMigrate with schemaName 'global-library', writeBack:true; catch ENOENT/cause.ENOENT → return initGlobalLibrary()); registerPaperInGlobalLibrary(entry) (validate via GlobalLibraryEntrySchema.parse, withLock, loadAndMigrate writeBack:false, UPSERT by id with updatedAt bump on update, atomicWriteFile, log the global-library.register event). Use the lazy-singleton SessionLogger pattern. Key difference from library.ts (document in a header comment): UPSERT not reject-on-duplicate, and auto-init not AlreadyExists-throw. This file consumes the same triple chokepoint per the critical-design-constraint that the global registry must NOT conflate the D-59 per-paper schema.
  </action>
  <verify>
    <automated>node --import tsx --test tests/global-library.test.ts && node --import tsx -e "import('./bin/lib/paths.ts').then(p=>{['pensmithGlobalLibraryIndexPath','pensmithActivePointerPath','pensmithStyleFingerprintsPath'].forEach(f=>{if(typeof p[f]!=='function')throw new Error('missing '+f)});console.log('OK')})"</automated>
  </verify>
  <done>tests/global-library.test.ts flips from skip to GREEN. The three paths exports exist. The registry uses withLock+atomicWriteFile with the existence-check inside the lock, UPSERTs by id, and lives in pensmithDataDir() (never .paper/).</done>
</task>

<task type="auto">
  <name>Task 2: list + open CLI verbs + REAL_VERB_LOADERS promotion</name>
  <read_first>
    - bin/cli/status.ts (FULL — thin-orchestrator, stdout-only, never-crash, return {ok,...})
    - bin/pensmith.ts lines 57-80 (REAL_VERB_LOADERS — exact dynamic-import pattern)
    - bin/lib/atomic-write.ts (atomicWriteFile — D-07 chokepoint for the active pointer write)
    - 08-PATTERNS.md sections "bin/cli/list.ts", "bin/cli/open.ts", "REAL_VERB_LOADERS promotion pattern"
  </read_first>
  <action>
    Create bin/cli/list.ts: listCommand (defineCommand) that loadGlobalLibrary(), groups entries into a Map keyed by entry.class||'Unfiled', and writes a grouped table to stdout where status renders `sectioning X/Y` when status==='sectioning' && sectioningProgress is present, else the bare status; empty/absent library prints a friendly "no papers yet" line and returns {ok:true, papers:[]}; never console.*, stdout-only. Create bin/cli/open.ts: openCommand with a required positional `name`; loadGlobalLibrary(), find entry by name; if not found print a "no paper named" hint and return {ok:false}; if folderPath does not exist on disk print a not-found message and return {ok:false}; otherwise mkdir dirname(pensmithActivePointerPath()) recursive and atomicWriteFile the pointer JSON {paperId, folderPath, openedAt} (D-07 chokepoint — NEVER raw fs.writeFile), print the switch confirmation, return {ok:true, folderPath}. Add both to REAL_VERB_LOADERS in bin/pensmith.ts as dynamic-import loaders (`list: () => import('./cli/list.js').then(m=>m.listCommand)` and the open equivalent) — the ONLY edit point; buildSubCommands auto-registers. Do NOT add to UX02_VERBS (list/open are already members — adding them would break the 16-verb bijection).
  </action>
  <verify>
    <automated>node --import tsx --test tests/global-library.test.ts && node --import tsx -e "import('./bin/pensmith.ts').then(m=>{const v=m.REAL_VERB_LOADERS;if(!v.list||!v.open)throw new Error('list/open not promoted');console.log('OK')})"</automated>
  </verify>
  <done>list prints papers grouped by class with X/Y sectioning; open writes the active pointer via atomicWriteFile and reports the switch; both are in REAL_VERB_LOADERS; UX02_VERBS still length 16.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concurrent verbs → global index | open + add + intake may write index.json concurrently |
| user `<name>` arg → open | untrusted paper-name lookup string |
| stored folderPath → open switch | absolute path read back and reported |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Denial of Service | global-library index.json concurrent write | mitigate | withLock (proper-lockfile) around every read-mutate-write, same chokepoint as library.ts; existence-check INSIDE the lock |
| T-08-01-02 | Tampering | conflating global registry with D-59 per-paper LIBRARY.json | mitigate | Separate file (pensmithDataDir()/library/index.json) + separate schema (GlobalLibrarySchema); library.ts untouched |
| T-08-01-03 | Tampering | active.json pointer write | mitigate | atomicWriteFile (D-07 chokepoint), never raw fs.writeFile; pointer lives in pensmithDataDir(), never inside a sync-folder-risk .paper/ |
| T-08-01-04 | Information Disclosure | open on a missing/relocated folder | mitigate | fs.existsSync guard before switching; never crash, return {ok:false} with a clear message (status.ts never-crash precedent) |
| T-08-01-SC | Tampering | npm/pip/cargo installs | accept | No installs in this plan — all deps (proper-lockfile, zod) already present (08-RESEARCH audit) |
</threat_model>

<verification>
- `node --import tsx --test tests/global-library.test.ts` GREEN.
- REAL_VERB_LOADERS contains list + open; UX02_VERBS unchanged (length 16).
- `npm run check` (lint + typecheck) passes — no raw fs.writeFile, no os.homedir/env reads outside paths.ts.
</verification>

<success_criteria>
- Global registry at pensmithDataDir()/library/index.json with its own schema, UPSERT-by-id, withLock+atomicWriteFile.
- list groups by class with status (X/Y sectioning); open switches active paper via the active pointer.
- LIB-05 7-state lifecycle representable and round-tripped in tests.
- No 17th verb (16-bijection intact).
</success_criteria>

<output>
Create `.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-01-SUMMARY.md` when done
</output>
