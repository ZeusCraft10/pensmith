---
phase: 08-style-match-sketch-add-library-byo-pdf-polish
plan: 02
type: execute
wave: 1
depends_on: ["08-00"]
files_modified:
  - bin/lib/schemas/style.ts
  - bin/lib/style-match.ts
autonomous: true
requirements: [STYL-01, STYL-02]
must_haves:
  truths:
    - "buildStyleProfile produces a deterministic pure-stats profile (no LLM at profile-build time) from .md/.txt/.docx samples"
    - "The style profile is written ONLY to .paper/STYLE.json — never to a global cache (per-paper-only is load-bearing)"
    - "The fingerprint registry at pensmithDataDir()/style-fingerprints.json stores hashes + paper IDs only — NO prose features"
    - "Cross-paper reuse is detected (same sample fingerprint, different paper) and the prior-papers list is returned to the caller for an unconditional notice"
  artifacts:
    - path: "bin/lib/schemas/style.ts"
      provides: "StyleProfileSchema (pure-stats features + fingerprint)"
      contains: "StyleProfileSchema"
    - path: "bin/lib/style-match.ts"
      provides: "buildStyleProfile, writeStyleProfile, checkAndRegisterFingerprint, styleMatchToVoiceHint"
      exports: ["buildStyleProfile", "writeStyleProfile", "checkAndRegisterFingerprint", "styleMatchToVoiceHint"]
  key_links:
    - from: "bin/lib/style-match.ts writeStyleProfile"
      to: ".paper/STYLE.json"
      via: "atomicWriteFile(join(paperDir, 'STYLE.json'))"
      pattern: "STYLE\\.json"
    - from: "bin/lib/style-match.ts checkAndRegisterFingerprint"
      to: "pensmithDataDir()/style-fingerprints.json"
      via: "withLock + atomicWriteFile"
      pattern: "pensmithStyleFingerprintsPath"
---

<objective>
Ship the pure-stats style-match pipeline (STYL-01, STYL-02): build a deterministic per-paper prose profile from a folder of writing samples, write it ONLY to .paper/STYLE.json, and detect cross-paper sample reuse via a fingerprint registry that stores hashes + paper IDs but NEVER prose features. No LLM is called at profile-build time — the rendered voiceHint is the signal the drafter consumes (wired in 08-05).

Purpose: Match the user's established voice while honoring two load-bearing non-negotiables — per-paper-only storage (no global prose cache) and unconditional cross-paper-reuse transparency.
Output: StyleProfileSchema + style-match.ts with 4 exports (build/write/register/render).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-RESEARCH.md
@.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-PATTERNS.md

<interfaces>
<!-- 08-00 RED tests assert against these. paths exports land in 08-01 (parallel wave); style-match imports pensmithStyleFingerprintsPath from paths.js. -->

From bin/lib/paths.ts (pensmithStyleFingerprintsPath added by 08-01, same wave — both written this wave; ordering note in threat model):
  pensmithStyleFingerprintsPath(platform?, env?): string  // pensmithDataDir()/style-fingerprints.json
  paperDir(root?): string                                  // <root>/.paper

From bin/lib/atomic-write.ts: atomicWriteFile(path, contents): Promise<void>   // D-07 chokepoint
From bin/lib/lock.ts: withLock(path, fn): Promise<T>                            // proper-lockfile chokepoint
From node:crypto: createHash('sha256').update(buf).digest('hex')

StyleProfile feature set (08-RESEARCH Pattern 3 / 08-PATTERNS schema block):
  $schemaVersion:1, samplesDir, sampleSetFingerprint(64-hex), samplesAnalyzed,
  features: { medianSentenceLengthWords, p25/p75SentenceLengthWords, typeTokenRatio[0,1],
    passiveVoiceRate[0,1], subordinatingClauseRate[0,1], openingWordTopN{}, closingWordTopN{},
    avgParagraphLengthSentences }, generatedAt
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StyleProfileSchema (bin/lib/schemas/style.ts)</name>
  <read_first>
    - bin/lib/schemas/library.ts (schema-file structure + CURRENT_*_VERSION literal pattern)
    - 08-PATTERNS.md section "bin/lib/schemas/style.ts" (exact field list)
    - 08-RESEARCH.md Pattern 3 (feature semantics + bounds)
  </read_first>
  <behavior>
    - StyleProfileSchema.parse rejects a fingerprint that is not exactly 64 hex chars (z.string().length(64))
    - typeTokenRatio, passiveVoiceRate, subordinatingClauseRate are bounded [0,1]
    - samplesAnalyzed is a positive integer
    - openingWordTopN/closingWordTopN are z.record(z.string(), z.number())
  </behavior>
  <action>
    Create bin/lib/schemas/style.ts with CURRENT_STYLE_VERSION=1 and StyleProfileSchema per the 08-PATTERNS field list: $schemaVersion z.literal(CURRENT_STYLE_VERSION); samplesDir min(1); sampleSetFingerprint z.string().length(64); samplesAnalyzed int().min(1); features object {medianSentenceLengthWords, p25SentenceLengthWords, p75SentenceLengthWords: z.number(); typeTokenRatio/passiveVoiceRate/subordinatingClauseRate: z.number().min(0).max(1); openingWordTopN/closingWordTopN: z.record(z.string(), z.number()); avgParagraphLengthSentences: z.number()}; generatedAt z.string().datetime(). Export StyleProfile type.
  </action>
  <verify>
    <automated>node --import tsx -e "import('./bin/lib/schemas/style.ts').then(m=>{m.StyleProfileSchema.parse({$schemaVersion:1,samplesDir:'x',sampleSetFingerprint:'a'.repeat(64),samplesAnalyzed:1,features:{medianSentenceLengthWords:10,p25SentenceLengthWords:5,p75SentenceLengthWords:15,typeTokenRatio:0.5,passiveVoiceRate:0.1,subordinatingClauseRate:0.1,openingWordTopN:{The:3},closingWordTopN:{end:2},avgParagraphLengthSentences:4},generatedAt:new Date().toISOString()});let threw=false;try{m.StyleProfileSchema.parse({$schemaVersion:1,samplesDir:'x',sampleSetFingerprint:'short',samplesAnalyzed:1,features:{},generatedAt:''})}catch{threw=true};if(!threw)throw new Error('schema did not reject bad input');console.log('OK')})"</automated>
  </verify>
  <done>StyleProfileSchema parses a valid profile and rejects a non-64-char fingerprint; CURRENT_STYLE_VERSION + StyleProfile type exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: style-match.ts — build (pure stats), write (per-paper only), fingerprint registry, voiceHint render</name>
  <read_first>
    - bin/lib/pdf-text.ts (pure-function chokepoint shape — single public API, deterministic)
    - bin/lib/library.ts (withLock + loadAndMigrate + atomicWriteFile for the registry side)
    - bin/lib/atomic-write.ts + bin/lib/lock.ts (the write chokepoints)
    - 08-PATTERNS.md section "bin/lib/style-match.ts" (buildStyleProfile, writeStyleProfile, checkAndRegisterFingerprint, styleMatchToVoiceHint)
    - 08-RESEARCH.md "Code Examples → Style Profile Build Pattern" + Anti-Patterns (no LLM at build, no global STYLE cache, no suppressing reuse notice)
  </read_first>
  <behavior>
    - buildStyleProfile reads .md/.txt/.docx files (sorted, deterministic), computes a content-hash fingerprint (sha256 of sorted per-file content hashes), and pure-stats features; same samples → identical fingerprint + features (deterministic, no LLM)
    - buildStyleProfile throws a clear error when the samples dir has zero matching files
    - writeStyleProfile writes ONLY to join(path.resolve(paperDir), 'STYLE.json') via atomicWriteFile — it must NOT import pensmithDataDir for its write target (Pitfall 1 / Warning sign)
    - checkAndRegisterFingerprint returns priorPapers=[] on first registration of a fingerprint, then a non-empty priorPapers (the earlier {paperId,paperName,addedAt}) when the SAME fingerprint is registered for a DIFFERENT paperId; the registry JSON contains hashes + paper IDs ONLY — no "features" key
    - styleMatchToVoiceHint renders the stats into a natural-language hint string (pure, no I/O)
  </behavior>
  <action>
    Create bin/lib/style-match.ts. buildStyleProfile(samplesDir): path.resolve first (path-traversal mitigation), readdir filtered to /\.(md|txt|docx)$/i and sorted; throw if empty; for .docx use the already-installed jszip to read word/document.xml and strip XML tags (mirror exporter.ts usage), .md/.txt read utf8; content fingerprint = sha256(sorted([sha256(fileBytes)...]).join('')); sentence segmentation via /(?<=[.!?])\s+(?=[A-Z])/ on combined text; features = median/p25/p75 of sentence word-lengths, typeTokenRatio = uniqueWords/totalWords, passiveVoiceRate via /\b(was|were|is|are|been|be)\s+\w+ed\b/gi count / sentences, subordinatingClauseRate via a small subordinator word list (because|although|while|since|whereas|if|when), openingWordTopN/closingWordTopN top-10 frequency tables, avgParagraphLengthSentences; return StyleProfileSchema.parse(...). writeStyleProfile(paperDir, profile): atomicWriteFile(join(path.resolve(paperDir),'STYLE.json'), JSON.stringify(profile,null,2)+'\n') — MUST NOT write under pensmithDataDir (the per-paper-only non-negotiable). checkAndRegisterFingerprint(fingerprint,paperId,paperName,folderPath): mkdir dirname(pensmithStyleFingerprintsPath()) recursive; withLock the registry; load existing (tolerant of ENOENT → empty), find prior entries for this fingerprint EXCLUDING the current paperId → priorPapers; append {paperId,paperName,folderPath,addedAt} to the fingerprint's entry list (never overwrite); atomicWriteFile; return {priorPapers}. The registry record stores ONLY fingerprint→[{paperId,paperName,folderPath,addedAt}] — NO prose features (Pitfall 1). styleMatchToVoiceHint(profile): pure render "Match this style: median sentence ~N words, vocabulary density X, ...". Header comment must state: NO LLM at profile-build (08-RESEARCH Anti-Pattern), per-paper STYLE.json only, registry stores no features, reuse notice is the CALLER's responsibility and is UNCONDITIONAL (not --yolo-gated).
  </action>
  <verify>
    <automated>node --import tsx --test tests/style-match.test.ts</automated>
  </verify>
  <done>tests/style-match.test.ts flips skip→GREEN: deterministic profile, per-paper STYLE.json write, no-features-in-registry, and cross-paper reuse detection (priorPapers non-empty on second paperId with same fingerprint) all pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user samplesDir → buildStyleProfile | untrusted directory path + arbitrary file contents |
| prose features → storage | features must stay per-paper; only the hash leaves the paper |
| concurrent intake → fingerprint registry | multiple papers may register fingerprints concurrently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Information Disclosure | global STYLE cache leak | mitigate | writeStyleProfile writes ONLY join(paperDir,'STYLE.json'); the module does NOT import pensmithDataDir for its profile write target (Pitfall 1); registry stores hashes + paper IDs only, asserted by the 08-00 RED test's "no features key" check |
| T-08-02-02 | Repudiation | undisclosed cross-paper reuse | mitigate | checkAndRegisterFingerprint always returns priorPapers; the consuming verb (08-05) prints the notice unconditionally (NOT --yolo-gated) per STYL-02 / Pitfall 2 |
| T-08-02-03 | Tampering | path traversal via samplesDir | mitigate | path.resolve(samplesDir) before any read; only .md/.txt/.docx files processed (V5/V12 ASVS) |
| T-08-02-04 | Denial of Service | fingerprint registry concurrent write | mitigate | withLock around registry read-mutate-write; atomicWriteFile |
| T-08-02-05 | Tampering | non-deterministic / costly profile build | mitigate | Pure-stats only, NO LLM at build time (08-RESEARCH Anti-Pattern); deterministic content-hash fingerprint asserted stable in tests |
| T-08-02-SC | Tampering | npm/pip/cargo installs | accept | No installs — jszip + zod already present (08-RESEARCH audit) |
</threat_model>

<verification>
- `node --import tsx --test tests/style-match.test.ts` GREEN.
- Manual grep: bin/lib/style-match.ts contains no pensmithDataDir reference in the writeStyleProfile path (profile is per-paper only).
- `npm run check` passes (no raw fs.writeFile; atomicWriteFile + withLock used).
</verification>

<success_criteria>
- Deterministic pure-stats StyleProfile from a samples folder; no LLM at build time.
- STYLE.json written per-paper only; registry stores hashes + paper IDs, never features.
- Cross-paper reuse detected and surfaced via priorPapers return value.
</success_criteria>

<output>
Create `.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-02-SUMMARY.md` when done
</output>
