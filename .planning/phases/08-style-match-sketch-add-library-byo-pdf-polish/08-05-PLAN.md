---
phase: 08-style-match-sketch-add-library-byo-pdf-polish
plan: 05
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - bin/lib/drafter-input.ts
  - bin/cli/write.ts
  - bin/cli/intake.ts
  - README.md
  - tests/repo-files.test.ts
autonomous: true
requirements: [STYL-03, STYL-04, LIB-04]
must_haves:
  truths:
    - "DrafterInputSchema accepts an additive optional styleProfilePath (no existing field changed; .strict() chokepoint preserved)"
    - "The write verb resolves voiceHint by priority PLAN.md voice_hint > style-match render > default — section override always wins"
    - "Intake registers the paper in the global library with a class (free-form, default 'Unfiled') — LIB-04"
    - "The style-match opt-in producer (intake --style-samples) BUILDS .paper/STYLE.json via buildStyleProfile→writeStyleProfile and surfaces cross-paper reuse UNCONDITIONALLY via checkAndRegisterFingerprint — the live producer behind the write.ts consumer"
    - "README ships a Style Match dual-use disclosure with honest framing (forbidden substrings 'impersonate' / 'evade detection' / 'undetectable' never appear)"
  artifacts:
    - path: "bin/lib/drafter-input.ts"
      provides: "additive styleProfilePath?: z.string().optional()"
      contains: "styleProfilePath"
    - path: "bin/cli/write.ts"
      provides: "voiceHint priority resolution (PLAN override > style-match > default)"
      contains: "voice_hint"
    - path: "bin/cli/intake.ts"
      provides: "global-library registration (LIB-04) + style-match opt-in producer (--style-samples)"
      contains: "buildStyleProfile"
    - path: "README.md"
      provides: "## Style Match dual-use disclosure (STYL-04)"
      contains: "Style Match"
  key_links:
    - from: "bin/cli/write.ts"
      to: "bin/lib/style-match.ts styleMatchToVoiceHint"
      via: "dynamic import when .paper/STYLE.json exists and PLAN voice_hint is empty"
      pattern: "styleMatchToVoiceHint"
    - from: "bin/cli/intake.ts (--style-samples opt-in)"
      to: "bin/lib/style-match.ts buildStyleProfile / checkAndRegisterFingerprint / writeStyleProfile"
      via: "opt-in producer run path — builds .paper/STYLE.json + surfaces priorPapers unconditionally"
      pattern: "buildStyleProfile"
    - from: "bin/cli/intake.ts"
      to: "bin/lib/global-library.ts registerPaperInGlobalLibrary"
      via: "non-fatal side effect at end of intake"
      pattern: "registerPaperInGlobalLibrary"
    - from: "tests/repo-files.test.ts"
      to: "README.md ## Style Match"
      via: "content guard"
      pattern: "Style Match"
---

<objective>
Wire style-match end-to-end (STYL-03 + STYL-04), register papers in the global library at intake with a class (LIB-04). This plan delivers BOTH ends of the style-match loop: (1) the PRODUCER — an opt-in `intake --style-samples` path that BUILDS `.paper/STYLE.json` from the user's writing samples and SURFACES cross-paper reuse unconditionally; and (2) the CONSUMER — the drafter integration that adds an optional styleProfilePath to the strict DrafterInputSchema and resolves voiceHint by a strict priority where a section's PLAN.md voice_hint always overrides the style-match render. Intake global-library registration is a non-fatal side effect. The README section uses honest framing only.

Purpose: Close the style-match loop FULLY — opt-in profile build (producer) → STYLE.json on disk → drafter consumes it (consumer) — plus the library lifecycle entry point (intake → global registry) and the sensitive-feature transparency disclosure. Covers SC-2 (STYLE.json is produced by a real verb path AND consumed by the drafter; cross-paper reuse surfaced; README disclosure) and SC-1 (class assignment at intake).
Output: opt-in style-match producer in intake, additive schema field, voiceHint resolution in write.ts, intake registration, README section + content guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-RESEARCH.md
@.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-PATTERNS.md
@.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-01-SUMMARY.md
@.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-02-SUMMARY.md

<interfaces>
<!-- The exact insertion points + chokepoints. -->

From bin/lib/drafter-input.ts (DrafterInputSchema is .strict() — line 71; add the new optional field at line 70 area, after `authors`):
  DrafterInputSchema = z.object({ planPath, sources, wordTarget, voiceHint, ...optional..., authors? }).strict()
  // ADD: styleProfilePath: z.string().optional()  — additive only, no existing field touched

From bin/cli/write.ts writeOneSection() (line 46; the assertDrafterInput call is at lines 51-56 with the hardcoded
  voiceHint: 'Formal academic tone (Tier-2 placeholder).'):
  // resolve voiceHint BEFORE assertDrafterInput; pass styleProfilePath when present

From bin/lib/frontmatter.ts: parseFrontmatter(text) → { frontmatter, body }   // read frontmatter.voice_hint
From bin/lib/style-match.ts (08-02):
  buildStyleProfile(samplesDir): Promise<StyleProfile>          // pure stats
  writeStyleProfile(paperDir, profile): Promise<void>          // writes paperDir/STYLE.json ONLY
  checkAndRegisterFingerprint(fingerprint, paperId, paperName): Promise<{priorPapers:[{paperId,paperName,addedAt}]}>
  styleMatchToVoiceHint(profile): string ; StyleProfileSchema (parse the loaded JSON)
From bin/lib/global-library.ts (08-01): registerPaperInGlobalLibrary(entry): Promise<GlobalLibrary>   // UPSERT
From bin/lib/state.ts (08-01 / Phase 1): loadState(paperRoot) → { paperId, ... } (paperId is the uuid for registration + fingerprint identity)

From bin/cli/intake.ts run() (current Tier-2 stub at lines 55-88; both noLlm and Tier-1 branches return {ok:true,...} at lines 69/84):
  args = { from, yolo }   // existing citty args — ADD `styleSamples` (string) as the opt-in flag; NO new verb
  // class read from config.toml [project] (default 'Unfiled') if present, else 'Unfiled'; paperId from STATE.json
  // register + (opt-in) build profile at the END of a successful run as non-fatal side effects

From references/honesty-framing.md (the locked-copy honest-framing precedent for the README disclosure tone)
From tests/repo-files.test.ts (CF-D24 content-guard style ~line 326) — add the README assertion
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DrafterInputSchema additive styleProfilePath + write.ts voiceHint priority resolution (CONSUMER)</name>
  <read_first>
    - bin/lib/drafter-input.ts (FULL — the .strict() chokepoint at line 71; additive optional fields convention at lines 53-70)
    - bin/cli/write.ts lines 46-61 (writeOneSection + the hardcoded voiceHint at line 55)
    - bin/lib/frontmatter.ts lines 29-63 (parseFrontmatter)
    - 08-PATTERNS.md section "bin/lib/drafter-input.ts (modify)" + "Write verb integration pattern"
    - 08-RESEARCH.md Pattern 3 (STYL-03 decision) + Pitfall 7 (per-section voice-hint override must win)
  </read_first>
  <action>
    Add to bin/lib/drafter-input.ts a single additive optional field inside the .strict() object, after `authors` (line 70): `styleProfilePath: z.string().optional()` with a comment noting STYL-03 — voiceHint is the load-bearing signal; styleProfilePath lets a capable Tier-1 drafter fetch the raw JSON. Do NOT modify any existing field; .strict() stays. In bin/cli/write.ts writeOneSection(), BEFORE the assertDrafterInput call, resolve voiceHint by strict priority (Pitfall 7): default 'Formal academic tone.'; read the section PLAN.md via parseFrontmatter and IF frontmatter.voice_hint is a non-empty string → voiceHint = that (section override WINS, do not consult style-match); ELSE if join(paperRoot, '.paper', 'STYLE.json') exists → dynamic-import style-match, parse the STYLE.json via StyleProfileSchema, voiceHint = styleMatchToVoiceHint(profile), and set styleProfilePath = the STYLE.json path. Pass the resolved voiceHint and (when set) styleProfilePath into assertDrafterInput. Keep the Tier-2 placeholder write path otherwise intact — this changes ONLY the voiceHint/styleProfilePath assembly. NOTE: the existsSync(STYLE.json) branch is now reachable in practice because Task 2's opt-in producer writes that file.
  </action>
  <verify>
    <automated>node --import tsx --test tests/write-style-integration.test.ts tests/drafter-input.test.ts</automated>
  </verify>
  <done>tests/write-style-integration.test.ts flips skip→GREEN: DrafterInputSchema accepts styleProfilePath (and still rejects unknown fields via .strict()); write.ts picks PLAN voice_hint over style-match, style-match over default; drafter-input.test.ts still green.</done>
</task>

<task type="auto">
  <name>Task 2: Intake global-library registration (LIB-04) + style-match opt-in PRODUCER (--style-samples → STYLE.json + unconditional reuse notice)</name>
  <read_first>
    - bin/cli/intake.ts FULL (current Tier-2 stub; the citty `args` block + both return points at lines 69/84 where the non-fatal side effects attach)
    - bin/lib/global-library.ts (08-01 — registerPaperInGlobalLibrary signature + GlobalLibraryEntry shape)
    - bin/lib/style-match.ts (08-02 — buildStyleProfile / checkAndRegisterFingerprint / writeStyleProfile signatures; reuse notice is the CALLER's job and UNCONDITIONAL)
    - bin/lib/state.ts (loadState → paperId; how paperId is obtained — read, do not duplicate)
    - bin/lib/paths.ts paperDir() (STYLE.json write target root)
    - 08-RESEARCH.md LIB-04 + Open-Q1/Q4 + STYL-02 (unconditional reuse notice) + Anti-Pattern (no --yolo-gating of the reuse notice)
  </read_first>
  <action>
    Part A — Global-library registration (LIB-04): at the END of intakeCommand.run(), in BOTH the noLlm and Tier-1 branches before each `return {ok:true,...}`, register the paper as a NON-FATAL side effect: derive class from config.toml [project] (default 'Unfiled' when absent), name from the project/title or folder basename, folderPath = path.resolve(process.cwd()), id = the paperId from STATE.json (loadState). Call registerPaperInGlobalLibrary({id, name, folderPath, class, status:'intake', createdAt, updatedAt}) inside a try/catch that WARNs to stderr and never fails the verb (Open-Q4 — non-fatal side effect). If STATE.json/paperId is not yet available in the Tier-2 placeholder path, guard with a WARN rather than crashing.

    Part B — Style-match opt-in PRODUCER (this is the live caller the reviewers found missing). Add an OPT-IN to the existing `intake` verb — NO 17th verb. Add a new citty arg `styleSamples` (type: 'string', description: 'Opt-in: path to a folder of your writing samples to match your voice (.md/.txt/.docx).'); the Tier-1 workflow body may instead surface this as an interactive opt-in prompt, but the CLI flag is the deterministic Tier-2 path. When `args.styleSamples` is provided (opt-in is explicit — absent flag = no profiling), run the producer as a NON-FATAL side effect after a successful intake, BEFORE the return: (1) const profile = await buildStyleProfile(args.styleSamples); (2) const {priorPapers} = await checkAndRegisterFingerprint(profile.sampleSetFingerprint, paperId, name) — then SURFACE priorPapers UNCONDITIONALLY: if priorPapers.length > 0, process.stdout.write a transparency notice naming the prior paper(s) (resolve each prior paper's display name from priorPapers / the global library) — this notice is NOT --yolo-gated and NOT suppressible (STYL-02 / Anti-Pattern); (3) await writeStyleProfile(paperDir(), profile) so `.paper/STYLE.json` exists on disk for the write.ts consumer. Wrap the whole producer in try/catch that WARNs and never fails the verb (a bad samples dir must not break intake), EXCEPT the reuse-notice print itself must not be swallowed when a profile built successfully. State in a comment: this is the producer end of the loop; write.ts (Task 1) is the consumer; the reuse notice is unconditional.
  </action>
  <verify>
    <automated>node --import tsx --test tests/style-match.test.ts tests/intake-style-producer.test.ts</automated>
  </verify>
  <done>intake registers the paper in the global library with a class (non-fatal). `intake --style-samples <dir>` builds .paper/STYLE.json via buildStyleProfile→writeStyleProfile, calls checkAndRegisterFingerprint, and prints the cross-paper-reuse notice UNCONDITIONALLY when a prior paper shares the fingerprint. tests/intake-style-producer.test.ts (added in 08-00) is GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: README Style Match dual-use disclosure (STYL-04) + content guard</name>
  <read_first>
    - references/honesty-framing.md (honest-framing tone precedent — "improves prose, does not evade detection")
    - README.md (current contents — add a new section, do not disturb existing)
    - tests/repo-files.test.ts ~line 326 (CF-D24 content-guard pattern to copy)
    - 08-PATTERNS.md section "README.md (modify)" (incl. the suggested content-guard assertions) + 08-RESEARCH.md STYL-04
  </read_first>
  <action>
    Add a `## Style Match` section to README.md with HONEST DUAL-USE framing. CRITICAL — the copy MUST be self-consistent with its own content guard: the guard forbids the substrings `impersonate`, `evade detection`, and `undetectable`, so the copy MUST NOT contain ANY of those three substrings, even in a negation. Do NOT write "does not make text undetectable" or "is not for impersonation" — those literal substrings would trip the guard. Instead, deliver the same honest meaning with compliant wording, e.g.: describe Style Match as an OPT-IN feature that mirrors the user's own established writing voice; disclose the dual-use nature plainly; state the positive-framed limits — "it improves prose to read like your own past writing" and "it does not claim to make AI authorship invisible to detectors, and it is intended for matching your own voice, not for passing off someone else's work as theirs" (phrase the misuse boundary WITHOUT the forbidden tokens — use "passing off someone else's work" instead of "impersonate", "make AI authorship invisible to detectors" instead of "evade detection"/"undetectable"). Mirror the honesty-framing.md tone. Then add a content guard to tests/repo-files.test.ts (CF-D24 style): assert README matches /## Style Match/ and a "match your.*voice|established voice" phrase, AND assert it does NOT contain /impersonate/i, /evade detection/i, or /undetectable/i. The copy and the guard must AGREE — re-read the section after writing and confirm none of the three forbidden substrings appear.
  </action>
  <verify>
    <automated>node --import tsx --test tests/repo-files.test.ts && node --import tsx -e "import('node:fs').then(fs=>{const r=fs.readFileSync('README.md','utf8');if(!/## Style Match/.test(r))throw new Error('no Style Match section');if(/evade detection|undetectable|impersonate/i.test(r))throw new Error('forbidden substring present');console.log('OK')})"</automated>
  </verify>
  <done>repo-files.test.ts GREEN with the new README guard; README has a Style Match section with honest framing and NONE of the forbidden substrings (impersonate / evade detection / undetectable) — the copy and its own guard agree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| STYLE.json contents → drafter input | a profile file read into the strict drafter contract |
| PLAN.md voice_hint vs style-match | precedence conflict that could over-fit prose |
| user --style-samples dir → producer | untrusted samples path drives profile build + fingerprint registration |
| README framing → user disclosure | honesty non-negotiable on a sensitive feature |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Tampering | drafter-input contract widening | mitigate | Only ONE additive optional field (styleProfilePath); .strict() chokepoint preserved so unknown fields still throw (T-3-10 / WRTE-04 intact) |
| T-08-05-02 | Tampering | style-match over-fitting / ignoring section override | mitigate | Strict priority: PLAN.md voice_hint always wins over style-match (Pitfall 7); asserted by write-style-integration.test.ts |
| T-08-05-03 | Information Disclosure | dishonest detection-evasion framing | mitigate | README copy avoids the forbidden substrings (impersonate/evade detection/undetectable) entirely while disclosing dual use; content guard in repo-files.test.ts enforces honest framing (STYL-04 non-negotiable); copy and guard reconciled |
| T-08-05-04 | Denial of Service | intake crash on global-library / producer failure | mitigate | Registration AND the style-match producer wrapped in try/catch → WARN, never fail the verb (Open-Q4 non-fatal side effect); a bad --style-samples dir does not break intake |
| T-08-05-05 | Tampering | malformed STYLE.json reaching the drafter | mitigate | STYLE.json parsed via StyleProfileSchema before rendering voiceHint; parse failure falls back to default tone |
| T-08-05-06 | Repudiation | suppressed cross-paper reuse notice | mitigate | The opt-in producer surfaces priorPapers UNCONDITIONALLY (not --yolo-gated, not suppressible) when a profile builds and a prior paper shares the fingerprint (STYL-02) |
| T-08-05-SC | Tampering | npm/pip/cargo installs | accept | No installs — zod already present (08-RESEARCH audit) |
</threat_model>

<verification>
- `node --import tsx --test tests/write-style-integration.test.ts tests/drafter-input.test.ts tests/style-match.test.ts tests/intake-style-producer.test.ts tests/repo-files.test.ts` GREEN.
- End-to-end: `intake --style-samples <dir>` writes .paper/STYLE.json → write.ts existsSync branch is reachable → drafter receives style-match voiceHint + styleProfilePath.
- README has `## Style Match` with honest framing and none of the forbidden substrings.
- `npm run check` passes — .strict() preserved on DrafterInputSchema.
</verification>

<success_criteria>
- styleProfilePath added additively; voiceHint priority PLAN override > style-match > default.
- The opt-in intake producer BUILDS .paper/STYLE.json and surfaces cross-paper reuse unconditionally — the live caller of buildStyleProfile/checkAndRegisterFingerprint/writeStyleProfile.
- Intake registers the paper in the global library with class (LIB-04), non-fatally.
- README ships the dual-use disclosure; copy and content guard agree (no forbidden substrings).
</success_criteria>

<output>
Create `.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-05-SUMMARY.md` when done
</output>
</content>
