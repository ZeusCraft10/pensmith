---
phase: 08-style-match-sketch-add-library-byo-pdf-polish
plan: 05
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - bin/lib/drafter-input.ts
  - bin/cli/write.ts
  - bin/cli/intake.ts
  - README.md
  - tests/repo-files.test.ts
autonomous: true
requirements: [STYL-03, STYL-04, LIB-04]
must_haves:
  truths:
    - "DrafterInputSchema accepts an additive optional styleProfilePath (no existing field changed; .strict() chokepoint preserved)"
    - "The write verb resolves voiceHint by priority PLAN.md voice_hint > style-match render > default — section override always wins"
    - "Intake registers the paper in the global library with a class (free-form, default 'Unfiled') — LIB-04"
    - "README ships a Style Match dual-use disclosure with honest framing (no 'impersonate' / 'evade detection' / 'undetectable')"
  artifacts:
    - path: "bin/lib/drafter-input.ts"
      provides: "additive styleProfilePath?: z.string().optional()"
      contains: "styleProfilePath"
    - path: "bin/cli/write.ts"
      provides: "voiceHint priority resolution (PLAN override > style-match > default)"
      contains: "voice_hint"
    - path: "bin/cli/intake.ts"
      provides: "global-library registration with class (LIB-04)"
      contains: "registerPaperInGlobalLibrary"
    - path: "README.md"
      provides: "## Style Match dual-use disclosure (STYL-04)"
      contains: "Style Match"
  key_links:
    - from: "bin/cli/write.ts"
      to: "bin/lib/style-match.ts styleMatchToVoiceHint"
      via: "dynamic import when .paper/STYLE.json exists and PLAN voice_hint is empty"
      pattern: "styleMatchToVoiceHint"
    - from: "bin/cli/intake.ts"
      to: "bin/lib/global-library.ts registerPaperInGlobalLibrary"
      via: "non-fatal side effect at end of intake"
      pattern: "registerPaperInGlobalLibrary"
    - from: "tests/repo-files.test.ts"
      to: "README.md ## Style Match"
      via: "content guard"
      pattern: "Style Match"
---

<objective>
Wire style-match into the drafter (STYL-03), register papers in the global library at intake with a class (LIB-04), and ship the README dual-use disclosure (STYL-04). The drafter integration adds ONLY an optional styleProfilePath to the strict DrafterInputSchema and resolves voiceHint by a strict priority where a section's PLAN.md voice_hint always overrides the style-match render. Intake registration is a non-fatal side effect. The README section uses honest framing only.

Purpose: Close the style-match loop (profile → drafter), complete the library lifecycle entry point (intake → global registry), and ship the sensitive-feature transparency disclosure. Covers SC-2 (drafter consumes profile + README disclosure) and SC-1 (class assignment at intake).
Output: additive schema field, voiceHint resolution in write.ts, intake registration, README section + content guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-RESEARCH.md
@.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-PATTERNS.md
@.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-01-SUMMARY.md
@.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-02-SUMMARY.md

<interfaces>
<!-- The exact insertion points + chokepoints. -->

From bin/lib/drafter-input.ts (DrafterInputSchema is .strict() — line 71; add the new optional field at line 70 area, after `authors`):
  DrafterInputSchema = z.object({ planPath, sources, wordTarget, voiceHint, ...optional..., authors? }).strict()
  // ADD: styleProfilePath: z.string().optional()  — additive only, no existing field touched

From bin/cli/write.ts writeOneSection() (line 46; the assertDrafterInput call is at lines 51-56 with the hardcoded
  voiceHint: 'Formal academic tone (Tier-2 placeholder).'):
  // resolve voiceHint BEFORE assertDrafterInput; pass styleProfilePath when present

From bin/lib/frontmatter.ts: parseFrontmatter(text) → { frontmatter, body }   // read frontmatter.voice_hint
From bin/lib/style-match.ts (08-02): styleMatchToVoiceHint(profile): string ; StyleProfileSchema (parse the loaded JSON)
From bin/lib/global-library.ts (08-01): registerPaperInGlobalLibrary(entry): Promise<GlobalLibrary>   // UPSERT

From bin/cli/intake.ts run() (line 55; both noLlm and Tier-1 branches return {ok:true,...} at lines 69/84):
  // class read from config.toml [project] class (default 'Unfiled'); paperId from STATE.json
  // register at the END of a successful run as a non-fatal side effect (try/catch → WARN, never fail the verb)

From references/honesty-framing.md (the locked-copy honest-framing precedent for the README disclosure tone)
From tests/repo-files.test.ts (CF-D24 content-guard style ~line 326) — add the README assertion
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DrafterInputSchema additive styleProfilePath + write.ts voiceHint priority resolution</name>
  <read_first>
    - bin/lib/drafter-input.ts (FULL — the .strict() chokepoint at line 71; additive optional fields convention at lines 53-70)
    - bin/cli/write.ts lines 46-61 (writeOneSection + the hardcoded voiceHint at line 55)
    - bin/lib/frontmatter.ts lines 29-63 (parseFrontmatter)
    - 08-PATTERNS.md section "bin/lib/drafter-input.ts (modify)" + "Write verb integration pattern"
    - 08-RESEARCH.md Pattern 3 (STYL-03 decision) + Pitfall 7 (per-section voice-hint override must win)
  </read_first>
  <action>
    Add to bin/lib/drafter-input.ts a single additive optional field inside the .strict() object, after `authors` (line 70): `styleProfilePath: z.string().optional()` with a comment noting STYL-03 — voiceHint is the load-bearing signal; styleProfilePath lets a capable Tier-1 drafter fetch the raw JSON. Do NOT modify any existing field; .strict() stays. In bin/cli/write.ts writeOneSection(), BEFORE the assertDrafterInput call, resolve voiceHint by strict priority (Pitfall 7): default 'Formal academic tone.'; read the section PLAN.md via parseFrontmatter and IF frontmatter.voice_hint is a non-empty string → voiceHint = that (section override WINS, do not consult style-match); ELSE if join(paperRoot, '.paper', 'STYLE.json') exists → dynamic-import style-match, parse the STYLE.json via StyleProfileSchema, voiceHint = styleMatchToVoiceHint(profile), and set styleProfilePath = the STYLE.json path. Pass the resolved voiceHint and (when set) styleProfilePath into assertDrafterInput. Keep the Tier-2 placeholder write path otherwise intact — this changes ONLY the voiceHint/styleProfilePath assembly.
  </action>
  <verify>
    <automated>node --import tsx --test tests/write-style-integration.test.ts tests/drafter-input.test.ts</automated>
  </verify>
  <done>tests/write-style-integration.test.ts flips skip→GREEN: DrafterInputSchema accepts styleProfilePath (and still rejects unknown fields via .strict()); write.ts picks PLAN voice_hint over style-match, style-match over default; drafter-input.test.ts still green.</done>
</task>

<task type="auto">
  <name>Task 2: Intake global-library registration (LIB-04) + README Style Match dual-use disclosure (STYL-04) + content guard</name>
  <read_first>
    - bin/cli/intake.ts lines 55-88 (both return points; where to register as a non-fatal side effect)
    - bin/lib/global-library.ts (08-01 — registerPaperInGlobalLibrary signature + GlobalLibraryEntry shape)
    - bin/lib/state.ts (how paperId is obtained; loadState / initState — read, do not duplicate)
    - references/honesty-framing.md (honest-framing tone precedent — "improves prose, does not evade detection")
    - README.md (current contents — add a new section, do not disturb existing)
    - tests/repo-files.test.ts ~line 326 (CF-D24 content-guard pattern to copy)
    - 08-PATTERNS.md section "README.md (modify)" (incl. the suggested content-guard assertions) + 08-RESEARCH.md LIB-04 + Open-Q1/Q4
  </read_first>
  <action>
    Intake registration (LIB-04): at the END of intakeCommand.run(), in BOTH the noLlm and Tier-1 branches before each `return {ok:true,...}`, register the paper as a NON-FATAL side effect: derive class from config.toml [project] class (default 'Unfiled'), name from config.toml [project] name (fallback derived from title or folder basename), folderPath = path.resolve(process.cwd()), id = the paperId from STATE.json; call registerPaperInGlobalLibrary({id, name, folderPath, class, status:'intake', createdAt, updatedAt}) inside a try/catch that WARNs to stderr and never fails the verb (Open-Q4 — global-library update is a non-fatal side effect). If STATE.json/paperId is not yet available in the Tier-2 placeholder path, guard the registration (skip with a WARN rather than crashing). README disclosure (STYL-04): add a `## Style Match` section to README.md with HONEST DUAL-USE framing — describe it as matching the user's established voice (opt-in), explicitly disclose the dual-use nature, and state it does NOT make text undetectable and is not for impersonation; mirror the honesty-framing.md tone. The copy MUST NOT contain "impersonate", "evade detection", or "undetectable". Add a content guard to tests/repo-files.test.ts (CF-D24 style): assert README matches /## Style Match/ and a "match your.*voice|established voice" phrase, and assert it does NOT contain /evade detection/i or /undetectable/i (08-PATTERNS suggested assertions).
  </action>
  <verify>
    <automated>node --import tsx --test tests/repo-files.test.ts && node --import tsx -e "import('node:fs').then(fs=>{const r=fs.readFileSync('README.md','utf8');if(!/## Style Match/.test(r))throw new Error('no Style Match section');if(/evade detection|undetectable|impersonate/i.test(r))throw new Error('dishonest framing present');console.log('OK')})"</automated>
  </verify>
  <done>repo-files.test.ts GREEN with the new README guard; intake registers the paper in the global library with a class (non-fatal); README has a Style Match section with honest framing and none of the forbidden phrases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| STYLE.json contents → drafter input | a profile file read into the strict drafter contract |
| PLAN.md voice_hint vs style-match | precedence conflict that could over-fit prose |
| README framing → user disclosure | honesty non-negotiable on a sensitive feature |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Tampering | drafter-input contract widening | mitigate | Only ONE additive optional field (styleProfilePath); .strict() chokepoint preserved so unknown fields still throw (T-3-10 / WRTE-04 intact) |
| T-08-05-02 | Tampering | style-match over-fitting / ignoring section override | mitigate | Strict priority: PLAN.md voice_hint always wins over style-match (Pitfall 7); asserted by write-style-integration.test.ts |
| T-08-05-03 | Information Disclosure | dishonest detection-evasion framing | mitigate | README forbids "impersonate"/"evade detection"/"undetectable"; content guard in repo-files.test.ts enforces honest framing (STYL-04 non-negotiable) |
| T-08-05-04 | Denial of Service | intake crash on global-library failure | mitigate | Registration wrapped in try/catch → WARN, never fails the verb (Open-Q4 non-fatal side effect) |
| T-08-05-05 | Tampering | malformed STYLE.json reaching the drafter | mitigate | STYLE.json parsed via StyleProfileSchema before rendering voiceHint; parse failure falls back to default tone |
| T-08-05-SC | Tampering | npm/pip/cargo installs | accept | No installs — zod already present (08-RESEARCH audit) |
</threat_model>

<verification>
- `node --import tsx --test tests/write-style-integration.test.ts tests/drafter-input.test.ts tests/repo-files.test.ts` GREEN.
- README has `## Style Match` with honest framing and none of the forbidden phrases.
- `npm run check` passes — .strict() preserved on DrafterInputSchema.
</verification>

<success_criteria>
- styleProfilePath added additively; voiceHint priority PLAN override > style-match > default.
- Intake registers the paper in the global library with class (LIB-04), non-fatally.
- README ships the dual-use disclosure; content guard enforces honest framing.
</success_criteria>

<output>
Create `.planning/phases/08-style-match-sketch-add-library-byo-pdf-polish/08-05-SUMMARY.md` when done
</output>
