---
phase: 12-live-research-intake-bootstrap-humanizer-task
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/research-discovery.test.ts
  - tests/intake-bootstrap.test.ts
  - tests/humanizer-task.test.ts
autonomous: true
requirements: [GEN-03, GEN-04, GEN-05]

must_haves:
  truths:
    - "The three Phase-12 behaviors each have a RED-by-skip test scaffold that reports skips (never failures) until the production seam exists"
    - "Every scaffold resolves its own file path via fileURLToPath(import.meta.url) so it runs on spaced repo paths"
    - "Every scaffold is offline: source cassettes + PENSMITH_NO_LLM + injected TaskRunner — zero live network or LLM or Task calls"
  artifacts:
    - path: "tests/research-discovery.test.ts"
      provides: "GEN-03 RED-by-skip scaffold (fan-out returns >=1 deduped candidate, DOI dedup, no-placeholder, zero-candidate WARN, D-15 ordering)"
      contains: "fileURLToPath"
    - path: "tests/intake-bootstrap.test.ts"
      provides: "GEN-04 RED-by-skip scaffold (STATE.json + paperId, idempotency, WARN-skip-guard flip)"
      contains: "fileURLToPath"
    - path: "tests/humanizer-task.test.ts"
      provides: "GEN-05 RED-by-skip scaffold (injected TaskRunner call-through + FINAL.md write + null-runner clean skip)"
      contains: "__setTaskRunnerForTest"
  key_links:
    - from: "tests/*.test.ts"
      to: "feature-detect / source-grep predicates"
      via: "existsSync + source token grep guarding test skip"
      pattern: "skip:\\s*!"
---

<objective>
Author the three Wave-0 RED-by-skip test scaffolds for Phase 12 (GEN-03 live research, GEN-04 intake bootstrap, GEN-05 humanizer Task). Each scaffold encodes the behavioral contract the later waves must satisfy, but skips cleanly (never fails) until its production seam exists — so the full suite stays GREEN with zero failures throughout the phase.

Purpose: Lock the executable acceptance criteria for all three requirements BEFORE any production code lands, in the locked RED-by-skip convention (05-01/06-01/08-00/10-00). This is the Nyquist sampling floor — every later task verifies against these files.
Output: tests/research-discovery.test.ts, tests/intake-bootstrap.test.ts, tests/humanizer-task.test.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-live-research-intake-bootstrap-humanizer-task/12-RESEARCH.md
@.planning/phases/12-live-research-intake-bootstrap-humanizer-task/12-PATTERNS.md
@.planning/phases/12-live-research-intake-bootstrap-humanizer-task/12-VALIDATION.md

<read_first>
- tests/humanizer-wrap.test.ts — the existing GEN-05 base scaffold; copy its stdout-capture + skip-guard + no-throw shape exactly (do NOT modify this file; the new tests/humanizer-task.test.ts extends its structure).
- tests/state.test.ts — the mkPaperRoot() tmp-dir + env-override + dynamic-import pattern; initState/loadState round-trip and StateAlreadyExistsError precedents to reuse for the intake-bootstrap scaffold.
- tests/intake-pii-egress.test.ts — the intake run() dynamic-import + __setInterpolateForTest seam usage; mirror its env-set-then-dynamic-import ordering.
- tests/tier-contract.test.ts — the PENSMITH_NO_LLM offline-gate usage; both adapter cassettes and the LLM mock key on this env.
- bin/lib/http-mock.ts — isOfflineMode() (keys on PENSMITH_NETWORK_TESTS !== '1') and loadCassetteFile()/loadCassetteDir(); the adapter offline path the scaffolds depend on.
</read_first>

<interfaces>
<!-- Contracts the scaffolds assert against. Extracted from the codebase + RESEARCH. -->

Existing seams to feature-detect (skip guards):
- bin/lib/research-orchestrator.ts — NEW in Wave 1 (Plan 02). Skip guard: existsSync of the built module path.
- bin/cli/intake.ts — initState() call is NEW in Wave 1 (Plan 03). Skip guard: source-grep intake.ts for the token `initState(` (existsSync alone is insufficient; intake.ts already exists).
- bin/lib/exporter.ts — __setTaskRunnerForTest export is NEW in Wave 2 (Plan 04). Skip guard: source-grep exporter.ts for `__setTaskRunnerForTest`.

Offline env (set BEFORE any dynamic import):
- process.env['PENSMITH_NO_LLM'] = '1'   // LLM mock; topic-disambiguator + source-evaluator parse fail → fallback
- (do NOT set PENSMITH_NETWORK_TESTS)     // isOfflineMode() → true → adapter cassettes fire

Injectable Task seam (GEN-05), to be created in Plan 04:
- export type TaskRunner = (skill: string, input: Record<string, string>) => Promise<{ output: string }>;
- export function __setTaskRunnerForTest(fn: TaskRunner | null): void;
- runHumanizer(draftMd: string, paperRoot?: string): Promise<string | null>;

STATE.json contract (GEN-04), from bin/lib/schemas/state.ts:
- { $schemaVersion: 2, paperId: string(min 1), createdAt: ISO-8601, sections?: [{n,slug}] }
- initState(paperRoot, seed?) throws StateAlreadyExistsError (code 'STATE_ALREADY_EXISTS') if present.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: research-discovery RED-by-skip scaffold</name>
  <read_first>
    - tests/tier-contract.test.ts (PENSMITH_NO_LLM offline usage)
    - tests/state.test.ts (mkPaperRoot tmp-dir + env-override + dynamic-import)
    - bin/lib/http-mock.ts (isOfflineMode, loadCassetteFile)
    - bin/cli/research.ts (current swap-seam shape; the run() return contract)
  </read_first>
  <files>tests/research-discovery.test.ts</files>
  <action>Create the GEN-03 scaffold. Resolve the test's own directory with fileURLToPath(import.meta.url) + dirname (NOT import.meta.url.pathname and NOT a regex strip of file://) so the suite runs on a spaced repo path — this was the Phase-11 local-vs-CI bug. Set process.env['PENSMITH_NO_LLM']='1' at module top and never set PENSMITH_NETWORK_TESTS (offline cassettes). Skip-guard EVERY test on a predicate `discoverySeamWired()` that returns existsSync of the built/source research-orchestrator module path AND a source-grep of bin/cli/research.ts confirming the swap-seam block was replaced (grep for an absence of the literal token `Phase-12 / GEN-03 swap seam` OR presence of an import from research-orchestrator). Author these test bodies (all skipped until the seam lands): (1) under offline cassettes + PENSMITH_NO_LLM, the discovery path returns >=1 deduped SourceCandidate on a fixture assignment; (2) two candidates with the same DOI collapse to one (DOI dedup via normalizeDoi); (3) the source-evaluator parse-failure path under PENSMITH_NO_LLM keeps all candidates (defensive fallback, T-11-10); (4) the zero-candidate degenerate case writes a real EMPTY LIBRARY.json (entries:[]) with a WARN — assert NO literal `tier2-placeholder` / `PLACEHOLDER_LIBRARY` / `_note` marker appears in the written file; (5) crossCheckRetractions runs BEFORE writeBibtex (D-15) — assert via call-order capture on injected seams or by asserting the orchestrator returns candidates to research.ts which owns the locked chokepoint sequence. Use loadCassetteFile/loadCassetteDir for the searchable-adapter fixtures. NO code fences with implementations in the action — write the actual test file via the editor. Keep all assertions inside skip-guarded test() blocks so the file reports skips, not failures.</action>
  <verify>
    <automated>node --import tsx --test tests/research-discovery.test.ts</automated>
  </verify>
  <done>tests/research-discovery.test.ts exists, runs with 0 failures (all tests skipped via discoverySeamWired() === false), uses fileURLToPath path resolution, sets PENSMITH_NO_LLM=1, and contains the five GEN-03 behavioral test bodies.</done>
</task>

<task type="auto">
  <name>Task 2: intake-bootstrap RED-by-skip scaffold</name>
  <read_first>
    - tests/state.test.ts (initState/loadState round-trip + StateAlreadyExistsError + mkPaperRoot)
    - tests/intake-pii-egress.test.ts (intake run() dynamic import + env ordering)
    - bin/cli/intake.ts (resolvePaperId line 167, registerPaperNonFatal WARN-skip guard line 195, runStyleProducerNonFatal line 231)
  </read_first>
  <files>tests/intake-bootstrap.test.ts</files>
  <action>Create the GEN-04 scaffold. Resolve paths with fileURLToPath(import.meta.url). Set PENSMITH_NO_LLM=1 before any dynamic import; use the mkPaperRoot()-style tmp-dir + HOME/LOCALAPPDATA/XDG_DATA_HOME override so global-library + STATE.json writes land in a sandbox. Skip-guard every test on `intakeBootstrapWired()` = a source-grep of bin/cli/intake.ts for the token `initState(` (existsSync is useless — intake.ts already exists). Author these skipped bodies: (1) running intake writes .paper/STATE.json conforming to the v2 schema with a non-null paperId ($schemaVersion === 2, paperId.length >= 1, createdAt parseable as ISO-8601); (2) idempotency — running intake twice on the same paper does NOT regenerate paperId (load STATE.json after run 1, run intake again, assert paperId unchanged and StateAlreadyExistsError was caught, not thrown out of run()); (3) WARN-skip-guard FLIP — after intake writes STATE.json, resolvePaperId returns non-null so the global-library registration proceeds (assert the registry entry exists / no `skipping global-library registration` WARN on the second observable path). Drive intake via its run() with PENSMITH_NO_LLM=1 and a fixture assignment. NO implementation code fences in the action.</action>
  <verify>
    <automated>node --import tsx --test tests/intake-bootstrap.test.ts</automated>
  </verify>
  <done>tests/intake-bootstrap.test.ts exists, 0 failures (all skipped via intakeBootstrapWired() === false), uses fileURLToPath, sets PENSMITH_NO_LLM=1, contains the three GEN-04 behavioral bodies (write, idempotency, WARN-skip flip).</done>
</task>

<task type="auto">
  <name>Task 3: humanizer-task RED-by-skip scaffold</name>
  <read_first>
    - tests/humanizer-wrap.test.ts (existing GEN-05 base: stdout capture, skip guard, no-throw, exporterModUrl pattern — copy exactly, do NOT modify the original)
    - bin/lib/exporter.ts (runHumanizer current stub lines 72-100; the present-but-no-transport banner copy)
    - bin/cli/done.ts (the before/after scoreHonesty + renderHonestyReport flow lines 406-427 — done.ts owns honesty; runHumanizer must NOT call scoreHonesty)
  </read_first>
  <files>tests/humanizer-task.test.ts</files>
  <action>Create the GEN-05 scaffold (a NEW file; do not edit tests/humanizer-wrap.test.ts). Resolve the exporter module URL via fileURLToPath/pathToFileURL like the existing humanizer-wrap test. Skip-guard every test on `taskSeamWired()` = a source-grep of bin/lib/exporter.ts for the token `__setTaskRunnerForTest`. Author these skipped bodies using the injectable seam: (1) call-through — inject a deterministic TaskRunner returning { output: '<humanized prose>' }, call runHumanizer(draft, tmpDir), assert it returns a path ending in FINAL.md, the file is written under paperDir(tmpDir) (NOT cwd+'/FINAL.md', Pitfall 8), and its content equals the injected output; (2) null-runner clean skip — inject null (Tier-2), assert runHumanizer returns null AND prints the 'humanizer skill present but no Task transport' banner AND never throws; (3) honesty-framing integrity — assert that the banner/skip copy does NOT contain the word 'undetectable' and the honest-framing copy stays 'improves prose' (this is a locked-framing regression guard; if asserting against renderHonestyReport, import it from honesty.ts and grep its output for the forbidden token). Always restore the seam to null in a finally block. NO implementation code fences in the action.</action>
  <verify>
    <automated>node --import tsx --test tests/humanizer-task.test.ts</automated>
  </verify>
  <done>tests/humanizer-task.test.ts exists, 0 failures (all skipped via taskSeamWired() === false), uses fileURLToPath, contains the three GEN-05 bodies (call-through FINAL.md, null-runner skip, framing-integrity guard), and restores the seam in finally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → filesystem | scaffolds write STATE.json / LIBRARY.json / FINAL.md into a tmp sandbox; HOME/XDG overrides keep them out of the real home dir |
| injected seams → code under test | TaskRunner / adapter fixtures are test-controlled; no live network, LLM, or Task transport |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-W0-01 | Tampering | spaced-path resolution in tests | mitigate | All scaffolds use fileURLToPath(import.meta.url) — never .pathname/regex-strip (Phase-11 local-vs-CI bug) |
| T-12-W0-02 | Denial of Service | accidental live network/LLM/Task in CI | mitigate | PENSMITH_NO_LLM=1 set; PENSMITH_NETWORK_TESTS unset (offline cassettes); TaskRunner injected; assertion: zero live calls |
| T-12-W0-03 | Information Disclosure | sandbox leak into real $HOME | mitigate | mkPaperRoot()-style HOME/LOCALAPPDATA/XDG_DATA_HOME override before any dynamic import |
| T-12-W0-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `node --import tsx --test tests/research-discovery.test.ts tests/intake-bootstrap.test.ts tests/humanizer-task.test.ts` — all three run with 0 failures, reporting skips.
- `npm test` — full suite stays GREEN (no new failures, only new skips).
- Grep each new test file for `fileURLToPath` (spaced-path safe) and confirm none use `import.meta.url.pathname` or a `file://` regex strip.
</verification>

<success_criteria>
- Three RED-by-skip scaffolds exist, each skipping cleanly until its Wave-1/Wave-2 seam lands.
- Zero new test failures in the full suite.
- Each scaffold is offline (PENSMITH_NO_LLM + cassettes + injected TaskRunner) and spaced-path safe (fileURLToPath).
- The GEN-05 scaffold guards the locked honest framing (no 'undetectable').
</success_criteria>

<output>
Create `.planning/phases/12-live-research-intake-bootstrap-humanizer-task/12-01-SUMMARY.md` when done.
</output>
