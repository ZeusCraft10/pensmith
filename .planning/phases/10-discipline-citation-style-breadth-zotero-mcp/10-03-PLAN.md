---
phase: 10-discipline-citation-style-breadth-zotero-mcp
plan: 03
type: execute
wave: 1
depends_on: ["10-00"]
files_modified:
  - bin/lib/schemas/source-candidate.ts
  - bin/lib/sources/zotero-mcp.ts
  - bin/lib/sources/index.ts
  - bin/lib/doctor/probes/zotero-mcp-presence.ts
  - templates/presets/disciplines.json
  - tests/sources/zotero-mcp.test.ts
  - tests/disciplines-schema.test.ts
  - tests/doctor-probes.test.ts
autonomous: true
requirements: [RSCH-06, CITE-02]
must_haves:
  truths:
    - "A zotero-mcp source adapter is registered in the sources registry and AdapterName union"
    - "When Zotero MCP is present (or a client is injected) AND authenticated AND a client is available, search() actually PULLS from the Zotero source and NORMALIZES each item to a SourceCandidate (source:'zotero-mcp', same shape every other adapter returns) — proven by an offline test that injects a fake Zotero client and asserts ≥1 normalized SourceCandidate is contributed"
    - "zotero-mcp search() returns [] (never throws) when Zotero MCP is absent AND no client is injected, OR when unauthenticated, OR when no client is wired — absence does not break research, and an offline test asserts the absent+no-client+unauth path returns [] while research still proceeds"
    - "The SourceCandidate discriminated union includes a 'zotero-mcp' source variant so normalized Zotero candidates validate against the locked D-14 schema"
    - "The doctor zotero-mcp-presence probe reports tri-state: ABSENT (WARN), CONFIGURED-but-no-ZOTERO_API_KEY (WARN), and configured+authenticated (PASS) — without ever logging the key value"
    - "disciplines.json carries all 6 PRD §8 fields per entry and the computer-science preset defaults to ieee (was apa)"
    - "No 17th verb is introduced — Zotero is a source provider + doctor probe only"
  artifacts:
    - path: "bin/lib/schemas/source-candidate.ts"
      provides: "'zotero-mcp' literal added to the SourceCandidate discriminated union (D-14)"
      contains: "zotero-mcp"
    - path: "bin/lib/sources/zotero-mcp.ts"
      provides: "presence(-or-injected-client)+auth-gated Zotero source adapter that pulls via an injectable client and normalizes to SourceCandidate[] when present+authenticated, returns [] when absent+no-client/unauth/no-client; + isZoteroAuthenticated helper + setZoteroClientForTest seam"
      exports: ["search", "isZoteroAuthenticated", "setZoteroClientForTest"]
      contains: "isZoteroMcpPresent"
    - path: "bin/lib/sources/index.ts"
      provides: "'zotero-mcp' entry in sources const (AdapterName auto-expands)"
      contains: "zotero-mcp"
    - path: "bin/lib/doctor/probes/zotero-mcp-presence.ts"
      provides: "tri-state auth probe (ABSENT/CONFIGURED_NO_AUTH/PASS), key presence checked by boolean only"
      contains: "ZOTERO_API_KEY"
    - path: "templates/presets/disciplines.json"
      provides: "6-field schema per entry (PRD §8) + CS→ieee correction"
      contains: "sourcePreference"
  key_links:
    - from: "bin/lib/sources/zotero-mcp.ts"
      to: "bin/lib/ecosystem-presence.ts isZoteroMcpPresent"
      via: "presence guard (OR an injected client) before pulling candidates"
      pattern: "isZoteroMcpPresent"
    - from: "bin/lib/sources/zotero-mcp.ts toCandidate"
      to: "bin/lib/schemas/source-candidate.ts SourceCandidate"
      via: "normalize injected-client raw items to source:'zotero-mcp' SourceCandidate"
      pattern: "source: ?'zotero-mcp'"
    - from: "bin/lib/sources/index.ts"
      to: "bin/lib/sources/zotero-mcp.ts"
      via: "import * as zoteroMcp + 'zotero-mcp' key in sources const"
      pattern: "zotero-mcp"
    - from: "bin/lib/doctor/probes/zotero-mcp-presence.ts"
      to: "process.env ZOTERO_API_KEY"
      via: "boolean !!process.env presence check (T-01-07 no-leak)"
      pattern: "ZOTERO_API_KEY"
---

<objective>
Complete RSCH-06 (Zotero MCP source provider, USED-AS-SOURCE when present +
absence-non-breaking) and the discipline-preset depth (CITE-02 discipline→style
data). Five coordinated changes:
(1) add a `'zotero-mcp'` variant to the locked D-14 SourceCandidate union so
normalized Zotero items validate; (2) a new presence(-or-injected-client)+auth-gated
`bin/lib/sources/zotero-mcp.ts` adapter that ACTUALLY pulls from the Zotero source
via an injectable client and normalizes results to `SourceCandidate[]` when present
+ authenticated (and returns `[]` when absent+no-client/unauth/no-client); (3) its
registration in `bin/lib/sources/index.ts`; (4) the `zotero-mcp-presence` doctor
probe upgraded to a tri-state auth check; (5) `disciplines.json` completed to the
6-field PRD §8 schema with the CS preset corrected from apa to ieee.

Purpose: Zotero is a SOURCE PROVIDER + DOCTOR PROBE, never a verb (no 17th verb).
The adapter is the EXECUTABLE "used-as-source when present" path: when Zotero MCP is
present (OR a client is injected) AND ZOTERO_API_KEY is set AND a client is wired,
search() pulls items and normalizes them to the SAME SourceCandidate shape every
other adapter returns, so they flow into the research pipeline (scoring + RSCH-11
retraction cross-check). When the path is absent+no-client OR unauthenticated OR no
client is wired, search() returns `[]` so research continues on the other 7 adapters
(ARCH-03).

Transport reality (RESEARCH §Anti-Patterns; Assumption A4): a real MCP tool call is
issued from the Tier-1 workflow body (Claude Code Task tool), not from inside the
Tier-2 adapter process. To make the normalization path EXECUTABLE AND TESTED without
a live MCP server on CI (the dev machine has no Zotero MCP), the adapter accepts an
INJECTABLE client (a `ZoteroClient` interface). In production Tier-1 the workflow
body wires the real MCP-backed client through this seam; in tests we inject a fake
client that returns canned raw items. Either way the pull → normalize → validate
path is real code exercised by a test. The live end-to-end against a real Zotero MCP
server remains a documented MANUAL-only item (it cannot run on CI).

CONSTRAINT: NO new npm deps. NO http.ts import (Zotero MCP uses the MCP protocol, not
HTTP; any future Zotero Web API call is a later phase and must go through http.ts
then). T-01-07 no-leak: check ZOTERO_API_KEY presence as a boolean only — NEVER
log/persist the value. Absence MUST NOT throw.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-discipline-citation-style-breadth-zotero-mcp/10-RESEARCH.md
@.planning/phases/10-discipline-citation-style-breadth-zotero-mcp/10-PATTERNS.md

<interfaces>
<!-- Exact code to mirror/extend. Use these directly. -->

bin/lib/sources/index.ts (full current content) — the registry to extend:
  import * as crossref from './crossref.js'; ... import * as retractionWatch from './retraction-watch.js';
  export const sources = { crossref, openalex, arxiv, pubmed, semanticscholar, unpaywall, 'retraction-watch': retractionWatch } as const;
  export type AdapterName = keyof typeof sources;   // auto-expands to include 'zotero-mcp'

bin/lib/ecosystem-presence.ts isZoteroMcpPresent() — already exported; checks
~/.claude/mcp_servers.json + ~/.config/claude/mcp_servers.json for a /zotero/i server name.
It is FILESYSTEM-based and is naturally false on CI — which is why the CANONICAL GATE
(Task 1 STEP B) treats an explicitly-injected client as a valid presence signal so the
offline test drives the present+authenticated branch via an INJECTED client, not by
writing a real mcp_servers.json. isZoteroAuthenticated() is NOT yet exported from this
module — Task 1 defines it inside the adapter (key-only predicate).

bin/lib/sources/semanticscholar.ts toCandidate() (lines 75-108) — the EXACT normalization
shape to mirror for the Zotero normalizer:
  source, id (required, non-empty), doi?, title (required), authors (required string[],
  drop entry if empty), year? (int 1800..2100), abstract?, retracted:false,
  last_verified:new Date().toISOString(), citekey (via generateCitekey({authors,year})),
  raw:item. Drop any item missing id/title/authors (return null, then .filter).
  import { generateCitekey } from '../citekey.js'.

bin/lib/schemas/source-candidate.ts (lines 42-51) — the D-14 discriminated union to extend:
  export const SourceCandidateSchema = z.discriminatedUnion('source', [
    z.object({ ...BaseFields, source: z.literal('crossref') }), ... 7 variants ...
  ]);
  ADD an 8th variant: z.object({ ...BaseFields, source: z.literal('zotero-mcp') }).
  BaseFields is reused verbatim (id/title/authors/year/doi/.../citekey/raw). No other
  schema change — type SourceCandidate auto-includes the new variant.

bin/lib/doctor/probes/zotero-mcp-presence.ts (full current content) — the binary probe to
make tri-state. candidatePaths() helper + isZoteroMcpPresent() already wired. Probe shape:
  export const zoteroMcpPresenceProbe: Probe = { id:'zotero-mcp-presence', async run(): Promise<ProbeResult> {...} };
ProbeResult severities: 'PASS' | 'WARN' | 'FAIL'; fields { id, severity, summary, detail?, fix? }.

tests/doctor-probes.test.ts existing zotero assertion (around lines 41-46): asserts
severity ∈ {PASS, WARN} and that detail contains 'Checked:' when severity === 'WARN'.
BOTH new WARN variants MUST keep 'Checked:' in detail so this stays green. The SENTINEL
no-leak test pattern lives ~lines 121-152 (mirror it for ZOTERO_API_KEY).

templates/presets/disciplines.json (full current content — 2 fields per entry):
  { "psychology": {"defaultTone":"academic-formal","defaultCitationStyle":"apa"},
    "computer-science": {"defaultTone":"technical","defaultCitationStyle":"apa"}, ... 9 keys total }
The 9 keys: psychology, computer-science, history, philosophy, biology, sociology,
economics, literature, other.

SourceCandidate type: import type { SourceCandidate } from '../schemas/source-candidate.js'.
PATTERNS.md shows the zotero-mcp.ts shell, probe tri-state, and disciplines.json blocks —
but PATTERNS.md's zotero-mcp.ts returns [] in EVERY path; that is the H1 defect. This plan
OVERRIDES it: search() pulls + normalizes via the injected client on the present+auth path,
using the CANONICAL GATE PREDICATE in Task 1 STEP B (the single source of truth that 10-00's
test quotes verbatim — H3 fix).
tests/sources/zotero-mcp.test.ts + tests/disciplines-schema.test.ts already RED from 10-00
(10-00 now also lands the present+authenticated injected-client test — see 10-00 Task 2 leg (c),
which references the CANONICAL GATE PREDICATE below verbatim).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 'zotero-mcp' schema variant + adapter that pulls+normalizes via injectable client + register in sources/index.ts</name>
  <files>bin/lib/schemas/source-candidate.ts, bin/lib/sources/zotero-mcp.ts, bin/lib/sources/index.ts</files>
  <read_first>
    - bin/lib/schemas/source-candidate.ts (the D-14 union — add the 8th literal variant)
    - bin/lib/sources/index.ts (the full registry to extend)
    - bin/lib/sources/semanticscholar.ts (the toCandidate normalizer shape — lines 75-108 — and the search() Array contract; mirror toCandidate for the Zotero items)
    - bin/lib/sources/crossref.ts (the adapter shell shape — search signature)
    - bin/lib/citekey.ts (generateCitekey — reuse for the Zotero citekey)
    - bin/lib/ecosystem-presence.ts (isZoteroMcpPresent — reuse, do not re-implement; note isZoteroAuthenticated is NOT here — define it in the adapter)
    - .planning/phases/10-discipline-citation-style-breadth-zotero-mcp/10-PATTERNS.md (bin/lib/sources/zotero-mcp.ts + sources/index.ts blocks — note the []-in-all-paths shell is OVERRIDDEN here)
    - .planning/phases/10-discipline-citation-style-breadth-zotero-mcp/10-RESEARCH.md (§Architecture Patterns Pattern 3; §Anti-Patterns — absence throwing, http import ban; Assumption A4 — adapter pulls via Tier-1 client, not Tier-2 network)
    - tests/sources/zotero-mcp.test.ts (the RED contract from 10-00 — including the present+authenticated injected-client test that quotes the CANONICAL GATE PREDICATE in STEP B)
  </read_first>
  <action>
    STEP A — extend the D-14 SourceCandidate union (bin/lib/schemas/source-candidate.ts):
    Add an 8th variant to the discriminatedUnion array:
    `z.object({ ...BaseFields, source: z.literal('zotero-mcp') })`. Reuse BaseFields
    verbatim. Add a one-line comment noting RSCH-06 adds Zotero as a normalizable source.
    `type SourceCandidate` auto-includes it; no consumer change needed beyond this.

    STEP B — create bin/lib/sources/zotero-mcp.ts as a PRESENCE(-OR-INJECTED-CLIENT)+AUTH-GATED
    adapter that ACTUALLY pulls + normalizes via an INJECTABLE client (this is the H1
    executable path):
    - Imports: `import { isZoteroMcpPresent } from '../ecosystem-presence.js';`
      `import { generateCitekey } from '../citekey.js';`
      `import type { SourceCandidate } from '../schemas/source-candidate.js';`
      Do NOT import http.ts (Zotero MCP uses the MCP protocol, not HTTP) and do NOT import
      citation-js (D-19).
    - Define the client seam:
        `export interface ZoteroItem { ... }` — the minimal raw fields the normalizer reads
        (id/key, title, creators/authors, date/year, DOI, abstractNote). Keep fields
        optional and defensive (the real MCP payload shape varies by server).
        `export interface ZoteroClient { search(query: string, limit: number): Promise<ZoteroItem[]>; }`
        A module-level `let _client: ZoteroClient | null = null;`.
        `export function setZoteroClientForTest(client: ZoteroClient | null): void { _client = client; }`
        — the injection seam (also used by the Tier-1 workflow body to wire the real
        MCP-backed client; named *ForTest for parity with _reset*ForTest conventions, but
        it is the production injection point too — document that in the header).
    - `function toCandidate(item: ZoteroItem): SourceCandidate | null` — MIRROR
      semanticscholar.toCandidate (lines 75-108): require a non-empty id (item.id ?? item.key)
      and a non-empty title and ≥1 author (map creators → "First Last" or .name; drop on
      empty); derive year as an int in 1800..2100 (parse from item.date if needed);
      `source: 'zotero-mcp'`; doi from item.DOI; retracted:false;
      last_verified:new Date().toISOString(); citekey via generateCitekey({authors,year});
      raw:item. Return null on any missing required field, then .filter at the call site.

    >>> CANONICAL GATE PREDICATE — SINGLE SOURCE OF TRUTH (H3 fix) <<<
    This is the ONE authoritative definition of the presence + auth gating. 10-00 Task 2
    leg (c) QUOTES this block VERBATIM as its test contract — do NOT restate it divergently
    anywhere, and the executor MUST implement it EXACTLY as written here (no variant).

    - `export async function search(query: string, limit = 10): Promise<SourceCandidate[]>`
      MUST encode these four steps, in this exact order:

        // (1) PRESENCE GATE — an injected _client is itself a valid presence signal, so a CI
        //     test that calls setZoteroClientForTest(fakeClient) reaches normalization even
        //     though isZoteroMcpPresent() is filesystem-FALSE on CI; real Tier-1 presence
        //     (isZoteroMcpPresent()) ALSO passes this gate. Absent AND no client → [].
        if (!isZoteroMcpPresent() && _client === null) return [];   // absent + no client → non-breaking []

        // (2) AUTH GATE — DECOUPLED from FS-presence (H3): the key alone authorizes the pull,
        //     so the injected-client CI path is authorizable with just ZOTERO_API_KEY. On the
        //     real path, the presence gate above already short-circuits the absent case.
        if (!isZoteroAuthenticated()) return [];                     // no ZOTERO_API_KEY → skip (Pitfall 6)

        // (3) NO-CLIENT GATE — present(+auth) but the client is not yet wired (real Tier-1
        //     before the workflow body injects, or Tier-2 default). Preserves absence-non-
        //     breaking on the un-wired real path and makes the (4) _client deref safe.
        if (_client === null) return [];                            // present+auth but no client wired → []

        // (4) PULL + NORMALIZE — reached only when a client is present AND authenticated.
        try {
          const items = await _client.search(query, limit);
          return items.map(toCandidate).filter((c): c is SourceCandidate => c !== null);
        } catch { return []; }                                      // ARCH-03: never throw, always an array

      The KEY contract: NEVER throw, ALWAYS return an array; and on the present(-or-injected)
      +authenticated+client path it ACTUALLY normalizes real items into SourceCandidate[]
      (the H1 fix). The CI test sets `setZoteroClientForTest(fakeClient)` + `ZOTERO_API_KEY`
      → passes (1) via _client, passes (2) via the key, passes (3) because _client !== null,
      reaches (4) → ≥1 normalized SourceCandidate. With `_client === null` and no key →
      gate (1) returns [] (absent path) or, if FS-present, gate (2) returns [] (unauth).

    - `export function isZoteroAuthenticated(): boolean { return !!process.env['ZOTERO_API_KEY']; }`
      — CANONICAL auth predicate (KEY-ONLY, decoupled from FS-presence per H3 so the
      injected-client CI path is authorizable). T-01-07: boolean presence of the env var
      ONLY, NEVER the value. IMPORTANT: the doctor probe (Task 2) does NOT call this helper —
      it composes `isZoteroMcpPresent() && !!process.env['ZOTERO_API_KEY']` independently for
      its tri-state PASS/CONFIGURED_NO_AUTH/ABSENT report, so doctor still reports real
      FS-presence + key state (M8's "PASS branch untestable on CI" is unchanged and out of
      scope for this revision).
    - Header comment: RSCH-06; the present(-or-injected)+authenticated USED-AS-SOURCE pull
      path + the absent+no-client/no-auth/no-client → [] degrade contract; the NO-http.ts and
      NO-17th-verb constraints; that the real MCP-backed client is wired through
      setZoteroClientForTest from the Tier-1 workflow body (workflows/research.md) and the
      live end-to-end against a real Zotero MCP server is a MANUAL-only item (no CI coverage
      of the transport).

    STEP C — register in bin/lib/sources/index.ts: add
    `import * as zoteroMcp from './zotero-mcp.js';` and `'zotero-mcp': zoteroMcp,` to the
    sources const. AdapterName (keyof typeof sources) auto-expands — no further type change.
    Note the index.ts header already documents that 'retraction-watch' omits search;
    zotero-mcp DOES expose search, so generic iterators that guard `if ('search' in adapter)`
    include it (and on CI it safely returns [] — absent + no client wired).
  </action>
  <verify>
    <automated>npx eslint bin/lib/schemas/source-candidate.ts bin/lib/sources/zotero-mcp.ts bin/lib/sources/index.ts && node --import tsx --test tests/sources/zotero-mcp.test.ts 2>&1 | tail -25</automated>
  </verify>
  <done>source-candidate.ts has a 'zotero-mcp' union variant; zotero-mcp.ts exists exporting search + isZoteroAuthenticated + setZoteroClientForTest, with a toCandidate normalizer mirroring semanticscholar; search() encodes the CANONICAL GATE PREDICATE EXACTLY (presence gate `!isZoteroMcpPresent() && _client === null` → [], key-only auth gate, no-client gate, then pull+normalize) — pulls+normalizes via the injected client on the present+authenticated+client path and returns [] on absent+no-client/unauth/no-client (never throws); isZoteroAuthenticated() is key-only (`!!process.env['ZOTERO_API_KEY']`); index.ts registers 'zotero-mcp'; ESLint passes (no http import, no citation-js); ALL tests/sources/zotero-mcp.test.ts assertions flip RED→GREEN (existence + no-throw absent → [] + 'zotero-mcp' in sources + present+authenticated injected-client → ≥1 normalized SourceCandidate); tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade zotero-mcp-presence doctor probe to tri-state auth check</name>
  <files>bin/lib/doctor/probes/zotero-mcp-presence.ts, tests/doctor-probes.test.ts</files>
  <read_first>
    - bin/lib/doctor/probes/zotero-mcp-presence.ts (the full binary probe to make tri-state)
    - bin/lib/doctor/probes/pandoc-presence.ts (the PASS/WARN probe shape reference)
    - tests/doctor-probes.test.ts (the existing zotero assertion ~lines 41-46 + the T-01-07 no-leak SENTINEL test ~lines 121-152)
    - .planning/phases/10-discipline-citation-style-breadth-zotero-mcp/10-PATTERNS.md (zotero-mcp-presence.ts tri-state block)
    - .planning/phases/10-discipline-citation-style-breadth-zotero-mcp/10-RESEARCH.md (§Common Pitfalls 6 configured-but-not-authenticated; §Code Examples auth-state check)
  </read_first>
  <action>
    Make the probe tri-state per PATTERNS.md (RESEARCH Pitfall 6). NOTE: the probe composes
    its own presence+key check inline — it does NOT call the adapter's key-only
    isZoteroAuthenticated() (which is decoupled from FS-presence per the H3 fix). The probe
    intentionally couples presence + key so it can distinguish ABSENT from CONFIGURED_NO_AUTH:
    - const API_KEY_ENV = 'ZOTERO_API_KEY' (module-level).
    - In run(): const configured = isZoteroMcpPresent(); const authenticated = configured &&
      !!process.env[API_KEY_ENV].
    - if (!configured) → WARN, summary 'Zotero MCP server not configured — ...', detail
      `Checked: ${candidatePaths().join(', ')}`, plus a fix line. (Keeps 'Checked:' in detail.)
    - else if (!authenticated) → WARN, summary
      `Zotero MCP configured but ${API_KEY_ENV} not set — Zotero sources will be skipped.`,
      detail `Checked: ${candidatePaths().join(', ')}. ${API_KEY_ENV} not found in env.`,
      fix `Set ${API_KEY_ENV} in your environment ...`. (MUST keep 'Checked:' in detail so
      the existing test's WARN-branch assertion holds.)
    - else → PASS, summary 'Zotero MCP configured and authenticated'.
    T-01-07: emit only the env-var NAME (API_KEY_ENV) and a boolean-derived message — NEVER
    interpolate process.env[API_KEY_ENV] into any string.

    Then extend tests/doctor-probes.test.ts with the tri-state coverage (do NOT remove the
    existing assertion):
    - Save/restore process.env.ZOTERO_API_KEY around each case (finally block).
    - CONFIGURED_NO_AUTH coverage is hard to force when Zotero is genuinely absent on CI
      (isZoteroMcpPresent() is false), so assert the contract that holds on any machine: the
      probe's run() result is always severity ∈ {PASS, WARN}; when severity === 'WARN' the
      detail contains 'Checked:'; and the summary/detail NEVER contains the literal value of
      ZOTERO_API_KEY (set a SENTINEL value into the env, run the probe, assert the sentinel
      does NOT appear anywhere in JSON.stringify(result)) — this is the load-bearing T-01-07
      no-leak assertion. Mirror the existing SENTINEL no-leak test pattern in the file.
  </action>
  <verify>
    <automated>npx eslint bin/lib/doctor/probes/zotero-mcp-presence.ts && node --import tsx --test tests/doctor-probes.test.ts 2>&1 | tail -25</automated>
  </verify>
  <done>The probe returns tri-state results; the no-leak test proves the ZOTERO_API_KEY value never appears in the probe output; the existing PASS/WARN + 'Checked:' assertions still pass; ESLint + tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Complete disciplines.json to the 6-field PRD §8 schema (CS→ieee)</name>
  <files>templates/presets/disciplines.json, tests/disciplines-schema.test.ts</files>
  <read_first>
    - templates/presets/disciplines.json (the full current 2-field-per-entry file — 9 keys)
    - bin/lib/citation-density.ts (DISCIPLINE_TARGETS map — the densityTarget center values to align with, per RESEARCH Option B)
    - .planning/phases/10-discipline-citation-style-breadth-zotero-mcp/10-PATTERNS.md (disciplines.json extend block — field semantics + example entries)
    - .planning/phases/10-discipline-citation-style-breadth-zotero-mcp/10-RESEARCH.md (§Architecture Patterns Pattern 4; §Open Questions 3 CS→ieee)
    - tests/disciplines-schema.test.ts (the RED contract from 10-00 — all 6 fields + CS ieee + densityTarget keys)
  </read_first>
  <action>
    Extend EVERY one of the 9 entries (psychology, computer-science, history, philosophy,
    biology, sociology, economics, literature, other) to carry all 6 PRD §8 fields:
    defaultTone (keep existing), defaultCitationStyle (keep existing EXCEPT correct
    computer-science from "apa" to "ieee" per PRD §8 / RESEARCH Open Q3), sourcePreference
    (ordered array of AdapterName strings — choose discipline-appropriate adapters, e.g.
    CS→["arxiv","semanticscholar","openalex"], psychology→["pubmed","openalex","semanticscholar"],
    biology→["pubmed","openalex","crossref"], history/philosophy/literature→["crossref","openalex"]
    , economics/sociology→["openalex","crossref","semanticscholar"], other→["crossref","openalex"]),
    sectioningConvention (array of default section headings appropriate to the discipline),
    counterargDefault ("off" | "optional" | "required"), and densityTarget
    ({ low, center, high }).

    Align each densityTarget.center with the existing DISCIPLINE_TARGETS center in
    citation-density.ts (cs:20, bio:25, psych:22, econ:18, history:12, lit:10, philosophy:12,
    other:15; sociology has no entry there → use 15 default) — per RESEARCH Option B,
    citation-density.ts keeps its own DISCIPLINE_TARGETS (do NOT touch that module); the
    disciplines.json densityTarget center MIRRORS those numbers for consistency. Pick
    low/high as a reasonable band around center (e.g. center±~10, low≥0).

    Verify other styles referenced by defaultCitationStyle all have a bundled .csl now
    (apa, ieee, chicago-author-date, mla all exist after 10-00) — no defaultCitationStyle
    should point at a missing style.

    No code consumes the new fields in THIS plan beyond the schema test — workflow wiring
    (research.md sourcePreference, outline sectioningConvention) is a downstream consumer not
    in this phase's scope; the data must merely be present and well-formed (CITE-05/RSCH-06
    schema completion). disciplines-schema.test.ts from 10-00 is the contract; if any of its
    assertions need a minor tweak to match final field shapes, adjust the test to match the
    committed JSON (the JSON is the source of truth for field VALUES; the test enforces field
    PRESENCE + the CS→ieee + densityTarget-keys invariants).
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('templates/presets/disciplines.json','utf8'))" && node --import tsx --test tests/disciplines-schema.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>disciplines.json is valid JSON with all 6 PRD §8 fields on all 9 entries; computer-science.defaultCitationStyle === 'ieee'; every densityTarget has low/center/high; all tests/disciplines-schema.test.ts assertions flip RED→GREEN; no defaultCitationStyle points at a missing .csl.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process.env ZOTERO_API_KEY → doctor output / logs | Secret env var reaching user-visible probe output or session log |
| absent Zotero MCP → research orchestrator | A missing optional dependency that must not break the research pipeline |
| injected Zotero client items → verifier | Source candidates that must pass the same scoring + retraction checks as other adapters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Information Disclosure | ZOTERO_API_KEY leak | mitigate | Probe + adapter check `!!process.env['ZOTERO_API_KEY']` (boolean) only; emit only the env-var NAME. The no-leak test sets a SENTINEL and asserts it never appears in the probe output (T-01-07 pattern). Value never reaches disk or session log. |
| T-10-03-02 | Denial of Service | Zotero absence breaks research | mitigate | search() returns [] (never throws) when absent + no client wired OR unauthenticated OR no client is wired; the canonical gate's presence/no-client guards keep the un-wired real path safe; the workflow capability_check (wired in 10-04) skips the Zotero pull and continues on the other 7 adapters. The verifier gate is never conditional on a Zotero call. |
| T-10-03-03 | Tampering | attacker-controlled Zotero candidates | mitigate | Zotero SourceCandidates (now actually produced on the present+auth+client path) flow through the SAME source-evaluator scoring + RSCH-11 retraction cross-check as every other adapter (no bypass) — toCandidate normalizes to the identical SourceCandidate shape, so no per-source carve-out exists. |
| T-10-03-04 | Tampering | preset mis-map (defaultCitationStyle → missing .csl) | mitigate | Task-3 verifies every defaultCitationStyle resolves to a bundled .csl committed in 10-00; CS corrected apa→ieee per PRD §8. |
| T-10-03-05 | Tampering | malformed injected Zotero item | mitigate | toCandidate drops any item missing id/title/authors (returns null, filtered out); search() wraps the client call in try/catch and returns [] on any throw — a hostile/broken client cannot crash research or emit an invalid SourceCandidate. |
| T-10-03-SC | Tampering | npm/pip/cargo installs | mitigate | NO installs this phase. RESEARCH Package Legitimacy Audit [OK]; no [ASSUMED]/[SUS] packages → no install checkpoint. |
</threat_model>

<verification>
- `npx eslint bin/lib/schemas/source-candidate.ts bin/lib/sources/zotero-mcp.ts bin/lib/sources/index.ts bin/lib/doctor/probes/zotero-mcp-presence.ts` — no http/citation-js imports; chokepoints intact.
- `node --import tsx --test tests/sources/zotero-mcp.test.ts tests/doctor-probes.test.ts tests/disciplines-schema.test.ts` — all GREEN, including the present+authenticated injected-client normalization test (the CANONICAL GATE PREDICATE lets the injected-client path reach normalization on CI).
- disciplines.json parses as JSON; no defaultCitationStyle points at a missing style file.
- `npm run check` green except the intake-clarifier re-pin + tier-contract parity owned by 10-04.
</verification>

<success_criteria>
- zotero-mcp adapter registered; present(-or-injected)+authenticated+client → pulls + normalizes to SourceCandidate[] (tested via injected fake client on CI through the canonical gate); absent+no-client/unauth/no-client → [] (never throws).
- SourceCandidate union carries a 'zotero-mcp' variant so normalized candidates validate.
- Doctor probe tri-state with key-value never leaked.
- disciplines.json carries all 6 PRD §8 fields; CS defaults to IEEE.
- No 17th verb introduced.
</success_criteria>

<output>
Create `.planning/phases/10-discipline-citation-style-breadth-zotero-mcp/10-03-SUMMARY.md` when done.
</output>
</content>
</output>
