---
phase: 10-discipline-citation-style-breadth-zotero-mcp
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - templates/citation-styles/mla.csl
  - templates/citation-styles/chicago-notes-bib.csl
  - templates/citation-styles/chicago-author-date.csl
  - templates/citation-styles/ieee.csl
  - templates/citation-styles/ama.csl
  - templates/citation-styles/vancouver.csl
  - templates/citation-styles/harvard.csl
  - tests/ris-write.test.ts
  - tests/sources/zotero-mcp.test.ts
  - tests/disciplines-schema.test.ts
  - tests/citation-render.test.ts
autonomous: true
requirements: [CITE-02, CITE-03, CITE-05, RSCH-06]
must_haves:
  truths:
    - "All 7 new bundled CSL files exist on disk and are valid CSL XML (start with <?xml or <style or a CSL comment), never a 404 HTML page"
    - "RED test files exist for RIS export, the Zotero adapter, and the discipline-schema completion, failing or skipping cleanly until Wave 1 implements them"
    - "tests/citation-render.test.ts asserts the 7 new .csl files exist and parametrizes a render test per style (skip-guarded until citations.ts renderStyle ships)"
  artifacts:
    - path: "templates/citation-styles/ieee.csl"
      provides: "IEEE bundled CSL template (offline render asset)"
      contains: "<style"
    - path: "templates/citation-styles/vancouver.csl"
      provides: "Vancouver bundled CSL template"
      contains: "<style"
    - path: "tests/ris-write.test.ts"
      provides: "CITE-05 RIS output contract RED suite"
      min_lines: 30
    - path: "tests/sources/zotero-mcp.test.ts"
      provides: "RSCH-06 absence-non-breaking degrade RED suite"
      min_lines: 25
    - path: "tests/disciplines-schema.test.ts"
      provides: "Discipline preset 6-field completeness RED suite"
      min_lines: 30
  key_links:
    - from: "tests/citation-render.test.ts"
      to: "templates/citation-styles/*.csl"
      via: "existsSync existence assertions per style"
      pattern: "citation-styles.*\\$\\{style\\}\\.csl"
    - from: "tests/ris-write.test.ts"
      to: "bin/lib/ris-write.ts"
      via: "import { writeRis } (skip-guarded until Wave 1)"
      pattern: "ris-write"
---

<objective>
Wave 0 RED scaffold for Phase 10. Land all failing/skip-guarded tests AND the
seven committed CSL files BEFORE any implementation code, per the project's
RED→GREEN convention (every prior phase opened with a Wave-0 RED scaffold).

This plan does TWO things:
1. Procures and commits the 7 new CSL style files into `templates/citation-styles/`
   from the official CSL `styles-distribution` repo (BUILD-TIME network fetch;
   the files become committed OFFLINE assets — never fetched at render time).
2. Creates the three new RED test files (`ris-write`, `zotero-mcp`, `disciplines-schema`)
   and extends `citation-render.test.ts` with per-style render + existence assertions.

Purpose: Establish the failing contract so Wave-1 implementers code against a fixed
target. The CSL files must land in Wave 0 because Wave-1 code reads them via
`readFileSync` — code referencing absent files would fail (RESEARCH Pitfall 7).
Output: 7 .csl assets + 3 new test files + 1 extended test file.

NO new npm deps — `citation-js@0.7.22` already bundles `@citation-js/plugin-csl` +
`@citation-js/plugin-ris`. Do NOT run `npm install` for any package in this phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-discipline-citation-style-breadth-zotero-mcp/10-RESEARCH.md
@.planning/phases/10-discipline-citation-style-breadth-zotero-mcp/10-PATTERNS.md

<interfaces>
<!-- Existing analogs the new test files mirror. Use these directly — no exploration needed. -->

Existing CSL asset (apa.csl) — the bundled-file pattern, already shipped Phase 3:
  templates/citation-styles/apa.csl  (committed XML, read via readFileSync at render time)
  package.json `files` array ALREADY includes "templates/" — NO package.json change needed.

Existing RED-by-skip scaffold pattern (tests/sources/crossref.test.ts):
  const ADAPTER = 'crossref';
  const adapterPath = new URL(`../../bin/lib/sources/${ADAPTER}.ts`, import.meta.url);
  test('production adapter exists', () => assert.ok(existsSync(adapterPath), 'MISSING: ...'));
  const skip = !existsSync(adapterPath);
  test('behavioral', { skip }, async () => { ... });

Existing SourceCandidate fixtures (tests/bibtex-write.test.ts lines 30-69) — the SAME
3 fixtures (Vaswani/He/Doe, each with a DOI) work verbatim for tests/ris-write.test.ts.
SourceCandidate type: import type { SourceCandidate } from '../bin/lib/schemas/source-candidate.js'.

Existing citation-render test (tests/citation-render.test.ts) — the renderApa behavioral
test (lines 39-64) is the per-style template; fixtureBibPath is
'../tests/fixtures/known-good-fixture/CITATIONS.bib' (already exists from Phase 3).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Procure and commit the 7 CSL style files</name>
  <files>templates/citation-styles/mla.csl, templates/citation-styles/chicago-notes-bib.csl, templates/citation-styles/chicago-author-date.csl, templates/citation-styles/ieee.csl, templates/citation-styles/ama.csl, templates/citation-styles/vancouver.csl, templates/citation-styles/harvard.csl</files>
  <read_first>
    - templates/citation-styles/apa.csl (the existing bundled-file precedent; note its pensmith comment header)
    - .planning/phases/10-discipline-citation-style-breadth-zotero-mcp/10-RESEARCH.md (§Standard Stack > New Artifacts; §Common Pitfalls 2/3/5)
  </read_first>
  <action>
    Fetch all 7 files from the official CSL `styles-distribution` repo (the resolved
    independent-style distribution — CONFIRMED HTTP 200 for all 7 at planning time).
    Use ONE consistent source repo to avoid dependent-style 404s. The base URL is
    `https://raw.githubusercontent.com/citation-style-language/styles-distribution/master/`.

    CORRECTION TO RESEARCH/PATTERNS URLs (verified at planning time): the RESEARCH.md
    and PATTERNS.md tables list `styles/master/vancouver.csl` and several `styles/master/*`
    URLs. `styles/master/vancouver.csl` returns 404 (vancouver is a DEPENDENT style in the
    main repo). Use the `styles-distribution` repo for ALL 7 files — it carries the
    fully-resolved independent styles and returns 200 for every file below.

    Download each upstream filename to its pensmith target filename (rename on save):
      modern-language-association.csl     -> templates/citation-styles/mla.csl
      chicago-fullnote-bibliography.csl   -> templates/citation-styles/chicago-notes-bib.csl   (NOT chicago-note-bibliography — Pitfall 2; this is CMOS 17th full note)
      chicago-author-date.csl             -> templates/citation-styles/chicago-author-date.csl  (NOW CMOS 18th — Pitfall 3; comments must say 18th, not 17th)
      ieee.csl                            -> templates/citation-styles/ieee.csl
      american-medical-association.csl    -> templates/citation-styles/ama.csl
      vancouver.csl                       -> templates/citation-styles/vancouver.csl
      harvard-cite-them-right.csl         -> templates/citation-styles/harvard.csl

    Fetch via the Bash tool with curl (this is a one-time BUILD-TIME procurement; it does
    NOT go through bin/lib/http.ts because it is not runtime code — the .csl files are
    committed offline assets, never fetched at render time).

    After each download, VALIDATE the file is real CSL XML, not a 404 page: the file MUST
    start with `<?xml` or `<style` (Pitfall 2 — a GitHub 404 returns `<!DOCTYPE html>` or
    `404: Not Found`). Reject and re-fetch any file that does not validate.

    CC-BY-SA attribution: each CSL file already carries its license + author in its
    `<info>/<rights>` and `<author>` elements — do NOT strip them. Do NOT prepend a
    pensmith comment header to these files (unlike apa.csl) — keeping the upstream bytes
    intact preserves attribution provenance and makes future re-procurement diffable.
    No README change and no package.json change needed (`templates/` is already in files[]).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=['mla','chicago-notes-bib','chicago-author-date','ieee','ama','vancouver','harvard'].map(s=>'templates/citation-styles/'+s+'.csl');let ok=true;for(const f of files){if(!fs.existsSync(f)){console.error('MISSING '+f);ok=false;continue;}const h=fs.readFileSync(f,'utf8').slice(0,200).trim();if(!(h.startsWith('<?xml')||h.startsWith('<style')||h.startsWith('<!--'))){console.error('NOT-CSL '+f+': '+h.slice(0,60));ok=false;}if(/<!DOCTYPE html|404: Not Found/.test(h)){console.error('404-PAGE '+f);ok=false;}}process.exit(ok?0:1);"</automated>
  </verify>
  <done>All 7 .csl files exist in templates/citation-styles/, each validates as CSL XML (no 404 HTML), and the automated check exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Create the 3 new RED test files (ris-write, zotero-mcp, disciplines-schema)</name>
  <files>tests/ris-write.test.ts, tests/sources/zotero-mcp.test.ts, tests/disciplines-schema.test.ts</files>
  <read_first>
    - tests/bibtex-write.test.ts (the EXACT structural template for ris-write.test.ts — copy the 3 fixtures verbatim)
    - tests/sources/crossref.test.ts (the RED-by-skip adapter scaffold for zotero-mcp.test.ts)
    - .planning/phases/10-discipline-citation-style-breadth-zotero-mcp/10-PATTERNS.md (tests/ris-write.test.ts, tests/sources/zotero-mcp.test.ts, tests/disciplines-schema.test.ts blocks — full code shown)
  </read_first>
  <action>
    Create three RED-by-skip test files following the project's Wave-0 convention
    (existence assertion fires RED immediately; behavioral tests skip-guard on
    existsSync until Wave 1 ships the module).

    tests/ris-write.test.ts (CITE-05): Mirror tests/bibtex-write.test.ts. Copy the 3
    SourceCandidate fixtures verbatim (Vaswani/He/Doe). Import `{ writeRis }` from
    '../bin/lib/ris-write.js' but skip-guard every behavioral test on
    `existsSync(new URL('../bin/lib/ris-write.ts', import.meta.url))`. Include an
    always-on existence assertion: `assert.ok(existsSync(risWritePath), 'MISSING: bin/lib/ris-write.ts — Plan 10-02 must create')`.
    Behavioral assertions (skip-guarded): writeRis(fixtures,target) produces output with
    `/TY\s+-\s+JOUR/` (RIS2001 JOUR tag — Pitfall 4 spec:'new'), `/ER\s+-/` (terminator),
    and exactly fixtures.length `^TY\s+-` tags; writeRis([],target) writes a zero-length file.

    tests/sources/zotero-mcp.test.ts (RSCH-06): Mirror tests/sources/crossref.test.ts.
    ADAPTER='zotero-mcp'. Always-on existence assertion for bin/lib/sources/zotero-mcp.ts.
    Skip-guarded behavioral tests: (a) search('test query') NEVER throws and returns an
    Array (the absence-non-breaking degrade contract — use assert.doesNotReject); (b)
    `'zotero-mcp' in sources` after importing '../../bin/lib/sources/index.js'.
    NOTE the relative path depth: this file is in tests/sources/, so the adapter URL is
    `../../bin/lib/sources/zotero-mcp.ts` and index is `../../bin/lib/sources/index.js`.

    tests/disciplines-schema.test.ts (RSCH-06/CITE-02 discipline→style coverage): Assert
    templates/presets/disciplines.json exists; assert EVERY entry contains all 6 PRD §8
    fields [defaultTone, defaultCitationStyle, sourcePreference, sectioningConvention,
    counterargDefault, densityTarget]; assert `computer-science.defaultCitationStyle === 'ieee'`
    (PRD §8 fix, currently 'apa'); assert every entry's densityTarget has low/center/high keys.
    These assertions are NOT skip-guarded — they fire RED now because Plan 10-03 has not yet
    expanded disciplines.json (it currently has only 2 fields per entry).

    Do NOT place any production implementation in these files — they are test-only.
  </action>
  <verify>
    <automated>node --import tsx --test tests/ris-write.test.ts tests/sources/zotero-mcp.test.ts tests/disciplines-schema.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All 3 test files exist and run; existence assertions for ris-write.ts and zotero-mcp.ts fire RED (those modules do not exist yet), disciplines-schema field assertions fire RED (6 fields not yet present), and behavioral tests for the unbuilt modules skip cleanly (no hard crash, no syntax errors).</done>
</task>

<task type="auto">
  <name>Task 3: Extend citation-render.test.ts with per-style existence + render assertions</name>
  <files>tests/citation-render.test.ts</files>
  <read_first>
    - tests/citation-render.test.ts (the existing renderApa behavioral test, lines 39-64 — the per-style template)
    - .planning/phases/10-discipline-citation-style-breadth-zotero-mcp/10-PATTERNS.md (tests/citation-render.test.ts extend block — full loop code shown)
  </read_first>
  <action>
    Extend (do NOT rewrite) tests/citation-render.test.ts. Append a parametrized block
    over the 7 new styles: const stylesToTest = ['mla','chicago-notes-bib',
    'chicago-author-date','ieee','ama','vancouver','harvard'] as const.

    For each style emit TWO tests:
    1. Always-on existence: `templates/citation-styles/${style}.csl exists (CITE-02/03)` —
       assert.ok(existsSync(cslPath), 'MISSING: ...'). These PASS now (Task 1 committed the
       files) but stay as regression guards.
    2. Skip-guarded render: `renderStyle('${style}') produces non-empty bibliography (CITE-02/03)`.
       Skip guard = the existing `shouldSkip` (citations.ts/apa.csl/fixture present) OR
       !existsSync(cslPath). Body: import { parseBib, renderStyle } from '../bin/lib/citations.js';
       parse fixtureBibPath; assert renderStyle(entries, style) returns a non-empty string.
       This SKIPS now because citations.ts does not yet export renderStyle (Plan 10-01 ships it),
       then GREENS once Plan 10-01 lands. Guard the import so a missing renderStyle export
       skips rather than throws (the existing shouldSkip predicate covers file presence; add
       a try/catch or feature-detect on the imported renderStyle symbol if needed to keep the
       suite GREEN — no hard failures while renderStyle is absent).

    Keep the existing renderApa tests UNCHANGED — they are the Wave-0 contract.
  </action>
  <verify>
    <automated>node --import tsx --test tests/citation-render.test.ts 2>&1 | tail -25</automated>
  </verify>
  <done>tests/citation-render.test.ts runs with the 7 per-style existence assertions GREEN (files committed in Task 1) and the 7 render tests SKIPPED (renderStyle not yet shipped); the original renderApa tests remain unchanged and pass; zero hard failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSL upstream repo → committed asset | Build-time network fetch of attacker-or-typo-influenced .csl content that becomes a committed offline render asset |
| .csl file → citeproc render (Wave 1) | Malformed CSL XML reaching the citation-js template registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-00-01 | Tampering | CSL file procurement (curl from GitHub) | mitigate | Validate each downloaded file starts with `<?xml`/`<style` and is NOT `<!DOCTYPE html>`/`404: Not Found` (Task 1 automated check). A 404 HTML page or typo'd URL is rejected, not committed. |
| T-10-00-02 | Tampering | bundled .csl content | mitigate | Files are fetched once at build time from the official `citation-style-language/styles-distribution` repo (provenance recorded in this plan) and COMMITTED; render is offline `readFileSync` only — no render-time network. CC-BY-SA attribution preserved in each file's `<info>/<rights>` (upstream bytes kept intact). |
| T-10-00-03 | Spoofing | upstream URL substitution | accept | Procurement uses hard-coded official CSL repo URLs over HTTPS; risk of a compromised GitHub raw endpoint is accepted as low for a public, widely-mirrored CC-BY-SA corpus. |
| T-10-00-SC | Tampering | npm/pip/cargo installs | mitigate | NO package installs in this phase (citation-js bundles plugin-csl + plugin-ris). RESEARCH §Package Legitimacy Audit confirms [OK] for all three transitive deps; no [ASSUMED]/[SUS] packages, so no install checkpoint required. |
</threat_model>

<verification>
- `node --import tsx --test tests/ris-write.test.ts tests/sources/zotero-mcp.test.ts tests/disciplines-schema.test.ts tests/citation-render.test.ts` runs cleanly (RED/skip pattern as designed, no syntax errors, no hard crashes on unbuilt modules).
- All 7 .csl files validate as CSL XML via the Task 1 automated check.
- `npm test` full suite stays GREEN-with-expected-REDs (the new RED existence assertions for ris-write.ts/zotero-mcp.ts and the disciplines 6-field assertions are the intended failures; everything else green).
</verification>

<success_criteria>
- 7 new CSL files committed to templates/citation-styles/, each valid CSL XML.
- tests/ris-write.test.ts, tests/sources/zotero-mcp.test.ts, tests/disciplines-schema.test.ts exist and encode the Wave-1 contracts.
- tests/citation-render.test.ts extended with 7 existence (GREEN) + 7 render (SKIP) tests.
- No new npm dependency added. No package.json change.
</success_criteria>

<output>
Create `.planning/phases/10-discipline-citation-style-breadth-zotero-mcp/10-00-SUMMARY.md` when done.
</output>
