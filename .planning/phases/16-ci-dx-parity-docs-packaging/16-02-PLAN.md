---
phase: 16-ci-dx-parity-docs-packaging
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - package.json
  - .github/workflows/ci.yml
  - bin/lib/http-mock.ts
  - tests/repo-files.test.ts
autonomous: true
requirements: [CI-01, CI-02, CI-03, DOCS-03]
must_haves:
  truths:
    - "`npm run check` runs prebuild FIRST so a green local run implies a green CI run"
    - "CI fails when `git status --porcelain` is non-empty after build (stale-derived-file drift)"
    - "CI runs the suite under non-TTY stdin and enforces the c8 coverage gate"
    - "nock lives in devDependencies; http-mock.ts loads without nock present (lazy import)"
  artifacts:
    - path: "package.json"
      provides: "prebuild-first check, test:coverage script, nock in devDependencies"
      contains: "npm run prebuild"
    - path: ".github/workflows/ci.yml"
      provides: "porcelain-clean gate + non-TTY coverage step"
      contains: "git status --porcelain"
    - path: "bin/lib/http-mock.ts"
      provides: "lazy nock import (no top-level nock dependency at module load)"
    - path: "tests/repo-files.test.ts"
      provides: "CI-01 check-order assertion + DOCS-03 nock-placement assertions"
  key_links:
    - from: "package.json scripts.check"
      to: ".github/workflows/ci.yml step order"
      via: "both run prebuild→lint→tsc→build→tier-contract→test→validate"
      pattern: "npm run prebuild"
    - from: ".c8rc.json"
      to: "package.json test:coverage (c8)"
      via: "c8 auto-discovers .c8rc.json"
      pattern: "c8 node scripts/run-tests.mjs"
    - from: "bin/lib/http-mock.ts loadCassettes/clearCassettes/recordCassettes/finalizeRecording"
      to: "nock (devDependency)"
      via: "await import('nock') inside the function bodies only"
      pattern: "await import\\('nock'\\)"
---

<objective>
Land the CI/build-tooling parity and packaging cleanup: make `npm run check`
prebuild-first (CI-01), add the porcelain-clean gate + non-TTY coverage step to
ci.yml (CI-02 + CI-03), lazy-import nock inside http-mock.ts and move nock to
devDependencies (DOCS-03), and add the CI-01 + DOCS-03 structural assertions to
repo-files.test.ts. This plan is the SOLE owner of package.json, ci.yml,
http-mock.ts, and (in this wave) repo-files.test.ts — no parallel plan touches
any of them.

Purpose: Close the "local==CI" gap (the v0.1.0 ship-break class) and ship a
production-safe package (nock is dev-only; the cassette reader path the 7
adapters use loads without it).
Output: edited package.json, ci.yml, http-mock.ts, repo-files.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/16-ci-dx-parity-docs-packaging/16-RESEARCH.md
@.planning/phases/16-ci-dx-parity-docs-packaging/16-PATTERNS.md

@package.json
@.github/workflows/ci.yml
@bin/lib/http-mock.ts
@tests/repo-files.test.ts

<interfaces>
<!-- Current package.json scripts (from file read) — the exact strings to transform: -->
  "check": "npm run lint && npm run typecheck && npm run build && npm run test:tier-contract && npm test && npm run validate:manifests"
  "coverage": "c8 node scripts/run-tests.mjs"          // → rename key to "test:coverage"
  dependencies.nock: "^14"                              // → move to devDependencies

<!-- ci.yml: the last named step today is "Validate plugin manifests" (run: node scripts/validate-plugin-manifest.cjs). -->
<!-- The existing `- run: npm test` step (line ~53) is what CI-03 replaces with the coverage step. -->
<!-- Precedent for shell: bash on the matrix: the "Verify macos runner is arm64" step (lines 25-30). -->

<!-- http-mock.ts: top-level `import nock from 'nock'` at line 53. nock-USING functions: -->
<!--   loadCassettes (205, sync void), clearCassettes (229, sync void), -->
<!--   recordCassettes (255, async), finalizeRecording (288, sync void). -->
<!-- nock-FREE production functions (DO NOT change): isOfflineMode, loadCassetteFile, loadCassetteDir. -->
<!-- VERIFIED: the four nock-using functions have ZERO production callers (grep) — only docs + a -->
<!-- cassette-refresh.yml COMMENT reference. So changing their sync→async signatures has no caller ripple. -->

<!-- repo-files.test.ts: 'package.json contract' test (lines 45-65) reads `scripts`, `deps`, `dev`. -->
<!-- WN-3 hash-pin template lives at lines 174-180 (doctor-output.md pin — NOT touched in this plan). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: package.json — prebuild-first check (CI-01), test:coverage rename (CI-03), nock → devDependencies (DOCS-03)</name>
  <files>package.json</files>
  <read_first>
    - package.json scripts block (lines 15-28), dependencies (43-61), devDependencies (62-71).
    - 16-RESEARCH.md "CI-01" (the exact final `check` string) + "Open Question 2" (replace npm test with test:coverage — recommend replace) + "DOCS-03 scope = nock → devDependencies only".
    - 16-PATTERNS.md "package.json — CI-01 + CI-03 + DOCS-03".
  </read_first>
  <action>
Make three edits to package.json, all in this one plan (package.json is touched
by no other Phase-16 plan — no parallel conflict):
  1. CI-01: prepend `npm run prebuild && ` to the `check` script so the final
     value is exactly:
     `npm run prebuild && npm run lint && npm run typecheck && npm run build && npm run test:tier-contract && npm test && npm run validate:manifests`.
     This matches the ci.yml step order (prebuild→lint→tsc→build→tier-contract→test→validate).
     Per RESEARCH Pitfall 6, the implicit second prebuild via build's lifecycle
     hook is harmless/idempotent — accept the double-run; do NOT rename the
     prebuild script.
  2. CI-03: rename the `coverage` script key to `test:coverage` (same value
     `c8 node scripts/run-tests.mjs`). Delete the `coverage` key; add
     `test:coverage`. c8 auto-discovers the `.c8rc.json` created in Plan 01.
  3. DOCS-03: remove `"nock": "^14"` from `dependencies` and add `"nock": "^14"`
     to `devDependencies`. http-mock.ts is PRODUCTION (RESEARCH verdict: imported
     by 7 adapters + honesty/plagiarism/freshness) so it stays in dist; only the
     nock dep moves — Task 3 makes that safe via lazy import.
Do not touch the `files` array (http-mock.ts ships in dist via `dist/`).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.scripts.check.startsWith('npm run prebuild')) throw new Error('CI-01'); if(!p.scripts['test:coverage']) throw new Error('CI-03 rename'); if(p.scripts.coverage) throw new Error('coverage key not removed'); if(p.dependencies.nock) throw new Error('DOCS-03 dep'); if(!p.devDependencies.nock) throw new Error('DOCS-03 devDep')"</automated>
  </verify>
  <acceptance_criteria>check starts with `npm run prebuild`; `test:coverage` exists and `coverage` is gone; nock is in devDependencies and absent from dependencies.</acceptance_criteria>
  <done>All three edits land; the `node -e` assertion passes; `npm run check` (after Task 3) is runnable prebuild-first.</done>
</task>

<task type="auto">
  <name>Task 2: ci.yml — porcelain-clean gate (CI-02) + non-TTY coverage step (CI-03)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (full — note the existing `- run: npm test` step at line ~53 and the final "Validate plugin manifests" step at lines 55-56, plus the `shell: bash` precedent at lines 25-30).
    - 16-RESEARCH.md "CI-02: Porcelain-Clean Gate" + "CI-03: ci.yml step for coverage" + Pitfall 4 (`< /dev/null` needs `shell: bash` on Windows).
    - 16-PATTERNS.md "ci.yml — CI-02 + CI-03" (exact YAML to append/replace).
  </read_first>
  <action>
Two edits to ci.yml (this plan is the sole owner of ci.yml — no parallel conflict):
  1. CI-03: REPLACE the existing `- run: npm test` step with a named coverage
     step that runs under non-TTY stdin (per RESEARCH Open-Q2 — single run, not
     test-twice):
       - name: Test suite + coverage gate (non-TTY stdin — CI-03)
         shell: bash
         run: npm run test:coverage < /dev/null
     `shell: bash` is REQUIRED on all 3 OSes for the `< /dev/null` redirect to
     work on Windows (Git Bash) — Pitfall 4. The `< /dev/null` detaches stdin so
     `process.stdin.isTTY` is false, exercising the prompts short-circuit path
     (the v0.1.0 unref'd-timer hang class). c8 enforces the `.c8rc.json` gate.
  2. CI-02: APPEND after the "Validate plugin manifests" step a porcelain gate:
       - name: Assert working tree is clean after build (stale-derived-file guard — CI-02)
         shell: bash
         run: |
           status=$(git status --porcelain)
           if [ -n "$status" ]; then
             echo "FAIL: build/prebuild produced untracked or modified files:"
             echo "$status"
             exit 1
           fi
           echo "OK: working tree is clean."
     The gitignore already covers version.generated.ts + verbs.json + dist/
     (RESEARCH confirmed) so the gate fails ONLY on genuine drift. Keep it in the
     existing 3-OS matrix (do not add a separate job). Use the exact YAML from
     16-PATTERNS.md so indentation matches the existing steps.
  </action>
  <verify>
    <automated>node -e "const y=require('node:fs').readFileSync('.github/workflows/ci.yml','utf8'); if(!/git status --porcelain/.test(y)) throw new Error('CI-02 missing'); if(!/test:coverage < \/dev\/null/.test(y)) throw new Error('CI-03 missing'); if(/^\s*- run: npm test\s*$/m.test(y)) throw new Error('old npm test step not replaced')"</automated>
  </verify>
  <acceptance_criteria>ci.yml contains the porcelain-clean step (fails on dirty tree, prints offending paths) and the non-TTY coverage step (`npm run test:coverage < /dev/null`, shell: bash); the bare `- run: npm test` step is gone.</acceptance_criteria>
  <done>Both steps present with `shell: bash`; the old npm test step replaced; YAML indentation matches existing steps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: bin/lib/http-mock.ts — lazy nock import (DOCS-03) + repo-files.test.ts CI-01/DOCS-03 assertions</name>
  <files>bin/lib/http-mock.ts, tests/repo-files.test.ts</files>
  <behavior>
    - http-mock.ts has NO top-level `import nock from 'nock'`; nock is referenced only via `await import('nock')` inside loadCassettes/clearCassettes/recordCassettes/finalizeRecording.
    - isOfflineMode/loadCassetteFile/loadCassetteDir remain synchronous and nock-free (the 7-adapter path loads without nock).
    - tests/http-mock.test.ts (Plan 01 RED-by-skip) flips GREEN: no top-level nock import + production-facing functions callable.
    - repo-files.test.ts asserts `scripts.check` starts with `npm run prebuild` (CI-01) and nock is in devDependencies / not in dependencies (DOCS-03).
  </behavior>
  <read_first>
    - bin/lib/http-mock.ts (full — the four nock-using functions at lines 205/229/255/288 and the `nock.Body` type use at line 219).
    - bin/lib/prompts.ts lazy-import analog (`const { askClack } = await import('./prompts/clack.js')`) and bin/lib/pdf-text.ts conditional `await import('./pymupdf-shellout.js')` — the established lazy-import pattern.
    - 16-PATTERNS.md "bin/lib/http-mock.ts — DOCS-03 lazy nock import" (the per-function transform + the `nock.Body` typing note).
    - tests/repo-files.test.ts 'package.json contract' test (lines 45-65) — where to add the two assertions.
    - tests/http-mock.test.ts (Plan 01) — the contract this task must satisfy.
  </read_first>
  <action>
Edit http-mock.ts to lazy-import nock (this plan owns http-mock.ts):
  1. Remove the top-level `import nock from 'nock'` (line 53).
  2. Inside `loadCassettes`, `clearCassettes`, `recordCassettes`, and
     `finalizeRecording`, add `const { default: nock } = await import('nock')`
     before the first nock reference in each. Make `loadCassettes`,
     `clearCassettes`, and `finalizeRecording` return `Promise<void>` (async) —
     VERIFIED these four have ZERO production callers (only docs +
     cassette-refresh.yml comment), so the sync→async change has no caller
     ripple. recordCassettes is already async.
  3. Replace the `nock.Body` type at the `.reply()` call with `unknown` (or a
     locally-derived type) so removing the top-level import does not break tsc —
     per the PATTERNS "Nonce typing note". Keep the docblocks; update the
     lifecycle docblock signatures if they name a sync return.
  4. Do NOT touch isOfflineMode/loadCassetteFile/loadCassetteDir/Cassette/
     SENSITIVE_HEADERS — they are nock-free and are the production-facing surface.
Then add two assertions to repo-files.test.ts 'package.json contract' test:
  - CI-01: `assert.ok(scripts?.['check']?.startsWith('npm run prebuild'), 'CI-01: check must start with npm run prebuild')`.
  - DOCS-03: `assert.ok(!deps?.['nock'], 'DOCS-03: nock must NOT be in dependencies')` and
    `assert.ok(dev?.['nock'], 'DOCS-03: nock must be in devDependencies')` (deps/dev are already read in that test).
Do NOT edit the README/PRIVACY/CONTRIBUTING test or the doctor-output.md
hash-pin in this plan — those belong to Plans 03/04 (file-ownership serialization
of repo-files.test.ts across waves).
  </action>
  <verify>
    <automated>node --import tsx --test tests/http-mock.test.ts tests/repo-files.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>http-mock.ts has no top-level nock import; the four nock-using functions lazy-import it; tsc --noEmit clean; tests/http-mock.test.ts now GREEN (guard opened); repo-files.test.ts CI-01 + DOCS-03 assertions pass.</acceptance_criteria>
  <done>Lazy import landed; production-facing functions still callable without nock at module load; repo-files asserts CI-01 + DOCS-03; tsc + the two test files pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → CI | the check-order + porcelain + coverage gates are the contract that "green local ⇒ green CI"; a false-passing gate defeats the whole phase |
| production npm install → nock (dev-only) | a prod install (`npm install -g pensmith` / `npm ci --omit=dev`) has no nock; http-mock.ts must not throw at module load |
| build/prebuild → git working tree | prebuild emits gitignored derived files; the porcelain gate proves no NON-ignored drift escaped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-CI02 | Tampering | porcelain-clean gate | mitigate | The step does `[ -n "$status" ] && exit 1` and prints the offending paths — it actually fails on drift (not a no-op echo). RESEARCH confirmed gitignore covers all prebuild/build outputs so the gate fails only on genuine drift, not false positives |
| T-16-CI03 | Tampering | coverage gate false-pass | mitigate | `.c8rc.json` (Plan 01) sets `check-coverage:true` + thresholds; c8 exits non-zero below threshold. The `< /dev/null` non-TTY run exercises the isTTY short-circuit so a TTY-only hang cannot mask a regression |
| T-16-DEP | Tampering / supply-chain | nock moved to devDeps while http-mock.ts is production | mitigate | Lazy `await import('nock')` inside the four nock-using functions (which have no production callers) means module load never imports nock; the production-facing reader functions stay nock-free. tests/http-mock.test.ts (Plan 01) is the CI-enforced fail-safe |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed (RESEARCH Package Legitimacy Audit: nock is a MOVE not an install; c8 already present). No `[ASSUMED]`/`[SUS]` entries; no install checkpoint required |
</threat_model>

<verification>
- `npm run check` runs prebuild first (CI-01) and completes green locally.
- `git status --porcelain` is empty after `npm run check` (the CI-02 gate, runnable locally).
- `npm run test:coverage` (with `.c8rc.json`) meets the 85/72/82/85 thresholds.
- `node --import tsx --test tests/http-mock.test.ts` is GREEN (no top-level nock; production-facing functions callable).
- `node --import tsx --test tests/repo-files.test.ts` passes the CI-01 + DOCS-03 assertions.
- `npx tsc --noEmit` clean after the nock-import change.
- No edits to README.md, PRIVACY.md, CONTRIBUTING.md, the doctor-output.md pin, or any workflows/*.md (owned by Plans 03/04).
</verification>

<success_criteria>
- package.json: prebuild-first check, test:coverage script, nock in devDependencies.
- ci.yml: porcelain gate + non-TTY coverage step (shell: bash), old `npm test` step replaced.
- http-mock.ts: lazy nock import; tsc clean; production-facing functions load without nock.
- repo-files.test.ts: CI-01 + DOCS-03 assertions pass.
- Full check green + porcelain clean locally.
</success_criteria>

<output>
Create `.planning/phases/16-ci-dx-parity-docs-packaging/16-02-SUMMARY.md` when done.
</output>
