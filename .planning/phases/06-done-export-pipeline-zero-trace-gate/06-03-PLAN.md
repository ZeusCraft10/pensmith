---
phase: 06-done-export-pipeline-zero-trace-gate
plan: 03
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - bin/lib/honesty.ts
  - tests/repo-files.test.ts
autonomous: true
requirements: [DONE-04, DONE-05]

must_haves:
  truths:
    - "A GPTZero honesty score can be computed before AND after humanize; an absent GPTZERO_API_KEY (or offline) skips cleanly returning null, never crashing"
    - "The user-facing framing ('improves prose, not evades detection') is rendered VERBATIM from references/honesty-framing.md (the locked copy) — never inlined in code"
    - "All HTTP goes through bin/lib/http.ts with source 'generic' + noCache true; the GPTZERO_API_KEY value never reaches the session log or cost ledger (presence-check only at the call boundary)"
    - "The backend is pluggable (DONE-05): a HonestyBackend strategy selected by a honestyBackend config value; GPTZero ships, Originality/Sapling are not-implemented stubs that return null/throw-not-implemented rather than fabricating a score"
  artifacts:
    - path: "bin/lib/honesty.ts"
      provides: "scoreHonesty + renderHonestyReport + HonestyBackend interface + GPTZero backend (DONE-04/05)"
      contains: "scoreHonesty"
      min_lines: 90
  key_links:
    - from: "bin/lib/honesty.ts"
      to: "references/honesty-framing.md"
      via: "findPkgRoot walk-up + readFileSync + section/blockquote parse (same as http.ts loadWarnString)"
      pattern: "honesty-framing"
    - from: "bin/lib/honesty.ts"
      to: "bin/lib/http.ts"
      via: "httpFetch(gptzeroUrl, { method: 'POST', source: 'generic', noCache: true })"
      pattern: "api.gptzero.me"
---

<objective>
Implement `bin/lib/honesty.ts` — the detection-aware honesty score (DONE-04) with a pluggable backend (DONE-05). It POSTs the draft text to GPTZero's `/v2/predict/text` through the http.ts chokepoint, parses the AI-probability defensively, and renders a before/after report whose honest-framing note is read VERBATIM from the locked `references/honesty-framing.md`. Absent key / offline → clean null skip, never a crash. Backend is selected via a `honestyBackend` config strategy; Originality/Sapling are not-implemented stubs.

Purpose: This is the non-negotiable honest-framing surface (CLAUDE.md: "GPTZero score is transparency, never 'undetectable'; the humanizer 'improves prose,' it does not 'evade detection.'"). The framing copy is locked in 06-01 and rendered verbatim here so drift is a CI failure. The module follows the verify/pass2.ts + http.ts patterns: PENSMITH_NO_LLM / key-absence guard, http.ts as sole network chokepoint, defensive response parse (unparseable → null), and the API-key-never-logged invariant (T-01-07 / runtime.ts getProviderApiKey precedent).
Output: One production module turning the RED tests/honesty.test.ts from Wave 0 GREEN, including the verbatim-framing assertion. Re-pins references/honesty-framing.md ONLY if the verbatim-render contract forces a copy adjustment (otherwise the 06-01 pin stands).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-done-export-pipeline-zero-trace-gate/06-RESEARCH.md
@.planning/phases/06-done-export-pipeline-zero-trace-gate/06-PATTERNS.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

From bin/lib/http.ts (SOLE HTTP call site; also the findPkgRoot + locked-copy read pattern to mirror):
```
export async function fetch(url: string, opts?: FetchOptions): Promise<HttpResponse>;  // import as httpFetch
export interface FetchOptions { method?: 'GET'|'POST'|'HEAD'; headers?: Record<string,string>; body?: string|Buffer; source?: HttpSource; noCache?: boolean; ... }
export interface HttpResponse { status: number; headers: Record<string,string>; body: string; cached: boolean; }
```
findPkgRoot + locked-copy read (http.ts lines 77-126 — COPY this shape for references/honesty-framing.md):
```
function findPkgRoot(start: string): string { /* walk up <=8 dirs until a dir has package.json; works in tsx (bin/lib/honesty.ts) AND post-build (dist/bin/lib/honesty.js) */ }
const PKG_ROOT = findPkgRoot(__dirname);
const FRAMING_FILE = path.join(PKG_ROOT, 'references', 'honesty-framing.md');
// __dirname via: const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename);
// parse: split on /\r?\n/, find the '## Note' heading, return the following '> ' blockquote text (same section-scan as http.ts loadWarnString lines 101-126)
```

From bin/lib/http-mock.ts (offline test seam):
```
export function isOfflineMode(): boolean;  // true unless PENSMITH_NETWORK_TESTS === '1'
export function loadCassetteFile(adapter: string, basename: string): Cassette[] | null;  // tests/fixtures/cassettes/gptzero/predict-text.json
export interface Cassette { scope: string; method: 'GET'|'POST'; path: string; status: number; response: unknown; ... }
```

KEY-ABSENCE / NO-LLM GUARD (verify/pass2.ts lines 215-219 — the canonical pattern):
```
const apiKey = process.env['GPTZERO_API_KEY'];
if (!apiKey) { process.stdout.write('pensmith: GPTZero API key not set — honesty score skipped.\n'); return null; }
// API-KEY-NEVER-LOGGED (T-01-07): the key value is passed ONLY into the request headers; it never enters a log payload, the cost ledger, or stdout. Use presence-check only for any banner.
```

GPTZero v2 (06-RESEARCH Pattern 3 + Code Examples lines 506-548):
- POST https://api.gptzero.me/v2/predict/text, headers { 'x-api-key': <key>, 'content-type': 'application/json' }, body { "document": <text> }, source 'generic', noCache true.
- Response shape (cassette in 06-01): { documents: [ { class_probabilities: { ai: number }, document_classification: 'HUMAN_ONLY'|'MIXED'|'AI_ONLY' } ] }.
- aiProbability = documents[0].class_probabilities.ai (0..1). Display percent = Math.round(prob*100). Unexpected/unparseable → return null (skip, never fabricate).

references/honesty-framing.md (locked in 06-01): "## Output format" (before/after blockquote lines with XX% placeholder) + "## Note" (the verbatim "improves prose ... does not ... undetectable" sentence). bin/lib/honesty.ts MUST read the note from this file — NOT inline it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the GPTZero backend + scoreHonesty (key-absence/offline skip-clean, defensive parse, key never logged)</name>
  <read_first>
    - bin/lib/verify/pass2.ts (key-absence guard lines 215-219; defensive response parse lines 162-189; the getProviderApiKey no-leak precedent)
    - bin/lib/http.ts (FetchOptions POST + source 'generic' + noCache; the header-allowlist comment — x-api-key must not be cached/logged)
    - bin/lib/http-mock.ts (isOfflineMode + loadCassetteFile for the gptzero cassette)
    - tests/fixtures/cassettes/gptzero/predict-text.json (the response object scoreHonesty parses → 0.82)
    - tests/honesty.test.ts (RED assertions: absent key → null; cassette → {0.82, AI_ONLY, gptzero}; pluggable backend)
    - .planning/phases/06-done-export-pipeline-zero-trace-gate/06-RESEARCH.md (Pattern 3 + Code Examples; Pitfall 4 GPTZero rate limits; Security Domain V8 API-key-never-logged lines 689/696)
  </read_first>
  <files>bin/lib/honesty.ts</files>
  <behavior>
    - scoreWithGptzero(text): GPTZERO_API_KEY absent → null (skip-clean, banner). Offline → read gptzero cassette, parse, return HonestyScore. Live → httpFetch POST with x-api-key header; status !== 200 → null; parse defensively; unparseable → null.
    - The cassette response (ai: 0.82, AI_ONLY) → { aiProbability: 0.82, classification: 'AI_ONLY', backend: 'gptzero' }.
    - Never throws; never logs the key value.
    - scoreHonesty(text, config?) delegates to the selected backend (Task 2).
  </behavior>
  <action>
    Create bin/lib/honesty.ts with a header comment: "All HTTP through bin/lib/http.ts — ESLint chokepoint enforced. Honest-framing copy read VERBATIM from references/honesty-framing.md (locked, hash-pinned). GPTZERO_API_KEY value never logged (T-01-07 presence-check only). Detection-aware honesty score (DONE-04) is transparency-only — never an undetectability claim (PROJECT.md non-negotiable)."

    Define and export:
      `export interface HonestyScore { aiProbability: number; classification: 'HUMAN_ONLY'|'MIXED'|'AI_ONLY'; backend: string; }`
      `export interface HonestyBackend { name: string; score(text: string): Promise<HonestyScore | null>; }`

    Implement the GPTZero backend (an object/class implementing HonestyBackend, name 'gptzero'):
      - Key-absence guard first: `const apiKey = process.env['GPTZERO_API_KEY']; if (!apiKey) { stdout banner; return null; }` — banner text is plain stdout, NEVER includes the key.
      - Offline branch (isOfflineMode()): loadCassetteFile('gptzero','predict-text'); parse the response object via a shared `parseGptzeroResponse(raw: unknown): HonestyScore | null` (try/catch, defensive — unexpected shape → null).
      - Live branch: `await httpFetch('https://api.gptzero.me/v2/predict/text', { method:'POST', source:'generic', noCache:true, headers:{ 'x-api-key': apiKey, 'content-type':'application/json' }, body: JSON.stringify({ document: text }) })`; status !== 200 → null; else parseGptzeroResponse(JSON.parse(resp.body)). Wrap in try/catch → null on any error (advisory skip, never crash; Pitfall 4 429 → null).
      - parseGptzeroResponse: read documents[0].class_probabilities.ai (?? null-guard) and document_classification (default 'MIXED'); aiProbability defaults to 0 only when the field is a number, else null result. The key value never enters any log/return payload.
  </action>
  <verify>
    <automated>node --import tsx --test tests/honesty.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - With GPTZERO_API_KEY deleted, scoreHonesty/the gptzero backend returns null (Wave-0 skip-clean assertion passes; env restored in test's finally).
    - With the gptzero cassette active and a key present, the backend returns { aiProbability: 0.82, classification: 'AI_ONLY', backend: 'gptzero' }.
    - The module never references the resolved key value in any stdout/return/log payload (presence-check only) — confirmed by a source scan in the honesty test (assert no `${apiKey}` interpolation into stdout).
    - No direct fetch/undici import — `npm run lint` passes.
    - `tsc --noEmit` passes.
  </acceptance_criteria>
  <done>GPTZero backend lands: skip-clean on absent key/offline-no-cassette, defensive parse, key never logged, advisory-never-throws. The before/after score capability (called twice by done.ts) is available.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement renderHonestyReport (verbatim framing from the locked copy) + pluggable backend selection (DONE-05)</name>
  <read_first>
    - bin/lib/http.ts (findPkgRoot lines 77-91 + loadWarnString section/blockquote parse lines 96-126 — the EXACT locked-copy read pattern to copy)
    - references/honesty-framing.md (the locked copy created in 06-01 — the '## Note' blockquote is what renderHonestyReport must emit verbatim)
    - tests/honesty.test.ts (RED assertions: report contains '82%' + '41%' AND the verbatim note from the framing file; DONE-05 backend selection)
    - .planning/phases/06-done-export-pipeline-zero-trace-gate/06-RESEARCH.md (Honesty Framing Render code example lines 584-606; Open Question 3 pluggable backends lines 744-747)
  </read_first>
  <files>bin/lib/honesty.ts</files>
  <behavior>
    - loadFramingNote(): reads references/honesty-framing.md (findPkgRoot walk-up, works in tsx + dist), extracts the '## Note' blockquote text verbatim; defensive fallback only if the file is unreadable (should never happen — references/ ships).
    - renderHonestyReport(before, after, backend): emits the before line (Math.round(before*100)%), the after line (Math.round(after*100)% OR 'N/A (humanizer not installed)' when after is null), then the verbatim note from loadFramingNote().
    - selectHonestyBackend(config?): returns the GPTZero backend by default; for honestyBackend 'originality'|'sapling' returns a not-implemented stub backend whose score() returns null (or throws a typed 'not implemented' — pick null for advisory-never-crash consistency); unknown value → default gptzero.
    - scoreHonesty(text, config?): resolves the backend via selectHonestyBackend(config) then calls backend.score(text).
  </behavior>
  <action>
    Add to bin/lib/honesty.ts:

    `findPkgRoot` + `PKG_ROOT` + `FRAMING_FILE` exactly as http.ts does (fileURLToPath(import.meta.url) → __dirname → walk up ≤8 dirs to package.json). Add `loadFramingNote(): string` mirroring http.ts loadWarnString: read FRAMING_FILE, split lines, scan for the '## Note' heading, return the following `> ` blockquote text trimmed (verbatim). Memoize like warnString. Defensive fallback string only on read failure.

    `export function renderHonestyReport(before: number, after: number | null, backend: string): string` per the 06-RESEARCH render example lines 591-606: build the before line `Pensmith honesty check (before humanize): reads as ${Math.round(before*100)}% AI-generated (${backend}).`, the after line (percent or the N/A variant when after === null), then append the verbatim note from loadFramingNote(). The note text MUST be the file's note, never an inlined literal.

    Pluggable backend (DONE-05): `export function selectHonestyBackend(config?: { honestyBackend?: string }): HonestyBackend`. Default → the gptzero backend from Task 1. For 'originality' / 'sapling' → return a stub HonestyBackend (name set accordingly) whose score() returns null with a one-line stdout banner ("pensmith: <backend> honesty backend not implemented — score skipped."). Unknown/undefined → gptzero. Then `export async function scoreHonesty(text: string, config?: { honestyBackend?: string }): Promise<HonestyScore | null>` resolves the backend and calls score(text).
  </action>
  <verify>
    <automated>node --import tsx --test tests/honesty.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/honesty.test.ts is fully GREEN, including: renderHonestyReport(0.82, 0.41, 'gptzero') contains '82%' and '41%' AND a note substring that EQUALS the '## Note' blockquote text read from references/honesty-framing.md (verbatim-render assertion — proves the copy is not inlined).
    - renderHonestyReport(0.82, null, 'gptzero') emits the 'N/A (humanizer not installed)' after-line and still emits the verbatim note.
    - selectHonestyBackend({ honestyBackend: 'originality' }) returns a stub whose score() returns null (DONE-05 pluggable; no fabricated score, no crash); unknown value falls back to gptzero.
    - The rendered note contains NO 'evade'/'beat'/'defeat' detection wording (transparency-only) — assert this in the test as a guard on the honest-framing non-negotiable.
    - `npm run lint` + `tsc --noEmit` pass.
  </acceptance_criteria>
  <done>DONE-04 + DONE-05 land: before/after honesty report with the framing note rendered VERBATIM from the locked copy (drift = CI failure), and a pluggable backend strategy (GPTZero shipped; Originality/Sapling not-implemented stubs). The honesty RED suite goes GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GPTZERO_API_KEY (secret) → request headers | The key must reach ONLY the x-api-key header; never a log, cost ledger, stdout, or return payload |
| locked copy (honesty-framing.md) → rendered report | A drifted/inlined framing could turn transparency into an undetectability claim (the core non-negotiable) |
| GPTZero response → score parse | Untrusted remote JSON; parsed defensively, unexpected shape → null (no fabricated score) |
| honesty.ts → network | Must route through http.ts (ESLint chokepoint) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Information Disclosure | GPTZERO_API_KEY in logs/ledger/stdout | mitigate | Presence-check only (`if (!apiKey) skip`); value passed solely into x-api-key header; http.ts cache-header allowlist drops x-api-key; honesty test asserts no key interpolation into stdout (T-01-07 precedent) |
| T-06-03-02 | Tampering | honest-framing copy drift / inlining | mitigate | Note read VERBATIM from references/honesty-framing.md (hash-pinned in 06-01); test asserts rendered note == file note AND no 'evade/beat/defeat' tokens (transparency-only non-negotiable) |
| T-06-03-03 | Spoofing | fabricated/garbage GPTZero response | mitigate | parseGptzeroResponse is defensive — unexpected shape / non-200 / parse error → null (skip), never a fabricated score; score shown as-is with no trust claim |
| T-06-03-04 | Denial of Service | GPTZero 429 / free-tier rate limit | mitigate | http.ts retry + generic bucket; any error → null skip (advisory); Pitfall 4 — 2 calls/paper, graceful 429 handling |
| T-06-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (jszip added in 06-01); nothing to slopcheck here |
</threat_model>

<verification>
- `node --import tsx --test tests/honesty.test.ts` — fully GREEN (skip-clean, cassette parse, verbatim framing render, pluggable backend).
- `node --import tsx --test tests/repo-files.test.ts` — GREEN (honesty-framing.md pin still valid; re-pin ONLY if a verbatim-render need forced a copy edit, in which case update the pin in the same commit).
- `npm run lint` — GREEN (no direct fetch/undici; chokepoint honored).
- `tsc --noEmit` — GREEN.
- Manual (deferred per 06-VALIDATION): live GPTZero call with a real key — not run in CI.
</verification>

<success_criteria>
- bin/lib/honesty.ts ships scoreHonesty + renderHonestyReport + HonestyBackend interface + GPTZero backend + selectHonestyBackend.
- Absent key / offline-no-cassette → clean null skip; key value never logged; response parsed defensively.
- The honest-framing note is rendered VERBATIM from references/honesty-framing.md (drift = CI failure); wording is transparency-only.
- Backend is pluggable (DONE-05); Originality/Sapling are not-implemented stubs that skip cleanly.
- tests/honesty.test.ts GREEN; lint + typecheck pass.
</success_criteria>

<output>
Create `.planning/phases/06-done-export-pipeline-zero-trace-gate/06-03-SUMMARY.md` when done.
</output>
