---
phase: 06-done-export-pipeline-zero-trace-gate
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - references/honesty-framing.md
  - CONTRIBUTING.md
  - tests/repo-files.test.ts
  - tests/zero-trace-export.test.ts
  - tests/plagiarism.test.ts
  - tests/honesty.test.ts
  - tests/exporter.test.ts
  - tests/export-gate.test.ts
  - tests/humanizer-wrap.test.ts
  - tests/fixtures/sample-zero-trace.docx
  - tests/fixtures/sample-zero-trace.pdf
  - tests/fixtures/cassettes/duckduckgo/html-search.json
  - tests/fixtures/cassettes/gptzero/predict-text.json
  - tests/fixtures/section-pass2-unsupported/VERIFICATION.md
  - scripts/make-zero-trace-fixture.mjs
  - scripts/make-zero-trace-pdf-fixture.mjs
autonomous: true
requirements: [DONE-02, DONE-03, DONE-04, DONE-05, DONE-06, DONE-07, DONE-08, DONE-09, TEST-10]

must_haves:
  truths:
    - "A RED test suite for every Phase-6 deliverable exists and SKIPs its behavioral tests pending the unbuilt modules (skip-guarded RED, never RED-by-crash) — mirroring the Phase-5 known-bad-* RED scaffold"
    - "A committed fixture .docx (a real ZIP carrying identifying metadata across the FULL Dublin-Core+cp+app field set AND a 'pensmith' string in BOTH a docProps field the patch must blank (cp:category) AND a non-.xml entry (_rels/.rels)) exists so zeroTracePatch can be tested OFFLINE and WITHOUT Pandoc — a real negative control authored INDEPENDENTLY of the patch regexes"
    - "A committed fixture .pdf (a real PDF carrying a 'pensmith'-bearing /Info dictionary (Producer/Creator/Author) AND an XMP metadata stream, where the non-'pensmith' identifying token 'Trace Sentinel' appears in BOTH /Info and the XMP stream) exists so zeroTracePdf can be scrubbed + scanned OFFLINE and WITHOUT Pandoc or a PDF engine — and Test D can assert that BOTH 'pensmith' AND 'Trace Sentinel' are gone after scrub (proving structural XMP removal, not literal masking) — the build machine has neither tool"
    - "TEST-10 scans ALL FOUR export formats (.md, .tex, .docx, .pdf) deterministically OFFLINE → exactly zero 'pensmith'/identifying-metadata occurrences; the SCRUB LOGIC for every format is CI-gated via committed fixtures and the exporter's deterministic offline md→tex writer (.tex is a REAL produced+scanned artifact, not 'clean by construction'; live Pandoc-produced output remains a documented manual check)"
    - "references/honesty-framing.md is the single locked source of the GPTZero honest-framing copy, byte-pinned by SHA-256 in repo-files.test.ts (WN-3 pattern); a CONTRIBUTING.md drift rule forbids editing it without re-pinning"
    - "jszip + pdf-lib are added as dependencies (slopcheck OK, no install/postinstall scripts, pure-JS no native build) and resolvable by node:test"
    - "Offline cassettes for DuckDuckGo HTML and GPTZero exist, each <=51200 bytes (D-25), in the Cassette[] array schema loadCassetteFile expects"
    - "A committed section VERIFICATION.md fixture carrying a real renderPass2Section-shaped UNSUPPORTED row exists so the Pass-2 → DONE-09 gate feed (readSectionUnsupported) can be tested against the EXACT on-disk markdown shape pass2.ts emits"
  artifacts:
    - path: "references/honesty-framing.md"
      provides: "LOCKED honest-framing copy: before/after output lines + the 'improves prose, not evades detection' note — read VERBATIM by bin/lib/honesty.ts"
      contains: "improves"
    - path: "tests/zero-trace-export.test.ts"
      provides: "TEST-10/DONE-07 RED suite — scans EVERY export format (.md/.tex/.docx/.pdf) bytes/ZIP entries for 'pensmith' + non-empty metadata fields -> 0, all OFFLINE + Pandoc-free; PDF Test D also asserts the non-'pensmith' 'Trace Sentinel' token is gone + the PDF still loads; .tex Test F scans a real offline-produced artifact"
      contains: "zeroTracePatch"
    - path: "tests/fixtures/sample-zero-trace.docx"
      provides: "Offline real-ZIP negative-control fixture (full identifying field set + 'pensmith' in cp:category AND _rels/.rels) for Pandoc-independent zeroTracePatch testing"
    - path: "tests/fixtures/sample-zero-trace.pdf"
      provides: "Offline real-PDF negative-control fixture ('pensmith'-bearing /Info dict + XMP stream; the non-'pensmith' token 'Trace Sentinel' in BOTH /Info and XMP) for Pandoc/engine-independent zeroTracePdf testing"
    - path: "tests/fixtures/section-pass2-unsupported/VERIFICATION.md"
      provides: "On-disk section VERIFICATION.md carrying a renderPass2Section-shaped ## Pass-2 table with an UNSUPPORTED row — the readSectionUnsupported parse fixture"
      contains: "UNSUPPORTED"
    - path: "tests/fixtures/cassettes/duckduckgo/html-search.json"
      provides: "DDG HTML response cassette (Cassette[] schema) for plagiarism.ts offline tests"
    - path: "tests/fixtures/cassettes/gptzero/predict-text.json"
      provides: "GPTZero /v2/predict/text response cassette (Cassette[] schema) for honesty.ts offline tests"
  key_links:
    - from: "references/honesty-framing.md"
      to: "tests/repo-files.test.ts"
      via: "SHA-256 byte-pin (real hash computed after the file is byte-stable; WN-3 single-source lockstep)"
      pattern: "honesty-framing"
    - from: "tests/fixtures/sample-zero-trace.docx"
      to: "tests/repo-files.test.ts"
      via: "SHA-256 byte-pin so the binary fixture cannot silently drift"
      pattern: "sample-zero-trace"
    - from: "tests/fixtures/sample-zero-trace.pdf"
      to: "tests/repo-files.test.ts"
      via: "SHA-256 byte-pin so the binary PDF fixture cannot silently drift"
      pattern: "sample-zero-trace"
---

<objective>
Land the Phase 6 Wave 0 RED scaffold BEFORE any export-pipeline implementation exists. This plan creates: the failing test suite for all six deliverables (plagiarism, honesty, exporter+zero-trace, gate, humanizer-absent, and the TEST-10 zero-trace gate), the committed real-ZIP fixture `.docx` AND the committed real-PDF fixture `.pdf` that make the zero-trace test deterministic and runnable OFFLINE WITHOUT Pandoc or a PDF engine, the on-disk section VERIFICATION.md fixture that feeds the Pass-2 → DONE-09 gate test, the offline DDG + GPTZero cassettes, the LOCKED honesty-framing copy file with its SHA-256 byte-pin + CONTRIBUTING.md drift rule, and the `jszip` + `pdf-lib` dependencies.

Purpose: Per this project's RED→GREEN convention (Phase 5 precedent — Wave 0 lands tests + fixtures + locked copy + sentinels before implementation), the gating zero-trace test (TEST-10/DONE-07) is the highest-risk item and must exist as a deterministic, offline, Pandoc-independent contract for ALL FOUR formats (.md, .tex, .docx, .pdf) before exporter.ts is written. The NON-NEGOTIABLE (CLAUDE.md / ROADMAP SC1) is ZERO pensmith trace in ANY export — so PDF is NOT scoped out: the .docx fixture lets `zeroTracePatch()` be tested without Pandoc, and the .pdf fixture lets `zeroTracePdf()` be tested without Pandoc OR a PDF engine (both ABSENT on the build machine). The PDF fixture is a GENUINE negative control: it carries the non-'pensmith' token 'Trace Sentinel' in BOTH /Info AND the XMP stream, so Test D can prove the XMP stream was STRUCTURALLY removed (both 'pensmith' AND 'Trace Sentinel' absent), not merely literal-masked (which the cycle-2 HIGH showed a catalog-only delete + byte-sweep would fail to do). The .tex contract is gated on a REAL offline-produced .tex artifact (the exporter's deterministic md→tex writer, no Pandoc), not "clean by construction". The honest-framing copy is locked here so drift becomes a CI failure (the non-negotiable per CLAUDE.md: "improves prose, does NOT evade detection").

Output: Six RED test files (skip-guarded on the unbuilt modules), two committed binary fixtures (.docx + .pdf) + the two generator scripts that produced them, one on-disk section VERIFICATION.md fixture for the Pass-2 feed test, two offline cassettes, the locked `references/honesty-framing.md` + its byte-pin in repo-files.test.ts, a CONTRIBUTING.md lock rule, and `jszip` + `pdf-lib` added to package.json. No `bin/lib/*` production module is created by this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-done-export-pipeline-zero-trace-gate/06-RESEARCH.md
@.planning/phases/06-done-export-pipeline-zero-trace-gate/06-PATTERNS.md
@.planning/phases/06-done-export-pipeline-zero-trace-gate/06-VALIDATION.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

From bin/lib/http-mock.ts (cassette schema + loader — the offline test seam; nock CANNOT intercept undici so adapters/tests read cassettes directly):
```
export interface Cassette {
  scope: string;            // e.g. 'https://html.duckduckgo.com' or 'https://api.gptzero.me'
  method: 'GET' | 'POST';
  path: string;             // e.g. '/html/?q=...' or '/v2/predict/text'
  status: number;           // 200
  response: unknown;        // string (DDG HTML) OR object (GPTZero JSON)
  responseHeaders?: Record<string, string>;
  requestHeaders?: Record<string, string>;
}
export function isOfflineMode(): boolean;  // true unless PENSMITH_NETWORK_TESTS === '1'
export function loadCassetteFile(adapter: string, basename: string): Cassette[] | null;  // reads tests/fixtures/cassettes/<adapter>/<basename>.json; returns null if absent; throws if not a JSON array
```
Cassette files are a JSON ARRAY of Cassette objects. D-25 size cap: each cassette file <= 51200 bytes.

From tests/repo-files.test.ts (the established SHA-256 byte-pin pattern — copy it for the new locked files):
```
test('references/<file> hash-pin (...)', () => {
  const bytes = readFileSync('references/<file>');          // raw bytes, no BOM strip
  const hash = createHash('sha256').update(bytes).digest('hex');
  const PINNED = '<64-hex>';                                 // regenerate after every intentional edit
  assert.equal(hash, PINNED, `... drifted. Update PINNED to ${hash} if intentional.`);
});
```
Regenerate command (run AFTER the file is byte-stable):
`node -e "console.log(require('node:crypto').createHash('sha256').update(require('node:fs').readFileSync('<path>')).digest('hex'))"`

From tests/known-bad-citations.test.ts (the RED-stub structure to mirror — existence assertion always runs; behavioral test skip-guarded on missing module/fixture):
```
const modPath = fileURLToPath(new URL('../bin/lib/<module>.ts', import.meta.url));
test('<module> file exists', () => assert.ok(existsSync(modPath), 'MISSING: created in wave 1'));
test('<behavior>', { skip: !existsSync(modPath) }, async () => { /* dynamic import + assert */ });
```

From bin/lib/ecosystem-presence.ts (humanizer presence — already returns false on this machine):
```
export function isHumanizerSkillPresent(): boolean;  // ~/.claude/skills/humanizer/ exists, is a dir, non-empty
export function isPandocPresent(): boolean;          // `pandoc --version` succeeds
```

From bin/lib/verify/pass2.ts (result shapes the done-gate RED stub references — DO NOT redefine, import the types — AND the EXACT renderPass2Section markdown shape the Pass-2 feed fixture must mirror):
```
export type Pass2Verdict = 'SUPPORTED' | 'PARTIAL' | 'UNSUPPORTED' | 'UNCLEAR';
export interface Pass2Result { citekey: string; claimSentence: string; verdict: Pass2Verdict; rationale: string; evidence: string; }
// renderPass2Section emits a 4-column table (NO evidence column), verdict cell BOLDED with **:
//   ## Pass-2 (claim support, advisory — LLM-judged)
//   <blank line>
//   | Citekey | Claim Sentence | Verdict | Rationale |
//   |---------|---------------|---------|-----------|
//   | <citekey> | <claimSentence ≤60 chars> | **UNSUPPORTED** | <rationale ≤200 chars> |
// Empty results emit: '## Pass-2 (claim support, advisory)\n\n_(no citations to judge)_\n'
```
From bin/lib/verify/pass4.ts:
```
export interface Pass4Result { paragraphIndex: number; totalSentences: number; claimsDetected: number; orphanCount: number; claims: Pass4ClaimResult[]; }
```

package.json `files[]` ships `references/` (so honesty-framing.md is shipped) but NOT `tests/` (so the fixture .docx + .pdf + cassettes + section VERIFICATION.md fixture are test-only — correct). jszip AND pdf-lib MUST be runtime `dependencies` entries (exporter.ts ships in dist/ and uses both).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add jszip + pdf-lib dependencies, build the committed real-ZIP fixture .docx + real-PDF fixture .pdf (each a genuine negative control) + their generator scripts, the section Pass-2 VERIFICATION.md fixture, and the DDG + GPTZero offline cassettes</name>
  <read_first>
    - bin/lib/http-mock.ts (Cassette interface lines 98-106, loadCassetteFile lines 156-164, isOfflineMode lines 139-141)
    - tests/fixtures/cassettes/ (browse one existing adapter dir, e.g. crossref/, to copy the exact Cassette[] JSON array shape and a real responseHeaders block)
    - bin/lib/verify/pass2.ts (renderPass2Section lines 298-315 — copy the EXACT ## Pass-2 table markdown shape, including the bolded **UNSUPPORTED** verdict cell, for the section VERIFICATION.md fixture)
    - .planning/phases/06-done-export-pipeline-zero-trace-gate/06-RESEARCH.md (Pattern 1 zero-trace DOCX XML targets lines 161-227; Pattern 2 PDF metadata lines 241-262 + Pitfall 3 lines 420-424; Pattern 3 GPTZero response fields lines 264-293; Pattern 4 DDG endpoint lines 295-324; Standard Stack jszip lines 64-90)
    - .planning/phases/06-done-export-pipeline-zero-trace-gate/06-PATTERNS.md ("No Analog Found" section lines 784-794 — the fixture .docx must be generated programmatically with JSZip)
    - package.json (dependencies block + files[] — confirm references/ ships, tests/ does not)
  </read_first>
  <files>package.json, package-lock.json, scripts/make-zero-trace-fixture.mjs, scripts/make-zero-trace-pdf-fixture.mjs, tests/fixtures/sample-zero-trace.docx, tests/fixtures/sample-zero-trace.pdf, tests/fixtures/section-pass2-unsupported/VERIFICATION.md, tests/fixtures/cassettes/duckduckgo/html-search.json, tests/fixtures/cassettes/gptzero/predict-text.json</files>
  <action>
    Run `npm install jszip pdf-lib` so both land in package.json `dependencies` and package-lock.json. jszip is slopcheck [OK] (06-RESEARCH Package Legitimacy Audit — 13 yrs, ~2M/wk, github.com/Stuk/jszip, NO postinstall, disposition Approved). pdf-lib is slopcheck [OK] (registry-verified at planning time: ~9 yrs / first published 2017-09, ~7.4M/wk downloads, github.com/Hopding/pdf-lib, PURE-JS — no gypfile / no native build, NO install/preinstall/postinstall script; its package.json `scripts` are dev/release-only and DO NOT run on `npm install`; sub-deps @pdf-lib/standard-fonts, @pdf-lib/upng, pako, tslib are all pure-JS). pdf-lib is the PDF zero-trace scrub engine (HIGH-1) — it loads + rewrites a PDF in-process with no external engine, so the PDF scrub is CI-gated OFFLINE on the engine-absent build machine. Do NOT install adm-zip. Confirm both ship their own .d.ts; only add @types/* if `tsc --noEmit` later complains — note that as a discretionary follow-up, do not pre-add it. NOTE for the executor: re-run slopcheck on jszip AND pdf-lib at install time and record the verdict in the SUMMARY (the planning-time audit is a prior, not a substitute).

    Create scripts/make-zero-trace-fixture.mjs: a one-shot Node ESM generator that builds the fixture .docx using JSZip and writes it to tests/fixtures/sample-zero-trace.docx. The fixture must be a REAL minimal DOCX ZIP (openable structure is NOT required — only that it is a valid ZIP with the metadata entries the test scans). CRITICAL — this fixture is a GENUINE NEGATIVE CONTROL, authored INDEPENDENTLY of the patch's regexes: it must carry identifying values across the FULL field set the patch is required to handle, AND embed a 'pensmith'-bearing value in at least one field the patch must blank but might miss if too narrow, AND a 'pensmith' value in a NON-.xml entry, so a too-narrow patch makes TEST-10 FAIL. Hand-author the XML literally (do NOT mirror the patch's exact regex source) — vary tag forms (mix a self-closing element and an attribute-bearing element) so the patch must be tolerant. It MUST contain, at minimum, these entries:
      - `docProps/core.xml` with the FULL Dublin-Core + cp identifying set, each NON-EMPTY: `<dc:creator>Trace Sentinel</dc:creator>`, `<dc:title>pensmith draft</dc:title>` (literal 'pensmith'), `<dc:subject xml:lang="en">Trace Subject</dc:subject>` (ATTRIBUTE-bearing form — proves the patch handles attributes), `<dc:description>Trace Description</dc:description>`, `<cp:keywords>trace, sentinel</cp:keywords>`, `<cp:category>pensmith-category</cp:category>` (literal 'pensmith' in a field the NARROW patch did NOT originally target — the negative-control trap), `<cp:contentStatus>Draft</cp:contentStatus>`, `<cp:lastModifiedBy>Trace Sentinel</cp:lastModifiedBy>`, and `<dcterms:created xsi:type="dcterms:W3CDTF">2026-06-18T12:00:00Z</dcterms:created>` / `<dcterms:modified xsi:type="dcterms:W3CDTF">2026-06-18T12:00:00Z</dcterms:modified>` (real, non-epoch). Use the standard OOXML core.xml namespace declarations (dc, cp, dcterms, xsi).
      - `docProps/app.xml` with `<Application>Microsoft Word 12.0.0</Application>`, `<Company>Pensmith Test Co</Company>`, `<Manager>Trace Manager</Manager>`, and a SELF-CLOSING `<Template/>` (proves the patch handles self-closing tags).
      - `_rels/.rels` (a NON-.xml-extension entry) containing the literal string 'pensmith' somewhere in its content (e.g. a relationship Target or a comment) — the entry the .xml-only sweep would MISS. This is the trap that fails a too-narrow sweep.
      - `[Content_Types].xml` and a trivial `word/document.xml` (any well-formed XML body, no 'pensmith') so the ZIP resembles a real DOCX shape.
      To keep the binary deterministic across machines/OSes (Pitfall 6 in 06-RESEARCH), pass `{ date: new Date(0) }` to every `zip.file(name, content, { date })` call AND generate with `zip.generateAsync({ type: 'nodebuffer', platform: 'UNIX' })` (or the JSZip-stable equivalent that fixes ZIP entry metadata). The goal: re-running the script on any machine produces a byte-identical file so the repo-files byte-pin in Task 3 stays stable.

    Create scripts/make-zero-trace-pdf-fixture.mjs: a one-shot Node ESM generator that writes a REAL minimal PDF to tests/fixtures/sample-zero-trace.pdf carrying identifying metadata — the PDF negative control. Author the PDF BYTES BY HAND (a tiny hand-written PDF object structure — header, one catalog/page object, an /Info dictionary, and an XMP metadata stream — written with fs.writeFileSync of a Buffer), NOT via pdf-lib, so the fixture is authored INDEPENDENTLY of the scrub library (genuine negative control, not a closed loop). It MUST contain:
      - An `/Info` dictionary with `/Producer (pensmith 0.1)`, `/Creator (pensmith exporter)`, `/Author (Trace Sentinel)`, `/Title (pensmith draft)`, `/Subject (Trace Subject)`, `/Keywords (trace, sentinel)` — multiple identifying fields, two of them carrying the literal 'pensmith' AND the non-'pensmith' identifying token 'Trace Sentinel' in `/Author`.
      - An XMP metadata stream (an uncompressed object whose stream body is `<?xpacket ...?><x:xmpmeta ...> ... <pdf:Producer>pensmith 0.1</pdf:Producer> <dc:creator><rdf:Seq><rdf:li>Trace Sentinel</rdf:li></rdf:Seq></dc:creator> ... </x:xmpmeta><?xpacket end="w"?>`) so the literal 'pensmith' ALSO appears in the XMP stream (not just /Info) AND the non-'pensmith' token 'Trace Sentinel' appears in the XMP `<dc:creator>` (so a scrub that only removes the literal 'pensmith' — e.g. a byte-sweep over an intact XMP stream — would leave 'Trace Sentinel' behind, and Test D would catch it). The 'Trace Sentinel' token MUST therefore appear in BOTH the /Info dict AND the XMP stream.
      - Keep the PDF UNCOMPRESSED (no FlateDecode) and minimal so it is a valid, parseable PDF that pdf-lib can load and rewrite.
      - Use a fixed PDF /ID and fixed object offsets / no timestamps so the file is byte-deterministic (re-running the script yields a byte-identical PDF for the Task-3 byte-pin). Validate at the end of the script that `await PDFDocument.load(bytes)` (pdf-lib) succeeds and that the raw bytes contain 'pensmith' in BOTH the /Info region and the XMP stream AND contain 'Trace Sentinel' in BOTH the /Info region and the XMP stream.

    Run both scripts once to produce tests/fixtures/sample-zero-trace.docx + tests/fixtures/sample-zero-trace.pdf and COMMIT both binaries. Both scripts are committed too (provenance + regeneration path).

    Create tests/fixtures/section-pass2-unsupported/VERIFICATION.md: a section VERIFICATION.md carrying a real renderPass2Section-shaped `## Pass-2` table (copy the EXACT shape from bin/lib/verify/pass2.ts renderPass2Section — header `| Citekey | Claim Sentence | Verdict | Rationale |`, the dashed separator row, and at least one data row whose verdict cell is the BOLDED `**UNSUPPORTED**` for a real citekey, plus one `**SUPPORTED**` row so the parser must filter by verdict, not just count rows). Precede the table with a plausible Pass-1/Pass-3 blocking-status preamble and a `## Pass-2 (claim support, advisory — LLM-judged)` heading EXACTLY as renderPass2Section emits it. This is the on-disk input the HIGH-3 readSectionUnsupported unit test (06-05) parses; it pins the writer↔parser contract.

    Create tests/fixtures/cassettes/duckduckgo/html-search.json: a JSON array (Cassette[]) with one entry — scope 'https://html.duckduckgo.com', method 'GET', path a representative `/html/?q=<encoded-distinctive-phrase>`, status 200, and `response` a STRING of trimmed DDG result HTML containing at least two `<a class="result__a" href="https://example.com/...">...</a>` result links (the parse target plagiarism.ts will extract) plus one non-result link to prove the parser filters. Keep the HTML minimal (well under the 51200-byte D-25 cap). Add a realistic `responseHeaders` content-type 'text/html'.

    Create tests/fixtures/cassettes/gptzero/predict-text.json: a JSON array (Cassette[]) with one entry — scope 'https://api.gptzero.me', method 'POST', path '/v2/predict/text', status 200, and `response` an OBJECT matching the GPTZero v2 shape from 06-RESEARCH Pattern 3 / Code Examples: `{ "documents": [ { "class_probabilities": { "ai": 0.82, "human": 0.15, "mixed": 0.03 }, "document_classification": "AI_ONLY", "predicted_class": "ai" } ] }`. This is the response honesty.ts parses; the 0.82 → 82% display is asserted by the honesty RED test in Task 2.
  </action>
  <verify>
    <automated>node -e "const z=require('jszip'); const fs=require('node:fs'); z.loadAsync(fs.readFileSync('tests/fixtures/sample-zero-trace.docx')).then(async zip=>{ const core=await zip.file('docProps/core.xml').async('string'); const rels=await zip.file('_rels/.rels').async('string'); if(!core.includes('Trace Sentinel')||!core.toLowerCase().includes('pensmith')||!core.includes('cp:category')) throw new Error('docx core.xml missing deliberate trace/category'); if(!rels.toLowerCase().includes('pensmith')) throw new Error('docx _rels/.rels missing pensmith trap'); }).then(()=>{ const { PDFDocument }=require('pdf-lib'); const pdf=fs.readFileSync('tests/fixtures/sample-zero-trace.pdf'); return PDFDocument.load(pdf).then(()=>{ const s=pdf.toString('latin1'); if(!/\/Producer\s*\(pensmith/.test(s)||!/x:xmpmeta[\s\S]*pensmith/.test(s)) throw new Error('pdf fixture missing /Info or XMP pensmith trace'); if(!/\/Author\s*\(Trace Sentinel/.test(s)||!/x:xmpmeta[\s\S]*Trace Sentinel/.test(s)) throw new Error('pdf fixture missing Trace Sentinel in /Info or XMP'); console.log('fixtures OK'); }); })"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "require('jszip')"` AND `node -e "require('pdf-lib')"` resolve (both installed; both under dependencies in package.json, not devDependencies).
    - tests/fixtures/sample-zero-trace.docx exists, is a valid ZIP loadable by JSZip, and its docProps/core.xml contains the FULL identifying set (dc:creator, dc:title, dc:subject [attribute-bearing], dc:description, cp:keywords, cp:category, cp:contentStatus, cp:lastModifiedBy) with non-epoch timestamps; the literal 'pensmith' appears in dc:title AND cp:category AND in the NON-.xml entry _rels/.rels; docProps/app.xml contains Application + Company + Manager + a self-closing Template.
    - tests/fixtures/sample-zero-trace.pdf exists, loads via `PDFDocument.load`, and its raw bytes carry the literal 'pensmith' in BOTH the /Info dictionary (/Producer or /Creator) AND the XMP stream, AND carry the non-'pensmith' token 'Trace Sentinel' in BOTH the /Info dictionary (/Author) AND the XMP stream (`<dc:creator>`).
    - scripts/make-zero-trace-fixture.mjs + scripts/make-zero-trace-pdf-fixture.mjs are committed and re-running each produces a byte-identical fixture (deterministic — `{ date: new Date(0) }` on every docx entry; fixed /ID + offsets for the pdf).
    - tests/fixtures/section-pass2-unsupported/VERIFICATION.md contains a `## Pass-2 (claim support, advisory — LLM-judged)` heading + a 4-column table with a bolded `**UNSUPPORTED**` row and a `**SUPPORTED**` row, matching renderPass2Section's exact emitted shape.
    - tests/fixtures/cassettes/duckduckgo/html-search.json parses as a Cassette[] array; its response string contains >=2 `result__a` anchors; file <= 51200 bytes.
    - tests/fixtures/cassettes/gptzero/predict-text.json parses as a Cassette[] array; response.documents[0].class_probabilities.ai === 0.82 and document_classification === 'AI_ONLY'; file <= 51200 bytes.
  </acceptance_criteria>
  <done>jszip + pdf-lib dependencies added; deterministic real-ZIP fixture .docx (full identifying field set + 'pensmith' in cp:category AND _rels/.rels — a genuine negative control) AND deterministic real-PDF fixture .pdf ('pensmith' in /Info + XMP; the non-'pensmith' token 'Trace Sentinel' in BOTH /Info + XMP so a literal-only scrub fails Test D) committed alongside their generators; the section Pass-2 UNSUPPORTED VERIFICATION.md fixture pins the writer↔parser contract; DDG + GPTZero offline cassettes authored in the Cassette[] schema under the D-25 size cap — all the offline, Pandoc-free, engine-free inputs Waves 1-2 need.</done>
</task>

<task type="auto">
  <name>Task 2: Write the six RED test files (skip-guarded on unbuilt modules) covering DONE-02..09 + TEST-10 — with TEST-10 scanning ALL FOUR formats offline</name>
  <read_first>
    - tests/known-bad-pass2.test.ts AND tests/known-bad-citations.test.ts (the RED-by-skip structure — existence test always runs, behavioral test guarded by `{ skip: !existsSync(modPath) }`)
    - tests/cassette-no-leak.test.ts (the ZIP entry-iteration + violation-collection pattern lines 55-83 — the zero-trace test mirrors this against the fixture .docx)
    - tests/compile-refuse.test.ts (mkdtempSync .paper fixture-root pattern lines 49-56 — used by exporter + gate tests)
    - tests/revise-swap.test.ts (loadCassetteFile usage + env-restore try/finally pattern — used by plagiarism/honesty tests)
    - bin/lib/verify/pass2.ts + pass4.ts (import Pass2Result/Pass4Result/Pass2Verdict types — DO NOT redefine)
    - bin/lib/ecosystem-presence.ts (isHumanizerSkillPresent — already false here)
    - .planning/phases/06-done-export-pipeline-zero-trace-gate/06-PATTERNS.md (per-test analog sections: zero-trace-export lines 416-451, plagiarism lines 468-497, honesty lines 499-526, exporter lines 530-551, done-gate lines 554-576, humanizer-absent lines 580-600)
  </read_first>
  <files>tests/zero-trace-export.test.ts, tests/plagiarism.test.ts, tests/honesty.test.ts, tests/exporter.test.ts, tests/export-gate.test.ts, tests/humanizer-wrap.test.ts</files>
  <action>
    All six files use the node:test built-in runner imports (`import { test } from 'node:test'; import assert from 'node:assert/strict';`). Each module-existence test ALWAYS runs and asserts the Wave-1/2 module file exists; each behavioral test is guarded `{ skip: !existsSync(modPath) }` so the suite is RED-BY-SKIP (skips, zero failures) until the module lands — NEVER RED-by-crash. Reference the EXACT exported symbol names below so Waves 1-2 implement against a fixed contract.

    tests/zero-trace-export.test.ts (TEST-10 / DONE-07 — the gating test, MUST scan ALL FOUR formats offline + Pandoc-independent + engine-independent). NON-NEGOTIABLE per ROADMAP SC1: every format (.md, .tex, .docx, .pdf) is scanned for 'pensmith' + identifying metadata → exactly 0. The SCRUB LOGIC for each format is CI-gated via committed fixtures + the exporter's deterministic offline md→tex writer; live Pandoc-produced output is a documented manual check only.
      - Test A — DOCX negative control (always runs): load tests/fixtures/sample-zero-trace.docx with JSZip and assert it currently DOES contain a trace — non-empty dc:creator AND the literal 'pensmith' in BOTH cp:category AND _rels/.rels — proving the fixture is a valid negative control (the RED baseline the patch must clean).
      - Test B — DOCX scrub (skip-guarded on bin/lib/exporter.ts existing): dynamic-import `zeroTracePatch` from '../bin/lib/exporter.js'; copy the fixture into an mkdtemp dir; call `zeroTracePatch(tmpDocxPath)`; reload the patched ZIP and assert across EVERY non-dir entry (INCLUDING non-.xml entries like _rels/.rels): (1) no entry's text contains 'pensmith' (case-insensitive) — explicitly assert _rels/.rels is clean; (2) docProps/core.xml dc:creator, dc:title, dc:subject, dc:description, cp:keywords, cp:category, cp:contentStatus, cp:lastModifiedBy are all empty/blank; (3) dcterms:created and dcterms:modified equal the epoch '1970-01-01T00:00:00Z'; (4) docProps/app.xml Application, Company, Manager, Template are empty/removed. Collect violations into an array and `assert.deepEqual(violations, [])` so failures name the offending entry+field.
      - Test C — PDF negative control (always runs): read tests/fixtures/sample-zero-trace.pdf raw bytes (latin1) and assert it currently DOES contain 'pensmith' in BOTH the /Info region (e.g. `/Producer (pensmith`) AND the XMP stream (`x:xmpmeta` ... `pensmith`), AND DOES contain the non-'pensmith' token 'Trace Sentinel' in BOTH the /Info region (`/Author (Trace Sentinel`) AND the XMP stream — proving the PDF fixture is a valid negative control for BOTH a literal-'pensmith' scrub AND a structural-removal scrub.
      - Test D — PDF scrub (skip-guarded on exporter.ts): dynamic-import `zeroTracePdf` from '../bin/lib/exporter.js'; copy the fixture .pdf into an mkdtemp dir; call `zeroTracePdf(tmpPdfPath)`; reload the scrubbed bytes and assert: (1) the raw bytes contain NO 'pensmith' (case-insensitive) anywhere; (2) the raw bytes contain NO 'Trace Sentinel' anywhere — this is the load-bearing assertion proving the XMP stream was STRUCTURALLY removed (the indirect object deleted from the pdf-lib context before save), NOT merely literal-masked by a byte-sweep (a byte-sweep would delete only 'pensmith' and leave 'Trace Sentinel' in an intact `<x:xmpmeta>` block); (3) the /Info dictionary identifying fields (Producer, Creator, Author, Title, Subject, Keywords) are empty or removed AND no `<x:xmpmeta>` stream carrying identifying values remains; (4) the scrubbed PDF still loads via `PDFDocument.load` (NOT corrupted — proving no length-altering byte edit was performed on the serialized PDF). This runs OFFLINE with NO Pandoc and NO PDF engine (pdf-lib is pure-JS).
      - Test E — md trace-free (skip-guarded on exporter.ts): call exportDraft on a draft containing NO 'pensmith' string with format 'md' (md-only path) and assert the produced .md (written into the exporter's distinct export dir) bytes contain no 'pensmith' and no YAML/HTML generator comment.
      - Test F — tex trace-free on a REAL offline-produced artifact (skip-guarded on exporter.ts): call `exportDraft({ inputPath, format: 'latex', paperRoot, pandocPresent: false })` and assert the exporter PRODUCES A REAL .tex FILE via its deterministic offline md→tex writer (NO Pandoc), then read that emitted .tex artifact's bytes and assert they contain NO 'pensmith' string AND NO generator comment (no `% pensmith`, no `% Generated by` line). This is a scanned real artifact, not "clean by construction" — drive it from the exporter's offline path so it runs WITHOUT Pandoc and the produced .tex (not the md fallback) is the thing scanned. (Live Pandoc-produced .tex remains a documented manual check; this CI test gates the no-generator-comment contract on a real offline-producible .tex.)

    tests/plagiarism.test.ts (DONE-02): module-exists test for bin/lib/plagiarism.ts. Behavioral (skip-guarded, offline via isOfflineMode default): dynamic-import `runPlagiarism` and `extractDistinctivePhrases` and `renderPlagiarismSection` from '../bin/lib/plagiarism.js'. Assert: (1) extractDistinctivePhrases on a multi-sentence draft returns <=10 phrases each >=5 words; (2) runPlagiarism(draftMd) in offline mode (reads the duckduckgo cassette) returns a PlagiarismResult[] where the matched phrase carries >=2 result URLs parsed from the cassette HTML; (3) runPlagiarism NEVER throws on a transport-error simulation (advisory-never-throws); (4) renderPlagiarismSection returns a markdown '## Plagiarism Check' table string.

    tests/honesty.test.ts (DONE-04, DONE-05): module-exists test for bin/lib/honesty.ts. Behavioral (skip-guarded): dynamic-import `scoreHonesty`, `renderHonestyReport`, and the `HonestyBackend` interface usage from '../bin/lib/honesty.js'. Assert: (1) with GPTZERO_API_KEY deleted, scoreHonesty('text') resolves to null (skip-clean — restore env in finally); (2) with the gptzero cassette active (offline) and a key present, scoreHonesty returns { aiProbability: 0.82, classification: 'AI_ONLY', backend: 'gptzero' }; (3) renderHonestyReport(0.82, 0.41, 'gptzero') returns a string containing '82%' and '41%' AND containing the EXACT note substring read from references/honesty-framing.md (assert the rendered note equals the framing-file note verbatim — proving the copy is rendered from the locked file, not inlined); (4) DONE-05: assert a `selectBackend(config)` (or equivalent) honors a `honestyBackend` config value and that an unknown/unimplemented backend (originality/sapling) returns null or a not-implemented stub rather than crashing.

    tests/exporter.test.ts (DONE-06, DONE-08): module-exists test for bin/lib/exporter.ts. Behavioral (skip-guarded): create an mkdtemp .paper root with DRAFT.md + CITATIONS.bib; dynamic-import `exportDraft` from '../bin/lib/exporter.js'. Assert: (1) with Pandoc absent (the machine default — call exportDraft with format 'docx'), it falls back to markdown-only, writes a .md output INTO the exporter's distinct export dir (NOT onto the source DRAFT.md), prints a banner containing 'Pandoc' to stdout, and does NOT throw ENOENT; (2) the export output path is under the distinct export dir (default `.paper/export/`), never `paperDir(paperRoot)` itself; (3) DONE-08: CITATIONS.bib is copied into the export dir alongside the exported output (existsSync at exportDir/CITATIONS.bib) and the copy source/destination are distinct paths; (4) exportDraft accepts the injected/explicit pandocPresent=false path deterministically (do not depend on the real Pandoc binary).

    tests/export-gate.test.ts (DONE-09 + DONE-01): module-exists test for bin/cli/done.ts (or the extracted `runDoneGate`/`runDone` lib it delegates to — reference whichever Wave-2 names the gate function; pin the name `runDoneGate`). Behavioral (skip-guarded): import `runDoneGate` and assert: (1) an UNSUPPORTED Pass2Result + a Pass4Result with orphanCount>0 + a plagiarism hit each, with an injected `approve: async () => false`, yields { exported: false } and the injected approver WAS called (gate fired with a per-issue summary); (2) `yolo: true` yields { gateSkipped: true } and the injected approver is NEVER called (assert it throws if called); (3) with zero issues and `approve: async () => true`, the generic confirm STILL runs (approver called once) and result is { exported: true } — proving the generic gate fires even on a clean paper (PRD §7.9). NOTE: the disk→gate feed (readSectionUnsupported over the section VERIFICATION.md fixture) + the NON-yolo on-disk gate integration test are added in 06-05 (HIGH-3) — this file pins the gate-LOGIC contract; 06-05 pins the disk-feed contract.

    tests/humanizer-wrap.test.ts (DONE-03): module-exists test for the humanizer-wrap entrypoint (reference the Wave-2 `runHumanizer` or the done-orchestrator humanize step; pin `runHumanizer`). Behavioral (skip-guarded): since isHumanizerSkillPresent() is false on this machine, assert runHumanizer(draftMd) (1) does NOT throw, (2) returns null (or the unchanged draft signal) so export proceeds on DRAFT.md, and (3) writes a stdout banner containing 'humanizer skill not found'. Capture stdout via the process.stdout.write override pattern from 06-PATTERNS lines 588-598 and restore in finally.
  </action>
  <verify>
    <automated>node --import tsx --test tests/zero-trace-export.test.ts tests/plagiarism.test.ts tests/honesty.test.ts tests/exporter.test.ts tests/export-gate.test.ts tests/humanizer-wrap.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All six test files run. Every module-existence test currently FAILS-as-expected? NO — they are RED markers; instead author them as `test('... module exists', () => assert.ok(existsSync(modPath), 'MISSING: created in wave 1'))` which FAILS until the module lands. To keep Wave 0 green-able, follow the Phase-5 precedent: mirror tests/known-bad-pass2.test.ts EXACTLY — whatever stance it uses for existence assertions (skip-guarded informational OR accepted-RED `assert.ok`), use the SAME stance here; do not invent a stricter one. Document the resulting Wave-0 test status (which tests are skipped vs. the accepted-RED existence markers) in the SUMMARY.
    - Every behavioral test SKIPS (not errors) because the Wave-1/2 modules do not yet exist — confirmed by the run reporting skipped tests with ZERO errored tests.
    - zero-trace-export.test.ts Test A (DOCX) AND Test C (PDF — incl. the 'Trace Sentinel' negative-control assertion) PASS now (both fixtures genuinely carry a trace — valid negative controls); the four scrub/scan tests (B docx, D pdf, E md, F tex) are present and skip pending exporter.ts.
    - TEST-10 demonstrably covers ALL FOUR formats (.md, .tex, .docx, .pdf): grep the test file confirms a scan assertion exists for each format's produced/fixture artifact; Test D asserts BOTH 'pensmith' AND 'Trace Sentinel' absence + still-loads; Test F scans a REAL offline-produced .tex artifact (not the md fallback).
    - The honesty test's verbatim-note assertion reads references/honesty-framing.md and compares the rendered note to the file's note text (the assertion is authored even though it skips pending honesty.ts).
    - No test imports `fetch`/`undici` directly (all HTTP-bearing behavior is exercised via the cassettes through the future modules) — `npm run lint` passes on the new test files.
  </acceptance_criteria>
  <done>Six RED test files land — zero-trace (offline, Pandoc-free AND engine-free, scanning ALL FOUR formats .md/.tex/.docx/.pdf against committed fixtures + a real offline .tex artifact; Test D asserts both 'pensmith' AND 'Trace Sentinel' gone + still-loads), plagiarism, honesty (with verbatim-framing assertion), exporter (Pandoc-absent fallback into a distinct export dir + bib copy), done-gate (fires on issues, generic on clean, --yolo skips), and humanizer-absent (skip-clean). Behavioral tests skip pending Waves 1-2; the contract symbol names (incl. zeroTracePdf) are pinned.</done>
</task>

<task type="auto">
  <name>Task 3: Author the LOCKED honesty-framing copy, byte-pin it + the fixture .docx + the fixture .pdf in repo-files.test.ts, add the CONTRIBUTING.md drift rule</name>
  <read_first>
    - references/http-warnings.md (the locked-copy file STRUCTURE — section heading + `> ` blockquote line that code parses; honesty.ts will parse honesty-framing.md the same way)
    - tests/repo-files.test.ts (the http-warnings.md hash-pin test lines 159-166 — copy this test verbatim shape for honesty-framing.md AND for the fixture .docx AND for the fixture .pdf)
    - CONTRIBUTING.md (find the existing locked-copy / tier-contract rules section to append the honesty-framing lock rule in the same style)
    - .planning/phases/06-done-export-pipeline-zero-trace-gate/06-RESEARCH.md (exact framing output format lines 287-293; LOCKED framing copy rule lines 284-285)
    - .planning/phases/06-done-export-pipeline-zero-trace-gate/06-PATTERNS.md (references/honesty-framing.md structure lines 344-376; CONTRIBUTING.md lock-rule pattern lines 372-376)
  </read_first>
  <files>references/honesty-framing.md, CONTRIBUTING.md, tests/repo-files.test.ts</files>
  <action>
    Create references/honesty-framing.md following the references/http-warnings.md structure (a section heading + `> ` blockquote lines that bin/lib/honesty.ts parses at module load via the same loadWarnString-style section scan). It MUST contain, VERBATIM, the honest-framing copy from 06-RESEARCH lines 287-293:
      - An "## Output format" section with two blockquote lines: the before-humanize line and the after-humanize line, each using the `XX%` placeholder and naming the backend (GPTZero).
      - A "## Note" section whose blockquote is the EXACT note: that the score reflects prose patterns, the humanizer improves readability/prose, and it does NOT promise to make output undetectable. This sentence is the non-negotiable honest-framing requirement (CLAUDE.md: "improves prose, does NOT evade detection") — render it so honesty.ts can extract the note paragraph after the "## Note" heading exactly like http.ts extracts its WARN line.
      - A header comment noting the file is LOCKED and that edits require re-pinning the SHA-256 in tests/repo-files.test.ts (mirror the http-warnings.md header note).
    Do NOT include any language framing the humanizer as "evading", "beating", or "defeating" detection — the wording must be transparency-only.

    Compute the SHA-256 of the byte-stable references/honesty-framing.md and add a hash-pin test to tests/repo-files.test.ts copying the http-warnings.md test shape (lines 159-166): read raw bytes, sha256, assert.equal against the PINNED real hash, with the regenerate-command comment. Use the real computed hash (NOT a __PENDING__ sentinel — this is a static copy file, not a runtime prompt slug, so it is byte-pinned GREEN from creation, exactly like http-warnings.md).

    Add a SECOND hash-pin test to tests/repo-files.test.ts for tests/fixtures/sample-zero-trace.docx (the binary fixture from Task 1): read raw bytes, sha256, assert against the real PINNED hash, with a comment that the fixture is regenerated via `node scripts/make-zero-trace-fixture.mjs`. This guards the binary fixture from silent drift (a changed fixture could mask a zero-trace regression).

    Add a THIRD hash-pin test to tests/repo-files.test.ts for tests/fixtures/sample-zero-trace.pdf (the PDF fixture from Task 1): read raw bytes, sha256, assert against the real PINNED hash, with a comment that the fixture is regenerated via `node scripts/make-zero-trace-pdf-fixture.mjs`. This guards the PDF negative control from silent drift (a changed PDF fixture could mask a PDF zero-trace regression — HIGH-1).

    Append a "Honesty framing copy is LOCKED" rule to CONTRIBUTING.md in the same section/style as the existing locked-copy + tier-contract rules: state that references/honesty-framing.md is the single source of the GPTZero honest-framing prose, that bin/lib/honesty.ts renders it verbatim, that any wording change is a deliberate PR that MUST also re-pin the SHA-256 in tests/repo-files.test.ts, and that the framing must remain transparency-only ("improves prose, does not evade detection") — never an undetectability claim.
  </action>
  <verify>
    <automated>node --import tsx --test tests/repo-files.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - references/honesty-framing.md exists with an "## Output format" section (before + after blockquote lines) and a "## Note" section whose blockquote contains the verbatim "improves ... does not ... undetectable" honest-framing sentence; it contains NO "evade/beat/defeat detection" wording.
    - tests/repo-files.test.ts has a hash-pin test for references/honesty-framing.md that PASSES with the real computed SHA-256 (not a sentinel).
    - tests/repo-files.test.ts has a hash-pin test for tests/fixtures/sample-zero-trace.docx that PASSES with the real computed SHA-256.
    - tests/repo-files.test.ts has a hash-pin test for tests/fixtures/sample-zero-trace.pdf that PASSES with the real computed SHA-256.
    - CONTRIBUTING.md contains the honesty-framing lock rule (re-pin requirement + transparency-only constraint) in the existing locked-copy rule style.
    - `node --import tsx --test tests/repo-files.test.ts` is fully GREEN (all three new pins pass; no regression to existing pins).
  </acceptance_criteria>
  <done>The honest-framing copy is locked and byte-pinned (GREEN from creation, WN-3 static-copy pattern); the binary fixture .docx AND .pdf are byte-pinned against drift; CONTRIBUTING.md forbids silent framing drift. The single source of truth for the "improves prose, not evades detection" non-negotiable is established before honesty.ts is written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| locked copy file (honesty-framing.md) → bin/lib/honesty.ts render | A drifted or rewritten copy could silently turn the honest framing into an undetectability claim (the core non-negotiable) |
| binary fixture .docx / .pdf → zero-trace test | A silently-changed fixture could remove the trace it is meant to be a negative control for, masking a real zero-trace regression for that format |
| cassette JSON (DDG / GPTZero) → future module parsing | Crafted cassette content is the offline test seam; oversized cassettes (>51200 B, D-25) bloat the repo |
| section VERIFICATION.md fixture → readSectionUnsupported parser | The on-disk Pass-2 table shape pins the writer↔parser contract; a fixture that desyncs from renderPass2Section would let a silent parser miss escape (HIGH-3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | references/honesty-framing.md honest-framing copy | mitigate | SHA-256 byte-pin in repo-files.test.ts + CONTRIBUTING.md lock rule; transparency-only wording enforced by the test asserting no "evade/beat/defeat" tokens (added by honesty.ts test in 06-03) |
| T-06-01-02 | Tampering | tests/fixtures/sample-zero-trace.docx negative control | mitigate | SHA-256 byte-pin in repo-files.test.ts; deterministic generator (`{ date: new Date(0) }`) makes re-pin reproducible; fixture authored INDEPENDENTLY of the patch regexes (genuine negative control, not a closed loop) |
| T-06-01-04 | Tampering | tests/fixtures/sample-zero-trace.pdf negative control | mitigate | SHA-256 byte-pin in repo-files.test.ts; hand-authored PDF bytes (independent of pdf-lib scrub) with fixed /ID + offsets → deterministic; carries 'pensmith' in /Info AND XMP AND the non-'pensmith' token 'Trace Sentinel' in /Info AND XMP, so a too-narrow OR literal-only PDF scrub (e.g. a byte-sweep over an intact XMP stream) fails TEST-10 Test D — forcing the structural XMP-object delete |
| T-06-01-03 | Information Disclosure | fixture .docx/.pdf authored with a synthetic name ('Trace Sentinel') | accept | 'Trace Sentinel' is a SYNTHETIC placeholder trace in a TEST fixture (not shipped — tests/ is excluded from package.json files[]); it exists precisely to prove zeroTracePatch/zeroTracePdf strip it; no real PII is shipped |
| T-06-01-SC | Tampering | npm install (jszip + pdf-lib) supply chain | accept | jszip [OK] slopcheck (13 yrs, ~2M/wk, github.com/Stuk/jszip, NO postinstall); pdf-lib [OK] (~9 yrs, ~7.4M/wk, github.com/Hopding/pdf-lib, PURE-JS no native build, NO install/preinstall/postinstall — release scripts do not run on install; pure-JS sub-deps); both pinned by package-lock.json. Executor MUST re-run slopcheck on BOTH at install time and record the verdict in the SUMMARY (planning audit is a prior, not a substitute) |
</threat_model>

<verification>
- `node --import tsx --test tests/repo-files.test.ts` — GREEN (honesty-framing.md + sample-zero-trace.docx + sample-zero-trace.pdf byte-pins pass).
- `node --import tsx --test tests/zero-trace-export.test.ts tests/plagiarism.test.ts tests/honesty.test.ts tests/exporter.test.ts tests/export-gate.test.ts tests/humanizer-wrap.test.ts` — behavioral tests SKIP (modules unbuilt), zero ERRORED tests; zero-trace Test A (docx) + Test C (pdf, incl. 'Trace Sentinel' negative-control) pass (both fixtures are valid traced negative controls); the four scrub/scan tests (docx/pdf/md/tex) are present and skip-guarded.
- `node -e "require('jszip')"` AND `node -e "require('pdf-lib')"` — resolve (both dependencies added).
- `npm run lint` — GREEN on new test files (no direct fetch/undici import).
- Manual: confirm references/ ships in package.json files[] (honesty-framing.md will be in dist package) and tests/ does not (fixtures + cassettes + section VERIFICATION.md fixture are test-only); confirm both cassettes are valid Cassette[] arrays <= 51200 bytes; confirm slopcheck verdicts for jszip + pdf-lib recorded in SUMMARY.
</verification>

<success_criteria>
- jszip + pdf-lib are runtime dependencies; the deterministic real-ZIP fixture .docx + the deterministic real-PDF fixture .pdf + their generators are committed; the section Pass-2 UNSUPPORTED VERIFICATION.md fixture is committed; DDG + GPTZero cassettes exist in the Cassette[] schema under the D-25 size cap.
- Six RED test files exist (zero-trace, plagiarism, honesty, exporter, done-gate, humanizer-absent) with behavioral tests skip-guarded on the unbuilt Wave-1/2 modules and the contract symbol names (incl. zeroTracePdf) pinned.
- TEST-10 scans ALL FOUR formats (.md, .tex, .docx, .pdf) OFFLINE — the .docx scrub via the committed ZIP fixture, the .pdf scrub via the committed PDF fixture (pure-JS pdf-lib, no Pandoc/engine) with Test D asserting BOTH 'pensmith' AND 'Trace Sentinel' gone + still-loads, the .md no-trace + the .tex no-generator-comment via the exporter's deterministic offline md→tex path on a REAL produced artifact. The zero-trace SCRUB LOGIC for every format is Pandoc-INDEPENDENT and engine-INDEPENDENT.
- references/honesty-framing.md is the locked, byte-pinned single source of the honest-framing copy; CONTRIBUTING.md forbids silent drift; the wording is transparency-only.
- The fixture .docx AND .pdf are byte-pinned against drift; both are genuine negative controls authored independently of their scrub logic; the PDF carries the non-'pensmith' 'Trace Sentinel' token in BOTH /Info AND XMP.
</success_criteria>

<output>
Create `.planning/phases/06-done-export-pipeline-zero-trace-gate/06-01-SUMMARY.md` when done.
</output>
</content>
