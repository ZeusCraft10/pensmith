---
phase: 06-done-export-pipeline-zero-trace-gate
plan: 05
type: execute
wave: 2
depends_on: ["06-01", "06-02", "06-03", "06-04"]
files_modified:
  - bin/cli/done.ts
  - bin/pensmith.ts
  - workflows/done.md
  - tests/tier-contract.test.ts
  - tests/export-gate.test.ts
autonomous: true
requirements: [DONE-01, DONE-03, DONE-09]

must_haves:
  truths:
    - "`pensmith done` runs whole-paper Pass 4 on the compiled .paper/DRAFT.md (DONE-01) and feeds any orphan claims into the export-confirmation gate"
    - "The humanizer wrap detects ~/.claude/skills/humanizer/; when absent it skips with a clear banner, scores the draft as-is, and NEVER fails the export (DONE-03)"
    - "The DONE-09 export-confirmation gate ALWAYS prompts (generic confirm even on a clean paper); when any UNSUPPORTED claim, orphan claim, or plagiarism hit is present it shows a per-issue summary first; only --yolo skips the gate"
    - "readSectionUnsupported parses the EXACT renderPass2Section ## Pass-2 table shape from on-disk section VERIFICATION.md, surfaces UNSUPPORTED rows to the gate, and FAILS SAFE on an unparseable Pass-2 table (treats it as issues-present → require confirmation) — it NEVER silently reports clean (HIGH-3)"
    - "The disk→gate feed is TESTED: a real renderPass2Section-shaped UNSUPPORTED row drives readSectionUnsupported in a unit test, AND a NON-`--yolo` gate integration test driven from on-disk Pass-2 data asserts the gate shows the per-issue summary and requires confirmation (does NOT auto-proceed); the existing --yolo test stays as the bypass case"
    - "done.ts is a thin orchestrator (delegates to bin/lib/*: runPass4, runPlagiarism, scoreHonesty, exportDraft) following bin/cli/compile.ts; stdout-only; 'done' stays the existing locked verb (no 17th verb)"
    - "Both tiers exercise done/export via the SAME workflow body + bin/lib path (CLI-only mcpTool, the compile precedent); tier-contract parity is asserted on the offline path"
  artifacts:
    - path: "bin/cli/done.ts"
      provides: "done verb thin orchestrator: Pass4 → plagiarism → honesty(before) → humanize → honesty(after) → DONE-09 gate → exportDraft (DONE-01/03/09); readSectionUnsupported fails safe on unparseable Pass-2"
      contains: "runDoneGate"
      min_lines: 90
    - path: "workflows/done.md"
      provides: "filled done workflow body with capability_check + numbered pipeline steps (mirrors workflows/compile.md)"
      contains: "capability_check"
    - path: "bin/pensmith.ts"
      provides: "done verb promoted from stub to real loader in REAL_VERB_LOADERS"
      contains: "done:"
    - path: "tests/export-gate.test.ts"
      provides: "HIGH-3 additions: readSectionUnsupported parse test over the renderPass2Section fixture + NON-yolo on-disk gate integration test + fail-safe-on-unparseable assertion"
      contains: "readSectionUnsupported"
  key_links:
    - from: "bin/cli/done.ts"
      to: "bin/lib/verify/pass4.ts + bin/lib/plagiarism.ts + bin/lib/honesty.ts + bin/lib/exporter.ts"
      via: "thin-orchestrator delegation (runPass4 / runPlagiarism / scoreHonesty / exportDraft)"
      pattern: "runPass4|runPlagiarism|scoreHonesty|exportDraft"
    - from: "bin/cli/done.ts readSectionUnsupported"
      to: ".paper/sections/*/VERIFICATION.md ## Pass-2 table (renderPass2Section shape)"
      via: "parse the exact 4-column **UNSUPPORTED**-bolded table pass2.ts emits; fail safe on desync"
      pattern: "Pass-2|UNSUPPORTED"
    - from: "bin/pensmith.ts REAL_VERB_LOADERS"
      to: "bin/cli/done.ts doneCommand"
      via: "done: () => import('./cli/done.js').then(m => m.doneCommand)"
      pattern: "done:"
    - from: "tests/tier-contract.test.ts"
      to: "workflows/done.md + bin/cli/done.ts"
      via: "CLI-only contract case + bespoke dual-tier parity test (compile precedent)"
      pattern: "done"
---

<objective>
Implement `bin/cli/done.ts` — the thin orchestrator for the `done` verb — wiring the full export pipeline: whole-paper Pass 4 audit (DONE-01) → plagiarism check → honesty score (before) → humanizer wrap (DONE-03, skip-clean if absent) → honesty score (after) → DONE-09 export-confirmation gate → exportDraft. Fill in `workflows/done.md` (the Phase-2 stub) with the capability_check + numbered pipeline so both tiers run the same body. Promote `done` from a dispatcher stub to a real loader in `bin/pensmith.ts`. Extend `tests/tier-contract.test.ts` with the done/export parity case, and ADD to `tests/export-gate.test.ts` the HIGH-3 coverage that proves the Pass-2 disk→gate feed actually works (unit parse + non-yolo on-disk integration + fail-safe).

Purpose: This is the keystone that assembles the Wave-1 modules into the `done` umbrella verb. DONE-09 is the SOLE escape valve reconciling the Core Value ("every citation supports its claim") with VRFY-07 (Pass 2/4 are advisory, never auto-block) — without the gate, the Core Value would force compile/export to block automatically. The gate ALWAYS prompts (generic confirm even when clean), shows a per-issue summary when UNSUPPORTED/orphan/plagiarism issues exist, and only --yolo skips it. The Pass-2 UNSUPPORTED feed is the load-bearing input to that gate — so it MUST be tested end-to-end from on-disk data (not just injected) and MUST fail safe rather than silently drop UNSUPPORTED rows on a parser desync (HIGH-3). `done` is the existing locked verb (verbs.ts already has it) — no 17th verb; done.ts is a thin orchestrator over bin/lib following compile.ts exactly.
Output: One CLI orchestrator + the filled workflow body + the verb-loader wiring + the tier-contract extension + the HIGH-3 gate-feed tests, turning the RED tests/export-gate.test.ts + tests/humanizer-wrap.test.ts from Wave 0 GREEN and keeping tier-contract green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-done-export-pipeline-zero-trace-gate/06-RESEARCH.md
@.planning/phases/06-done-export-pipeline-zero-trace-gate/06-PATTERNS.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

From bin/cli/compile.ts (the thin-orchestrator template — copy its shape: defineCommand, args incl. --yolo, 100% delegation to a runX lib, stdout-only, no console.*):
```
export const compileCommand = defineCommand({ meta: {...}, args: { yolo: {type:'boolean',default:false}, ... }, async run({ args }) { const result = await runCompile({...}); process.stdout.write(...); return {...}; } });
export default compileCommand;
```

From bin/lib/verify/pass4.ts (DONE-01 whole-paper Pass 4 — runPass4 already exists; offline placeholder under PENSMITH_NO_LLM):
```
export interface Pass4Result { paragraphIndex: number; totalSentences: number; claimsDetected: number; orphanCount: number; claims: Pass4ClaimResult[]; }
export async function runPass4(draftMd: string, opts: { n: number; scopeCapUsd?: number }): Promise<Pass4Result[]>;
```

From bin/lib/verify/pass2.ts — BOTH the type AND the EXACT on-disk markdown shape readSectionUnsupported must parse (HIGH-3 — pin this contract):
```
export type Pass2Verdict = 'SUPPORTED'|'PARTIAL'|'UNSUPPORTED'|'UNCLEAR';
export interface Pass2Result { citekey: string; claimSentence: string; verdict: Pass2Verdict; rationale: string; evidence: string; }
// renderPass2Section(results) emits EXACTLY (the parser MUST track this shape):
//   ## Pass-2 (claim support, advisory — LLM-judged)
//   <blank>
//   | Citekey | Claim Sentence | Verdict | Rationale |
//   |---------|---------------|---------|-----------|
//   | <citekey> | <claimSentence ≤60, pipe/newline-stripped> | **<VERDICT>** | <rationale ≤200, pipe/newline-stripped> |
// NOTE: the verdict cell is BOLDED with ** (e.g. **UNSUPPORTED**) and there is NO evidence column.
// Empty results emit: '## Pass-2 (claim support, advisory)\n\n_(no citations to judge)_\n'
```

From bin/lib/plagiarism.ts (06-02) + bin/lib/honesty.ts (06-03) + bin/lib/exporter.ts (06-04):
```
export async function runPlagiarism(draftMd: string, opts?: { maxPhrases?: number }): Promise<PlagiarismResult[]>;   // PlagiarismResult { phrase; matches: {url;title?}[] }
export function renderPlagiarismSection(results): string;
export async function scoreHonesty(text: string, config?: { honestyBackend?: string }): Promise<HonestyScore | null>;  // HonestyScore { aiProbability; classification; backend }
export function renderHonestyReport(before: number, after: number | null, backend: string): string;
export async function exportDraft(opts: { inputPath; outputDir; format: 'docx'|'pdf'|'latex'|'md'; paperRoot?; pandocPresent? }): Promise<ExportResult>;  // ExportResult { outputPath; format; pandocUsed; bibCopied }
```

From bin/lib/ecosystem-presence.ts (humanizer detection — false on this machine):
```
export function isHumanizerSkillPresent(): boolean;
```

From bin/lib/paths.ts:
```
export function paperDir(paperRoot?: string): string;   // .paper dir; DRAFT.md at join(paperDir(root),'DRAFT.md'); sections under join(paperDir(root),'sections')
```

From bin/lib/prompts.ts / bin/lib/prompts/clack.ts (the Tier-aware confirm facade — DONE-09 gate; --yolo skips calling it):
```
import { askQuestion } from '../lib/prompts.js';   // returns { value: boolean } for a confirm question
const answer = await askQuestion({ id: 'export-confirm', kind: 'confirm', label: 'Export the paper?', default: true });
```
The gate function MUST accept an INJECTABLE approver (for tests) — design runDoneGate to take `approve: () => Promise<boolean>` so tests can pass a deterministic approver and assert it is/ isn't called (the Wave-0 export-gate test depends on this).

From bin/pensmith.ts (REAL_VERB_LOADERS — the ONLY edit point to promote a verb from stub to real; compile precedent line 52):
```
const REAL_VERB_LOADERS: Partial<Record<Ux02Verb, () => Promise<AnyCommandDef>>> = { ..., compile: () => import('./cli/compile.js').then((m) => m.compileCommand) };
// add: done: () => import('./cli/done.js').then((m) => m.doneCommand)
```

From tests/tier-contract.test.ts (CONTRACT_CASES — done is CLI-only like compile; bespoke dual-tier parity test, NOT the generic single-section loop):
```
interface ContractCase { name: string; mcpTool: string | null; cliArgs: string[]; verbFile: string; expectedArtifact: string; }
// compile case: { name:'compile', mcpTool:null, cliArgs:['compile','--yolo'], verbFile:'bin/cli/compile.ts', expectedArtifact:'.paper/DRAFT.md' }
// runCliInDir(args, cwd) spawns the CLI with PENSMITH_NO_LLM=1; cases needing multi-section setup are skipped in the generic loop and get a bespoke test (see `if (tc.name === 'compile') continue;`).
```
'done' has NO pensmith_done MCP tool (the Tier-1 surface is workflows/done.md delegating to the SAME bin/cli/done.ts → bin/lib path as the CLI) — documented asymmetry, locked-16 verbs preserved (compile precedent).

From tests/fixtures/section-pass2-unsupported/VERIFICATION.md (committed in 06-01 — the on-disk renderPass2Section-shaped Pass-2 fixture for the HIGH-3 feed tests):
```
// A real section VERIFICATION.md whose ## Pass-2 table carries one **UNSUPPORTED** row + one **SUPPORTED** row,
// matching renderPass2Section's exact emitted shape. The readSectionUnsupported parse test + the non-yolo
// on-disk gate integration test both consume this fixture.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the DONE-09 gate (runDoneGate) + humanizer wrap (runHumanizer) + whole-paper Pass 4 wiring</name>
  <read_first>
    - bin/cli/compile.ts (thin-orchestrator shape, args, stdout-only, runCompile delegation)
    - bin/lib/verify/pass4.ts (runPass4 signature + offline placeholder behavior)
    - bin/lib/prompts.ts + bin/lib/prompts/clack.ts (askQuestion confirm facade lines 74-79)
    - bin/lib/ecosystem-presence.ts (isHumanizerSkillPresent)
    - tests/export-gate.test.ts + tests/humanizer-wrap.test.ts (the RED assertions: gate fires on issues with injected approver=false → exported:false; --yolo → gateSkipped:true, approver never called; clean paper → generic confirm still runs; humanizer absent → null/banner/no-throw)
    - .planning/phases/06-done-export-pipeline-zero-trace-gate/06-RESEARCH.md (Pattern 6 DONE-09 gate logic lines 346-379; Pattern 5 humanizer wrapping lines 326-344; Pitfall 7 humanizer-absent hard-exit lines 444-448)
    - .planning/phases/06-done-export-pipeline-zero-trace-gate/06-PATTERNS.md (done-gate analog lines 554-576; humanizer-absent analog lines 580-600; @clack confirm gate lines 710-727)
  </read_first>
  <files>bin/cli/done.ts</files>
  <behavior>
    - runHumanizer(draftMd, paperRoot): isHumanizerSkillPresent() false → stdout banner 'humanizer skill not found at ~/.claude/skills/humanizer/ — skipping humanize step.' + return null (export proceeds on DRAFT.md). Present (Tier 1) → invoke humanizer, write .paper/FINAL.md, return its path. Never throws.
    - collectGateIssues({ pass2Results, pass4Results, plagiarismResults }): groups UNSUPPORTED Pass2 + orphanCount>0 Pass4 + plagiarism-hit results; returns { unsupported[], orphanClaims[], plagiarismHits[], hasIssues }.
    - runDoneGate({ issues, yolo, approve }): yolo true → { gateSkipped: true } and approve NEVER called. Else: if hasIssues → render per-issue summary to stdout first; ALWAYS call approve() (generic confirm even when clean); approve()===false → { exported: false }; true → { exported: true }.
    - All three never throw.
  </behavior>
  <action>
    Create bin/cli/done.ts header comment mirroring compile.ts: "THIN ORCHESTRATOR — delegates to bin/lib/* (runPass4, runPlagiarism, scoreHonesty, exportDraft). 'done' IS one of the locked UX-02 16 verbs (no new verb) — promotes the Phase-2 dispatcher stub to a real loader. stdout-only (no console.*). DONE-09 gate is the SOLE escape valve reconciling Core Value with VRFY-07 (advisory Pass 2/4 never auto-block)."

    Implement and export (these are the gate/humanize primitives the orchestrator + tests use):
      `export async function runHumanizer(draftMd: string, paperRoot?: string): Promise<string | null>` per the behavior block — isHumanizerSkillPresent() gate first; absent → banner + null (never throw). Present-path (Tier 1 Task invocation) may be a documented best-effort: when no Task transport is wired (Tier-2 / current era), treat present-but-no-transport the same as the skip path with a distinct banner so the export still proceeds (06-RESEARCH A7). Return the FINAL.md path only when a real humanized artifact was written.

      `export interface GateIssues { unsupported: Pass2Result[]; orphanClaims: Pass4Result[]; plagiarismHits: PlagiarismResult[]; hasIssues: boolean; }`
      `export function collectGateIssues(input: { pass2Results: Pass2Result[]; pass4Results: Pass4Result[]; plagiarismResults: PlagiarismResult[] }): GateIssues` — filter UNSUPPORTED, orphanCount>0, matches.length>0; hasIssues = any non-empty.

      `export interface DoneGateResult { exported: boolean; gateSkipped?: boolean; }`
      `export async function runDoneGate(input: { issues: GateIssues; yolo: boolean; approve: () => Promise<boolean> }): Promise<DoneGateResult>` per the behavior block. Render the per-issue summary (counts + a few example citekeys/phrases, table-cell-safe) to stdout when hasIssues, BEFORE calling approve(). ALWAYS call approve() in the non-yolo path (generic confirm even when clean — PRD §7.9). The real `approve` supplied by the CLI run() wraps `askQuestion({ kind:'confirm', ... })`; tests inject a deterministic approve.

      Add the DONE-01 whole-paper Pass 4 helper used by run(): read `.paper/DRAFT.md` (paperDir), call `runPass4(draftMd, { n: 0 })`, aggregate orphan results. (Pass 2 UNSUPPORTED is read from section VERIFICATION.md ## Pass-2 tables in Task 2.)
  </action>
  <verify>
    <automated>node --import tsx --test tests/export-gate.test.ts tests/humanizer-wrap.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/export-gate.test.ts GREEN: with UNSUPPORTED + orphan + plagiarism issues and injected approve()=false → { exported: false } and approve WAS called (after a per-issue summary printed); yolo:true → { gateSkipped: true } and approve NEVER called (test injects a throwing approver); zero issues + approve()=true → generic confirm STILL runs (approve called once) and { exported: true }.
    - tests/humanizer-wrap.test.ts GREEN: runHumanizer with the skill absent does not throw, returns null, and writes a 'humanizer skill not found' banner to stdout.
    - collectGateIssues correctly buckets the three issue types; hasIssues reflects presence.
    - `npm run lint` (stdout-only, no console.*) + `tsc --noEmit` pass.
  </acceptance_criteria>
  <done>The DONE-09 gate (always-confirm, per-issue summary, --yolo-only skip, injectable approver) + DONE-03 humanizer wrap (skip-clean, never-throws) + DONE-01 whole-paper Pass 4 helper land; the gate + humanizer RED suites go GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the doneCommand orchestrator + the FAIL-SAFE section Pass-2 UNSUPPORTED reader + VERIFICATION.md emission; promote the verb loader; add the HIGH-3 disk→gate feed tests</name>
  <read_first>
    - bin/cli/compile.ts (full file — the run() delegation shape, args, stdout outcome lines)
    - bin/lib/verify/pass2.ts (renderPass2Section lines 298-315 — the EXACT ## Pass-2 table shape written to section VERIFICATION.md; the bolded **UNSUPPORTED** verdict cell is what to match)
    - tests/fixtures/section-pass2-unsupported/VERIFICATION.md (the committed on-disk fixture from 06-01 — the parse + integration tests consume it)
    - tests/export-gate.test.ts (the existing Wave-0 gate-logic tests — ADD the HIGH-3 tests here; do NOT remove the existing injected-data + --yolo cases)
    - tests/compile-refuse.test.ts (mkdtemp .paper fixture-root pattern — used by the non-yolo on-disk integration test to seed a section VERIFICATION.md)
    - bin/pensmith.ts (REAL_VERB_LOADERS lines 37-53 — the single edit point; compile loader line 52 is the template)
    - bin/lib/plagiarism.ts + bin/lib/honesty.ts + bin/lib/exporter.ts (the three lib entrypoints to delegate to)
    - 06-RESEARCH.md (Open Question 2 honesty/VERIFICATION.md storage lines 738-742 — write whole-paper .paper/VERIFICATION.md mirroring .paper/COMPILE-REPORT.md)
  </read_first>
  <files>bin/cli/done.ts, bin/pensmith.ts, tests/export-gate.test.ts</files>
  <behavior>
    - doneCommand: defineCommand with args { yolo (default false), format (default 'docx'), raw (default false, skips humanize) }. run() resolves paperRoot=process.cwd(), runs the pipeline: Pass4(DRAFT.md) → runPlagiarism(DRAFT.md) → scoreHonesty(before) → (raw? skip : runHumanizer) → scoreHonesty(after, on FINAL.md if produced) → collectGateIssues (Pass2 UNSUPPORTED read from section VERIFICATION.md + Pass4 orphans + plagiarism) → runDoneGate(real askQuestion approver) → if exported: exportDraft({ inputPath: FINAL.md ?? DRAFT.md, outputDir, format, paperRoot }). Writes the honesty report + plagiarism section + Pass-4 summary to .paper/VERIFICATION.md.
    - readSectionUnsupported(paperRoot): scans .paper/sections/*/VERIFICATION.md for ## Pass-2 table rows with the bolded **UNSUPPORTED** verdict, returns Pass2Result[]. FAIL-SAFE (HIGH-3): a VERIFICATION.md that HAS a ## Pass-2 section whose table is present-but-UNPARSEABLE (header/separator/row shape does not match the pinned renderPass2Section contract) MUST surface a synthetic UNSUPPORTED sentinel row (so hasIssues becomes true and the gate requires confirmation) — it must NEVER silently return [] for an unparseable-but-present table. A genuinely ABSENT Pass-2 section (no ## Pass-2 heading at all) or a missing file returns [] (nothing to report). Distinguish "no Pass-2 section" (clean) from "Pass-2 section present but unparseable" (fail safe → issue).
    - Gate rejection → stdout 'export cancelled by user', return { ok:false }. Export success → stdout the output path, return { ok:true, ...result }.
  </behavior>
  <action>
    Add to bin/cli/done.ts:

    Pin the Pass-2 table contract as a module-level constant the parser uses (HIGH-3 — so a future renderPass2Section change that desyncs is caught): the exact header line `| Citekey | Claim Sentence | Verdict | Rationale |` and the bolded-verdict cell convention `**<VERDICT>**`. Add a comment referencing bin/lib/verify/pass2.ts renderPass2Section as the single writer of this shape.

    `readSectionUnsupported(paperRoot: string): Pass2Result[]` — list section dirs under paperDir/sections, read each VERIFICATION.md, locate the `## Pass-2` heading. If no `## Pass-2` heading is present → contribute nothing (clean). If the heading IS present:
      - Parse the markdown table beneath it expecting the pinned header `| Citekey | Claim Sentence | Verdict | Rationale |` + a dashed separator + data rows; for each data row, read the verdict cell (strip the `**` bold markers) and collect rows whose verdict is `UNSUPPORTED` into Pass2Result[] (citekey + claimSentence + verdict at minimum; rationale best-effort from the row; evidence '' — renderPass2Section omits it).
      - The `_(no citations to judge)_` empty-section marker → contribute nothing (clean — explicitly recognized).
      - FAIL SAFE: if the `## Pass-2` heading is present but the table header does NOT match the pinned contract, or rows cannot be parsed into the expected 4 cells, push a synthetic `{ citekey: '<unparseable>', claimSentence: 'Pass-2 table in <section>/VERIFICATION.md could not be parsed — failing safe', verdict: 'UNSUPPORTED', rationale: 'parser/writer contract desync', evidence: '' }` so hasIssues becomes true and the gate requires confirmation. NEVER let an unparseable-but-present Pass-2 table yield a silent clean.
      - Defensive on I/O only: a missing sections dir or unreadable file is skipped (returns nothing for that section); it must never THROW out of readSectionUnsupported. (Note the distinction from the fail-safe above: missing/absent = clean; present-but-unparseable = issue.)

    `export const doneCommand = defineCommand({ ... })` mirroring compile.ts:
      - args: yolo (boolean, default false, 'Skip the export confirmation gate.'), format (string, default 'docx', 'Export format: docx | pdf | latex | md.'), raw (boolean, default false, 'Skip the humanizer step.').
      - run({ args }): paperRoot = process.cwd(); draftPath = join(paperDir(paperRoot),'DRAFT.md'); read it (missing → stdout error + { ok:false }, never throw). Run the pipeline per the behavior block. Honesty: scoreHonesty before; if !raw run runHumanizer; scoreHonesty after on FINAL.md when produced (else after=null). Build the report via renderHonestyReport(before.aiProbability ?? skip, after?.aiProbability ?? null, backend) — when scoreHonesty returns null (no key), emit the skip banner instead of a fabricated percent. collectGateIssues({ pass2Results: readSectionUnsupported(paperRoot), pass4Results, plagiarismResults }). gateResult = runDoneGate({ issues, yolo: args.yolo===true, approve: async () => (await askQuestion({ id:'export-confirm', kind:'confirm', label:'Export the paper?', default:true })).value }). If !gateResult.exported && !gateResult.gateSkipped → stdout 'pensmith done: export cancelled by user.' + { ok:false }. Else exportDraft({ inputPath: finalPath ?? draftPath, outputDir: paperDir(paperRoot), format: validated format, paperRoot }); write .paper/VERIFICATION.md (honesty report + renderPlagiarismSection + a Pass-4 orphan summary) via atomicWriteFile; stdout `pensmith done: exported ${result.outputPath}`; return { ok:true, ...result }.
      - stdout-only; no console.*.
    `export default doneCommand;`

    In bin/pensmith.ts REAL_VERB_LOADERS: add `done: () => import('./cli/done.js').then((m) => m.doneCommand),` (the single edit point; mirror the compile entry's comment noting 'done' is one of the locked 16 — promoting the Phase-2 stub, no new verb).

    ADD to tests/export-gate.test.ts (HIGH-3 — keep the existing Wave-0 injected-data + --yolo cases intact; these are NEW tests in the same file):
      - readSectionUnsupported parse test (unit): copy tests/fixtures/section-pass2-unsupported/VERIFICATION.md into an mkdtemp `.paper/sections/01-intro/VERIFICATION.md`, call `readSectionUnsupported(tmpPaperRoot)`, and assert the UNSUPPORTED row is detected (>=1 Pass2Result with verdict 'UNSUPPORTED' and the fixture's citekey) AND the SUPPORTED row is NOT included (the parser filters by verdict, proving it reads the bolded verdict cell, not just row count). This exercises the disk→parser path the Wave-0 tests never touched.
      - readSectionUnsupported fail-safe test (unit): write a `.paper/sections/02-x/VERIFICATION.md` that HAS a `## Pass-2` heading but a DELIBERATELY MALFORMED table (e.g. a 3-column table, or a header that does not match the pinned contract). Assert `readSectionUnsupported` returns a NON-empty array carrying the synthetic `<unparseable>` UNSUPPORTED sentinel (NOT []). Also assert that a section whose VERIFICATION.md has NO `## Pass-2` heading contributes nothing (clean). This proves the parser fails safe on desync and never silently reports clean.
      - NON-`--yolo` on-disk gate integration test: seed an mkdtemp `.paper` with a DRAFT.md + a section VERIFICATION.md containing the UNSUPPORTED row (the fixture); drive the gate from that on-disk Pass-2 data via collectGateIssues({ pass2Results: readSectionUnsupported(root), ... }) + runDoneGate({ issues, yolo:false, approve: <injected spy returning false> }); assert (a) the injected approver WAS called (the gate did NOT auto-proceed), (b) a per-issue summary mentioning the UNSUPPORTED citekey was written to stdout BEFORE the approver was called (capture stdout), and (c) result.exported === false. This is the non-yolo path the existing --yolo integration test never covered.
  </action>
  <verify>
    <automated>node --import tsx --test tests/export-gate.test.ts && node --import tsx --test tests/cli-verbs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - doneCommand exists, is registered in REAL_VERB_LOADERS, and `pensmith done --help` resolves the real command (not the stub) — cli-verbs.test.ts still passes (16 verbs intact, no 17th).
    - readSectionUnsupported parses ## Pass-2 **UNSUPPORTED** rows from section VERIFICATION.md files (filtering out **SUPPORTED**) and returns Pass2Result[]; a missing/absent Pass-2 section → empty array, no throw.
    - readSectionUnsupported FAILS SAFE: a present-but-unparseable ## Pass-2 table yields a synthetic UNSUPPORTED sentinel (NON-empty), never a silent clean — asserted by the new fail-safe test.
    - tests/export-gate.test.ts contains the three NEW HIGH-3 tests (parse, fail-safe, non-yolo on-disk integration) AND retains the original injected-data + --yolo cases; all GREEN.
    - The orchestrator delegates 100% to bin/lib (runPass4/runPlagiarism/scoreHonesty/exportDraft) — no business logic inline beyond wiring; stdout-only.
    - A gate rejection prints 'export cancelled' and returns { ok:false }; a clean export prints the output path.
    - `npm run lint` + `tsc --noEmit` pass; the existing suite has no regressions (`npm test`).
  </acceptance_criteria>
  <done>The done orchestrator assembles the full pipeline (Pass4 → plagiarism → honesty before/after → humanize → DONE-09 gate → export + VERIFICATION.md emission), reads section Pass-2 UNSUPPORTED for the gate with a FAIL-SAFE parser pinned to the renderPass2Section contract, and is promoted to a real verb loader. The HIGH-3 disk→gate feed is now tested end-to-end (unit parse + fail-safe + non-yolo on-disk integration) — keeping the locked-16-verb invariant.</done>
</task>

<task type="auto">
  <name>Task 3: Fill workflows/done.md (capability_check + pipeline body) and extend tier-contract with the done/export parity case</name>
  <read_first>
    - workflows/compile.md (the workflow-body structure to mirror: capability_check, Overview, Outputs, numbered Body steps)
    - workflows/done.md (the Phase-2 stub to replace)
    - tests/tier-contract.test.ts (the compile contract case lines 437-450 + the `if (tc.name === 'compile') continue;` bespoke-case pattern + runCliInDir + the multi-section seed helpers)
    - CONTRIBUTING.md (the D-24 rule: every workflow body added in any later phase MUST add a tier-contract case — done.md is added here, so its case is added here)
    - .planning/phases/06-done-export-pipeline-zero-trace-gate/06-PATTERNS.md (workflows/done.md analog lines 380-412)
  </read_first>
  <files>workflows/done.md, tests/tier-contract.test.ts</files>
  <action>
    Replace the workflows/done.md stub with a full body mirroring workflows/compile.md:
      - Title + one-line summary noting the NON-NEGOTIABLE: no exported doc carries a pensmith metadata trace, in ANY format (docx ZIP entries, pdf /Info + XMP, tex, md).
      - `<capability_check>` block: required Pandoc (for non-md formats) + humanizer skill (DONE-03); degrade_if_missing — no Pandoc → markdown-only export; no PDF engine → markdown-only fallback for pdf; no humanizer skill → skip humanize + no 'after' honesty score; no GPTZERO_API_KEY → skip honesty score. (Mirror the W4 closed-vocabulary capability_check shape the manifest validator expects — copy compile.md's block structure.)
      - Overview + Outputs (the exported file in outputDir + bundled CITATIONS.bib + .paper/VERIFICATION.md honesty/plagiarism/Pass-4 sections).
      - Numbered Body steps matching the orchestrator: 1 runPass4 on .paper/DRAFT.md (DONE-01); 2 runPlagiarism (DONE-02, advisory); 3 honesty score before (DONE-04, framed verbatim, skip if no key); 4 humanize (DONE-03, skip-clean if absent); 5 honesty score after; 6 DONE-09 export-confirmation gate (per-issue summary on UNSUPPORTED/orphan/plagiarism — note the Pass-2 feed fails safe on an unparseable table; generic confirm otherwise; --yolo skips); 7 exportDraft → mandatory per-format zero-trace scrub (zeroTracePatch for docx, zeroTracePdf for pdf) → bundle .bib (DONE-06/07/08).

    Extend tests/tier-contract.test.ts (the D-24 obligation for the new workflow body):
      - Add a 'done' ContractCase to CONTRACT_CASES: { name:'done', mcpTool:null (no pensmith_done MCP tool — Tier-1 surface is workflows/done.md delegating to the SAME bin/cli/done.ts → bin/lib path, the compile precedent; documented asymmetry, locked-16 preserved), cliArgs:['done','--yolo'], verbFile:'bin/cli/done.ts', expectedArtifact: the exported output under .paper/ (e.g. '.paper/DRAFT.md' produced by the md-only fallback, since Pandoc is absent in CI — pick the deterministic artifact the offline run actually produces) }.
      - Skip the generic single-section loop for 'done' (add `if (tc.name === 'done') continue;` alongside the compile skip) and add a bespoke dual-tier parity test: seed a temp .paper with a compiled DRAFT.md + CITATIONS.bib + at least one section VERIFICATION.md (so the gate has Pass-2 data), run `pensmith done --yolo` via runCliInDir with PENSMITH_NO_LLM=1, and assert the offline run (a) exits 0, (b) produces a deterministic export artifact in the temp .paper, (c) the exported/produced file contains NO 'pensmith' string (the zero-trace contract surfaced at the verb level — for the md fallback this is a plain string scan; the .docx ZIP scan + .pdf /Info+XMP scan live in tests/zero-trace-export.test.ts). Since there is no MCP done tool, the parity assertion is that the SAME workflow body + bin/lib path drives both tiers (assert workflows/done.md exists and the CLI uses bin/cli/done.ts) — mirror exactly how the compile bespoke case documents its CLI-only parity.
  </action>
  <verify>
    <automated>npm run test:tier-contract</automated>
  </verify>
  <acceptance_criteria>
    - workflows/done.md is a full body (no '(stub)') with a capability_check block (Pandoc + PDF-engine + humanizer + GPTZERO_API_KEY degrade paths) and 7 numbered pipeline steps; the manifest validator (workflows closed-vocabulary check) still passes.
    - tests/tier-contract.test.ts has a 'done' CLI-only case + a bespoke parity test; `npm run test:tier-contract` is GREEN.
    - The bespoke done parity test runs offline (PENSMITH_NO_LLM=1, Pandoc absent), exits 0, produces a deterministic artifact, and asserts no 'pensmith' string in the produced file.
    - `npm run lint` + `tsc --noEmit` pass; full `npm test` has no regressions.
  </acceptance_criteria>
  <done>The done workflow body is filled (both tiers run the same pipeline) and its tier-contract obligation (D-24) is satisfied with a CLI-only case + bespoke offline parity test asserting a trace-free produced artifact — closing the phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| advisory Pass 2/4 + plagiarism issues → export decision | The gate must surface issues and require confirm; it must NOT auto-block (VRFY-07) yet must NOT silently export over issues (Core Value) — only --yolo bypasses |
| section VERIFICATION.md (parsed) → gate issue set | Untrusted on-disk markdown parsed for UNSUPPORTED rows; a desync between renderPass2Section (writer) and readSectionUnsupported (parser) must FAIL SAFE (treat as issue), never silently drop UNSUPPORTED rows |
| humanizer skill / Task transport → FINAL.md | A missing skill must skip cleanly; the export must proceed on DRAFT.md, never fail |
| produced export artifact → user | Must carry zero pensmith trace (verb-level surface of DONE-07, every format) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Elevation of Privilege | DONE-09 gate bypass (exporting over issues without confirm) | mitigate | runDoneGate ALWAYS calls approve() in the non-yolo path (generic confirm even when clean); per-issue summary precedes confirm when issues exist; ONLY yolo:true skips; export-gate test asserts approve is/ isn't called per branch — INCLUDING a non-yolo on-disk integration test (HIGH-3) proving the gate does not auto-proceed over a real UNSUPPORTED row |
| T-06-05-02 | Tampering | malformed / desynced section VERIFICATION.md silently dropping UNSUPPORTED rows | mitigate | readSectionUnsupported is pinned to the renderPass2Section contract and FAILS SAFE — a present-but-unparseable ## Pass-2 table yields a synthetic UNSUPPORTED sentinel (require confirmation), never a silent clean; missing/absent Pass-2 = clean; I/O errors skipped, never thrown; unit tests cover parse, fail-safe, and the non-yolo feed (HIGH-3) |
| T-06-05-03 | Denial of Service | humanizer-absent (or no Task transport) hard-exit failing export | mitigate | runHumanizer gates on isHumanizerSkillPresent() + treats no-transport as skip-clean; banner + null + export proceeds on DRAFT.md; never throws (Pitfall 7) |
| T-06-05-04 | Information Disclosure | produced export carrying a pensmith trace at the verb level | mitigate | exportDraft runs the mandatory per-format scrub (zeroTracePatch for docx, zeroTracePdf for pdf — 06-04); the bespoke tier-contract test scans the produced offline artifact for 'pensmith' → 0; .docx ZIP + .pdf /Info+XMP scans in tests/zero-trace-export.test.ts |
| T-06-05-05 | Spoofing | misframed honesty score (undetectability claim) leaking via the report | mitigate | renderHonestyReport (06-03) renders the note VERBATIM from the locked references/honesty-framing.md; done.ts only passes percentages, never composes framing prose |
| T-06-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (jszip + pdf-lib added in 06-01); nothing to slopcheck here |
</threat_model>

<verification>
- `node --import tsx --test tests/export-gate.test.ts tests/humanizer-wrap.test.ts` — GREEN (gate branches + humanizer skip-clean + the HIGH-3 readSectionUnsupported parse + fail-safe + non-yolo on-disk integration tests).
- `node --import tsx --test tests/cli-verbs.test.ts` — GREEN (16 verbs intact; done is a real loader, no 17th verb).
- `npm run test:tier-contract` — GREEN (done CLI-only case + bespoke offline parity test; produced artifact trace-free).
- `npm run lint` + `tsc --noEmit` — GREEN (thin orchestrator, stdout-only, no console.*).
- `npm test` — full suite GREEN, no regressions; `npm run check` (lint + typecheck + build + tier-contract + tests + manifests) GREEN before /gsd:verify-work.
- Manual (documented check — the scrub + feed logic is already CI-gated): live `pensmith done` with Pandoc + real keys, all formats, grep for 'pensmith' → 0.
</verification>

<success_criteria>
- bin/cli/done.ts is a thin orchestrator wiring Pass4 → plagiarism → honesty(before) → humanize → honesty(after) → DONE-09 gate → exportDraft, delegating 100% to bin/lib; stdout-only; 'done' stays the existing locked verb.
- DONE-01: whole-paper Pass 4 runs on .paper/DRAFT.md and feeds orphan claims to the gate.
- DONE-03: humanizer wrap skips cleanly with a banner when absent and never fails export.
- DONE-09: the gate always prompts (generic confirm even when clean), shows a per-issue summary on UNSUPPORTED/orphan/plagiarism, and only --yolo skips; the Pass-2 feed (readSectionUnsupported) is pinned to the renderPass2Section contract, FAILS SAFE on desync, and is tested end-to-end (unit parse + fail-safe + non-yolo on-disk integration).
- workflows/done.md is filled; bin/pensmith.ts promotes done to a real loader; tier-contract has the done parity case (CLI-only, compile precedent) asserting a trace-free produced artifact.
- export-gate (incl. HIGH-3 tests) + humanizer-wrap + cli-verbs + tier-contract tests GREEN; lint + typecheck pass.
</success_criteria>

<output>
Create `.planning/phases/06-done-export-pipeline-zero-trace-gate/06-05-SUMMARY.md` when done.
</output>
</content>
