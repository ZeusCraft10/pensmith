---
phase: 05-verifier-completeness-pass-2-pass-4
plan: 05
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03", "05-04"]
files_modified:
  - bin/lib/prompt-loader.ts
autonomous: true
requirements: [VRFY-03, VRFY-06]

must_haves:
  truths:
    - "claim-support and orphan-label EXPECTED_PROMPT_HASHES entries hold REAL SHA-256 values (no __PENDING_HASH_ sentinels remain for these slugs)"
    - "The real hashes in prompt-loader.ts EXACTLY match the byte-pins already carried in tests/repo-files.test.ts PENDING_HASH_PINS (WN-3 single-source-of-truth lockstep)"
    - "loadPrompt('claim-support') and loadPrompt('orphan-label') succeed WITHOUT PENSMITH_ALLOW_PENDING_PROMPT_HASHES=1 (runtime drift detection restored)"
  artifacts:
    - path: "bin/lib/prompt-loader.ts"
      provides: "real SHA-256 re-pin for claim-support + orphan-label (sentinels replaced)"
      contains: "claim-support"
  key_links:
    - from: "bin/lib/prompt-loader.ts EXPECTED_PROMPT_HASHES"
      to: "tests/repo-files.test.ts PENDING_HASH_PINS"
      via: "WN-3 atomic re-pin — both surfaces carry the identical real hash after this commit"
      pattern: "claim-support|orphan-label"
---

<objective>
Close the WN-3 sentinel-then-real loop for Phase 5. Replace the two `__PENDING_HASH_claim-support__` / `__PENDING_HASH_orphan-label__` sentinels in `bin/lib/prompt-loader.ts` `EXPECTED_PROMPT_HASHES` with the real SHA-256 values — the SAME hashes `tests/repo-files.test.ts` `PENDING_HASH_PINS` has carried since Plan 05-01 (the prompt bodies are byte-stable from creation). After this single atomic re-pin, both surfaces agree and `loadPrompt('claim-support')` / `loadPrompt('orphan-label')` succeed at runtime WITHOUT the `PENSMITH_ALLOW_PENDING_PROMPT_HASHES=1` bypass — restoring full runtime drift detection. This mirrors the Phase 4 smoother re-pin precedent exactly (Plan 04-05 Task 4).

Purpose: VRFY-03 + VRFY-06 phase close. Until the sentinels are replaced, the advisory passes only resolve their prompts under the pending bypass; the re-pin is what makes Pass 2/4 production-real and re-arms the drift safety net.
Output: `bin/lib/prompt-loader.ts` with the two slugs holding real SHA-256 values matching repo-files; the full suite GREEN without the pending bypass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-verifier-completeness-pass-2-pass-4/05-RESEARCH.md
@.planning/phases/05-verifier-completeness-pass-2-pass-4/05-PATTERNS.md
@bin/lib/prompt-loader.ts

<interfaces>
<!-- Extracted from codebase — executor re-pins against these directly. -->

From bin/lib/prompt-loader.ts EXPECTED_PROMPT_HASHES (lines 91-117 — the two Phase-5 slugs are sentinels after Plan 05-01; the Phase-4 smoother entry on line 116 is the EXACT precedent for the post-re-pin shape: real hash + a comment noting WN-3 lockstep):
```
'claim-support': '__PENDING_HASH_claim-support__',   // ← replace with real SHA-256
'orphan-label':  '__PENDING_HASH_orphan-label__',    // ← replace with real SHA-256
```

From tests/repo-files.test.ts PENDING_HASH_PINS (the byte-pins added by Plan 05-01 already carry the REAL hashes — this is the source of truth to copy into the loader; the per-pin test loop @ line 258 recomputes the on-disk SHA-256 and asserts equality):
```
{ slug: 'claim-support', path: 'templates/prompts/claim-support.md', decision: 'Phase 5 D-12', hash: '<REAL_64_HEX>' },
{ slug: 'orphan-label',  path: 'templates/prompts/orphan-label.md',  decision: 'Phase 5 D-12', hash: '<REAL_64_HEX>' },
```

WN-3 re-pin rule (from prompt-loader.ts header + Phase-4 smoother precedent): the re-pin commit replaces the loader sentinels with the SAME real hashes repo-files already holds. Compute each hash from the on-disk file to confirm it matches repo-files BEFORE editing the loader:
```
node -e "console.log(require('node:crypto').createHash('sha256').update(require('node:fs').readFileSync('templates/prompts/claim-support.md')).digest('hex'))"
node -e "console.log(require('node:crypto').createHash('sha256').update(require('node:fs').readFileSync('templates/prompts/orphan-label.md')).digest('hex'))"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomic WN-3 re-pin — replace the two Phase-5 sentinels with real SHA-256 values matching repo-files</name>
  <read_first>
    - bin/lib/prompt-loader.ts (EXPECTED_PROMPT_HASHES lines 91-117; the smoother re-pin comment line 111-116 is the precedent to mirror; the sentinel-bypass logic lines 165-175 — confirm a real hash takes the non-sentinel validation path)
    - tests/repo-files.test.ts (PENDING_HASH_PINS lines 235-256 — the claim-support + orphan-label entries added by Plan 05-01 carry the REAL hashes; the per-pin loop line 258-271 + the loader/PIN cross-check loop line 271+)
    - .planning/phases/05-verifier-completeness-pass-2-pass-4/05-PATTERNS.md (§"WN-3 Sentinel Registration" Step 3 — atomic re-pin both surfaces)
    - .planning/phases/04-breadth-n-sections-compile-wave-scheduling/04-05-PLAN.md (the smoother re-pin task — the exact precedent for this operation)
  </read_first>
  <files>bin/lib/prompt-loader.ts</files>
  <action>
    Recompute the on-disk SHA-256 of templates/prompts/claim-support.md and templates/prompts/orphan-label.md with the node -e one-liners from the &lt;interfaces&gt; block. Confirm each computed hash EXACTLY equals the corresponding hash already in tests/repo-files.test.ts PENDING_HASH_PINS (added by Plan 05-01). If they differ, STOP — it means the prompt file drifted since Plan 05-01; do not paper over it (re-pin both surfaces to the new hash only if the drift is intentional, otherwise restore the prompt file).

    In bin/lib/prompt-loader.ts EXPECTED_PROMPT_HASHES: replace `'claim-support': '__PENDING_HASH_claim-support__'` with `'claim-support': '<REAL_64_HEX>'` and `'orphan-label': '__PENDING_HASH_orphan-label__'` with `'orphan-label': '<REAL_64_HEX>'`, using the confirmed real hashes. Update the Phase-5 comment block (added by Plan 05-01) to mirror the Phase-4 smoother comment shape (line 111-116): note these are now re-pinned to the real SHA-256 that tests/repo-files.test.ts already carries (WN-3 lockstep — both surfaces agree) and that loadPrompt now succeeds WITHOUT PENSMITH_ALLOW_PENDING_PROMPT_HASHES.

    Do NOT change tests/repo-files.test.ts in this plan — its PENDING_HASH_PINS already hold the real hashes from Plan 05-01 (WN-3: repo-files is real-from-creation; only the loader carried a sentinel). This task touches ONLY prompt-loader.ts. Do NOT modify any prompt file body, any pass module, or verify.ts.
  </action>
  <verify>
    <automated>node --import tsx --test tests/repo-files.test.ts && PENSMITH_NO_LLM=1 node --import tsx --test tests/known-bad-pass2.test.ts tests/known-bad-pass4.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - EXPECTED_PROMPT_HASHES for 'claim-support' and 'orphan-label' are 64-hex SHA-256 strings (no '__PENDING_HASH_' remains): `grep -E "__PENDING_HASH_(claim-support|orphan-label)__" bin/lib/prompt-loader.ts` returns nothing.
    - The loader hashes EXACTLY equal the repo-files PENDING_HASH_PINS hashes for both slugs (WN-3 lockstep — the repo-files cross-check loop that compares the two surfaces PASSES).
    - tests/repo-files.test.ts is fully GREEN.
    - The Pass 2/4 unit suites are GREEN WITHOUT setting PENSMITH_ALLOW_PENDING_PROMPT_HASHES=1 (the verify command in the acceptance does not export it) — loadPrompt resolves the slugs via the real hash path.
  </acceptance_criteria>
  <done>Phase-5 prompt slugs re-pinned to real SHA-256 atomically; both WN-3 surfaces agree; runtime drift detection restored (no pending bypass needed).</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite green-gate without the pending bypass</name>
  <read_first>
    - package.json (the `check` / `test` / `test:tier-contract` script definitions — what the phase green-gate runs)
    - .planning/phases/05-verifier-completeness-pass-2-pass-4/05-VALIDATION.md (§Sampling Rate — "Before /gsd:verify-work: npm run check must be green")
  </read_first>
  <files>bin/lib/prompt-loader.ts</files>
  <action>
    Run the phase green-gate to confirm the re-pin did not regress anything and that NO test or CI step still depends on PENSMITH_ALLOW_PENDING_PROMPT_HASHES=1 for the two Phase-5 slugs. Run `npm run check` (lint + typecheck + build + tier-contract + tests + manifests per 05-VALIDATION). If any test invocation in the repo still exports PENSMITH_ALLOW_PENDING_PROMPT_HASHES=1 SOLELY for claim-support/orphan-label and now passes without it, that is fine to leave (it is a harmless no-op for real-hashed slugs) — do NOT broaden scope by editing other plans' test files. This task adds no new code; it is the verification gate that the WN-3 loop is fully closed.

    If `npm run check` surfaces a failure traceable to the re-pin (e.g. a stale sentinel reference somewhere), fix ONLY the re-pin in prompt-loader.ts (this plan's single file). Any failure outside prompt-loader.ts that is not caused by the re-pin is out of scope and should be reported in the SUMMARY rather than silently patched.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` is fully GREEN (lint + typecheck + build + tier-contract + tests + manifests).
    - No remaining `__PENDING_HASH_claim-support__` / `__PENDING_HASH_orphan-label__` sentinel anywhere in the source tree: `grep -rn "__PENDING_HASH_\(claim-support\|orphan-label\)__" bin/ tests/` returns nothing.
    - The Phase-5 advisory passes (Pass 2 + Pass 4) are visible in VERIFICATION.md and tier-contract is GREEN — the full phase is complete.
  </acceptance_criteria>
  <done>Full suite green without the pending bypass; Phase 5 WN-3 loop closed; advisory verifier passes production-real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EXPECTED_PROMPT_HASHES (runtime) vs PENDING_HASH_PINS (PR-time) | The two pin surfaces must agree exactly; disagreement means drift or a bad re-pin |
| on-disk prompt file → SHA-256 → both pin surfaces | A mutated prompt body would invalidate both pins; the re-pin must be from the byte-stable file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | re-pin to a wrong/forged hash that does not match the on-disk prompt | mitigate | hash is recomputed from the on-disk file via node:crypto and cross-checked against repo-files PENDING_HASH_PINS BEFORE editing the loader; the repo-files per-pin loop + the loader/PIN cross-check loop both re-validate at test time |
| T-05-05-02 | Tampering | sentinel left in place → loadPrompt silently relies on the pending bypass in production | mitigate | acceptance greps for any remaining __PENDING_HASH_ for the two slugs (must be zero) and runs the Pass suites WITHOUT the bypass env, proving the real-hash path resolves |
| T-05-05-03 | Tampering | prompt drift between Plan 05-01 creation and this re-pin | mitigate | Task 1 STOPS if the recomputed hash differs from the repo-files pin; the discrepancy is surfaced, not silently re-pinned |
| T-05-SC | Tampering | npm installs | accept | No package installs in Phase 5 (05-RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- `node --import tsx --test tests/repo-files.test.ts` — GREEN (both pin surfaces agree).
- `PENSMITH_NO_LLM=1 node --import tsx --test tests/known-bad-pass2.test.ts tests/known-bad-pass4.test.ts` — GREEN WITHOUT the pending bypass.
- `grep -rn "__PENDING_HASH_\(claim-support\|orphan-label\)__" bin/ tests/` — no matches.
- `npm run check` — fully GREEN.
</verification>

<success_criteria>
- claim-support + orphan-label hold real SHA-256 values in EXPECTED_PROMPT_HASHES, matching repo-files exactly (WN-3 lockstep).
- loadPrompt resolves both slugs at runtime without PENSMITH_ALLOW_PENDING_PROMPT_HASHES=1 (drift detection re-armed).
- `npm run check` GREEN — Phase 5 complete with both advisory passes production-real and tier-parity proven.
</success_criteria>

<output>
Create `.planning/phases/05-verifier-completeness-pass-2-pass-4/05-05-SUMMARY.md` when done.
</output>
