---
phase: 05-verifier-completeness-pass-2-pass-4
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/known-bad-pass2.test.ts
  - tests/known-bad-pass4.test.ts
  - tests/verify-advisory-isolation.test.ts
  - tests/fixtures/pass2-adversarial.json
  - tests/fixtures/pass4-orphan.json
  - templates/prompts/claim-support.md
  - templates/prompts/orphan-label.md
  - bin/lib/prompt-loader.ts
  - tests/repo-files.test.ts
autonomous: true
requirements: [VRFY-03, VRFY-06]

must_haves:
  truths:
    - "RED test suite for Pass 2 (UNCLEAR-bias) and Pass 4 (deterministic orphan extraction) exists and SKIPs its behavioral tests pending the missing pass2.ts/pass4.ts modules (skip-guarded RED, mirroring known-bad-citations)"
    - "claim-support and orphan-label prompt slugs are registered in EXPECTED_PROMPT_HASHES BEFORE the prompt files carry real hashes (WN-3 sentinel-then-real ordering)"
    - "An advisory-isolation test asserts neither Pass 2 nor Pass 4 can set hasFail/status"
    - "The two new slugs are PERMITTED purely by registering them in bin/lib/prompt-loader.ts EXPECTED_PROMPT_HASHES (loadPrompt resolves them); bin/cli/verify.ts is NOT edited and the live D-13 chokepoint `grep -c \"loadPrompt\" bin/cli/verify.ts == 0` (whole-file, comments included) is preserved by a committed regression test"
  artifacts:
    - path: "tests/known-bad-pass2.test.ts"
      provides: "VRFY-03 RED suite — verdict enum + adversarial UNCLEAR-bias + ARCH-10 budget-before-call"
      contains: "runPass2"
    - path: "tests/known-bad-pass4.test.ts"
      provides: "VRFY-06 RED suite — deterministic extraction + orphan-count fixtures"
      contains: "extractClaimsFromParagraph"
    - path: "tests/verify-advisory-isolation.test.ts"
      provides: "advisory non-regression — Pass 2/4 never modify hasFail; committed D-13 0-hit regression for bin/cli/verify.ts"
      contains: "hasFail"
    - path: "tests/fixtures/pass2-adversarial.json"
      provides: ">=10 entries, >=5 thematically-adjacent-but-not-supportive (expected UNCLEAR)"
      min_lines: 30
    - path: "tests/fixtures/pass4-orphan.json"
      provides: "orphan-count fixtures: zero-cite, partial-cite, full-cite control, definition non-claim — every expected_orphan_count derived by executing the PINNED rule"
      min_lines: 20
    - path: "templates/prompts/claim-support.md"
      provides: "Pass 2 UNCLEAR-bias prompt with {{citekey}} {{claim_sentence}} {{source_abstract}} {{source_title}} {{source_authors}}"
    - path: "templates/prompts/orphan-label.md"
      provides: "Pass 4 edge-case claim-vs-definition classifier prompt with {{sentence}} {{paragraph_context}}"
  key_links:
    - from: "bin/lib/prompt-loader.ts EXPECTED_PROMPT_HASHES"
      to: "tests/repo-files.test.ts PENDING_HASH_PINS"
      via: "WN-3 single-source-of-truth lockstep (sentinel in loader, real byte-pin in repo-files)"
      pattern: "claim-support|orphan-label"
---

<objective>
Land the Phase 5 Wave 0 RED scaffold BEFORE any Pass 2/4 implementation exists. This plan creates the failing test suite, the adversarial/orphan fixtures, the two stub prompt files, and — critically — registers the `claim-support` and `orphan-label` prompt-hash sentinels in `EXPECTED_PROMPT_HASHES` so that the runtime prompt-loader can resolve the slugs the moment Plans 02/03 wire the LLM seams (WN-3 atomicity finding). Permission for the two new active slugs is granted SOLELY by their EXPECTED_PROMPT_HASHES registration — `bin/cli/verify.ts` is deliberately NOT touched (Pass 2/4 load their own prompts from their own modules), so the live D-13 chokepoint is preserved unchanged and pinned by a committed regression test.

Purpose: Per the project's RED→GREEN convention, the failing suite and prompt-hash sentinels must exist before the prompt files and pass modules. Skipping this creates the WN-3 ordering hazard (`unknown prompt` crash from the loader) called out in 05-RESEARCH Pitfall 3 + Pitfall 7. The pinned Pass-4 claim/orphan rule below is the single source of truth that Plan 05-03 implements against — the fixtures here are derived by mechanically executing that rule so the RED spec and the GREEN extractor cannot diverge.
Output: Three RED test files (one of which carries the committed D-13 0-hit regression for verify.ts), two JSON fixtures whose orphan counts are derived from the pinned rule, two stub prompt files, sentinel entries in the loader, and byte-pins in repo-files. bin/cli/verify.ts is NOT modified by this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-verifier-completeness-pass-2-pass-4/05-RESEARCH.md
@.planning/phases/05-verifier-completeness-pass-2-pass-4/05-PATTERNS.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

From bin/lib/prompt-loader.ts (EXPECTED_PROMPT_HASHES is the runtime sentinel/hash map; repo-files.test.ts imports it):
```
export const EXPECTED_PROMPT_HASHES: Record<string, string> = { ... };  // add 2 keys
export function loadPrompt(name: string): string;   // throws if slug not in map; bypasses hash check for __PENDING_HASH_<slug>__ when PENSMITH_ALLOW_PENDING_PROMPT_HASHES=1
export function interpolate(template: string, vars: Record<string, string>): string;  // throws on missing var
```

From tests/repo-files.test.ts (PENDING_HASH_PINS carries the REAL byte-pin SHA-256 for each prompt file; this test goes GREEN once the file is byte-stable, while the loader still holds a sentinel):
```
export const PENDING_HASH_PINS: ReadonlyArray<{ slug: string; path: string; decision: string; hash: string }> = [ ... ];
```
WN-3 lockstep rule (from repo-files.test.ts header + Phase 4 smoother precedent): repo-files PENDING_HASH_PINS holds the REAL hash from the moment the prompt file is byte-stable; prompt-loader EXPECTED_PROMPT_HASHES holds a `__PENDING_HASH_<slug>__` sentinel until the Plan 04 atomic re-pin. Both surfaces agree only after re-pin.

From tests/known-bad-citations.test.ts (the exact RED-stub structure to mirror — existence assertion + skip-guarded behavioral test):
```
const fixturePath = fileURLToPath(new URL('../tests/fixtures/known-bad-citations.json', import.meta.url));
test('... fixture file exists', () => assert.ok(existsSync(fixturePath), 'MISSING: ...'));
test('... behavior', { skip: !existsSync(fixturePath) || !existsSync(modPath) }, async () => { ... });
```

From bin/cli/verify.ts — the D-13 LOCKED comment block (lines 1-13) and the LIVE chokepoint mechanism (confirmed from Plan 03-07 SUMMARY Invariant #3 + auto-fix #6):
```
// THIN ORCHESTRATOR — D-13 LOCKED INVARIANT: this verb is 100% deterministic ...
// The hash-pinned `pass1-fuzzy-judge.md` and `pass3-quote-checker.md` prompts ...
// MUST NOT be invoked here. The D-13 LOCKED grep chokepoint ... a literal-string
// search ... against this file MUST return 0 hits (including this comment — hence
// the paraphrase above).
```
EXACT LIVE INVARIANT (do NOT weaken to a non-comment proxy): `grep -c "loadPrompt" bin/cli/verify.ts` returns 0 — a WHOLE-FILE literal search for the symbol `loadPrompt`, COMMENTS INCLUDED. Plan 03-07 auto-fix #6 paraphrased the original comment's two `loadPrompt` mentions to `prompt-loader call` / `prompt-loader symbol` precisely because the comment counts. NOTE: the literal `prompt-loader` (hyphenated) is NOT gated — it currently appears twice in the comment legitimately; only the symbol `loadPrompt` must be 0. verify.ts itself never calls the loader; Pass 2/4 call it from their own modules (pass2.ts / pass4.ts), so verify.ts needs NO edit to permit the new slugs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Pass 2 + Pass 4 fixtures (orphan counts derived from the PINNED rule) and the two RED behavioral test files</name>
  <read_first>
    - tests/known-bad-citations.test.ts (exact RED-stub structure + fixture-schema assertion pattern — the analog)
    - tests/fixtures/known-bad-citations.json (fixture array shape)
    - .planning/phases/05-verifier-completeness-pass-2-pass-4/05-PATTERNS.md (sections "tests/known-bad-pass2.test.ts", "tests/known-bad-pass4.test.ts", "tests/fixtures/pass2-adversarial.json", "tests/fixtures/pass4-orphan.json")
  </read_first>
  <files>tests/fixtures/pass2-adversarial.json, tests/fixtures/pass4-orphan.json, tests/known-bad-pass2.test.ts, tests/known-bad-pass4.test.ts</files>
  <action>
    PINNED PASS-4 CLAIM-EXTRACTION + ORPHAN-DETECTION RULE (the single source of truth — Plan 05-03 Task 1 implements EXACTLY this; every expected_orphan_count below is produced by mechanically executing it, NOT by prose intuition. This rule is fully LLM-independent.):

      R1 — Sentence split: split a paragraph on the pure regex boundary "a `.`, `!`, or `?` terminator followed by whitespace OR end-of-string". No NLP library, no abbreviation handling (residual abbreviation false-positives are an accepted advisory limitation). Trim each resulting sentence.
      R2 — In-text citation marker: a `[@citekey]` token matching /\[@([a-z][a-z0-9_-]*)\]/g. "Has a citation within proximity" = a `[@citekey]` token appears within ORPHAN_PROXIMITY_CHARS=500 characters of the sentence's character span inside the paragraph (paragraphs are short, so in practice this is "the paragraph contains a [@citekey]").
      R3 — Rhetorical skip: a sentence ending in `?` is NOT a claim (discarded before marker counting).
      R4 — Definition skip: a sentence matching DEFINITION_MARKERS /\b(defined as|refers to|known as)\b/i is NOT a claim (discarded before marker counting).
      R5 — Length skip: a sentence with word count < CLAIM_MIN_WORDS=8 is NOT a claim.
      R6 — Marker counting: count DISTINCT matches of CLAIM_MARKERS /\b(is|are|demonstrates|shows|proves|indicates|suggests|reveals|confirms|establishes|argues|claims|because|therefore|thus|hence|consequently)\b/gi in the surviving sentence. "Distinct" = number of unique marker LEMMAS matched (e.g. a sentence matching both `proves` and `is` has 2 distinct markers; a sentence matching `is` twice has 1).
      R7 — Confidence: 0 distinct markers → NOT a claim (dropped); exactly 1 → claimConfidence 'AMBIGUOUS'; 2+ → claimConfidence 'HIGH'.
      R8 — orphanCount (THE deterministic, LLM-INDEPENDENT summary number the fixtures assert): count ONLY 'HIGH'-confidence claim sentences that have NO in-text citation within proximity (R2). AMBIGUOUS sentences are NEVER counted toward orphanCount regardless of any later LLM Step-3 label. This is the non-negotiable: AMBIGUOUS labeling must never change orphanCount.

    Create tests/fixtures/pass2-adversarial.json: a JSON array of >=10 entries, each with keys citekey, claim_sentence, source_title, source_abstract, expected_verdict, adversarial_reason. At least 5 entries MUST have expected_verdict "UNCLEAR" where the abstract is thematically adjacent (same topic family) but does NOT literally support the specific claim — these calibrate the UNCLEAR-bias requirement (VRFY-03). Include at least one entry per other verdict value (SUPPORTED, PARTIAL, UNSUPPORTED) for enum coverage.

    Create tests/fixtures/pass4-orphan.json: a JSON array of entries, each with keys paragraph, in_text_citekeys (array), expected_orphan_count (number), description. For EVERY entry, you MUST compute expected_orphan_count by walking R1–R8 by hand on the paragraph string and recording the resulting HIGH-orphan count — do not estimate. In the `description` field, show the per-sentence walk (markers matched → confidence → cited?) so a future executor can re-verify. Include these required shapes:
      (a) Zero citations + multiple HIGH-confidence claims → all HIGH claims orphaned. (Author the paragraph so that, under R6/R7, the intended sentences genuinely reach 2+ distinct markers; verify by walking the rule.)
      (b) One citation covering some claims + at least one uncited HIGH claim → expected_orphan_count = number of uncited HIGH claims by R8.
      (c) Every HIGH claim cited → expected_orphan_count 0 (control).
      (d) A definition-style sentence ("X is defined as ...", "Y refers to ...") that MUST be dropped by R4 and therefore contributes 0 to both claim count and orphanCount.
      (e) The CANONICAL example from 05-PATTERNS, CORRECTED to the value the pinned rule actually produces: paragraph "Climate change demonstrates accelerating ice loss. This proves the feedback loop is intensifying. Ice sheets are retreating globally." with in_text_citekeys [] →
          S1 "Climate change demonstrates accelerating ice loss" : markers {demonstrates} = 1 distinct → AMBIGUOUS → NOT counted by R8.
          S2 "This proves the feedback loop is intensifying" : markers {proves, is} = 2 distinct → HIGH → uncited → orphan.
          S3 "Ice sheets are retreating globally" : markers {are} = 1 distinct → AMBIGUOUS → NOT counted by R8.
          → expected_orphan_count: 1 (NOT 2). description MUST state "One HIGH-confidence orphan (S2); S1 and S3 are AMBIGUOUS and excluded from orphanCount per R8." This corrects the stale 05-PATTERNS example (which said 2) so the fixture and the Plan 05-03 algorithm agree BY CONSTRUCTION.

    Create tests/known-bad-pass2.test.ts mirroring tests/known-bad-citations.test.ts structure. Tests: (1) fixture file exists; (2) fixture has >=10 entries and >=5 with expected_verdict "UNCLEAR"; (3) bin/lib/verify/pass2.ts module exists (skip-guarded); (4) with process.env.PENSMITH_NO_LLM='1' set, dynamic-import runPass2 from '../bin/lib/verify/pass2.js' and assert every returned result has verdict 'UNCLEAR' (placeholder path) and that the result objects carry citekey + claimSentence + verdict + rationale + evidence keys; (5) assert the pass2 module SOURCE TEXT contains the call-order tokens 'assertBudget' appearing before the LLM call site (ARCH-10) — read the pass2.ts source and assert indexOf('assertBudget') is non-negative (a source-level proxy for the budget-before-call invariant). Use `{ skip: !existsSync(...) }` guards so the suite is RED-by-skip (behavioral tests skip pending the module), not RED-by-crash.

    Create tests/known-bad-pass4.test.ts mirroring the same structure. Tests: (1) fixture exists; (2) determinism — dynamic-import extractClaimsFromParagraph from '../bin/lib/verify/pass4.js', call it twice on the same paragraph string and assert.deepEqual the two results (VRFY-06, PRD §14); (3) orphan-count — with PENSMITH_NO_LLM='1', for each fixture entry call runPass4(entry.paragraph, { n: 1 }) and assert the summed orphanCount across returned Pass4Result[] equals entry.expected_orphan_count (the values authored above via R1–R8); (4) definition non-claim — assert the definition-style fixture entry (shape d) yields expected_orphan_count 0. All behavioral tests skip-guarded on existsSync of pass4.ts.
  </action>
  <verify>
    <automated>node --import tsx --test tests/known-bad-pass2.test.ts tests/known-bad-pass4.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both test files run; the fixture-existence and fixture-shape assertions PASS (fixtures exist now).
    - The behavioral tests SKIP (not error) because bin/lib/verify/pass2.ts and bin/lib/verify/pass4.ts do not yet exist — confirmed by the run reporting skipped tests, zero failures.
    - tests/fixtures/pass2-adversarial.json parses as a JSON array with >=10 entries and >=5 expected_verdict "UNCLEAR".
    - tests/fixtures/pass4-orphan.json includes at least one control entry with expected_orphan_count 0, one definition-style non-claim entry, AND the corrected canonical entry (Climate-change paragraph) with expected_orphan_count 1.
    - Every pass4-orphan.json entry's expected_orphan_count equals the value produced by mechanically executing rule R1–R8 (the `description` field shows the per-sentence walk for re-verification); NO entry counts an AMBIGUOUS sentence as an orphan.
  </acceptance_criteria>
  <done>RED scaffold for VRFY-03 + VRFY-06 lands; behavioral tests skip pending pass2.ts/pass4.ts; fixtures present, well-formed, and orphan counts derived from the pinned R1–R8 rule (canonical example corrected to 1) so Plan 05-03 cannot diverge.</done>
</task>

<task type="auto">
  <name>Task 2: Advisory-isolation + D-13 0-hit regression test, WN-3 prompt-hash sentinels, stub prompt files, byte-pins</name>
  <read_first>
    - bin/lib/prompt-loader.ts (EXPECTED_PROMPT_HASHES map lines 91-117, the Phase-4 smoother sentinel comment pattern, loadPrompt sentinel-bypass logic)
    - tests/repo-files.test.ts (PENDING_HASH_PINS array lines 235-257, the per-pin hash-pin test loop, the WN-3 lockstep header)
    - bin/cli/verify.ts (hasFail / hasUnverifiable / status computation lines 119-124 — what the isolation test must guard; AND the D-13 0-hit invariant the regression test must pin)
    - templates/prompts/smoother.md (an existing prompt file format to mirror)
    - .planning/phases/05-verifier-completeness-pass-2-pass-4/05-PATTERNS.md (sections "templates/prompts/claim-support.md", "templates/prompts/orphan-label.md", "bin/lib/prompt-loader.ts (MODIFIED)", "WN-3 Sentinel Registration")
  </read_first>
  <files>templates/prompts/claim-support.md, templates/prompts/orphan-label.md, bin/lib/prompt-loader.ts, tests/repo-files.test.ts, tests/verify-advisory-isolation.test.ts</files>
  <action>
    Create templates/prompts/claim-support.md per 05-RESEARCH §"Pattern 5: Pass 2 Prompt — UNCLEAR-Bias Calibration" and 05-PATTERNS §claim-support. The body MUST reference exactly these interpolation vars: {{citekey}}, {{claim_sentence}}, {{source_abstract}}, {{source_title}}, {{source_authors}}. Include the hard-constraint block: UNCLEAR-bias default, SUPPORTED only on explicit support, PARTIAL for topic-not-assertion, UNSUPPORTED only on explicit contradiction/off-topic, never infer from thematic similarity, never fabricate evidence (evidence must be a substring of the abstract). Specify the single-JSON-object output contract with keys verdict, rationale (<=200 chars), evidence.

    Create templates/prompts/orphan-label.md: a Pass 4 Step-3 edge-case classifier prompt that, given an AMBIGUOUS sentence and its paragraph context, returns whether the sentence is a 'claim', a 'definition', or 'UNCLEAR'. MUST reference exactly {{sentence}} and {{paragraph_context}}. Specify single-JSON output with a 'label' key in that enum.

    In bin/lib/prompt-loader.ts EXPECTED_PROMPT_HASHES: add two new entries, 'claim-support' and 'orphan-label', each set to the sentinel strings '__PENDING_HASH_claim-support__' and '__PENDING_HASH_orphan-label__' respectively. Add a Phase-5 comment block mirroring the Phase-4 smoother comment (lines 111-116): note these are WN-3 sentinels re-pinned atomically to real SHA-256 in Plan 05-04, that PENSMITH_ALLOW_PENDING_PROMPT_HASHES=1 bypasses the runtime drift check during Waves 0-1, and that these two slugs are the ACTIVE Phase-5 advisory prompts invoked from bin/lib/verify/pass2.ts and bin/lib/verify/pass4.ts (NOT from bin/cli/verify.ts) while pass1-fuzzy-judge + pass3-quote-checker remain DORMANT. (This comment lives in prompt-loader.ts, which is NOT bound by the verify.ts-file 0-hit rule, so naming `loadPrompt`/slugs here is fine.) DO NOT touch bin/cli/verify.ts in this plan — the new slugs are permitted purely by this registration; verify.ts never loads a prompt.

    In tests/repo-files.test.ts PENDING_HASH_PINS: add two entries for claim-support and orphan-label with their real SHA-256 byte-pins. Compute each hash AFTER writing the prompt files via: node -e "console.log(require('node:crypto').createHash('sha256').update(require('node:fs').readFileSync('templates/prompts/claim-support.md')).digest('hex'))" (and the same for orphan-label.md). The repo-files byte-pin is the REAL hash (GREEN from creation per WN-3 — the prompt body is byte-stable); the loader holds the sentinel until Plan 05-04 re-pin.

    Create tests/verify-advisory-isolation.test.ts with TWO guards:
      (A) Advisory isolation: read bin/cli/verify.ts as text and assert (a) the substring 'hasFail' appears, and (b) there is NO line that assigns hasFail (or status) from a pass2/pass4 expression — assert the file does not match /hasFail\s*=.*pass[24]/ and does not match /status\s*=.*pass[24]/. Add a forward-looking guard so this stays meaningful after Plan 05-04 wiring: assert that if the file contains 'runPass2' or 'runPass4', those tokens never appear on the same line as 'hasFail =' or 'status ='. This PASSES now (verify.ts has no pass2/pass4 yet) and stays GREEN after Plan 05-04 wires the advisory calls below the frozen status line.
      (B) D-13 0-hit regression (the REAL live invariant — NOT a non-comment proxy): read bin/cli/verify.ts as raw UTF-8 text and assert the WHOLE-FILE literal symbol count of 'loadPrompt' is 0, i.e. assert (text.match(/loadPrompt/g) ?? []).length === 0 — COMMENTS INCLUDED. This is the committed regression the reviews requested (Plan 03-07 enforced `grep -c "loadPrompt" bin/cli/verify.ts == 0` only via an ad-hoc grep; this test makes it durable). Do NOT also forbid the literal `prompt-loader` (hyphenated) — that token legitimately appears in the verify.ts comment and is not part of the chokepoint. This guard pins the invariant Plan 05-04 must NOT break when it wires runPass2/runPass4.
  </action>
  <verify>
    <automated>PENSMITH_ALLOW_PENDING_PROMPT_HASHES=1 node --import tsx --test tests/repo-files.test.ts tests/verify-advisory-isolation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/repo-files.test.ts hash-pin tests for claim-support and orphan-label PASS (real byte-pins match the on-disk prompt files).
    - tests/verify-advisory-isolation.test.ts guard (A) PASSES (no pass2/pass4 → hasFail/status assignment present in verify.ts).
    - tests/verify-advisory-isolation.test.ts guard (B) PASSES: (text.match(/loadPrompt/g) ?? []).length === 0 against the FULL bin/cli/verify.ts source (comments included) — the live D-13 chokepoint, asserted exactly, not via `grep -v '^//'`.
    - bin/lib/prompt-loader.ts EXPECTED_PROMPT_HASHES contains keys 'claim-support' and 'orphan-label' with __PENDING_HASH_ sentinel values, and a Phase-5 comment naming them as ACTIVE slugs invoked from pass2.ts/pass4.ts.
    - bin/cli/verify.ts is BYTE-UNCHANGED by this plan (git diff shows no modification to that file).
    - `npm run typecheck` passes.
  </acceptance_criteria>
  <done>Prompt files byte-stable + byte-pinned; loader sentinels registered (WN-3 ordering satisfied — slugs resolvable before pass modules land); advisory-isolation guard GREEN; committed D-13 0-hit regression pins the real whole-file `loadPrompt`-count==0 invariant for verify.ts; verify.ts itself untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| draft text / claim text → LLM prompt (future Plans 02/03) | Untrusted section prose flows into prompt interpolation; this plan only stages the prompt files + var contract |
| prompt-hash sentinel → runtime loadPrompt | A mis-registered or drifted hash would crash or silently load wrong prompt content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | EXPECTED_PROMPT_HASHES sentinel vs repo-files byte-pin drift | mitigate | WN-3 single-source lockstep: repo-files holds real byte-pin from creation, loader holds sentinel until Plan 05-04 atomic re-pin; both surfaces tested |
| T-05-02 | Tampering | claim-support / orphan-label prompt body (prompt-injection surface for later phases) | mitigate | Var contract fixed to {{citekey}}/{{claim_sentence}}/{{source_abstract}}/{{source_title}}/{{source_authors}} (Pass 2) and {{sentence}}/{{paragraph_context}} (Pass 4); interpolate() throws on missing/unexpected vars (V5 input validation) |
| T-05-03 | Elevation of Privilege | advisory pass escalating to blocking verdict | mitigate | tests/verify-advisory-isolation.test.ts forbids any hasFail/status assignment from pass2/pass4 expressions (VRFY-07 enforcement) |
| T-05-04 | Tampering | D-13 chokepoint regression — a future edit slipping a `loadPrompt` literal into bin/cli/verify.ts (comment or code) | mitigate | tests/verify-advisory-isolation.test.ts guard (B) asserts whole-file `loadPrompt` count == 0 (comments included), pinning the live D-13 invariant durably |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in Phase 5 (05-RESEARCH §Package Legitimacy Audit: zero new packages); no slopcheck checkpoint required |
</threat_model>

<verification>
- `node --import tsx --test tests/known-bad-pass2.test.ts tests/known-bad-pass4.test.ts` — fixture/shape tests PASS, behavioral tests SKIP (modules not yet created).
- `PENSMITH_ALLOW_PENDING_PROMPT_HASHES=1 node --import tsx --test tests/repo-files.test.ts tests/verify-advisory-isolation.test.ts` — all GREEN (advisory isolation + D-13 0-hit regression).
- `npm run typecheck` — GREEN (no type regressions from map edits; verify.ts untouched).
- Manual: confirm EXPECTED_PROMPT_HASHES holds sentinels (NOT real hashes) for the two new slugs; confirm PENDING_HASH_PINS holds REAL hashes for them; confirm `git diff bin/cli/verify.ts` is empty.
</verification>

<success_criteria>
- RED suite for VRFY-03 (Pass 2) and VRFY-06 (Pass 4) exists; behavioral tests skip-guarded on missing pass modules.
- claim-support + orphan-label registered as WN-3 sentinels in the loader and real byte-pins in repo-files — slugs resolvable by the prompt-loader under the pending bypass before Plans 02/03 run.
- Advisory-isolation guard GREEN and forward-compatible with Plan 05-04 wiring; committed D-13 0-hit regression pins the live whole-file invariant for verify.ts.
- The two new slugs are permitted by EXPECTED_PROMPT_HASHES registration alone; bin/cli/verify.ts is NOT edited and its `loadPrompt`-count==0 chokepoint is preserved.
- Pass-4 fixture orphan counts are derived from the pinned R1–R8 rule (canonical example corrected to 1), so Plan 05-03 implements against a fixture set it cannot contradict.
</success_criteria>

<output>
Create `.planning/phases/05-verifier-completeness-pass-2-pass-4/05-01-SUMMARY.md` when done.
</output>
