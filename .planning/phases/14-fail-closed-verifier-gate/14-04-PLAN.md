---
phase: 14-fail-closed-verifier-gate
plan: 04
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - bin/cli/done.ts
autonomous: true
requirements: [GATE-04]
must_haves:
  truths:
    - "the humanized FINAL.md is re-checked before export: a citekey-set diff vs DRAFT.md AND a deterministic Pass-3 quote re-check"
    - "an added/dropped/swapped citekey in FINAL.md hard-blocks export"
    - "a Pass-3 NOT_FOUND quote in FINAL.md hard-blocks export"
    - "the re-check is a HARD block (return ok:false) BEFORE runDoneGate — not a soft advisory confirm"
    - "absent humanizer (finalPath === null) or --yolo → skip cleanly (no block)"
  artifacts:
    - path: "bin/cli/done.ts"
      provides: "GATE-04 reCheckFinalMd helper + call site between humanize and runDoneGate"
      contains: "reCheckFinalMd"
  key_links:
    - from: "bin/cli/done.ts"
      to: "bin/lib/verify/pass3.ts"
      via: "import { runPass3 } — Pass-3 quote re-check on FINAL.md"
      pattern: "runPass3"
    - from: "bin/cli/done.ts"
      to: "bin/lib/citation-token.ts"
      via: "import { extractCitekeys } — citekey-set diff"
      pattern: "extractCitekeys"
---

<objective>
Close GATE-04: after runHumanizer produces .paper/FINAL.md and BEFORE exportDraft, re-verify FINAL.md with (a) a citekey-set diff — the set of [@key] tokens in FINAL.md must equal the set in the compiled DRAFT.md — and (b) a deterministic Pass-3 quote re-check on FINAL.md's quotes. A mismatch or a NOT_FOUND hard-blocks export. The re-check is skipped cleanly when there is no FINAL.md (humanizer absent/skipped) or with --yolo.

Purpose: humanization is the last unguarded surface — today the humanized FINAL.md is exported without re-verification, so humanization could introduce, drop, swap, or break a verified citation/quote after the pre-compile gate already passed. GATE-04 makes citation integrity a HARD block at export (return ok:false BEFORE the DONE-09 advisory gate), consistent with the project's #1 non-negotiable that no FABRICATED/MIS-CITED/quote-NOT_FOUND escapes — even through the humanizer.
Output: 1 modified file (done.ts); the Wave-0 done-recheck skips turn into passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-fail-closed-verifier-gate/14-RESEARCH.md
@.planning/phases/14-fail-closed-verifier-gate/14-PATTERNS.md

<read_first>
- bin/cli/done.ts — lines 405-453: the runHumanizer call at 414, the finalPath !== null honesty block at 415-421 (the skip-guard precedent), the pass2Results read at 433 + runDoneGate at 434, the blocking-return pattern `return { ok: false }` at 450-452, draftPath at 391 + paperRoot at 390 + draftMd already in scope. Insertion point: after line 421 (end of honesty block) and BEFORE line 433 (pass2Results / runDoneGate).
- bin/cli/verify.ts — lines 108-119: the canonical parseBibtex + bibByCitekey Map construction to copy.
- bin/lib/verify/pass3.ts — line 54 runPass3(draftMd, bibByCitekey: Map<string, BibLike>): Promise<Pass3Result[]>; BibLike = { DOI?: string } at ~43; Pass3Result.verdict 'NOT_FOUND' value + quoteSnippet field.
- bin/lib/citation-token.ts — lines 39-53 extractCitekeys(md): string[] (deduped ordered).
- 14-RESEARCH.md §GATE-04 (lines 462-551) — the reCheckFinalMd code example (706-740), insertion-point analysis (495-545), Pitfall 4 (build bibByCitekey from full CITATIONS.bib, not DRAFT-filtered) and Pitfall 5.
- tests/done-recheck.test.ts — the GATE-04 contract these changes must satisfy.
</read_first>

<interfaces>
<!-- pass3 contract (pass3.ts:54) -->
runPass3(draftMd: string, bibByCitekey: Map<string, { DOI?: string }>): Promise<Pass3Result[]>
Pass3Result: { citekey: string; quoteSnippet: string; verdict: 'NOT_FOUND' | ...; ... }

<!-- citekey extraction (citation-token.ts:39) -->
extractCitekeys(md: string): string[]   // deduped, ordered

<!-- bib-read pattern to copy (verify.ts:108-119) -->
const bibEntries = await parseBibtex(readFileSync(bibPath, 'utf8'));
const bibByCitekey = new Map(bibEntries.map((e) => [String(e.id ?? ''), e]));

<!-- helper contract the Wave-0 test references -->
reCheckFinalMd(finalMd: string, draftMd: string, bibPath: string): Promise<{ passed: boolean; reason: string }>

<!-- in-scope done.ts vars at the call site -->
finalPath: string | null; draftMd: string; draftPath; paperRoot; args.yolo
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reCheckFinalMd helper (citekey-set diff + Pass-3) to done.ts</name>
  <files>bin/cli/done.ts</files>
  <read_first>bin/cli/verify.ts lines 108-119 (bib-read pattern), bin/lib/verify/pass3.ts line 54 + BibLike + Pass3Result.NOT_FOUND, bin/lib/citation-token.ts lines 39-53 (extractCitekeys), bin/cli/done.ts existing DoneGateResult shape (~80-83) + blocking-return at 450-452, 14-RESEARCH.md lines 706-740 (helper) + Pitfall 4/5, tests/done-recheck.test.ts (the contract).</read_first>
  <behavior>
    - reCheckFinalMd with finalMd/draftMd that have IDENTICAL citekey sets and no broken quotes → { passed: true, reason: '' }.
    - An added [@key] in finalMd not in draftMd → { passed: false } with a reason naming the added key.
    - A dropped [@key] (in draftMd, gone from finalMd) → { passed: false } naming the dropped key.
    - A swapped [@key] → { passed: false } (citekey-set diff catches it before Pass-3).
    - A FINAL.md whose quote does not match the OA source (Pass-3 NOT_FOUND, same citekey set) → { passed: false } naming the [@key] + quoteSnippet.
    - Absent bib file (CITATIONS.bib does not exist or is empty) → { passed: true } (no quotes to check, citekey-diff still applies).
  </behavior>
  <action>
    Add imports to done.ts: `import { runPass3 } from '../lib/verify/pass3.js';`, `import { extractCitekeys } from '../lib/citation-token.js';`, and `import { parseBibtex } from '../lib/citations.js';` (same path verify.ts uses). Add an async helper reCheckFinalMd(finalMd, draftMd, bibPath): Promise<{ passed: boolean; reason: string }> above the command definition, mirroring the DoneGateResult typed-result-never-throw shape. Step (a) — citekey-set diff FIRST (Pitfall 4/5): `const finalKeys = new Set(extractCitekeys(finalMd)); const draftKeys = new Set(extractCitekeys(draftMd));` if sizes differ or any final key is not in draft, compute added = finalKeys not in draftKeys and dropped = draftKeys not in finalKeys and return { passed: false, reason: `citekey-set mismatch after humanization — added: [...], dropped: [...]` }. Step (b) — only when sets are equal: if !existsSync(bibPath) or the bib text is whitespace-only, return { passed: true, reason: '' } (no quotes to check); else build bibByCitekey from the FULL CITATIONS.bib via the verify.ts:108-119 parseBibtex pattern (NOT filtered by DRAFT keys — Pitfall 4), run `const pass3Results = await runPass3(finalMd, bibByCitekey);`, filter verdict === 'NOT_FOUND'; if any, return { passed: false, reason: `Pass-3 quote NOT_FOUND in humanized FINAL.md: ...` } naming each [@citekey] + quoteSnippet; otherwise { passed: true, reason: '' }. Export reCheckFinalMd (named export) so tests/done-recheck.test.ts can import it.
  </action>
  <verify>
    <automated>node --import tsx --test tests/done-recheck.test.ts</automated>
  </verify>
  <done>done-recheck citekey-diff branches (match/added/dropped/swapped) + absent-bib skip-clean assertions PASS (no longer skip); reCheckFinalMd is exported; full suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the GATE-04 hard block between humanize and runDoneGate</name>
  <files>bin/cli/done.ts</files>
  <read_first>bin/cli/done.ts lines 405-453 (humanize/honesty block ending at 421, pass2Results/runDoneGate at 433-448, blocking-return at 450-452), 14-RESEARCH.md lines 495-551 (insertion point + why before runDoneGate + skip conditions), 14-PATTERNS.md done.ts blocking-return + null-guard sections, tests/done-recheck.test.ts (any call-site integration assertion).</read_first>
  <action>
    Insert the GATE-04 call site in done.ts immediately AFTER the honesty block (after line 421) and BEFORE the pass2Results read / runDoneGate (before line 433): `if (finalPath !== null && args.yolo !== true) { const finalMd = readFileSync(finalPath, 'utf8'); const bibPath = join(paperDir(paperRoot), 'CITATIONS.bib'); const gate4 = await reCheckFinalMd(finalMd, draftMd, bibPath); if (!gate4.passed) { process.stdout.write(\`pensmith done: GATE-04 BLOCKED — FINAL.md failed re-verification: ${gate4.reason}\n\`); return { ok: false }; } }`. This is a HARD block (return ok:false) placed BEFORE runDoneGate — citation integrity is not a soft advisory confirm (14-RESEARCH §GATE-04: belongs alongside the fail-closed gates, not inside the advisory flow). Skip conditions: finalPath === null (no humanizer / Tier-2 no-transport — the unhumanized DRAFT.md was already verified pre-compile) and args.yolo === true (the only override, per CONTEXT.md). draftMd, finalPath, paperRoot, args are already in scope; reuse the existing readFileSync/join/paperDir imports (no new file-write — GATE-04 reads only, no atomicWriteFile). Leave runDoneGate, the export-cancelled return at 450-452, and exportDraft at 474 UNCHANGED.
  </action>
  <verify>
    <automated>node --import tsx --test tests/done-recheck.test.ts && node --import tsx --test --test-name-pattern "GATE" tests/*.test.ts</automated>
  </verify>
  <done>The GATE-04 call site fires before runDoneGate; finalPath===null and --yolo skip cleanly; a failed re-check returns ok:false with a clear message; full suite green; done.ts adds no new file-write.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| humanizer (external skill) → FINAL.md | the humanizer can add/drop/swap/break a citation after the pre-compile gate already passed |
| FINAL.md text → export | a tampered/humanized [@key] that bypasses citekey validation could reach the exported document |
| CITATIONS.bib → Pass-3 | the bib supplies the DOIs Pass-3 re-fetches against |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Tampering | humanizer introduces/alters/drops a citation (gate-bypass) | mitigate | citekey-set diff (extractCitekeys FINAL vs DRAFT) HARD-blocks any add/drop/swap; runs BEFORE Pass-3 (Pitfall 4) |
| T-14-04-QUOTE | Tampering | humanizer breaks a verified quote | mitigate | deterministic runPass3 on FINAL.md quotes against the full CITATIONS.bib; any NOT_FOUND HARD-blocks export |
| T-14-04-SOFT | Elevation | citation-integrity failure treated as a soft advisory confirm | mitigate | GATE-04 returns ok:false BEFORE runDoneGate (hard block, not the DONE-09 advisory flow); only --yolo overrides per CONTEXT.md |
| T-14-04-CRAFTKEY | Tampering | crafted [@key] bypassing citekey grammar (V5) | mitigate | extractCitekeys uses the locked CITATION_TOKEN_RE; the diff compares against the pre-verified DRAFT set |
| T-14-04-LEAK | Information Disclosure | secret/PII leak via the re-check | accept | GATE-04 reads local files only (FINAL.md, DRAFT.md, CITATIONS.bib); Pass-3 network (OA fetch) is the existing cassette-backed path; no new secret/PII surface |
</threat_model>

<verification>
- `node --import tsx --test tests/done-recheck.test.ts` → added/dropped/swapped citekey blocks; clean passes; absent humanizer + --yolo skip clean.
- A FINAL.md with a tampered quote (same citekeys) → Pass-3 NOT_FOUND → export blocked.
- `grep` confirms done.ts imports runPass3 + extractCitekeys, the GATE-04 call site sits before runDoneGate, and no new atomicWriteFile was added.
- `npm test` → full suite green.
</verification>

<success_criteria>
- FINAL.md re-checked (citekey-set diff + Pass-3) before export.
- Added/dropped/swapped citekey OR NOT_FOUND quote → hard block (ok:false) before runDoneGate.
- Absent humanizer (finalPath===null) or --yolo → skip cleanly.
- bibByCitekey built from the full CITATIONS.bib (Pitfall 4); no new file-write.
</success_criteria>

<output>
Create `.planning/phases/14-fail-closed-verifier-gate/14-04-SUMMARY.md` when done
</output>
