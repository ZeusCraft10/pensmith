---
phase: 14-fail-closed-verifier-gate
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - bin/lib/verify/pass1.ts
autonomous: true
requirements: [GATE-03]
must_haves:
  truths:
    - "a DOI that is live-retracted on Retraction Watch at verify time escalates to MIS-CITED (blocking) in Pass-1"
    - "a transport error or no-hit (fetchById returns null) NEVER produces a false MIS-CITED"
    - "the re-query runs AFTER Crossref confirms the DOI resolves (not before), so it never queries a FABRICATED citation"
    - "the stored claimed.retracted offline-fast path still blocks (unchanged)"
    - "all retraction network goes through the existing cassette-backed http.ts adapter (offline-deterministic in CI)"
  artifacts:
    - path: "bin/lib/verify/pass1.ts"
      provides: "GATE-03 live retraction re-query inside verdictForCitekey"
      contains: "live re-query at verify time"
  key_links:
    - from: "bin/lib/verify/pass1.ts"
      to: "bin/lib/sources/retraction-watch.ts"
      via: "import { fetchById as retractionWatchFetchById }"
      pattern: "retractionWatchFetchById"
---

<objective>
Close GATE-03: at verify time, after a citation's DOI is confirmed real by Crossref, re-query Retraction Watch on that DOI and escalate a live retraction hit to MIS-CITED (blocking) in Pass-1's deterministic verdict path. A transport error or no-hit is a silent skip — never a false block.

Purpose: today Pass-1 only blocks on the STORED claimed.retracted flag (set at research time), so a work retracted AFTER research compiles clean — the live check exists only in the WARN-only freshness channel. GATE-03 makes the live retraction signal blocking, catching post-research retractions while preserving the deterministic-or-degrade invariant (block only on a confirmed hit; never block on an unreachable service).
Output: 1 modified file (pass1.ts); the Wave-0 gate-retraction skips turn into passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-fail-closed-verifier-gate/14-RESEARCH.md
@.planning/phases/14-fail-closed-verifier-gate/14-PATTERNS.md

<read_first>
- bin/lib/verify/pass1.ts — verdictForCitekey structure: claimed.retracted fast-path return shape at lines 113-118 (COPY this shape), the !claimed.DOI FABRICATED guard at 119-124, the Crossref resolve at line 126 + null-guard at 127-132, the JW comparison + redirect handling at 134-166. Insertion point is immediately AFTER the null-guard at 132 (i.e. once `actual` is confirmed non-null) and BEFORE the JW comparison at 134.
- bin/lib/verify/freshness.ts — line 27 (the exact import alias `import { fetchById as retractionWatchFetchById } from '../sources/retraction-watch.js';`) and lines 131-145 (the retraction-watch call + retraction_details usage precedent).
- bin/lib/sources/retraction-watch.ts — lines 93-127: fetchById signature returns Promise<SourceCandidate | null>, NEVER throws (catch returns null at ~122-126), offline cassette dispatch at top; the retraction_details field on the returned candidate.
- 14-RESEARCH.md §GATE-03 (lines 360-458), especially the insertion-point analysis (410-454) and Pitfall 1 (lines 568-574 — place AFTER Crossref to avoid the cassette fallback false-positive).
- tests/gate-retraction.test.ts + tests/known-bad-citations.test.ts — the GATE-03 contract + the regression that claimed.retracted still blocks.
</read_first>

<interfaces>
<!-- Adapter contract (retraction-watch.ts:93) -->
fetchById(doi: string): Promise<SourceCandidate | null>   // null on transport error OR no-hit; never throws
SourceCandidate.retraction_details?: string

<!-- Existing fast-path return shape to copy (pass1.ts:113-118) -->
{ citekey: ck, verdict: 'MIS-CITED', titleJW: 0, authorJW: 0, reason: '...' }

<!-- Insertion site (pass1.ts) -->
... const actual = await sources.crossref.fetchById(claimed.DOI);
    if (!actual) { return FABRICATED; }
    // <-- GATE-03 live re-query goes HERE (actual is confirmed non-null)
    const titleJW = jaroWinkler(...);
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add GATE-03 live retraction re-query to verdictForCitekey</name>
  <files>bin/lib/verify/pass1.ts</files>
  <read_first>bin/lib/verify/pass1.ts lines 92-167 (verdictForCitekey, esp. 113-118 fast-path shape + 126-132 Crossref resolve/guard + 134 JW start), bin/lib/verify/freshness.ts line 27 (import alias) + 131-145 (retraction_details usage), bin/lib/sources/retraction-watch.ts lines 93-127 (fetchById null-on-error guarantee), 14-RESEARCH.md lines 410-458 + Pitfall 1 (568-574), tests/gate-retraction.test.ts (the contract).</read_first>
  <behavior>
    - A citation whose claimed.DOI is the live-retracted cassette DOI (10.0000/gate03-retracted) → verdictForCitekey returns verdict 'MIS-CITED' with a reason naming the live re-query at verify time (and the retraction_details if present).
    - A citation whose claimed.DOI has no retraction cassette entry (fetchById returns null = transport/no-hit) → the retraction path does NOT short-circuit; the verdict falls through to the normal JW outcome (OK / MIS-CITED-by-JW) — NO false MIS-CITED attributed to retraction.
    - The stored claimed.retracted fast-path at 113-118 still returns MIS-CITED before any DOI resolution (offline-fast path unchanged — regression in known-bad-citations stays green).
    - The re-query is placed AFTER the Crossref null-guard, so a FABRICATED (DOI did not resolve) citation never reaches the retraction query (Pitfall 1).
  </behavior>
  <action>
    Add the import alias to pass1.ts top (mirror freshness.ts:27): `import { fetchById as retractionWatchFetchById } from '../sources/retraction-watch.js';`. Inside verdictForCitekey, insert the GATE-03 block immediately AFTER the Crossref null-guard (after line 132, where `actual` is confirmed non-null) and BEFORE the titleJW computation at line 134: query `const liveRetraction = await retractionWatchFetchById(claimed.DOI);` and `if (liveRetraction !== null) { const why = liveRetraction.retraction_details ? \`: ${liveRetraction.retraction_details}\` : ''; return { citekey: ck, verdict: 'MIS-CITED', titleJW: 0, authorJW: 0, reason: \`cited work appears in Retraction Watch (live re-query at verify time)${why}\` }; }`. Use the EXACT claimed.DOI value already verified via Crossref (Pitfall 1 — do NOT re-derive or normalize differently). Do NOT add a try/catch — fetchById already returns null on transport error (retraction-watch.ts catch at ~122-126), so null is the silent-skip signal; a thrown wrapper would be redundant and could mask the offline path. Leave the claimed.retracted fast-path (113-118), the !claimed.DOI guard, the Crossref resolve, and the JW/redirect logic UNCHANGED.
  </action>
  <verify>
    <automated>node --import tsx --test tests/gate-retraction.test.ts tests/known-bad-citations.test.ts</automated>
  </verify>
  <done>gate-retraction live-hit-blocks + transport-error-silent-skip assertions PASS (no longer skip); known-bad-citations regression (claimed.retracted still blocks) green; pass1.ts imports retractionWatchFetchById; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pass1.ts → Retraction Watch (network via http.ts) | the live re-query crosses to an external service; an unreachable service must not corrupt the verdict |
| resolved DOI → retraction-watch adapter | the DOI string is the query key; the wrong DOI could mis-match a cassette fallback entry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Tampering | post-research retraction not blocked (gate-bypass) | mitigate | live re-query on the Crossref-confirmed DOI; a non-null hit returns MIS-CITED (blocking) in the deterministic Pass-1 path |
| T-14-03-FALSEBLOCK | Denial of Service / Correctness | false MIS-CITED on an unreachable Retraction Watch | mitigate | fetchById returns null on any transport error (never throws); null = silent skip → the verdict falls through to JW; no try/catch added that could misclassify |
| T-14-03-FALLBACK | Spoofing | cassette fallback matches a DIFFERENT DOI's retraction (Pitfall 1) | mitigate | place the query AFTER Crossref resolution and use the verified claimed.DOI; FABRICATED citations never reach the query |
| T-14-03-LEAK | Information Disclosure | API key / PII leaking through the retraction re-query | mitigate | all network delegated to the existing retraction-watch adapter routed through http.ts (no new network path; HARD-02 SSRF is Phase-15 scope); no secret/PII enters the query (DOI only) |
</threat_model>

<verification>
- `node --import tsx --test tests/gate-retraction.test.ts` → live-retraction-blocks PASS; transport-error-silent-skip PASS (no false MIS-CITED).
- `node --import tsx --test tests/known-bad-citations.test.ts` → stored claimed.retracted fast-path still blocks (regression green).
- `grep` confirms pass1.ts imports retractionWatchFetchById and the query sits after the Crossref null-guard.
- `npm test` → full suite green (offline, cassette-driven, deterministic).
</verification>

<success_criteria>
- A live-retracted DOI at verify time → MIS-CITED (blocking) in Pass-1.
- A transport error / no-hit → silent skip (never a false block).
- Stored claimed.retracted offline-fast path unchanged and still blocking.
- Re-query placed after Crossref resolution (Pitfall 1 honored); no new network path (http.ts adapter reused).
</success_criteria>

<output>
Create `.planning/phases/14-fail-closed-verifier-gate/14-03-SUMMARY.md` when done
</output>
