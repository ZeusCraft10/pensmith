---
phase: 03-vertical-slice-one-section
plan: 05
type: execute
wave: 4
depends_on: ["03-00", "03-03"]
files_modified:
  - templates/prompts/intake-clarifier.md
  - templates/prompts/topic-disambiguator.md
  - templates/prompts/source-evaluator.md
  - templates/prompts/outline-author.md
  - templates/prompts/section-planner.md
  - templates/prompts/section-drafter.md
  - templates/prompts/pass1-fuzzy-judge.md
  - templates/prompts/pass3-quote-checker.md
  - templates/citation-styles/apa.csl
  - templates/presets/disciplines.json
autonomous: true
requirements:
  - INTK-01
  - INTK-02
  - INTK-03
  - INTK-04
  - RSCH-02
  - OUTL-01
  - OUTL-02
  - PLAN-01
  - WRTE-01
  - WRTE-03
  - VRFY-01
  - VRFY-04
  - VRFY-05
  - CITE-01
  - CITE-04
  - TEST-03
must_haves:
  truths:
    - "8 prompt files exist in templates/prompts/ using D-12 LOCKED slugs: intake-clarifier, topic-disambiguator, source-evaluator, outline-author, section-planner, section-drafter, pass1-fuzzy-judge, pass3-quote-checker"
    - "apa.csl is the bundled Citation Style Language template, hash-pinned (D-19)"
    - "templates/presets/disciplines.json contains the 8 default disciplines (per CONTEXT domain + REQUIREMENTS.md INTK-03 set: psychology, computer-science, history, philosophy, biology, sociology, economics, literature) each with defaultTone + defaultCitationStyle (INTK-04, INTK-03)"
    - "Phase 3 prompts ship hash-pinned but DORMANT per D-13: workflows/verify.md (Plan 06) MUST NOT invoke pass1-fuzzy-judge.md or pass3-quote-checker.md in the hot path — deterministic Pass-1/Pass-3 algorithm alone produces the verdict; the LLM prompts are calibrated for Phase 8 tie-break only"
    - "section-drafter is calibrated to NEVER invent DOIs, mark unverifiable claims explicitly, restrict to assigned_sources only (D-13, D-21, PRD §7.6)"
    - "Hash-pinning of all 8 prompts + apa.csl happens via the single-source-of-truth EXPECTED_PROMPT_HASHES map in bin/lib/prompt-loader.ts (created in Plan 07). This plan does NOT directly edit tests/repo-files.test.ts — Plan 09 wires the final re-pin after all 8 prompt bytes are stable"
  artifacts:
    - path: "templates/prompts/intake-clarifier.md"
      provides: "PRD §7.1 intake clarifying battery prompt (D-12 LOCKED slug, INTK-01, INTK-02, INTK-03)"
    - path: "templates/prompts/topic-disambiguator.md"
      provides: "Topic-disambiguation half of the research stage — narrows ambiguous assignment topic into a researchable scope (D-12 LOCKED slug, RSCH-02)"
    - path: "templates/prompts/source-evaluator.md"
      provides: "Source-evaluation half of the research stage — judges candidate sources for relevance/credibility post-search (D-12 LOCKED slug, RSCH-02)"
    - path: "templates/prompts/outline-author.md"
      provides: "Outline author — generates section list with slugs + depends_on (D-12 LOCKED slug, OUTL-01, OUTL-02)"
    - path: "templates/prompts/section-planner.md"
      provides: "Section planner — emits PlanFrontmatter-conformant YAML + assigned_sources (D-12 LOCKED slug, PLAN-01)"
    - path: "templates/prompts/section-drafter.md"
      provides: "Section drafter — Pandoc [@citekey] tokens only, NEVER invents DOIs (D-12 LOCKED slug, D-21, WRTE-01, WRTE-03, D-13)"
    - path: "templates/prompts/pass1-fuzzy-judge.md"
      provides: "Pass-1 LLM tie-break judge — DORMANT in Phase 3 per D-13 (calibrated for Phase 8 ambiguous-fuzzy-match arbitration only)"
    - path: "templates/prompts/pass3-quote-checker.md"
      provides: "Pass-3 LLM quote-checker — DORMANT in Phase 3 per D-13 (calibrated for Phase 8 ambiguous-quote-match arbitration only)"
    - path: "templates/citation-styles/apa.csl"
      provides: "Bundled APA 7 CSL template (D-19, CITE-04) — hash-pinned to prevent silent drift"
    - path: "templates/presets/disciplines.json"
      provides: "9 effective discipline presets — the 8 INTK-03 disciplines (psychology, computer-science, history, philosophy, biology, sociology, economics, literature) PLUS an explicit `other` fallback entry (REVIEWS amendment, see Task 5.3) — with tone + citation style (INTK-04, INTK-03 set). CYCLE-2 LOW MEDIUM REVIEWS CONVERGENCE: prose elsewhere in this plan that says \"8-discipline\" / \"8 disciplines\" refers to the INTK-03 baseline set; the actual disciplines.json has 9 keys including `other`."
  key_links:
    - from: "templates/citation-styles/apa.csl"
      to: "bin/lib/citations.ts renderApa"
      via: "caller reads apa.csl from disk and passes its content as cslTemplateString to renderApa()"
      pattern: "readFileSync.*apa.csl"
    - from: "templates/prompts/*.md"
      to: "bin/lib/prompt-loader.ts EXPECTED_PROMPT_HASHES"
      via: "Plan 07 creates the single-source-of-truth pin map; Plan 09 finalizes the SHA-256 values after byte-stable rebuild"
      pattern: "EXPECTED_PROMPT_HASHES"
---

<objective>
Land the 8 D-12 LOCKED prompt files + apa.csl + 9-key disciplines.json (the 8 INTK-03 disciplines + `other` fallback). Prompts are the production-grade calibration of intake/research/outline/plan/write for Phase 3, plus the DORMANT (D-13) Phase-8-targeted tie-break prompts for Pass-1/Pass-3. Hash-pinning prevents silent drift — any prompt edit must consciously update the pin (CONTRIBUTING.md rule from Phase 2 already covers the workflow).

Purpose: Wave-4 workflow bodies (Plan 06) and verb implementations (Plan 07) cannot run without these prompt files at the EXACT D-12 LOCKED slugs. Hash-pinning closes T-3-09 (silent prompt drift) once Plan 07 wires EXPECTED_PROMPT_HASHES and Plan 09 finalizes the SHA values.

Output: 8 prompts (D-12 LOCKED slugs) + 1 CSL + 1 JSON preset with 9 effective keys (8 INTK-03 disciplines + explicit `other` fallback per Task 5.3 REVIEWS amendment). CYCLE-2 LOW prose sweep: "8-discipline" / "8 disciplines" phrasing throughout this plan refers to the INTK-03 baseline; the concrete disciplines.json has 9 keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@PRD.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-vertical-slice-one-section/03-CONTEXT.md
@.planning/phases/03-vertical-slice-one-section/03-RESEARCH.md
@tests/repo-files.test.ts
@templates/citation-styles/

<interfaces>
D-12 LOCKED prompt slugs (CONTEXT.md, verbatim):
- intake-clarifier
- topic-disambiguator
- source-evaluator
- outline-author
- section-planner
- section-drafter
- pass1-fuzzy-judge        (DORMANT in Phase 3 — D-13)
- pass3-quote-checker      (DORMANT in Phase 3 — D-13)

D-13 LOCKED stance: Phase 3's deterministic Pass-1 (Jaro-Winkler fuzzy match) and Pass-3 (Levenshtein quote match) algorithms produce the verdict on their own. The pass1-fuzzy-judge and pass3-quote-checker prompts ship hash-pinned but are NOT invoked by workflows/verify.md in Phase 3. They are calibrated for Phase 8 ambiguous-case tie-break.

D-20 LOCKED canonical BibTeX: `.paper/CITATIONS.bib` is written by the research verb (Plan 04 RSCH-09 task) using bin/lib/bibtex-write.ts (or an extension of bin/lib/citations.ts under the D-19 chokepoint). It is read at verify time by bin/lib/citations.ts (citation-js parser).

INTK-04 disciplines preset shape (REQUIREMENTS.md INTK-03 set: CS/Bio/History/Lit/Psych/Econ/Philosophy/Other → mapped to canonical slugs):
```json
{
  "psychology":       { "defaultTone": "academic-formal", "defaultCitationStyle": "apa" },
  "computer-science": { "defaultTone": "technical",        "defaultCitationStyle": "apa" },
  "history":          { "defaultTone": "narrative",        "defaultCitationStyle": "chicago-author-date" },
  "philosophy":       { "defaultTone": "argumentative",    "defaultCitationStyle": "chicago-author-date" },
  "biology":          { "defaultTone": "technical",        "defaultCitationStyle": "apa" },
  "sociology":        { "defaultTone": "academic-formal", "defaultCitationStyle": "apa" },
  "economics":        { "defaultTone": "analytical",       "defaultCitationStyle": "apa" },
  "literature":       { "defaultTone": "interpretive",     "defaultCitationStyle": "mla" }
}
```
Note: Phase 3 ships only apa.csl; the preset references chicago-author-date + mla but those files don't yet exist (deferred to a later phase; loader must degrade gracefully — fall back to apa.csl when a referenced style file is absent).

Hash-pinning strategy (per D-12):
- This plan WRITES the 8 prompt files at the locked slugs.
- Plan 07 creates `bin/lib/prompt-loader.ts` with an `EXPECTED_PROMPT_HASHES` map keyed by the same 8 slugs.
- Plan 07 refactors `tests/repo-files.test.ts` to drive its hash-pin tests from `EXPECTED_PROMPT_HASHES` (single source of truth — drift between the two becomes structurally impossible).
- Plan 09 finalizes the SHA-256 values after all files are byte-stable.
- This plan therefore does NOT add prompt hash-pins directly to tests/repo-files.test.ts. The apa.csl hash-pin (Task 5.3) DOES land here because it uses a different decision (D-19) and is not part of the prompt-loader map.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 5.1: Write 6 active prompt files at D-12 LOCKED slugs</name>
  <files>templates/prompts/intake-clarifier.md, templates/prompts/topic-disambiguator.md, templates/prompts/source-evaluator.md, templates/prompts/outline-author.md, templates/prompts/section-planner.md, templates/prompts/section-drafter.md</files>
  <read_first>
    - PRD.md §7.1 (intake), §7.2 (research), §7.3 (outline), §7.4 (plan), §7.5 (write)
    - .planning/phases/03-vertical-slice-one-section/03-CONTEXT.md (D-12 LOCKED slug list verbatim, D-13 DORMANT stance, D-20 canonical BibTeX, D-21 Pandoc tokens)
    - .planning/REQUIREMENTS.md (INTK-03 8-discipline set the intake-clarifier surfaces)
    - .planning/phases/03-vertical-slice-one-section/03-RESEARCH.md (§ Validation — deterministic Pass 1 + Pass 3)
  </read_first>
  <action>
    Each prompt is a Markdown file with the following structure:
    ```markdown
    ---
    promptId: <slug>
    decision: D-12 (hash-pinned LOCKED slug)
    requirements: [<REQ-IDs>]
    ---

    # <Title>

    ## Role
    <Single sentence: who you are>

    ## Inputs
    <What variables the workflow passes in via {{ }} placeholders>

    ## Task
    <What to produce, in 2-5 bullets>

    ## Hard Constraints
    - NEVER do X
    - ALWAYS do Y

    ## Output Format
    <Exact format expected — JSON / YAML / Markdown structure>
    ```

    Content guidance per prompt:

    **intake-clarifier.md** (INTK-01/02/03 — D-12 LOCKED slug):
    - promptId: intake-clarifier
    - Role: Academic writing intake assistant
    - Inputs: {{assignment}}
    - Task: Ask the user 3-5 clarifying questions about scope, length, citation style, discipline, audience
    - Surface the 8 INTK-03 disciplines as suggested values in the discipline question: CS, Bio, History, Lit, Psych, Econ, Philosophy, Other (mapped client-side to the 8 preset keys in templates/presets/disciplines.json)
    - Output: numbered question list (1 question per line)

    **topic-disambiguator.md** (RSCH-02 — D-12 LOCKED slug, replaces former "research-queries"):
    - promptId: topic-disambiguator
    - Role: Topic disambiguator — narrows ambiguous assignment topic into a researchable scope BEFORE search queries fire
    - Inputs: {{topic}}, {{discipline}}, {{assignment}}
    - Task: Identify 1-3 plausible scopes the assignment could mean, propose 5-10 specific search queries per chosen scope (NOT broad — each ≤8 words, includes a discipline-specific term)
    - Hard constraint: NEVER invent DOIs, journal names, or author names in queries
    - Hard constraint: surface ambiguity rather than guessing — if the topic could mean multiple things, list them and let the workflow decide (in --yolo mode the workflow picks scope #1)
    - Output: JSON object `{ "scopes": [{ "label": string, "queries": string[] }] }`

    **source-evaluator.md** (RSCH-02 — D-12 LOCKED slug, replaces other half of former "research-queries"):
    - promptId: source-evaluator
    - Role: Source evaluator — judges candidate sources for relevance/credibility POST-search, BEFORE the workflow writes .paper/LIBRARY.json
    - Inputs: {{candidateSources}} (array of SourceCandidate from the 7 adapters, pre-dedupe)
    - Task: For each candidate emit a brief verdict (`{citekey, keep: boolean, reason: string}`); the workflow filters on `keep: true` before persistence
    - Hard constraint: NEVER invent metadata; only judge what the adapters returned
    - Hard constraint: prefer recent peer-reviewed sources for empirical claims; allow older canonical works for theoretical framing
    - Output: JSON array of verdict objects

    **outline-author.md** (OUTL-01/02 — D-12 LOCKED slug, replaces former "outline-propose"):
    - promptId: outline-author
    - Role: Outline architect
    - Inputs: {{topic}}, {{length}}, {{candidateSources}} (array of SourceCandidate, post source-evaluator filter)
    - Task: Propose 3-7 sections, each with: number, slug (kebab-case), title, depends_on (array of slugs), estimated_word_count
    - Hard constraint: depends_on must reference sections by slug (D-03)
    - Hard constraint: NO self-reference in depends_on (D-04)
    - Output: YAML matching PlanFrontmatter shape (top-level: list of sections)

    **section-planner.md** (PLAN-01 — D-12 LOCKED slug, replaces former "plan-section"):
    - promptId: section-planner
    - Role: Section planner
    - Inputs: {{section}} (number, slug, title, depends_on), {{candidateSources}}
    - Task: Assign 3-15 SourceCandidate citekeys to assigned_sources, write a 100-200 word section brief
    - Hard constraint: assigned_sources MUST be citekeys that exist in candidateSources (no inventions)
    - Output: PLAN.md frontmatter YAML + a "## Brief" Markdown section

    **section-drafter.md** (WRTE-01/03, D-21, D-13 — D-12 LOCKED slug, replaces former "write-section"):
    - promptId: section-drafter
    - Role: Section drafter
    - Inputs: {{section}}, {{brief}}, {{assignedSources}} (array of SourceCandidate, restricted to this section's mapped sources only — PRD §7.6)
    - Task: Write the section in Markdown using Pandoc [@citekey] tokens for citations
    - Hard constraint: NEVER invent a DOI, author name, year, or journal. Every citation MUST be `[@<citekey>]` where citekey is in assignedSources.
    - Hard constraint: NEVER use `[1]` or `(Author, 2024)` style — Pandoc tokens only (D-21)
    - Hard constraint: If a claim cannot be supported by assignedSources, write it WITHOUT a citation rather than fabricating one (the orphan-claim verifier will flag it later — D-13)
    - Output: Markdown body only (no frontmatter; the workflow wraps it)

    For each prompt, prefer concrete examples over abstract instructions. Each prompt should be 30-80 lines.

    The two DORMANT prompts (pass1-fuzzy-judge, pass3-quote-checker) are created in Task 5.2, not here.
  </action>
  <verify>
    <automated>ls templates/prompts/intake-clarifier.md templates/prompts/topic-disambiguator.md templates/prompts/source-evaluator.md templates/prompts/outline-author.md templates/prompts/section-planner.md templates/prompts/section-drafter.md 2>&1 | grep -v "No such" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - All 6 prompt files exist in templates/prompts/ at the EXACT D-12 LOCKED slugs (intake-clarifier, topic-disambiguator, source-evaluator, outline-author, section-planner, section-drafter)
    - Each prompt has YAML frontmatter with `promptId` MATCHING the filename slug, `decision: D-12 (hash-pinned LOCKED slug)`, `requirements: [...]`
    - Each prompt has a `## Hard Constraints` section
    - section-drafter.md contains the literal substring "Pandoc [@citekey]" AND "NEVER invent"
    - topic-disambiguator.md output is `{ "scopes": [...] }` (JSON object, not bare array — distinguishes it from source-evaluator)
    - source-evaluator.md operates on POST-search candidates (`{{candidateSources}}`), not pre-search query terms (distinct from topic-disambiguator)
    - No prompt file exceeds 4096 bytes (keeps hash-pinned files small + reviewable)
    - NO file at the old slugs exists (intake-clarify, research-queries, outline-propose, plan-section, write-section, draft-summary, verify-pass1, verify-pass3 must all be absent)
  </acceptance_criteria>
  <done>
    6 active prompts written at D-12 LOCKED slugs with D-13 calibration baked in: NEVER invent, Pandoc-tokens-only, restricted-view drafter. Files small + reviewable. Old slugs absent.
  </done>
</task>

<task type="auto">
  <name>Task 5.2: Write 2 DORMANT tie-break prompts (pass1-fuzzy-judge, pass3-quote-checker) per D-13</name>
  <files>templates/prompts/pass1-fuzzy-judge.md, templates/prompts/pass3-quote-checker.md</files>
  <read_first>
    - .planning/phases/03-vertical-slice-one-section/03-CONTEXT.md (D-12 LOCKED slugs, D-13 DORMANT stance verbatim)
    - templates/prompts/intake-clarifier.md (frontmatter shape reference from Task 5.1)
  </read_first>
  <action>
    These 2 prompts ship hash-pinned but are NOT invoked by Phase 3's workflows/verify.md. They are calibrated for Phase 8 ambiguous-case tie-break. The DORMANT status MUST be encoded in the frontmatter AND in the prompt body so that any future invocation site (or human reviewer) sees the intent.

    **pass1-fuzzy-judge.md** (VRFY-01 — D-12 LOCKED slug, D-13 DORMANT):
    ```markdown
    ---
    promptId: pass1-fuzzy-judge
    decision: D-12 (hash-pinned LOCKED slug)
    dormant_in_phase: 3
    dormant_reason: "D-13 — Phase 3 deterministic Pass-1 (Jaro-Winkler) is the sole verdict source. This prompt is calibrated for Phase 8 ambiguous-fuzzy-match tie-break (e.g., titleJW between 0.85 and 0.92 — the band where the deterministic gate alone may misjudge). DO NOT invoke from workflows/verify.md in Phase 3."
    requirements: [VRFY-01]
    ---

    # Pass 1 Fuzzy Judge (Phase 8 tie-break — DORMANT in Phase 3)

    ## Role
    LLM tie-break judge for ambiguous Pass-1 citation-integrity cases. ONLY invoked when the deterministic Jaro-Winkler verdict falls inside the calibration band defined by the workflow (Phase 8 only).

    ## Inputs
    {{citekey}}, {{claimedTitle}}, {{claimedAuthor}}, {{foundTitle}}, {{foundAuthor}}, {{titleJW}}, {{authorJW}}

    ## Task
    Decide whether the claimed and found metadata refer to the same work. Output JSON: `{ "verdict": "OK" | "MIS-CITED", "reason": string }`.

    ## Hard Constraints
    - This prompt is DORMANT in Phase 3 (D-13). The deterministic Jaro-Winkler verdict alone is authoritative.
    - When invoked in Phase 8: NEVER alter the verdict outside the calibration band. If both JW scores are clearly above or clearly below threshold, you MUST defer to the deterministic verdict.
    - NEVER fetch additional metadata. Decide from the 7 inputs only.

    ## Output Format
    Single JSON object: `{ "verdict": "OK" | "MIS-CITED", "reason": "<≤200 chars>" }`
    ```

    **pass3-quote-checker.md** (VRFY-04/05 — D-12 LOCKED slug, D-13 DORMANT):
    ```markdown
    ---
    promptId: pass3-quote-checker
    decision: D-12 (hash-pinned LOCKED slug)
    dormant_in_phase: 3
    dormant_reason: "D-13 — Phase 3 deterministic Pass-3 (Levenshtein-substring after NFKC normalization) is the sole verdict source. This prompt is calibrated for Phase 8 ambiguous-quote-match tie-break (e.g., levRatio between 0.90 and 0.95 — the band where the deterministic gate alone may misjudge paraphrase vs distortion). DO NOT invoke from workflows/verify.md in Phase 3."
    requirements: [VRFY-04, VRFY-05]
    ---

    # Pass 3 Quote Checker (Phase 8 tie-break — DORMANT in Phase 3)

    ## Role
    LLM tie-break judge for ambiguous Pass-3 quote-integrity cases. ONLY invoked when the deterministic Levenshtein verdict falls inside the calibration band defined by the workflow (Phase 8 only).

    ## Inputs
    {{quote}}, {{pdfContext}} (a ±500-char window around the best-match region from the PDF), {{levRatio}}

    ## Task
    Decide whether `quote` is a faithful (possibly paraphrased) extract of the surrounding PDF context, or a NOT_FOUND fabrication/distortion.

    ## Hard Constraints
    - This prompt is DORMANT in Phase 3 (D-13). The deterministic Levenshtein verdict alone is authoritative.
    - When invoked in Phase 8: NEVER alter the verdict outside the calibration band.
    - NEVER hallucinate additional context. Decide from the 3 inputs only.

    ## Output Format
    Single JSON object: `{ "verdict": "OK" | "NOT_FOUND", "reason": "<≤200 chars>" }`
    ```
  </action>
  <verify>
    <automated>grep -l "dormant_in_phase: 3" templates/prompts/pass1-fuzzy-judge.md templates/prompts/pass3-quote-checker.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist at templates/prompts/pass1-fuzzy-judge.md and templates/prompts/pass3-quote-checker.md
    - Each file's frontmatter contains `dormant_in_phase: 3` AND a `dormant_reason` field referencing D-13
    - Each file's body contains the literal substring "DORMANT in Phase 3"
    - Each file's body contains the literal substring "calibration band" (signals the Phase 8 tie-break role)
    - Each file ≤ 4096 bytes
    - NO file at the old slugs verify-pass1.md or verify-pass3.md exists
  </acceptance_criteria>
  <done>
    Both DORMANT prompts shipped at D-12 LOCKED slugs with the D-13 stance encoded in both frontmatter and body. workflows/verify.md (Plan 06) MUST NOT import or invoke them.
  </done>
</task>

<task type="auto">
  <name>Task 5.3: Add apa.csl + 8-discipline disciplines.json preset</name>
  <files>templates/citation-styles/apa.csl, templates/presets/disciplines.json</files>
  <read_first>
    - templates/citation-styles/ directory (currently empty per discovery)
    - tests/citation-render.test.ts (the smoke test that consumes apa.csl)
    - .planning/REQUIREMENTS.md (INTK-03 — defines the 8-discipline set: CS/Bio/History/Lit/Psych/Econ/Philosophy/Other)
    - .planning/phases/03-vertical-slice-one-section/03-CONTEXT.md (D-19, INTK-04, Domain section confirming 8 presets)
  </read_first>
  <action>
    1. Create `templates/citation-styles/apa.csl`:
       - Source: Official APA 7 CSL from https://github.com/citation-style-language/styles/blob/master/apa.csl
       - At the top of the file, prepend an XML comment:
         ```xml
         <!--
           Pensmith bundled APA 7 CSL — D-19, CITE-04
           Source: github.com/citation-style-language/styles/blob/<COMMIT-SHA>/apa.csl
           Date pinned: 2026-05-17
           Hash-pin: tests/repo-files.test.ts asserts SHA-256 stability
           To update: replace this file, run `npm test -- --test-name-pattern="apa.csl hash-pin"`,
                      copy the new SHA into the test, commit BOTH changes in a single commit.
         -->
         ```
       - Otherwise the file content is the verbatim upstream CSL XML.
       - Fetch via: `curl -fsSL https://raw.githubusercontent.com/citation-style-language/styles/master/apa.csl -o templates/citation-styles/apa.csl` then prepend the comment block.
       - Verify it's valid XML: `node -e "require('node:fs').readFileSync('templates/citation-styles/apa.csl','utf8').includes('<?xml')"` should print true.

    2. Create `templates/presets/disciplines.json` with EXACTLY 8 presets per REQUIREMENTS.md INTK-03 + CONTEXT.md Domain section (mapped to canonical kebab-case slugs):
       ```json
       {
         "psychology":       { "defaultTone": "academic-formal", "defaultCitationStyle": "apa" },
         "computer-science": { "defaultTone": "technical",        "defaultCitationStyle": "apa" },
         "history":          { "defaultTone": "narrative",        "defaultCitationStyle": "chicago-author-date" },
         "philosophy":       { "defaultTone": "argumentative",    "defaultCitationStyle": "chicago-author-date" },
         "biology":          { "defaultTone": "technical",        "defaultCitationStyle": "apa" },
         "sociology":        { "defaultTone": "academic-formal", "defaultCitationStyle": "apa" },
         "economics":        { "defaultTone": "analytical",       "defaultCitationStyle": "apa" },
         "literature":       { "defaultTone": "interpretive",     "defaultCitationStyle": "mla" }
       }
       ```
       INTK-03 mapping: CS→computer-science, Bio→biology, History→history, Lit→literature, Psych→psychology, Econ→economics, Philosophy→philosophy, Other→handled by the intake-clarifier prompt as a free-text fallback (no preset entry — the workflow degrades to defaultTone="academic-formal", defaultCitationStyle="apa" when discipline ∉ presets).

    3. Run `npm test -- --test-name-pattern="citation-render"` to confirm apa.csl satisfies the Plan 02 smoke test (Vaswani 2017 entry → APA-formatted string containing "Attention is all you need" + "á" from the accent fixture).

    **REVIEWS CONVERGENCE — CSL provenance SHA pin (Codex LOW substantive consensus #16, OpenCode LOW).** Replace `<COMMIT-SHA>` placeholder with a concrete pinned SHA in the comment block. Update the action to:
    - Fetch from a SPECIFIC, hash-pinned commit URL, NOT `master` HEAD: `curl -fsSL https://raw.githubusercontent.com/citation-style-language/styles/3e21d5f3f9c52ec3f3b9c2bd31bd62e8e7a09a30/apa.csl -o templates/citation-styles/apa.csl` (the SHA must be locked at write time; if upstream is unreachable at write, use the most recent commit that was reachable, pin it, document the date).
    - The provenance comment block becomes:
      ```xml
      <!--
        Pensmith bundled APA 7 CSL — D-19, CITE-04
        Source: github.com/citation-style-language/styles/blob/<EXACT-40-CHAR-SHA>/apa.csl
        Upstream commit pinned: <full SHA, set at file-write time>
        Date pinned: 2026-05-21
        Hash-pin: tests/repo-files.test.ts asserts SHA-256 of THIS file (post-comment-prepend) is stable
        To update: replace this file, copy the new SHA into the test, commit BOTH in a single commit.
      -->
      ```
    - tests/repo-files.test.ts asserts the apa.csl SHA-256 (D-19 pin) AND that the file body (after the comment) round-trips through citation-js without error.

    **REVIEWS CONVERGENCE — "Other" discipline preset (OpenCode LOW substantive consensus #17).** Reviewers flagged that the README/intake surface lists "Other" but `disciplines.json` has no "other" entry, forcing the workflow into a special-case fallback that's easy to drift. Lock both options:
    - **Option A (chosen):** Add an explicit `"other"` entry to disciplines.json:
      ```json
      "other": { "defaultTone": "academic-formal", "defaultCitationStyle": "apa" }
      ```
      This makes the preset table self-documenting (9 keys now). The intake-clarifier prompt still asks for free-text when user picks "Other", but the workflow no longer needs a special-case branch.
    - Acceptance criterion update: disciplines.json now has EXACTLY 9 keys (the 8 INTK-03 disciplines PLUS `other`).
    - workflows/intake.md (Plan 06) is amended in parallel: when discipline is "Other" with free-text, the free text is logged but `disciplines.other` provides the tone/style defaults.

    **REVIEWS CONVERGENCE — DORMANT prompt drift guard (OpenCode LOW substantive consensus #13).** Add an acceptance-criterion grep in Plan 06 verify.md body amendment: `grep -E "pass1-fuzzy-judge|pass3-quote-checker" workflows/verify.md` MUST return 0. This is the structural enforcement that the DORMANT prompts cannot be invoked from Phase 3 hot path. Documented here for cross-reference; the test sentinel lives in Plan 00 Wave 0 (tests/repo-files.test.ts).
  </action>
  <verify>
    <automated>npm test -- --test-name-pattern="citation-render"</automated>
  </verify>
  <acceptance_criteria>
    - `templates/citation-styles/apa.csl` exists, contains `<?xml` near the top
    - File starts with the Pensmith provenance comment block
    - `tests/citation-render.test.ts` PASSES (no skip — apa.csl is now present)
    - `templates/presets/disciplines.json` parses as JSON with EXACTLY 9 keys: psychology, computer-science, history, philosophy, biology, sociology, economics, literature, other (REVIEWS amendment — `other` added as explicit fallback)
    - Each preset value has `defaultTone` and `defaultCitationStyle` fields
    - The 9 keys: 8 INTK-03 disciplines + `other` (REVIEWS amendment): CS→computer-science, Bio→biology, History→history, Lit→literature, Psych→psychology, Econ→economics, Philosophy→philosophy, "Other"→`other` preset (NOT a special-case fallback)
  </acceptance_criteria>
  <done>
    apa.csl bundled and proves out via citation-render smoke; 8-discipline preset lands per INTK-03 + CONTEXT Domain.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prompt file content → LLM context | Prompts are interpolated into LLM prompts at runtime; silent edits change model behavior. |
| apa.csl content → citation-js render | Edits change every rendered bibliography across the project. |
| disciplines.json → intake defaults | Edits change default tone/citation-style for users. |
| DORMANT prompt slugs → workflows/verify.md | D-13 forbids invocation in Phase 3. Encoded in frontmatter `dormant_in_phase: 3`; Plan 06 verify body audit MUST surface zero references. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-09 | Tampering | Silent drift of prompts or apa.csl | mitigate | SHA-256 hash-pin on all 8 prompts via single-source-of-truth EXPECTED_PROMPT_HASHES map (Plan 07) consumed by tests/repo-files.test.ts. apa.csl pinned independently here (D-19). CONTRIBUTING.md rule (Phase 2 CF-D24) requires intentional pin update with the edit. |
| T-3-PROMPT-01 | Tampering | Prompt edit weakens deterministic-gate calibration (e.g., removes "NEVER invent") | mitigate | D-13 calibration baked into section-drafter.md hard constraints. Hash-pin enforces awareness of any change; reviewer must judge whether the new prompt preserves the gate's intent. |
| T-3-DORMANT-01 | Tampering | DORMANT prompt invoked in Phase 3 hot path | mitigate | dormant_in_phase: 3 in frontmatter + DORMANT in title + Plan 06 verify body audit (`grep -E "invoke|prompt|Task" workflows/verify.md` must return 0 LLM-invocation matches per BL-2 enforcement). |
| T-3-CSL-01 | Tampering | apa.csl edit breaks bibliographic rendering | mitigate | hash-pin + tests/citation-render.test.ts asserts the Vaswani fixture renders the expected substring. |

</threat_model>

<verification>
- `npm test -- --test-name-pattern="citation-render"` green
- `ls templates/prompts/*.md` returns exactly 8 files at the D-12 LOCKED slugs (and zero files at the old slugs)
- `ls templates/citation-styles/` includes apa.csl
- `ls templates/presets/disciplines.json` exists and parses as JSON with EXACTLY 9 keys (8 INTK-03 + `other` per Task 5.3 REVIEWS amendment) — CYCLE-2 LOW prose sweep: prior "8 keys" wording was an off-by-one carried forward from the pre-REVIEWS Plan 05 draft
- `npm run lint` + `npm run typecheck` green
- No file in templates/prompts/ exceeds 4096 bytes
- `grep -c "dormant_in_phase: 3" templates/prompts/*.md` returns 2 (the two DORMANT prompts)
</verification>

<success_criteria>
1. 8 prompts at D-12 LOCKED slugs (6 active + 2 DORMANT) + apa.csl + 9-key disciplines.json land (8 INTK-03 disciplines + `other` fallback per Task 5.3 REVIEWS amendment; CYCLE-2 LOW prose sweep).
2. D-13 stance encoded in DORMANT prompt frontmatter + body (workflows/verify.md MUST NOT invoke them in Phase 3).
3. Phase-3 calibration ("NEVER invent", Pandoc-tokens-only, restricted-view drafter) baked into section-drafter.md.
4. tests/citation-render.test.ts green against the real apa.csl.
5. disciplines.json contains the 8 INTK-03 presets (psychology, computer-science, history, philosophy, biology, sociology, economics, literature) PLUS the `other` fallback key (9 effective keys total per Task 5.3 REVIEWS amendment; CYCLE-2 LOW prose sweep).
6. Old prompt slugs (intake-clarify, research-queries, outline-propose, plan-section, write-section, draft-summary, verify-pass1, verify-pass3) absent from templates/prompts/.
</success_criteria>

<output>
Create `.planning/phases/03-vertical-slice-one-section/03-05-SUMMARY.md` with:
- 8 D-12 LOCKED prompt slugs + their SHA-256 hashes (for Plan 07 to seed EXPECTED_PROMPT_HASHES)
- apa.csl SHA-256 + provenance commit
- Prompt byte sizes (all ≤ 4096)
- 9-key disciplines.json key list = 8 INTK-03 + `other` (confirms INTK-03 mapping + the REVIEWS-added `other` fallback; CYCLE-2 LOW prose sweep)
- DORMANT-status confirmation: `grep -c "dormant_in_phase: 3"` output
</output>
