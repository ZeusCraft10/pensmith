---
phase: 03-vertical-slice-one-section
plan: 06
type: execute
wave: 4
depends_on: ["03-03", "03-04", "03-05"]
files_modified:
  - workflows/intake.md
  - workflows/research.md
  - workflows/outline.md
  - workflows/plan.md
  - workflows/write.md
  - workflows/verify.md
autonomous: true
requirements:
  - INTK-01
  - INTK-02
  - INTK-03
  - INTK-04
  - INTK-05
  - RSCH-01
  - RSCH-02
  - RSCH-03
  - RSCH-04
  - RSCH-08
  - RSCH-09
  - OUTL-01
  - OUTL-02
  - OUTL-03
  - OUTL-04
  - PLAN-01
  - PLAN-04
  - WRTE-01
  - WRTE-03
  - VRFY-01
  - VRFY-02
  - VRFY-04
  - VRFY-05
  - VRFY-07
  - VRFY-08
must_haves:
  truths:
    - "6 workflow bodies fill the Phase-2 stubs: intake, research, outline, plan, write, verify"
    - "Each workflow body uses the <capability_check> pattern from Phase 2 — degrades gracefully when Task/MCP/AskUserQuestion is unavailable (Tier 1 → Tier 2 equivalence)"
    - "Each workflow body delegates to the matching bin/cli/<verb>.ts (created in Plan 07) — workflow body is the prompt that drives the verb"
    - "Tier-contract test passes for all 6 new workflows (TIER-06 — preserves the Phase 2 equivalence guarantee)"
    - "Approval gates default ON (PRD non-negotiable): outline step pauses for user approval unless --yolo (OUTL-03)"
    - "Section-as-phase invariant is enforced in plan/write/verify: every action targets `.paper/sections/<NN>-<slug>/` only (ARCH-02, TEST-09)"
    - "intake workflow reads templates/presets/disciplines.json and uses templates/prompts/intake-clarifier.md (D-12 LOCKED slug, INTK-01/02/03/04)"
    - "research workflow consumes bin/lib/sources/index.ts registry (RSCH-03/04 — search across all 7 adapters) AND writes .paper/CITATIONS.bib via bin/lib/bibtex-write.ts (RSCH-09, D-20)"
    - "verify workflow is 100% DETERMINISTIC in Phase 3 (D-13): runs deterministic Pass-1 + Pass-3 only; reads .paper/CITATIONS.bib via bin/lib/citations.ts (D-19, D-20); NEVER invokes pass1-fuzzy-judge.md or pass3-quote-checker.md prompts (those are DORMANT, calibrated for Phase 8 tie-break only)"
  artifacts:
    - path: "workflows/intake.md"
      provides: "Intake workflow body (INTK-01..05)"
    - path: "workflows/research.md"
      provides: "Research workflow body (RSCH-01..04, RSCH-08, RSCH-09 — writes .paper/CITATIONS.bib)"
    - path: "workflows/outline.md"
      provides: "Outline workflow body with approval gate (OUTL-01..04)"
    - path: "workflows/plan.md"
      provides: "Per-section plan workflow body (PLAN-01, PLAN-04)"
    - path: "workflows/write.md"
      provides: "Per-section write workflow body (WRTE-01, WRTE-03)"
    - path: "workflows/verify.md"
      provides: "Per-section verify workflow body — 100% DETERMINISTIC per D-13 (VRFY-01/02/04/05/07/08)"
  key_links:
    - from: "workflows/<verb>.md"
      to: "bin/cli/<verb>.ts"
      via: "<capability_check> degrades to `pensmith <verb>` shell invocation when Task tool unavailable"
      pattern: "pensmith <verb>"
    - from: "workflows/research.md"
      to: "bin/lib/sources/index.ts + bin/lib/bibtex-write.ts"
      via: "research verb calls sources[adapter].search(query) for each adapter, then serializes filtered SourceCandidate[] to .paper/CITATIONS.bib"
      pattern: "sources\\[.*\\]\\.search|writeBibtex"
    - from: "workflows/verify.md"
      to: "bin/lib/fuzzy.ts + bin/lib/citations.ts"
      via: "deterministic Pass-1 (Jaro-Winkler) reads .paper/CITATIONS.bib via citations.ts (D-19 chokepoint, D-20 canonical BibTeX) — NO LLM invocation between Pass-1 fetch and sectionVerification write"
      pattern: "jaroWinkler|levenshteinSubstring|parseBibtex"
---

<objective>
Light up the 6 workflow body stubs that Phase 2 left as `<capability_check>` skeletons. Each workflow body is a Markdown prompt that drives the matching verb's behavior — both via the Task tool (Tier 1) and via shell invocation of `pensmith <verb>` (Tier 2). Tier equivalence is preserved by the tier-contract test (extended in Plan 00 Wave 0 to fail-red for the 6 new cases, then turning green when Plan 07 wires the verbs).

Purpose: This is the connective tissue between the Tier 1 Claude Code plugin and the Tier 2 portable CLI. Without these bodies, the workflows are empty stubs. Verify is specifically constrained per D-13: 100% deterministic in Phase 3.

Output: 6 workflow Markdown files with full body content + capability checks + approval gates + (verify) explicit no-LLM-invocation assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-vertical-slice-one-section/03-CONTEXT.md
@.planning/phases/03-vertical-slice-one-section/03-RESEARCH.md
@.planning/phases/03-vertical-slice-one-section/03-PATTERNS.md
@PRD.md
@workflows/doctor.md
@workflows/intake.md
@workflows/research.md

<interfaces>
Phase 2 already created the 16 workflow stubs (workflows/*.md) with full `<capability_check>` blocks per `02-06-PLAN.md`. This plan fills the body of 6 of them.

Pattern from workflows/doctor.md (the only currently-implemented body):
```markdown
---
verb: doctor
tier: both
---

# Doctor

<capability_check>
  <if has="Task">delegate to Task tool with prompt: ...</if>
  <else-if has="MCP">call MCP tool ...</else-if>
  <else>shell: `pensmith doctor`</else>
</capability_check>

## Body
<actual prompt the verb runs against>
```

Each workflow body must:
1. Read its required prompt files (templates/prompts/<D-12-LOCKED-slug>.md) via the prompt loader (NEW helper landed in Plan 07).
2. Call the matching bin/cli/<verb>.ts entrypoint (also landed in Plan 07).
3. Include the W4 closed-vocabulary capability check exactly as defined in Phase 2 (`Task`, `MCP`, `AskUserQuestion`, `WebSearch`, `WebFetch`).
4. Outline workflow includes the approval gate: pause for user confirmation before writing outline → state, unless `--yolo` flag set.

D-12 LOCKED slug mapping (verbatim from CONTEXT.md):
- intake.md         → templates/prompts/intake-clarifier.md
- research.md       → templates/prompts/topic-disambiguator.md + templates/prompts/source-evaluator.md
- outline.md        → templates/prompts/outline-author.md
- plan.md           → templates/prompts/section-planner.md
- write.md          → templates/prompts/section-drafter.md
- verify.md         → (NO prompts in Phase 3 — D-13 DORMANT; deterministic Pass-1 + Pass-3 only)

D-13 LOCKED stance for verify.md: Phase 3 verify is 100% deterministic. The pass1-fuzzy-judge.md and pass3-quote-checker.md prompts exist (Plan 05 ships them hash-pinned) but workflows/verify.md MUST NOT reference, load, or invoke them. They are calibrated for Phase 8 ambiguous-case tie-break only.

D-20 LOCKED canonical BibTeX: `.paper/CITATIONS.bib` is the single source of truth for citation metadata at verify time. The research workflow writes it via bin/lib/bibtex-write.ts (under the D-19 citation-js chokepoint, Plan 04 RSCH-09 task). The verify workflow reads it via bin/lib/citations.ts (also D-19 chokepoint).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 6.1: Fill intake, research, outline workflow bodies</name>
  <files>workflows/intake.md, workflows/research.md, workflows/outline.md</files>
  <read_first>
    - workflows/doctor.md (the reference complete body from Phase 2)
    - workflows/intake.md, workflows/research.md, workflows/outline.md (current stubs to expand)
    - templates/prompts/intake-clarifier.md, topic-disambiguator.md, source-evaluator.md, outline-author.md (the D-12 LOCKED-slug prompts these workflows wrap)
    - templates/presets/disciplines.json (intake reads this)
    - PRD.md §7.1, §7.2, §7.3 (intake/research/outline behavioral spec)
    - .planning/phases/03-vertical-slice-one-section/03-CONTEXT.md (D-12 LOCKED slugs, D-20 canonical BibTeX)
  </read_first>
  <action>
    For each workflow, preserve the existing `<capability_check>` block from Phase 2. Append a `## Body` section that contains the actual delegation logic.

    **workflows/intake.md `## Body`:**
    ```markdown
    ## Body

    1. **Read inputs**:
       - Assignment text from `.paper/INTAKE.md` if present, else prompt user.
       - templates/presets/disciplines.json (8 INTK-03 presets)
       - templates/prompts/intake-clarifier.md (D-12 LOCKED slug — the prompt template; interpolate {{assignment}})

    2. **Detect discipline** (heuristic match assignment keywords against the 8 presets; fall back to "academic-formal" tone + "apa" style for unrecognized).

    3. **Run clarifying battery** (INTK-02):
       - If Task/MCP available: delegate to model with intake-clarifier.md prompt
       - If AskUserQuestion available (Tier 1): present 3-5 questions via the tool
       - Tier 2 fallback: write questions to stdout, read answers from stdin via @clack/prompts (bin/lib/prompts.ts from Phase 2)

    4. **Apply PII redaction** (INTK-05): before persisting answers, run them through the PII redactor (uses tests/pii.test.ts contract).

    5. **Write `.paper/INTAKE.md`** with the clarified assignment + discipline + tone + citation style.

    6. **Shell fallback**: `pensmith intake [--from <file>] [--yolo]`
    ```

    **REVIEWS CONVERGENCE — PII redaction ordering (Codex MEDIUM consensus #18).** The current step ordering (3. clarifying battery → 4. PII redaction → 5. write INTAKE.md) is correct but ambiguous: are the QUESTIONS PII-redacted, or only the ANSWERS? Lock here:
    - PII redaction operates on the USER'S ANSWERS only, NEVER on the prompt template or the LLM-generated questions (those are pensmith-controlled strings and contain no user PII).
    - Sequence: (a) prompt user, (b) collect raw answer string, (c) `redactPII(answer)` from bin/lib/pii.ts, (d) persist redacted answer to `.paper/INTAKE.md` AND a separate `.paper/INTAKE.raw.local` file (gitignored — never committed) for the user's own forensics.
    - The redactor processes input in this order (deterministic): EMAIL → PHONE → SSN-LIKE → CREDIT-CARD-LIKE → URL_WITH_QUERY → IP_ADDRESS → IBAN_LIKE. Multi-pattern coverage matters: redacting EMAIL last would cause `foo@bar.com` in a URL to slip through PHONE-shaped regex; processing EMAIL first ensures clean tokenization.
    - Acceptance: workflows/intake.md body explicitly enumerates the 7-stage redaction order in a code-comment block; tests/pii.test.ts asserts ordering by feeding `foo@bar.com (+1-555-555-5555)` and asserting the result is `[REDACTED-EMAIL] (+[REDACTED-PHONE])` (NOT `[REDACTED-PHONE]` swallowing the email).

    **workflows/research.md `## Body`:**
    ```markdown
    ## Body

    1. **Read `.paper/INTAKE.md`** for topic + discipline.

    2. **Disambiguate topic + generate queries** (RSCH-02): invoke templates/prompts/topic-disambiguator.md (D-12 LOCKED slug) → `{scopes: [{label, queries}]}` JSON. In --yolo mode, pick scope #1; otherwise present to user for selection.

    3. **Run adapters in parallel** (RSCH-03/04):
       - import { sources } from 'bin/lib/sources/index.ts'
       - For each query × each adapter, call sources[adapter].search(query)
       - Deduplicate by DOI; preserve provenance (which adapter found it first)
       - Emit a UNION SourceCandidate[] (D-14)

    4. **Evaluate candidates** (RSCH-02 second half): invoke templates/prompts/source-evaluator.md (D-12 LOCKED slug) on the deduped SourceCandidate[] → keep/reject verdicts. Filter on `keep: true`.

    5. **Cross-check via retraction-watch** (D-15): for each surviving candidate with DOI, call sources['retraction-watch'].fetchById(doi) — if returns a record, flag the candidate as `retracted: true` (do NOT silently drop; verify will mark uses as MIS-CITED with reason="cited a retracted work").

    6. **Persist `.paper/LIBRARY.json`** with the dedup'd + filtered candidates.

    7. **Write canonical BibTeX `.paper/CITATIONS.bib`** (RSCH-09, D-20): call `writeBibtex(library)` from bin/lib/bibtex-write.ts (Plan 04) which serializes via citation-js (D-19 chokepoint) and atomic-writes via bin/lib/atomic-write.ts (D-07 chokepoint). This file is the canonical citation source-of-truth that the verify workflow reads at Pass-1 time.

    8. **Shell fallback**: `pensmith research [--queries <n>] [--yolo]`
    ```

    **REVIEWS CONVERGENCE — retraction surfacing in outline approval gate (Codex MEDIUM consensus #19, OpenCode MEDIUM).** D-15 says retracted candidates are kept (not silently dropped) and the verify verb marks uses as MIS-CITED. But reviewers flagged: by the time we reach verify, the outline + plan + write have ALREADY consumed the retracted source. Surface twice:
    - In `workflows/research.md` step 5, after retraction-watch cross-check: if ANY candidate has `retracted: true`, the workflow body emits a WARN line to stderr: `WARN: ${count} retracted source(s) found in LIBRARY.json: ${citekeys.join(', ')}. These will FAIL Pass-1 if cited.`
    - In `workflows/outline.md` step 4 APPROVAL GATE: the printed outline includes a `RETRACTED` annotation next to any section whose assigned_sources contains a retracted citekey: `> ⚠ Section 03 (results) — 2 of 5 assigned sources flagged as retracted. Recommend revising before approval.`
    - Acceptance criteria additions:
      - workflows/research.md `## Body` contains literal `WARN: ${count} retracted` template
      - workflows/outline.md `## Body` contains literal `RETRACTED` annotation in the approval-gate rendering
      - tests/retraction-surface.test.ts (Plan 00 Wave 0 sentinel) asserts both surfacing paths fire when the test fixture LIBRARY.json contains a `retracted: true` candidate

    **workflows/outline.md `## Body`:**
    ```markdown
    ## Body

    1. **Read `.paper/INTAKE.md` and `.paper/LIBRARY.json`**.

    2. **Generate outline** (OUTL-01/02): invoke templates/prompts/outline-author.md (D-12 LOCKED slug) → list of sections {n, slug, title, depends_on, estimated_word_count}.

    3. **Validate**:
       - PlanFrontmatterSchema validates each entry (D-04 refinements: slug format, no-self-ref)
       - Walker check (OUTL-04): all depends_on slugs resolve to an existing section in this outline AND no cycles (Tarjan or simple DFS)

    4. **APPROVAL GATE** (OUTL-03, PRD non-negotiable):
       - Print outline to stdout in a human-readable table (section #, slug, title, depends, word target)
       - Unless `--yolo` flag set: pause and request user confirmation ("approve / edit / cancel")
       - If "edit": loop back to step 2 with the user's feedback as additional context
       - If "cancel": exit non-zero, no state mutation

    5. **Persist `.paper/OUTLINE.md`** (human-readable Markdown) + create `.paper/sections/<NN>-<slug>/` directory per section (empty — PLAN.md lands in plan verb).

    6. **Shell fallback**: `pensmith outline [--yolo]`
    ```

    Each body MUST preserve the Phase 2 `<capability_check>` block above the `## Body` header.
  </action>
  <verify>
    <automated>npm test -- --test-name-pattern="tier-contract.*(intake|research|outline)"</automated>
  </verify>
  <acceptance_criteria>
    - All 3 workflows have a `## Body` section (grep `grep -l "^## Body" workflows/intake.md workflows/research.md workflows/outline.md` returns 3)
    - Original `<capability_check>` blocks preserved (grep -c "capability_check" returns ≥2 per file: open + close tag)
    - outline.md `## Body` contains the literal "APPROVAL GATE" and references "--yolo"
    - research.md `## Body` references `sources['retraction-watch']` (D-15 cross-check present)
    - research.md `## Body` references both `templates/prompts/topic-disambiguator.md` AND `templates/prompts/source-evaluator.md` (D-12 LOCKED slugs — replaces the former single research-queries prompt)
    - research.md `## Body` references `.paper/CITATIONS.bib` AND `bin/lib/bibtex-write.ts` (BL-4: D-20 canonical BibTeX writing)
    - intake.md `## Body` references `INTK-05` (PII redaction step present) AND `templates/prompts/intake-clarifier.md` (D-12 LOCKED slug — not old "intake-clarify")
    - intake.md `## Body` references "8 INTK-03 presets" or "8 presets" (confirms 8-discipline alignment)
    - outline.md `## Body` references `templates/prompts/outline-author.md` (D-12 LOCKED slug — not old "outline-propose")
    - No workflow body references any of the old slugs (intake-clarify, research-queries, outline-propose) — `grep -E "intake-clarify\.md|research-queries\.md|outline-propose\.md" workflows/{intake,research,outline}.md` returns 0
    - Tier-contract tests for intake/research/outline cases pass (Plan 00 extends tier-contract red; this task's bodies must satisfy them)
  </acceptance_criteria>
  <done>
    intake/research/outline workflow bodies complete with capability checks, approval gate (outline), PII redaction (intake), retraction cross-check (research), canonical BibTeX write (research), all using D-12 LOCKED prompt slugs.
  </done>
</task>

<task type="auto">
  <name>Task 6.2: Fill plan, write, verify workflow bodies (verify = 100% DETERMINISTIC per D-13)</name>
  <files>workflows/plan.md, workflows/write.md, workflows/verify.md</files>
  <read_first>
    - workflows/plan.md, workflows/write.md, workflows/verify.md (current stubs)
    - templates/prompts/section-planner.md, section-drafter.md (D-12 LOCKED slugs)
    - templates/prompts/pass1-fuzzy-judge.md, pass3-quote-checker.md (DORMANT — must NOT be referenced in verify.md per D-13)
    - bin/lib/paths.ts (sectionPlan, sectionDraft, sectionVerification helpers — Plan 03)
    - bin/lib/fuzzy.ts (deterministic gates — Plan 01)
    - bin/lib/normalize.ts (Plan 01)
    - bin/lib/citations.ts (D-19 chokepoint, parses .paper/CITATIONS.bib at verify time per D-20)
    - PRD.md §7.4, §7.5, §7.6 (plan/write/verify spec)
    - .planning/phases/03-vertical-slice-one-section/03-CONTEXT.md (D-08 LOCKED state enum, D-13 DORMANT stance, D-20 canonical BibTeX)
  </read_first>
  <action>
    **workflows/plan.md `## Body`:**
    ```markdown
    ## Body

    1. **Parse args**: `pensmith plan <N>` — N is the section number. Read OUTLINE.md to resolve slug.

    2. **Read inputs**:
       - `.paper/OUTLINE.md` to get section {n, slug, title, depends_on, word_count}
       - `.paper/LIBRARY.json` for SourceCandidate[]
       - templates/prompts/section-planner.md (D-12 LOCKED slug)

    3. **Run planner**: invoke section-planner.md prompt → PlanFrontmatter YAML + Brief

    4. **Validate PlanFrontmatter**: PlanFrontmatterSchema.parse — refuses self-ref (D-04), validates D-08 LOCKED status enum

    5. **Write `<sectionPlan(n, slug)>`** (.paper/sections/NN-slug/PLAN.md):
       - YAML frontmatter (validated) with `status: 'planned'` (D-08 LOCKED enum default)
       - `## Brief` section
       - Set verified_against_draft_hash: null

    6. **Section-isolation invariant**: this verb MUST NOT touch any other section's directory (TEST-09).

    7. **Shell fallback**: `pensmith plan <N> [--revise] [--yolo]`
    ```

    **workflows/write.md `## Body`:**
    ```markdown
    ## Body

    1. **Parse args**: `pensmith write <N>`.

    2. **Read inputs**:
       - sectionPlan(n, slug) — get assigned_sources citekeys + brief
       - LIBRARY.json — resolve assigned_sources → SourceCandidate[] (RESTRICTED VIEW per PRD §7.6 — drafter sees ONLY assigned, not the full library)
       - templates/prompts/section-drafter.md (D-12 LOCKED slug)

    3. **Set status to 'writing'** (D-08 LOCKED enum): update PlanFrontmatter `status: 'writing'` via bin/lib/frontmatter.ts updateFrontmatter (round-trip-safe per D-08).

    4. **Construct drafter input** via assertDrafterInput (bin/lib/drafter-input.ts from Plan 07) — throws on superset/missing fields (T-3-10, WRTE-04).

    5. **Run drafter**: invoke section-drafter.md prompt → Markdown body with Pandoc [@citekey] tokens (D-21).

    6. **Write `<sectionDraft(n, slug)>`** (.paper/sections/NN-slug/DRAFT.md).

    7. **Update PlanFrontmatter**: status = 'written' (D-08 LOCKED enum value, NOT 'drafted'), verified_against_draft_hash = sha256(DRAFT.md) — invalidated on next write.

    8. **Section-isolation invariant**: this verb MUST NOT touch other sections (TEST-09).

    9. **Shell fallback**: `pensmith write <N> [--yolo]`
    ```

    **workflows/verify.md `## Body`** (100% DETERMINISTIC per D-13 — NO LLM invocations):
    ```markdown
    ## Body

    > **D-13 LOCKED INVARIANT — Phase 3 verify path is 100% deterministic.**
    > NO LLM call SHALL be made between the Pass-1 fetch and the sectionVerification write.
    > The pass1-fuzzy-judge.md and pass3-quote-checker.md prompts exist (Plan 05 hash-pins them) but are DORMANT — calibrated for Phase 8 ambiguous-case tie-break only.
    > Narration in VERIFICATION.md is built from template literals embedded in this body, NOT from prompt invocations.
    > Audit: `grep -E "invoke|prompt|Task|delegate" workflows/verify.md` MUST return 0 matches for LLM-invocation patterns inside the `## Body` section.

    1. **Parse args**: `pensmith verify <N>`.

    2. **Set status to 'verifying'** (D-08 LOCKED enum): update PlanFrontmatter `status: 'verifying'` via bin/lib/frontmatter.ts.

    3. **Read inputs**:
       - sectionDraft(n, slug) — DRAFT.md with Pandoc [@citekey] tokens
       - sectionPlan(n, slug) — assigned_sources
       - `.paper/CITATIONS.bib` — canonical BibTeX (D-20) parsed via bin/lib/citations.ts (D-19 chokepoint) → resolves citekeys to DOIs + metadata. This file is the single source of truth; LIBRARY.json is NOT consulted at verify time.

    4. **PASS 1 — Citation Integrity (DETERMINISTIC, VRFY-01)**:
       - Extract every [@citekey] from DRAFT.md
       - For each, look up the parsed-bib entry → get claimed {title, authors, doi}
       - For each DOI: call sources.crossref.fetchById(doi) → get actual {title, authors} (cassette-backed in CI)
       - Compute jaroWinkler(actualTitle, claimedTitle) vs TITLE_JW_THRESHOLD (0.92)
       - Compute jaroWinkler(actualAuthor, claimedAuthor) vs AUTHOR_JW_THRESHOLD (0.85)
       - Verdict AND-gate (DETERMINISTIC, no LLM): both ≥ threshold → OK; else MIS-CITED. DOI doesn't resolve → FABRICATED. Citekey absent from .paper/CITATIONS.bib → FABRICATED (citation invented by drafter).
       - Build pass1Results array.

    5. **Narrate Pass-1 results into VERIFICATION.md** via TEMPLATE LITERAL (no LLM):
       For each pass1Result, format as `| ${citekey} | ${verdict} | titleJW=${titleJW.toFixed(2)} authorJW=${authorJW.toFixed(2)} | ${reason} |` in a Markdown table. The narration is mechanical string interpolation — no prompt is invoked.

    6. **PASS 3 — Quote Integrity (DETERMINISTIC, VRFY-04/05)**:
       - Extract every quoted passage from DRAFT.md (heuristic: text in "..." or > 25 words on a single line)
       - For each quote with an associated citekey, fetch OA PDF via sources.unpaywall.fetchById(doi) → URL → http.get → bytes → extractPdfText (bin/lib/pdf-text.ts)
       - nfkcNormalize(quote) and nfkcNormalize(pdfText)
       - levenshteinSubstring(normalizedQuote, normalizedPdfText) vs QUOTE_LEV_THRESHOLD (0.95)
       - Verdict (DETERMINISTIC, no LLM): ratio ≥ threshold → OK; else NOT_FOUND.

    7. **Narrate Pass-3 results into VERIFICATION.md** via TEMPLATE LITERAL (no LLM):
       For each pass3Result, format as `| ${quote.slice(0,40)}... | ${verdict} | levRatio=${ratio.toFixed(3)} |` in a Markdown table.

    8. **Compute overall verdict** (DETERMINISTIC): PASS iff every pass1 verdict is OK AND every pass3 verdict is OK; otherwise FAIL with the count breakdown.

    9. **Write `<sectionVerification(n, slug)>`** (.paper/sections/NN-slug/VERIFICATION.md) with both pass narratives + overall verdict + the D-13 LOCKED invariant footer ("This verification was produced by the deterministic Pass-1/Pass-3 algorithms. No LLM was invoked at verify time per D-13.").

    10. **Update PlanFrontmatter**: status = 'verified' (if PASS) or 'failed' (if FAIL) per D-08 LOCKED enum; verified_against_draft_hash = sha256(DRAFT.md). If this hash changes (re-write), the verification is stale.

    11. **Section-isolation invariant** (TEST-09).

    12. **Shell fallback**: `pensmith verify <N> [--yolo]`
    ```

    **REVIEWS CONVERGENCE — PDF_UNAVAILABLE / TEXT_UNAVAILABLE / UNVERIFIABLE verdicts (OpenCode HIGH #2, Codex HIGH consensus #2).** The verify body above treats every Pass-3 failure as `NOT_FOUND`, but reviewers flagged 3 distinct failure modes that need distinct verdicts so the writer can disambiguate:
    - `PDF_UNAVAILABLE` — Unpaywall returned no OA PDF URL for the DOI (paywalled or no OA copy exists). The citation may still be valid; we just can't run Pass-3.
    - `TEXT_UNAVAILABLE` — PDF fetched successfully but `extractPdfText()` returned <50 non-whitespace chars (image-only/scanned PDF, per Plan 02 amendment).
    - `UNVERIFIABLE` — Composite Pass-3 verdict (= PDF_UNAVAILABLE OR TEXT_UNAVAILABLE) — the source's claims couldn't be checked at quote level, BUT Pass-1 citation integrity still applies independently.
    - `NOT_FOUND` — PDF fetched + text extracted, but levenshteinSubstring(normalizedQuote, normalizedPdfText) < QUOTE_LEV_THRESHOLD. The quote is fabricated or distorted.

    Update workflows/verify.md `## Body` step 6 to:
    ```
    6. **PASS 3 — Quote Integrity (DETERMINISTIC, VRFY-04/05)**:
       For each quote with an associated citekey:
       (a) Fetch OA PDF URL: const oaUrl = sources.unpaywall.fetchById(doi)?.oa_pdf_url
           If !oaUrl → verdict = 'PDF_UNAVAILABLE', reason = 'No OA PDF available for DOI ${doi} (Unpaywall returned no oa_pdf_url)'
       (b) Else fetch bytes: const buf = await http.get(oaUrl) → buf
       (c) Extract text: const text = await extractPdfText(buf)
           If text.replace(/\s/g,'').length < 50 → verdict = 'TEXT_UNAVAILABLE', reason = 'PDF appears image-only or scanned (<50 non-whitespace chars). Pass 3 cannot run.'
       (d) Else normalize + match: const ratio = levenshteinSubstring(nfkcNormalize(quote), nfkcNormalize(text))
           If ratio >= QUOTE_LEV_THRESHOLD → verdict = 'OK', else verdict = 'NOT_FOUND'

       Compute Pass-3 status per citekey:
       - If any quote has verdict NOT_FOUND → section Pass-3 FAILS for this source (status: 'failed')
       - Else if all quotes are OK → section Pass-3 PASSES for this source
       - Else if all quotes are PDF_UNAVAILABLE or TEXT_UNAVAILABLE → section Pass-3 is UNVERIFIABLE for this source (status: 'unverifiable' per D-08-AMENDED)
       - Mixed (some OK, some PDF_UNAVAILABLE): section Pass-3 is UNVERIFIABLE for this source overall (do NOT auto-promote to PASS — surface to writer)
    ```

    Update step 8 (overall verdict) to:
    ```
    8. **Compute overall verdict** (DETERMINISTIC):
       - PASS iff every pass1 verdict is OK AND every pass3 verdict is OK (NO unverifiable, NO not_found)
       - UNVERIFIABLE iff Pass-1 all OK AND Pass-3 has ≥1 unverifiable verdict AND zero not_found verdicts → overall status='unverifiable' (D-08-AMENDED). Does NOT block compile (per PRD §3 — the README disclaimer covers this), but DOES surface in VERIFICATION.md so the writer knows.
       - FAIL otherwise (any FABRICATED / MIS-CITED / NOT_FOUND)
    ```

    Update step 10 to:
    ```
    10. **Update PlanFrontmatter**: status per D-08-AMENDED enum:
        - PASS → 'verified'
        - UNVERIFIABLE → 'unverifiable'
        - FAIL → 'failed'
        verified_against_draft_hash = sha256(DRAFT.md)
    ```

    **REVIEWS CONVERGENCE — Pass 1 field-presence criteria (OpenCode MEDIUM #5, Codex MEDIUM consensus #9).** Reviewers flagged that the current Pass-1 AND-gate assumes title/author always non-empty after fetchById. Edge cases:
    - Crossref returns title as an empty array `title: []` for a small fraction of entries → claimedTitle = '' → jaroWinkler('', anything) = 0 → MIS-CITED (spurious failure).
    - Multi-DOI redirect: some DOIs resolve to a new canonical DOI (e.g. 10.1000/abc → 10.1000/xyz). Currently this looks like a different paper.

    Add to verify.md `## Body` step 4 AFTER the JW computation:
    ```
    4a. **Field-presence sub-gate (DETERMINISTIC, no LLM)**:
        Before the JW AND-gate, validate:
        - claimed.title.length >= 1 AND claimed.authors[0]?.length >= 1 OR else verdict='MIS-CITED', reason='claimed citation metadata incomplete (empty title or no authors)'
        - actual.title.length >= 1 OR else verdict='MIS-CITED', reason='source API returned empty title — entry may be malformed upstream; manual review recommended'
        - retracted-flag handling: if entry.retracted === true → verdict='MIS-CITED', reason='cited a retracted work (per Retraction Watch cross-check at research time)'. Override even if JW thresholds pass — retraction is a citation-integrity failure regardless of metadata match.
        - Multi-DOI redirect handling: if fetchById(claimed.doi) returns a record with a DIFFERENT doi field than claimed.doi (Crossref returns `DOI: <canonical>` which may differ from the queried), treat as OK iff jaroWinkler(actualTitle, claimedTitle) >= 0.98 AND jaroWinkler(actualAuthor, claimedAuthor) >= 0.95 (stricter band to account for crossref publishing 2 distinct DOIs for the same work). Otherwise verdict='MIS-CITED', reason='claimed DOI ${claimed.doi} resolves to a different work (canonical: ${actual.doi})'.
    ```

    Acceptance criteria additions for Task 6.2:
    - workflows/verify.md `## Body` contains literal `'PDF_UNAVAILABLE'` AND `'TEXT_UNAVAILABLE'` AND `'unverifiable'` (D-08-AMENDED per-source verdicts)
    - workflows/verify.md `## Body` step 4a contains literal `'cited a retracted work'`
    - workflows/verify.md `## Body` step 4a contains literal `'Multi-DOI redirect'` AND `0.98` (the stricter redirect band)
    - tests/verify-verdicts.test.ts (Plan 00 Wave 0 sentinel) asserts each of {PDF_UNAVAILABLE, TEXT_UNAVAILABLE, NOT_FOUND, UNVERIFIABLE, MIS-CITED-empty-title, MIS-CITED-retracted, MIS-CITED-multi-doi, OK-multi-doi-strict-match} produces the expected verdict end-to-end on cassette fixtures.

    **REVIEWS CONVERGENCE — quote-extractor ≥10-word minimum (Codex HIGH #4, OpenCode HIGH consensus #4).** Per PRD §7.6 spec the quote extractor heuristic above ("text in '...' or > 25 words on a single line") is too lax. Codex flagged: a 4-word quoted phrase ("a deep neural network") matches noisy Levenshtein on virtually any paper containing those words → false-POSITIVE pass. Lock here:
    - bin/lib/quote-extractor.ts (NEW — created in Plan 07 amendment) extracts quotes per these rules:
      - Block quotes (lines beginning with `> `): always include if word count >= 10
      - Inline quotes (text wrapped in `"…"` or `"…"` or `'…'`): include only if the quote contains >= 10 words AND >= 60 characters
      - Multi-paragraph quotes (single block quote spanning multiple lines): treat as one quote, word count = total
      - Strip Pandoc citation tokens BEFORE counting words: `[@vaswani2017]` is NOT a word
    - Quotes with fewer than 10 words are NOT extracted (Pass-3 silently skips them; the writer is responsible for inline-cite integrity at Pass-1 level)
    - Acceptance criteria additions:
      - workflows/verify.md step 6 references `bin/lib/quote-extractor.ts extractQuotes(draftMd)` (NOT the inline heuristic above)
      - tests/quote-extractor.test.ts (Plan 00 Wave 0 sentinel) asserts: 9-word quote NOT extracted, 10-word quote extracted, block quote of 12 words extracted, multi-paragraph block quote treated as one entry, Pandoc tokens stripped before counting.

    **REVIEWS CONVERGENCE — `new` vs `intake` naming reconciliation (Codex HIGH #9, OpenCode MEDIUM consensus #5).** README quick start says `pensmith new <topic>` but Plan 06 workflow body says `pensmith intake`. Lock here:
    - **Canonical verb name: `intake`** (matches workflows/intake.md filename + bin/cli/intake.ts filename per ARCH-02).
    - `pensmith new` is an ALIAS that maps to `pensmith intake`. The bin/cli/index.ts dispatcher (Plan 07) handles the alias.
    - README quick-start MAY use `pensmith new` for user-friendly framing; CLI --help shows both.
    - Acceptance criterion: bin/cli/index.ts (Plan 07) `commands` map has both `intake` and `new` keys pointing to the same handler. tests/cli-aliases.test.ts asserts `pensmith new` and `pensmith intake` produce byte-identical outputs.
  </action>
  <verify>
    <automated>npm test -- --test-name-pattern="tier-contract.*(plan-section|write-section|verify-section)"</automated>
  </verify>
  <acceptance_criteria>
    - All 3 workflows have a `## Body` section
    - write.md body references "assertDrafterInput" + "RESTRICTED VIEW" + "Pandoc [@citekey]"
    - write.md body uses D-08 LOCKED enum values: `status = 'writing'` AND `status = 'written'` (NOT 'drafting' / 'drafted')
    - write.md body references `templates/prompts/section-drafter.md` (D-12 LOCKED slug — NOT old "write-section.md")
    - plan.md body references `templates/prompts/section-planner.md` (D-12 LOCKED slug — NOT old "plan-section.md")
    - verify.md body references BOTH "jaroWinkler" AND "levenshteinSubstring" AND both threshold constants
    - verify.md body has both "PASS 1" and "PASS 3" sections; deterministic computation produces the verdict end-to-end
    - verify.md body references `.paper/CITATIONS.bib` AND `bin/lib/citations.ts` (BL-4: D-20 canonical BibTeX read via D-19 chokepoint)
    - verify.md body uses D-08 LOCKED enum values: `status: 'verifying'`, `status = 'verified'`, `status = 'failed'`
    - verify.md body contains the literal "D-13 LOCKED INVARIANT" header
    - verify.md body contains the literal "100% deterministic"
    - **BL-2 GATE (mechanical)**: `grep -cE "invoke .*prompt|delegate to Task|invoke verify-pass|invoke pass1|invoke pass3" workflows/verify.md` returns 0 (no LLM-invocation patterns)
    - **BL-2 GATE (slug absence)**: `grep -c "pass1-fuzzy-judge\.md\|pass3-quote-checker\.md\|verify-pass1\.md\|verify-pass3\.md" workflows/verify.md` returns 0 (verify body never references the DORMANT prompts)
    - All 3 workflows reference the section-isolation invariant (grep "TEST-09" returns 3 hits across the files)
    - Tier-contract tests for plan-section/write-section/verify-section pass
  </acceptance_criteria>
  <done>
    plan/write/verify workflow bodies wire the deterministic algorithms; verify is 100% deterministic per D-13 (zero LLM invocations, DORMANT prompts not referenced); section-isolation invariant guarded in every verb; D-08 LOCKED status enum values used throughout; tier-contract green.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workflow body → bin/cli/<verb>.ts | Workflow is the prompt; the verb is the executor. The body must not bypass the verb's input validation (e.g. assertDrafterInput). |
| Workflow → other sections | Each per-section verb (plan/write/verify) is invariant-bound to only touch its target sectionDir. |
| Verify workflow → LLM (D-13) | NO LLM call at verify time in Phase 3. Verdict is deterministic. The DORMANT prompts (pass1-fuzzy-judge.md, pass3-quote-checker.md) ship hash-pinned but are never invoked from this body. |
| Verify workflow → .paper/CITATIONS.bib (D-20) | Verify reads canonical BibTeX through bin/lib/citations.ts (D-19 citation-js chokepoint). LIBRARY.json is NOT consulted at verify time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-08 | Tampering | Workflow verb mutates wrong section | mitigate | TEST-09 mtime invariant (section-isolation.test.ts) enforces in CI; per-verb sectionDir(n,slug) is the only filesystem write target. |
| T-3-10 | Tampering | Drafter input contract bypass (workflow passes extra fields, leaking info) | mitigate | write.md calls assertDrafterInput; drafter-input.ts (Plan 07) throws on superset. |
| T-3-VERDICT-01 | Repudiation | LLM influence on Pass-1/Pass-3 verdict | mitigate | D-13 LOCKED invariant: verify body invokes NO prompts. Verdict is produced by jaroWinkler/levenshteinSubstring alone. Narration into VERIFICATION.md is mechanical template-literal interpolation, not LLM-generated. Audit gate: grep returns 0 LLM-invocation patterns in workflows/verify.md ## Body. |
| T-3-DORMANT-01 | Tampering | DORMANT prompt accidentally wired into Phase 3 verify | mitigate | Plan 05 frontmatter `dormant_in_phase: 3` + Plan 06 BL-2 GATE acceptance criteria (grep for pass1-fuzzy-judge.md / pass3-quote-checker.md in workflows/verify.md must return 0). |
| T-3-BIBTEX-01 | Tampering | Verify reads stale or wrong citation metadata | mitigate | D-20 canonical BibTeX: verify reads .paper/CITATIONS.bib (single source of truth) via bin/lib/citations.ts (D-19 chokepoint). Research workflow atomic-writes the file via bin/lib/bibtex-write.ts (D-07 chokepoint). |
| T-3-APPROVAL-01 | Repudiation | Outline written without user confirmation | mitigate | outline.md APPROVAL GATE pauses unless --yolo; PRD non-negotiable. |
| T-3-PII-01 | Information Disclosure | PII from intake answers persisted unredacted | mitigate | intake.md body invokes PII redactor before persisting INTAKE.md; tests/pii.test.ts contract. |

</threat_model>

<verification>
- `npm test -- --test-name-pattern="tier-contract"` green across all 6 new cases
- `grep -l "^## Body" workflows/{intake,research,outline,plan,write,verify}.md` returns 6
- All 6 files preserve their Phase-2 `<capability_check>` blocks
- `npm run lint` green
- No workflow body references a section dir outside its own (`.paper/sections/<N>-<slug>/`)
- **BL-2 GATE**: `grep -cE "invoke .*prompt|delegate to Task|invoke verify-pass|invoke pass1|invoke pass3" workflows/verify.md` returns 0
- **BL-2 GATE**: `grep -c "pass1-fuzzy-judge\.md\|pass3-quote-checker\.md" workflows/verify.md` returns 0
- **BL-4 GATE**: `grep -c "\.paper/CITATIONS\.bib" workflows/{research,verify}.md` returns ≥ 1 per file
- **BL-1 GATE**: `grep -E "intake-clarify\.md|research-queries\.md|outline-propose\.md|plan-section\.md|write-section\.md|verify-pass[13]\.md|draft-summary\.md" workflows/*.md` returns 0 (no old slug references)
</verification>

<success_criteria>
1. 6 workflow bodies complete with capability checks + delegations using D-12 LOCKED slugs.
2. Outline approval gate ON by default.
3. PII redaction in intake.
4. Retraction cross-check in research; canonical .paper/CITATIONS.bib written by research (BL-4, D-20).
5. Verify is 100% DETERMINISTIC per D-13 (zero LLM invocations between Pass-1 fetch and sectionVerification write; DORMANT prompts never referenced from verify.md).
6. D-08 LOCKED status enum values used in write.md and verify.md ('writing', 'written', 'verifying', 'verified', 'failed').
7. Section-isolation invariant referenced in every per-section verb.
8. Tier-contract green for all 6 cases.
</success_criteria>

<output>
Create `.planning/phases/03-vertical-slice-one-section/03-06-SUMMARY.md` with:
- 6 workflow body sizes (LOC)
- Capability-check preservation confirmation
- Tier-contract test output (all 6 cases)
- BL-2 GATE output: grep results showing 0 LLM-invocation patterns in workflows/verify.md
- BL-4 GATE output: grep results showing .paper/CITATIONS.bib referenced in research.md AND verify.md
- BL-1 GATE output: grep results showing 0 old-slug references across all 6 workflows
</output>
