---
phase: 13-citation-rendering-at-export
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/exporter.test.ts
autonomous: true
requirements: [REND-01, REND-02, REND-03]
must_haves:
  truths:
    - "The exporter test suite contains REND-01/02/03 assertions that target the OFFLINE citation-js render path (no Pandoc)"
    - "The new assertions are RED-by-skip: they SKIP (not fail) until the resolveAndRenderCitations wiring lands, so the full suite stays green"
    - "The REND-03 assertion uses the committed known-good fixture (vaswani2017attention) and asserts a formatted reference appears AND no raw [@key] survives"
    - "An assertion confirms buildPandocArgs emits the citeproc flags and that the bib is copied before the pandoc shellout"
    - "An assertion confirms the zero-trace export test still passes on citation-rendered output"
  artifacts:
    - path: "tests/exporter.test.ts"
      provides: "REND-01/02/03 RED-by-skip citation-render assertions + Pandoc-args + zero-trace non-regression checks"
      contains: "resolveAndRenderCitations"
  key_links:
    - from: "tests/exporter.test.ts"
      to: "tests/fixtures/known-good-fixture/CITATIONS.bib"
      via: "fileURLToPath fixture read"
      pattern: "known-good-fixture"
    - from: "tests/exporter.test.ts"
      to: "bin/lib/exporter.ts source"
      via: "source-grep skip predicate on resolveAndRenderCitations"
      pattern: "resolveAndRenderCitations"
---

<objective>
Extend `tests/exporter.test.ts` with RED-by-skip assertions for REND-01 (in-text `[@key]` resolution), REND-02 (rendered bibliography), and REND-03 (offline formatted-reference assertion on the known-good fixture), plus guard assertions for the Pandoc citeproc args, the bib-copy-before-pandoc ordering, and zero-trace non-regression.

Purpose: Lock the offline-testable REND contract as failing-by-skip tests BEFORE the Wave-1 wiring lands, so Plan 02's GREEN flips these skips to active without rewriting the contract. The REND-03 assertion is OFFLINE (citation-js path), never Pandoc-gated, per the phase quality gate.

Output: `tests/exporter.test.ts` extended with new test cases that SKIP until `resolveAndRenderCitations` is wired into `bin/lib/exporter.ts`. Full suite stays green (0 failures, new tests reported as skips).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-citation-rendering-at-export/13-CONTEXT.md
@.planning/phases/13-citation-rendering-at-export/13-RESEARCH.md
@.planning/phases/13-citation-rendering-at-export/13-PATTERNS.md
@.planning/phases/13-citation-rendering-at-export/13-VALIDATION.md

@tests/exporter.test.ts
@tests/zero-trace-export.test.ts
@tests/fixtures/known-good-fixture/section.md
@tests/fixtures/known-good-fixture/CITATIONS.bib

<interfaces>
<!-- Contracts the Wave-1 wiring (Plan 02) will satisfy. The Wave-0 tests are written AGAINST these so the GREEN in Plan 02 flips the skips. Executor: use these directly — no codebase exploration needed. -->

ExportOptions (bin/lib/exporter.ts:347) — Plan 02 ADDS one optional field after pandocPresent:
  style?: string   // CSL style key (e.g. 'apa', 'ieee'); undefined → defaults to 'apa'

ExportResult (bin/lib/exporter.ts:359) — unchanged shape:
  { outputPath: string; format: ExportFormat; pandocUsed: boolean; bibCopied: boolean; risCopied: boolean }

New helper Plan 02 ADDS to bin/lib/exporter.ts (the skip predicate keys on its name in source):
  async function resolveAndRenderCitations(md: string, bibPath: string, style: string): Promise<string>

Existing test helpers in tests/exporter.test.ts (REUSE verbatim, do not redefine):
  - exporterSrcPath = fileURLToPath(new URL('../bin/lib/exporter.ts', import.meta.url))
  - exporterModUrl  = new URL('../bin/lib/exporter.js', import.meta.url)
  - seedPaper(slug): { root, inputPath }   // writes .paper/DRAFT.md + .paper/CITATIONS.bib
  - type ExportDraft  // local call-signature type — EXTEND it with style?: string

Existing exporter test patterns to mirror:
  - pandocPresent: false injection keeps a test fully offline
  - { skip: <predicate> } as the second arg to test(...)
  - module loaded via: const mod = await import(exporterModUrl.href) as { exportDraft: ExportDraft }

Known-good fixture (committed, do NOT modify):
  - tests/fixtures/known-good-fixture/section.md  → '# Background\n\nAttention mechanisms were popularized by [@vaswani2017attention] as a replacement for recurrence.\n'
  - tests/fixtures/known-good-fixture/CITATIONS.bib → @article{vaswani2017attention, author={Vaswani, Ashish and Shazeer, Noam and Parmar, Niki}, title={Attention is All You Need}, ...}
  - APA in-text for this entry renders as "(Vaswani et al., 2017)"; bibliography contains "Vaswani"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a source-grep RED-by-skip predicate keyed on the resolveAndRenderCitations wiring</name>
  <read_first>tests/exporter.test.ts (lines 18-44 — fileURLToPath imports, exporterSrcPath/exporterModUrl, the existing module-presence RED-by-skip test); STATE decision [07-01] (source-grep skip predicates gate RED-by-skip for files that already exist as stubs); STATE decision [10-00] (symbol feature-detect via source scan keeps tsc clean while target export is absent)</read_first>
  <files>tests/exporter.test.ts</files>
  <action>
  Because bin/lib/exporter.ts ALREADY exists (shipped Phase 6), the existing existsSync(exporterSrcPath) skip predicate is always-true and would make the new REND assertions run RED immediately — breaking the green-suite convention ([05-01]/[06-01]/[10-00]). Add a NEW skip predicate that detects whether the Plan-02 wiring is present by reading the exporter SOURCE text and checking for the resolveAndRenderCitations helper name. Define a module-scope const (e.g. `renderCitationsWired`) computed once via readFileSync(exporterSrcPath,'utf8').includes('resolveAndRenderCitations'). Guard it so a missing source file yields false (never throws). All REND-01/02/03 behavioral tests below use `{ skip: !renderCitationsWired }`. Do NOT use existsSync alone — the source-grep predicate is the load-bearing RED-by-skip mechanism here (per D-07-01 precedent). Add a module-presence consistency test mirroring the existing one (line 38): if renderCitationsWired is true assert behavioral tests active, else assert the wiring is absent (Wave-0 RED-by-skip).
  </action>
  <verify>
    <automated>node --import tsx --test tests/exporter.test.ts</automated>
  </verify>
  <done>tests/exporter.test.ts defines a renderCitationsWired source-grep predicate (false when the resolveAndRenderCitations helper is absent from exporter source); the predicate never throws on a missing file; a consistency test reports the Wave-0 RED-by-skip state. The full test file still parses and runs with 0 failures (new behavioral tests skip).</done>
</task>

<task type="auto">
  <name>Task 2: Add the REND-01/02/03 offline assertions on the known-good fixture (RED-by-skip)</name>
  <read_first>tests/exporter.test.ts (lines 28-101 — seedPaper, the pandocPresent:false bibcopy test at 75-85, the ExportDraft local type at 24-26); tests/fixtures/known-good-fixture/section.md + CITATIONS.bib; 13-RESEARCH.md "Verified: full offline round-trip on the known-good fixture" + the REND-03 assertion block in 13-PATTERNS.md (lines 346-352); 13-CONTEXT.md REND-03 offline-testable decision</read_first>
  <files>tests/exporter.test.ts</files>
  <action>
  First EXTEND the local ExportDraft call-signature type to include `style?: string` (so the new exportDraft({..., style:'apa'}) calls typecheck). Then add an offline REND test, guarded `{ skip: !renderCitationsWired }`, that: (a) creates a tmp paper via mkdtemp, writes .paper/DRAFT.md as the fixture section.md content and .paper/CITATIONS.bib as the fixture CITATIONS.bib content (read both fixture files via a FIXTURE_DIR computed with fileURLToPath(new URL('./fixtures/known-good-fixture', import.meta.url)) — spaced-path safe per the Phase-11 lesson); (b) calls mod.exportDraft({ inputPath, format:'md', paperRoot:root, pandocPresent:false, style:'apa' }); (c) reads res.outputPath and asserts REND-01: NOT rendered.includes('[@') (no raw token survives), REND-03: rendered.includes('Vaswani') (a formatted reference appears), REND-02: rendered.includes('## References') (bibliography heading present). Add a second guarded test asserting the in-text form specifically — rendered contains '(Vaswani et al., 2017)' (the APA in-text string verified in RESEARCH) — to pin REND-01 beyond mere token-absence. Keep both fully offline (pandocPresent:false). Do NOT modify the committed fixture files.
  </action>
  <verify>
    <automated>node --import tsx --test tests/exporter.test.ts</automated>
  </verify>
  <done>Two RED-by-skip offline tests assert (1) no raw [@key] survives, the APA in-text "(Vaswani et al., 2017)" appears, a "## References" bibliography heading appears, and "Vaswani" appears in the rendered output; (2) the FIXTURE_DIR is resolved via fileURLToPath. Both skip cleanly while the wiring is absent; the ExportDraft type accepts style?. Full suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Add Pandoc-args, bib-ordering, and zero-trace non-regression guard assertions</name>
  <read_first>bin/lib/exporter.ts (buildPandocArgs at line 453; the bibCopied block at 557-565; the docx/pdf execFileAsync at 538); tests/zero-trace-export.test.ts (the md/latex zero-trace test shape); 13-RESEARCH.md Pitfall 3 (--citeproc before --csl/--bibliography) + Pitfall 4 (bib copied before pandoc); 13-CONTEXT.md zero-trace invariant</read_first>
  <files>tests/exporter.test.ts</files>
  <action>
  Add a guarded `{ skip: !renderCitationsWired }` test that imports buildPandocArgs structurally. Since buildPandocArgs is module-internal (not exported), assert its effect indirectly OR — preferred and simpler — assert against the EXPORTER SOURCE TEXT (already read for the predicate): the source contains the literal flags '--citeproc', '--csl', and '--bibliography', and the source orders the bib-copy block (the `bibSrc`/`bibDst`/`copyFile` lines) BEFORE the docx/pdf `execFileAsync('pandoc'` call (assert the source index of the bib copyFile is less than the source index of the first execFileAsync pandoc invocation for the docx/pdf branch). This is a deterministic source-ordering assertion that proves Pitfall-4 compliance without needing Pandoc installed. Add a final guarded test that runs the existing zero-trace contract on a citation-rendered md output: seed a paper, export with format:'md', pandocPresent:false, style:'apa', then assert the rendered output does NOT contain the literal 'pensmith' (case-insensitive) — proving the bibliography/References append introduced no trace. Do NOT modify tests/zero-trace-export.test.ts (it must continue to pass unchanged); this is an additional in-file regression guard.
  </action>
  <verify>
    <automated>node --import tsx --test tests/exporter.test.ts</automated>
  </verify>
  <done>A guarded test asserts the exporter source contains --citeproc/--csl/--bibliography and that the bib-copy block precedes the docx/pdf pandoc execFileAsync call (Pitfall-4 ordering). A guarded test asserts a citation-rendered md export contains no literal 'pensmith' (zero-trace non-regression). Both skip while the wiring is absent. tests/zero-trace-export.test.ts is unmodified and still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → exporter source text | The RED-by-skip predicate and the Pandoc-args/ordering assertions read bin/lib/exporter.ts as text; the boundary is read-only file access to a repo-internal source file (no untrusted input). |
| test → committed fixture | The REND-03 test reads the committed known-good fixture (.bib + section.md); trusted repo content, read-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-zt | Information Disclosure | citation-rendered md output | mitigate | The Task-3 zero-trace guard asserts no literal 'pensmith' appears in citation-rendered output; tests/zero-trace-export.test.ts stays unmodified and green (load-bearing zero-trace regression check). |
| T-13-01-det | Tampering | RED-by-skip predicate | mitigate | Predicate is a pure source-text grep computed once at module load; no wall-clock, no network — deterministic skip/run decision. |
| T-13-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (test-only edits); RESEARCH Package Legitimacy Audit confirms zero new packages. |
</threat_model>

<verification>
- `node --import tsx --test tests/exporter.test.ts` — full file parses, runs, 0 failures (new REND tests reported as skips).
- `node --import tsx --test tests/zero-trace-export.test.ts` — still passes unchanged.
- `npm run check` (typecheck + lint + tests) — green; the extended ExportDraft type with style? typechecks clean.
</verification>

<success_criteria>
- REND-01/02/03 assertions exist in tests/exporter.test.ts, all OFFLINE (pandocPresent:false), all RED-by-skip via the renderCitationsWired source-grep predicate.
- REND-03 uses the committed known-good fixture (vaswani2017attention), asserts "Vaswani" appears AND no raw [@key] survives — NOT Pandoc-gated.
- Pandoc-args (--citeproc/--csl/--bibliography) + bib-before-pandoc ordering asserted from source.
- Zero-trace non-regression asserted on citation-rendered output; tests/zero-trace-export.test.ts unmodified.
- Full suite green (new tests skip until Plan 02 lands).
</success_criteria>

<output>
Create `.planning/phases/13-citation-rendering-at-export/13-01-SUMMARY.md` when done
</output>
