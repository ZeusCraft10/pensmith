---
phase: 13-citation-rendering-at-export
plan: 02
type: execute
wave: 1
depends_on: ["13-01"]
files_modified:
  - bin/lib/citations.ts
  - bin/lib/exporter.ts
  - bin/cli/done.ts
  - tests/citation-render.test.ts
autonomous: true
requirements: [REND-01, REND-02, REND-03]
must_haves:
  truths:
    - "An exported document (offline md path) contains formatted in-text citations in place of every [@key] token — no raw [@key] survives"
    - "Every export includes a formatted bibliography / reference list in the paper's CSL style"
    - "The discipline → CSL style is resolved from INTAKE.md at done time, defaulting to APA when INTAKE.md is missing or unparseable"
    - "On the Pandoc path, buildPandocArgs emits --citeproc --csl <style.csl> --bibliography <bibDst>, and CITATIONS.bib is copied into the export dir BEFORE the pandoc shellout"
    - "The zero-trace export test still passes on citation-rendered output (no pensmith trace introduced)"
    - "All citation-js usage stays inside bin/lib/citations.ts (D-19 chokepoint); exporter.ts imports only renderStyle/renderInText/parseBib"
  artifacts:
    - path: "bin/lib/citations.ts"
      provides: "renderInText(entries, style) per-entry in-text CSL render (offline, deterministic, memoized template registration)"
      contains: "export async function renderInText"
    - path: "bin/lib/exporter.ts"
      provides: "resolveAndRenderCitations helper + buildPandocArgs citeproc args + ExportOptions.style + bib-copy reorder"
      contains: "resolveAndRenderCitations"
    - path: "bin/cli/done.ts"
      provides: "INTAKE.md discipline read → resolveStyleName → style passed into exportDraft (never-throw)"
      contains: "resolveStyleName"
  key_links:
    - from: "bin/lib/exporter.ts"
      to: "bin/lib/citations.ts"
      via: "import { parseBib, renderStyle, renderInText } from './citations.js'"
      pattern: "from './citations.js'"
    - from: "bin/cli/done.ts"
      to: "bin/lib/exporter.ts"
      via: "exportDraft({ ..., style })"
      pattern: "exportDraft\\("
    - from: "bin/cli/done.ts"
      to: "bin/lib/intake-parse.ts"
      via: "parseIntakeMd(intakeText).discipline → resolveStyleName"
      pattern: "parseIntakeMd"
---

<objective>
Wire the existing citation renderer into the export path so `[@key]` tokens resolve to formatted in-text citations and a formatted bibliography is appended/generated in the paper's CSL style — on BOTH the offline citation-js fallback path and the Pandoc citeproc primary path. This flips the Plan-01 Wave-0 RED-by-skip assertions to GREEN.

Purpose: Make exports submission-ready — no literal `[@key]` survives. Closes milestone-audit MEDIUM-1 by giving the dormant `renderStyle`/`resolveStyleName` a production consumer. Preserves zero-trace, determinism, the verifier gate, and the 16-verb bijection (exporter-internal change, no new verb).

Output: `bin/lib/citations.ts` gains `renderInText`; `bin/lib/exporter.ts` gains `resolveAndRenderCitations` + citeproc Pandoc args + `ExportOptions.style` + the bib-copy reorder; `bin/cli/done.ts` resolves the discipline → style and passes it into `exportDraft`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-citation-rendering-at-export/13-CONTEXT.md
@.planning/phases/13-citation-rendering-at-export/13-RESEARCH.md
@.planning/phases/13-citation-rendering-at-export/13-PATTERNS.md

@bin/lib/citations.ts
@bin/lib/exporter.ts
@bin/cli/done.ts
@bin/lib/intake-parse.ts
@tests/citation-render.test.ts
@.planning/phases/13-citation-rendering-at-export/13-01-SUMMARY.md

<interfaces>
<!-- Confirmed by source read. Use directly — no exploration needed. -->

bin/lib/citations.ts (the D-19 chokepoint — ALL citation-js/Cite usage lives here):
  export { Cite };                                                            // line 75
  function ensureStyleTemplate(style: string): void;                         // line 139 — memoized via registeredStyles Map (Pitfall 2 guard)
  export async function parseBib(bibtex: string): Promise<Array<Record<string, unknown>>>;  // line 189 — throws on empty/whitespace input
  export async function renderStyle(entries, style): Promise<string>;        // line 237 — BIBLIOGRAPHY only: Cite(entries).format('bibliography', {format:'text', template:`pensmith-${style}`, lang:'en-US'})
  export function resolveStyleName(discipline: string): string;             // line 262 — unknown disciplines → 'apa'
  const PKG_ROOT = findPkgRoot(__dirname);                                  // line 102 — findPkgRoot at 87; CSL at path.join(PKG_ROOT,'templates','citation-styles',`${style}.csl`)

bin/lib/exporter.ts:
  export interface ExportOptions { inputPath; outputDir?; format; paperRoot?; pandocPresent? }   // line 347
  export interface ExportResult { outputPath; format; pandocUsed; bibCopied; risCopied }         // line 359
  function buildPandocArgs(inputPath, outputPath, format): string[]                              // line 453 — current args: --from markdown --to <to> --output <out> --metadata title=/author=/date= (+pdf --variable blanks)
  export async function exportDraft(opts: ExportOptions): Promise<ExportResult>                   // line 489
    - latex branch: line 500 (pandoc execFileAsync at 505)
    - md-only / Pandoc-absent branch: line 525 (reads md, writeMarkdown at 532)
    - docx/pdf branch: line 533 (pandoc execFileAsync at 538; zeroTracePatch/zeroTracePdf at 542-543; catch→md fallback at 545-554)
    - bibCopied block: lines 559-565 (bibSrc/bibDst/copyFile) — CURRENTLY AFTER the pandoc calls; MUST move BEFORE them
    - risCopied block: lines 567-578 — leave where it is (RIS is not a --bibliography input)
  existing imports (lines 30-40): execFile, fsp, existsSync, basename/extname/join, promisify, JSZip, PDFDocument/PDFName, atomicWriteFile, isHumanizerSkillPresent/isPandocPresent, paperDir

bin/cli/done.ts:
  imports (24-34): defineCommand; readFileSync/existsSync/readdirSync; join; exportDraft/runHumanizer/ExportFormat from '../lib/exporter.js'; paperDir from '../lib/paths.js'
  the exportDraft call: lines 458-462 — { inputPath: finalPath ?? draftPath, format, paperRoot } (NO style today)
  paperDir(paperRoot) is the .paper dir; INTAKE.md lives at join(paperDir(paperRoot),'INTAKE.md')

bin/lib/intake-parse.ts:
  export function parseIntakeMd(text: string): ParsedIntake   // returns { topic, discipline, assignment }; discipline is an INTK-03 slug

Existing test homes (no new test files needed):
  - tests/citation-render.test.ts  — existing Wave-0 stub for renderStyle/apa.csl; ADD the renderInText assertion HERE
  - tests/exporter.test.ts         — Plan-01 REND-01/02/03 + Pandoc-args/ordering + zero-trace guards (Task 2 turns these GREEN)
  - tests/cli-stubs.test.ts        — exercises the done CLI verb (Task 3 must keep it green)
  - tests/zero-trace-export.test.ts — must continue to pass unmodified
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add renderInText(entries, style) to citations.ts (the D-19 in-text render path)</name>
  <read_first>bin/lib/citations.ts (the header D-19 comment lines 1-75, ensureStyleTemplate at 139, parseBib at 189, renderStyle at 237-251 — copy its forceType:'@csl/object' + format:'text'/lang:'en-US' shape); tests/citation-render.test.ts (the existing Wave-0 stub that exercises renderStyle/apa.csl — the renderInText assertion is ADDED here, NOT a new file); 13-RESEARCH.md "Verified: citation-js format('citation') output per style" + Pitfall 1 (per-entry Cite, NOT one combined Cite) + Pitfall 2 (template-collision via ensureStyleTemplate memoization); STATE decision 10-01 (renderApa delegates to renderStyle — single Map-guarded registration)</read_first>
  <files>bin/lib/citations.ts, tests/citation-render.test.ts</files>
  <behavior>
    - renderInText([vaswani2017attention], 'apa') returns a single in-text string equal to "(Vaswani et al., 2017)" (trimmed).
    - renderInText([entry], 'ieee') returns "[1]" (numeric single-entry group always numbers 1 — accept this offline; documented limitation).
    - Two back-to-back calls for the same style never throw "template already registered" (ensureStyleTemplate memoization holds).
    - renderInText is byte-stable for identical input (format:'text' + lang:'en-US', no wall-clock).
    - An empty entries array throws a clear TypeError (mirrors renderStyle's Array.isArray guard) — callers guard before calling.
  </behavior>
  <action>
  In bin/lib/citations.ts add an exported async function `renderInText(entries: Array<Record<string,unknown>>, style: string): Promise<string>` immediately after renderStyle. It MUST go through the same chokepoint plumbing renderStyle uses: validate Array.isArray (throw TypeError naming the function on failure), call ensureStyleTemplate(style) to register the pensmith-${style} template (Pitfall-2 memoized — never call templates.add directly), then `new Cite(entries, { forceType:'@csl/object' }).format('citation', { format:'text', template:`pensmith-${style}`, lang:'en-US' })` and return the result. This produces ONE combined in-text string for the given entries group (per Pitfall 1 — callers pass ONE entry per token to get a per-key string). Keep it offline+deterministic exactly like renderStyle (format:'text', lang:'en-US' → bundled en-US locale, no fetch). Do NOT add a numeric-counter reconstruction — accept that numeric styles return [1] offline (Open-Q1; the Pandoc path does correct numbering). Update the citations.ts header comment block to note renderInText is the in-text sibling of renderStyle. This keeps ALL citation-js/Cite usage inside citations.ts (D-19). Then in tests/citation-render.test.ts add a behavioral test (guarded by the file's existing shouldSkip predicate) that parses the known-good fixture bib via parseBib and asserts renderInText(entries,'apa') trims to "(Vaswani et al., 2017)" and that calling it twice for 'apa' does not throw.
  </action>
  <verify>
    <automated>node --import tsx --test tests/citation-render.test.ts</automated>
  </verify>
  <done>renderInText is exported from citations.ts, returns "(Vaswani et al., 2017)" for the fixture entry in APA, goes through ensureStyleTemplate (no template-collision on repeat calls), is offline/deterministic, and throws TypeError on a non-array input. tests/citation-render.test.ts has a renderInText assertion that passes. No new import of citation-js outside citations.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire exporter.ts — resolveAndRenderCitations helper, buildPandocArgs citeproc args, ExportOptions.style, bib-copy reorder</name>
  <read_first>bin/lib/exporter.ts (ExportOptions 347, buildPandocArgs 453-468, exportDraft 489-581 incl. the latex/md/docx-pdf branches and the bibCopied block 557-565); bin/lib/citations.ts (findPkgRoot 87-102 — copy this PKG_ROOT pattern verbatim; renderStyle/renderInText/parseBib signatures); 13-RESEARCH.md Patterns 1 & 2 + Pitfalls 1-7 (esp. 4: bib-before-pandoc; 5: empty-bib guard; 6: multi-cite [@a; @b]; 3: --citeproc before --csl/--bibliography); 13-PATTERNS.md "Memoized Style Template Registration" (call renderStyle FIRST to prime registration, then the in-text loop) + "PKG_ROOT / CSL Path Resolution"; tests/exporter.test.ts (the Plan-01 REND assertions this task must turn GREEN)</read_first>
  <files>bin/lib/exporter.ts</files>
  <behavior>
    - resolveAndRenderCitations(fixtureSection, fixtureBibPath, 'apa') returns text where [@vaswani2017attention] is replaced by "(Vaswani et al., 2017)", with "## References" + a bibliography containing "Vaswani" appended; no raw "[@" survives.
    - A multi-cite token "[@a; @b]" is replaced by a single parenthesized group joining both formatted cites (semicolon-separated); no raw "[@" survives.
    - An empty or missing bib path returns the md unchanged (no "## References" appended, no throw) — Pitfall 5.
    - An unknown citekey (not in the bib) is left as its original "[@key]" literal (harmless), never throws.
    - buildPandocArgs(..., { cslPath, bibPath }) appends '--citeproc','--csl',cslPath,'--bibliography',bibPath AFTER the metadata flags and only when both are present; --citeproc precedes --csl/--bibliography (Pitfall 3).
    - exportDraft offline (pandocPresent:false) with style:'apa' renders citations into the md output; with no style it leaves the md untouched (back-compat).
    - CITATIONS.bib is copied into the export dir BEFORE any pandoc execFileAsync call (Pitfall 4).
    - zeroTracePatch/zeroTracePdf still run on the Pandoc docx/pdf path; the offline path introduces no 'pensmith' literal.
  </behavior>
  <action>
  Make four coordinated edits, all importing the renderer via './citations.js' ONLY (D-19; never 'citation-js'):
  (1) Add imports: `import { parseBib, renderStyle, renderInText } from './citations.js';` plus `import path from 'node:path';`, `import { fileURLToPath } from 'node:url';`, `import { statSync } from 'node:fs';` (existsSync already imported). Copy the findPkgRoot helper + PKG_ROOT const verbatim from citations.ts (lines 87-102) so the CSL path resolves: `path.join(PKG_ROOT,'templates','citation-styles',`${style}.csl`)`. Use fileURLToPath(import.meta.url) for __filename (spaced-path safe — Phase-11 lesson).
  (2) Add ExportOptions.style?: string (optional, after pandocPresent) documented as the resolved CSL key; undefined → defaults to 'apa'. When the offline helper runs, default style to opts.style ?? 'apa'.
  (3) Add the async helper resolveAndRenderCitations(md, bibPath, style): guard !existsSync(bibPath) → return md; read bibText, guard !bibText.trim() → return md (Pitfall 5); parseBib(bibText) → entries. Call `const bibliography = await renderStyle(entries, style)` FIRST so ensureStyleTemplate registers pensmith-${style} before any in-text render (PATTERNS "Memoized Style Template Registration" ordering pitfall). Build a Map<string,string> intextMap by iterating entries one at a time (Pitfall 1): for each entry derive id = String(entry.id ?? ''), skip empty, set intextMap.set(id, (await renderInText([entry], style)).trim()). Replace tokens with regex /\[(@[^\]]+)\]/g: split inner on ';', trim, strip leading '@'; single key → intextMap.get(key) ?? `[@${key}]`; multi-key → strip outer parens from each formatted cite and wrap the joined (';'-separated) list in one paren pair (Pitfall 6). Append '\n\n## References\n\n' + bibliography only when bibliography.trim() is non-empty (Pitfall 5; use '## References', NEVER any 'pensmith' label — zero-trace).
  (4) Extend buildPandocArgs with a 4th optional arg `citeOpts?: { cslPath: string; bibPath: string }`; after the existing args (and pdf --variable block) push '--citeproc','--csl',citeOpts.cslPath,'--bibliography',citeOpts.bibPath only when both fields are present (Pitfall 3 ordering). 
  Then in exportDraft: MOVE the bibCopied/bibSrc/bibDst/copyFile block (currently 559-565) to run BEFORE the format branches' pandoc calls (so bibDst exists for --bibliography) — keep the bibSrc !== bibDst && existsSync(bibSrc) guard unchanged (Pitfall 4/7). In the latex (505) and docx/pdf (538) pandoc call sites, pass citeOpts = { cslPath: path.join(PKG_ROOT,'templates','citation-styles',`${style}.csl`), bibPath: bibDst } as the 4th buildPandocArgs arg ONLY when opts.style is set AND bibCopied (else pass undefined → unchanged behavior). In the md-only/Pandoc-absent branch (525) AND in the docx/pdf catch→md fallback (545-554), after reading md, if (opts.style && bibCopied) reassign `md = await resolveAndRenderCitations(md, bibDst, opts.style ?? 'apa')` before writeMarkdown — so no raw [@key] escapes on any offline path. Do NOT call resolveAndRenderCitations when Pandoc succeeds (it already resolved citations — RESEARCH anti-pattern). The risCopied block stays unchanged. zeroTracePatch/zeroTracePdf calls stay exactly as-is.
  </action>
  <verify>
    <automated>node --import tsx --test tests/exporter.test.ts</automated>
  </verify>
  <done>The Plan-01 REND-01/02/03 offline assertions now PASS (skip predicate true): exportDraft offline md+style:'apa' yields "(Vaswani et al., 2017)", "## References", "Vaswani", and no raw "[@". buildPandocArgs emits --citeproc/--csl/--bibliography (citeproc first); the bib copy runs before the pandoc calls. The exporter imports citation-js only via './citations.js'. zero-trace export test still passes; no 'pensmith' literal in citation-rendered output.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve discipline → style in done.ts and pass it into exportDraft (never-throw)</name>
  <read_first>bin/cli/done.ts (imports 24-34; the draft-read try/catch at 388-399 as the never-throw precedent; the exportDraft call at 458-462); bin/lib/intake-parse.ts (parseIntakeMd return shape { topic, discipline, assignment }); bin/lib/citations.ts resolveStyleName (262 — unknown → 'apa'); tests/cli-stubs.test.ts (the existing done-verb coverage this change must keep green); 13-RESEARCH.md Pattern 3 (discipline resolution in done.ts) + the fallback note (missing INTAKE.md → undefined → APA); STATE "Never-Throw Advisory Pattern" in 13-PATTERNS.md</read_first>
  <files>bin/cli/done.ts</files>
  <action>
  Add imports: `import { parseIntakeMd } from '../lib/intake-parse.js';` and `import { resolveStyleName } from '../lib/citations.js';` (after the existing exporter/paths imports). Immediately before the exportDraft call (line 458), insert a try/catch-and-skip block that reads INTAKE.md from join(paperDir(paperRoot),'INTAKE.md') via readFileSync, parses it with parseIntakeMd, and sets `let style: string | undefined = resolveStyleName(discipline)`. On ANY error (missing/unreadable/unparseable INTAKE.md) the catch leaves style undefined and writes nothing to stdout — export still proceeds (mirrors the draft-read never-throw at 388-399; exportDraft defaults undefined → 'apa'). Add `style` to the exportDraft({ inputPath, format, paperRoot }) call so it becomes { inputPath, format, paperRoot, style }. Do NOT add a CLI --style/--discipline flag, do NOT add a 17th verb, do NOT touch the VALID_FORMATS/format resolution above it. This is the sole done.ts change.
  </action>
  <verify>
    <automated>node --import tsx --test tests/cli-stubs.test.ts tests/exporter.test.ts</automated>
  </verify>
  <done>done.ts reads INTAKE.md discipline, resolves it via resolveStyleName, and passes style into exportDraft; a missing/unparseable INTAKE.md never throws and falls back to APA. No new CLI flag, no new verb. The cli-stubs and exporter suites pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .bib content → exported document | Bibliographic fields (author/title/journal) from CITATIONS.bib render into the export. The .bib only ever contains verifier-passed entries (compile's refuse-gate runs upstream), so the content is trusted; still rendered as plain text (format:'text'), not LaTeX/HTML. |
| INTAKE.md → style selection | done.ts reads a discipline slug from INTAKE.md; the value only indexes resolveStyleName's fixed map (unknown → 'apa'); no filesystem/shell use of the value. |
| filesystem → CSL path | buildPandocArgs constructs a CSL path from PKG_ROOT + a fixed style key; the style key is a controlled CSL slug, not free user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-zt | Information Disclosure | resolveAndRenderCitations / bibliography append | mitigate | Append uses the literal '## References' only — NEVER any string containing 'pensmith'; bibliography is plain-text paper content. The existing zeroTracePatch/zeroTracePdf sweep is unchanged and still runs on the Pandoc path; the Plan-01 zero-trace guard asserts no 'pensmith' literal in citation-rendered output. (Load-bearing concern.) |
| T-13-02-inj | Tampering | citation content injection via a hostile .bib field | mitigate | Rendering is format:'text' (plain text, not LaTeX/HTML) via the citations.ts chokepoint; parseBib normalizes BibTeX to CSL-JSON; on the Pandoc path citeproc processes the bib in its own pipeline. Bounded further: only verifier-passed entries reach the .bib. Unknown citekeys are emitted as their original literal, never executed. |
| T-13-02-det | Tampering | renderInText / resolveAndRenderCitations determinism | mitigate | renderInText uses format:'text' + lang:'en-US' (bundled locale, no fetch, no wall-clock); ensureStyleTemplate is Map-memoized; output is byte-stable for identical input. No Date.now/random in the render path. |
| T-13-02-d19 | Tampering | citation-js import discipline | mitigate | exporter.ts imports only parseBib/renderStyle/renderInText from './citations.js' (D-19 chokepoint enforced by no-restricted-imports); no direct 'citation-js' or Cite.plugins access outside citations.ts. |
| T-13-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs — Phase 13 is code-only (RESEARCH Package Legitimacy Audit: zero new packages; citation-js already slopcheck [OK] + D-19 chokepoint). |
</threat_model>

<verification>
- `node --import tsx --test tests/exporter.test.ts` — Plan-01 REND-01/02/03 + Pandoc-args/ordering + zero-trace guards all GREEN.
- `node --import tsx --test tests/citation-render.test.ts` — renderInText behavior (APA in-text, no template collision, deterministic).
- `node --import tsx --test tests/cli-stubs.test.ts` — done CLI verb still green after the discipline→style wiring.
- `node --import tsx --test tests/zero-trace-export.test.ts` — still passes unmodified.
- `npm run check` — full typecheck + lint (D-19 no-restricted-imports clean) + full suite green.
</verification>

<success_criteria>
- Offline export resolves every `[@key]` (incl. multi-cite `[@a; @b]`) to a formatted in-text citation; no raw `[@key]` survives (REND-01).
- Every export includes a formatted bibliography in the resolved CSL style (REND-02).
- The Plan-01 REND-03 offline assertion passes: a formatted reference ("Vaswani") appears on the known-good fixture, NOT Pandoc-gated.
- Pandoc path emits `--citeproc --csl --bibliography`; bib copied before the pandoc shellout.
- done.ts resolves discipline→style from INTAKE.md, defaults to APA, never throws.
- Zero-trace preserved (test still passes; no 'pensmith' literal in rendered output); determinism preserved; D-19 chokepoint intact; no new verb.
</success_criteria>

<output>
Create `.planning/phases/13-citation-rendering-at-export/13-02-SUMMARY.md` when done
</output>
