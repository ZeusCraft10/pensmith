---
phase: 00-repo-skeleton-plugin-manifest
plan: 04
type: execute
wave: 3
depends_on:
  - "00-01"
  - "00-02"
  - "00-03"
files_modified:
  - .github/workflows/ci.yml
autonomous: false
requirements:
  - REPO-04
must_haves:
  truths:
    - "CI workflow at .github/workflows/ci.yml exists and runs on push and pull_request to main"
    - "CI matrix is [ubuntu-latest, macos-latest, windows-latest] × [Node 20.10] (linux-x64 + macos-arm64 + windows-x64 per D-10)"
    - "CI steps run in this order: checkout → setup-node@v4 (with cache:npm) → npm ci → npm run lint → npx tsc --noEmit → npm run build → npm test → npm run validate:manifests (where npm test invokes scripts/run-tests.mjs which runs node --import tsx --test against discovered *.test.ts files)"
    - "fail-fast is false so all three OSes report independently"
    - "After pushing to GitHub, the CI matrix shows green on all three OSes (verified by human checkpoint after first push)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI matrix"
      contains: "macos-latest"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/validate-plugin-manifest.cjs"
      via: "final CI step after build"
      pattern: "validate-plugin-manifest"
    - from: "actions/setup-node@v4 cache:npm"
      to: "package-lock.json"
      via: "automatic cache key derivation"
      pattern: "cache: npm"
---

<replan_history>
- **Cycle 2 (2026-05-07):** No structural changes — the `node --test` vs `npm test` contradiction flagged by codex was already resolved in the cycle-1 final draft. The CI YAML in this plan invokes `npm test` (line 112), which Plan 01's `package.json` resolves to `node scripts/run-tests.mjs` (the portable cross-platform runner). REPO-04 in REQUIREMENTS.md was updated cycle-1 to read "`npm test` (which discovers and runs `tests/**/*.test.ts` via `scripts/run-tests.mjs` + `node --import tsx --test`)" — the requirement, the plan, and the YAML are all consistent. `node scripts/validate-plugin-manifest.cjs` is the manifest validation step (step name "Validate plugin manifests" in the YAML), correctly using the `.cjs` extension.
- See `00-VALIDATION.md` cycle-2 update for the per-task verification map (00-04-T1 automated + 00-04-T2 human-checkpoint).
</replan_history>

<objective>
Wire GitHub Actions CI (REPO-04 + D-09 + D-10 + D-11 + D-12) so that every push and pull request runs the full Phase 0 acceptance pipeline on linux-x64, macos-arm64, and windows-x64 with Node 20.10. After this plan, Phase 0 is complete: a fresh git push triggers `npm ci → lint → typecheck → build → test → validate:manifests` on all three OSes and reports independently.

Purpose: Per Pitfall 8 + RESEARCH §"Pitfall C: macos-arm64 runner availability", cross-platform path landmines surface only on real OSes. CI on Linux only is a false-pass machine. The matrix gate exists from Phase 0 forward; every later phase inherits it as a precondition.

Output: `.github/workflows/ci.yml` plus a human verification step that the first push to GitHub actually shows green on all three matrix entries (and that `macos-latest` is reporting `arm64` per Pitfall C).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/00-repo-skeleton-plugin-manifest/00-CONTEXT.md
@.planning/phases/00-repo-skeleton-plugin-manifest/00-RESEARCH.md
@package.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create .github/workflows/ci.yml with the locked matrix and step order</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/00-repo-skeleton-plugin-manifest/00-CONTEXT.md (D-09, D-10, D-11, D-12 are LOCKED)
    - .planning/phases/00-repo-skeleton-plugin-manifest/00-RESEARCH.md §"Concrete File Shapes" `.github/workflows/ci.yml` exact YAML + §"Pitfall C: macos-arm64 runner availability" + §"Pitfall D: dist/ chicken-and-egg"
    - package.json (`scripts.lint`, `scripts.typecheck`, `scripts.build`, `scripts.test`, `scripts.validate:manifests`)
  </read_first>
  <action>
    Create `.github/workflows/ci.yml`. Reproduce the YAML from RESEARCH §"Concrete File Shapes" verbatim, with the `npm run build` step BEFORE `npm test` and `npm run validate:manifests` (per RESEARCH §"Pitfall D" — the validator's Pitfall D guard requires `dist/mcp/server.js` to exist when `dist/` exists). Note: tests are invoked via `npm test` (which runs `scripts/run-tests.mjs`) rather than raw `node --test`, because the runner is required for portable Windows-safe test discovery (D-10) and tsx-loader sourcing of `.test.ts` files.

    File content:
    ```yaml
    name: CI

    on:
      push:
        branches: [main]
      pull_request:
        branches: [main]

    jobs:
      check:
        strategy:
          fail-fast: false
          matrix:
            os: [ubuntu-latest, macos-latest, windows-latest]
            node: ['20.10']
        runs-on: ${{ matrix.os }}
        steps:
          - uses: actions/checkout@v4

          - uses: actions/setup-node@v4
            with:
              node-version: ${{ matrix.node }}
              cache: npm

          - name: Verify macos runner is arm64 (Pitfall C)
            if: matrix.os == 'macos-latest'
            run: |
              echo "runner.arch=$RUNNER_ARCH"
              test "$RUNNER_ARCH" = "ARM64"
            shell: bash

          - run: npm ci

          - run: npm run lint

          - run: npx tsc --noEmit

          - name: Build (produces dist/mcp/server.js for manifest validation)
            run: npm run build

          - run: npm test

          - name: Validate plugin manifests
            run: node scripts/validate-plugin-manifest.cjs
    ```

    Notes about the YAML:
    - Per D-10: matrix is exactly `[ubuntu-latest, macos-latest, windows-latest]` × `[node@20.10]`. `ubuntu-latest` is linux-x64, `macos-latest` is arm64 since macos-14 (RESEARCH Pitfall C verified 2024-2025), `windows-latest` is windows-x64.
    - Per D-11: step order is `setup-node@v4 → npm ci → npm run lint → tsc --noEmit → build → test → validate:manifests`. The `npm run build` insertion BETWEEN typecheck and test (rather than at the end) is explicit: the manifest validator's Pitfall D guard fires only if `dist/` exists without `dist/mcp/server.js` — building first means the guard sees the path resolve cleanly.
    - Per D-12: `cache: npm` on `setup-node@v4` reads `package-lock.json` for the cache key automatically — no separate `actions/cache@v4` step.
    - `fail-fast: false` ensures all three OS entries report independently. If macOS fails, Linux + Windows still run to completion. This is essential for diagnosing Pitfall 8 cross-platform issues.
    - The Pitfall C arch verification step (`test "$RUNNER_ARCH" = "ARM64"`) explicitly fails the macOS run if GitHub silently demotes `macos-latest` back to Intel. This is cheap insurance.

    Run a YAML lint locally to confirm the file parses. Use Node's built-in approach:
    ```bash
    node -e "console.log('YAML file present:', require('fs').existsSync('.github/workflows/ci.yml'))"
    ```
    A real YAML parser is in `package.json` only at Phase 8 (`yaml` package); for Phase 0 we rely on a syntactic pass via the GitHub Actions runner itself when CI fires.

    DO NOT add codecov, no separate cache action, no scheduled triggers, no environment variables (D-12 + Deferred Ideas in CONTEXT.md). Phase 6 will add the launch-prep operational settings.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('.github/workflows/ci.yml','utf8'); for (const s of ['ubuntu-latest','macos-latest','windows-latest','20.10','actions/setup-node@v4','npm ci','npm run lint','npx tsc --noEmit','npm run build','npm test','validate-plugin-manifest.cjs','fail-fast: false','RUNNER_ARCH']) { if (!c.includes(s)) { console.error('CI missing:',s); process.exit(1); } } console.log('CI yaml contains all required steps')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists and contains all of these literal strings: `name: CI`, `ubuntu-latest`, `macos-latest`, `windows-latest`, `20.10`, `actions/checkout@v4`, `actions/setup-node@v4`, `cache: npm`, `npm ci`, `npm run lint`, `npx tsc --noEmit`, `npm run build`, `npm test`, `validate-plugin-manifest.cjs`, `fail-fast: false`, `RUNNER_ARCH`
    - The CI step order in the file is exactly: checkout → setup-node → arch verify → `npm ci` → lint → typecheck → build → test → validate:manifests
    - The `node-version` matrix is exactly `['20.10']` (single entry; D-10 locks Node 22 to v0.2)
    - `on:` triggers are `push: { branches: [main] }` and `pull_request: { branches: [main] }`
    - The verify command exits 0 (confirms all required strings present)
    - Locally, `npm run check` (= lint + typecheck + test + validate:manifests) and `npm run build` BOTH exit 0 — proving the steps the CI matrix will run are themselves green on the dev machine
  </acceptance_criteria>
  <done>CI workflow file exists with the locked matrix + step order; arch verification step protects against Pitfall C; `npm run check` + `npm run build` both green locally so the workflow has a working pipeline to execute.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — first git push to GitHub triggers green CI on all three OSes</name>
  <what-built>
    The full Phase 0 repo (Plans 01–04) + the CI workflow at `.github/workflows/ci.yml`. After this checkpoint, the user pushes the repo to GitHub for the first time and confirms the matrix is green on linux-x64, macos-arm64, and windows-x64 with Node 20.10.
  </what-built>
  <how-to-verify>
    1. Confirm the user has a GitHub repository at github.com/akhilachanta/pensmith (or the user's chosen name). If the repo does not yet exist, the user creates it via `gh repo create akhilachanta/pensmith --public --source=. --remote=origin --push` (requires `gh` CLI authenticated; user may also use the GitHub web UI). Do NOT auto-create the repo without user confirmation — repo creation is a deliberate launch step.
    2. The user pushes the current branch to `main`: `git push -u origin main`. The first push triggers the CI workflow.
    3. Open the Actions tab on GitHub: `https://github.com/<user>/pensmith/actions`. Confirm the `CI` workflow is queued, then running, then green on ALL THREE matrix entries (`check (ubuntu-latest, 20.10)`, `check (macos-latest, 20.10)`, `check (windows-latest, 20.10)`).
    4. Click into the macos-latest run, expand the "Verify macos runner is arm64 (Pitfall C)" step, confirm the output is `runner.arch=ARM64` and the step passed.
    5. Click into the windows-latest run, expand the `npm run lint` and `npx tsc --noEmit` steps, confirm no Windows-specific failures (CRLF lint storms, path resolution). The `.gitattributes` `eol=lf` from Plan 01 is the protection here; Pitfall 8 is the failure mode it prevents.
    6. Confirm all three runs reach the final `Validate plugin manifests` step and it exits 0 with `✓ plugin.json + marketplace.json + .mcp.json valid`.
  </how-to-verify>
  <resume-signal>
    Type `approved` if all three matrix entries are green and macos-latest reported arm64.
    If any matrix entry failed, paste the failing step name + last 30 lines of step output. The orchestrator will route to gap-closure.
    If repo creation is blocked (not your call to make), type `blocked: <reason>` and the workflow halts.
  </resume-signal>
</task>

</tasks>

<verification>
- Locally: `npm run check && npm run build` exits 0
- The verify command in Task 1 confirms `.github/workflows/ci.yml` contains every required string
- Human checkpoint (Task 2) confirms first push is green on all three OSes with macos-latest = arm64
</verification>

<success_criteria>
1. `.github/workflows/ci.yml` exists with the locked matrix from D-10, step order from D-11, cache strategy from D-12
2. Pitfall C arch verification step explicitly fails if `macos-latest` reports anything other than ARM64
3. `fail-fast: false` ensures independent reporting per OS
4. Locally, the full `npm run check && npm run build` pipeline exits 0
5. After first push, CI shows green on linux-x64, macos-arm64 (verified), and windows-x64 — verified by human checkpoint
6. Phase 0 ROADMAP success criterion #1 (lint + tsc --noEmit + npm test all pass on all three OSes in CI) is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/00-repo-skeleton-plugin-manifest/00-04-SUMMARY.md` summarizing:
- The CI YAML file path + matrix entries
- Local verification: literal stdout from `npm run check && npm run build`
- Link to the first green CI run (after the human checkpoint)
- Confirmation that macos-latest reported `RUNNER_ARCH=ARM64`
- Any deviation from the plan (e.g., if the user chose a different repo name or branch)
</output>
