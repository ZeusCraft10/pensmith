---
phase: 04-breadth-n-sections-compile-wave-scheduling
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - bin/lib/revise.ts
  - bin/cli/revise.ts
  - workflows/revise.md
  - templates/prompts/revise-swap.md
  - bin/lib/prompt-loader.ts
  - bin/pensmith.ts
  - tests/revise-swap.test.ts
  - tests/repo-files.test.ts
  - tests/tier-contract.test.ts
  - tests/cassettes/revise-swap-suggest.json
  - tests/cassettes/revise-swap-remove.json
  - tests/cassettes/revise-swap-rejected.json
autonomous: true
requirements: [WRTE-02, PLAN-02, PLAN-03, RSCH-10]
must_haves:
  truths:
    - "`pensmith revise --section N` swaps a verifier-flagged citation via an LLM proposal behind an approval gate"
    - "The same bin/lib/revise.ts chokepoint backs both Tier 1 and Tier 2 (no divergent code path)"
    - "On accept, DRAFT.md is patched atomically and verified_against_draft_hash is reset to null"
    - "The approval gate is default-on; --yolo skips approval and auto-loops at most 2 retries, then emits RETRY_EXHAUSTED"
    - "`--research <query>` appends to .paper/RESEARCH.md AND sections/<N>/RESEARCH-LOG.md without disturbing other sections"
    - "A per-section voice hint flows into the re-draft input (WRTE-02 per-section consume point)"
    - "workflows/revise.md has a tier-contract registry entry in THIS plan (CONTRIBUTING.md D-24)"
  artifacts:
    - path: "bin/lib/revise.ts"
      provides: "runRevise — single Tier-1/Tier-2 chokepoint: parse verdict → LLM swap → approval → patch → reset hash"
      exports: ["runRevise", "ReviseResult"]
    - path: "bin/cli/revise.ts"
      provides: "citty revise verb delegating 100% to runRevise"
      contains: "runRevise"
    - path: "workflows/revise.md"
      provides: "approval-gate capability_check (AskUserQuestion → clack) + Body"
      contains: "approval"
    - path: "templates/prompts/revise-swap.md"
      provides: "hash-pinned strict-JSON citation-swap prompt"
  key_links:
    - from: "bin/lib/revise.ts"
      to: "templates/prompts/revise-swap.md"
      via: "loadPrompt('revise-swap') hash-validated"
      pattern: "revise-swap"
    - from: "bin/lib/revise.ts"
      to: "bin/lib/atomic-write.ts::atomicWriteFile"
      via: "DRAFT.md patch + PLAN.md frontmatter reset"
      pattern: "atomicWriteFile"
    - from: "bin/lib/revise.ts"
      to: "bin/lib/citation-token.ts::replaceCitekeys"
      via: "locate + swap/remove the flagged [@key] token in DRAFT.md (Plan 01 output)"
      pattern: "replaceCitekeys"
---

<objective>
Ship the section-mutation surface: `pensmith revise --section N` (and the Tier-1 slash equivalent) parses the section's VERIFICATION.md for the first FABRICATED/MIS-CITED/NOT_FOUND verdict, asks an LLM (via a hash-pinned strict-JSON prompt) to propose a citekey swap drawn ONLY from the section's `assigned_sources`, renders the diff behind the default-on approval gate (PRD §19), and on accept patches DRAFT.md atomically and resets `verified_against_draft_hash` to null. `--yolo` skips the approval and auto-loops the same code path up to 2 retries (then `RETRY_EXHAUSTED`). `--research <query>` appends new findings to the project RESEARCH.md + bib and a per-section RESEARCH-LOG.md without touching other sections. A per-section voice hint is threaded into the re-draft input (WRTE-02 per-section consume point).

Purpose: WRTE-02 (per-section revise/voice-hint chokepoint — D-06), PLAN-02 (`--revise` re-plan from verification feedback), PLAN-03 (`--research <query>` section-scoped, no cross-section disturbance), RSCH-10 (re-verify uses the freshness-aware Pass 1 from Plan 02). Depends on Plan 02 (freshness/Pass 1) AND Plan 01 (consumes `replaceCitekeys` from `bin/lib/citation-token.ts` to locate/swap the flagged token — declared though wave order already makes it safe). Runs in Wave 2 alongside Plan 03.
Output: `bin/lib/revise.ts`, `bin/cli/revise.ts`, `workflows/revise.md`, `templates/prompts/revise-swap.md`, prompt-loader + repo-files patches, the revise tier-contract registry entry, test + 3 cassettes.

NOTE on WRTE-02: Phase 4 delivers the per-section revise chokepoint and the voice-hint consume point per 04-CONTEXT/04-RESEARCH D-06. Full style-match featurization (STYL-*) is Phase 8; Phase 4 does NOT build style profiles.
NOTE on prompt slugs: 04-CONTEXT.md D-12 (smoother) and D-05 (revise-swap) explicitly authorize two NEW hash-pinned prompt slugs for Phase 4, superseding the Phase-3 "8 LOCKED slugs" comment in prompt-loader.ts. Update that comment to reflect Phase-4 D-05/D-12.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-breadth-n-sections-compile-wave-scheduling/04-CONTEXT.md
@.planning/phases/04-breadth-n-sections-compile-wave-scheduling/04-RESEARCH.md
@.planning/phases/04-breadth-n-sections-compile-wave-scheduling/04-PATTERNS.md
@.planning/phases/04-breadth-n-sections-compile-wave-scheduling/04-VALIDATION.md
@CLAUDE.md
@.planning/phases/04-breadth-n-sections-compile-wave-scheduling/04-01-SUMMARY.md
@.planning/phases/04-breadth-n-sections-compile-wave-scheduling/04-02-SUMMARY.md

<interfaces>
<!-- Extracted contracts — use directly. -->

From bin/lib/citation-token.ts (Plan 01 — CONSUMED HERE):
  CITATION_TOKEN_RE = /\[@([a-z][a-z0-9_-]*)\]/g
  extractCitekeys(md): string[]   // deduped, first-appearance order
  replaceCitekeys(md, fn): string // round-trips every [@key]; used to locate + swap the flagged token

From bin/lib/prompt-loader.ts: loadPrompt(slug) hash-validates against
EXPECTED_PROMPT_HASHES (Record<string,string>); interpolate(body, vars) throws on
missing var. Sentinel pattern: a `__PENDING_HASH_<slug>__` value bypasses validation
only when PENSMITH_ALLOW_PENDING_PROMPT_HASHES=1 (use during prompt authoring, then
re-pin the real SHA-256). tests/repo-files.test.ts imports EXPECTED_PROMPT_HASHES
(single source of truth — WN-3): adding a slug requires the matching pin in BOTH files.

Revise-swap prompt strict-JSON contract (04-RESEARCH §I):
  { "action": "swap" | "remove", "flagged_citekey": "<k>",
    "replacement_citekey": "<k>"|null, "rationale": "<sentence>",
    "patch": { "before_excerpt": "...[@flagged]...", "after_excerpt": "...[@replacement]..." } }

From tests/tier-contract.test.ts: the `PHASE_3_CASES` registry + `interface Phase3Case`.
`revise` currently has NO registry entry. CONTRIBUTING.md D-24 requires the new
workflows/revise.md body to register its tier-contract case in THIS plan (the plan
that creates the workflow). At minimum a stub case lands here; Plan 05 Task 4 extends
it with the full --yolo parity assertions.

D-05/D-06 (LOCKED): single bin/lib/revise.ts chokepoint for both tiers; approval
gate default-on, --yolo skips. On accept: patch DRAFT.md, reset
verified_against_draft_hash → null. D-06 retry cap = 2 under --yolo, then RETRY_EXHAUSTED.
"remove" action = MECHANICAL bracket-clause delete (NOT LLM prose rewrite — 04-RESEARCH §I).
Multi-failure: one-at-a-time, in order of appearance.

D-09 (LOCKED) --research: append project-level .paper/RESEARCH.md + merge into
.paper/CITATIONS.bib (with non-standard `from_section: <N>` annotation), AND append a
provenance entry to sections/<N>/RESEARCH-LOG.md (query, adapter, hit-count,
citekeys-added, timestamp). RESEARCH-LOG.md is the ONLY section-level file --research creates.

Reuse: bin/lib/verify/pass1.ts + pass3.ts (re-verify after swap), @clack/prompts
(approval UI; non-TTY → exit code 3 + "use --yolo"), bin/lib/frontmatter.ts /
state.ts updateFrontmatter (reset hash under withLock), bin/lib/sources/*.ts (research adapters),
bin/lib/bibtex-write.ts (bib merge via citations.ts chokepoint).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — revise-swap prompt (sentinel-pinned) + failing tests + cassettes</name>
  <files>templates/prompts/revise-swap.md, bin/lib/prompt-loader.ts, tests/repo-files.test.ts, tests/cassettes/revise-swap-suggest.json, tests/cassettes/revise-swap-remove.json, tests/cassettes/revise-swap-rejected.json, tests/revise-swap.test.ts</files>
  <read_first>
    - templates/prompts/section-planner.md (structured-output prompt with constraints + Input/Output sections — closest analog)
    - bin/lib/prompt-loader.ts (EXPECTED_PROMPT_HASHES map lines 87-101; sentinel + env-bypass lines 150-158)
    - tests/repo-files.test.ts (PENDING_HASH_PINS / prompt-hash pin assertions — imports EXPECTED_PROMPT_HASHES)
    - tests/section-isolation.test.ts (multi-step fixture + mtime assertions)
    - bin/lib/http-mock.ts (cassette schema + header scrub)
    - bin/lib/citation-token.ts (Plan 01 — replaceCitekeys, the token-locating helper consumed in Task 2)
    - 04-RESEARCH §I (revise-swap flow + prompt skeleton + risks), 04-CONTEXT.md D-05/D-06/D-09
    - 04-VALIDATION.md row WRTE-02 + Manual-Only "--revise approval gate UX"
  </read_first>
  <action>
    Author `templates/prompts/revise-swap.md` per the 04-RESEARCH §I skeleton: 4 hard constraints (replacement ONLY from the `## Available sources` list; replacement must support the surrounding claim or recommend `remove`; no new citekeys / none from outside the assigned list; STRICT JSON output matching the contract in `<interfaces>`). Include the JSON schema in the prompt body and Input sections for the flagged citation, claim context, verifier reason, and available sources. Register it in `bin/lib/prompt-loader.ts` `EXPECTED_PROMPT_HASHES` with a `__PENDING_HASH_revise-swap__` sentinel for now, and add the matching pin in `tests/repo-files.test.ts` (both files in the SAME commit — WN-3). Update the prompt-loader header comment to note Phase-4 D-05/D-12 authorize the two new slugs (`revise-swap`, and `smoother` from Plan 05).

    Create three nock-compatible cassettes (each ≤3KB, header-scrubbed) for the LLM swap call: `revise-swap-suggest.json` (a clean swap proposal), `revise-swap-remove.json` (a `remove` recommendation), `revise-swap-rejected.json` (a proposal the user/test rejects).

    Create `tests/revise-swap.test.ts` (RED, WRTE-02): seed a section with a VERIFICATION.md flagging one citation + a PLAN.md with `assigned_sources`. Run `runRevise({ ..., yolo: true })` against the suggest cassette and assert DRAFT.md is patched AND PLAN.md frontmatter `verified_against_draft_hash` is reset to null. Add: a rejected-proposal case leaves DRAFT.md unchanged (no-op, exit 0); a `--yolo` retry-exhaustion case (2 failed retries) writes `RETRY_EXHAUSTED` to VERIFICATION.md; a `--research <query>` case appends to BOTH `.paper/RESEARCH.md` and `sections/<N>/RESEARCH-LOG.md` while leaving a sibling section's files untouched (mtime check). Do NOT exercise the interactive approval UI in CI — drive the `--yolo` path or stub `@clack/prompts`.
  </action>
  <verify>
    <automated>node --import tsx --test tests/revise-swap.test.ts 2>&1; if ($LASTEXITCODE -eq 0) { throw "Expected failing (RED) tests but the suite passed" }</automated>
  </verify>
  <acceptance_criteria>
    - `templates/prompts/revise-swap.md` exists with the 4 hard constraints and the strict-JSON schema in its body.
    - `EXPECTED_PROMPT_HASHES` has a `revise-swap` entry (sentinel acceptable) AND `tests/repo-files.test.ts` has the matching pin (both edited together).
    - Three cassettes exist ≤3KB each, no Authorization/x-api-key headers.
    - `tests/revise-swap.test.ts` is RED (fails — confirmed by non-zero exit — because `bin/lib/revise.ts` is absent), and covers: accept+reset, reject no-op, RETRY_EXHAUSTED, and --research cross-section isolation.
  </acceptance_criteria>
  <done>Sentinel-pinned prompt, RED test, and 3 cassettes committed; prompt-loader header updated for Phase-4 slugs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: revise.ts chokepoint — verdict parse → LLM swap → approval → patch → reset</name>
  <files>bin/lib/revise.ts</files>
  <read_first>
    - tests/revise-swap.test.ts (RED spec)
    - bin/lib/verify/pass1.ts (lines ~177-197 — single-export pipeline both CLI + future MCP call; structured failure output to parse)
    - bin/lib/verify/pass3.ts (structured failure output)
    - bin/lib/frontmatter.ts + bin/lib/state.ts (updateFrontmatter under withLock — reset verified_against_draft_hash)
    - bin/lib/atomic-write.ts (atomicWriteFile — sole writer)
    - bin/lib/citation-token.ts (Plan 01 — extractCitekeys/replaceCitekeys for locating + removing the flagged token)
    - bin/lib/sources/*.ts (research adapters for --research), bin/lib/bibtex-write.ts (bib merge via citations.ts)
    - 04-RESEARCH §I (6-step flow + remove=mechanical-delete), 04-CONTEXT.md D-05/D-06/D-09
  </read_first>
  <behavior>
    - runRevise parses the first failing citation, loads assigned_sources, calls the LLM, validates strict JSON via zod, gates on approval (skipped under yolo).
    - On accept (swap): replace the flagged [@k] in DRAFT.md (atomic), reset hash null. On remove: mechanically delete the bracketed clause containing the token (no LLM prose rewrite).
    - --yolo retry cap 2, then RETRY_EXHAUSTED to VERIFICATION.md.
    - --research appends project RESEARCH.md + bib (from_section annotation) + section RESEARCH-LOG.md only.
    - Identical code path for both tiers (D-06).
  </behavior>
  <action>
    Create `bin/lib/revise.ts` exporting `runRevise(opts: { paperRoot: string; n: number; slug: string; yolo: boolean; research?: string }): Promise<ReviseResult>` and `ReviseResult`. Flow (04-RESEARCH §I):
    1. Parse `sections/<N>/VERIFICATION.md` for the FIRST failing citation (FABRICATED/MIS-CITED/NOT_FOUND), in order of appearance (one-at-a-time).
    2. Load PLAN.md frontmatter → `assigned_sources` + any voice hint (WRTE-02 consume point — thread the voice hint into the prompt vars).
    3. Call the LLM via `loadPrompt('revise-swap')` + `interpolate(...)`; parse the response with a strict zod schema. REJECT if `action ∉ {swap,remove}` or `replacement_citekey ∉ assigned_sources` (LLM-response-injection mitigation).
    4. Approval gate (default-on): render a before/after diff and prompt via `@clack/prompts`; skip when `yolo`. Non-TTY without `--yolo` → exit code 3 + "use --yolo to auto-accept".
    5. On accept: `swap` → use `replaceCitekeys` (from bin/lib/citation-token.ts, Plan 01) to locate the flagged token in DRAFT.md and substitute the replacement, write via `atomicWriteFile`; `remove` → mechanically delete the bracketed clause containing the token (NO LLM rewrite — 04-RESEARCH §I). Then reset PLAN.md `verified_against_draft_hash` → null via `updateFrontmatter` under `withLock` (D-05). On reject: no-op, return exit 0.
    6. `--yolo` auto-loop: re-run the same path up to 2 retries; on exhaustion write `RETRY_EXHAUSTED` to VERIFICATION.md (D-06).
    For `research`: append findings to `.paper/RESEARCH.md` and merge new entries into `.paper/CITATIONS.bib` via `bin/lib/bibtex-write.ts` (adding the non-standard `from_section: <N>` annotation), AND append a provenance row to `sections/<N>/RESEARCH-LOG.md` (query, adapter, hit-count, citekeys-added, ISO timestamp). Touch NO other section's files (D-09 / PLAN-03). ALL writes route through `atomicWriteFile`. This single module backs both tiers (D-06) — no Tier-specific branch in the logic.
  </action>
  <verify>
    <automated>node --import tsx --test tests/revise-swap.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/revise-swap.test.ts passes (GREEN): accept+reset-null, reject no-op, RETRY_EXHAUSTED, --research cross-section isolation.
    - `runRevise` rejects an LLM response whose `replacement_citekey` is not in `assigned_sources` (strict zod / membership check).
    - The flagged-token swap goes through `replaceCitekeys` from `bin/lib/citation-token.ts` (Grep confirms the Plan 01 helper is the locator, not a bespoke regex).
    - `remove` deletes the bracketed clause mechanically (no second LLM call in the remove branch — Grep shows one prompt invocation max per attempt).
    - All writes in `bin/lib/revise.ts` go through `atomicWriteFile` (no `fs.writeFile`); hash reset goes through `updateFrontmatter`/`withLock`.
    - `--research` writes only `.paper/RESEARCH.md`, `.paper/CITATIONS.bib`, and `sections/<N>/RESEARCH-LOG.md` — a sibling section's mtime is unchanged.
    - `tsc --noEmit` + `npm run lint` pass.
  </acceptance_criteria>
  <done>Single revise chokepoint green across all four behavioral cases; cross-section isolation preserved.</done>
</task>

<task type="auto">
  <name>Task 3: revise verb + workflow body + re-pin real prompt hash + revise tier-contract entry</name>
  <files>bin/cli/revise.ts, workflows/revise.md, bin/pensmith.ts, bin/lib/prompt-loader.ts, tests/repo-files.test.ts, tests/tier-contract.test.ts</files>
  <read_first>
    - bin/cli/verify.ts (lines 40-139 — thin citty orchestrator delegating to bin/lib/ + structured return)
    - bin/lib/verbs.ts (UX02_VERBS list — `revise` is already in the 16-verb UX-02 set) and bin/pensmith.ts (REAL_VERB_LOADERS map lines ~37-50)
    - workflows/verify.md (full capability_check + Body + Outputs pattern)
    - bin/lib/revise.ts (Task 2 output)
    - bin/lib/prompt-loader.ts + tests/repo-files.test.ts (re-pin the sentinel to the real SHA-256)
    - tests/tier-contract.test.ts (PHASE_3_CASES registry + interface Phase3Case; runCliInDir / runMcpToolInDir helpers)
    - CONTRIBUTING.md ("Tier contract — do not skip"; D-24 — every new workflow body registers its tier-contract case in the SAME plan that creates the workflow)
    - 04-CONTEXT.md D-06 (Tier-2 surface mirrors Tier-1)
  </read_first>
  <action>
    Create `bin/cli/revise.ts` as a citty `defineCommand({ args: { n: positional, section: alias-of-n, yolo: boolean, research: string } })` delegating 100% to `bin/lib/revise.ts::runRevise` (thin orchestrator — no logic in the verb). Register the real loader in `bin/pensmith.ts` `REAL_VERB_LOADERS` so the already-declared `revise` UX-02 verb resolves (do NOT add a new top-level verb — `revise` is in the locked 16).

    Create `workflows/revise.md` mirroring `workflows/verify.md` structure: a `<capability_check>` requiring `Task` (LLM call) and `AskUserQuestion` (approval gate) with `degrade_if_missing` → `@clack/prompts` TTY for approval, and exit-code-3 for non-TTY without `--yolo`. Body steps follow the 04-RESEARCH §I flow. Do NOT skip the approval gate (PRD §19 non-negotiable). Do NOT reference dormant Pass 2/4 prompts.

    Re-pin the prompt: now that `revise-swap.md` is byte-stable, compute its SHA-256 and replace the `__PENDING_HASH_revise-swap__` sentinel in `bin/lib/prompt-loader.ts` with the real hash, updating the matching pin in `tests/repo-files.test.ts` in the SAME commit (WN-3 lockstep).

    Satisfy the CONTRIBUTING.md D-24 obligation IN THIS PLAN, where `workflows/revise.md` is created: add a `revise` entry to the `PHASE_3_CASES` registry in `tests/tier-contract.test.ts` (mcpTool `pensmith_revise` if registered, else `null`; `cliArgs: ['revise', MIDDLE_SECTION, '--yolo']`; `verbFile: 'bin/cli/revise.ts'`; `expectedArtifact` = the patched section DRAFT.md or the RESEARCH-LOG.md). At minimum land a STUB case that seeds a one-flagged-citation fixture, runs `--yolo`, and asserts both tiers reach the same terminal patched state. Do NOT defer the registry entry to Plan 05 — D-24 requires it here. Plan 05 Task 4 MAY extend it with full ±-tolerance parity assertions.
  </action>
  <verify>
    <automated>node --import tsx --test tests/revise-swap.test.ts tests/repo-files.test.ts tests/tier-contract.test.ts; npm run build 2>&1 | Select-String -Pattern "error TS" -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - `bin/cli/revise.ts` exists and delegates entirely to `runRevise` (no business logic in the verb body).
    - `revise` resolves via `bin/pensmith.ts` REAL_VERB_LOADERS (no new verb added outside the locked 16).
    - `workflows/revise.md` has the approval-gate `<capability_check>` (AskUserQuestion → clack degrade) and references no Pass 2/4 prompt.
    - The `revise-swap` entry in `EXPECTED_PROMPT_HASHES` is a real 64-char SHA-256 (no sentinel), matching the pin in `tests/repo-files.test.ts`; `loadPrompt('revise-swap')` succeeds WITHOUT `PENSMITH_ALLOW_PENDING_PROMPT_HASHES`.
    - `tests/tier-contract.test.ts` has a `revise` registry entry (at minimum a stub case) added in THIS plan — CONTRIBUTING.md D-24 obligation satisfied where workflows/revise.md is created.
    - `tests/repo-files.test.ts` and `tests/tier-contract.test.ts` pass; `npm run build`, `tsc --noEmit`, `npm run lint` pass.
  </acceptance_criteria>
  <done>revise verb + workflow wired both tiers; prompt hash re-pinned real; revise tier-contract entry landed in-plan; repo-files green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM swap response → DRAFT.md patch | Model output drives a file mutation |
| --research query → outbound adapter calls | User query reaches research adapters / network |
| approval gate → file mutation | Human/--yolo decision gates the patch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Tampering | revise-swap LLM response | mitigate | Strict zod parse; reject if action ∉ {swap,remove} or replacement_citekey ∉ assigned_sources (no new citekeys) |
| T-04-15 | Elevation of privilege | approval-gate bypass | mitigate | Gate default-on (PRD §19); only `--yolo` (explicit user intent) skips; non-TTY exits code 3 unless --yolo |
| T-04-16 | Tampering | --yolo unbounded auto-loop | mitigate | Retry cap = 2 (D-06); RETRY_EXHAUSTED then human-required — no unbounded redrafting |
| T-04-17 | Tampering | cross-section disturbance via --research | mitigate | --research writes only project RESEARCH.md/bib + sections/<N>/RESEARCH-LOG.md; sibling-mtime test guards isolation (PLAN-03) |
| T-04-18 | Tampering | prompt drift | mitigate | revise-swap.md hash-pinned in EXPECTED_PROMPT_HASHES; loadPrompt re-validates at runtime (WN-3 lockstep with repo-files test) |
| T-04-SC | Tampering | cassettes / npm installs | mitigate | No new deps; cassettes git-committed, scrubbed, size-capped; CI `npm run check` runs them |
</threat_model>

<verification>
- `node --import tsx --test tests/revise-swap.test.ts tests/repo-files.test.ts tests/tier-contract.test.ts` green.
- `loadPrompt('revise-swap')` succeeds without the pending-hash env bypass.
- `npm run build`, `tsc --noEmit`, `npm run lint` clean.
</verification>

<success_criteria>
- WRTE-02 / D-06: single `bin/lib/revise.ts` chokepoint backs both tiers; voice-hint consume point present.
- PLAN-02: `--revise` swaps a flagged citation and resets the verification hash behind the approval gate.
- PLAN-03: `--research <query>` appends section-scoped without disturbing other sections.
- RSCH-10: re-verify after swap uses the freshness-aware Pass 1 from Plan 02.
- Approval gate default-on; `--yolo` cap-2 auto-loop → RETRY_EXHAUSTED.
- CONTRIBUTING.md D-24: workflows/revise.md tier-contract entry registered in this plan.
</success_criteria>

<output>
Create `.planning/phases/04-breadth-n-sections-compile-wave-scheduling/04-04-SUMMARY.md` when done.
</output>
