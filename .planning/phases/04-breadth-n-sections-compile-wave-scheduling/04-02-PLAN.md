---
phase: 04-breadth-n-sections-compile-wave-scheduling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/verify/freshness.ts
  - bin/lib/verify/pass1.ts
  - bin/lib/compile-report.ts
  - bin/lib/schemas/compile-report.ts
  - bin/lib/paths.ts
  - tests/freshness-probe.test.ts
  - tests/compile-report-schema.test.ts
  - tests/letter-suffix-paths.test.ts
  - tests/cassettes/doi-head-ok.json
  - tests/cassettes/doi-head-404.json
  - tests/cassettes/retraction-watch-hit.json
autonomous: true
requirements: [RSCH-10, ARCH-20]
must_haves:
  truths:
    - "A stale DOI (HEAD 4xx/5xx after retry) emits a WARN in VERIFICATION.md and does NOT block verification"
    - "A retraction-watch hit emits a WARN and does NOT block"
    - "Network transport errors (ECONNREFUSED/ETIMEDOUT) are silent, not WARN — they are noise, not staleness"
    - "renderCompileReport emits frontmatter (schema v1, D-14 reserved keys) plus exactly the 5 D-14 body sections in fixed order"
    - "Path-walking code tolerates existing 03b-style letter-suffix directory names without error and rejects path-traversal inputs"
    - "sectionDir tolerates an optional letterSuffix without changing existing 3-arg callers"
  artifacts:
    - path: "bin/lib/verify/freshness.ts"
      provides: "probeFreshness(citekey, doi) — DOI HEAD + retraction-watch, WARN-only"
      exports: ["probeFreshness", "FreshnessResult"]
    - path: "bin/lib/compile-report.ts"
      provides: "renderCompileReport — deterministic markdown renderer for COMPILE-REPORT.md schema v1 (D-14 locked layout)"
      exports: ["renderCompileReport"]
    - path: "bin/lib/schemas/compile-report.ts"
      provides: "CompileReportSchema — zod frontmatter schema, schema_version literal 1, D-14 reserved keys"
      exports: ["CompileReportSchema"]
    - path: "bin/lib/paths.ts"
      provides: "sectionDir letterSuffix option + parseSectionDirName defensive parser"
      exports: ["sectionDir", "parseSectionDirName"]
  key_links:
    - from: "bin/lib/verify/pass1.ts"
      to: "bin/lib/verify/freshness.ts::probeFreshness"
      via: "post-verdict freshness probe per resolved citekey"
      pattern: "probeFreshness"
    - from: "bin/lib/verify/freshness.ts"
      to: "bin/lib/http.ts"
      via: "HEAD request through the http chokepoint"
      pattern: "http"
---

<objective>
Ship the three Phase 4 utility surfaces that have no dependency on the scheduler or compile pipeline: (1) the RSCH-10 source-freshness probe wired into Pass 1 as WARN-only, (2) the deterministic COMPILE-REPORT.md renderer + its zod frontmatter schema (consumed by Plan 05) following the CONTEXT.md D-14 LOCKED schema, and (3) letter-suffix path tolerance (`parseSectionDirName` + optional `letterSuffix` on `sectionDir`) so future inserted sections (`03b-...`) never break path-walking code.

Purpose: RSCH-10 (auto-recheck source freshness, advisory), ARCH-20 (path scheme reservation for letter-suffix). The COMPILE-REPORT renderer/schema is supporting infrastructure for Plan 05's COMP-07 emission — it is NOT itself a canonical COMP requirement (COMP IDs are anchored in Plan 05). Independent of Plan 01; both run in Wave 1.
Output: `bin/lib/verify/freshness.ts`, patched `pass1.ts`, `bin/lib/compile-report.ts`, `bin/lib/schemas/compile-report.ts`, patched `paths.ts`, 3 test files, 3 cassettes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-breadth-n-sections-compile-wave-scheduling/04-CONTEXT.md
@.planning/phases/04-breadth-n-sections-compile-wave-scheduling/04-RESEARCH.md
@.planning/phases/04-breadth-n-sections-compile-wave-scheduling/04-PATTERNS.md
@.planning/phases/04-breadth-n-sections-compile-wave-scheduling/04-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Extracted contracts — use directly. -->

<!-- SOURCE PRECEDENCE (revision ruling): 04-CONTEXT.md <decisions> D-14 is the
     LOCKED source of truth for the COMPILE-REPORT schema. 04-RESEARCH.md DRIFTED
     and unilaterally renamed the body sections and frontmatter keys — RESEARCH is
     WRONG where it conflicts. Use the D-14 schema below verbatim. -->

COMPILE-REPORT.md schema v1 (04-CONTEXT.md D-14 — LOCKED, authoritative):
  Frontmatter (reserved keys, EXACTLY these):
    schema_version: 1
    compiled_at: <ISO-8601>
    sections_count: <N>
    stale_resolved_count: <int>
    refuse_reasons: []        # empty array on success
    title: ''                 # Pandoc-reserved; Phase 4 writes empty string
    author: ''                # Pandoc-reserved; Phase 4 writes empty string
    abstract: ''              # Pandoc-reserved; Phase 4 writes empty string
  (Do NOT add RESEARCH.md's `outline_hash` / `pandoc_target` — they are NOT in the
   D-14 reserved-key set. If a future phase genuinely needs them they go in a
   non-reserved slot, not the v1 frontmatter.)
  Body — 5 sections, FIXED ORDER (04-CONTEXT.md D-14):
    1. ## Transitions Changed
    2. ## Cross-Section Consistency Flags
    3. ## Citation Density
    4. ## Compile-Staleness Resolved
    5. ## Advisory Findings
       (Phase 4 writes the header + the explicit empty marker
        `_No advisory passes ran — Phase 5 will populate._`)

RSCH-10 freshness output (04-RESEARCH §J) — VERIFICATION.md section:
  ## Source Freshness (RSCH-10)
  | Citekey | Probe | Status | Detail |  (rows per source; WARN-only)

From bin/lib/paths.ts (current — patch target): sectionDir(n, slug) hardcodes
`${pad2(n)}-${validateSlug(slug)}`; pad2 validates integer in [0,99]; validateSlug
enforces `/^[a-z0-9-]+$/`.

Reuse: bin/lib/http.ts (HEAD via undici chokepoint), bin/lib/sources/retraction-watch.ts
(fetchById, cassette-backed), bin/lib/budget.ts Semaphore (Semaphore(5) for HEAD fan-out),
bin/lib/schemas/handoff.ts (literal schema_version + .refine pattern to mirror).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — failing tests + cassettes for freshness, report schema, paths</name>
  <files>tests/cassettes/doi-head-ok.json, tests/cassettes/doi-head-404.json, tests/cassettes/retraction-watch-hit.json, tests/freshness-probe.test.ts, tests/compile-report-schema.test.ts, tests/letter-suffix-paths.test.ts</files>
  <read_first>
    - bin/lib/sources/retraction-watch.ts (cassette-backed DOI-keyed cross-check; warn-on-failure semantics; cassette fallback lines ~94-114) — VERIFY whether this is a REAL adapter or a Phase-3 stub (RESEARCH §J risk A3); the freshness test must reflect whichever it is.
    - bin/lib/http-mock.ts (nock-compat cassette schema lines ~32-100; sensitive-header scrub ~43-51)
    - tests/doi.test.ts (DOI-keyed probe test pattern)
    - tests/paths.test.ts (existing sectionDir test pattern)
    - tests/schemas.test.ts (zod parse + validate pattern)
    - tests/cassette-size.test.ts (≤50KB ceiling) and tests/cassette-no-leak.test.ts (header scrub)
    - 04-RESEARCH §J (freshness), §K (letter-suffix paths) — but use 04-CONTEXT.md D-14 (LOCKED) for the report schema, NOT RESEARCH's §F titles.
    - 04-VALIDATION.md rows RSCH-10, D-15
  </read_first>
  <action>
    Create three nock-compatible cassettes under `tests/cassettes/` matching the `http-mock.ts` schema (`{ scope, method, path, status, response, responseHeaders? }`), each ≤2KB, with NO Authorization/x-api-key headers (the scrub in http-mock.ts:43-51 must keep them clean):
    - `doi-head-ok.json` — HEAD `doi.org/<doi>` → 200.
    - `doi-head-404.json` — HEAD `doi.org/<doi>` → 404.
    - `retraction-watch-hit.json` — retraction-watch lookup returning a retracted record.

    Create three failing test files (RED):
    - `tests/freshness-probe.test.ts` (RSCH-10) — three cases via cassettes: DOI HEAD 200 → no WARN; DOI HEAD 404 → WARN row, no hard-fail (return value indicates advisory, not blocking); retraction-watch hit → WARN row. Assert a simulated transport error (ECONNREFUSED) produces NO WARN (silent). Do not assert on stderr ordering. If, after reading `retraction-watch.ts`, the adapter is a Phase-3 STUB (not a live HTTP adapter), the test must assert the stubbed behavior: the cassette reflects the stub and freshness.ts logs a DEBUG line for the stubbed retraction-watch branch rather than asserting a real network round-trip (RESEARCH §J risk A3).
    - `tests/compile-report-schema.test.ts` — `CompileReportSchema.parse` accepts a valid frontmatter object (schema_version 1, ISO compiled_at, sections_count int, stale_resolved_count int, refuse_reasons [], empty title/author/abstract) and REJECTS `schema_version: 2` AND rejects an object carrying `outline_hash`/`pandoc_target` (strict — those are NOT reserved keys); assert `renderCompileReport(input)` output body contains exactly 5 `## ` headers in the fixed D-14 order listed in `<interfaces>` (`## Transitions Changed`, `## Cross-Section Consistency Flags`, `## Citation Density`, `## Compile-Staleness Resolved`, `## Advisory Findings`), and that `## Advisory Findings` contains the empty marker `_No advisory passes ran — Phase 5 will populate._` when no advisory entries are supplied.
    - `tests/letter-suffix-paths.test.ts` (D-15) — `parseSectionDirName('03b-validity-threats')` → `{ n: 3, letterSuffix: 'b', slug: 'validity-threats' }`; `parseSectionDirName('03-intro')` → `{ n: 3, letterSuffix: undefined, slug: 'intro' }`; reject (`null` or throw) on `..`, an absolute path, and a string containing a null byte; assert lexicographic order `'03' < '03b' < '04'`; assert `sectionDir(3, 'foo', { letterSuffix: 'b' })` ends with `03b-foo` and the existing 3-arg `sectionDir(3, 'foo')` is unchanged.
  </action>
  <verify>
    <automated>node --import tsx --test tests/freshness-probe.test.ts tests/compile-report-schema.test.ts tests/letter-suffix-paths.test.ts 2>&1; if ($LASTEXITCODE -eq 0) { throw "Expected failing (RED) tests but the suite passed" }</automated>
  </verify>
  <acceptance_criteria>
    - Three cassettes exist under tests/cassettes/, each ≤2KB, with no sensitive headers.
    - Three RED test files exist referencing probeFreshness, CompileReportSchema, renderCompileReport, parseSectionDirName, sectionDir.
    - Suites fail because production code is absent (confirmed by non-zero exit — the verify command throws if the suite passes — not by a string grep).
    - The report-schema test asserts exactly 5 body `## ` headers in the fixed D-14 order from `<interfaces>` and the `## Advisory Findings` empty marker, and rejects `outline_hash`/`pandoc_target`.
    - The freshness test reflects whether retraction-watch.ts is a real adapter or a stub (RESEARCH §J risk A3).
  </acceptance_criteria>
  <done>RED tests + cassettes committed for all three utility surfaces; report schema asserts the D-14 locked layout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: freshness probe + Pass 1 wiring (RSCH-10, WARN-only)</name>
  <files>bin/lib/verify/freshness.ts, bin/lib/verify/pass1.ts</files>
  <read_first>
    - tests/freshness-probe.test.ts (RED spec)
    - bin/lib/verify/pass1.ts (runPass1 aggregator lines ~177-197 — the post-verdict extension point)
    - bin/lib/sources/retraction-watch.ts (fetchById; cassette fallback) — confirm REAL adapter vs Phase-3 stub; if a stub, the retraction-watch branch in freshness.ts is stubbed with a DEBUG log (RESEARCH §J risk A3)
    - bin/lib/http.ts (HEAD request chokepoint)
    - bin/lib/budget.ts (Semaphore — Semaphore(5) for HEAD fan-out)
    - bin/lib/doi.ts (DOI normalization/validation — validate format before HEAD, SSRF mitigation)
    - 04-CONTEXT.md D-10 (WARN-only policy); PRD §14 (hard-block reserved for FABRICATED/MIS-CITED/quote-NOT_FOUND)
  </read_first>
  <behavior>
    - probeFreshness: DOI HEAD 200 → ok; 4xx/5xx after one retry → warn; retraction-watch hit → warn; transport error → silent (no warn).
    - If retraction-watch.ts is a Phase-3 stub, the retraction-watch branch emits a DEBUG log and never fabricates a hit; the cassette reflects the stub.
    - Pass 1 appends a `freshness` field to its result and surfaces a `## Source Freshness (RSCH-10)` table in VERIFICATION.md; it NEVER escalates a freshness issue to FABRICATED/MIS-CITED.
  </behavior>
  <action>
    Create `bin/lib/verify/freshness.ts` exporting `probeFreshness(citekey: string, doi: string | null): Promise<FreshnessResult>` and the `FreshnessResult` type. Validate the DOI format via `bin/lib/doi.ts` BEFORE issuing any request (SSRF mitigation — HEAD goes only to `doi.org`, never an arbitrary URL). Issue the HEAD via `bin/lib/http.ts` (10s timeout, 1 retry via the existing retry pattern). Distinguish a real HTTP 4xx/5xx (→ WARN) from a transport error such as ECONNREFUSED/ETIMEDOUT (→ silent, optional DEBUG). Check retraction-watch via the existing `retraction-watch.ts` adapter; a hit → WARN. If retraction-watch.ts is a Phase-3 stub (no live HTTP), guard the retraction-watch branch with a DEBUG log noting the stub and do not assert a network result (RESEARCH §J risk A3). Use `new Semaphore(5)` if probing multiple sources concurrently. Persist nothing (results are in-memory for the run, per D-04 spirit).

    Patch `bin/lib/verify/pass1.ts`: after the main verdict loop in `runPass1`, invoke `probeFreshness` per resolved citekey, attach `freshness: FreshnessResult[]` to the `Pass1Result`, and emit the `## Source Freshness (RSCH-10)` table (format in `<interfaces>`) into the VERIFICATION.md output. Freshness verdicts are WARN-only — under NO circumstance do they change the blocking Pass 1 verdict (D-10 / PRD §14). All file writes route through `atomicWriteFile` (never `fs.writeFile`).
  </action>
  <verify>
    <automated>node --import tsx --test tests/freshness-probe.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/freshness-probe.test.ts passes (GREEN): 404 → WARN, retraction hit → WARN, transport error → silent.
    - `bin/lib/verify/freshness.ts` validates DOI format before any HEAD request (Grep shows a doi.ts call before the http call).
    - If retraction-watch.ts is a stub, freshness.ts logs a DEBUG line for the stubbed branch (Grep shows the DEBUG guard).
    - Patched `pass1.ts` never maps a freshness result to FABRICATED or MIS-CITED (the blocking verdict path is untouched — confirm existing Pass 1 blocking tests still pass).
    - `npm run lint` passes (HEAD request goes through bin/lib/http.ts, not a direct fetch/undici import).
  </acceptance_criteria>
  <done>Freshness probe wired into Pass 1 as advisory WARN-only; existing Pass 1 blocking behavior intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: COMPILE-REPORT schema + renderer (D-14 locked) + letter-suffix path tolerance</name>
  <files>bin/lib/schemas/compile-report.ts, bin/lib/compile-report.ts, bin/lib/paths.ts</files>
  <read_first>
    - tests/compile-report-schema.test.ts, tests/letter-suffix-paths.test.ts (RED specs)
    - bin/lib/schemas/handoff.ts (lines ~23-49 — literal schema_version + .refine + nested arrays pattern)
    - bin/cli/verify.ts (lines ~121-134 — template-literal deterministic narration pattern, no LLM)
    - bin/lib/paths.ts (lines ~172-183 — sectionDir; pad2; validateSlug — patch target)
    - 04-CONTEXT.md D-14 (LOCKED report schema — authoritative), 04-RESEARCH §K (parseSectionDirName) — ignore RESEARCH §F section titles; D-14 wins.
  </read_first>
  <behavior>
    - CompileReportSchema: strict, schema_version literal 1, datetime compiled_at, sections_count + stale_resolved_count ints, refuse_reasons array, Pandoc keys present (default empty), rejects schema_version 2 and unknown keys (including outline_hash/pandoc_target).
    - renderCompileReport: deterministic (no LLM), emits frontmatter + the 5 D-14 fixed-order body sections + the Advisory Findings empty marker.
    - parseSectionDirName: parses `NN[letter]-slug`, rejects traversal/absolute/null-byte; sectionDir gains optional letterSuffix, existing callers unchanged.
  </behavior>
  <action>
    Create `bin/lib/schemas/compile-report.ts` exporting `CompileReportSchema = z.object({ schema_version: z.literal(1), compiled_at: z.string().datetime(), sections_count: z.number().int().nonnegative(), stale_resolved_count: z.number().int().nonnegative(), refuse_reasons: z.array(z.string()).default([]), title: z.string().default(''), author: z.string().default(''), abstract: z.string().default('') })`. Strict-by-default — do NOT use `.passthrough()` or `.catchall` (ARCH-07 refuse-forward-incompat); this means an object carrying `outline_hash` or `pandoc_target` is REJECTED (those are RESEARCH-drift keys, not D-14 reserved keys). The Pandoc-reserved keys (`title`/`author`/`abstract`) MUST be present even when empty so Phase 6 export reads them directly.

    Create `bin/lib/compile-report.ts` exporting `renderCompileReport(input: CompileReportInput): string`. Build the YAML frontmatter from a `CompileReportSchema`-validated object, then emit exactly the 5 D-14 body sections in this fixed order: `## Transitions Changed`, `## Cross-Section Consistency Flags`, `## Citation Density`, `## Compile-Staleness Resolved`, `## Advisory Findings`. The `## Advisory Findings` section, when given no advisory entries (Phase 4 always), emits the explicit empty marker `_No advisory passes ran — Phase 5 will populate._`. Any other body section with no entries emits its own explicit empty marker. Deterministic template-literal narration only — NO LLM call (mirror `bin/cli/verify.ts` narration). Do NOT bump `schema_version` for additive future fields (D-14 additive-forward rule).

    Patch `bin/lib/paths.ts`: change `sectionDir` to accept an optional final argument `opts?: { letterSuffix?: string }` and emit `${pad2(n)}${opts.letterSuffix}-${validateSlug(slug)}` when `letterSuffix` is provided, else the existing `${pad2(n)}-${validateSlug(slug)}` (existing 3-arg callers unchanged — D-15: Phase 4 does NOT emit suffixed paths, only tolerates them). Add `parseSectionDirName(basename: string): { n: number; letterSuffix?: string; slug: string } | null` that parses `NN[letter]-slug`, validates the slug, and returns `null` (or throws — match the RED test) on `..`, absolute paths, or null bytes (V12 ASVS path-traversal mitigation).
  </action>
  <verify>
    <automated>node --import tsx --test tests/compile-report-schema.test.ts tests/letter-suffix-paths.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both test files pass (GREEN).
    - `CompileReportSchema` uses `z.literal(1)` for schema_version, carries the D-14 reserved keys (sections_count, stale_resolved_count, refuse_reasons, title/author/abstract), and rejects extra keys including `outline_hash`/`pandoc_target` (no passthrough).
    - `renderCompileReport` output has exactly the 5 D-14 `## ` headers in fixed order, includes the `## Advisory Findings` empty marker, and contains no LLM/network call (pure function).
    - `parseSectionDirName('03b-x')` → `{n:3, letterSuffix:'b', slug:'x'}`; `..`/absolute/null-byte inputs are rejected.
    - Existing `tests/paths.test.ts` still passes (no regression to 3-arg sectionDir callers).
    - `npm run lint` and `tsc --noEmit` pass.
  </acceptance_criteria>
  <done>Report schema + renderer (D-14 locked layout) + path tolerance green; existing path tests intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| citekey DOI → outbound HEAD | DOI value drives an outbound HTTPS request (SSRF surface) |
| directory basename → parseSectionDirName | Filesystem-derived name parsed into path components |
| report input object → YAML frontmatter | Compile pipeline data serialized to disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | SSRF / Spoofing | freshness DOI HEAD | mitigate | Validate DOI format via doi.ts before request; HEAD targets `doi.org` only, never an arbitrary URL; 10s timeout + 1 retry cap |
| T-04-06 | Tampering | parseSectionDirName basename | mitigate | Reject `..`, absolute paths, null bytes; slug validated via validateSlug (V12 ASVS) |
| T-04-07 | Information disclosure | freshness probe value leak | accept | Probe sends no request body; only emits status codes to VERIFICATION.md — no secret surface |
| T-04-08 | Tampering | COMPILE-REPORT forward-incompat | mitigate | `schema_version: z.literal(1)` + strict zod (no passthrough) refuses forward-incompat keys including outline_hash/pandoc_target (ARCH-07) |
| T-04-09 | Tampering | freshness escalated to a block | mitigate | Pass 1 blocking verdict path untouched; freshness is WARN-only by construction (D-10 / PRD §14) |
| T-04-SC | Tampering | cassettes / npm installs | mitigate | No new deps; cassettes git-committed, header-scrubbed, size-capped; CI `npm run check` runs them |
</threat_model>

<verification>
- `node --import tsx --test tests/freshness-probe.test.ts tests/compile-report-schema.test.ts tests/letter-suffix-paths.test.ts` green.
- Existing Pass 1 and paths tests still pass (no regression).
- `npm run lint`, `tsc --noEmit`, cassette-size and cassette-no-leak tests green.
</verification>

<success_criteria>
- RSCH-10: stale DOI / retraction hit → WARN in VERIFICATION.md, never blocks.
- COMPILE-REPORT infrastructure: `renderCompileReport` + `CompileReportSchema` produce a schema-v1 report with the D-14 LOCKED frontmatter keys + 5 body sections (consumed by Plan 05's COMP-07 emission).
- ARCH-20 (letter-suffix): `parseSectionDirName` + optional `sectionDir` letterSuffix tolerate `03b-...` directories with traversal protection.
</success_criteria>

<output>
Create `.planning/phases/04-breadth-n-sections-compile-wave-scheduling/04-02-SUMMARY.md` when done.
</output>
