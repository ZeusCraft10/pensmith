---
phase: 04-breadth-n-sections-compile-wave-scheduling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/outline-parse.ts
  - bin/lib/scheduler.ts
  - bin/lib/schemas/wave-graph.ts
  - bin/lib/citation-token.ts
  - bin/lib/schemas/plan-frontmatter.ts
  - tests/wave-scheduler.test.ts
  - tests/wave-override.test.ts
  - tests/scheduler-stateless.test.ts
  - tests/outline-parse.test.ts
  - tests/citation-token.test.ts
autonomous: true
requirements: [ARCH-19, ARCH-20, PLAN-02, PLAN-03, COMP-06]
must_haves:
  truths:
    - "parseOutline turns the on-disk .paper/OUTLINE.md into an ordered, slug-keyed section list"
    - "Kahn topological sort assigns each section computed_wave = max(deps.computed_wave) + 1; roots are wave 1 (COMP-06)"
    - "A PLAN.md wave override is honored when valid and REJECTED when it is less than max(deps.wave) + 1"
    - "runWave runs sibling sections concurrently under a Semaphore cap and never cancels a sibling on one failure"
    - "The scheduler writes nothing to disk — STATE.json mtime is unchanged across runs"
    - "A dependency cycle is detected and surfaced (never silently accepted)"
  artifacts:
    - path: "bin/lib/outline-parse.ts"
      provides: "parseOutline(raw: string): ParsedOutline — pure string→object parser"
      exports: ["parseOutline", "ParsedOutline", "ParsedOutlineSection"]
    - path: "bin/lib/scheduler.ts"
      provides: "buildWaveGraph + runWave — Kahn sort (COMP-06), override validation, bounded-parallel wave execution"
      exports: ["buildWaveGraph", "runWave"]
    - path: "bin/lib/schemas/wave-graph.ts"
      provides: "zod SectionNode + WaveGraph schemas (in-memory only, strict)"
      exports: ["SectionNodeSchema", "WaveGraphSchema"]
    - path: "bin/lib/citation-token.ts"
      provides: "CITATION_TOKEN_RE + extractCitekeys + replaceCitekeys shared helpers"
      exports: ["CITATION_TOKEN_RE", "extractCitekeys", "replaceCitekeys"]
  key_links:
    - from: "bin/lib/scheduler.ts"
      to: "bin/lib/budget.ts::Semaphore"
      via: "import + new Semaphore(maxParallel)"
      pattern: "Semaphore"
    - from: "bin/lib/scheduler.ts"
      to: "bin/lib/outline-parse.ts::parseOutline"
      via: "graph construction from parsed outline"
      pattern: "parseOutline"
---

<objective>
Build the read-only wave scheduler: parse the on-disk outline into an ordered section graph, compute waves via Kahn topological sort honoring optional per-section `wave:` overrides, and execute siblings in bounded parallel via the existing `Semaphore`. Also extract the shared citation-token helpers (regex + extract + replace) that the compile smoother (Plan 05) depends on, and add the optional `wave` field to the PLAN.md frontmatter schema.

Purpose: ARCH-19 (bounded-parallel wave primitive), ARCH-20 (no on-disk scheduler state), PLAN-02 (wave override honored), PLAN-03 (override validated against deps), COMP-06 (`computeWaves()` — the Kahn topological sort that orders sections by `depends_on` for parallel section writing in Tier 1; this plan IS the canonical home of COMP-06). This plan unblocks Plan 03 (write orchestration) and Plan 05 (compile pipeline).
Output: `bin/lib/outline-parse.ts`, `bin/lib/scheduler.ts`, `bin/lib/schemas/wave-graph.ts`, `bin/lib/citation-token.ts`, patched `plan-frontmatter.ts`, and 5 test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-breadth-n-sections-compile-wave-scheduling/04-CONTEXT.md
@.planning/phases/04-breadth-n-sections-compile-wave-scheduling/04-RESEARCH.md
@.planning/phases/04-breadth-n-sections-compile-wave-scheduling/04-PATTERNS.md
@.planning/phases/04-breadth-n-sections-compile-wave-scheduling/04-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — use directly, no exploration needed. -->

<!-- SOURCE PRECEDENCE (revision ruling): REQUIREMENTS.md (canonical) and
     04-CONTEXT.md <decisions> (locked) OUTRANK 04-RESEARCH.md wherever they
     conflict. COMP-06 = "Wave scheduling (computeWaves()) topologically sorts by
     depends_on for parallel section writing in Tier 1" per REQUIREMENTS.md. -->

From bin/lib/budget.ts (line 166) — REUSE, do not add p-limit/p-map:
```typescript
export class Semaphore {
  constructor(maxConcurrency: number); // throws if not a positive integer
  async acquire(): Promise<void>;
  release(): void;
  async withLock<T>(fn: () => Promise<T>): Promise<T>; // pairs acquire/release in try/finally
}
```

From bin/lib/schemas/plan-frontmatter.ts (current shape — patch target):
```typescript
export const PlanFrontmatterSchema = z.object({
  section: z.number().int().min(1),
  slug: z.string().regex(/^[a-z0-9-]+$/),
  title: z.string(),
  depends_on: z.array(z.string().regex(SLUG)).default([]),
  assigned_sources: z.array(z.string()).default([]),
  verified_against_draft_hash: z.string().nullable().default(null),
  status: z.enum(['planned','writing','written','verifying','verified','failed','unverifiable']).default('planned'),
  last_verification: z.unknown().optional(),
  was_current_at_migration: z.boolean().optional(),
}).refine((p) => !p.depends_on.includes(p.slug), { message: '...no-self-ref...' });
```

From bin/lib/verify/pass1.ts (line 187) and bin/lib/citekey.ts (line 25) — REUSE verbatim:
```typescript
// pass1.ts citation token extraction regex:
/\[@([a-z][a-z0-9_-]*)\]/g
// citekey.ts grammar:
export const CITEKEY_RE = /^[a-z][a-z0-9_-]*$/;
```

From templates/prompts/outline-author.md — the LLM emits section objects shaped
`{ number, slug, title, depends_on: slug[], estimated_word_count }`. The slug example
is `02-attention-mechanism` (the number prefix is part of the slug string).
NOTE: PlanFrontmatterSchema's field is `section` (not `number`). The on-disk
.paper/OUTLINE.md is "human-readable Markdown" persisted by workflows/outline.md
step 5 as a table `| # | slug | title | depends_on | word target | assigned_sources |`.
The exact persisted byte format is NOT fully pinned — Task 1 below MUST read the
actual persisted format before locking the parser regex.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — failing tests + lock the OUTLINE.md parse contract</name>
  <files>tests/outline-parse.test.ts, tests/citation-token.test.ts, tests/wave-scheduler.test.ts, tests/wave-override.test.ts, tests/scheduler-stateless.test.ts</files>
  <read_first>
    - workflows/outline.md (the persisted .paper/OUTLINE.md format — table at step 5)
    - templates/prompts/outline-author.md (the section object shape the LLM emits)
    - bin/lib/outline.ts (existing raw loader — parser must NOT duplicate fs I/O)
    - tests/schemas.test.ts (zod parse + refine assertion pattern)
    - tests/budget.test.ts (Semaphore unit-test pattern — node:test, no nock, no sleeps)
    - tests/handoff.test.ts (fixture .paper/ dir + after-run mtime check pattern)
    - .planning/phases/04-breadth-n-sections-compile-wave-scheduling/04-VALIDATION.md (Per-Task Verification Map rows ARCH-19/ARCH-20/PLAN-02/PLAN-03/COMP-06)
  </read_first>
  <action>
    First, determine the canonical on-disk `.paper/OUTLINE.md` format. Read `workflows/outline.md` step 5 and search the repo (Grep) for any existing OUTLINE.md fixture under `tests/`. If a fixture exists, lock the parser against it; if not, the persisted format is the markdown table `| # | slug | title | depends_on | word target | assigned_sources |` defined in `workflows/outline.md`. Record the chosen format as a comment block at the top of `tests/outline-parse.test.ts` so the parser and tests agree on one source of truth.

    Create five failing test files (RED) covering this plan's requirement IDs, using `node:test` + `import assert from 'node:assert/strict'`:
    - `tests/outline-parse.test.ts` — `parseOutline(raw)` returns sections in appearance order; each has `n` (number), `slug`, `title`, `depends_on` (string[]); a malformed line throws an error whose message names the offending line. No fs I/O.
    - `tests/citation-token.test.ts` — `extractCitekeys('text [@smith2020] and [@jones-2019]')` returns `['smith2020','jones-2019']` deduped; `replaceCitekeys(md, fn)` swaps each `[@key]` via the callback; assert the placeholder family `{{cite_K_M}}` does NOT match `CITATION_TOKEN_RE`.
    - `tests/wave-scheduler.test.ts` (ARCH-19, COMP-06) — `runWave(nodes, sem)` with a synthetic node list and `new Semaphore(2)` resolves all nodes; assert at most 2 run concurrently (track a live counter, no real sleeps — use `Promise.resolve()` ticks); a node that rejects does NOT prevent sibling nodes from resolving (D-03). Add a COMP-06 case: `buildWaveGraph` on a fixture where `depends_on` order ≠ appearance order assigns `computed_wave` by topological depth (root = wave 1, dependent = wave 2), proving the Kahn sort orders by `depends_on`, not by file order.
    - `tests/wave-override.test.ts` (PLAN-02, PLAN-03) — suite "honors override": a section declaring `wave: 3` whose deps sit in wave 1 gets `computed_wave === 3`. Suite name including the literal token `reject` (so `-t reject` selects it): `buildWaveGraph` THROWS when a section declares `wave: 1` but a dep is in wave 2 (`wave < max(deps.computed_wave)+1`).
    - `tests/scheduler-stateless.test.ts` (ARCH-20) — seed a fixture `.paper/STATE.json`, run `buildWaveGraph` + `runWave`, assert the STATE.json mtime is byte-for-byte unchanged after the run (scheduler persists nothing). Use one `mkdtempSync` per test; no racing cleanup hooks.

    Each test imports from the not-yet-created modules so the suite fails to load (acceptable RED). Do not stub Kahn by re-implementing it inside the test — use fixture graphs with hand-computed expected waves.
  </action>
  <verify>
    <automated>node --import tsx --test tests/outline-parse.test.ts tests/citation-token.test.ts tests/wave-scheduler.test.ts tests/wave-override.test.ts tests/scheduler-stateless.test.ts 2>&1; if ($LASTEXITCODE -eq 0) { throw "Expected failing (RED) tests but the suite passed" }</automated>
  </verify>
  <acceptance_criteria>
    - All five test files exist under tests/ and reference the exported symbols listed in this plan's must_haves.artifacts.
    - Running the five files fails (RED) because the production modules do not yet exist — confirmed by a non-zero exit (the verify command throws if the suite unexpectedly passes), not by a string grep.
    - tests/outline-parse.test.ts contains a header comment recording the exact on-disk OUTLINE.md format the parser targets, derived from workflows/outline.md (or a discovered fixture).
    - tests/wave-override.test.ts has a suite whose name contains "reject" so `-t reject` selects the PLAN-03 case.
    - tests/wave-scheduler.test.ts contains a COMP-06 topological-depth case proving Kahn orders by depends_on.
    - No test uses real timeouts/sleeps; concurrency is observed via a synchronous live-counter.
  </acceptance_criteria>
  <done>Five RED test files committed; OUTLINE.md parse format locked in a test header comment; COMP-06 topological-sort case present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: parseOutline + citation-token helpers + plan-frontmatter wave field</name>
  <files>bin/lib/citation-token.ts, bin/lib/outline-parse.ts, bin/lib/schemas/plan-frontmatter.ts</files>
  <read_first>
    - tests/outline-parse.test.ts, tests/citation-token.test.ts (RED specs from Task 1)
    - bin/lib/outline.ts (raw loader — parser is pure, no fs)
    - bin/lib/citekey.ts (CITEKEY_RE grammar; pure-helper module shape; invariant-throw pattern at lines ~56-60)
    - bin/lib/verify/pass1.ts (around line 187 — the `/\[@([a-z][a-z0-9_-]*)\]/g` extraction matchAll loop to factor out)
    - bin/lib/schemas/plan-frontmatter.ts (lines 31-56 — patch target)
    - bin/lib/paths.ts (validateSlug — reuse for slug validation in parseOutline)
  </read_first>
  <behavior>
    - parseOutline: ordered sections, slug-validated, throws "couldn't parse line N" on malformed entries; pure string-in/object-out.
    - extractCitekeys returns deduped citekeys in first-appearance order; replaceCitekeys round-trips every `[@key]`.
    - PlanFrontmatterSchema gains optional `wave` (positive int, no default); existing fixtures with no `wave` still parse.
  </behavior>
  <action>
    Create `bin/lib/citation-token.ts` exporting `CITATION_TOKEN_RE = /\[@([a-z][a-z0-9_-]*)\]/g`, `extractCitekeys(md: string): string[]` (dedup via `new Set`, preserve first-appearance order), and `replaceCitekeys(md: string, fn: (key: string) => string): string`. Do NOT extend the regex to support Pandoc locator syntax (`[@key, p. 23]`) — bare `[@key]` only (Phase 10 scope). The placeholder family `{{cite_K_M}}` is disjoint from the regex by construction; do not special-case it.

    Create `bin/lib/outline-parse.ts` exporting interfaces `ParsedOutlineSection { n: number; slug: string; title: string; estimated_word_count?: number; depends_on: string[] }` and `ParsedOutline { paper_title: string; sections: ParsedOutlineSection[] }`, and `parseOutline(raw: string): ParsedOutline`. The parser is PURE (no fs I/O — `outline.ts` owns reading). Validate each slug via `paths.ts` `validateSlug`. On a malformed entry, throw an Error naming the line ("outline-parse: couldn't parse line N: <text>"). Mirror the strict invariant-throw style of `generateCitekey` in citekey.ts. Match the on-disk format locked in Task 1's test header.

    Patch `bin/lib/schemas/plan-frontmatter.ts`: add `wave: z.number().int().positive().optional()` between `assigned_sources` and `verified_against_draft_hash`. Do NOT make it required and do NOT add a default (`undefined` means "compute via Kahn"). Keep the existing `.refine` no-self-ref invariant unchanged.
  </action>
  <verify>
    <automated>node --import tsx --test tests/outline-parse.test.ts tests/citation-token.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/outline-parse.test.ts and tests/citation-token.test.ts pass (GREEN).
    - `bin/lib/citation-token.ts` exports CITATION_TOKEN_RE, extractCitekeys, replaceCitekeys; the regex literal is exactly `/\[@([a-z][a-z0-9_-]*)\]/g` (no locator-syntax extension).
    - `bin/lib/outline-parse.ts` does NOT import node:fs or node:fs/promises (Grep confirms zero fs imports).
    - PlanFrontmatterSchema parses a fixture WITH `wave: 3` and a fixture WITHOUT `wave` (both valid); a fixture with `wave: 0` or `wave: -1` fails.
    - `npm run lint` passes (no direct fetch/fs chokepoint violations introduced).
  </acceptance_criteria>
  <done>parseOutline, citation-token helpers, and the optional wave field all green and lint-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: scheduler — Kahn sort (COMP-06), override validation, bounded-parallel runWave</name>
  <files>bin/lib/schemas/wave-graph.ts, bin/lib/scheduler.ts</files>
  <read_first>
    - tests/wave-scheduler.test.ts, tests/wave-override.test.ts, tests/scheduler-stateless.test.ts (RED specs)
    - bin/lib/budget.ts (lines 150-210 — Semaphore API)
    - bin/lib/handoff.ts (assembleHandoff lines ~43-54 — input-shape → validated output pattern)
    - bin/lib/outline-parse.ts (Task 2 output — parseOutline)
    - bin/lib/schemas/plan-frontmatter.ts (patched — depends_on + optional wave)
    - bin/lib/state.ts (PLAN.md frontmatter read — scheduler reads section state READ-ONLY)
    - 04-RESEARCH.md §A (runWave code block) and §B (WaveGraph data structure) — NOTE: where RESEARCH conflicts with REQUIREMENTS/CONTEXT, the canonical/locked sources win.
    - 04-CONTEXT.md D-01 (Kahn + override-reject), D-02 (Semaphore cap), D-03 (within-wave failure), D-04 (read-only statefulness)
  </read_first>
  <behavior>
    - buildWaveGraph: roots get computed_wave 1; each node computed_wave = max(deps.computed_wave)+1 (COMP-06 topological sort by depends_on); valid wave override promotes computed_wave; invalid override (< max(deps)+1) throws; cycle throws with the residual slug list.
    - runWave: executes nodes via sem.withLock; Promise.allSettled so one rejection does not cancel siblings; returns per-node settled status.
    - No disk writes anywhere in the module.
  </behavior>
  <action>
    Create `bin/lib/schemas/wave-graph.ts` exporting `SectionNodeSchema` (fields per 04-RESEARCH §B: `n`, `slug`, `title`, `depends_on: string[]`, `wave_override?: number`, `computed_wave: number`, `status: z.enum(['pending','in_flight','done','failed','blocked'])`) and `WaveGraphSchema`. Strict-by-default (no `.passthrough()`, no `.catchall`); these are in-memory dev-typo guards, never serialized — do NOT add a `schema_version` envelope.

    Create `bin/lib/scheduler.ts` exporting `buildWaveGraph(outline: ParsedOutline, plans: Map<slug, PlanFrontmatter>): WaveGraph` (this is the canonical `computeWaves()` of COMP-06) and `runWave(nodes: SectionNode[], sem: Semaphore, run: (node) => Promise<unknown>): Promise<PromiseSettledResult<unknown>[]>`.
    - buildWaveGraph: build `nodes: Map<string, SectionNode>` keyed by slug; run Kahn's algorithm to assign `computed_wave = max(deps.computed_wave) + 1` (roots = wave 1) — topologically sorting by `depends_on` (COMP-06). For each section with a `wave_override` (the patched PLAN.md `wave` field), validate `wave_override >= max(deps.computed_wave) + 1` and THROW a structured error (naming the section slug and the violated bound) if not (D-01 / PLAN-03); if valid and greater than the Kahn depth, promote `computed_wave` to the override (PLAN-02). After Kahn, run an explicit cycle-detect: if `nodes.size !== sum(waves[].length)`, throw naming the unprocessed slugs (04-RESEARCH §P pitfall 1). Skip any section whose PLAN.md is absent (treat as "not yet planned", INFO not error).
    - runWave: `Promise.allSettled(nodes.map(node => sem.withLock(() => run(node))))`. Wrap each `run(node)` call so a thrown non-Error normalizes to `{ slug, error }` before allSettled sees it (04-RESEARCH §P pitfall 5). Do NOT nest withLock calls (pitfall 4).
    - The module reads PLAN.md frontmatter via `bin/lib/state.ts` helpers only; it MUST NOT write to STATE.json or any wave-progress file (D-04 / ARCH-20). Reuse the exported `Semaphore` from budget.ts; do NOT add any new dependency.
  </action>
  <verify>
    <automated>node --import tsx --test tests/wave-scheduler.test.ts tests/wave-override.test.ts tests/scheduler-stateless.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All three scheduler test files pass (GREEN), including the `-t reject` PLAN-03 case and the COMP-06 topological-depth case.
    - `bin/lib/scheduler.ts` imports `Semaphore` from `./budget.js` (or re-export) and adds NO new package dependency (Grep: no p-limit, no p-map).
    - `bin/lib/scheduler.ts` contains no `atomicWriteFile`, no `fs.writeFile`, no STATE.json write (Grep confirms scheduler is read-only).
    - `bin/lib/scheduler.ts` contains no `parseInt(` or `Number(` applied to a directory basename (04-RESEARCH §K — section discovery is via parsed outline, not directory parsing).
    - A cycle in a fixture graph throws an error whose message lists the unprocessed slugs.
    - `npm run lint` and `tsc --noEmit` pass.
  </acceptance_criteria>
  <done>Scheduler computes correct waves (COMP-06 Kahn sort), validates overrides, runs siblings in bounded parallel, persists nothing; all three suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OUTLINE.md / PLAN.md frontmatter → scheduler | User/LLM-authored YAML and markdown parsed into the in-memory graph |
| slug strings → graph keys | Slugs become Map keys and (downstream) filesystem path segments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | parseOutline slug field | mitigate | Validate every slug via `paths.ts` validateSlug (`/^[a-z0-9-]+$/`); reject `..`, absolute paths, null bytes by construction (V12 ASVS) |
| T-04-02 | Denial of Service | buildWaveGraph cycle | mitigate | Explicit cycle-detect after Kahn; throw with residual slug list instead of looping forever (04-RESEARCH §P-1) |
| T-04-03 | Tampering | PLAN.md `wave` override | mitigate | zod `z.number().int().positive()`; runtime validation rejects `wave < max(deps.wave)+1` (PLAN-03) |
| T-04-04 | Repudiation | scheduler statefulness | accept | Scheduler is read-only (D-04); no audit trail needed because it mutates nothing |
| T-04-SC | Tampering | npm installs | mitigate | Zero new deps added (D-15); reuse in-repo Semaphore — no install surface in this plan |
</threat_model>

<verification>
- `node --import tsx --test tests/outline-parse.test.ts tests/citation-token.test.ts tests/wave-scheduler.test.ts tests/wave-override.test.ts tests/scheduler-stateless.test.ts` all green.
- `npm run lint` and `tsc --noEmit` clean.
- No new runtime dependency in package.json.
</verification>

<success_criteria>
- ARCH-19: `runWave` honors `--max-parallel` via Semaphore (bounded concurrency observed in test).
- ARCH-20: scheduler-stateless test proves STATE.json mtime unchanged across a run.
- COMP-06: `buildWaveGraph` (computeWaves) topologically sorts sections by `depends_on` (Kahn), proven by the topological-depth test case.
- PLAN-02: a valid `wave:` override is honored.
- PLAN-03: an invalid `wave:` override is rejected at graph-build time.
- Shared `citation-token.ts` helpers exist for Plan 05's smoother substitution.
</success_criteria>

<output>
Create `.planning/phases/04-breadth-n-sections-compile-wave-scheduling/04-01-SUMMARY.md` when done.
</output>
