---
phase: 07-single-command-ux-layer-hooks-flags
plan: 04
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - skills/pensmith.md
  - skills/plan-section.md
  - skills/write-section.md
  - skills/verify-section.md
  - .claude-plugin/plugin.json
  - tests/tier-contract.test.ts
autonomous: false
requirements: [UX-02, UX-03, UX-04, UX-05]
user_setup: []
must_haves:
  truths:
    - "Natural-language triggers ('redo section 3', 'make it sound less AI', 'where am I?') route to the correct skill via skill descriptions"
    - "The hidden plumbing namespace (/pensmith:plan-section, /pensmith:write-section, /pensmith:verify-section) is registered for scripting"
    - "Inline conversational corrections (length change, add/drop section, swap source, redo) map to the EXISTING revise/plan verb paths — no 17th verb"
    - "Verb shortcuts produce equivalent output in BOTH tiers (tier-contract parity extended for the new verb surface)"
  artifacts:
    - path: "skills/pensmith.md"
      provides: "primary NL-routing skill with PRD §5.4 trigger phrases"
      contains: "where am I"
    - path: "skills/plan-section.md"
      provides: "plumbing skill for /pensmith:plan-section"
      contains: "plan section"
    - path: "skills/write-section.md"
      provides: "plumbing skill for /pensmith:write-section"
      contains: "write section"
    - path: "skills/verify-section.md"
      provides: "plumbing skill for /pensmith:verify-section"
      contains: "verify section"
    - path: ".claude-plugin/plugin.json"
      provides: "skills array registering the colon-prefix plumbing namespace"
      contains: "pensmith:plan-section"
    - path: "tests/tier-contract.test.ts"
      provides: "verb-shortcut + plumbing-namespace parity case"
      contains: "tier"
  key_links:
    - from: "skills/pensmith.md"
      to: "bin/lib/router.ts"
      via: "skill body delegates bare /pensmith to the same resolveNextAction routing"
      pattern: "resolveNextAction"
    - from: ".claude-plugin/plugin.json"
      to: "skills/plan-section.md"
      via: "skills array { name: 'pensmith:plan-section', file: 'skills/plan-section.md' }"
      pattern: "plan-section"
    - from: "skills/plan-section.md"
      to: "workflows/plan.md"
      via: "plumbing skill invokes the existing plan verb / plan --revise path"
      pattern: "plan"
---

<objective>
Write the natural-language routing skills (primary + plumbing namespace), register the colon-prefix plumbing namespace in plugin.json, and extend the tier-contract test to assert verb-shortcut + plumbing-namespace parity across both tiers.

Purpose: UX-03/04/05 are Tier-1 model-routing features — the skill DESCRIPTIONS are the implementation (no code routes NL phrases; the model selects a skill by matching its description against the user message). Inline corrections (redo/swap-source/length/add-drop) map to the EXISTING plan/revise code paths (per 04-04: revise ships via `plan --revise`, NOT a 17th verb). The 16-verb bijection stays intact.
Output: 4 skill markdown files, an updated plugin.json skills array, and an extended tier-contract case.

Per project CLAUDE.md non-negotiables: SINGLE-COMMAND UX (/pensmith is the only README quick-start command; verb shortcuts + plumbing namespace are power-user fallbacks) and TWO-TIER parity (the new verb surface works in both tiers).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-single-command-ux-layer-hooks-flags/07-RESEARCH.md
@.planning/phases/07-single-command-ux-layer-hooks-flags/07-PATTERNS.md
@.planning/phases/07-single-command-ux-layer-hooks-flags/07-02-SUMMARY.md
@./CLAUDE.md

<interfaces>
<!-- Contracts to satisfy. The 07-01 RED tests assert exactly these. -->

From bin/lib/verbs.ts (the locked 16 — plumbing skills map onto these, no 17th):
```typescript
export const UX02_VERBS = ['doctor','new','next','status','research','outline',
  'plan','write','verify','compile','done','resume','list','open','sketch','add'] as const;
```

Existing .claude-plugin/plugin.json shape (add a `skills` array alongside mcpServers):
```json
{ "name": "pensmith", "version": "0.1.0-dev", "description": "...", "mcpServers": { ... } }
```

PRD §5.4 NL trigger mapping (the description strings must contain these phrases verbatim):
- "redo section N" / "section N needs work"  → plan + write for section N (via plan --revise)
- "make it sound less AI"                    → humanize (folded under done)
- "where am I?" / "what's next?"             → status / bare /pensmith routing
- "resume" / "continue where I left off"     → resume

Skill description anti-pattern (Pitfall 7): vague descriptions ("manages sections") route nothing — the description MUST contain the exact user phrases.
</interfaces>

@.claude-plugin/plugin.json
@tests/tier-contract.test.ts
@tests/skill-descriptions.test.ts
@tests/nl-triggers.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write skills/pensmith.md + plumbing namespace skills (UX-03, UX-04, UX-05)</name>
  <read_first>
    - 07-RESEARCH "Natural-Language Trigger Routing via Skill Descriptions" + "Plumbing Namespace" + Pitfall 7
    - 07-PATTERNS "skills/pensmith.md" + "skills/plan-section.md ... verify-section.md" structure patterns
    - tests/skill-descriptions.test.ts (the exact phrase-match assertions to satisfy)
    - tests/nl-triggers.test.ts (the subset-of-UX02_VERBS / no-17th-verb invariant)
    - PRD §5.4 trigger-phrase table; ./CLAUDE.md SINGLE-COMMAND UX non-negotiable
  </read_first>
  <files>skills/pensmith.md, skills/plan-section.md, skills/write-section.md, skills/verify-section.md</files>
  <action>
    Create four skill markdown files, each with YAML frontmatter (name + description) followed by a short workflow body.

    skills/pensmith.md (name: pensmith) — primary NL-routing skill. The description MUST contain the verbatim PRD §5.4 phrases: "start my paper", "what's next?", "where am I?", "continue where I left off", "resume". State that bare /pensmith routes to the correct next step automatically via the state-aware router (resolveNextAction over STATE.json + PLAN.md frontmatter). The body delegates to the bare-command routing — reference the same decision table as bin/lib/router.ts; do not duplicate logic, point at the verb.

    skills/plan-section.md (name: pensmith:plan-section) — plumbing. Description triggers: "plan section N", "redo section N", "section N needs a new plan", "section N needs work". The body maps redo/revise/swap-source/length-change inline corrections onto the EXISTING `plan` verb and `plan --revise` path (per 04-04 — NOT a new verb). Make explicit: swap-source and redo route through revise; never introduce a 17th verb.

    skills/write-section.md (name: pensmith:write-section) — plumbing. Triggers: "write section N", "draft section N", "rewrite section N". Body delegates to the existing `write` verb.

    skills/verify-section.md (name: pensmith:verify-section) — plumbing. Triggers: "verify section N", "check section N citations", "re-verify section N". Body delegates to the existing `verify` verb.

    Every description string must be concrete enough to route (Pitfall 7) — list the exact phrases. Keep bodies short (these are routing shims, not workflow rewrites). Cover the "make it sound less AI" → humanize-under-done mapping in skills/pensmith.md's description so that NL trigger routes to the done/humanize path.
  </action>
  <verify>
    <automated>node --import tsx --test tests/skill-descriptions.test.ts tests/nl-triggers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: four skill files exist with name + description frontmatter; skills/pensmith.md description matches /where am I/i, /what'?s next/i, /resume/i; plan-section matches /plan section/i + /redo section/i
    - behavior: nl-triggers.test.ts confirms skill-mapped target verbs ⊆ UX02_VERBS and UX02_VERBS.length===16 (no 17th verb); inline corrections route to plan/revise
    - test-command: `node --import tsx --test tests/skill-descriptions.test.ts tests/nl-triggers.test.ts` exits 0, no skips, no failures
  </acceptance_criteria>
  <done>Four skill files exist with concrete NL trigger phrases; inline corrections map to existing plan/revise; no 17th verb; the skill + nl-trigger tests are GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Register plumbing namespace in plugin.json + extend tier-contract parity (UX-03, UX-02)</name>
  <read_first>
    - .claude-plugin/plugin.json (current shape — add a skills array alongside mcpServers)
    - 07-RESEARCH "Plumbing Namespace — Skill File Naming Convention" + Assumption A1 (verify colon-prefix name format against the Anthropic plugin schema before shipping)
    - scripts/validate-plugin-manifest.cjs (the manifest validator — confirm it tolerates / validates the new skills array)
    - tests/tier-contract.test.ts (the parity-case structure to extend) + tests/skill-descriptions.test.ts (plugin.json skills-array assertion)
    - bin/lib/verbs.ts UX02_VERBS (the verbs the plumbing namespace maps onto)
  </read_first>
  <files>.claude-plugin/plugin.json, tests/tier-contract.test.ts</files>
  <action>
    Edit .claude-plugin/plugin.json: add a `skills` array alongside `mcpServers` registering the four colon-prefix skills — { name:'pensmith', file:'skills/pensmith.md' }, { name:'pensmith:plan-section', file:'skills/plan-section.md' }, { name:'pensmith:write-section', file:'skills/write-section.md' }, { name:'pensmith:verify-section', file:'skills/verify-section.md' }. BEFORE writing, verify the exact `skills` field name + colon-name allowance against the Anthropic plugin schema (07-RESEARCH Assumption A1 / Open Question 2) — if the schema rejects colon names or a `skills` array, fall back to documenting the plumbing namespace in CONTRIBUTING.md and surface the deviation in the SUMMARY (do NOT ship a manifest that fails validate-plugin-manifest.cjs). Run the manifest validator after editing.

    Extend tests/tier-contract.test.ts with a verb-shortcut + plumbing-namespace parity case: assert the new verbs (next/status/resume — now real per 07-02) are present in BOTH tiers' surface, and that the plumbing namespace resolves to the same underlying verb in Tier 1 (skill) and Tier 2 (CLI). Follow the existing assert-tier-equivalent (±20%) helper pattern. Keep the 16-verb bijection assertion (no MCP tool added; no 17th verb).
  </action>
  <verify>
    <automated>node --import tsx --test tests/skill-descriptions.test.ts && npm run test:tier-contract</automated>
  </verify>
  <acceptance_criteria>
    - source: plugin.json has a validated skills array with the four colon-prefix entries (OR a documented CONTRIBUTING.md fallback if the schema rejects it, noted in SUMMARY); tier-contract.test.ts has the new parity case
    - behavior: validate-plugin-manifest.cjs passes; tier-contract asserts next/status/resume + plumbing parity across both tiers; UX02_VERBS stays 16
    - test-command: `node --import tsx --test tests/skill-descriptions.test.ts && npm run test:tier-contract` both exit 0
  </acceptance_criteria>
  <done>The plumbing namespace is registered (or documented per the schema fallback) and validates; tier-contract parity covers the new verb surface; no 17th verb / 16-bijection intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify live NL-trigger routing (UX-04, UX-05)</name>
  <what-built>Natural-language routing skills (skills/pensmith.md + 3 plumbing skills), the plugin.json plumbing namespace, and tier-contract parity for the new verb surface. All deterministic contracts (skill-file content, plumbing registration, tier parity) are covered by automated tests; this checkpoint verifies the LIVE model-routing behavior that CI cannot assert (07-VALIDATION Manual-Only: UX-04 live NL routing).</what-built>
  <how-to-verify>
    1. In a real Claude Code session with the pensmith plugin loaded, type: "where am I?" → confirm the pensmith status/routing skill is invoked (not an unrelated skill).
    2. Type: "redo section 3" → confirm it routes to the plan/plan --revise path for section 3 (NOT a new verb, NOT a full rewrite from scratch).
    3. Type: "make it sound less AI" → confirm it routes to the humanize-under-done path.
    4. Type: "continue where I left off" → confirm SessionStart/resume routing picks up the HANDOFF.json state.
    5. Confirm the plumbing namespace is scriptable: invoke /pensmith:plan-section and confirm it maps to the plan verb.
  </how-to-verify>
  <resume-signal>Type "approved" if all five NL phrases route correctly, or describe which phrase routed to the wrong skill.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user NL message → skill selection | The model routes by matching the message against skill descriptions; ambiguous descriptions misroute |
| plugin.json → Claude Code plugin loader | Manifest must validate against the plugin schema (validate-plugin-manifest.cjs) |
| plumbing skill → verb path | Plumbing skills must map onto the locked 16 verbs only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-14 | Spoofing | NL trigger ambiguity / misroute | mitigate | distinct, non-overlapping trigger phrases per skill (Pitfall 7); skill-descriptions.test.ts asserts the exact phrases are present |
| T-07-15 | Tampering | 16-verb bijection / no 17th verb | mitigate | nl-triggers.test.ts asserts skill targets ⊆ UX02_VERBS and length===16; tier-contract adds no MCP tool |
| T-07-16 | Tampering | plugin.json manifest corruption | mitigate | validate-plugin-manifest.cjs runs after the edit; colon-name format verified against the schema before shipping (A1) |
| T-07-SC | Tampering | npm installs | accept | No new packages (07-RESEARCH audit: none) |
</threat_model>

<verification>
- `node --import tsx --test tests/skill-descriptions.test.ts tests/nl-triggers.test.ts` → GREEN, no skips
- `npm run test:tier-contract` → GREEN (new verb-shortcut + plumbing parity case passes)
- `npm run check` → GREEN (lint + typecheck + build + tier-contract + tests + manifests, incl. validate-plugin-manifest.cjs)
- Human-verify checkpoint: live NL routing confirmed for the five PRD §5.4 phrases
</verification>

<success_criteria>
- 4 skill files exist with concrete PRD §5.4 trigger phrases
- Plumbing namespace registered in plugin.json (or documented fallback) and validates
- Inline corrections map to existing plan/revise paths — no 17th verb
- Tier-contract parity extended for next/status/resume + plumbing namespace
- UX02_VERBS.length===16 preserved
- Live NL routing human-verified
</success_criteria>

<output>
Create `.planning/phases/07-single-command-ux-layer-hooks-flags/07-04-SUMMARY.md` when done
</output>
