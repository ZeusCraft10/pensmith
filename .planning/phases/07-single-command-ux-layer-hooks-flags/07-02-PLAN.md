---
phase: 07-single-command-ux-layer-hooks-flags
plan: 02
type: execute
wave: 1
depends_on: ["07-01"]
files_modified:
  - bin/lib/router.ts
  - bin/lib/estimator.ts
  - bin/cli/next.ts
  - bin/cli/status.ts
  - bin/cli/resume.ts
  - bin/pensmith.ts
autonomous: true
requirements: [UX-01, UX-02, ERGO-01, ERGO-02, ERGO-03, ERGO-04]
must_haves:
  truths:
    - "Bare `pensmith` (no verb) resolves a state-aware next action from STATE.json + PLAN.md frontmatter — intake / research / outline / next section / compile / done / resume"
    - "`pensmith next`, `pensmith status`, `pensmith resume` all execute as real verbs in Tier 2 (no longer stubs)"
    - "`--estimate` prints a token + USD projection table without any LLM or network call"
    - "`--yolo` refuses (exits non-zero) when the projected cost exceeds 50% of the session cap"
    - "`--dry-run` forces offline mode and `--show-prompts` mirrors every prompt to stderr"
    - "The locked 16-verb set is unchanged — no 17th verb is added"
  artifacts:
    - path: "bin/lib/router.ts"
      provides: "Pure resolveNextAction() decision table over STATE.json + PLAN.md frontmatter"
      exports: ["resolveNextAction", "RouterDecision"]
    - path: "bin/lib/estimator.ts"
      provides: "projectEstimate() pure cost projection + 50%-cap predicate"
      exports: ["projectEstimate", "EstimateResult"]
    - path: "bin/cli/next.ts"
      provides: "next verb — thin orchestrator calling resolveNextAction()"
      exports: ["nextCommand"]
    - path: "bin/cli/status.ts"
      provides: "status verb — state display"
      exports: ["statusCommand"]
    - path: "bin/cli/resume.ts"
      provides: "resume verb — HANDOFF-aware resume"
      exports: ["resumeCommand"]
    - path: "bin/pensmith.ts"
      provides: "root run() → router; 4 global flags; 3 new verb loaders"
      contains: "resolveNextAction"
  key_links:
    - from: "bin/pensmith.ts"
      to: "bin/lib/router.ts"
      via: "root defineCommand run() calls resolveNextAction"
      pattern: "resolveNextAction"
    - from: "bin/pensmith.ts"
      to: "bin/lib/session-log.ts"
      via: "--show-prompts → setMirrorPromptsToStderr(true)"
      pattern: "setMirrorPromptsToStderr"
    - from: "bin/lib/estimator.ts"
      to: "bin/lib/pricing.ts"
      via: "estimateCost() per step"
      pattern: "estimateCost"
    - from: "bin/cli/next.ts"
      to: "bin/lib/router.ts"
      via: "resolveNextAction call"
      pattern: "resolveNextAction"
---

<objective>
Build the state-aware bare `/pensmith` router and the `--estimate` projector, promote the `next` / `status` / `resume` stub verbs to real implementations, and wire the four global flags (`--dry-run`, `--estimate`, `--yolo`, `--show-prompts`) into the root `bin/pensmith.ts` command.

Purpose: UX-01 (bare command resolves the next step) is the headline of Phase 7. The router is a PURE FUNCTION over STATE.json + PLAN.md frontmatter (no FSM library); the estimator is a pure cost projection (no LLM call). Both are integration of already-built primitives (loadState, estimateCost, isOfflineMode, setMirrorPromptsToStderr).
Output: 2 new lib modules, 3 new CLI verb files, and the upgraded root dispatcher.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-single-command-ux-layer-hooks-flags/07-RESEARCH.md
@.planning/phases/07-single-command-ux-layer-hooks-flags/07-PATTERNS.md
@.planning/phases/07-single-command-ux-layer-hooks-flags/07-01-SUMMARY.md

<interfaces>
<!-- Contracts to implement. The RED tests in 07-01 assert exactly these. -->

From bin/lib/state.ts:
```typescript
export class StateNotFoundError extends Error { code: 'STATE_NOT_FOUND'; }
export function loadState(paperRoot: string): Promise<State>;  // throws StateNotFoundError on ENOENT
// State has: sections?: Array<{ n: number; slug: string; ... }>, phase?, etc.
```

From bin/lib/paths.ts: `paperDir(root)`, `sectionPlan(n, slug, root)` (section PLAN.md path helpers).
From bin/lib/frontmatter.ts: `parseFrontmatter(text) => { frontmatter, body }`.
From bin/lib/schemas/handoff.ts: `HandoffSchema`, `type Handoff` (snake_case; fields phase, next_action, current_section, section_pointers[].state).
From bin/lib/pricing.ts: `estimateCost({ providerId, modelId, inputTokens, outputTokens }): number` (pure, no IO).
From bin/lib/http-mock.ts: `isOfflineMode(): boolean` (returns `process.env.PENSMITH_NETWORK_TESTS !== '1'`).
From bin/lib/session-log.ts: `setMirrorPromptsToStderr(enabled: boolean): void`.
From bin/lib/verbs.ts: `UX02_VERBS` (the locked 16), `type Ux02Verb`.

Router contract (this plan implements):
```typescript
export type RouterDecision =
  | { verb: 'new' } | { verb: 'research' } | { verb: 'outline' }
  | { verb: 'plan'; n: number; slug: string }
  | { verb: 'write'; n: number; slug: string }
  | { verb: 'verify'; n: number; slug: string }
  | { verb: 'compile' } | { verb: 'done' }
  | { verb: 'status'; reason: 'done' }
  | { verb: 'resume'; handoff: Handoff };
export function resolveNextAction(paperRoot: string): Promise<RouterDecision>;
```

Estimator contract (this plan implements):
```typescript
export interface EstimateRow { step: string; inputTokens: number; outputTokens: number; usd: number; }
export interface EstimateResult { rows: EstimateRow[]; totalUsd: number; exceedsHalfCap: boolean; }
export function projectEstimate(args: { paperRoot: string; sessionCapUsd?: number }): Promise<EstimateResult>;
```

Existing thin-orchestrator analog (copy structure): bin/cli/compile.ts (defineCommand, stdout-only, runX delegation).
</interfaces>

@bin/pensmith.ts
@bin/cli/compile.ts
@tests/pensmith-router.test.ts
@tests/estimator.test.ts
@tests/flags.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bin/lib/router.ts — pure state-aware decision table (UX-01)</name>
  <read_first>
    - bin/lib/state.ts loadState + StateNotFoundError (catch → 'new', do NOT re-throw)
    - bin/lib/paths.ts paperDir + sectionPlan helpers
    - bin/lib/frontmatter.ts parseFrontmatter
    - bin/lib/schemas/handoff.ts HandoffSchema + Handoff type
    - 07-RESEARCH "Bare /pensmith State-Aware Router Decision Table" + the resolveNextAction Code Example
    - 07-PATTERNS "bin/lib/router.ts" section (StateNotFoundError catch pattern, file-existence pattern, parseFrontmatter pattern)
  </read_first>
  <files>bin/lib/router.ts</files>
  <behavior>
    - No STATE.json (loadState throws StateNotFoundError) → { verb:'new' } (caught, not re-thrown)
    - STATE.json present, no .paper/RESEARCH.md → { verb:'research' }
    - RESEARCH.md present, no .paper/OUTLINE.md (or zero sections) → { verb:'outline' }
    - HANDOFF.json present + phase!=='done' → { verb:'resume', handoff } (checked before section walk)
    - Sections present: walk sorted by n; first PLAN.md with status 'planned' → plan; 'writing' → write; 'written'|'verifying' → verify; verified/failed/unverifiable → continue
    - All sections verified, no DRAFT.md → { verb:'compile' }; DRAFT.md present, no FINAL.md → { verb:'done' }; both present → { verb:'status', reason:'done' }
    - Malformed HANDOFF.json (JSON.parse or HandoffSchema fails) → fall through (never crash)
  </behavior>
  <action>
    Implement resolveNextAction(paperRoot) as a PURE FUNCTION per the 07-RESEARCH decision table and 07-PATTERNS catch pattern. Read-only: use existsSync + readFileSync + loadState; never write. Catch StateNotFoundError specifically (instanceof) and return { verb:'new' } — re-throw any other error. Read HANDOFF.json via HandoffSchema.safeParse (never throw on malformed). Walk state.sections sorted ascending by n; resolve PLAN.md path with sectionPlan(n, slug, paperRoot) and read status via parseFrontmatter. Use the section-state vocabulary from SectionStateSchema (planned/writing/written/verifying/verified/failed/unverifiable). Export RouterDecision (discriminated union) and resolveNextAction. No FSM library; no console.* (this is a lib).
  </action>
  <verify>
    <automated>node --import tsx --test tests/pensmith-router.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: bin/lib/router.ts exports resolveNextAction + RouterDecision; catches StateNotFoundError with instanceof and returns { verb:'new' }
    - behavior: tests/pensmith-router.test.ts un-skips (module now exists) and all decision-table cases PASS
    - test-command: `node --import tsx --test tests/pensmith-router.test.ts` exits 0, no skips, no failures
  </acceptance_criteria>
  <done>Router resolves all decision-table branches; StateNotFoundError → 'new' without crashing in a fresh dir; HANDOFF malformed input never throws; router test GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bin/lib/estimator.ts — pure token+USD projection + 50%-cap predicate (ERGO-02, ERGO-03)</name>
  <read_first>
    - bin/lib/pricing.ts estimateCost signature + MODEL_PRICES (pure, no IO)
    - bin/lib/state.ts loadState (read sections to count write/verify/plan steps)
    - bin/lib/router.ts resolveNextAction (reuse to know which steps remain)
    - 07-RESEARCH "--estimate Flag Implementation" + Pitfall 8 (estimate must NOT trigger real LLM)
    - 07-PATTERNS "bin/lib/estimator.ts" section (STEP_HEURISTICS frozen table + 50%-cap pattern)
  </read_first>
  <files>bin/lib/estimator.ts</files>
  <behavior>
    - projectEstimate returns { rows, totalUsd, exceedsHalfCap }
    - totalUsd === sum of rows[].usd (each row usd === estimateCost(step heuristic))
    - exceedsHalfCap === (totalUsd > sessionCapUsd * 0.5); sessionCapUsd defaults to 5.0
    - Zero network calls, zero LLM calls, zero COSTS.jsonl writes during projection
    - Per-section steps (plan/write/verify) multiplied by the section count from STATE.json
  </behavior>
  <action>
    Implement projectEstimate as a pure projection. Define a frozen STEP_HEURISTICS table (new/research/outline/plan/write/verify/compile/done input+output token estimates per 07-PATTERNS; label values as conservative estimates in comments). Read STATE.json via loadState to count sections (plan/write/verify rows are per-section). For each remaining step build an EstimateRow with usd = estimateCost({ providerId, modelId, inputTokens, outputTokens }) using the configured provider+model (read from runtime config defaults; do NOT import any LLM call path — only pricing.ts). Sum to totalUsd; set exceedsHalfCap. Set process.env PENSMITH_DRY_RUN='1' defensively at module entry is NOT needed (no network here) but DO NOT import runtime.ts LLM client paths (Pitfall 8). Export projectEstimate, EstimateRow, EstimateResult.
  </action>
  <verify>
    <automated>node --import tsx --test tests/estimator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: estimator.ts imports estimateCost from pricing.ts and does NOT import any runtime LLM-call path
    - behavior: totalUsd equals row sum; exceedsHalfCap flips at 50% of sessionCapUsd; no COSTS.jsonl written
    - test-command: `node --import tsx --test tests/estimator.test.ts` exits 0, no skips, no failures
  </acceptance_criteria>
  <done>projectEstimate produces a correct projection with the 50%-cap predicate, performs zero billing, and the estimator test is GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Promote next/status/resume verbs + wire root run() router + 4 global flags (UX-01, UX-02, ERGO-01..04)</name>
  <read_first>
    - bin/pensmith.ts (REAL_VERB_LOADERS map + defineCommand; the ONLY edit point for loaders)
    - bin/cli/compile.ts (thin-orchestrator template: defineCommand, stdout-only, runX)
    - bin/lib/router.ts resolveNextAction (Task 1 — root run() + next verb call it)
    - bin/lib/estimator.ts projectEstimate (Task 2 — --estimate calls it)
    - bin/lib/http-mock.ts isOfflineMode + 07-PATTERNS "--dry-run wiring" (set PENSMITH_NETWORK_TESTS='' and PENSMITH_DRY_RUN='1')
    - bin/lib/session-log.ts setMirrorPromptsToStderr + 07-PATTERNS "--show-prompts wiring"
    - 07-RESEARCH Pitfall 4 (bare /pensmith → root run(), NOT the next subcommand) + Pitfall 5 (StateNotFoundError → intake)
  </read_first>
  <files>bin/cli/next.ts, bin/cli/status.ts, bin/cli/resume.ts, bin/pensmith.ts</files>
  <action>
    Create three thin orchestrators copying the bin/cli/compile.ts structure (defineCommand, stdout-only — no console.*, export named command + default):
    - next.ts (nextCommand): call resolveNextAction(process.cwd()), then dispatch to the resolved verb's command and run it (import the target verb loader and invoke). At minimum print the resolved verb to stdout and execute it; do NOT introduce a 17th verb.
    - status.ts (statusCommand): loadState + walk section PLAN.md frontmatter (reuse the router's read helpers or parseFrontmatter) and print a status table (phase, per-section state, next action from resolveNextAction). Handle StateNotFoundError → print "no active paper — run `pensmith new`".
    - resume.ts (resumeCommand): read .paper/HANDOFF.json via HandoffSchema.safeParse; if present + phase!=='done', print the resume summary and dispatch to resolveNextAction's verb; if absent, fall back to resolveNextAction.

    Edit bin/pensmith.ts:
    1. Add next/status/resume to REAL_VERB_LOADERS (the documented single edit point) pointing at the new ./cli/*.js modules. Do NOT add any key outside UX02_VERBS.
    2. Add four global flags to the root defineCommand `args`: 'dry-run', 'estimate', 'yolo', 'show-prompts' (all boolean, default false) per 07-PATTERNS flag-declaration pattern.
    3. Add a root `run({ args })` handler: if args['show-prompts'] → setMirrorPromptsToStderr(true); if args['dry-run'] → set process.env.PENSMITH_NETWORK_TESTS='' and process.env.PENSMITH_DRY_RUN='1' BEFORE any dispatch; if args['estimate'] → call projectEstimate, print the projection table to stdout, and if exceedsHalfCap && args['yolo'] → print the refusal and process.exit(1) (ERGO-03 + ARCH-11, enforced not advisory), else exit 0 without executing; otherwise (bare invocation, no verb) → call resolveNextAction(process.cwd()) and dispatch to the resolved verb. Keep the existing pathToFileURL main-guard.
    Note: bare `pensmith` must hit root run(), NOT the next subcommand (Pitfall 4). The --estimate/--yolo refusal MUST be a hard exit(1), not a warning.
  </action>
  <verify>
    <automated>node --import tsx --test tests/flags.test.ts tests/cli-verbs.test.ts tests/pensmith-router.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: REAL_VERB_LOADERS now has next/status/resume; root defineCommand declares dry-run/estimate/yolo/show-prompts; root run() calls resolveNextAction and (on --estimate) projectEstimate
    - behavior: bare invocation routes via resolveNextAction; --yolo with over-50% estimate exits non-zero; --dry-run leaves isOfflineMode()===true; --show-prompts calls setMirrorPromptsToStderr; cli-verbs.test.ts still sees exactly 16 verbs
    - test-command: `node --import tsx --test tests/flags.test.ts tests/cli-verbs.test.ts tests/pensmith-router.test.ts` exits 0, no failures
  </acceptance_criteria>
  <done>next/status/resume execute as real verbs; bare `pensmith` is state-aware; all four global flags work; --yolo refuses over-cap estimates with a non-zero exit; the 16-verb bijection is intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem (.paper) → router/estimator | STATE.json + PLAN.md + HANDOFF.json are read as untrusted local files; parsed via Zod safeParse |
| CLI args → process env | --dry-run mutates PENSMITH_NETWORK_TESTS / PENSMITH_DRY_RUN before any dispatch |
| --estimate path → billing ledger | estimator must never reach the LLM/network/COSTS.jsonl |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | HANDOFF.json / STATE.json parsing | mitigate | router uses HandoffSchema.safeParse + try/catch; malformed input falls through, never crashes (Pitfall 5) |
| T-07-06 | Information Disclosure | --estimate must not bill | mitigate | estimator.ts imports only pricing.ts (pure math); no runtime LLM-call path import (Pitfall 8) |
| T-07-07 | Elevation of Privilege | --yolo gate bypass | mitigate | the >50%-cap refusal is a hard process.exit(1) in the root run() estimate path, not advisory (ARCH-11) |
| T-07-08 | Spoofing | --dry-run bypassing cassette gate | mitigate | --dry-run sets PENSMITH_NETWORK_TESTS='' (never '1') so isOfflineMode() stays true (Security Domain control) |
| T-07-09 | Tampering | 16-verb bijection | mitigate | loaders added only for keys already in UX02_VERBS; cli-verbs.test.ts asserts the count stays 16 |
| T-07-SC | Tampering | npm installs | accept | No new packages (07-RESEARCH audit: none) |
</threat_model>

<verification>
- `node --import tsx --test tests/pensmith-router.test.ts tests/estimator.test.ts tests/flags.test.ts tests/cli-verbs.test.ts` → all GREEN, no skips on router/estimator
- `npm run check` (lint + typecheck + build + tier-contract + tests + manifests) → GREEN
- Bare `node dist/bin/pensmith.js` in a fixture dir routes to the correct verb (manual smoke acceptable; router test covers the logic)
</verification>

<success_criteria>
- bin/lib/router.ts + bin/lib/estimator.ts exist and their RED tests are now GREEN
- next/status/resume promoted to real verbs in REAL_VERB_LOADERS
- Four global flags declared + wired in bin/pensmith.ts root run()
- --yolo refuses over-50%-cap estimates with non-zero exit
- UX02_VERBS.length===16 preserved (no 17th verb)
</success_criteria>

<output>
Create `.planning/phases/07-single-command-ux-layer-hooks-flags/07-02-SUMMARY.md` when done
</output>
