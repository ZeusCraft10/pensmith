---
phase: 07-single-command-ux-layer-hooks-flags
plan: 02
type: execute
wave: 1
depends_on: ["07-01"]
files_modified:
  - bin/lib/router.ts
  - bin/lib/estimator.ts
  - bin/cli/next.ts
  - bin/cli/status.ts
  - bin/cli/resume.ts
  - bin/pensmith.ts
autonomous: true
requirements: [UX-01, UX-02, ERGO-01, ERGO-02, ERGO-03, ERGO-04]
must_haves:
  truths:
    - "Bare `pensmith` (no verb) resolves a state-aware next WORK action from STATE.json + PLAN.md frontmatter — intake / research / outline / next section / compile / done (HANDOFF is ignored by routing per H4)"
    - "`pensmith next`, `pensmith status`, `pensmith resume` all execute as real verbs in Tier 2 (no longer stubs); resume hands off to the next WORK verb and clears HANDOFF (never loops on itself — H4)"
    - "`--estimate` prints a token + USD projection table without any LLM or network call"
    - "`--yolo` refuses (hard exit non-zero) when the projected cost exceeds 50% of the session cap — the pre-flight runs WHENEVER --yolo is present, for ANY verb (incl. non-gate write/plan/verify) and bare invocation, independent of --estimate (H1 / C2-H1)"
    - "`--yolo` in a paper-less directory does NOT crash — the cap pre-flight treats a missing STATE.json as nothing-to-estimate / under-cap (C2-H1 fresh-dir guard)"
    - "Global flags are applied in a PRE-DISPATCH seam (not a root run()), so an explicit verb runs EXACTLY once and --dry-run/--show-prompts take effect for explicit verbs (H2)"
    - "Global flags are FORWARDED into manually-dispatched verbs: the bare/next/resume loader-table dispatch constructs the verb args object carrying the parsed global flags (at minimum `yolo`, plus dry-run/estimate/show-prompts where they affect the dispatched verb) so a manually-dispatched compile/done sees yolo:true exactly as if invoked explicitly and skips its OWN approval gate (C3-HIGH-2)"
    - "resolveNextAction is TOTAL over SectionStateSchema — every section state (incl. failed/unverifiable, routed to verify) maps to a concrete RouterDecision; the function NEVER returns undefined, so the bare dispatcher's decision.verb access can never crash (C3-HIGH-1)"
    - "resolveNextAction NEVER throws and NEVER returns undefined for ANY on-disk STATE.json — absent (→ new), well-typed (→ correct verb), AND malformed/schema-invalid/forward-incompat/permission-denied (→ status/attention with a stderr diagnostic). loadState only translates ENOENT; every OTHER load/parse error (invalid JSON SyntaxError, SchemaValidationError, ForwardIncompatError, EACCES/EPERM) is caught and re-classified to status/attention — so SC1 'never crash' holds across the FULL STATE.json failure surface, not just the well-typed state space (C4-HIGH)"
    - "`--dry-run` forces offline mode for SOURCE adapters (PENSMITH_NETWORK_TESTS='') AND sets PENSMITH_NO_LLM='1' so the EXISTING LLM call sites (verify/pass2.ts + pass4.ts) short-circuit to the offline placeholder — a verb path makes ZERO network calls (H3 / C2-H3); `--show-prompts` mirrors every prompt to stderr"
    - "The locked 16-verb set is unchanged — no 17th verb is added"
  artifacts:
    - path: "bin/lib/router.ts"
      provides: "Pure resolveNextAction() decision table over STATE.json + PLAN.md frontmatter; IGNORES HANDOFF and never returns 'resume' (H4); TOTAL over the FULL loadState error surface — ENOENT→new, any OTHER load/parse error→status/attention, never throws (C4-HIGH)"
      exports: ["resolveNextAction", "RouterDecision"]
    - path: "bin/lib/estimator.ts"
      provides: "projectEstimate() pure cost projection + 50%-cap predicate; guarded so a paper-less dir (StateNotFoundError) AND a corrupt/schema-invalid STATE.json (any other load error) both return the empty projection { rows:[], totalUsd:0, exceedsHalfCap:false } — never throws (C2-H1 / C4-HIGH)"
      exports: ["projectEstimate", "EstimateResult"]
    - path: "bin/cli/next.ts"
      provides: "next verb — thin orchestrator calling resolveNextAction()"
      exports: ["nextCommand"]
    - path: "bin/cli/status.ts"
      provides: "status verb — state display"
      exports: ["statusCommand"]
    - path: "bin/cli/resume.ts"
      provides: "resume verb — HANDOFF-aware resume"
      exports: ["resumeCommand"]
    - path: "bin/pensmith.ts"
      provides: "pre-dispatch flag seam (NOT root run()); 4 global flags; yolo cap pre-flight for ANY --yolo command line; --dry-run sets PENSMITH_NETWORK_TESTS='' + PENSMITH_NO_LLM='1' (H3); bare→router dispatch gated on no-verb; the bare/next/resume loader-table dispatch FORWARDS parsed global flags (yolo,...) into the dispatched verb's args (C3-HIGH-2); 3 new verb loaders; exported REAL_VERB_LOADERS + a shared dispatchVerb(decision/verb, globalFlags) helper"
      contains: "resolveNextAction"
  key_links:
    - from: "bin/pensmith.ts"
      to: "bin/lib/router.ts"
      via: "pre-dispatch wrapper calls resolveNextAction on bare invocation (no verb)"
      pattern: "resolveNextAction"
    - from: "bin/pensmith.ts"
      to: "bin/lib/session-log.ts"
      via: "--show-prompts → setMirrorPromptsToStderr(true) in the pre-dispatch seam"
      pattern: "setMirrorPromptsToStderr"
    - from: "bin/pensmith.ts"
      to: "bin/lib/estimator.ts"
      via: "yolo cap pre-flight calls projectEstimate whenever --yolo is present (H1)"
      pattern: "projectEstimate"
    - from: "bin/pensmith.ts"
      to: "bin/cli/compile.ts + bin/cli/done.ts (args.yolo)"
      via: "the shared dispatchVerb helper forwards parsed globalFlags.yolo into the manually-dispatched verb's args so its own approval-gate-skip engages (C3-HIGH-2)"
      pattern: 'yolo:.*globalFlags'
    - from: "bin/pensmith.ts"
      to: "process.env.PENSMITH_NO_LLM"
      via: "--dry-run sets PENSMITH_NO_LLM='1' BEFORE dispatch (the LLM call sites in pass2/pass4 already honor it, H3)"
      pattern: "PENSMITH_NO_LLM"
    - from: "bin/lib/estimator.ts"
      to: "bin/lib/pricing.ts"
      via: "estimateCost() per step"
      pattern: "estimateCost"
    - from: "bin/cli/resume.ts"
      to: "bin/lib/router.ts"
      via: "resume dispatches to resolveNextAction's WORK verb then clears HANDOFF (H4)"
      pattern: "resolveNextAction"
    - from: "bin/cli/next.ts"
      to: "bin/lib/router.ts"
      via: "resolveNextAction call"
      pattern: "resolveNextAction"
---

<objective>
Build the state-aware bare `/pensmith` router and the `--estimate` projector, promote the `next` / `status` / `resume` stub verbs to real implementations, and wire the four global flags (`--dry-run`, `--estimate`, `--yolo`, `--show-prompts`) into a PRE-DISPATCH seam in `bin/pensmith.ts` (NOT a root run() — citty falls through to it after every verb, per corrected Pitfall 4 / review H2). `--dry-run` gates BOTH egress channels via env vars the existing code already honors: `PENSMITH_NETWORK_TESTS=''` (source adapters → cassettes) and `PENSMITH_NO_LLM='1'` (the LLM call sites in verify/pass2.ts + pass4.ts → offline placeholder).

Purpose: UX-01 (bare command resolves the next step) is the headline of Phase 7. The router is a PURE FUNCTION over STATE.json + PLAN.md frontmatter (no FSM library) that IGNORES HANDOFF so bare /pensmith never loops on resume (H4); the estimator is a pure cost projection (no LLM call). The four flags must apply to explicit verbs too (H2), the --yolo >50%-cap refusal must fire whenever --yolo is present for ANY verb independent of --estimate AND must not crash in a paper-less dir (H1 / C2-H1), and --dry-run must gate the LLM CLIENT (not just source adapters) so a verb path makes zero network calls (H3 / C2-H3). CRITICAL SAFETY INVARIANT (SC1 / C4-HIGH): both resolveNextAction and projectEstimate must be TOTAL over the ENTIRE on-disk STATE.json surface — not only the well-typed SectionStateSchema state space (cycle-3 fix) but ALSO a present-but-corrupt or schema-invalid STATE.json. loadState only translates ENOENT; a malformed file (invalid JSON, a sections entry missing slug, non-numeric n, bad $schemaVersion, ForwardIncompatError, or a permission error) makes loadState THROW a SyntaxError/SchemaValidationError/ForwardIncompatError that the cycle-3 catch (StateNotFoundError-only) re-throws, so the exception bubbles out before any RouterDecision/projection exists and the bare dispatcher crashes on `decision.verb`. This plan widens BOTH catches so the corrupt/invalid case is handled WITHOUT throwing.
Output: 2 new lib modules (router, estimator), 3 new CLI verb files, and the upgraded dispatcher with a pre-dispatch flag seam. NO Phase-5 verify code is modified (the dry-run gate reuses the existing PENSMITH_NO_LLM guard in pass2/pass4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-single-command-ux-layer-hooks-flags/07-RESEARCH.md
@.planning/phases/07-single-command-ux-layer-hooks-flags/07-PATTERNS.md
@.planning/phases/07-single-command-ux-layer-hooks-flags/07-01-SUMMARY.md

<interfaces>
<!-- Contracts to implement. The RED tests in 07-01 assert exactly these. -->

From bin/lib/state.ts (loadState error contract — load-bearing for C4-HIGH):
```typescript
export class StateNotFoundError extends Error { code: 'STATE_NOT_FOUND'; }
export function loadState(paperRoot: string): Promise<State>;
// loadState (state.ts:209-247) translates ONLY ENOENT → StateNotFoundError (line 234).
// EVERY OTHER error bubbles UNCHANGED:
//   - invalid JSON          → SyntaxError      (JSON.parse, loader.ts:98)
//   - schema-invalid        → SchemaValidationError (loader.ts:119 — e.g. sections[i].slug missing, n non-numeric, bad $schemaVersion literal)
//   - disk newer than build → ForwardIncompatError   (loader.ts:102)
//   - permission denied     → ErrnoException EACCES/EPERM (bubbles unchanged)
// State has: sections?: Array<{ n: number; slug: string }> (schema/state.ts:87 — sections is .optional(), so state.sections may be undefined), etc.
```

From bin/lib/migrations/loader.ts (the concrete error CLASSES escaping loadState for a present-but-corrupt/invalid STATE.json):
```typescript
export class SchemaValidationError extends Error { zodIssue: z.ZodIssue[]; }
export class ForwardIncompatError extends Error { diskVersion: number; codeVersion: number; }
// (invalid JSON surfaces as a plain SyntaxError; permission errors as NodeJS.ErrnoException)
```

From bin/lib/paths.ts: `paperDir(root)`, `sectionPlan(n, slug, root)` (section PLAN.md path helpers).
From bin/lib/frontmatter.ts: `parseFrontmatter(text) => { frontmatter, body }`.
From bin/lib/schemas/handoff.ts: `HandoffSchema`, `type Handoff` (snake_case; fields phase, next_action, current_section, section_pointers[].state). Consumed ONLY by bin/cli/resume.ts (summary) — NOT by the router.
From bin/lib/pricing.ts: `estimateCost({ providerId, modelId, inputTokens, outputTokens }): number` (pure, no IO).
From bin/lib/http-mock.ts: `isOfflineMode(): boolean` (returns `process.env.PENSMITH_NETWORK_TESTS !== '1'`).
From bin/lib/verify/pass2.ts + pass4.ts (DO NOT EDIT — referenced for the gate they already honor): both compute `noLlm = process.env['PENSMITH_NO_LLM'] === '1' || !process.env['ANTHROPIC_API_KEY']` and, when noLlm, return the offline UNCLEAR placeholder with ZERO `messages.create()` calls and ZERO COSTS.jsonl appends. This is the gate --dry-run sets.
From bin/lib/session-log.ts: `setMirrorPromptsToStderr(enabled: boolean): void`.
From bin/lib/verbs.ts: `UX02_VERBS` (the locked 16), `type Ux02Verb`.

NOTE (C2-H3 — verified): bin/lib/runtime.ts is config / API-key load ONLY (loadRuntimeConfig, getProviderApiKey, getOpenAlexApiKey, getS2ApiKey). It has NO model-invocation function and is NOT modified by this plan. The dry-run LLM gate is the env var PENSMITH_NO_LLM, honored by the pass2/pass4 call sites above.

Router contract (this plan implements):
```typescript
export type RouterDecision =
  | { verb: 'new' } | { verb: 'research' } | { verb: 'outline' }
  | { verb: 'plan'; n: number; slug: string }
  | { verb: 'write'; n: number; slug: string }
  | { verb: 'verify'; n: number; slug: string }
  | { verb: 'compile' } | { verb: 'done' }
  // C3-HIGH-1: status.reason widened ('done' | 'attention') + optional section so
  // the resolver is TOTAL — a stuck-section terminus has a valid return and the
  // function never falls through to `undefined`.
  // C4-HIGH: 'attention' is ALSO the disposition for a present-but-corrupt /
  // schema-invalid STATE.json (loadState threw something other than StateNotFoundError).
  | { verb: 'status'; reason: 'done' | 'attention'; section?: { n: number; slug: string } }
  | { verb: 'resume'; handoff: Handoff };  // member exists ONLY for resume.ts typing; resolveNextAction never emits it
export function resolveNextAction(paperRoot: string): Promise<RouterDecision>;
// NEVER throws and NEVER returns undefined for ANY on-disk STATE.json (C4-HIGH).
```

Estimator contract (this plan implements):
```typescript
export interface EstimateRow { step: string; inputTokens: number; outputTokens: number; usd: number; }
export interface EstimateResult { rows: EstimateRow[]; totalUsd: number; exceedsHalfCap: boolean; }
export function projectEstimate(args: { paperRoot: string; sessionCapUsd?: number }): Promise<EstimateResult>;
// NEVER throws for ANY on-disk STATE.json — absent OR corrupt/schema-invalid → empty projection (C2-H1 / C4-HIGH).
```

Existing thin-orchestrator analog (copy structure): bin/cli/compile.ts (defineCommand, stdout-only, runX delegation).
</interfaces>

@bin/pensmith.ts
@bin/cli/compile.ts
@bin/lib/verify/pass2.ts
@bin/lib/verify/pass4.ts
@tests/pensmith-router.test.ts
@tests/estimator.test.ts
@tests/flags.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bin/lib/router.ts — pure state-aware decision table (UX-01)</name>
  <read_first>
    - bin/lib/state.ts loadState (state.ts:209-247): it ONLY translates ENOENT → StateNotFoundError (line 234); EVERY OTHER load error bubbles UNCHANGED — invalid JSON (SyntaxError from JSON.parse in loader.ts:98), SchemaValidationError (loader.ts:119 — e.g. a sections entry missing slug, non-numeric n, bad $schemaVersion literal), ForwardIncompatError (loader.ts:102 — disk version newer than build), and permission errors (EACCES/EPERM). The router must catch ALL of these without throwing (C4-HIGH).
    - bin/lib/migrations/loader.ts SchemaValidationError + ForwardIncompatError + the raw SyntaxError/ENOENT surface (the concrete error CLASSES that can escape loadState for a present-but-corrupt/invalid STATE.json)
    - bin/lib/paths.ts paperDir + sectionPlan helpers
    - bin/lib/frontmatter.ts parseFrontmatter
    - bin/lib/schemas/handoff.ts HandoffSchema + Handoff type (type-only import; router does NOT read HANDOFF.json)
    - bin/lib/schemas/state.ts SectionStateSchema (state.ts:39-41 — the FULL set planned/writing/written/verifying/verified/failed/unverifiable the switch must be exhaustive over; 'failed'/'unverifiable' route to verify, NOT continue — C3-HIGH-1) AND Schema.sections (line 87 — `.optional()`, so state.sections can be `undefined`; guard before any `.sort()`/iteration — C4-HIGH)
    - 07-RESEARCH "Bare /pensmith State-Aware Router Decision Table" + the COMPLETE SECTION-STATE → VERB MAP in "PINNED ORDERING" + the resolveNextAction Code Example (now TOTAL)
    - 07-PATTERNS "bin/lib/router.ts" section (StateNotFoundError catch pattern, file-existence pattern, parseFrontmatter pattern)
  </read_first>
  <files>bin/lib/router.ts</files>
  <behavior>
    H4 PINNED ORDERING — resolveNextAction IGNORES HANDOFF.json and NEVER returns { verb:'resume' }. It is the pure next-WORK-verb resolver. The function is TOTAL over SectionStateSchema (C3-HIGH-1) AND over the full STATE.json load-failure surface (C4-HIGH): every reachable state maps to a concrete RouterDecision and the function can NEVER return `undefined` and NEVER throw. Order:
    - loadState throws → CLASSIFY the error (C4-HIGH):
        * StateNotFoundError (instanceof — ENOENT, the file is ABSENT) → { verb:'new' }
        * ANY OTHER error (the file is PRESENT but corrupt/schema-invalid/forward-incompat/permission-denied — SyntaxError, SchemaValidationError, ForwardIncompatError, EACCES/EPERM) → { verb:'status', reason:'attention' } AND emit a one-line stderr diagnostic. A present-but-corrupt file is NOT the same as an absent one, so it routes to status/attention (NOT 'new').
    - STATE.json loads OK, no .paper/RESEARCH.md → { verb:'research' }
    - RESEARCH.md present, no .paper/OUTLINE.md (or zero/undefined sections) → { verb:'outline' }
    - Sections present: GUARD state.sections for null/undefined FIRST (schema makes it optional) — treat missing/empty sections as "no sections yet" → { verb:'outline' }; otherwise walk sorted ascending by n; the FIRST section that is not 'verified' decides the verb via the COMPLETE state→verb map below.
    - All sections verified, no DRAFT.md → { verb:'compile' }; DRAFT.md present, no FINAL.md → { verb:'done' }; both present → { verb:'status', reason:'done' }
    - HANDOFF.json is NOT read or returned here (H4): a non-done HANDOFF must resolve to the next WORK verb (plan/write/verify/compile/done), so bare /pensmith always advances and never loops on resume. The { verb:'resume' } arm exists in RouterDecision ONLY for the explicit `resume` verb's own typing (Task 3), produced by bin/cli/resume.ts — never by resolveNextAction.

    COMPLETE SECTION-STATE → VERB MAP (C3-HIGH-1 — TOTAL; mirrors 07-RESEARCH "PINNED ORDERING"). For the first walked section that is not 'verified':
    - PLAN.md missing on disk → { verb:'plan', n, slug }
    - status 'planned'        → { verb:'plan',   n, slug }
    - status 'writing'        → { verb:'write',  n, slug }
    - status 'written'        → { verb:'verify', n, slug }
    - status 'verifying'      → { verb:'verify', n, slug }
    - status 'failed'         → { verb:'verify', n, slug }   ← re-attempt verification; NOT 'continue'
    - status 'unverifiable'   → { verb:'verify', n, slug }   ← re-attempt verification; NOT 'continue'
    - status 'verified'       → CONTINUE to next section (the ONLY continue case)
    - unrecognized status     → { verb:'status', reason:'attention', section:{n,slug} } (defensive: a hand-edited PLAN.md must not crash routing)
    CRITICAL (C3-HIGH-1): 'failed' and 'unverifiable' are NO LONGER 'continue'. Because every non-'verified' section RETURNS a verb, the post-walk compile branch is reached ONLY when EVERY section is 'verified'. This closes the totality gap where a reachable state like `[verified, failed, verified]` with no DRAFT.md matched NO branch and fell through to `undefined` (which crashed the bare dispatcher's `decision.verb` access — SC1/SC5). A guaranteed terminal fallback (`{ verb:'status', reason:'attention' }`) after the switch's `default` + the three post-walk returns makes the resolver provably total to both the TS compiler and at runtime.
    CRITICAL (C4-HIGH): the cycle-3 fix made the resolver total over the WELL-TYPED state space, but loadState THROWS on a present-but-corrupt/schema-invalid STATE.json and the StateNotFoundError-ONLY catch re-threw it — the exception escaped before any RouterDecision existed and the bare dispatcher crashed on `decision.verb`. Widening the catch (StateNotFoundError → new; ELSE → status/attention) closes the LAST totality gap: SC1 'never crash' now holds for absent, well-typed, AND malformed STATE.json. The sections-null guard closes a sibling crash (a `.sort()` on undefined).
  </behavior>
  <action>
    Implement resolveNextAction(paperRoot) as a PURE FUNCTION per the 07-RESEARCH "PINNED ORDERING" (H4) AND the COMPLETE SECTION-STATE → VERB MAP (C3-HIGH-1). Read-only: use existsSync + readFileSync + loadState; never write. DO NOT read or branch on HANDOFF.json here (H4): resolveNextAction must NEVER return { verb:'resume' }; it always resolves a concrete next WORK verb. Apply the checks in the pinned order: load → research → outline → section walk → compile → done → status(done).

    LOAD-ERROR CLASSIFICATION (C4-HIGH — load-bearing, FIRST step): wrap the loadState call in try/catch and CATCH ALL errors (do NOT re-throw). Re-classify inside the catch:
      - `if (err instanceof StateNotFoundError) return { verb:'new' };` (ENOENT — file ABSENT)
      - `else { process.stderr.write(<one-line diagnostic naming the corrupt STATE.json + err.message>); return { verb:'status', reason:'attention' }; }` (file PRESENT but corrupt/schema-invalid/forward-incompat/permission-denied — SyntaxError, SchemaValidationError, ForwardIncompatError, EACCES/EPERM all land here). The catch is "catch-then-classify", NOT "catch StateNotFoundError, re-throw the rest" — that re-throw was the C4-HIGH crash. This is a lib, so the ONLY console-channel write permitted is the single process.stderr.write diagnostic on the corrupt path (NOT stdout, NOT console.log).

    SECTIONS-NULL GUARD (C4-HIGH): state.sections is `.optional()` in the schema, so it may be `undefined` even on a well-typed file. BEFORE any `.sort()`/iteration, guard `const sections = state.sections ?? [];` and treat an empty/missing list as "no sections yet" → fall to the outline branch (or whatever the pinned order dictates pre-walk). NEVER call `.sort()` on a possibly-undefined value.

    TOTALITY (C3-HIGH-1 — load-bearing): walk the guarded sections sorted ascending by n; resolve PLAN.md path with sectionPlan(n, slug, paperRoot) and read status via parseFrontmatter. Implement the per-section decision as an EXHAUSTIVE `switch (status)` over SectionStateSchema (planned/writing/written/verifying/verified/failed/unverifiable) PLUS a `default`:
      - 'verified' → `continue` (the ONLY continue case);
      - 'planned' (and PLAN.md missing on disk) → return { verb:'plan', n, slug };
      - 'writing' → return { verb:'write', n, slug };
      - 'written' | 'verifying' | 'failed' | 'unverifiable' → return { verb:'verify', n, slug } (route 'failed'/'unverifiable' BACK to verify — do NOT treat them as 'continue', that was the totality bug);
      - `default:` (unrecognized hand-edited status) → return { verb:'status', reason:'attention', section:{ n, slug } }.
    Because every non-'verified' branch RETURNS, the loop falls through ONLY when all sections are 'verified', so the post-walk branches (compile → done → status(done)) are total over the all-verified case. resolveNextAction MUST therefore NEVER return `undefined` AND NEVER throw — the union return is exhaustive and the load path is fully caught. Widen RouterDecision's status arm to `{ verb:'status'; reason:'done' | 'attention'; section?: { n:number; slug:string } }`. Export RouterDecision (discriminated union — keep the { verb:'resume'; handoff } member for the explicit `resume` verb's typing, but never emit it from this function) and resolveNextAction. No FSM library; no console.* beyond the single corrupt-path stderr diagnostic.
  </action>
  <verify>
    <automated>node --import tsx --test tests/pensmith-router.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: bin/lib/router.ts exports resolveNextAction + RouterDecision; the loadState call is wrapped in a CATCH-ALL try/catch that re-classifies (StateNotFoundError instanceof → { verb:'new' }; ELSE → { verb:'status', reason:'attention' } + one stderr diagnostic) and NEVER re-throws (C4-HIGH); state.sections is guarded with `?? []` before any `.sort()`/iteration (C4-HIGH); does NOT read HANDOFF.json and never returns { verb:'resume' } (H4); the section walk is an EXHAUSTIVE switch over SectionStateSchema with a `default` arm and routes 'failed'/'unverifiable' to `verify` (NOT 'continue') — the function is TOTAL and never returns `undefined` (C3-HIGH-1)
    - behavior: tests/pensmith-router.test.ts un-skips (module now exists) and ALL decision-table cases PASS, INCLUDING (a) the H4 case (non-done HANDOFF present → next WORK verb, decision.verb !== 'resume'); (b) the C3-HIGH-1 totality cases — every section state (writing/written/verifying/failed/unverifiable) plus the mixed `[verified, failed, verified]`-no-DRAFT case resolve to a valid non-undefined verb ('failed'/'unverifiable' → 'verify'); and (c) the C4-HIGH corrupt-STATE cases — a STATE.json with INVALID JSON and a SCHEMA-INVALID STATE.json (a sections entry missing slug) both resolve to { verb:'status', reason:'attention' } WITHOUT throwing (decision is defined, decision.verb === 'status'); no input throws and no input yields `undefined`
    - test-command: `node --import tsx --test tests/pensmith-router.test.ts` exits 0, no skips, no failures
  </acceptance_criteria>
  <done>Router resolves all decision-table branches in the H4-pinned order; StateNotFoundError → 'new' without crashing in a fresh dir; a present-but-corrupt/schema-invalid STATE.json → { verb:'status', reason:'attention' } with a stderr diagnostic and WITHOUT throwing (C4-HIGH); state.sections null/undefined is guarded before any .sort() (C4-HIGH); a non-done HANDOFF never traps routing (verb !== 'resume'); the section walk is TOTAL over SectionStateSchema (failed/unverifiable → verify, exhaustive switch + default + guaranteed terminal returns) so resolveNextAction NEVER returns undefined and NEVER throws (C3-HIGH-1 + C4-HIGH); router test GREEN including the H4 case, the every-section-state + mixed-stuck-section cases, AND the invalid-JSON + schema-invalid corrupt-STATE cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bin/lib/estimator.ts — pure token+USD projection + 50%-cap predicate + fresh-dir guard (ERGO-02, ERGO-03)</name>
  <read_first>
    - bin/lib/pricing.ts estimateCost signature + MODEL_PRICES (pure, no IO)
    - bin/lib/state.ts loadState (state.ts:209-247): it ONLY translates ENOENT → StateNotFoundError; EVERY OTHER load error bubbles UNCHANGED (invalid JSON SyntaxError, SchemaValidationError, ForwardIncompatError, EACCES/EPERM). The estimator must catch ALL of these → empty projection, never re-throw (C2-H1 / C4-HIGH).
    - bin/lib/migrations/loader.ts SchemaValidationError + ForwardIncompatError (the error classes a corrupt/invalid STATE.json surfaces through loadState)
    - bin/lib/router.ts resolveNextAction (reuse to know which steps remain)
    - 07-RESEARCH "--estimate Flag Implementation" + Pitfall 5 (StateNotFoundError guard) + Pitfall 8 (estimate must NOT trigger real LLM)
    - 07-PATTERNS "bin/lib/estimator.ts" section (STEP_HEURISTICS frozen table + 50%-cap pattern + StateNotFoundError guard)
  </read_first>
  <files>bin/lib/estimator.ts</files>
  <behavior>
    - projectEstimate returns { rows, totalUsd, exceedsHalfCap }
    - totalUsd === sum of rows[].usd (each row usd === estimateCost(step heuristic))
    - exceedsHalfCap === (totalUsd > sessionCapUsd * 0.5); sessionCapUsd defaults to 5.0 BUT the dispatcher passes the configured cap (see Task 3 — env PENSMITH_COST_CAP_USD, else $5) so a lowered cap tightens the H1 refusal threshold (review L1, material to H1 correctness)
    - C2-H1 / C4-HIGH LOAD-ERROR GUARD: WRAP the loadState call in a CATCH-ALL try/catch. On StateNotFoundError (paper-less dir) AND on ANY OTHER load/parse error (a present-but-corrupt or schema-invalid STATE.json — SyntaxError, SchemaValidationError, ForwardIncompatError, EACCES/EPERM) return { rows: [], totalUsd: 0, exceedsHalfCap: false } so the yolo cap pre-flight sees "under cap" and NEVER crashes. Do NOT re-throw — `--yolo`/`--estimate` against a corrupt STATE.json must behave like the empty-projection case, identical to the absent-file case.
    - Zero network calls, zero LLM calls, zero COSTS.jsonl writes during projection
    - Per-section steps (plan/write/verify) multiplied by the section count from STATE.json
  </behavior>
  <action>
    Implement projectEstimate as a pure projection. Define a frozen STEP_HEURISTICS table (new/research/outline/plan/write/verify/compile/done input+output token estimates per 07-PATTERNS; label values as conservative estimates in comments). Read STATE.json via loadState to count sections (plan/write/verify rows are per-section) — WRAP the loadState call in a CATCH-ALL try/catch: on ANY error (StateNotFoundError for a paper-less dir, OR a SyntaxError/SchemaValidationError/ForwardIncompatError/EACCES for a present-but-corrupt/invalid STATE.json) return { rows: [], totalUsd: 0, exceedsHalfCap: false } (C2-H1 fresh-dir guard widened to the full load-failure surface per C4-HIGH); do NOT re-throw. Also guard `state.sections ?? []` before counting (schema makes it optional — never assume it is present). For each remaining step build an EstimateRow with usd = estimateCost({ providerId, modelId, inputTokens, outputTokens }) using the configured provider+model (read from runtime config defaults; do NOT import any LLM call path — only pricing.ts and state.ts). Sum to totalUsd; set exceedsHalfCap = totalUsd > (sessionCapUsd ?? 5.0) * 0.5. Do NOT import runtime.ts LLM-call paths (there are none) and do NOT import verify/pass2.ts or verify/pass4.ts (Pitfall 8). Export projectEstimate, EstimateRow, EstimateResult.
  </action>
  <verify>
    <automated>node --import tsx --test tests/estimator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: estimator.ts imports estimateCost from pricing.ts + loadState/StateNotFoundError from state.ts and does NOT import any runtime LLM-call path or verify/pass2|pass4; the loadState call is wrapped in a CATCH-ALL try/catch that returns the empty projection on StateNotFoundError AND on any other load/parse error (C2-H1 / C4-HIGH); state.sections guarded with `?? []`
    - behavior: totalUsd equals row sum; exceedsHalfCap flips at 50% of sessionCapUsd; a paper-less dir returns the empty projection (no throw); a present-but-CORRUPT STATE.json (invalid JSON OR schema-invalid) ALSO returns { rows:[], totalUsd:0, exceedsHalfCap:false } without throwing (C4-HIGH); no COSTS.jsonl written
    - test-command: `node --import tsx --test tests/estimator.test.ts` exits 0, no skips, no failures
  </acceptance_criteria>
  <done>projectEstimate produces a correct projection with the 50%-cap predicate, returns an empty projection (no crash) in a paper-less dir (C2-H1) AND against a corrupt/schema-invalid STATE.json (C4-HIGH — same empty-projection disposition), performs zero billing, and the estimator test is GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Promote next/status/resume verbs + PRE-DISPATCH flag seam + yolo cap pre-flight (ANY --yolo verb) + HANDOFF lifecycle (UX-01, UX-02, ERGO-01..04 — fixes H1/C2-H1/H2/H4)</name>
  <read_first>
    - bin/pensmith.ts (REAL_VERB_LOADERS map + defineCommand + the pathToFileURL main-guard; the ONLY edit point for loaders)
    - bin/cli/compile.ts + bin/cli/done.ts (thin-orchestrator template AND the existing `yolo` arg + gate-skip pattern). NOTE the EXACT read seam the forwarding must satisfy: compile.ts run({args}) reads `yolo: args.yolo === true` (compile.ts:90) and done.ts reads the same (done.ts:436) to skip their approval gate. A manually-dispatched compile/done that does NOT receive `yolo:true` in its args object would run the cost-cap pre-flight (financial gate) but NOT skip its own approval gate — the C3-HIGH-2 bug. The forwarding must put `yolo:true` (and the other global flags) into the args object passed to the dispatched verb's run().
    - bin/lib/router.ts resolveNextAction (Task 1 — bare dispatch + next/resume call it; it IGNORES HANDOFF per H4 and NEVER throws per C4-HIGH)
    - bin/lib/estimator.ts projectEstimate (Task 2 — the cap pre-flight calls it; it is guarded against ALL load errors per C2-H1/C4-HIGH)
    - bin/lib/http-mock.ts isOfflineMode + 07-PATTERNS "isOfflineMode() + PENSMITH_NO_LLM gates" (--dry-run sets PENSMITH_NETWORK_TESTS='' AND PENSMITH_NO_LLM='1' AND PENSMITH_DRY_RUN='1')
    - bin/lib/verify/pass2.ts:215 + pass4.ts:392 (CONFIRM the PENSMITH_NO_LLM guard --dry-run relies on — DO NOT EDIT these files)
    - bin/lib/session-log.ts setMirrorPromptsToStderr + 07-PATTERNS "--show-prompts wiring"
    - bin/lib/schemas/handoff.ts HandoffSchema + Handoff (resume reads it; lifecycle clears it)
    - node_modules/citty/dist/index.mjs:209-228 (H2: subcommand at 217 then parent run() at 228 unconditionally; bare invocation throws 'No command specified' at 225 — so bare must NOT go through runMain)
    - 07-RESEARCH CORRECTED Pitfall 3 (yolo cap scope — ANY verb) + Pitfall 4 (pre-dispatch seam, NOT root run()) + Pitfall 5 (StateNotFoundError) + HANDOFF.json Lifecycle section (H4)
  </read_first>
  <files>bin/cli/next.ts, bin/cli/status.ts, bin/cli/resume.ts, bin/pensmith.ts</files>
  <action>
    Create three thin orchestrators copying the bin/cli/compile.ts structure (defineCommand, stdout-only — no console.*, export named command + default). Reuse the SHARED dispatch table: export REAL_VERB_LOADERS from bin/pensmith.ts so next/resume dispatch through it rather than re-importing verb modules — this avoids the circular-dep risk reviewers flagged (M7).

    SHARED FLAG-FORWARDING DISPATCH CONTRACT (C3-HIGH-2 — load-bearing): export a single helper from bin/pensmith.ts, `async function dispatchVerb(verb: Ux02Verb, opts: { args?: Record<string, unknown>; globalFlags?: { yolo?: boolean; dryRun?: boolean; estimate?: boolean; showPrompts?: boolean } }): Promise<unknown>`. It (1) loads the CommandDef via REAL_VERB_LOADERS[verb] (or the stub), (2) BUILDS the args object the verb's run() will read by MERGING `opts.args` with the FORWARDED global flags — at minimum `yolo: opts.globalFlags?.yolo === true` (compile.ts:90 + done.ts:436 read exactly `args.yolo === true`), and consistently the others where they affect the dispatched verb, and (3) invokes `cmd.run?.({ args: mergedArgs, rawArgs: [], cmd })`. The KEY POINT: a verb reached via the manual loader-table path (bare / next / resume) must see `yolo:true` in its `args` EXACTLY as if citty had parsed `--yolo` on an explicit invocation — otherwise its OWN approval-gate-skip (compile/done) never engages even though the cost-cap pre-flight ran. EVERY manual dispatch below goes through dispatchVerb and passes the parsed globalFlags; NONE call `cmd.run()` with a bare/empty args object.
    - next.ts (nextCommand): call resolveNextAction(process.cwd()), then dispatch to the resolved verb via dispatchVerb(decision.verb, { args: <decision n/slug if any>, globalFlags: <the parsed global flags incl. yolo> }) so the resolved verb receives the forwarded flags (C3-HIGH-2). If the resolved decision is { verb:'status', reason:'attention' } (a corrupt STATE.json per C4-HIGH), dispatch to status so the user sees the diagnostic — do NOT throw. next declares the same four global flags in its OWN defineCommand args (so `pensmith next --yolo` parses), reads them via citty `args`, and threads them into dispatchVerb's globalFlags. Route the "resolved verb" diagnostic to STDERR (not stdout) so stdout stays byte-equivalent to the underlying verb (tier/scripting parity). Do NOT introduce a 17th verb.
    - status.ts (statusCommand): loadState + walk section PLAN.md frontmatter (reuse the router's read helpers or parseFrontmatter) and print a status table (phase, per-section state, next action from resolveNextAction). Handle StateNotFoundError -> print a "no active paper — run pensmith new" message; handle a CORRUPT/schema-invalid STATE.json (any other load error) -> print a "STATE.json is unreadable/corrupt — inspect or restore" message and exit cleanly (status must itself never crash — it is the C4-HIGH attention terminus).
    - resume.ts (resumeCommand) — H4 LIFECYCLE + C3-HIGH-2 FLAG FORWARD: resume declares the same four global flags in its OWN defineCommand args (so `pensmith resume --yolo` parses) and reads them via citty `args`. Read .paper/HANDOFF.json via HandoffSchema.safeParse. If present + phase!=='done', print the resume summary (stderr per parity), then compute the NEXT WORK VERB via resolveNextAction(process.cwd()) — which IGNORES HANDOFF, so it returns plan/write/verify/compile/done, NEVER 'resume' (no re-entry). Dispatch to that work verb via dispatchVerb(decision.verb, { args: <decision n/slug if any>, globalFlags: { yolo: args.yolo === true, ... } }) so e.g. `pensmith resume --yolo` resolving to compile/done forwards yolo:true and the verb skips its OWN approval gate (C3-HIGH-2). AFTER handing off, CONSUME the HANDOFF: best-effort delete .paper/HANDOFF.json (rmSync(path,{force:true}) in a try/catch) so a stale pointer cannot re-trigger resume next time. If HANDOFF absent -> just resolveNextAction + dispatchVerb (still forwarding flags). resume MUST never dispatch to itself.

    Edit bin/pensmith.ts:
    1. Add next/status/resume to REAL_VERB_LOADERS (the documented single edit point) pointing at the new ./cli/*.js modules. Do NOT add any key outside UX02_VERBS. EXPORT REAL_VERB_LOADERS AND the shared `dispatchVerb` helper (the flag-forwarding dispatch contract above) so next/resume AND the bare path all reuse ONE dispatch path that forwards global flags (C3-HIGH-2).
    2. Add the four global flags to the root defineCommand `args`: 'dry-run', 'estimate', 'yolo', 'show-prompts' (all boolean, default false) per 07-PATTERNS flag-declaration pattern. Keep the root command with subCommands ONLY and NO run() (H2 — citty falls through to a root run() after every verb).
    3. H2 PRE-DISPATCH SEAM — add a manual argv pre-parse BEFORE the runMain call inside the existing pathToFileURL main-guard:
       a. Scan process.argv for --show-prompts / --dry-run / --estimate / --yolo (handle them as global flags that may appear in ANY position, including after the verb, e.g. pensmith write --dry-run). This pre-parse runs BEFORE any subcommand, so the flags take effect for explicit verbs (fixes H2(b)).
       b. If --show-prompts present -> setMirrorPromptsToStderr(true). If --dry-run present -> set process.env.PENSMITH_NETWORK_TESTS='' AND process.env.PENSMITH_NO_LLM='1' AND process.env.PENSMITH_DRY_RUN='1'. PENSMITH_NO_LLM is the LOAD-BEARING LLM gate (verify/pass2.ts + pass4.ts already honor it — zero messages.create(), zero COSTS.jsonl). PENSMITH_DRY_RUN is an advisory marker only. Do NOT add or reference a runtime.ts model-call seam (none exists, C2-H3).
       c. H1 / C2-H1 YOLO CAP PRE-FLIGHT — run this WHENEVER --yolo is present, INDEPENDENT of --estimate, for ANY command line (every verb incl. non-gate write/plan/verify AND bare invocation — NOT scoped to gate-skipping verbs). Resolve the configured cap: read process.env.PENSMITH_COST_CAP_USD (D-32 override) if a finite >0 number, else default 5.0 — do NOT reference a `cost_cap_usd` runtime-config field (the schema has none; C2-M3). Call projectEstimate({ paperRoot: process.cwd(), sessionCapUsd: <configured cap> }); if result.exceedsHalfCap -> write the refusal to stderr and process.exit(1) (hard refusal, ARCH-11 — NOT advisory, NOT nested inside any --estimate branch). projectEstimate is guarded against ALL load errors (Task 2), so --yolo in a paper-less dir OR against a corrupt STATE.json sees an empty estimate (exceedsHalfCap:false) and does NOT crash.
       d. If --estimate present (regardless of --yolo) -> call projectEstimate, print the projection table to stdout, then exit 0 WITHOUT executing a verb (estimate is a preview). The H1 refusal in (c) already fired before this if --yolo + over-cap.
       e. Determine whether an explicit verb is present: find the first non-flag argv token and check membership in UX02_VERBS. If a verb IS present -> call runMain(command) exactly once (citty dispatches the verb; there is NO root run() to fall through into, so the verb runs exactly once — fixes H2(a); on the explicit path citty itself parses --yolo into the subcommand's args, so no manual forwarding is needed there). If NO verb is present (bare invocation) -> call resolveNextAction(process.cwd()) and dispatch to the resolved verb via `dispatchVerb(decision.verb, { args: <decision n/slug>, globalFlags: { yolo, dryRun, estimate, showPrompts } })` using the flags parsed in (a) — so e.g. bare `pensmith --yolo` resolving to compile/done forwards yolo:true and the verb skips its OWN approval gate, exactly as if invoked explicitly (C3-HIGH-2). resolveNextAction never throws (C4-HIGH), so even bare `pensmith` against a corrupt STATE.json dispatches to status/attention rather than crashing. Do NOT also call runMain — that would double-dispatch, and citty throws 'No command specified' on a bare runMain.
    Note (H2): the root command keeps subCommands only and NO run(). Bare routing lives in the pre-parse wrapper, gated on "no subcommand matched," so an explicit verb can never trigger a second router dispatch. The --yolo over-cap refusal MUST be a hard exit(1) and MUST cover non-gate verbs (C2-H1).
    Note (C3-HIGH-2): EVERY manually-dispatched path (bare, next, resume) routes through the single `dispatchVerb` helper and forwards the parsed global flags into the dispatched verb's `args` (at minimum `yolo`). A manual dispatch MUST NEVER invoke `cmd.run()` with an empty/bare args object — that is the exact bug where `pensmith --yolo`→compile or `pensmith resume --yolo` ran the financial cap pre-flight but the verb's OWN approval-gate-skip never engaged. The explicit-verb path (runMain) needs no manual forwarding because citty parses --yolo into the subcommand args directly.
  </action>
  <verify>
    <automated>node --import tsx --test tests/flags.test.ts tests/cli-verbs.test.ts tests/pensmith-router.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: REAL_VERB_LOADERS has next/status/resume and is exported/reused by next+resume; bin/pensmith.ts ALSO exports a shared `dispatchVerb(verb,{args,globalFlags})` helper that MERGES forwarded global flags (at minimum `yolo: globalFlags.yolo === true`) into the dispatched verb's args before calling cmd.run(); the bare path (step 3e), next.ts, and resume.ts ALL dispatch through dispatchVerb passing the parsed globalFlags (C3-HIGH-2 — no manual dispatch invokes cmd.run() with a bare/empty args object); root defineCommand declares dry-run/estimate/yolo/show-prompts and has NO run(); the argv pre-parse (NOT a root run()) applies flag setup BEFORE dispatch; --dry-run sets PENSMITH_NETWORK_TESTS='' + PENSMITH_NO_LLM='1' + PENSMITH_DRY_RUN='1'; the yolo cap pre-flight calls projectEstimate whenever --yolo is present for ANY verb (not scoped to gate verbs, not inside the --estimate branch) and reads the cap from PENSMITH_COST_CAP_USD/$5; resume.ts deletes HANDOFF.json after handing off and dispatches to resolveNextAction's WORK verb; status.ts handles BOTH StateNotFoundError and a corrupt-STATE load error without crashing (C4-HIGH attention terminus)
    - behavior (H1/C2-H1): pensmith write --yolo and pensmith plan --yolo (non-gate) AND pensmith compile --yolo AND bare pensmith --yolo, WITHOUT --estimate, all exit non-zero when over 50% cap (flags.test.ts H1 case GREEN); under-cap --yolo exits 0; --yolo in a paper-less dir does NOT crash (exit 0, no StateNotFoundError); --yolo against a corrupt STATE.json does NOT crash (exit 0, empty projection — C4-HIGH)
    - behavior (H2): an explicit verb runs EXACTLY once (no second router dispatch in stdout); --show-prompts/--dry-run take effect for explicit verbs (flags.test.ts H2 case GREEN)
    - behavior (C3-HIGH-2): a verb dispatched via the BARE path and via `pensmith resume --yolo` RECEIVES yolo:true in its args and skips its OWN approval gate (not just the cost-cap pre-flight) — flags.test.ts C3-HIGH-2 case GREEN; the same un-forwarded plan would leave the approval gate active and FAIL the assertion
    - behavior (H3/C2-H3): with PENSMITH_NO_LLM set by --dry-run, a verb path that WOULD call the LLM (verify) makes zero network calls and writes no COSTS.jsonl line (flags.test.ts H3 case GREEN)
    - behavior (H4): with a non-done HANDOFF, bare pensmith resolves to the next WORK verb and resume hands off then clears HANDOFF.json (no resume->resume loop; pensmith-router.test.ts H4 case GREEN)
    - behavior (C4-HIGH): bare pensmith against a corrupt/schema-invalid STATE.json resolves to status/attention and exits cleanly (no crash, no unhandled throw)
    - behavior: --dry-run leaves isOfflineMode()===true; cli-verbs.test.ts still sees exactly 16 verbs
    - test-command: node --import tsx --test tests/flags.test.ts tests/cli-verbs.test.ts tests/pensmith-router.test.ts exits 0, no failures
  </acceptance_criteria>
  <done>next/status/resume execute as real verbs via the shared loader table; bare pensmith is state-aware and never loops on resume; flag setup happens in a pre-dispatch seam so explicit verbs honor --show-prompts/--dry-run and run exactly once; the bare/next/resume manual-dispatch paths all route through the shared dispatchVerb helper that FORWARDS global flags (≥ yolo) into the dispatched verb's args so a manually-dispatched compile/done skips its OWN approval gate exactly as on the explicit path (C3-HIGH-2); --dry-run sets PENSMITH_NO_LLM so the existing LLM call sites make zero egress (H3, no runtime.ts edit); --yolo refuses over-50%-cap with a hard exit(1) for ANY verb WITHOUT needing --estimate and WITHOUT crashing in a paper-less dir OR against a corrupt STATE.json (H1/C2-H1/C4-HIGH); status + bare routing both survive a corrupt STATE.json (C4-HIGH); resume consumes/clears HANDOFF; the 16-verb bijection is intact.</done>
</task>

<task type="auto">
  <name>Task 4: --dry-run gates the LLM call sites via PENSMITH_NO_LLM — zero network egress on verb paths, no Phase-5 edits (ERGO-01 — fixes H3/C2-H3)</name>
  <read_first>
    - bin/lib/verify/pass2.ts:210-290 (runPass2 — the noLlm guard at line 215 that --dry-run sets; CONFIRM no edit is needed)
    - bin/lib/verify/pass4.ts:386-490 (runPass4 — the noLlm guard at line 392; CONFIRM no edit is needed)
    - bin/lib/runtime.ts (CONFIRM: config/API-key load ONLY — there is NO model-invocation seam to add a dry-run guard to; do NOT edit this file)
    - bin/lib/http-mock.ts isOfflineMode (the SOURCE-adapter gate --dry-run also sets via PENSMITH_NETWORK_TESTS='')
    - bin/lib/budget.ts appendCost / COSTS.jsonl path (the H3 test asserts no COSTS.jsonl append — under PENSMITH_NO_LLM the pass2/pass4 placeholder path never calls appendCost)
    - bin/cli/verify.ts (the verb path the H3 test drives under --dry-run WITH a fake ANTHROPIC_API_KEY present)
    - 07-RESEARCH CORRECTED "--dry-run Flag Implementation" + Assumption A3 (the LLM gate is PENSMITH_NO_LLM, honored by the EXISTING call sites — not a runtime.ts seam)
  </read_first>
  <files></files>
  <action>
    OPTION 1 CHOSEN (minimal, reuses the existing guard — no Phase-5 verify code changes). There is NO model-invocation function in bin/lib/runtime.ts and NO canonical LLM chokepoint in bin/lib/; the ONLY live messages.create() calls are in bin/lib/verify/pass2.ts (runPass2) and bin/lib/verify/pass4.ts (runPass4), BOTH already gated on `process.env['PENSMITH_NO_LLM'] === '1' || !process.env['ANTHROPIC_API_KEY']`. Therefore the dry-run LLM gate is implemented ENTIRELY by Task 3 setting `process.env.PENSMITH_NO_LLM='1'` in the pre-dispatch seam (already specced in Task 3.b). This task is the CONFIRMATION + WIRING-VERIFICATION step — it adds NO new files and edits NO Phase-5 code:
    1. CONFIRM by reading pass2.ts:215 + pass4.ts:392 that the noLlm short-circuit returns the offline placeholder with zero messages.create() and zero appendCost — so once PENSMITH_NO_LLM='1' is set, `verify <N> --dry-run` makes zero egress even WITH a real ANTHROPIC_API_KEY present.
    2. CONFIRM bin/lib/runtime.ts is config-load only (no model-invocation seam) — do NOT add a dry-run guard there; the false "runtime.ts model-call seam" claim is removed from 07-RESEARCH/07-PATTERNS.
    3. Ensure the H3 RED test in 07-01 (tests/flags.test.ts) drives `verify <N> --dry-run` WITH a fake ANTHROPIC_API_KEY set in the child env (a path that WOULD call the LLM absent the guard — NON-VACUOUS) and asserts ZERO network egress + no COSTS.jsonl append. This is the regression gate that the PENSMITH_NO_LLM wiring in Task 3 satisfies.
    Why NOT Option 2 (new canonical model-call chokepoint + migrate pass2/pass4): Option 1 already achieves "zero external calls" for ALL current LLM call sites with no Phase-5 code change, so the durable-chokepoint refactor is unjustified scope this phase. If a future phase adds LLM call sites OUTSIDE pass2/pass4, revisit a single guarded wrapper then.
    NET: this task ships NO code of its own — its deliverable is that the H3 behavior is satisfied by Task 3's env-set + the existing guards, and that the plan/research/patterns no longer claim a fictional runtime.ts seam. files_modified is empty for this task by design.
  </action>
  <verify>
    <automated>node --import tsx --test tests/flags.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: NO edits to bin/lib/runtime.ts, bin/lib/verify/pass2.ts, or bin/lib/verify/pass4.ts (the dry-run LLM gate is the env var PENSMITH_NO_LLM, set in Task 3, honored by the existing guards)
    - behavior: the H3 case in flags.test.ts un-skips and PASSES — `verify <N> --dry-run` with a fake ANTHROPIC_API_KEY present makes zero network egress and writes no COSTS.jsonl line; the live (non-dry-run) path is unchanged
    - test-command: node --import tsx --test tests/flags.test.ts exits 0, no failures (H3 case GREEN)
  </acceptance_criteria>
  <done>--dry-run gates the LLM call sites via PENSMITH_NO_LLM (the signal pass2/pass4 already honor); a verb path under --dry-run makes zero external calls and bills nothing; NO Phase-5 verify code or runtime.ts is modified; the H3 RED test (driving the LLM-calling verify path, non-vacuously) is GREEN; the false runtime.ts-seam claim is removed from the docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem (.paper) → router/estimator | STATE.json + PLAN.md + HANDOFF.json are read as untrusted local files; parsed via Zod safeParse |
| CLI args → process env | --dry-run mutates PENSMITH_NETWORK_TESTS / PENSMITH_NO_LLM / PENSMITH_DRY_RUN before any dispatch |
| --estimate path → billing ledger | estimator must never reach the LLM/network/COSTS.jsonl |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | HANDOFF.json / STATE.json parsing | mitigate | router uses HandoffSchema.safeParse + try/catch; malformed input falls through, never crashes (Pitfall 5) |
| T-07-06 | Information Disclosure | --estimate must not bill | mitigate | estimator.ts imports only pricing.ts + state.ts (pure math + read); no runtime LLM-call path and no verify/pass2|pass4 import (Pitfall 8) |
| T-07-07 | Elevation of Privilege | --yolo gate bypass (H1/C2-H1) | mitigate | the >50%-cap refusal is a hard process.exit(1) run in the PRE-DISPATCH seam WHENEVER --yolo is present, for ANY verb (incl. non-gate write/plan/verify) and bare invocation, independent of --estimate — not advisory, not nested, not scoped to gate verbs (ARCH-11) |
| T-07-08 | Spoofing | --dry-run bypassing cassette gate | mitigate | --dry-run sets PENSMITH_NETWORK_TESTS='' (never '1') so isOfflineMode() stays true (Security Domain control) |
| T-07-09 | Tampering | 16-verb bijection | mitigate | loaders added only for keys already in UX02_VERBS; cli-verbs.test.ts asserts the count stays 16 |
| T-07-18 | Tampering | citty root run() double-dispatch (H2) | mitigate | flag setup + bare routing live in a pre-dispatch argv pre-parse, NOT a root run(); explicit verbs run via runMain exactly once; flags.test.ts asserts single dispatch + flags-for-explicit-verb |
| T-07-19 | Information Disclosure | --dry-run LLM network leak (H3/C2-H3) | mitigate | --dry-run sets PENSMITH_NO_LLM='1'; the EXISTING LLM call sites (verify/pass2.ts:215 + pass4.ts:392) short-circuit to the offline placeholder — zero messages.create(), zero COSTS.jsonl; flags.test.ts drives `verify --dry-run` with a key present and asserts zero egress (NON-vacuous). No fictional runtime.ts seam. |
| T-07-20 | Denial of Service | resume/router circularity (H4) | mitigate | resolveNextAction ignores HANDOFF and never returns 'resume'; resume dispatches to the next WORK verb and clears HANDOFF.json; pensmith-router.test.ts asserts verb !== 'resume' |
| T-07-21 | Denial of Service | --yolo crash in paper-less dir (C2-H1) | mitigate | projectEstimate catches StateNotFoundError → empty projection; the yolo pre-flight sees under-cap and exits 0 instead of throwing |
| T-07-24 | Denial of Service | corrupt/schema-invalid STATE.json crashes router + estimator (C4-HIGH) | mitigate | loadState translates ONLY ENOENT; both resolveNextAction and projectEstimate now CATCH-ALL on loadState — StateNotFoundError → new/empty, any OTHER load error (SyntaxError/SchemaValidationError/ForwardIncompatError/EACCES) → status/attention + empty projection, never re-thrown; state.sections guarded `?? []` before .sort(); pensmith-router.test.ts + estimator.test.ts cover the invalid-JSON + schema-invalid cases so SC1 'never crash' holds across the FULL STATE.json failure surface |
| T-07-SC | Tampering | npm installs | accept | No new packages (07-RESEARCH audit: none) |
</threat_model>

<verification>
- `node --import tsx --test tests/pensmith-router.test.ts tests/estimator.test.ts tests/flags.test.ts tests/cli-verbs.test.ts` → all GREEN, no skips on router/estimator
- `npm run check` (lint + typecheck + build + tier-contract + tests + manifests) → GREEN
- `verify <N> --dry-run` with a fake ANTHROPIC_API_KEY present makes zero network calls and appends no COSTS.jsonl line (H3/C2-H3 — flags.test.ts drives it; the gate is PENSMITH_NO_LLM, not a runtime.ts seam)
- `pensmith write --yolo` / `plan --yolo` / `compile --yolo` / bare `pensmith --yolo` over 50% cap all exit non-zero; `pensmith --yolo` in a paper-less dir does not crash (H1/C2-H1); `pensmith --yolo` against a corrupt STATE.json does not crash (C4-HIGH — empty projection, exit 0)
- Bare `node dist/bin/pensmith.js` in a fixture dir routes to the correct WORK verb even with a non-done HANDOFF present (H4 — never loops on resume); router test covers the logic
- `resolveNextAction` is TOTAL over SectionStateSchema: every section state (incl. failed/unverifiable → verify) maps to a concrete decision and the function NEVER returns undefined; the `[verified, failed, verified]`-no-DRAFT case routes to `verify` (C3-HIGH-1 — pensmith-router.test.ts covers every state)
- `resolveNextAction` is TOTAL over the STATE.json LOAD-FAILURE surface too: an absent file → new, a present-but-corrupt (invalid JSON) and a schema-invalid (sections entry missing slug) STATE.json → status/attention WITHOUT throwing; projectEstimate returns the empty projection in all three cases (C4-HIGH — pensmith-router.test.ts + estimator.test.ts cover the corrupt cases)
- Bare `pensmith --yolo` and `pensmith resume --yolo` resolving to compile/done FORWARD yolo:true into the dispatched verb so its own approval gate is skipped, not just the cost-cap pre-flight (C3-HIGH-2 — flags.test.ts covers the bare + resume paths)
</verification>

<success_criteria>
- bin/lib/router.ts + bin/lib/estimator.ts exist and their RED tests are now GREEN; router ignores HANDOFF and never returns 'resume' (H4)
- next/status/resume promoted to real verbs in REAL_VERB_LOADERS; resume hands off to the next WORK verb and clears HANDOFF (H4)
- Four global flags declared + wired in a PRE-DISPATCH seam (not a root run()); explicit verbs run exactly once and honor the flags (H2)
- --yolo refuses over-50%-cap estimates with a hard non-zero exit WHENEVER --yolo is present for ANY verb, independent of --estimate, and does not crash in a paper-less dir OR against a corrupt STATE.json (H1/C2-H1/C4-HIGH)
- --dry-run sets PENSMITH_NO_LLM='1' so the existing LLM call sites make zero network calls and bill nothing (H3/C2-H3); no runtime.ts/pass2/pass4 edits
- UX02_VERBS.length===16 preserved (no 17th verb)
- resolveNextAction is TOTAL over SectionStateSchema — every section state (incl. failed/unverifiable, routed to verify) maps to a concrete RouterDecision; never returns undefined (C3-HIGH-1)
- resolveNextAction AND projectEstimate are TOTAL over the FULL on-disk STATE.json surface — absent (→new/empty), well-typed (→correct verb/projection), AND malformed/schema-invalid (→status/attention/empty) — neither EVER throws; state.sections null/undefined is guarded before any .sort() (C4-HIGH)
- the bare/next/resume manual-dispatch paths forward global flags (≥ yolo) into the dispatched verb's args via the shared dispatchVerb helper, so a manually-dispatched compile/done skips its own approval gate (C3-HIGH-2)
</success_criteria>

<output>
Create `.planning/phases/07-single-command-ux-layer-hooks-flags/07-02-SUMMARY.md` when done
</output>
</output>
</output>
