---
phase: 07-single-command-ux-layer-hooks-flags
plan: 02
type: execute
wave: 1
depends_on: ["07-01"]
files_modified:
  - bin/lib/router.ts
  - bin/lib/estimator.ts
  - bin/lib/runtime.ts
  - bin/cli/next.ts
  - bin/cli/status.ts
  - bin/cli/resume.ts
  - bin/pensmith.ts
autonomous: true
requirements: [UX-01, UX-02, ERGO-01, ERGO-02, ERGO-03, ERGO-04]
must_haves:
  truths:
    - "Bare `pensmith` (no verb) resolves a state-aware next WORK action from STATE.json + PLAN.md frontmatter — intake / research / outline / next section / compile / done (HANDOFF is ignored by routing per H4)"
    - "`pensmith next`, `pensmith status`, `pensmith resume` all execute as real verbs in Tier 2 (no longer stubs); resume hands off to the next WORK verb and clears HANDOFF (never loops on itself — H4)"
    - "`--estimate` prints a token + USD projection table without any LLM or network call"
    - "`--yolo` refuses (hard exit non-zero) when the projected cost exceeds 50% of the session cap — the pre-flight runs WHENEVER --yolo is active, independent of --estimate, on the gate-skipping verbs (H1)"
    - "Global flags are applied in a PRE-DISPATCH seam (not a root run()), so an explicit verb runs EXACTLY once and --dry-run/--show-prompts take effect for explicit verbs (H2)"
    - "`--dry-run` forces offline mode for SOURCE adapters AND gates the LLM CLIENT at the runtime seam so a verb path makes ZERO network calls (H3); `--show-prompts` mirrors every prompt to stderr"
    - "The locked 16-verb set is unchanged — no 17th verb is added"
  artifacts:
    - path: "bin/lib/router.ts"
      provides: "Pure resolveNextAction() decision table over STATE.json + PLAN.md frontmatter; IGNORES HANDOFF and never returns 'resume' (H4)"
      exports: ["resolveNextAction", "RouterDecision"]
    - path: "bin/lib/estimator.ts"
      provides: "projectEstimate() pure cost projection + 50%-cap predicate"
      exports: ["projectEstimate", "EstimateResult"]
    - path: "bin/cli/next.ts"
      provides: "next verb — thin orchestrator calling resolveNextAction()"
      exports: ["nextCommand"]
    - path: "bin/cli/status.ts"
      provides: "status verb — state display"
      exports: ["statusCommand"]
    - path: "bin/cli/resume.ts"
      provides: "resume verb — HANDOFF-aware resume"
      exports: ["resumeCommand"]
    - path: "bin/pensmith.ts"
      provides: "pre-dispatch flag seam (NOT root run()); 4 global flags; yolo cap pre-flight; bare→router dispatch gated on no-verb; 3 new verb loaders; exported REAL_VERB_LOADERS"
      contains: "resolveNextAction"
    - path: "bin/lib/runtime.ts"
      provides: "LLM-client dry-run guard — when PENSMITH_DRY_RUN==='1' the model seam returns a canned/cassette stub with zero network egress + zero billing (H3)"
      contains: "PENSMITH_DRY_RUN"
  key_links:
    - from: "bin/pensmith.ts"
      to: "bin/lib/router.ts"
      via: "pre-dispatch wrapper calls resolveNextAction on bare invocation (no verb)"
      pattern: "resolveNextAction"
    - from: "bin/pensmith.ts"
      to: "bin/lib/session-log.ts"
      via: "--show-prompts → setMirrorPromptsToStderr(true) in the pre-dispatch seam"
      pattern: "setMirrorPromptsToStderr"
    - from: "bin/pensmith.ts"
      to: "bin/lib/estimator.ts"
      via: "yolo cap pre-flight calls projectEstimate whenever --yolo is active (H1)"
      pattern: "projectEstimate"
    - from: "bin/pensmith.ts"
      to: "process.env.PENSMITH_DRY_RUN"
      via: "--dry-run sets PENSMITH_DRY_RUN='1' BEFORE dispatch (consumed by runtime seam, H3)"
      pattern: "PENSMITH_DRY_RUN"
    - from: "bin/lib/runtime.ts"
      to: "bin/lib/http-mock.ts"
      via: "dry-run guard returns a canned/cassette stub instead of a live LLM call (H3)"
      pattern: "PENSMITH_DRY_RUN"
    - from: "bin/lib/estimator.ts"
      to: "bin/lib/pricing.ts"
      via: "estimateCost() per step"
      pattern: "estimateCost"
    - from: "bin/cli/resume.ts"
      to: "bin/lib/router.ts"
      via: "resume dispatches to resolveNextAction's WORK verb then clears HANDOFF (H4)"
      pattern: "resolveNextAction"
    - from: "bin/cli/next.ts"
      to: "bin/lib/router.ts"
      via: "resolveNextAction call"
      pattern: "resolveNextAction"
---

<objective>
Build the state-aware bare `/pensmith` router and the `--estimate` projector, promote the `next` / `status` / `resume` stub verbs to real implementations, wire the four global flags (`--dry-run`, `--estimate`, `--yolo`, `--show-prompts`) into a PRE-DISPATCH seam in `bin/pensmith.ts` (NOT a root run() — citty falls through to it after every verb, per corrected Pitfall 4 / review H2), and gate the LLM client on `--dry-run` at the runtime seam.

Purpose: UX-01 (bare command resolves the next step) is the headline of Phase 7. The router is a PURE FUNCTION over STATE.json + PLAN.md frontmatter (no FSM library) that IGNORES HANDOFF so bare /pensmith never loops on resume (H4); the estimator is a pure cost projection (no LLM call). The four flags must apply to explicit verbs too (H2), the --yolo >50%-cap refusal must fire whenever --yolo is active independent of --estimate (H1), and --dry-run must gate the LLM CLIENT (not just source adapters) so a verb path makes zero network calls (H3).
Output: 2 new lib modules (router, estimator), an LLM-client dry-run guard in runtime.ts, 3 new CLI verb files, and the upgraded dispatcher with a pre-dispatch flag seam.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-single-command-ux-layer-hooks-flags/07-RESEARCH.md
@.planning/phases/07-single-command-ux-layer-hooks-flags/07-PATTERNS.md
@.planning/phases/07-single-command-ux-layer-hooks-flags/07-01-SUMMARY.md

<interfaces>
<!-- Contracts to implement. The RED tests in 07-01 assert exactly these. -->

From bin/lib/state.ts:
```typescript
export class StateNotFoundError extends Error { code: 'STATE_NOT_FOUND'; }
export function loadState(paperRoot: string): Promise<State>;  // throws StateNotFoundError on ENOENT
// State has: sections?: Array<{ n: number; slug: string; ... }>, phase?, etc.
```

From bin/lib/paths.ts: `paperDir(root)`, `sectionPlan(n, slug, root)` (section PLAN.md path helpers).
From bin/lib/frontmatter.ts: `parseFrontmatter(text) => { frontmatter, body }`.
From bin/lib/schemas/handoff.ts: `HandoffSchema`, `type Handoff` (snake_case; fields phase, next_action, current_section, section_pointers[].state).
From bin/lib/pricing.ts: `estimateCost({ providerId, modelId, inputTokens, outputTokens }): number` (pure, no IO).
From bin/lib/http-mock.ts: `isOfflineMode(): boolean` (returns `process.env.PENSMITH_NETWORK_TESTS !== '1'`).
From bin/lib/session-log.ts: `setMirrorPromptsToStderr(enabled: boolean): void`.
From bin/lib/verbs.ts: `UX02_VERBS` (the locked 16), `type Ux02Verb`.

Router contract (this plan implements):
```typescript
export type RouterDecision =
  | { verb: 'new' } | { verb: 'research' } | { verb: 'outline' }
  | { verb: 'plan'; n: number; slug: string }
  | { verb: 'write'; n: number; slug: string }
  | { verb: 'verify'; n: number; slug: string }
  | { verb: 'compile' } | { verb: 'done' }
  | { verb: 'status'; reason: 'done' }
  | { verb: 'resume'; handoff: Handoff };
export function resolveNextAction(paperRoot: string): Promise<RouterDecision>;
```

Estimator contract (this plan implements):
```typescript
export interface EstimateRow { step: string; inputTokens: number; outputTokens: number; usd: number; }
export interface EstimateResult { rows: EstimateRow[]; totalUsd: number; exceedsHalfCap: boolean; }
export function projectEstimate(args: { paperRoot: string; sessionCapUsd?: number }): Promise<EstimateResult>;
```

Existing thin-orchestrator analog (copy structure): bin/cli/compile.ts (defineCommand, stdout-only, runX delegation).
</interfaces>

@bin/pensmith.ts
@bin/cli/compile.ts
@tests/pensmith-router.test.ts
@tests/estimator.test.ts
@tests/flags.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bin/lib/router.ts — pure state-aware decision table (UX-01)</name>
  <read_first>
    - bin/lib/state.ts loadState + StateNotFoundError (catch → 'new', do NOT re-throw)
    - bin/lib/paths.ts paperDir + sectionPlan helpers
    - bin/lib/frontmatter.ts parseFrontmatter
    - bin/lib/schemas/handoff.ts HandoffSchema + Handoff type
    - 07-RESEARCH "Bare /pensmith State-Aware Router Decision Table" + the resolveNextAction Code Example
    - 07-PATTERNS "bin/lib/router.ts" section (StateNotFoundError catch pattern, file-existence pattern, parseFrontmatter pattern)
  </read_first>
  <files>bin/lib/router.ts</files>
  <behavior>
    H4 PINNED ORDERING — resolveNextAction IGNORES HANDOFF.json and NEVER returns { verb:'resume' }. It is the pure next-WORK-verb resolver. Order:
    - No STATE.json (loadState throws StateNotFoundError) → { verb:'new' } (caught via instanceof, not re-thrown)
    - STATE.json present, no .paper/RESEARCH.md → { verb:'research' }
    - RESEARCH.md present, no .paper/OUTLINE.md (or zero sections) → { verb:'outline' }
    - Sections present: walk sorted by n; first PLAN.md with status 'planned' → plan; 'writing' → write; 'written'|'verifying' → verify; verified/failed/unverifiable → continue
    - All sections verified, no DRAFT.md → { verb:'compile' }; DRAFT.md present, no FINAL.md → { verb:'done' }; both present → { verb:'status', reason:'done' }
    - HANDOFF.json is NOT read or returned here (H4): a non-done HANDOFF must resolve to the next WORK verb (plan/write/verify/compile/done), so bare /pensmith always advances and never loops on resume. The { verb:'resume' } arm exists in RouterDecision ONLY for the explicit `resume` verb's own typing (Task 3), produced by bin/cli/resume.ts — never by resolveNextAction.
  </behavior>
  <action>
    Implement resolveNextAction(paperRoot) as a PURE FUNCTION per the 07-RESEARCH "PINNED ORDERING" (H4). Read-only: use existsSync + readFileSync + loadState; never write. Catch StateNotFoundError specifically (instanceof) and return { verb:'new' } — re-throw any other error. DO NOT read or branch on HANDOFF.json here (H4): resolveNextAction must NEVER return { verb:'resume' }; it always resolves a concrete next WORK verb. Apply the checks in the pinned order: new → research → outline → section walk → compile → done → status(done). Walk state.sections sorted ascending by n; resolve PLAN.md path with sectionPlan(n, slug, paperRoot) and read status via parseFrontmatter. Use the section-state vocabulary from SectionStateSchema (planned/writing/written/verifying/verified/failed/unverifiable). Export RouterDecision (discriminated union — keep the { verb:'resume'; handoff } member for the explicit `resume` verb's typing, but never emit it from this function) and resolveNextAction. No FSM library; no console.* (this is a lib).
  </action>
  <verify>
    <automated>node --import tsx --test tests/pensmith-router.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: bin/lib/router.ts exports resolveNextAction + RouterDecision; catches StateNotFoundError with instanceof and returns { verb:'new' }; does NOT read HANDOFF.json and never returns { verb:'resume' } (H4)
    - behavior: tests/pensmith-router.test.ts un-skips (module now exists) and all decision-table cases PASS, INCLUDING the H4 case (non-done HANDOFF present → resolves to the next WORK verb, decision.verb !== 'resume')
    - test-command: `node --import tsx --test tests/pensmith-router.test.ts` exits 0, no skips, no failures
  </acceptance_criteria>
  <done>Router resolves all decision-table branches in the H4-pinned order; StateNotFoundError → 'new' without crashing in a fresh dir; a non-done HANDOFF never traps routing (verb !== 'resume'); router test GREEN including the H4 case.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bin/lib/estimator.ts — pure token+USD projection + 50%-cap predicate (ERGO-02, ERGO-03)</name>
  <read_first>
    - bin/lib/pricing.ts estimateCost signature + MODEL_PRICES (pure, no IO)
    - bin/lib/state.ts loadState (read sections to count write/verify/plan steps)
    - bin/lib/router.ts resolveNextAction (reuse to know which steps remain)
    - 07-RESEARCH "--estimate Flag Implementation" + Pitfall 8 (estimate must NOT trigger real LLM)
    - 07-PATTERNS "bin/lib/estimator.ts" section (STEP_HEURISTICS frozen table + 50%-cap pattern)
  </read_first>
  <files>bin/lib/estimator.ts</files>
  <behavior>
    - projectEstimate returns { rows, totalUsd, exceedsHalfCap }
    - totalUsd === sum of rows[].usd (each row usd === estimateCost(step heuristic))
    - exceedsHalfCap === (totalUsd > sessionCapUsd * 0.5); sessionCapUsd defaults to 5.0 BUT when a configured cap (cost_cap_usd) is available it is passed in by the caller and used (so a lowered cap tightens the H1 refusal threshold — review L1, material to H1 correctness)
    - Zero network calls, zero LLM calls, zero COSTS.jsonl writes during projection
    - Per-section steps (plan/write/verify) multiplied by the section count from STATE.json
  </behavior>
  <action>
    Implement projectEstimate as a pure projection. Define a frozen STEP_HEURISTICS table (new/research/outline/plan/write/verify/compile/done input+output token estimates per 07-PATTERNS; label values as conservative estimates in comments). Read STATE.json via loadState to count sections (plan/write/verify rows are per-section). For each remaining step build an EstimateRow with usd = estimateCost({ providerId, modelId, inputTokens, outputTokens }) using the configured provider+model (read from runtime config defaults; do NOT import any LLM call path — only pricing.ts). Sum to totalUsd; set exceedsHalfCap. Set process.env PENSMITH_DRY_RUN='1' defensively at module entry is NOT needed (no network here) but DO NOT import runtime.ts LLM client paths (Pitfall 8). Export projectEstimate, EstimateRow, EstimateResult.
  </action>
  <verify>
    <automated>node --import tsx --test tests/estimator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: estimator.ts imports estimateCost from pricing.ts and does NOT import any runtime LLM-call path
    - behavior: totalUsd equals row sum; exceedsHalfCap flips at 50% of sessionCapUsd; no COSTS.jsonl written
    - test-command: `node --import tsx --test tests/estimator.test.ts` exits 0, no skips, no failures
  </acceptance_criteria>
  <done>projectEstimate produces a correct projection with the 50%-cap predicate, performs zero billing, and the estimator test is GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Promote next/status/resume verbs + PRE-DISPATCH flag seam + yolo cap pre-flight + HANDOFF lifecycle (UX-01, UX-02, ERGO-01..04 — fixes H1/H2/H4)</name>
  <read_first>
    - bin/pensmith.ts (REAL_VERB_LOADERS map + defineCommand + the pathToFileURL main-guard; the ONLY edit point for loaders)
    - bin/cli/compile.ts + bin/cli/done.ts (thin-orchestrator template AND the existing `yolo` arg + gate-skip pattern the H1 pre-flight must guard)
    - bin/lib/router.ts resolveNextAction (Task 1 — bare dispatch + next/resume call it; it IGNORES HANDOFF per H4)
    - bin/lib/estimator.ts projectEstimate (Task 2 — the cap pre-flight calls it; pass the configured cap)
    - bin/lib/http-mock.ts isOfflineMode + 07-PATTERNS "--dry-run wiring" (set PENSMITH_NETWORK_TESTS='' and PENSMITH_DRY_RUN='1')
    - bin/lib/session-log.ts setMirrorPromptsToStderr + 07-PATTERNS "--show-prompts wiring"
    - bin/lib/schemas/handoff.ts HandoffSchema + Handoff (resume reads it; lifecycle clears it)
    - node_modules/citty/dist/index.mjs:209-228 (H2: subcommand at 217 then parent run() at 228 unconditionally; setup() at 209 runs BEFORE dispatch; `default` runs only when no explicit verb)
    - 07-RESEARCH CORRECTED Pitfall 4 (pre-dispatch seam, NOT root run()) + HANDOFF.json Lifecycle section (H4) + Pitfall 5 (StateNotFoundError -> intake)
  </read_first>
  <files>bin/cli/next.ts, bin/cli/status.ts, bin/cli/resume.ts, bin/pensmith.ts</files>
  <action>
    Create three thin orchestrators copying the bin/cli/compile.ts structure (defineCommand, stdout-only — no console.*, export named command + default). Reuse the SHARED dispatch table: export REAL_VERB_LOADERS from bin/pensmith.ts so next/resume dispatch through it rather than re-importing verb modules — this avoids the circular-dep risk reviewers flagged.
    - next.ts (nextCommand): call resolveNextAction(process.cwd()), then dispatch to the resolved verb via the shared loader table and run it. Route the "resolved verb" diagnostic to STDERR (not stdout) so stdout stays byte-equivalent to the underlying verb (tier/scripting parity). Do NOT introduce a 17th verb.
    - status.ts (statusCommand): loadState + walk section PLAN.md frontmatter (reuse the router's read helpers or parseFrontmatter) and print a status table (phase, per-section state, next action from resolveNextAction). Handle StateNotFoundError -> print a "no active paper — run pensmith new" message.
    - resume.ts (resumeCommand) — H4 LIFECYCLE: read .paper/HANDOFF.json via HandoffSchema.safeParse. If present + phase!=='done', print the resume summary, then compute the NEXT WORK VERB via resolveNextAction(process.cwd()) — which IGNORES HANDOFF, so it returns plan/write/verify/compile/done, NEVER 'resume' (no re-entry). Dispatch to that work verb via the shared loader table. AFTER handing off, CONSUME the HANDOFF: best-effort delete .paper/HANDOFF.json (rmSync(path,{force:true}) in a try/catch) so a stale pointer cannot re-trigger resume next time. If HANDOFF absent -> just resolveNextAction + dispatch. resume MUST never dispatch to itself.

    Edit bin/pensmith.ts:
    1. Add next/status/resume to REAL_VERB_LOADERS (the documented single edit point) pointing at the new ./cli/*.js modules. Do NOT add any key outside UX02_VERBS. Export REAL_VERB_LOADERS (or a small dispatch helper) so next/resume reuse it.
    2. Add the four global flags to the root defineCommand `args`: 'dry-run', 'estimate', 'yolo', 'show-prompts' (all boolean, default false) per 07-PATTERNS flag-declaration pattern.
    3. H2 PRE-DISPATCH SEAM — do NOT add a root run() (citty falls through to it AFTER every verb -> double execution). Instead, add a manual argv pre-parse BEFORE the runMain call inside the existing pathToFileURL main-guard:
       a. Scan process.argv for --show-prompts / --dry-run / --estimate / --yolo (handle them as global flags that may appear in ANY position, including after the verb, e.g. pensmith write --dry-run). This pre-parse runs BEFORE any subcommand, so the flags take effect for explicit verbs (fixes H2(b)).
       b. If --show-prompts present -> setMirrorPromptsToStderr(true). If --dry-run present -> set process.env.PENSMITH_NETWORK_TESTS='' AND process.env.PENSMITH_DRY_RUN='1' (the LLM-client stub in Task 4 reads PENSMITH_DRY_RUN).
       c. H1 YOLO CAP PRE-FLIGHT — run this WHENEVER --yolo is active, INDEPENDENT of --estimate, on the gate-skipping verb paths (compile / done / outline) AND bare invocation: call projectEstimate({paperRoot: process.cwd(), sessionCapUsd: <configured cap>}); if result.exceedsHalfCap -> write the refusal to stderr and process.exit(1) (hard refusal, ARCH-11 — NOT advisory). This must NOT be nested inside any --estimate branch. Scope it to the gate-skipping verbs + bare invocation so plain non-gate verbs are not surprised.
       d. If --estimate present (regardless of --yolo) -> call projectEstimate, print the projection table to stdout, then exit 0 WITHOUT executing a verb (estimate is a preview). The H1 refusal in (c) already fired before this if --yolo + over-cap.
       e. Determine whether an explicit verb is present: find the first non-flag argv token and check membership in UX02_VERBS. If a verb IS present -> call runMain(command) exactly once (citty dispatches the verb; there is NO root run() to fall through into, so the verb runs exactly once — fixes H2(a)). If NO verb is present (bare invocation) -> call resolveNextAction(process.cwd()) and dispatch to the resolved verb via the shared loader table (do NOT also call runMain — that would double-dispatch).
    Note (H2): the root command keeps subCommands only and NO run(). Bare routing lives in the pre-parse wrapper, gated on "no subcommand matched," so an explicit verb can never trigger a second router dispatch. The --yolo over-cap refusal MUST be a hard exit(1).
  </action>
  <verify>
    <automated>node --import tsx --test tests/flags.test.ts tests/cli-verbs.test.ts tests/pensmith-router.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: REAL_VERB_LOADERS has next/status/resume and is exported/reused by next+resume; root defineCommand declares dry-run/estimate/yolo/show-prompts; the argv pre-parse (NOT a root run()) applies flag setup BEFORE dispatch; the yolo cap pre-flight calls projectEstimate whenever --yolo is active (not inside the --estimate branch); resume.ts deletes HANDOFF.json after handing off and dispatches to resolveNextAction's WORK verb
    - behavior (H1): pensmith compile --yolo and bare pensmith --yolo WITHOUT --estimate exit non-zero when over 50% cap (flags.test.ts H1 case GREEN); under-cap --yolo exits 0
    - behavior (H2): an explicit verb runs EXACTLY once (no second router dispatch in stdout); --show-prompts/--dry-run take effect for explicit verbs (flags.test.ts H2 case GREEN)
    - behavior (H4): with a non-done HANDOFF, bare pensmith resolves to the next WORK verb and resume hands off then clears HANDOFF.json (no resume->resume loop; pensmith-router.test.ts H4 case GREEN)
    - behavior: --dry-run leaves isOfflineMode()===true; cli-verbs.test.ts still sees exactly 16 verbs
    - test-command: node --import tsx --test tests/flags.test.ts tests/cli-verbs.test.ts tests/pensmith-router.test.ts exits 0, no failures
  </acceptance_criteria>
  <done>next/status/resume execute as real verbs via the shared loader table; bare pensmith is state-aware and never loops on resume; flag setup happens in a pre-dispatch seam so explicit verbs honor --show-prompts/--dry-run and run exactly once; --yolo refuses over-50%-cap with a hard exit(1) WITHOUT needing --estimate; resume consumes/clears HANDOFF; the 16-verb bijection is intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: --dry-run gates the LLM client at the runtime seam — zero network egress on verb paths (ERGO-01 — fixes H3)</name>
  <read_first>
    - bin/lib/runtime.ts (the LLM-client / model-invocation seam — add the dry-run guard at the single chokepoint where a verb would call the model; runtime.ts is config-load today, so locate/define the model-invocation entry the Tier-2 verbs route through and guard THAT)
    - bin/lib/http-mock.ts isOfflineMode + loadCassetteFile (cassette source for canned responses; the LLM stub returns a canned/cassette response, never a live call)
    - bin/lib/budget.ts assertBudget + COSTS.jsonl append path (the dry-run stub must NOT append a cost record — H3 test asserts no COSTS.jsonl line)
    - bin/cli/write.ts + bin/cli/research.ts (the verb paths the H3 test drives under --dry-run)
    - 07-RESEARCH CORRECTED "--dry-run Flag Implementation" + Assumption A3 (LLM client must honor PENSMITH_DRY_RUN, not just source adapters)
  </read_first>
  <files>bin/lib/runtime.ts</files>
  <behavior>
    - When process.env.PENSMITH_DRY_RUN === '1', the LLM-client seam returns a canned/cassette response and makes ZERO network calls (no undici/http egress, no provider API hit)
    - In dry-run, NO COSTS.jsonl record is appended (the stub path skips billing) — the estimate is the only cost signal under --dry-run
    - When PENSMITH_DRY_RUN is unset, the seam behaves exactly as before (no behavior change to the live path)
    - Driving a verb path (write/research) under --dry-run completes without any external call and without throwing on missing API keys (the stub short-circuits before getProviderApiKey is required)
  </behavior>
  <action>
    Add an explicit dry-run guard at the LLM-client seam in bin/lib/runtime.ts (the single model-invocation chokepoint the Tier-2 verbs use; if the seam is a thin wrapper not yet present, introduce it here as the canonical call point so verbs route through ONE guarded function). At the TOP of that call: if process.env['PENSMITH_DRY_RUN'] === '1', SHORT-CIRCUIT — return a canned/cassette stub response (use loadCassetteFile / a fixed deterministic stub) and make ZERO network calls; do NOT call getProviderApiKey, do NOT touch undici/http, and do NOT append to COSTS.jsonl. Keep the live path unchanged when the env var is unset. This complements isOfflineMode() (which gates SOURCE adapters): the dry-run guard gates the LLM CLIENT specifically, closing the H3 leak. No console.* (lib). The H3 RED test in 07-01 drives a verb under --dry-run and asserts zero network egress + no COSTS.jsonl append — implement until that case is GREEN.
  </action>
  <verify>
    <automated>node --import tsx --test tests/flags.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: bin/lib/runtime.ts (or the model-invocation seam it owns) checks process.env.PENSMITH_DRY_RUN==='1' and returns a stub with zero network calls + zero COSTS.jsonl append
    - behavior: the H3 case in flags.test.ts un-skips and PASSES — a verb driven under --dry-run makes zero network egress and writes no COSTS.jsonl line; the live (non-dry-run) path is unchanged
    - test-command: node --import tsx --test tests/flags.test.ts exits 0, no failures (H3 case GREEN)
  </acceptance_criteria>
  <done>The LLM client honors --dry-run via PENSMITH_DRY_RUN at the runtime seam; a verb path under --dry-run makes zero external calls and bills nothing; the H3 RED test is GREEN; the live path is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem (.paper) → router/estimator | STATE.json + PLAN.md + HANDOFF.json are read as untrusted local files; parsed via Zod safeParse |
| CLI args → process env | --dry-run mutates PENSMITH_NETWORK_TESTS / PENSMITH_DRY_RUN before any dispatch |
| --estimate path → billing ledger | estimator must never reach the LLM/network/COSTS.jsonl |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | HANDOFF.json / STATE.json parsing | mitigate | router uses HandoffSchema.safeParse + try/catch; malformed input falls through, never crashes (Pitfall 5) |
| T-07-06 | Information Disclosure | --estimate must not bill | mitigate | estimator.ts imports only pricing.ts (pure math); no runtime LLM-call path import (Pitfall 8) |
| T-07-07 | Elevation of Privilege | --yolo gate bypass (H1) | mitigate | the >50%-cap refusal is a hard process.exit(1) run in the PRE-DISPATCH seam WHENEVER --yolo is active (independent of --estimate), on the gate-skipping verbs — not advisory, not nested in --estimate (ARCH-11) |
| T-07-08 | Spoofing | --dry-run bypassing cassette gate | mitigate | --dry-run sets PENSMITH_NETWORK_TESTS='' (never '1') so isOfflineMode() stays true (Security Domain control) |
| T-07-09 | Tampering | 16-verb bijection | mitigate | loaders added only for keys already in UX02_VERBS; cli-verbs.test.ts asserts the count stays 16 |
| T-07-18 | Tampering | citty root run() double-dispatch (H2) | mitigate | flag setup + bare routing live in a pre-dispatch argv pre-parse, NOT a root run(); explicit verbs run via runMain exactly once; flags.test.ts asserts single dispatch + flags-for-explicit-verb |
| T-07-19 | Information Disclosure | --dry-run LLM network leak (H3) | mitigate | runtime.ts LLM seam short-circuits to a canned/cassette stub when PENSMITH_DRY_RUN==='1' — zero network egress + zero COSTS.jsonl; flags.test.ts drives a verb path and asserts zero egress |
| T-07-20 | Denial of Service | resume/router circularity (H4) | mitigate | resolveNextAction ignores HANDOFF and never returns 'resume'; resume dispatches to the next WORK verb and clears HANDOFF.json; pensmith-router.test.ts asserts verb !== 'resume' |
| T-07-SC | Tampering | npm installs | accept | No new packages (07-RESEARCH audit: none) |
</threat_model>

<verification>
- `node --import tsx --test tests/pensmith-router.test.ts tests/estimator.test.ts tests/flags.test.ts tests/cli-verbs.test.ts` → all GREEN, no skips on router/estimator
- `npm run check` (lint + typecheck + build + tier-contract + tests + manifests) → GREEN
- A verb path under `--dry-run` makes zero network calls and appends no COSTS.jsonl line (H3 — flags.test.ts drives it)
- Bare `node dist/bin/pensmith.js` in a fixture dir routes to the correct WORK verb even with a non-done HANDOFF present (H4 — never loops on resume); router test covers the logic
</verification>

<success_criteria>
- bin/lib/router.ts + bin/lib/estimator.ts exist and their RED tests are now GREEN; router ignores HANDOFF and never returns 'resume' (H4)
- next/status/resume promoted to real verbs in REAL_VERB_LOADERS; resume hands off to the next WORK verb and clears HANDOFF (H4)
- Four global flags declared + wired in a PRE-DISPATCH seam (not a root run()); explicit verbs run exactly once and honor the flags (H2)
- --yolo refuses over-50%-cap estimates with a hard non-zero exit WHENEVER --yolo is active, independent of --estimate (H1)
- --dry-run gates the LLM client at the runtime seam — a verb path makes zero network calls and bills nothing (H3)
- UX02_VERBS.length===16 preserved (no 17th verb)
</success_criteria>

<output>
Create `.planning/phases/07-single-command-ux-layer-hooks-flags/07-02-SUMMARY.md` when done
</output>
