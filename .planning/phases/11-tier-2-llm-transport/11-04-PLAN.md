---
phase: 11-tier-2-llm-transport
plan: 04
type: execute
wave: 2
depends_on: ["11-02"]
files_modified:
  - bin/cli/research.ts
  - bin/cli/plan.ts
  - bin/cli/revise.ts
  - bin/lib/revise-swap.ts
autonomous: true
requirements: [GEN-02, GEN-06]
nyquist_compliant: true

must_haves:
  truths:
    - "research, plan, and revise call complete() (directly or via the shared proposeSwap seam) and produce real artifacts when a key is configured"
    - "With no key configured, each of the three verbs prints a fail-loud stderr banner and exits non-zero — never ok:true with an empty library"
    - "research replaces PLACEHOLDER_LIBRARY with a real LIBRARY.json built from a defensively-parsed model response, still flowing through crossCheckRetractions → writeBibtex → writeRis"
    - "plan and revise share ONE real proposeSwap implementation; runRevise's replacement_citekey ∈ assigned_sources membership guard stays intact"
  artifacts:
    - path: "bin/lib/revise-swap.ts"
      provides: "shared real proposeSwap(vars: ReviseSwapVars) => Promise<string> via complete()"
      exports: ["proposeSwap"]
    - path: "bin/cli/research.ts"
      provides: "real research via complete() + defensive parse; fail-loud on missing key"
      contains: "complete("
    - path: "bin/cli/plan.ts"
      provides: "real PLAN.md via complete(); shared proposeSwap for --revise; fail-loud on missing key"
      contains: "complete("
  key_links:
    - from: "bin/cli/research.ts"
      to: "bin/lib/sources/retraction-cross-check.ts"
      via: "crossCheckRetractions(candidates) before writeBibtex/writeRis"
      pattern: "crossCheckRetractions"
    - from: "bin/cli/plan.ts"
      to: "bin/lib/revise-swap.ts"
      via: "shared proposeSwap import"
      pattern: "proposeSwap"
    - from: "bin/cli/revise.ts"
      to: "bin/lib/revise-swap.ts"
      via: "shared proposeSwap import"
      pattern: "proposeSwap"
---

<objective>
Wire the three remaining generative verbs — research, plan, revise — to the Tier-2 transport (GEN-02) with fail-loud on missing key (GEN-06). research replaces its PLACEHOLDER_LIBRARY with a real LIBRARY.json built from a defensively-parsed complete() response. plan and revise share a single real proposeSwap (bin/lib/revise-swap.ts) that calls complete() with the revise-swap prompt, eliminating the duplicated tier2ProposeSwap stubs; plan also writes a real PLAN.md via complete().

Purpose: Completes the six-verb GEN-02/GEN-06 wiring. After this plan, no generative verb emits a tier2-placeholder; all six call the transport or fail loud.
Output: Modified bin/cli/{research,plan,revise}.ts + new bin/lib/revise-swap.ts. Flips the research/plan/revise per-verb T-11-05 (fail-loud) and T-11-06 (offline-real) integration tests from skip to GREEN.

SCOPE FENCE (CRITICAL — do NOT exceed): For research.ts, Phase 11 = "wire the transport + remove the silent placeholder path." The FULL live-adapter candidate discovery + dedup + retraction cross-check that POPULATES LIBRARY.json from real adapters is GEN-03 / Phase 12 — do NOT build it here. Phase 11 research: call complete() with the topic-disambiguator prompt, parse the response defensively into SourceCandidate[], and flow that array (possibly empty with a WARN) through the EXISTING crossCheckRetractions → writeBibtex → writeRis chokepoints. Leave a clean seam (a comment + a single parse function) for Phase 12 to swap in live discovery. An empty candidates array with a WARN is acceptable; a placeholder LIBRARY.json with `_note` is NOT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tier-2-llm-transport/11-CONTEXT.md
@.planning/phases/11-tier-2-llm-transport/11-RESEARCH.md
@.planning/phases/11-tier-2-llm-transport/11-PATTERNS.md

<interfaces>
From bin/lib/anthropic.ts (11-02):
```typescript
export async function complete(opts: CompleteOptions): Promise<CompleteResult>;
// CompleteResult = { text: string; inputTokens: number; outputTokens: number }
```

From bin/lib/revise.ts (existing — the runRevise injection seam to satisfy):
```typescript
export interface ReviseSwapVars { flagged_citekey: string; verifier_reason: string; claim_context: string; available_sources: string; voice_hint: string }
// ReviseOptions.proposeSwap?: (vars: ReviseSwapVars) => Promise<string>;  // returns raw strict-JSON
// runRevise parses the JSON via ReviseSwapSchema and REJECTS any replacement_citekey ∉ assigned_sources (membership guard — must stay intact).
// ReviseSwapSchema = { action:'swap'|'remove'; flagged_citekey; replacement_citekey: string|null; rationale; patch:{before_excerpt, after_excerpt} }
```

From bin/lib/schemas/source-candidate.ts:
```typescript
export const SourceCandidateSchema = z.discriminatedUnion('source', [...]); // source ∈ crossref|openalex|arxiv|pubmed|semanticscholar|unpaywall|retraction-watch|zotero-mcp
// Required: id, title, authors[], last_verified(ISO), citekey(/^[a-z][a-z0-9_-]*$/), source. Optional: year, doi, abstract, oa_pdf_url, retracted(default false), retraction_details, raw.
export type SourceCandidate = z.infer<typeof SourceCandidateSchema>;
```

Existing research chokepoints (keep using):
- crossCheckRetractions(candidates) from '../lib/sources/retraction-cross-check.js' — MUST run before writeBibtex (D-15 surface-twice)
- writeBibtex(candidates, bibPath) from '../lib/bibtex-write.js' (D-19/D-20)
- writeRis(candidates, risPath) from '../lib/ris-write.js'

From bin/lib/prompt-loader.ts: loadPrompt(name), interpolate(template, vars)
D-12 LOCKED slugs: research → 'topic-disambiguator' (+ 'source-evaluator'); plan → 'section-planner'; revise/plan --revise → 'revise-swap'

Current placeholder anchors (to REPLACE):
- research.ts: PLACEHOLDER_LIBRARY const lines 27-37; placeholder write block lines 74-98 (mode:'tier2-placeholder')
- plan.ts: tier2ProposeSwap stub lines 39-47; TIER2_PLAN write lines 105-109
- revise.ts: tier2ProposeSwap stub lines 34-45; runRevise call passing proposeSwap lines 86-97

Shared fail-loud shape (11-PATTERNS §Fail-Loud): MissingApiKeyError → stderr banner naming env var + process.exitCode=1 + return ok:false. NEVER ok:true on missing key.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bin/lib/revise-swap.ts shared proposeSwap + plan.ts/revise.ts wiring</name>
  <files>bin/lib/revise-swap.ts, bin/cli/plan.ts, bin/cli/revise.ts</files>
  <read_first>
    - bin/lib/revise.ts (ReviseSwapVars lines 64-71; ReviseSwapSchema lines 52-62; ReviseOptions.proposeSwap seam lines 90-95; the membership guard in runRevise that rejects replacement_citekey ∉ assigned_sources)
    - bin/cli/revise.ts (self-analog — tier2ProposeSwap stub lines 34-45; runRevise call lines 86-97)
    - bin/cli/plan.ts (self-analog — tier2ProposeSwap stub lines 39-47; TIER2_PLAN write lines 105-109)
    - bin/lib/anthropic.ts (complete() — from 11-02)
    - bin/lib/prompt-loader.ts (loadPrompt('revise-swap'), loadPrompt('section-planner'), interpolate)
    - .planning/phases/11-tier-2-llm-transport/11-PATTERNS.md (bin/cli/plan.ts + bin/cli/revise.ts sections; shared-dedup proposeSwap opportunity)
  </read_first>
  <behavior>
    - bin/lib/revise-swap.ts exports proposeSwap(vars: ReviseSwapVars): Promise<string> that loads the 'revise-swap' prompt, interpolates vars, calls complete(), and returns the raw model text (the strict-JSON the prompt asks for). The signature matches ReviseOptions.proposeSwap exactly.
    - revise with no key: fail-loud banner + non-zero exit + ok:false BEFORE calling runRevise; no patch applied.
    - revise with key (or PENSMITH_NO_LLM=1): runRevise receives the real proposeSwap; the replacement_citekey ∈ assigned_sources membership guard still rejects out-of-set citekeys (verifier-blocks-escape preserved).
    - plan with no key: fail-loud banner + non-zero exit + ok:false; no TIER2_PLAN written.
    - plan with key (or PENSMITH_NO_LLM=1): writes a real PLAN.md from complete() with the section-planner prompt; --revise path uses the SAME shared proposeSwap.
  </behavior>
  <action>
    Create bin/lib/revise-swap.ts. Import complete from './anthropic.js', loadPrompt + interpolate from './prompt-loader.js', and `import type { ReviseSwapVars } from './revise.js'`. Export `async function proposeSwap(vars: ReviseSwapVars): Promise<string>`: `const prompt = loadPrompt('revise-swap'); const interpolated = interpolate(prompt, vars as unknown as Record<string,string>); const result = await complete({ system: <revise-swap system prompt>, messages:[{role:'user', content: interpolated}], scope:'task', scopeId: \`revise-${vars.flagged_citekey}\` }); return result.text;`. Document that the returned text is the strict-JSON the revise-swap prompt requests; runRevise owns parsing + the membership guard.

    In revise.ts: delete the local tier2ProposeSwap stub (lines 34-45). Add the shared fail-loud probe BEFORE the runRevise call: getProviderApiKey('anthropic') in try/catch → MissingApiKeyError → banner "pensmith revise: ERROR — no LLM key configured." naming ANTHROPIC_API_KEY + exitCode=1 + return ok:false. Then pass the imported `proposeSwap` (from '../lib/revise-swap.js') into the runRevise options where tier2ProposeSwap was passed. Do NOT alter runRevise's membership guard.

    In plan.ts: delete the local tier2ProposeSwap stub (lines 39-47). Add the same fail-loud probe (banner "pensmith plan: ERROR — no LLM key configured."). For the --revise path, pass the imported shared `proposeSwap`. For the normal plan path, replace the TIER2_PLAN write (lines 105-109) with: `const prompt = loadPrompt('section-planner'); const interpolated = interpolate(prompt, <vars the section-planner template declares>); const result = await complete({ system, messages:[{role:'user', content: interpolated}], scope:'section', scopeId: \`plan-${n}\` }); await atomicWriteFile(targetPath, result.text);`. Delete the TIER2_PLAN constant. NEVER log the resolved key.
  </action>
  <verify>
    <automated>node --import tsx --test tests/llm-transport.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - plan + revise per-verb T-11-05 (fail-loud) and T-11-06 (offline-real) tests flip from skip to PASS.
    - Exactly ONE proposeSwap implementation exists (grep: tier2ProposeSwap absent from BOTH plan.ts and revise.ts; both import proposeSwap from '../lib/revise-swap.js').
    - runRevise membership guard untouched — existing revise tests (replacement_citekey ∉ assigned_sources rejection, T-04-14) stay green.
    - TIER2_PLAN constant removed; no `ok:true` on the missing-key path in either verb (grep).
    - `npm run lint` + `npm run typecheck` pass; `npm test` full suite 0 failures.
  </acceptance_criteria>
  <done>Shared revise-swap.ts created; plan + revise wired + fail-loud; duplicated stubs gone; membership guard intact; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: research.ts — real complete() + defensive SourceCandidate parse (Phase-12 seam preserved)</name>
  <files>bin/cli/research.ts</files>
  <read_first>
    - bin/cli/research.ts (self-analog — PLACEHOLDER_LIBRARY lines 27-37; the full placeholder run() block lines 55-98; crossCheckRetractions → writeBibtex → writeRis ordering)
    - bin/lib/schemas/source-candidate.ts (SourceCandidateSchema required/optional fields — the parse target)
    - bin/lib/anthropic.ts (complete() — from 11-02)
    - bin/lib/prompt-loader.ts (loadPrompt('topic-disambiguator'); 'source-evaluator' also pinned)
    - .planning/phases/11-tier-2-llm-transport/11-RESEARCH.md (Pitfall 7 research artifact-shape complexity; Open Question 2 — empty array + WARN acceptable, placeholder NOT)
    - .planning/phases/11-tier-2-llm-transport/11-PATTERNS.md (bin/cli/research.ts section — defensive JSON.parse → SourceCandidate[] → existing chokepoints)
  </read_first>
  <behavior>
    - research with no key (PENSMITH_NO_LLM unset): fail-loud banner naming ANTHROPIC_API_KEY + non-zero exit + ok:false; no LIBRARY.json/.bib/.ris placeholder written.
    - research with key (or PENSMITH_NO_LLM=1): calls complete() with the topic-disambiguator prompt; defensively parses the response into SourceCandidate[]; on parse failure emits a WARN and falls back to candidates=[] (NOT a crash, NOT a placeholder _note); flows candidates through crossCheckRetractions → writeBibtex → writeRis; writes a real LIBRARY.json (NOT PLACEHOLDER_LIBRARY) whose content does NOT contain 'tier2-placeholder' or the `_note` string.
  </behavior>
  <action>
    In research.ts: delete the PLACEHOLDER_LIBRARY constant (lines 27-37). Add the shared fail-loud probe at the top of run(): getProviderApiKey('anthropic') in try/catch → MissingApiKeyError → banner "pensmith research: ERROR — no LLM key configured." + exitCode=1 + return ok:false. Then: `const prompt = loadPrompt('topic-disambiguator'); const interpolated = interpolate(prompt, <vars the topic-disambiguator template declares>); const result = await complete({ system, messages:[{role:'user', content: interpolated}], scope:'task', scopeId:'research' })`.

    Add a single defensive parse function (the Phase-12 seam — comment it as "Phase 12 / GEN-03 replaces this with live-adapter discovery"): try JSON.parse(result.text) → expect an array → validate each element with SourceCandidateSchema.safeParse, collecting only the successes; on any throw or non-array, `process.stderr.write` a WARN and set `candidates = []`. Then run the EXISTING ordering unchanged: `await crossCheckRetractions(candidates)` (BEFORE writeBibtex — D-15), `await writeBibtex(candidates, bibPath)`, `await writeRis(candidates, risPath)`. Build a real LIBRARY.json: `JSON.stringify({ $schemaVersion: 1, entries: candidates }, null, 2)` (NO `_note`, NO 'tier2-placeholder') and atomicWriteFile it. Return `{ ok:true, library, bib, ris }` WITHOUT `mode:'tier2-placeholder'`.

    Keep loadPrompt's hash-pin defense-in-depth (both topic-disambiguator and source-evaluator stay pinned at module load). Do NOT implement live adapter discovery (Phase 12). NEVER log the resolved key.
  </action>
  <verify>
    <automated>node --import tsx --test tests/llm-transport.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - research per-verb T-11-05 (fail-loud) and T-11-06 (offline-real) tests flip from skip to PASS.
    - PLACEHOLDER_LIBRARY constant removed; written LIBRARY.json contains neither 'tier2-placeholder' nor the `_note` string (grep the offline-run output artifact).
    - crossCheckRetractions still runs BEFORE writeBibtex (grep ordering preserved — D-15).
    - A malformed model response yields a WARN + empty candidates (not a thrown crash) — covered by an offline/parse-fallback test path.
    - `npm run lint` + `npm run typecheck` pass; `npm test` full suite 0 failures; `npm run test:tier-contract` green.
    - The defensive-parse function carries a comment marking it as the Phase-12 / GEN-03 swap seam.
  </acceptance_criteria>
  <done>research wired to complete() with defensive parse; real LIBRARY.json; fail-loud; D-15 ordering preserved; Phase-12 seam documented; suite + tier-contract green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM response → SourceCandidate[] (research) | the model's JSON is untrusted; safeParse + WARN-fallback is the boundary enforcer (Pitfall 7) |
| LLM proposeSwap response → DRAFT.md patch (revise) | the model's replacement_citekey is untrusted; runRevise's ∈ assigned_sources membership guard is the boundary enforcer |
| missing-key state → CLI exit | a key-less run must surface as non-zero exit, never a silent ok:true empty library |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09 | Tampering | LLM injecting a citekey not in assigned_sources via revise/plan --revise | mitigate | runRevise's existing membership guard (rejects replacement_citekey ∉ assigned_sources) is preserved unchanged; the shared proposeSwap only returns raw text — runRevise owns parse + guard. Verified by the existing T-04-14 revise tests staying green. |
| T-11-10 | Tampering | Malformed/hostile LLM JSON crashing research or injecting bad library entries | mitigate | research parses with SourceCandidateSchema.safeParse per element + array-shape guard; any failure → WARN + empty candidates, never a crash or unvalidated entry (Pitfall 7). |
| T-11-11 | Repudiation | Silent ok:true with empty library on missing key (GEN-06 core regression) | mitigate | every no-key path returns ok:false + process.exitCode=1; research no longer writes a placeholder LIBRARY.json with `_note`; grep asserts no ok:true on the missing-key branch. |
| T-11-12 | Information Disclosure | API-key value leaking via verb-level logging | mitigate | getProviderApiKey is a presence probe only; complete() owns the no-leak header path; no verb binds the key to a logged variable (T-01-07). |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No installs in Phase 11 (11-RESEARCH §Package Legitimacy Audit). |
</threat_model>

<verification>
- `node --import tsx --test tests/llm-transport.test.ts` → research/plan/revise per-verb T-11-05 + T-11-06 GREEN.
- `npm run lint` + `npm run typecheck` → clean (no key logged; placeholders removed; one proposeSwap).
- `npm test` + `npm run test:tier-contract` → green (incl. T-04-14 membership guard regression).
</verification>

<success_criteria>
- research, plan, revise produce real artifacts via complete() when a key is configured; no tier2-placeholder / _note output.
- Each fails loud (banner + non-zero exit + ok:false) when no key is configured.
- One shared proposeSwap; runRevise membership guard intact; research defensive parse + Phase-12 seam preserved.
</success_criteria>

<output>
Create `.planning/phases/11-tier-2-llm-transport/11-04-SUMMARY.md` when done.
</output>
