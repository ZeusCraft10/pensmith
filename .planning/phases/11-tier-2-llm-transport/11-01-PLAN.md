---
phase: 11-tier-2-llm-transport
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/llm-transport.test.ts
autonomous: true
requirements: [GEN-01, GEN-02, GEN-06]
nyquist_compliant: true

must_haves:
  truths:
    - "The full test suite stays GREEN (skips, not failures) before bin/lib/anthropic.ts exists"
    - "A single test file enumerates every Phase-11 behavioral contract (chokepoint, no-leak, budget-gate, fail-loud, offline seam, provider request shapes)"
    - "Each behavioral test skips via a feature-detect guard on the unbuilt module/export, never a hard import failure"
  artifacts:
    - path: "tests/llm-transport.test.ts"
      provides: "RED-by-skip scaffold covering T-11-01..T-11-08"
      contains: "isNoLlmMode"
      min_lines: 80
  key_links:
    - from: "tests/llm-transport.test.ts"
      to: "bin/lib/anthropic.js"
      via: "dynamic import via URL .href specifier + symbol feature-detect"
      pattern: "await import\\("
---

<objective>
Land the Wave-0 RED-by-skip test scaffold for the Tier-2 LLM transport. This file enumerates every Phase-11 behavioral contract (transport chokepoint, key no-leak, pre-call budget gate, fail-loud-on-missing-key, PENSMITH_NO_LLM offline seam, and provider request-body shapes) as tests that SKIP — not fail — until `bin/lib/anthropic.ts` and the wired verbs land in Waves 1-2.

Purpose: Establish the executable contract first so Waves 1-2 implement against a fixed target, and so the full suite stays GREEN (skips, zero failures) throughout the phase per the locked Wave-0 RED-by-skip convention (05-01 / 06-01 / 08-00 / 10-00 precedent).
Output: `tests/llm-transport.test.ts` (RED-by-skip; flips to real assertions as Waves 1-2 land).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tier-2-llm-transport/11-CONTEXT.md
@.planning/phases/11-tier-2-llm-transport/11-RESEARCH.md
@.planning/phases/11-tier-2-llm-transport/11-PATTERNS.md
@.planning/phases/11-tier-2-llm-transport/11-VALIDATION.md

<interfaces>
<!-- Contracts the scaffold must reference. The transport (Wave 1) implements these. -->

The transport public surface that Wave 1 will export (assert these shapes in skip-guarded tests):
- `export async function complete(opts: CompleteOptions): Promise<CompleteResult>`
- `export function isNoLlmMode(): boolean`  // returns process.env['PENSMITH_NO_LLM'] === '1'
- `CompleteOptions = { system: string; messages: Array<{role:'user'|'assistant'; content:string}>; model?: string; maxTokens?: number; scopeCapUsd?: number; scope?: 'paper'|'section'|'task'; scopeId?: string }`
- `CompleteResult = { text: string; inputTokens: number; outputTokens: number }`

Existing helpers the tests reuse (already shipped):
- From bin/lib/budget.ts: `assertBudget(spec, estimateUsd)`, `appendCost(record)`, `BudgetSpec = { scope:'paper'|'section'|'task'; scopeId:string; cap:number }`, `BudgetExceededError`
- From bin/lib/runtime.ts: `getProviderApiKey(providerId, opts?)`, `MissingApiKeyError`, `loadRuntimeConfig(opts?)`
- From bin/lib/pricing.ts: `estimateCost({providerId, modelId, inputTokens, outputTokens})`; known models: anthropic/claude-haiku-4, claude-sonnet-4, claude-opus-4; openai/gpt-4o, gpt-4o-mini, gpt-5
- From bin/lib/http.ts: `fetch(url, opts)` where opts allows `method:'POST'`, `headers`, `body:string`, `source:'generic'`, `noCache:true`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED-by-skip transport scaffold (chokepoint + no-leak + budget-gate + offline seam)</name>
  <files>tests/llm-transport.test.ts</files>
  <read_first>
    - tests/llm-transport.test.ts (the file being created — confirm it does not yet exist)
    - tests/http-cache-no-header-leak.test.ts (PRIMARY analog — withFreshState helper lines 35-62; no-leak sentinel assertion lines 87-146; MockAgent setup)
    - tests/runtime.test.ts (dynamic-import-after-env-reset pattern lines 52-64)
    - tests/budget.test.ts (withProjectRoot chdir + .paper mkdir helper lines 31-45)
    - .planning/phases/11-tier-2-llm-transport/11-PATTERNS.md (tests/llm-transport.test.ts section — every assertion pattern + helper shape spelled out)
    - .planning/phases/11-tier-2-llm-transport/11-RESEARCH.md (§Wave 0 Gaps T-11-01..T-11-08)
  </read_first>
  <behavior>
    - T-11-01: under PENSMITH_NO_LLM=1, `complete()` returns deterministic offline mock text with inputTokens=0/outputTokens=0 and issues NO HTTP request (MockAgent.disableNetConnect must not be hit).
    - T-11-02: with a key set and PENSMITH_NO_LLM unset, `complete()` calls `assertBudget` BEFORE any http.ts request — assert by seeding COSTS.jsonl over the cap and expecting BudgetExceededError with no MockAgent interception fired.
    - T-11-03 (no-leak, mirrors T-01-07): set ANTHROPIC_API_KEY to a unique sentinel, drive `complete()` through a MockAgent that records the request, then assert the sentinel value NEVER appears in any session-log file, in stdout/stderr capture, or in any .paper/COSTS.jsonl line. The sentinel MAY appear only inside the recorded request headers.
    - isNoLlmMode(): returns true iff process.env['PENSMITH_NO_LLM']==='1'.
  </behavior>
  <action>
    Create tests/llm-transport.test.ts using node:test + node:assert/strict. Import the target module via a runtime URL .href dynamic-import specifier (the 08-00 / 10-00 pattern: `await import(new URL('../bin/lib/anthropic.js', import.meta.url).href)`) so tsc --noEmit stays clean while the module is absent. Declare a local TypeScript interface for the imported surface so types resolve without the real module.

    Add a `transportReady()` skip predicate that feature-detects via symbol probe: dynamic-import in a try/catch, then `typeof mod.complete === 'function' && typeof mod.isNoLlmMode === 'function'`; return false on import throw. Every behavioral test begins with `if (!(await transportReady())) { t.skip('anthropic.ts not yet built'); return; }` using the test context `t.skip` (or node:test `{skip:true}` option computed before run) — choose the node:test skip mechanism that yields a SKIP count, NOT a failure, in `node scripts/run-tests.mjs` (which has no expected-RED allowlist — see decision [10-00]).

    Port `withFreshState` from the no-header-leak analog and EXTEND it to also save/restore PENSMITH_NO_LLM, ANTHROPIC_API_KEY, OPENAI_API_KEY, and to chdir into a tmp `.paper/`-containing root (merge the budget.ts withProjectRoot chdir behavior) so assertBudget/appendCost resolve into the tmpdir COSTS.jsonl. Install/teardown a undici MockAgent (disableNetConnect) in try/finally; restore the saved global dispatcher.

    Write T-11-01, T-11-02, T-11-03, and an isNoLlmMode unit test as skip-guarded tests per the <behavior> block. For the no-leak assertion, read every file under the tmp session-log dir and assert the sentinel substring is absent (reuse the no-header-leak analog's disk-sweep loop). Do NOT inline the transport implementation — these tests only describe behavior.
  </action>
  <verify>
    <automated>node --import tsx --test tests/llm-transport.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --import tsx --test tests/llm-transport.test.ts` reports SKIPs and 0 failures (module absent → all behavioral tests skip).
    - `npm run typecheck` passes (URL .href dynamic import + local interface keeps tsc clean).
    - The file contains an `isNoLlmMode` reference and a `transportReady()` feature-detect guard (grep: `transportReady` present; `await import(` present).
    - No `import { complete } from '../bin/lib/anthropic.js'` static top-level import exists (would break tsc while module is absent) — assert via grep that the only reference is the dynamic URL.href import.
  </acceptance_criteria>
  <done>tests/llm-transport.test.ts exists; quick command shows skips + zero failures; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED-by-skip fail-loud + provider request-shape + per-verb integration scaffolds</name>
  <files>tests/llm-transport.test.ts</files>
  <read_first>
    - tests/llm-transport.test.ts (the file from Task 1 — extend it)
    - tests/http.test.ts (MockAgent intercept + request-body capture pattern lines 54-80)
    - tests/tier-contract.test.ts (how the CLI verbs are spawned with no key; confirm cases A-D do NOT exercise generative verb output directly — Pitfall 6)
    - bin/cli/intake.ts, bin/cli/outline.ts, bin/cli/research.ts, bin/cli/plan.ts, bin/cli/write.ts, bin/cli/revise.ts (the six verbs whose post-wiring behavior these tests assert; read the current placeholder return shapes)
    - .planning/phases/11-tier-2-llm-transport/11-RESEARCH.md (§Critical API Shapes — anthropic vs openai request body/header shapes; T-11-04..T-11-08)
  </read_first>
  <behavior>
    - T-11-04: getProviderApiKey throwing MissingApiKeyError propagates out of `complete()` (with PENSMITH_NO_LLM unset, no key set) — assert `complete()` rejects with MissingApiKeyError.
    - T-11-07: anthropic provider → POST to api.anthropic.com/v1/messages with body { model, max_tokens, system, messages } and headers x-api-key + anthropic-version: 2023-06-01 + content-type; MockAgent captures and asserts the body shape.
    - T-11-08: openai provider → POST to api.openai.com/v1/chat/completions with body { model, max_tokens, messages:[{role:'system'},...] } and authorization: Bearer header; MockAgent captures and asserts the body shape.
    - T-11-05 (integration, per verb): spawning each of the six CLI verbs with NO key configured and PENSMITH_NO_LLM unset exits non-zero and writes a banner to stderr; the verb never returns/prints `ok:true` and never writes a `tier2-placeholder` artifact.
    - T-11-06 (integration, per verb): running each verb under PENSMITH_NO_LLM=1 writes a NON-placeholder artifact (offline mock counts as real for CI; the `mode:'tier2-placeholder'` string must be absent from output and artifact).
  </behavior>
  <action>
    Extend tests/llm-transport.test.ts with: (a) T-11-04 skip-guarded unit test asserting MissingApiKeyError propagation; (b) T-11-07 / T-11-08 skip-guarded MockAgent request-body-shape tests using the http.test.ts intercept-and-capture pattern (intercept the provider origin, capture the POST body, JSON.parse it, assert the per-provider field set + header set per 11-RESEARCH §Critical API Shapes). Drive provider selection by writing a runtime config under the tmp HOME with a `defaultModel` set (anthropic/claude-haiku-4 for T-11-07; openai/gpt-4o for T-11-08) — the runtime-config provider schema is `{ name, apiKeyEnv, defaultModel? }`.

    Add the six per-verb integration tests (T-11-05 fail-loud, T-11-06 offline-real) as skip-guarded blocks. Guard them on `transportReady()` AND on a per-verb source-grep predicate (e.g. the verb file no longer contains its TIER2_* placeholder constant) so they skip cleanly until Wave 2 wires that verb — mirror the 07-01 source-grep skip-predicate pattern (existsSync alone cannot detect not-yet-wired behavior). Spawn the built CLI via `node --import tsx` with `import.meta.resolve('tsx')` absolute specifier (decision [07-01]: a bare `--import tsx` resolves against the child tmpdir cwd and crashes). For T-11-05, clear ANTHROPIC_API_KEY/OPENAI_API_KEY and PENSMITH_NO_LLM in the child env and assert exit code !== 0 + stderr banner. For T-11-06, set PENSMITH_NO_LLM=1 and assert the written artifact does NOT contain `tier2-placeholder`.

    Keep every test skip-guarded — zero failures before Wave 1/2 land. Do NOT implement any transport or verb code here.
  </action>
  <verify>
    <automated>node --import tsx --test tests/llm-transport.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --import tsx --test tests/llm-transport.test.ts` reports SKIPs and 0 failures.
    - `npm test` (full suite) passes with 0 failures (new skips only) — confirms the scaffold did not break any existing test.
    - `npm run test:tier-contract` stays green (the scaffold is additive; tier-contract unaffected).
    - The file references all six verb names and both provider endpoints (grep: `api.anthropic.com` and `api.openai.com` present; six `spawn`/child-invocation sites present).
    - Each per-verb integration test is guarded by BOTH transportReady() and a per-verb source-grep predicate (grep: a wiredFor-style predicate per verb).
  </acceptance_criteria>
  <done>All T-11-04..T-11-08 + six per-verb integration tests present and skip-guarded; full suite + tier-contract green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → real provider endpoints | The scaffold MUST NOT make a live network call; MockAgent.disableNetConnect is the boundary enforcer |
| env var (API key sentinel) → disk/log | The no-leak test asserts the sentinel never crosses to any persisted file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | API-key value reaching session log / stdout / COSTS.jsonl | mitigate | T-11-03 no-leak test sweeps every tmp session-log file + stdout/stderr capture for the sentinel value; this is the load-bearing Wave-0 assertion that Wave 1 must satisfy |
| T-11-02 | Denial of Service | Budget bypass (LLM call skipping assertBudget) | mitigate | T-11-02 seeds COSTS.jsonl over cap and asserts BudgetExceededError fires with NO MockAgent interception — proves assertBudget precedes the network call |
| T-11-03 | Tampering | Prompt-injection via untrusted content reaching the transport | accept (this plan) | Scaffold only; the caller-side redaction + delimiting assertion is exercised in Wave-2 verb wiring (11-03/11-04) and hardened in Phase 15 (HARD-04). No untrusted content flows through a test scaffold |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Phase 11 — 11-RESEARCH §Package Legitimacy Audit confirms all deps pre-existing; no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- `node --import tsx --test tests/llm-transport.test.ts` → skips + 0 failures.
- `npm run typecheck` → clean (URL.href dynamic import keeps tsc green while module absent).
- `npm test` → full suite, 0 failures (additive skips only).
- `npm run test:tier-contract` → green.
</verification>

<success_criteria>
- A single test file enumerates T-11-01..T-11-08 plus six per-verb integration tests, all skip-guarded.
- Suite stays GREEN (skips, never failures) before bin/lib/anthropic.ts exists.
- No live network call possible (MockAgent.disableNetConnect) and no static import of the unbuilt module.
</success_criteria>

<output>
Create `.planning/phases/11-tier-2-llm-transport/11-01-SUMMARY.md` when done.
</output>
